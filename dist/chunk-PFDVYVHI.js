import {
  deriveDisclosures
} from "./chunk-GCZJRQIF.js";
import {
  DistributionDerivationError,
  MATRIX_AXIS_ORDER,
  MatrixDerivationError,
  deriveAdjacencyMatrix,
  deriveDegreeDistribution,
  deriveDelayDistribution,
  deriveDelayMatrix,
  deriveIsiFromEvents,
  deriveIsiFromIntervals,
  derivePopulationRateCounts,
  deriveWeightDistribution,
  deriveWeightMatrix,
  validateArtifactStructure
} from "./chunk-TEVJHERV.js";
import {
  CATEGORICAL_SERIES_STYLES,
  RESPONSE_EVENT_MEMBERSHIP_CANONICALIZATION_ID,
  SKILL_CATALOG,
  THEMES,
  UNCERTAINTY_STYLES_BY_KIND,
  axesAreCompatible,
  binary64RelativeDifferenceWithinEpsilons,
  canonicalUnitFor,
  compareExactUnitArraySumToDifference,
  compareExactUnitSumToValue,
  conversionFactor,
  conversionReceipt,
  convert,
  convertDifference,
  convertExactUnitSum,
  deriveExactAggregateCountRateInUnit,
  deriveExactAggregateCountRateOverIntervalsInUnit,
  deriveExactCountRateInUnit,
  deriveExactCountRateMinusAggregateRateOverIntervalsInUnit,
  deriveExactCountRateWithIntegerFactorsInUnit,
  dimensionOf,
  divideExactIntegerByConvertedDifference,
  exactBinary64AffineFraction,
  exactBinary64DivideByIntegerProduct,
  exactBinary64EmpiricalQuantileType7,
  exactBinary64Mean,
  exactBinary64RatioToDifference,
  exactBinary64RatioToMean,
  exactBinary64SampleStandardDeviation,
  exactBinary64Sum,
  exactBinary64SumUnits,
  exactBinary64WeightedMean,
  exactBinary64WeightedSum,
  exactRationalToBinary64,
  finiteBinary64ToMinSubnormalUnits,
  floorExactBinary64TimesSafeInteger,
  isKnownUnit,
  isRoundedMeanOfSafeNonnegativeIntegers,
  materializeCenteredLagBins,
  materializeWidthBins,
  reciprocalUnit,
  responseEventMembershipDigest,
  roundedBinary64Mean,
  unitLabel,
  verifyBinnedPeakValueLattice,
  verifyPeakBasisAgainstWindow,
  verifyResponseEventScope,
  verifyResponseRateAuthority
} from "./chunk-6TQKFRP5.js";
import {
  ARTIFACT_CONTRACT,
  ARTIFACT_CONTRACT_IDENTITY,
  BUDGET_PROFILES,
  DEFAULT_PROFILE,
  canonicalDigest,
  canonicalDigestExcluding,
  canonicalize,
  deepFreeze,
  getBuildIdentity,
  makeError,
  sha256Digest,
  tryGetBudgetLimits,
  trySelectTighterBudgetProfile,
  utf8ByteLength
} from "./chunk-22OHKNZ5.js";

// src/render/buildFigure.ts
import {
  isValidatedRequest as isValidatedRequest2,
  parseAndValidateRequest,
  validateRequestValue
} from "#cortexel-request-capability";

// src/analysis/bins.ts
function edgesFromWidth(start, stop, width) {
  const result = materializeWidthBins(start, stop, width);
  if (!result.ok) throw new Error(`invalid width-mode bin specification (${result.reason})`);
  return [...result.edges];
}
function tryEdgesFromWidth(start, stop, width) {
  const result = materializeWidthBins(start, stop, width);
  return result.ok ? [...result.edges] : void 0;
}
function binIndex(value, bins) {
  const { edges, finalEdgeInclusive: finalEdgeInclusive2 } = bins;
  const n = edges.length - 1;
  if (!Number.isFinite(value) || n < 1) return -1;
  if (value < edges[0]) return -1;
  if (value > edges[n]) return -1;
  if (value === edges[n]) return finalEdgeInclusive2 ? n - 1 : -1;
  let lo = 0;
  let hi = n;
  while (lo < hi) {
    const mid = lo + hi + 1 >> 1;
    if (edges[mid] <= value) lo = mid;
    else hi = mid - 1;
  }
  return lo;
}
function binCenters(bins) {
  const centers = [];
  for (let i = 0; i < bins.edges.length - 1; i++) {
    centers.push(roundedBinary64Mean([bins.edges[i], bins.edges[i + 1]]));
  }
  return centers;
}

// src/analysis/psth.ts
var PsthDerivationError = class extends Error {
  code;
  instancePath;
  constructor(code, instancePath, message) {
    super(message);
    this.name = "PsthDerivationError";
    this.code = code;
    this.instancePath = instancePath;
  }
};
var PSTH_VALUE_TOLERANCE = 1e-12;
var PSTH_MAX_EVENTS = 2e6;
var PSTH_MAX_BINS = 1e5;
var PSTH_MAX_IDENTITIES = 1e5;
function assertPublicInputShape(input) {
  const candidate = input;
  if (!candidate || typeof candidate !== "object") {
    throw new PsthDerivationError(
      "SCIENCE_NORMALIZATION_UNVERIFIABLE",
      "",
      "PSTH derivation requires an object input."
    );
  }
  if (candidate.mode !== "events" && candidate.mode !== "prebinned") {
    throw new PsthDerivationError(
      "SCIENCE_NORMALIZATION_UNVERIFIABLE",
      "/data/mode",
      `PSTH mode must be events or prebinned; got ${String(candidate.mode)}.`
    );
  }
  if (![
    "count",
    "count_per_trial",
    "total_event_rate_per_trial",
    "mean_rate_per_selected_sender_per_trial"
  ].includes(String(candidate.normalization))) {
    throw new PsthDerivationError(
      "SCIENCE_NORMALIZATION_UNVERIFIABLE",
      "/parameters/normalization",
      `unknown PSTH normalization ${String(candidate.normalization)}.`
    );
  }
  if (candidate.denominatorPolicy !== "uniform_trial_count" && candidate.denominatorPolicy !== "per_bin_covering_trials") {
    throw new PsthDerivationError(
      "SCIENCE_DENOMINATOR_INVALID",
      "/parameters/denominatorPolicy",
      `unknown PSTH denominator policy ${String(candidate.denominatorPolicy)}.`
    );
  }
  if (candidate.senderExposurePolicy !== void 0 && candidate.senderExposurePolicy !== "all_selected_senders_cover_every_counted_trial_bin") {
    throw new PsthDerivationError(
      "SCIENCE_DENOMINATOR_INVALID",
      "/parameters/senderExposurePolicy",
      `unknown PSTH sender-exposure policy ${String(candidate.senderExposurePolicy)}.`
    );
  }
  const baseline = candidate.baseline;
  if (baseline !== void 0 && baseline?.mode !== "subtract_mean_rate") {
    throw new PsthDerivationError(
      "SCIENCE_NORMALIZATION_UNVERIFIABLE",
      "/parameters/baseline/mode",
      `unknown PSTH baseline mode ${String(baseline?.mode)}.`
    );
  }
}
function assertObservationBudgets(input) {
  const binCount = input.bins.edges.length - 1;
  if (binCount > PSTH_MAX_BINS) {
    throw new PsthDerivationError(
      "RESOURCE_OBSERVATIONS_EXCEEDED",
      "/parameters/bins",
      `PSTH derivation materializes at most ${PSTH_MAX_BINS} bins; got ${binCount}.`
    );
  }
  if (input.trialIds.length > PSTH_MAX_IDENTITIES) {
    throw new PsthDerivationError(
      "RESOURCE_OBSERVATIONS_EXCEEDED",
      "/data/trialIds",
      `PSTH derivation accepts at most ${PSTH_MAX_IDENTITIES} trial identities.`
    );
  }
  if (input.mode === "events") {
    if (input.eventTimes.length > PSTH_MAX_EVENTS) {
      throw new PsthDerivationError(
        "RESOURCE_OBSERVATIONS_EXCEEDED",
        "/data/eventTimes/values",
        `PSTH derivation accepts at most ${PSTH_MAX_EVENTS} source events.`
      );
    }
    if (input.recordedSenderIds.length > PSTH_MAX_IDENTITIES) {
      throw new PsthDerivationError(
        "RESOURCE_OBSERVATIONS_EXCEEDED",
        "/data/recordedSenderIds",
        `PSTH derivation accepts at most ${PSTH_MAX_IDENTITIES} selected-sender identities.`
      );
    }
  } else if (input.counts.length > PSTH_MAX_BINS || input.trialDenominators.length > PSTH_MAX_BINS || (input.suppliedValues?.values.length ?? 0) > PSTH_MAX_BINS) {
    throw new PsthDerivationError(
      "RESOURCE_OBSERVATIONS_EXCEEDED",
      "/data/counts",
      `prebinned PSTH arrays accept at most ${PSTH_MAX_BINS} entries.`
    );
  }
}
function endpointMatchesCanonicalTypedConversion(edge, edgeUnit, windowEndpoint, windowUnit) {
  const converted2 = convert(windowEndpoint, windowUnit, edgeUnit);
  return edge === converted2;
}
function assertBinsTileWindow(input) {
  const { edges, unit } = input.bins;
  if (edges.length < 2) {
    throw new PsthDerivationError(
      "SCIENCE_BIN_EDGES_INVALID",
      "/parameters/bins",
      "a PSTH needs at least two authoritative bin edges."
    );
  }
  for (let index = 0; index < edges.length; index++) {
    if (!Number.isFinite(edges[index])) {
      throw new PsthDerivationError(
        "SCIENCE_BIN_EDGES_INVALID",
        `/parameters/bins/edges/${index}`,
        `every authoritative PSTH bin edge must be finite; edge ${index} is ${edges[index]}.`
      );
    }
    if (index > 0 && !(edges[index] > edges[index - 1])) {
      throw new PsthDerivationError(
        "SCIENCE_BIN_EDGES_INVALID",
        `/parameters/bins/edges/${index}`,
        `authoritative PSTH bin edges must be strictly increasing; edge ${index - 1} is ${edges[index - 1]} and edge ${index} is ${edges[index]}.`
      );
    }
  }
  if (input.relativeWindow.boundary !== "[start,stop)" && input.relativeWindow.boundary !== "[start,stop]") {
    throw new PsthDerivationError(
      "SCIENCE_BIN_EDGES_INVALID",
      "/data/relativeWindow/boundary",
      "a PSTH relative-window boundary must be explicitly declared as [start,stop) or [start,stop]."
    );
  }
  if (input.bins.boundary !== "[lo,hi)" && input.bins.boundary !== "[lo,hi]") {
    throw new PsthDerivationError(
      "SCIENCE_BIN_EDGES_INVALID",
      "/parameters/bins/boundary",
      "a PSTH bin boundary must be explicitly declared as [lo,hi) or [lo,hi]."
    );
  }
  const closedWindow = input.relativeWindow.boundary === "[start,stop]";
  const expectedBinBoundary = closedWindow ? "[lo,hi]" : "[lo,hi)";
  if (input.bins.boundary !== expectedBinBoundary || input.bins.finalEdgeInclusive !== closedWindow) {
    throw new PsthDerivationError(
      "SCIENCE_BIN_EDGES_INVALID",
      "/parameters/bins",
      `PSTH membership and placement boundaries must agree: relative-window boundary ${input.relativeWindow.boundary} requires bin boundary ${expectedBinBoundary} and finalEdgeInclusive ${closedWindow}.`
    );
  }
  let extent;
  try {
    extent = convertDifference(
      input.relativeWindow.start,
      input.relativeWindow.stop,
      input.relativeWindow.unit,
      unit
    );
  } catch (error) {
    throw new PsthDerivationError(
      "SCIENCE_NUMERIC_RESOLUTION_UNREPRESENTABLE",
      "/data/relativeWindow",
      `the typed relative-window extent cannot be represented in the bin unit (${error instanceof Error ? error.message : "numeric conversion failed"}).`
    );
  }
  const startMatches = endpointMatchesCanonicalTypedConversion(
    edges[0],
    unit,
    input.relativeWindow.start,
    input.relativeWindow.unit
  );
  const stopMatches = endpointMatchesCanonicalTypedConversion(
    edges[edges.length - 1],
    unit,
    input.relativeWindow.stop,
    input.relativeWindow.unit
  );
  if (!startMatches || !stopMatches) {
    throw new PsthDerivationError(
      "SCIENCE_BIN_EDGES_INVALID",
      "/parameters/bins",
      `PSTH bins must tile the typed relative window exactly after each window endpoint is correctly rounded once into the bin unit (${extent} ${unit} extent).`
    );
  }
}
function assertTimeUnit(unit, path) {
  if (dimensionOf(unit) !== "time") {
    throw new PsthDerivationError(
      "SCIENCE_UNIT_DIMENSION_MISMATCH",
      path,
      `PSTH time coordinates require a registered time unit; got ${unit}.`
    );
  }
}
function assertUniqueIds(ids, path, label) {
  const seen = /* @__PURE__ */ new Set();
  for (let index = 0; index < ids.length; index++) {
    if (seen.has(ids[index])) {
      throw new PsthDerivationError(
        "SCIENCE_DENOMINATOR_INVALID",
        `${path}/${index}`,
        `the ${label} must contain unique ids; ${ids[index]} appears more than once.`
      );
    }
    seen.add(ids[index]);
  }
}
function assertFiniteValues(values, path, label) {
  for (let index = 0; index < values.length; index++) {
    if (!Number.isFinite(values[index])) {
      throw new PsthDerivationError(
        "SCIENCE_NUMERIC_RESOLUTION_UNREPRESENTABLE",
        `${path}/${index}`,
        `every ${label} must be finite; entry ${index} is ${values[index]}.`
      );
    }
  }
}
function isRateNormalization(normalization) {
  return normalization === "total_event_rate_per_trial" || normalization === "mean_rate_per_selected_sender_per_trial";
}
function expectedValueKind(normalization) {
  if (normalization === "count") return "count";
  if (normalization === "count_per_trial") return "ratio";
  return "firing_rate";
}
function validateSafeCount(value, path) {
  if (!Number.isSafeInteger(value) || value < 0) {
    throw new PsthDerivationError(
      "SCIENCE_COUNT_NOT_INTEGER",
      path,
      `a PSTH count must be an exact non-negative safe integer; got ${value}.`
    );
  }
}
function validatePositiveDenominator(value, path) {
  if (!Number.isSafeInteger(value) || value < 1) {
    throw new PsthDerivationError(
      "SCIENCE_DENOMINATOR_INVALID",
      path,
      `a PSTH denominator must be a positive safe integer; got ${value}.`
    );
  }
}
function exactDifferenceWithinScaleTolerance(actual, expected, scale) {
  if (!Number.isFinite(actual) || !Number.isFinite(expected) || !Number.isFinite(scale) || scale < 0) {
    return false;
  }
  const actualUnits = finiteBinary64ToMinSubnormalUnits(actual);
  const expectedUnits = finiteBinary64ToMinSubnormalUnits(expected);
  const scaleUnits = finiteBinary64ToMinSubnormalUnits(scale);
  const toleranceUnits = finiteBinary64ToMinSubnormalUnits(PSTH_VALUE_TOLERANCE);
  const difference = actualUnits >= expectedUnits ? actualUnits - expectedUnits : expectedUnits - actualUnits;
  if (scaleUnits === 0n) return difference === 0n;
  return difference << 1074n <= scaleUnits * toleranceUnits;
}
function exactCountTotal(counts) {
  return counts.reduce(
    (sum, count) => sum + (count === null ? 0n : BigInt(count)),
    0n
  );
}
function deriveNormalizedValues(counts, denominators, edges, binUnit, selectedSenderCount, normalization, valueUnit) {
  return counts.map((count, index) => {
    const denominator = denominators[index];
    if (count === null || denominator === null) return null;
    if (normalization === "count") return count;
    if (normalization === "count_per_trial") {
      return exactRationalToBinary64(BigInt(count), BigInt(denominator));
    }
    const factors = normalization === "mean_rate_per_selected_sender_per_trial" ? [denominator, selectedSenderCount] : [denominator];
    return deriveExactCountRateWithIntegerFactorsInUnit(
      count,
      factors,
      edges[index],
      edges[index + 1],
      binUnit,
      valueUnit
    );
  });
}
function matchingCanonicalEdgeIndices(edges, edgeUnit, value, valueUnit) {
  const matches = [];
  for (let index = 0; index < edges.length; index++) {
    if (endpointMatchesCanonicalTypedConversion(edges[index], edgeUnit, value, valueUnit)) {
      matches.push(index);
    }
  }
  return matches;
}
function deriveBaseline(input, counts, denominators, selectedSenderCount, valueUnit) {
  if (!input.baseline) {
    return {
      rate: null,
      corrected: counts.map(() => null),
      startIndex: null,
      stopIndex: null,
      exactBaselineCount: null
    };
  }
  if (!isRateNormalization(input.normalization)) {
    throw new PsthDerivationError(
      "SCIENCE_NORMALIZATION_UNVERIFIABLE",
      "/parameters/baseline",
      "PSTH baseline subtraction is defined only for a rate normalization."
    );
  }
  if (!(input.baseline.stop > input.baseline.start)) {
    throw new PsthDerivationError(
      "SCIENCE_BIN_EDGES_INVALID",
      "/parameters/baseline/stop",
      "the PSTH baseline interval must have strictly positive width."
    );
  }
  const startMatches = matchingCanonicalEdgeIndices(
    input.bins.edges,
    input.bins.unit,
    input.baseline.start,
    input.relativeWindow.unit
  );
  const stopMatches = matchingCanonicalEdgeIndices(
    input.bins.edges,
    input.bins.unit,
    input.baseline.stop,
    input.relativeWindow.unit
  );
  const startIndex = startMatches.length === 1 ? startMatches[0] : -1;
  const stopIndex = stopMatches.length === 1 ? stopMatches[0] : -1;
  if (startIndex < 0 || stopIndex <= startIndex) {
    throw new PsthDerivationError(
      "SCIENCE_BIN_EDGES_INVALID",
      "/parameters/baseline",
      "each PSTH baseline bound must identify exactly one authoritative edge after one correctly rounded typed conversion into the bin unit; no match, an ambiguous match, or reversed edges is invalid."
    );
  }
  let baselineCount = 0n;
  const intervals = [];
  for (let index = startIndex; index < stopIndex; index++) {
    const count = counts[index];
    const denominator = denominators[index];
    if (count === null || denominator === null) continue;
    baselineCount += BigInt(count);
    intervals.push({
      lower: input.bins.edges[index],
      upper: input.bins.edges[index + 1],
      integerWeight: denominator
    });
  }
  if (intervals.length === 0) {
    throw new PsthDerivationError(
      "SCIENCE_DENOMINATOR_INVALID",
      "/parameters/baseline",
      "no included trial covered any complete bin in the baseline interval, so its total exposure is zero."
    );
  }
  const integerFactors = input.normalization === "mean_rate_per_selected_sender_per_trial" ? [selectedSenderCount] : [];
  let rate;
  let corrected;
  try {
    rate = deriveExactAggregateCountRateOverIntervalsInUnit(
      baselineCount,
      integerFactors,
      intervals,
      input.bins.unit,
      valueUnit
    );
    corrected = counts.map((count, index) => {
      const denominator = denominators[index];
      if (count === null || denominator === null) return null;
      const perBinFactors = input.normalization === "mean_rate_per_selected_sender_per_trial" ? [denominator, selectedSenderCount] : [denominator];
      return deriveExactCountRateMinusAggregateRateOverIntervalsInUnit(
        count,
        perBinFactors,
        input.bins.edges[index],
        input.bins.edges[index + 1],
        baselineCount,
        integerFactors,
        intervals,
        input.bins.unit,
        valueUnit
      );
    });
  } catch (error) {
    throw new PsthDerivationError(
      "SCIENCE_NUMERIC_RESOLUTION_UNREPRESENTABLE",
      "/parameters/baseline",
      `the exact aggregate baseline exposure cannot be represented (${error instanceof Error ? error.message : "numeric derivation failed"}).`
    );
  }
  return {
    rate,
    corrected,
    startIndex,
    stopIndex,
    exactBaselineCount: baselineCount.toString(10)
  };
}
function auditSuppliedValues(supplied, expected, normalization) {
  if (!supplied) return;
  const kind = expectedValueKind(normalization);
  if (supplied.kind !== kind) {
    throw new PsthDerivationError(
      "SCIENCE_NORMALIZATION_UNVERIFIABLE",
      "/data/rates/kind",
      `normalization ${normalization} requires supplied normalized values of kind ${kind}; got ${supplied.kind}.`
    );
  }
  const expectedDimension = isRateNormalization(normalization) ? "frequency" : "dimensionless";
  if (dimensionOf(supplied.unit) !== expectedDimension) {
    throw new PsthDerivationError(
      "SCIENCE_UNIT_DIMENSION_MISMATCH",
      "/data/rates/unit",
      `normalization ${normalization} requires a ${expectedDimension} value unit; got ${supplied.unit}.`
    );
  }
  if (supplied.values.length !== expected.length) {
    throw new PsthDerivationError(
      "SCIENCE_NORMALIZATION_UNVERIFIABLE",
      "/data/rates/values",
      "supplied normalized values must have exactly one entry per authoritative PSTH bin."
    );
  }
  const finiteExpected = expected.filter((value) => value !== null);
  if (finiteExpected.some((value) => !Number.isFinite(value) || value < 0)) {
    throw new PsthDerivationError(
      "SCIENCE_NORMALIZATION_UNVERIFIABLE",
      "/data/counts",
      "a count-derived PSTH normalization must remain finite and non-negative before optional baseline subtraction."
    );
  }
  for (let index = 0; index < expected.length; index++) {
    const expectedValue = expected[index];
    const suppliedValue = supplied.values[index];
    if (expectedValue === null !== (suppliedValue === null)) {
      throw new PsthDerivationError(
        "SCIENCE_NORMALIZATION_UNVERIFIABLE",
        `/data/rates/values/${index}`,
        "supplied normalized values must have the same missing-bin mask as counts and trial denominators."
      );
    }
    if (suppliedValue !== null && (!Number.isFinite(suppliedValue) || suppliedValue < 0)) {
      throw new PsthDerivationError(
        "SCIENCE_NORMALIZATION_UNVERIFIABLE",
        `/data/rates/values/${index}`,
        "a supplied count-derived PSTH value must be finite and non-negative."
      );
    }
    if (expectedValue !== null && (suppliedValue === null || (normalization === "count" ? !Number.isSafeInteger(suppliedValue) || suppliedValue !== expectedValue : !exactDifferenceWithinScaleTolerance(
      suppliedValue,
      expectedValue,
      Math.abs(expectedValue)
    )))) {
      throw new PsthDerivationError(
        "SCIENCE_NORMALIZATION_UNVERIFIABLE",
        `/data/rates/values/${index}`,
        `supplied PSTH value ${suppliedValue} does not match the value ${expectedValue} re-derived from its exact count, covering-trial denominator, selected-sender denominator, and typed bin exposure within the declared relative tolerance ${PSTH_VALUE_TOLERANCE}.`
      );
    }
  }
}
function conversionsFor(input) {
  const pairs = [];
  if (input.relativeWindow.unit !== input.bins.unit) {
    pairs.push([input.relativeWindow.unit, input.bins.unit]);
  }
  if (input.mode === "events") {
    if (input.eventTimeUnit !== input.bins.unit) pairs.push([input.eventTimeUnit, input.bins.unit]);
    if (input.alignmentUnit !== input.bins.unit) pairs.push([input.alignmentUnit, input.bins.unit]);
  }
  if (isRateNormalization(input.normalization)) {
    if (input.bins.unit !== "s") pairs.push([input.bins.unit, "s"]);
    const outputUnit = input.mode === "prebinned" ? input.suppliedValues?.unit ?? "Hz" : "Hz";
    if (outputUnit !== "Hz") pairs.push(["Hz", outputUnit]);
  }
  const unique = [...new Map(pairs.map((pair) => [`${pair[0]}\0${pair[1]}`, pair])).values()];
  return {
    labels: unique.map(([from, to]) => `${from} -> ${to}`),
    receipts: unique.map(([from, to]) => conversionReceipt(from, to))
  };
}
function deriveEvents(input) {
  if (input.denominatorPolicy !== "uniform_trial_count") {
    throw new PsthDerivationError(
      "SCIENCE_DENOMINATOR_INVALID",
      "/parameters/denominatorPolicy",
      "events-mode per_bin_covering_trials requires per-trial coverage records, but this request branch carries none. Cortexel refuses instead of inventing uniform coverage."
    );
  }
  const lengths = [input.eventTimes.length, input.eventSenderIds.length, input.eventTrialIds.length];
  if (!lengths.every((length) => length === lengths[0])) {
    throw new PsthDerivationError(
      "SCIENCE_NORMALIZATION_UNVERIFIABLE",
      "/data/eventTimes/values",
      "event times, sender ids, and trial ids must remain positionally parallel."
    );
  }
  if (input.trialIds.length !== input.alignmentTimes.length || input.trialIds.length < 1) {
    throw new PsthDerivationError(
      "SCIENCE_DENOMINATOR_INVALID",
      "/data/alignmentTimes",
      "every declared trial must have exactly one alignment time."
    );
  }
  validatePositiveDenominator(input.recordedSenderIds.length, "/data/recordedSenderIds");
  validatePositiveDenominator(input.trialIds.length, "/data/trialIds");
  assertUniqueIds(input.recordedSenderIds, "/data/recordedSenderIds", "selected-sender universe");
  assertUniqueIds(input.trialIds, "/data/trialIds", "trial universe");
  assertFiniteValues(input.eventTimes, "/data/eventTimes/values", "event time");
  assertFiniteValues(input.alignmentTimes, "/data/alignmentTimes", "alignment time");
  const alignmentByTrial = /* @__PURE__ */ new Map();
  input.trialIds.forEach((trialId, index) => alignmentByTrial.set(trialId, input.alignmentTimes[index]));
  const senderUniverse = new Set(input.recordedSenderIds);
  const counts = new Array(input.bins.edges.length - 1).fill(0);
  let excludedOutOfWindowCount = 0;
  let acceptedEventCount = 0;
  const windowStart = convert(input.relativeWindow.start, input.relativeWindow.unit, input.bins.unit);
  const windowStop = convert(input.relativeWindow.stop, input.relativeWindow.unit, input.bins.unit);
  if (!(windowStop > windowStart)) {
    throw new PsthDerivationError(
      "SCIENCE_NUMERIC_RESOLUTION_UNREPRESENTABLE",
      "/data/relativeWindow",
      "the relative-window endpoints collapse after the single typed conversion into the bin unit."
    );
  }
  const closedStop = input.relativeWindow.boundary === "[start,stop]";
  const placement = {
    edges: input.bins.edges,
    finalEdgeInclusive: closedStop && input.bins.finalEdgeInclusive
  };
  for (let index = 0; index < input.eventTimes.length; index++) {
    const senderId = input.eventSenderIds[index];
    const trialId = input.eventTrialIds[index];
    const alignment = alignmentByTrial.get(trialId);
    if (!senderUniverse.has(senderId) || alignment === void 0) {
      throw new PsthDerivationError(
        "SCIENCE_NORMALIZATION_UNVERIFIABLE",
        `/data/eventTrialIds/${index}`,
        "each PSTH event must resolve to one declared selected sender and one declared aligned trial."
      );
    }
    let relative;
    try {
      relative = convertExactUnitSum(
        [
          { value: input.eventTimes[index], unit: input.eventTimeUnit },
          { value: -alignment, unit: input.alignmentUnit }
        ],
        input.bins.unit
      );
    } catch (error) {
      throw new PsthDerivationError(
        "SCIENCE_NUMERIC_RESOLUTION_UNREPRESENTABLE",
        `/data/eventTimes/values/${index}`,
        `event ${index} cannot be aligned through one exact typed subtraction (${error instanceof Error ? error.message : "numeric conversion failed"}).`
      );
    }
    const inWindow2 = relative >= windowStart && (closedStop ? relative <= windowStop : relative < windowStop);
    if (!inWindow2) {
      excludedOutOfWindowCount++;
      continue;
    }
    const bin = binIndex(relative, placement);
    if (bin < 0) {
      throw new PsthDerivationError(
        "SCIENCE_EVENT_OUT_OF_WINDOW",
        `/data/eventTimes/values/${index}`,
        `event ${index} is inside the declared relative window after alignment but falls in no authoritative bin. The window and bin boundary declarations are inconsistent at this value.`
      );
    }
    counts[bin]++;
    acceptedEventCount++;
  }
  return {
    counts,
    denominators: counts.map(() => input.trialIds.length),
    selectedSenderCount: input.recordedSenderIds.length,
    includedTrialCount: input.trialIds.length,
    excludedTrialCount: 0,
    excludedOutOfWindowCount,
    acceptedEventCount
  };
}
function derivePrebinned(input) {
  validatePositiveDenominator(input.recordedSenderCount, "/data/recordedSenderCount");
  validatePositiveDenominator(input.includedTrialCount, "/data/includedTrialCount");
  if (input.trialIds.length !== input.alignmentTimes.length) {
    throw new PsthDerivationError(
      "SCIENCE_DENOMINATOR_INVALID",
      "/data/alignmentTimes",
      "every pre-binned trial-universe id must retain exactly one declared alignment time."
    );
  }
  assertUniqueIds(input.trialIds, "/data/trialIds", "trial universe");
  assertFiniteValues(input.alignmentTimes, "/data/alignmentTimes", "alignment time");
  if (!Number.isSafeInteger(input.excludedTrialCount) || input.excludedTrialCount < 0) {
    throw new PsthDerivationError(
      "SCIENCE_DENOMINATOR_INVALID",
      "/data/excludedTrialCount",
      "the excluded-trial count must be an exact non-negative safe integer."
    );
  }
  if (input.includedTrialCount + input.excludedTrialCount !== input.trialIds.length) {
    throw new PsthDerivationError(
      "SCIENCE_DENOMINATOR_INVALID",
      "/data/trialIds",
      "the declared trial universe length must equal includedTrialCount + excludedTrialCount exactly."
    );
  }
  const binCount = input.bins.edges.length - 1;
  if (input.counts.length !== binCount || input.trialDenominators.length !== binCount) {
    throw new PsthDerivationError(
      "SCIENCE_NORMALIZATION_UNVERIFIABLE",
      "/data/counts",
      "prebinned counts and trial denominators must have exactly one entry per authoritative bin."
    );
  }
  for (let index = 0; index < binCount; index++) {
    const count = input.counts[index];
    const denominator = input.trialDenominators[index];
    if (count === null !== (denominator === null)) {
      throw new PsthDerivationError(
        "SCIENCE_DENOMINATOR_INVALID",
        `/data/trialDenominators/${index}`,
        "a pre-binned PSTH count and covering-trial denominator must be null together."
      );
    }
    if (count === null || denominator === null) {
      if (input.denominatorPolicy === "uniform_trial_count") {
        throw new PsthDerivationError(
          "SCIENCE_DENOMINATOR_INVALID",
          `/data/trialDenominators/${index}`,
          "uniform_trial_count asserts that every included trial covered every bin, so an unobserved null bin is a contradiction."
        );
      }
      continue;
    }
    validateSafeCount(count, `/data/counts/${index}`);
    validatePositiveDenominator(denominator, `/data/trialDenominators/${index}`);
    if (denominator > input.includedTrialCount) {
      throw new PsthDerivationError(
        "SCIENCE_DENOMINATOR_INVALID",
        `/data/trialDenominators/${index}`,
        `bin ${index} declares ${denominator} covering trials, above includedTrialCount ${input.includedTrialCount}.`
      );
    }
    if (input.denominatorPolicy === "uniform_trial_count" && denominator !== input.includedTrialCount) {
      throw new PsthDerivationError(
        "SCIENCE_DENOMINATOR_INVALID",
        `/data/trialDenominators/${index}`,
        `uniform_trial_count requires denominator ${input.includedTrialCount} in every observed bin; bin ${index} declares ${denominator}.`
      );
    }
  }
  return {
    counts: [...input.counts],
    denominators: [...input.trialDenominators],
    selectedSenderCount: input.recordedSenderCount,
    includedTrialCount: input.includedTrialCount,
    excludedTrialCount: input.excludedTrialCount,
    excludedOutOfWindowCount: null,
    acceptedEventCount: null
  };
}
function derivePsthUnchecked(input) {
  assertPublicInputShape(input);
  assertObservationBudgets(input);
  assertTimeUnit(input.bins.unit, "/parameters/bins/unit");
  assertTimeUnit(input.relativeWindow.unit, "/data/relativeWindow/unit");
  assertTimeUnit(input.alignmentUnit, "/data/alignmentUnit");
  if (input.mode === "events") {
    assertTimeUnit(input.eventTimeUnit, "/data/eventTimes/unit");
  }
  if (input.normalization === "mean_rate_per_selected_sender_per_trial" && input.senderExposurePolicy !== "all_selected_senders_cover_every_counted_trial_bin") {
    throw new PsthDerivationError(
      "SCIENCE_DENOMINATOR_INVALID",
      "/parameters/senderExposurePolicy",
      "the per-selected-sender PSTH normalization requires an explicit assertion that every selected sender was observable in every counted trial/bin."
    );
  }
  assertBinsTileWindow(input);
  const base = input.mode === "events" ? deriveEvents(input) : derivePrebinned(input);
  if (input.mode === "prebinned" && input.suppliedValues) {
    const requiredKind = expectedValueKind(input.normalization);
    const requiredDimension = isRateNormalization(input.normalization) ? "frequency" : "dimensionless";
    if (input.suppliedValues.kind !== requiredKind) {
      throw new PsthDerivationError(
        "SCIENCE_NORMALIZATION_UNVERIFIABLE",
        "/data/rates/kind",
        `normalization ${input.normalization} requires supplied normalized values of kind ${requiredKind}; got ${input.suppliedValues.kind}.`
      );
    }
    if (dimensionOf(input.suppliedValues.unit) !== requiredDimension) {
      throw new PsthDerivationError(
        "SCIENCE_UNIT_DIMENSION_MISMATCH",
        "/data/rates/unit",
        `normalization ${input.normalization} requires a ${requiredDimension} value unit; got ${input.suppliedValues.unit}.`
      );
    }
  }
  const suppliedValueUnit = input.mode === "prebinned" ? input.suppliedValues?.unit : void 0;
  const valueUnit = isRateNormalization(input.normalization) ? suppliedValueUnit ?? "Hz" : "1";
  let values;
  try {
    values = deriveNormalizedValues(
      base.counts,
      base.denominators,
      input.bins.edges,
      input.bins.unit,
      base.selectedSenderCount,
      input.normalization,
      valueUnit
    );
  } catch (error) {
    throw new PsthDerivationError(
      "SCIENCE_NUMERIC_RESOLUTION_UNREPRESENTABLE",
      "/data/counts",
      `the exact PSTH normalization cannot be represented (${error instanceof Error ? error.message : "numeric derivation failed"}).`
    );
  }
  if (input.mode === "prebinned") {
    auditSuppliedValues(input.suppliedValues, values, input.normalization);
  }
  const baseline = deriveBaseline(
    input,
    base.counts,
    base.denominators,
    base.selectedSenderCount,
    valueUnit
  );
  const corrected = baseline.corrected;
  const displayValues = baseline.rate === null ? values : corrected;
  let binWidths2;
  try {
    binWidths2 = input.bins.edges.slice(0, -1).map((lower, index) => convertDifference(lower, input.bins.edges[index + 1], input.bins.unit, input.bins.unit));
  } catch (error) {
    throw new PsthDerivationError(
      "SCIENCE_NUMERIC_RESOLUTION_UNREPRESENTABLE",
      "/parameters/bins",
      `an exact PSTH bin width cannot be represented (${error instanceof Error ? error.message : "numeric derivation failed"}).`
    );
  }
  const countTotal = exactCountTotal(base.counts);
  const conversions = conversionsFor(input);
  const missingBinCount = base.counts.filter((count) => count === null).length;
  const receipt = {
    operation: "psth.derive",
    mode: input.mode,
    normalization: input.normalization,
    denominatorPolicy: input.denominatorPolicy,
    binCount: base.counts.length,
    binUnit: input.bins.unit,
    binWidths: binWidths2,
    binBoundary: input.bins.boundary,
    relativeWindow: input.relativeWindow,
    finalEdgeInclusive: input.bins.finalEdgeInclusive,
    countTotalExact: countTotal.toString(10),
    selectedSenderCount: base.selectedSenderCount,
    includedTrialCount: base.includedTrialCount,
    excludedTrialCount: base.excludedTrialCount,
    trialDenominators: base.denominators,
    acceptedEventCount: base.acceptedEventCount,
    excludedOutOfWindowCount: base.excludedOutOfWindowCount,
    missingBinCount,
    valueUnit,
    exactExposureAuthority: "integer_count_over_integer_denominators_and_exact_typed_endpoint_differences_one_round_to_binary64",
    senderDenominatorApplied: input.normalization === "mean_rate_per_selected_sender_per_trial",
    senderExposurePolicy: input.senderExposurePolicy ?? null,
    senderExposureVerifiableFromInput: false,
    suppliedNormalizedValues: input.mode === "prebinned" && input.suppliedValues !== void 0,
    suppliedNormalizedValuesVerified: input.mode === "prebinned" && input.suppliedValues !== void 0 ? true : null,
    suppliedValueRelativeTolerance: input.mode === "prebinned" && input.suppliedValues !== void 0 && input.normalization !== "count" ? PSTH_VALUE_TOLERANCE : null,
    suppliedCountExactEquality: input.mode === "prebinned" && input.suppliedValues !== void 0 && input.normalization === "count" ? true : null,
    suppliedValueTolerancePolicy: input.mode === "prebinned" && input.suppliedValues !== void 0 ? input.normalization === "count" ? "exact_safe_integer_equality" : "per_value_relative_to_exact_derived_magnitude_zero_requires_exact_zero" : null,
    baselineMode: input.baseline?.mode ?? null,
    baselineStart: input.baseline?.start ?? null,
    baselineStop: input.baseline?.stop ?? null,
    baselineUnit: input.baseline ? input.relativeWindow.unit : null,
    baselineStartBinIndex: baseline.startIndex,
    baselineStopBinIndexExclusive: baseline.stopIndex,
    baselineCountTotalExact: baseline.exactBaselineCount,
    baselineRate: baseline.rate,
    baselineExposureAuthority: baseline.rate === null ? null : "sum_of_integer_covering_trial_weights_times_exact_typed_endpoint_differences",
    baselineCorrectionAuthority: baseline.rate === null ? null : "single_exact_signed_rational_difference_one_round_to_binary64",
    unitConversions: conversions.receipts
  };
  return {
    mode: input.mode,
    edges: [...input.bins.edges],
    binUnit: input.bins.unit,
    binWidths: binWidths2,
    relativeWindowStart: input.relativeWindow.start,
    relativeWindowStop: input.relativeWindow.stop,
    relativeWindowUnit: input.relativeWindow.unit,
    relativeWindowBoundary: input.relativeWindow.boundary,
    binBoundary: input.bins.boundary,
    finalEdgeInclusive: input.bins.finalEdgeInclusive,
    counts: base.counts,
    trialDenominators: base.denominators,
    values,
    displayValues,
    valueUnit,
    baselineCorrectedValues: corrected,
    baselineRate: baseline.rate,
    baselineBinStartIndex: baseline.startIndex,
    baselineBinStopIndex: baseline.stopIndex,
    baselineStart: input.baseline?.start ?? null,
    baselineStop: input.baseline?.stop ?? null,
    baselineUnit: input.baseline ? input.relativeWindow.unit : null,
    selectedSenderCount: base.selectedSenderCount,
    senderExposurePolicy: input.senderExposurePolicy ?? null,
    includedTrialCount: base.includedTrialCount,
    excludedTrialCount: base.excludedTrialCount,
    excludedOutOfWindowCount: base.excludedOutOfWindowCount,
    acceptedEventCount: base.acceptedEventCount,
    missingBinCount,
    exactCountTotal: countTotal.toString(10),
    unitConversions: conversions.labels,
    conversionReceipts: conversions.receipts,
    suppliedValuesPresent: input.mode === "prebinned" && input.suppliedValues !== void 0,
    suppliedValuesVerified: input.mode === "prebinned" && input.suppliedValues !== void 0 ? true : null,
    receipt
  };
}
function derivePsth(input) {
  try {
    return derivePsthUnchecked(input);
  } catch (error) {
    if (error instanceof PsthDerivationError) throw error;
    throw new PsthDerivationError(
      "SCIENCE_NUMERIC_RESOLUTION_UNREPRESENTABLE",
      "/data/relativeWindow",
      `the PSTH derivation could not preserve a finite typed numeric invariant (${error instanceof Error ? error.message : "numeric derivation failed"}).`
    );
  }
}

// src/analysis/correlogram.ts
var DEFAULT_MAX_PAIRWISE_OPERATIONS = BUDGET_PROFILES.standard.pairwiseOperations;
var PairwiseBudgetExceededError = class extends Error {
  limit;
  observedLowerBound;
  constructor(limit, observedLowerBound) {
    super(`correlogram pair count exceeds ${limit}`);
    this.name = "PairwiseBudgetExceededError";
    this.limit = limit;
    this.observedLowerBound = observedLowerBound;
  }
};
function requireExactCount(value, name) {
  if (!Number.isSafeInteger(value) || value < 0) {
    throw new RangeError(`${name} must be a non-negative safe integer`);
  }
}
function deriveCorrelogramPairAccounting(referenceEventCount, targetEventCount, kind, countedPairCount) {
  requireExactCount(referenceEventCount, "reference event count");
  requireExactCount(targetEventCount, "target event count");
  requireExactCount(countedPairCount, "counted pair count");
  if (kind === "auto" && targetEventCount !== referenceEventCount) {
    throw new RangeError("an autocorrelogram uses the same event count in both roles");
  }
  const candidate = BigInt(referenceEventCount) * BigInt(targetEventCount);
  const selfPairs = kind === "auto" ? BigInt(referenceEventCount) : 0n;
  const counted = BigInt(countedPairCount);
  const outOfRange = candidate - selfPairs - counted;
  if (outOfRange < 0n) {
    throw new RangeError(
      "counted pairs exceed the candidate role product after the auto self-pair exclusion"
    );
  }
  if (candidate > BigInt(Number.MAX_SAFE_INTEGER)) {
    throw new RangeError("candidate pair count exceeds the exact JSON integer domain");
  }
  return {
    candidatePairCount: Number(candidate),
    countedPairCount,
    outOfRangePairCount: Number(outOfRange),
    sameEventSelfPairCountExcluded: Number(selfPairs)
  };
}
function deriveEligibleCorrelogramReferenceCounts(referenceTimes, referenceTimeUnit, binEdges, binEdgeUnit, window) {
  if (!Number.isFinite(window.start) || !Number.isFinite(window.stop) || !(window.stop > window.start)) {
    throw new RangeError("correlogram eligibility window must be finite and ordered");
  }
  if (binEdges.length < 2) {
    throw new RangeError("correlogram eligibility requires at least two bin edges");
  }
  for (let index = 0; index < binEdges.length; index++) {
    if (!Number.isFinite(binEdges[index])) {
      throw new RangeError(`correlogram bin edge ${index} must be finite`);
    }
    if (index > 0 && !(binEdges[index] > binEdges[index - 1])) {
      throw new RangeError("correlogram bin edges must be strictly increasing");
    }
  }
  for (let index = 0; index < referenceTimes.length; index++) {
    if (!Number.isFinite(referenceTimes[index])) {
      throw new RangeError(`correlogram reference time ${index} must be finite`);
    }
  }
  const openStart = window.boundary === "(start,stop]";
  const binCount = binEdges.length - 1;
  const deltas = new Array(binCount + 1).fill(0);
  const firstTrue = (predicate) => {
    let lower = 0;
    let upper = binEdges.length;
    while (lower < upper) {
      const middle = lower + Math.floor((upper - lower) / 2);
      if (predicate(binEdges[middle])) upper = middle;
      else lower = middle + 1;
    }
    return lower;
  };
  for (const time of referenceTimes) {
    const firstAdmissibleLowerEdge = firstTrue((edge) => {
      const comparison = compareExactUnitSumToValue(
        [
          { value: time, unit: referenceTimeUnit },
          { value: edge, unit: binEdgeUnit }
        ],
        { value: window.start, unit: window.unit }
      );
      return openStart ? comparison > 0 : comparison >= 0;
    });
    const firstUpperEdgeBeyondStop = firstTrue((edge) => compareExactUnitSumToValue(
      [
        { value: time, unit: referenceTimeUnit },
        { value: edge, unit: binEdgeUnit }
      ],
      { value: window.stop, unit: window.unit }
    ) > 0);
    const lowerBin = Math.min(firstAdmissibleLowerEdge, binCount);
    const upperBinExclusive = Math.max(
      0,
      Math.min(firstUpperEdgeBeyondStop - 1, binCount)
    );
    if (lowerBin < upperBinExclusive) {
      deltas[lowerBin]++;
      deltas[upperBinExclusive]--;
    }
  }
  const counts = new Array(binCount);
  let active = 0;
  for (let index = 0; index < binCount; index++) {
    active += deltas[index];
    counts[index] = active;
  }
  return counts;
}
function deriveCorrelogramTargetRates(pairCounts, eligibleReferenceEventCounts, binWidth) {
  if (pairCounts.length !== eligibleReferenceEventCounts.length) {
    throw new RangeError("each correlogram pair-count bin requires one eligible-reference count");
  }
  if (!Number.isFinite(binWidth.value) || !(binWidth.value > 0)) {
    throw new RangeError("correlogram bin width must be finite and positive");
  }
  const binWidthSeconds = convertDifference(0, binWidth.value, binWidth.unit, "s");
  return pairCounts.map((pairCount, index) => {
    const eligibleReferenceEvents = eligibleReferenceEventCounts[index];
    requireExactCount(pairCount, `pair count at bin ${index}`);
    requireExactCount(eligibleReferenceEvents, `eligible reference count at bin ${index}`);
    if (eligibleReferenceEvents === 0) {
      if (pairCount !== 0) {
        throw new RangeError(
          `pair count at bin ${index} must be zero when no reference event is eligible`
        );
      }
      return {
        pairCount,
        eligibleReferenceEvents,
        denominatorSeconds: 0,
        value: null,
        status: "undefined_zero_eligible_reference_events"
      };
    }
    const exposureSeconds = eligibleReferenceEvents * binWidthSeconds;
    if (!Number.isFinite(exposureSeconds) || !(exposureSeconds > 0)) {
      throw new RangeError(`target-rate exposure at bin ${index} is not finite positive binary64`);
    }
    const value = deriveExactCountRateInUnit(
      pairCount,
      eligibleReferenceEvents,
      0,
      binWidth.value,
      binWidth.unit,
      "Hz"
    );
    return {
      pairCount,
      eligibleReferenceEvents,
      denominatorSeconds: exposureSeconds,
      value,
      status: "defined"
    };
  });
}
function sortedTrains(referenceTimes, targetTimes, kind) {
  const ref = [...referenceTimes].sort((a, b) => a - b);
  return { ref, tgt: kind === "auto" ? ref : [...targetTimes].sort((a, b) => a - b) };
}
function requireCorrelogramBins(bins) {
  if (bins.finalEdgeInclusive) {
    throw new RangeError(
      "correlogram bins are left-closed/right-open at every edge; finalEdgeInclusive must be false"
    );
  }
}
function countSortedEligiblePairs(ref, tgt, bins, kind, stopAfter) {
  const lagMin = bins.edges[0];
  const lagMax = bins.edges[bins.edges.length - 1];
  let lower = 0;
  let upper = 0;
  let total = 0;
  for (let i = 0; i < ref.length; i++) {
    const reference = ref[i];
    while (lower < tgt.length && tgt[lower] - reference < lagMin) lower++;
    if (upper < lower) upper = lower;
    while (upper < tgt.length && tgt[upper] - reference < lagMax) {
      upper++;
    }
    let eligible = upper - lower;
    if (kind === "auto" && i >= lower && i < upper) eligible--;
    total += eligible;
    if (total > stopAfter) return stopAfter + 1;
  }
  return total;
}
function countEligibleCorrelogramPairs(referenceTimes, targetTimes, bins, kind, stopAfter = Number.MAX_SAFE_INTEGER - 1) {
  if (!Number.isSafeInteger(stopAfter) || stopAfter < 0) {
    throw new RangeError("correlogram pair budget must be a non-negative safe integer");
  }
  requireCorrelogramBins(bins);
  const { ref, tgt } = sortedTrains(referenceTimes, targetTimes, kind);
  return countSortedEligiblePairs(ref, tgt, bins, kind, stopAfter);
}
function computeCorrelogram(referenceTimes, targetTimes, bins, kind, maxPairwiseOperations = DEFAULT_MAX_PAIRWISE_OPERATIONS) {
  if (!Number.isSafeInteger(maxPairwiseOperations) || maxPairwiseOperations < 0) {
    throw new RangeError("correlogram pair budget must be a non-negative safe integer");
  }
  requireCorrelogramBins(bins);
  const lagMin = bins.edges[0];
  const lagMax = bins.edges[bins.edges.length - 1];
  const counts = new Array(Math.max(0, bins.edges.length - 1)).fill(0);
  const selfPairsExcluded = kind === "auto" ? referenceTimes.length : 0;
  let zeroLagRetainedDistinctPairs = 0;
  let totalPairs = 0;
  const { ref, tgt } = sortedTrains(referenceTimes, targetTimes, kind);
  const eligible = countSortedEligiblePairs(ref, tgt, bins, kind, maxPairwiseOperations);
  if (eligible > maxPairwiseOperations) {
    throw new PairwiseBudgetExceededError(maxPairwiseOperations, eligible);
  }
  let lower = 0;
  let upper = 0;
  for (let i = 0; i < ref.length; i++) {
    const r = ref[i];
    while (lower < tgt.length && tgt[lower] - r < lagMin) lower++;
    if (upper < lower) upper = lower;
    while (upper < tgt.length && tgt[upper] - r < lagMax) {
      upper++;
    }
    for (let j = lower; j < upper; j++) {
      if (kind === "auto" && i === j) {
        continue;
      }
      const lag = tgt[j] - r;
      const index = binIndex(lag, bins);
      if (index < 0) continue;
      counts[index]++;
      totalPairs++;
      if (lag === 0) zeroLagRetainedDistinctPairs++;
    }
  }
  const pairAccounting = deriveCorrelogramPairAccounting(
    referenceTimes.length,
    kind === "auto" ? referenceTimes.length : targetTimes.length,
    kind,
    totalPairs
  );
  return {
    counts,
    kind,
    totalPairs,
    selfPairsExcluded,
    zeroLagRetainedDistinctPairs,
    receipt: {
      operation: "correlogram.pair_count",
      lagConvention: "target_time - reference_time",
      binBoundary: "left_closed_right_open",
      positiveOuterEdge: "excluded",
      kind,
      selfPairPolicy: kind === "auto" ? "identical-event self-pairs excluded; distinct coincident events retained" : "no self-pair exclusion (cross-correlogram of two trains)",
      pairAccounting
    }
  };
}

// src/analysis/topology.ts
function computeDegrees(nodeIds, sourceIds, targetIds, direction, countingPolicy) {
  const index = /* @__PURE__ */ new Map();
  nodeIds.forEach((id, i) => index.set(id, i));
  const degree = new Array(nodeIds.length).fill(0);
  const neighbours = countingPolicy === "count_unique_neighbors" ? nodeIds.map(() => /* @__PURE__ */ new Set()) : void 0;
  const n = Math.min(sourceIds.length, targetIds.length);
  for (let e = 0; e < n; e++) {
    const countedId = direction === "in" ? targetIds[e] : sourceIds[e];
    const neighbourId = direction === "in" ? sourceIds[e] : targetIds[e];
    const i = index.get(countedId);
    if (i === void 0) continue;
    if (neighbours) {
      neighbours[i].add(neighbourId);
    } else {
      degree[i]++;
    }
  }
  if (neighbours) {
    for (let i = 0; i < degree.length; i++) degree[i] = neighbours[i].size;
  }
  return { degree, nodeIds, direction, countingPolicy, totalConnections: n };
}
function aggregateTopologyScalar(values, method) {
  if (values.length === 0) return null;
  for (const value of values) {
    if (!Number.isFinite(value)) {
      throw new Error("topology scalar aggregation requires finite binary64 values");
    }
  }
  let result;
  switch (method) {
    case "sum":
      result = exactBinary64Sum(values);
      break;
    case "mean":
      result = exactBinary64Mean(values);
      break;
    case "min": {
      result = values[0];
      for (let index = 1; index < values.length; index++) {
        if (values[index] < result) result = values[index];
      }
      break;
    }
    case "max": {
      result = values[0];
      for (let index = 1; index < values.length; index++) {
        if (values[index] > result) result = values[index];
      }
      break;
    }
    case "no_aggregation":
      if (values.length !== 1) {
        throw new Error(
          `no_aggregation asserts exactly one topology value; got ${values.length}`
        );
      }
      result = values[0];
      break;
    default:
      throw new Error("topology scalar aggregation requires a declared supported method");
  }
  if (!Number.isFinite(result)) {
    throw new Error("topology scalar aggregate is outside the finite binary64 range");
  }
  return result === 0 ? 0 : result;
}

// src/core/spatial.ts
var SPATIAL_DOMAIN_TOLERANCE_EPSILON_MULTIPLIER = 8;
var SPATIAL_DOMAIN_ENDPOINT_ROUNDING_EPSILON_MULTIPLIER = 32;
var EPSILON_DENOMINATOR = 1n << 52n;
var TOLERANCE_NUMERATOR = BigInt(SPATIAL_DOMAIN_TOLERANCE_EPSILON_MULTIPLIER);
var ROUNDING_CAP_NUMERATOR = BigInt(
  SPATIAL_DOMAIN_ENDPOINT_ROUNDING_EPSILON_MULTIPLIER
);
function absolute(value) {
  return value < 0n ? -value : value;
}
function spatialDomainAxis(center, extent) {
  if (!Number.isFinite(center) || !Number.isFinite(extent) || !(extent > 0)) {
    throw new Error("a spatial domain axis requires a finite center and positive finite extent");
  }
  const centerUnits = finiteBinary64ToMinSubnormalUnits(center);
  const extentUnits = finiteBinary64ToMinSubnormalUnits(extent);
  const lowerNumerator = 2n * centerUnits - extentUnits;
  const upperNumerator = 2n * centerUnits + extentUnits;
  const lower = exactRationalToBinary64(lowerNumerator, 2n, -1074);
  const upper = exactRationalToBinary64(upperNumerator, 2n, -1074);
  if (!Number.isFinite(lower) || !Number.isFinite(upper) || !(upper > lower)) {
    throw new Error(
      "spatial domain endpoints are not a finite strictly ordered binary64 interval"
    );
  }
  const lowerUnits = finiteBinary64ToMinSubnormalUnits(lower);
  const upperUnits = finiteBinary64ToMinSubnormalUnits(upper);
  const lowerRoundingErrorTwiceUnits = absolute(2n * lowerUnits - lowerNumerator);
  const upperRoundingErrorTwiceUnits = absolute(2n * upperUnits - upperNumerator);
  const roundingLimit = 2n * ROUNDING_CAP_NUMERATOR * extentUnits;
  if (lowerRoundingErrorTwiceUnits * EPSILON_DENOMINATOR > roundingLimit || upperRoundingErrorTwiceUnits * EPSILON_DENOMINATOR > roundingLimit) {
    throw new Error(
      "spatial domain endpoint rounding exceeds the extent-relative binary64 allowance"
    );
  }
  return {
    lower,
    upper,
    period: extent,
    center,
    extent,
    lowerRoundingErrorTwiceUnits,
    upperRoundingErrorTwiceUnits
  };
}
function spatialDomainAxisContains(axis, value) {
  if (!Number.isFinite(value)) return false;
  const valueUnits = finiteBinary64ToMinSubnormalUnits(value);
  const lowerUnits = finiteBinary64ToMinSubnormalUnits(axis.lower);
  const upperUnits = finiteBinary64ToMinSubnormalUnits(axis.upper);
  const extentUnits = finiteBinary64ToMinSubnormalUnits(axis.extent);
  const lowerAllowanceNumerator = 2n * TOLERANCE_NUMERATOR * extentUnits + axis.lowerRoundingErrorTwiceUnits * EPSILON_DENOMINATOR;
  const upperAllowanceNumerator = 2n * TOLERANCE_NUMERATOR * extentUnits + axis.upperRoundingErrorTwiceUnits * EPSILON_DENOMINATOR;
  const denominator = 2n * EPSILON_DENOMINATOR;
  return (valueUnits - lowerUnits) * denominator + lowerAllowanceNumerator >= 0n && (valueUnits - upperUnits) * denominator - upperAllowanceNumerator <= 0n;
}
function exactMinimumImageDisplacement(source, target, period) {
  if (!Number.isFinite(source) || !Number.isFinite(target) || !Number.isFinite(period) || !(period > 0)) {
    throw new Error("minimum-image routing requires finite coordinates and a positive finite period");
  }
  const sourceUnits = finiteBinary64ToMinSubnormalUnits(source);
  const targetUnits = finiteBinary64ToMinSubnormalUnits(target);
  const periodUnits = finiteBinary64ToMinSubnormalUnits(period);
  const raw = targetUnits - sourceUnits;
  let positiveRemainder = raw % periodUnits;
  if (positiveRemainder < 0n) positiveRemainder += periodUnits;
  const doubled = 2n * positiveRemainder;
  const minimum = doubled <= periodUnits ? positiveRemainder : positiveRemainder - periodUnits;
  const tie = doubled === periodUnits;
  const displacement = exactRationalToBinary64(minimum, 1n, -1074);
  if (!Number.isFinite(displacement)) {
    throw new Error("minimum-image displacement is outside finite binary64");
  }
  return { displacement, wrapped: minimum !== raw, tie };
}
function classifySpatialChord(source, target, domain, autapse = false) {
  if (autapse) {
    return {
      dx: 0,
      dy: 0,
      wrappedX: false,
      wrappedY: false,
      halfPeriodTieX: false,
      halfPeriodTieY: false,
      pathKind: "autapse_loop"
    };
  }
  const rawDx = exactRationalToBinary64(
    finiteBinary64ToMinSubnormalUnits(target.x) - finiteBinary64ToMinSubnormalUnits(source.x),
    1n,
    -1074
  );
  const rawDy = exactRationalToBinary64(
    finiteBinary64ToMinSubnormalUnits(target.y) - finiteBinary64ToMinSubnormalUnits(source.y),
    1n,
    -1074
  );
  if (!Number.isFinite(rawDx) || !Number.isFinite(rawDy)) {
    throw new Error("spatial chord displacement is outside finite binary64");
  }
  if (!domain || domain.edgeChordRule !== "minimum_image") {
    return {
      dx: rawDx,
      dy: rawDy,
      wrappedX: false,
      wrappedY: false,
      halfPeriodTieX: false,
      halfPeriodTieY: false,
      pathKind: "straight_chord"
    };
  }
  const x = domain.periodicX ? exactMinimumImageDisplacement(source.x, target.x, domain.periodX) : { displacement: rawDx, wrapped: false, tie: false };
  const y = domain.periodicY ? exactMinimumImageDisplacement(source.y, target.y, domain.periodY) : { displacement: rawDy, wrapped: false, tie: false };
  const wrapped = x.wrapped || y.wrapped;
  const tieAxes = `${x.tie ? "x" : ""}${y.tie ? "y" : ""}`;
  const pathKind = tieAxes === "" ? `minimum_image_${wrapped ? "wrapped" : "direct"}` : `minimum_image_${wrapped ? "wrapped" : "direct"}_half_period_tie_${tieAxes}`;
  return {
    dx: x.displacement,
    dy: y.displacement,
    wrappedX: x.wrapped,
    wrappedY: y.wrapped,
    halfPeriodTieX: x.tie,
    halfPeriodTieY: y.tie,
    pathKind
  };
}
function routeSpatialChord(source, target, domain, autapse = false) {
  const route = classifySpatialChord(source, target, domain, autapse);
  if (!domain || route.pathKind === "autapse_loop" || !route.wrappedX && !route.wrappedY) {
    return { ...route, segments: [[source, target]] };
  }
  const end = {
    x: exactBinary64Sum([source.x, route.dx]),
    y: exactBinary64Sum([source.y, route.dy])
  };
  if (!Number.isFinite(end.x) || !Number.isFinite(end.y)) {
    throw new Error("minimum-image route endpoint is outside finite binary64");
  }
  if (route.dx !== 0 && end.x === source.x || route.dy !== 0 && end.y === source.y) {
    throw new Error("minimum-image route displacement collapses at the coordinate origin");
  }
  const crossings = [];
  if (route.wrappedX && end.x < domain.xMin) {
    crossings.push({ t: (domain.xMin - source.x) / route.dx, shiftX: domain.periodX, shiftY: 0 });
  } else if (route.wrappedX && end.x > domain.xMax) {
    crossings.push({ t: (domain.xMax - source.x) / route.dx, shiftX: -domain.periodX, shiftY: 0 });
  }
  if (route.wrappedY && end.y < domain.yMin) {
    crossings.push({ t: (domain.yMin - source.y) / route.dy, shiftX: 0, shiftY: domain.periodY });
  } else if (route.wrappedY && end.y > domain.yMax) {
    crossings.push({ t: (domain.yMax - source.y) / route.dy, shiftX: 0, shiftY: -domain.periodY });
  }
  for (const crossing of crossings) {
    if (!Number.isFinite(crossing.t) || crossing.t < 0 || crossing.t > 1) {
      throw new Error("minimum-image boundary crossing is outside the routed chord");
    }
  }
  crossings.sort((left, right) => left.t - right.t);
  const grouped = [];
  for (const crossing of crossings) {
    const previous = grouped[grouped.length - 1];
    if (previous && crossing.t === previous.t) {
      previous.shiftX += crossing.shiftX;
      previous.shiftY += crossing.shiftY;
    } else {
      grouped.push({ ...crossing });
    }
  }
  const segments = [];
  let previousT = 0;
  let offsetX = 0;
  let offsetY = 0;
  for (const crossing of grouped) {
    const start = {
      x: source.x + route.dx * previousT + offsetX,
      y: source.y + route.dy * previousT + offsetY
    };
    const boundary = {
      x: source.x + route.dx * crossing.t + offsetX,
      y: source.y + route.dy * crossing.t + offsetY
    };
    if (start.x !== boundary.x || start.y !== boundary.y) segments.push([start, boundary]);
    offsetX += crossing.shiftX;
    offsetY += crossing.shiftY;
    previousT = crossing.t;
  }
  const finalStart = {
    x: source.x + route.dx * previousT + offsetX,
    y: source.y + route.dy * previousT + offsetY
  };
  if (finalStart.x !== target.x || finalStart.y !== target.y) segments.push([finalStart, target]);
  if (segments.length === 0) {
    throw new Error("minimum-image route collapsed to zero visible length");
  }
  return { ...route, segments };
}
function reverseSpatialSegments(segments) {
  return [...segments].reverse().map(([from, to]) => [to, from]);
}

// src/analysis/responseCurve.ts
var RESPONSE_CURVE_METHODS = [
  "mean_firing_rate",
  "peak_firing_rate",
  "first_spike_latency",
  "event_count"
];
function issue(code, path, message) {
  return { ok: false, issue: { code, path, message } };
}
function codeUnitCompare(left, right) {
  return left < right ? -1 : left > right ? 1 : 0;
}
function numericCompare(left, right) {
  return left < right ? -1 : left > right ? 1 : 0;
}
function exactResponseCurveMean(values) {
  if (values.length === 0) throw new Error("a mean requires at least one retained value");
  for (const value of values) {
    if (!Number.isFinite(value)) throw new Error("response values must be finite");
  }
  return exactBinary64Mean(values);
}
function responseCurveEstimate(retained, estimator, trimFraction) {
  for (const value of retained) {
    if (!Number.isFinite(value)) throw new Error("response values must be finite");
  }
  if (retained.length === 0) return null;
  if (estimator === "mean") return exactResponseCurveMean(retained);
  const ordered = [...retained].sort(numericCompare);
  if (estimator === "median") {
    const middle = Math.floor(ordered.length / 2);
    const estimate = ordered.length % 2 === 1 ? ordered[middle] : exactResponseCurveMean([ordered[middle - 1], ordered[middle]]);
    return estimate === 0 ? 0 : estimate;
  }
  if (estimator === "trimmed_mean") {
    if (trimFraction === void 0 || !(trimFraction >= 0 && trimFraction < 0.5)) {
      throw new Error("trimmed_mean requires a finite trimFraction in [0, 0.5)");
    }
    const trimPerTail = floorExactBinary64TimesSafeInteger(trimFraction, ordered.length);
    const retainedAfterTrim = ordered.slice(trimPerTail, ordered.length - trimPerTail);
    if (retainedAfterTrim.length === 0) {
      throw new Error("trimmed_mean removed every retained response");
    }
    return exactResponseCurveMean(retainedAfterTrim);
  }
  throw new Error(`unknown response-curve estimator: ${String(estimator)}`);
}
function canonicalZero(value) {
  return value === 0 ? 0 : value;
}
function validateResponseValue(value, method, mode, path) {
  if (value === null) return null;
  if (!Number.isFinite(value)) {
    return issue("invalid_numeric", path, "response values must be finite or null");
  }
  if (method === "mean_firing_rate" || method === "peak_firing_rate") {
    if (value < 0) {
      return issue(
        "invalid_response_value",
        path,
        `${method} is a firing rate and cannot be negative`
      );
    }
  } else if (method === "first_spike_latency") {
    if (value < 0) {
      return issue(
        "invalid_response_value",
        path,
        "a defined first-spike latency must be non-negative; zero means the first event occurred exactly at the included measurement-window start, while null means no spike occurred"
      );
    }
  } else if (method === "event_count") {
    if (mode === "repeats" && (!Number.isSafeInteger(value) || value < 0)) {
      return issue(
        "count_not_integer",
        path,
        "a raw repeat event count must be an exact non-negative safe integer"
      );
    }
    if (mode === "aggregates" && value < 0) {
      return issue(
        "invalid_response_value",
        path,
        "an aggregate estimator over event counts may be fractional but cannot be negative"
      );
    }
  }
  return canonicalZero(value);
}
function isFailure(value) {
  return typeof value === "object" && value !== null && "ok" in value && value.ok === false;
}
function deriveResponseCurve(input) {
  const { conditions } = input;
  if (!["numeric", "ordinal", "nominal"].includes(conditions.axis)) {
    return issue("invalid_estimator", "/data/conditions/axis", "response-curve axis is not registered");
  }
  if (!["mean", "median", "trimmed_mean"].includes(input.estimator)) {
    return issue("invalid_estimator", "/parameters/estimator", "response-curve estimator is not registered");
  }
  if (!RESPONSE_CURVE_METHODS.includes(input.responseMethod)) {
    return issue("invalid_response_value", "/parameters/responseMethod", "response method is not registered");
  }
  if (!["independent", "paired"].includes(input.repeatDesign)) {
    return issue("paired_repeats_incomplete", "/parameters/repeatDesign", "repeat design is not registered");
  }
  if (input.estimator === "trimmed_mean" && (input.trimFraction === void 0 || !(input.trimFraction >= 0 && input.trimFraction < 0.5))) {
    return issue(
      "invalid_estimator",
      "/parameters/trimFraction",
      "trimmed_mean requires a finite trimFraction in [0, 0.5)"
    );
  }
  if (input.estimator !== "trimmed_mean" && input.trimFraction !== void 0) {
    return issue(
      "invalid_estimator",
      "/parameters/trimFraction",
      "trimFraction is meaningful only for the trimmed_mean estimator"
    );
  }
  if (conditions.ids.length === 0) {
    return issue("length_mismatch", "/data/conditions/ids", "the declared condition universe is empty");
  }
  if (conditions.labels && conditions.labels.length !== conditions.ids.length) {
    return issue(
      "length_mismatch",
      "/data/conditions/labels",
      "condition labels must be parallel to condition ids"
    );
  }
  if (conditions.axis === "numeric") {
    if (!conditions.inputValues || conditions.inputValues.length !== conditions.ids.length) {
      return issue(
        "length_mismatch",
        "/data/conditions/input/values",
        "numeric condition inputs must be parallel to condition ids"
      );
    }
    const seenInputs = /* @__PURE__ */ new Map();
    for (let index = 0; index < conditions.inputValues.length; index++) {
      const value = conditions.inputValues[index];
      if (!Number.isFinite(value)) {
        return issue("invalid_numeric", `/data/conditions/input/values/${index}`, "condition inputs must be finite");
      }
      if (conditions.inputScale === "log10" && !(value > 0)) {
        return issue(
          "nonpositive_log_input",
          `/data/conditions/input/values/${index}`,
          "a log10 response-curve input axis requires every declared condition input to be strictly positive"
        );
      }
      const prior = seenInputs.get(value);
      if (prior !== void 0) {
        return issue(
          "duplicate_input",
          `/data/conditions/input/values/${index}`,
          `numeric input ${value} is declared by more than one condition (indices ${prior} and ${index}); equal-input observations belong under one condition unless another explicit series factor exists`
        );
      }
      seenInputs.set(value, index);
    }
  }
  const declaredIndex = /* @__PURE__ */ new Map();
  for (let index = 0; index < conditions.ids.length; index++) {
    const id = conditions.ids[index];
    if (declaredIndex.has(id)) {
      return issue(
        "duplicate_condition",
        `/data/conditions/ids/${index}`,
        `condition id ${JSON.stringify(id)} appears more than once`
      );
    }
    declaredIndex.set(id, index);
  }
  const conditionOrder = conditions.ids.map((_id, index) => index);
  if (conditions.axis === "numeric") {
    const values = conditions.inputValues;
    conditionOrder.sort((left, right) => numericCompare(values[left], values[right]) || left - right);
  }
  const displayIndex = /* @__PURE__ */ new Map();
  conditionOrder.forEach((declaredOrdinal, displayOrdinal) => {
    displayIndex.set(declaredOrdinal, displayOrdinal);
  });
  const rawRepeatsSupplied = input.repeats !== void 0;
  if (rawRepeatsSupplied === (input.aggregates !== void 0)) {
    return issue(
      "length_mismatch",
      "/data",
      "exactly one of raw repeats or per-condition aggregates must be supplied"
    );
  }
  if (input.binnedPeakAudit && (!input.repeats || input.responseMethod !== "peak_firing_rate")) {
    return issue(
      "rate_audit_incoherent",
      "/data/observations/response/audit/peakBinCounts",
      "a binned-peak count audit is legal only for raw peak_firing_rate repeats"
    );
  }
  const repeatsByDeclaredCondition = conditions.ids.map(() => []);
  let aggregateValues;
  let aggregateSamples;
  let aggregateExcluded;
  if (input.repeats) {
    const repeats = input.repeats;
    const binnedPeakAudit = input.binnedPeakAudit;
    if (binnedPeakAudit && binnedPeakAudit.peakBinCounts.length !== repeats.responses.length) {
      return issue(
        "length_mismatch",
        "/data/observations/response/audit/peakBinCounts",
        "peakBinCounts must be parallel to raw binned-peak response values"
      );
    }
    if (repeats.attemptedCounts.length !== conditions.ids.length) {
      return issue(
        "length_mismatch",
        "/data/observations/attemptedCounts",
        "declared attempted counts must be parallel to the condition universe"
      );
    }
    for (let index = 0; index < repeats.attemptedCounts.length; index++) {
      const count = repeats.attemptedCounts[index];
      if (!Number.isSafeInteger(count) || count < 0) {
        return issue(
          "invalid_numeric",
          `/data/observations/attemptedCounts/${index}`,
          "declared attempted counts must be exact non-negative safe integers"
        );
      }
    }
    if (repeats.conditionIds.length !== repeats.repeatIds.length || repeats.conditionIds.length !== repeats.responses.length) {
      return issue(
        "length_mismatch",
        "/data/observations",
        "conditionIds, repeatIds, and response.values must have identical lengths"
      );
    }
    const seenByCondition = /* @__PURE__ */ new Map();
    for (let sourceOrdinal = 0; sourceOrdinal < repeats.conditionIds.length; sourceOrdinal++) {
      const conditionId = repeats.conditionIds[sourceOrdinal];
      const ordinal = declaredIndex.get(conditionId);
      if (ordinal === void 0) {
        return issue(
          "unknown_condition",
          `/data/observations/conditionIds/${sourceOrdinal}`,
          `observation references undeclared condition ${JSON.stringify(conditionId)}`
        );
      }
      const repeatId = repeats.repeatIds[sourceOrdinal];
      const seen = seenByCondition.get(conditionId) ?? /* @__PURE__ */ new Set();
      if (seen.has(repeatId)) {
        return issue(
          "duplicate_repeat",
          `/data/observations/repeatIds/${sourceOrdinal}`,
          `repeat ${JSON.stringify(repeatId)} appears more than once in condition ${JSON.stringify(conditionId)}`
        );
      }
      seen.add(repeatId);
      seenByCondition.set(conditionId, seen);
      const checkedResponse = validateResponseValue(
        repeats.responses[sourceOrdinal],
        input.responseMethod,
        "repeats",
        `/data/observations/response/values/${sourceOrdinal}`
      );
      if (isFailure(checkedResponse)) return checkedResponse;
      const response = checkedResponse;
      if (binnedPeakAudit) {
        const peakBinCount = binnedPeakAudit.peakBinCounts[sourceOrdinal];
        if (peakBinCount === null !== (response === null)) {
          return issue(
            "rate_audit_incoherent",
            `/data/observations/response/audit/peakBinCounts/${sourceOrdinal}`,
            "a peak-bin count must be null exactly where its raw binned-peak rate is null"
          );
        }
        if (peakBinCount !== null && (!Number.isSafeInteger(peakBinCount) || peakBinCount < 0)) {
          return issue(
            "count_not_integer",
            `/data/observations/response/audit/peakBinCounts/${sourceOrdinal}`,
            "a peak-bin count must be an exact non-negative safe integer"
          );
        }
        if (peakBinCount !== null && response !== null) {
          let expectedRate;
          try {
            expectedRate = deriveExactAggregateCountRateInUnit(
              BigInt(peakBinCount),
              binnedPeakAudit.integerDivisor,
              1,
              binnedPeakAudit.binWidthValue,
              binnedPeakAudit.binWidthUnit,
              binnedPeakAudit.rateUnit
            );
          } catch (error) {
            return issue(
              "rate_audit_incoherent",
              `/data/observations/response/values/${sourceOrdinal}`,
              `raw binned-peak rate cannot be re-derived from its exact max-bin count (${error instanceof Error ? error.message : "numeric failure"})`
            );
          }
          if ((response === 0 ? 0 : response) !== expectedRate) {
            return issue(
              "rate_audit_incoherent",
              `/data/observations/response/values/${sourceOrdinal}`,
              `raw binned-peak rate ${response} ${binnedPeakAudit.rateUnit} does not equal the one-round exact rate ${expectedRate} ${binnedPeakAudit.rateUnit} derived from max-bin count ${peakBinCount}`
            );
          }
        }
      }
      repeatsByDeclaredCondition[ordinal].push({
        conditionId,
        repeatId,
        response,
        estimatorRole: response === null ? "undefined" : "retained",
        sourceOrdinal
      });
    }
    if (input.repeatDesign === "paired") {
      const referenceConditionId = conditions.ids[0];
      const referenceIds = seenByCondition.get(referenceConditionId) ?? /* @__PURE__ */ new Set();
      const referenceSorted = [...referenceIds].sort(codeUnitCompare);
      for (let ordinal = 1; ordinal < conditions.ids.length; ordinal++) {
        const conditionId = conditions.ids[ordinal];
        const candidate = seenByCondition.get(conditionId) ?? /* @__PURE__ */ new Set();
        const missing = referenceSorted.filter((repeatId) => !candidate.has(repeatId));
        const extra = [...candidate].filter((repeatId) => !referenceIds.has(repeatId)).sort(codeUnitCompare);
        if (missing.length > 0 || extra.length > 0) {
          const details = [
            missing.length > 0 ? `missing ${missing.map((value) => JSON.stringify(value)).join(", ")}` : "",
            extra.length > 0 ? `has unmatched ${extra.map((value) => JSON.stringify(value)).join(", ")}` : ""
          ].filter((part) => part.length > 0).join(" and ");
          return issue(
            "paired_repeats_incomplete",
            "/data/observations/repeatIds",
            `paired condition ${JSON.stringify(conditionId)} ${details} relative to ${JSON.stringify(referenceConditionId)}; every paired repeat id must occur at every condition`
          );
        }
      }
    }
    for (const rows of repeatsByDeclaredCondition) {
      rows.sort(
        (left, right) => codeUnitCompare(left.repeatId, right.repeatId) || left.sourceOrdinal - right.sourceOrdinal
      );
    }
    for (let ordinal = 0; ordinal < conditions.ids.length; ordinal++) {
      if (repeatsByDeclaredCondition[ordinal].length !== repeats.attemptedCounts[ordinal]) {
        return issue(
          "attempted_count_mismatch",
          `/data/observations/attemptedCounts/${ordinal}`,
          `condition ${JSON.stringify(conditions.ids[ordinal])} declares ${repeats.attemptedCounts[ordinal]} attempted repeats but supplies ${repeatsByDeclaredCondition[ordinal].length} rows`
        );
      }
    }
  } else {
    const aggregates = input.aggregates;
    if (input.estimator === "trimmed_mean") {
      if (!aggregates.trimmedCounts || aggregates.trimmedCounts.length !== conditions.ids.length) {
        return issue(
          "trimmed_count_incoherent",
          "/data/aggregates/trimmedCounts",
          "trimmed_mean aggregate input requires one total symmetric-tail trimmed count per condition"
        );
      }
    } else if (aggregates.trimmedCounts !== void 0) {
      return issue(
        "trimmed_count_incoherent",
        "/data/aggregates/trimmedCounts",
        "trimmedCounts is meaningful only for the trimmed_mean estimator"
      );
    }
    if (aggregates.responses.length !== conditions.ids.length || aggregates.sampleCounts.length !== conditions.ids.length || aggregates.excludedCounts.length !== conditions.ids.length) {
      return issue(
        "length_mismatch",
        "/data/aggregates",
        "aggregate responses, sampleCounts, and excludedCounts must be parallel to the declared condition universe"
      );
    }
    for (let index = 0; index < conditions.ids.length; index++) {
      const checkedResponse = validateResponseValue(
        aggregates.responses[index],
        input.responseMethod,
        "aggregates",
        `/data/aggregates/response/values/${index}`
      );
      if (isFailure(checkedResponse)) return checkedResponse;
      const response = checkedResponse;
      const sampleCount = aggregates.sampleCounts[index];
      const excludedCount2 = aggregates.excludedCounts[index];
      const trimmedCount2 = aggregates.trimmedCounts?.[index] ?? 0;
      if (response === null !== (sampleCount === null)) {
        return issue(
          "length_mismatch",
          `/data/aggregates/sampleCounts/${index}`,
          "aggregate response and retained sample count must share one missingness mask"
        );
      }
      if (sampleCount !== null && (!Number.isSafeInteger(sampleCount) || sampleCount < 1)) {
        return issue("invalid_numeric", `/data/aggregates/sampleCounts/${index}`, "retained sample counts must be positive safe integers");
      }
      if (!Number.isSafeInteger(excludedCount2) || excludedCount2 < 0) {
        return issue("invalid_numeric", `/data/aggregates/excludedCounts/${index}`, "excluded counts must be non-negative safe integers");
      }
      if (!Number.isSafeInteger(trimmedCount2) || trimmedCount2 < 0 || trimmedCount2 % 2 !== 0) {
        return issue(
          "trimmed_count_incoherent",
          `/data/aggregates/trimmedCounts/${index}`,
          "a total symmetric-tail trimmed count must be an exact non-negative even safe integer"
        );
      }
      if (sampleCount === null && trimmedCount2 !== 0) {
        return issue(
          "trimmed_count_incoherent",
          `/data/aggregates/trimmedCounts/${index}`,
          "a condition with no aggregate estimate cannot claim trimmed defined observations"
        );
      }
      const pretrimDefinedCount = (sampleCount ?? 0) + trimmedCount2;
      if (!Number.isSafeInteger(pretrimDefinedCount)) {
        return issue(
          "invalid_numeric",
          `/data/aggregates/trimmedCounts/${index}`,
          "pre-trim defined sample count exceeds the exact integer range"
        );
      }
      if (input.estimator === "trimmed_mean") {
        const expectedTrimmedCount = 2 * floorExactBinary64TimesSafeInteger(
          input.trimFraction,
          pretrimDefinedCount
        );
        if (trimmedCount2 !== expectedTrimmedCount) {
          return issue(
            "trimmed_count_incoherent",
            `/data/aggregates/trimmedCounts/${index}`,
            `trimmed count ${trimmedCount2} does not equal 2 * floor_exact((${sampleCount ?? 0} + ${trimmedCount2}) * ${input.trimFraction}) = ${expectedTrimmedCount}`
          );
        }
      }
      if (!Number.isSafeInteger(pretrimDefinedCount + excludedCount2)) {
        return issue("invalid_numeric", `/data/aggregates/excludedCounts/${index}`, "attempted sample count exceeds the exact integer range");
      }
      if (input.responseMethod === "event_count" && response !== null && sampleCount !== null) {
        const estimatorDenominator = input.estimator === "median" ? sampleCount % 2 === 0 ? 2 : 1 : sampleCount;
        if (!isRoundedMeanOfSafeNonnegativeIntegers(response, estimatorDenominator)) {
          return issue(
            "count_estimator_incoherent",
            `/data/aggregates/response/values/${index}`,
            `aggregate event-count value ${response} cannot be the correctly rounded ${input.estimator} of ${sampleCount} exact non-negative safe-integer counts under the declared estimator convention`
          );
        }
      }
    }
    aggregateValues = aggregates.responses.map(
      (response) => response === null ? null : canonicalZero(response)
    );
    aggregateSamples = aggregates.sampleCounts;
    aggregateExcluded = aggregates.excludedCounts;
  }
  const derivedConditions = [];
  let attemptedCount = 0;
  let retainedCount = 0;
  let trimmedCount = 0;
  let excludedCount = 0;
  let conditionsWithoutEstimate = 0;
  for (const declaredOrdinal of conditionOrder) {
    const rows = repeatsByDeclaredCondition[declaredOrdinal];
    let estimate;
    let retained;
    let sampleCount;
    let trimmed;
    let excluded;
    let attempted;
    let resultRows;
    if (input.repeats) {
      const binnedPeakAudit = input.binnedPeakAudit;
      const definedRows = rows.filter(
        (row) => row.response !== null
      ).sort(
        (left, right) => numericCompare(
          binnedPeakAudit ? binnedPeakAudit.peakBinCounts[left.sourceOrdinal] : left.response,
          binnedPeakAudit ? binnedPeakAudit.peakBinCounts[right.sourceOrdinal] : right.response
        ) || codeUnitCompare(left.repeatId, right.repeatId) || left.sourceOrdinal - right.sourceOrdinal
      );
      const trimPerTail = input.estimator === "trimmed_mean" ? floorExactBinary64TimesSafeInteger(input.trimFraction, definedRows.length) : 0;
      const roleBySourceOrdinal = /* @__PURE__ */ new Map();
      for (let index = 0; index < definedRows.length; index++) {
        roleBySourceOrdinal.set(
          definedRows[index].sourceOrdinal,
          index < trimPerTail ? "trimmed_low" : index >= definedRows.length - trimPerTail ? "trimmed_high" : "retained"
        );
      }
      resultRows = rows.map((row) => ({
        ...row,
        estimatorRole: row.response === null ? "undefined" : roleBySourceOrdinal.get(row.sourceOrdinal)
      }));
      const definedValues = rows.map((row) => row.response).filter((value) => value !== null);
      trimmed = 2 * trimPerTail;
      retained = definedValues.length - trimmed;
      sampleCount = retained;
      excluded = rows.length - definedValues.length;
      attempted = rows.length;
      try {
        if (binnedPeakAudit) {
          const orderedCounts = definedRows.map(
            (row) => binnedPeakAudit.peakBinCounts[row.sourceOrdinal]
          );
          if (orderedCounts.length === 0) {
            estimate = null;
          } else {
            let estimatorCounts;
            let estimatorDenominator;
            if (input.estimator === "median") {
              const middle = Math.floor(orderedCounts.length / 2);
              estimatorCounts = orderedCounts.length % 2 === 1 ? [orderedCounts[middle]] : [orderedCounts[middle - 1], orderedCounts[middle]];
              estimatorDenominator = estimatorCounts.length;
            } else {
              estimatorCounts = orderedCounts.slice(
                trimPerTail,
                orderedCounts.length - trimPerTail
              );
              estimatorDenominator = estimatorCounts.length;
            }
            const countTotal = estimatorCounts.reduce(
              (total, count) => total + BigInt(count),
              0n
            );
            estimate = deriveExactAggregateCountRateInUnit(
              countTotal,
              binnedPeakAudit.integerDivisor,
              estimatorDenominator,
              binnedPeakAudit.binWidthValue,
              binnedPeakAudit.binWidthUnit,
              binnedPeakAudit.rateUnit
            );
          }
        } else {
          estimate = responseCurveEstimate(definedValues, input.estimator, input.trimFraction);
        }
      } catch (error) {
        const message = error instanceof Error ? error.message : "response-curve estimator failed";
        return issue(
          "invalid_numeric",
          "/data/observations/response/values",
          `condition ${JSON.stringify(conditions.ids[declaredOrdinal])} cannot be estimated in finite binary64: ${message}`
        );
      }
    } else {
      estimate = aggregateValues[declaredOrdinal];
      sampleCount = aggregateSamples[declaredOrdinal];
      retained = sampleCount ?? 0;
      trimmed = input.aggregates.trimmedCounts?.[declaredOrdinal] ?? 0;
      excluded = aggregateExcluded[declaredOrdinal];
      attempted = retained + trimmed + excluded;
      resultRows = rows;
    }
    attemptedCount += attempted;
    retainedCount += retained;
    trimmedCount += trimmed;
    excludedCount += excluded;
    if (!Number.isSafeInteger(attemptedCount) || !Number.isSafeInteger(retainedCount) || !Number.isSafeInteger(trimmedCount) || !Number.isSafeInteger(excludedCount)) {
      return issue(
        "invalid_numeric",
        "/data/aggregates",
        "response-curve total accounting exceeds the exact safe-integer range"
      );
    }
    if (estimate === null) conditionsWithoutEstimate++;
    derivedConditions.push({
      conditionId: conditions.ids[declaredOrdinal],
      conditionLabel: conditions.labels?.[declaredOrdinal] ?? conditions.ids[declaredOrdinal],
      input: conditions.axis === "numeric" ? conditions.inputValues[declaredOrdinal] : null,
      declaredOrdinal,
      displayOrdinal: displayIndex.get(declaredOrdinal),
      estimate,
      attemptedCount: attempted,
      sampleCount,
      retainedCount: retained,
      trimmedCount: trimmed,
      excludedCount: excluded,
      repeats: resultRows
    });
  }
  const sortedRepeats = derivedConditions.flatMap((condition) => condition.repeats);
  return {
    ok: true,
    result: {
      axis: conditions.axis,
      inputScale: conditions.axis === "numeric" ? conditions.inputScale ?? "linear" : null,
      conditions: derivedConditions,
      sortedRepeats,
      attemptedCount,
      retainedCount,
      trimmedCount,
      excludedCount,
      conditionsWithoutEstimate,
      rawRepeatsSupplied
    }
  };
}

// src/analysis/traces.ts
function mean(values) {
  return exactBinary64Mean(values);
}
function aggregate(values, method) {
  const finite = values.filter((value) => value !== null && Number.isFinite(value));
  if (finite.length === 0) return null;
  switch (method) {
    case "mean":
      return mean(finite);
    case "min": {
      let result = finite[0];
      for (let i = 1; i < finite.length; i++) if (finite[i] < result) result = finite[i];
      return result === 0 ? 0 : result;
    }
    case "max": {
      let result = finite[0];
      for (let i = 1; i < finite.length; i++) if (finite[i] > result) result = finite[i];
      return result === 0 ? 0 : result;
    }
    case "median": {
      const ordered = [...finite].sort((a, b) => a - b);
      const middle = Math.floor(ordered.length / 2);
      const result = ordered.length % 2 === 1 ? ordered[middle] : mean([ordered[middle - 1], ordered[middle]]);
      return result === 0 ? 0 : result;
    }
    default:
      throw new Error(`unknown trace aggregate method: ${String(method)}`);
  }
}
function convertWithoutInventing(value, from, to) {
  return from === to ? value : convert(value, from, to);
}
function canonicalZero2(value) {
  return value === 0 ? 0 : value;
}
function insideWindow(value, window) {
  return value >= window.start && (window.finalEdgeInclusive ? value <= window.stop : value < window.stop);
}
function windowPosition(value, window) {
  if (value < window.start) return -1;
  if (window.finalEdgeInclusive ? value > window.stop : value >= window.stop) return 1;
  return 0;
}
function exactWindowPosition(sourceTime, sourceUnit, offset, window) {
  const terms = [
    { value: sourceTime, unit: sourceUnit },
    ...offset ? [{ value: offset.value, unit: offset.unit }] : []
  ];
  const start = compareExactUnitSumToValue(terms, { value: window.start, unit: window.unit });
  if (start < 0) return -1;
  const stop = compareExactUnitSumToValue(terms, { value: window.stop, unit: window.unit });
  if (window.finalEdgeInclusive ? stop > 0 : stop >= 0) return 1;
  return 0;
}
function stableCenteredRatio(value, center, denominator) {
  const directDifference = value - center;
  if (Number.isFinite(directDifference)) return directDifference / denominator;
  const magnitude = Math.max(Math.abs(value), Math.abs(center), Math.abs(denominator));
  if (magnitude === 0) return 0;
  return (value / magnitude - center / magnitude) / (denominator / magnitude);
}
function zCoordinateSystem(values) {
  let reference = values[0];
  for (let index = 1; index < values.length; index++) {
    if (values[index] < reference) reference = values[index];
  }
  const differences = values.map((value) => value - reference);
  if (differences.every(Number.isFinite)) {
    let scale2 = 0;
    for (const difference of differences) scale2 = Math.max(scale2, Math.abs(difference));
    if (scale2 > 0) {
      return {
        mode: "centered_difference",
        reference,
        scale: scale2,
        coordinates: differences.map((difference) => difference / scale2)
      };
    }
  }
  let scale = 0;
  for (const value of values) scale = Math.max(scale, Math.abs(value));
  if (!(scale > 0) || !Number.isFinite(scale)) {
    throw new Error("z_score requires distinct finite values");
  }
  return {
    mode: "scaled_value",
    reference: 0,
    scale,
    coordinates: values.map((value) => value / scale)
  };
}
function zCoordinate(value, receipt) {
  const scale = receipt.zInputScale;
  return receipt.zCoordinateMode === "centered_difference" ? stableCenteredRatio(value, receipt.zCoordinateReference, scale) : value / scale;
}
var EFFECT_RELATIVE_EPSILON_MULTIPLES = 8;
function binary64EffectWasPreserved(declared, applied, epsilonMultiples = EFFECT_RELATIVE_EPSILON_MULTIPLES) {
  return binary64RelativeDifferenceWithinEpsilons(declared, applied, epsilonMultiples);
}
function assertStrictAffineOrdering(sourceValues, transformedValues, transformDifference, effectEpsilonMultiples = EFFECT_RELATIVE_EPSILON_MULTIPLES) {
  if (sourceValues.length !== transformedValues.length) {
    throw new Error("trace affine transform source and output lengths differ");
  }
  const pairs = sourceValues.map((source, index) => ({ source, transformed: transformedValues[index] }));
  pairs.sort((a, b) => a.source - b.source || a.transformed - b.transformed);
  for (let index = 1; index < pairs.length; index++) {
    const previous = pairs[index - 1];
    const current = pairs[index];
    if (current.source > previous.source && !(current.transformed > previous.transformed)) {
      throw new Error("trace affine transform collapsed distinct values below binary64 resolution");
    }
    if (current.source > previous.source && transformDifference) {
      let expectedDifference;
      try {
        expectedDifference = transformDifference(previous.source, current.source);
      } catch (error) {
        if (error instanceof Error && error.message.includes("overflowed binary64")) continue;
        throw error;
      }
      const actualDifference = current.transformed - previous.transformed;
      if (!(expectedDifference > 0) || !Number.isFinite(expectedDifference) || !Number.isFinite(actualDifference) || !binary64EffectWasPreserved(expectedDifference, actualDifference, effectEpsilonMultiples)) {
        throw new Error("trace affine transform materially distorted distinct values at binary64 resolution");
      }
    }
  }
}
function assertNormalizationPostconditions(method, basisValues, transformedBasis) {
  assertStrictAffineOrdering(basisValues, transformedBasis);
  if (method === "z_score") {
    const sum2 = exactBinary64Sum(transformedBasis);
    const sumSquares = exactBinary64Sum(transformedBasis.map((value) => value * value));
    const expectedSquares = transformedBasis.length - 1;
    const sumTolerance = 16 * Number.EPSILON * Math.sqrt(
      transformedBasis.length * Math.max(0, transformedBasis.length - 1)
    );
    const squareTolerance = 32 * Number.EPSILON * Math.max(1, expectedSquares);
    if (!Number.isFinite(sum2) || !Number.isFinite(sumSquares) || Math.abs(sum2) > Math.max(Number.MIN_VALUE, sumTolerance) || Math.abs(sumSquares - expectedSquares) > squareTolerance) {
      throw new Error("z_score normalization identities are unrepresentable at binary64 resolution");
    }
    return;
  }
  if (method === "min_max") {
    let minimum = transformedBasis[0];
    let maximum = transformedBasis[0];
    for (let index = 1; index < transformedBasis.length; index++) {
      if (transformedBasis[index] < minimum) minimum = transformedBasis[index];
      if (transformedBasis[index] > maximum) maximum = transformedBasis[index];
    }
    if (minimum !== 0 || maximum !== 1) {
      throw new Error("min_max normalization endpoints are unrepresentable at binary64 resolution");
    }
    return;
  }
  const sum = exactBinary64Sum(transformedBasis);
  const tolerance = 16 * Number.EPSILON * transformedBasis.length;
  if (!Number.isFinite(sum) || Math.abs(sum - transformedBasis.length) > tolerance) {
    throw new Error("baseline normalization identity is unrepresentable at binary64 resolution");
  }
}
function applyTraceNormalization(value, receipt, dispersion = false) {
  let result;
  if (receipt.method === "z_score") {
    if (receipt.zInputScale && receipt.zCoordinateSampleStandardDeviation) {
      const coordinateDeviation = receipt.zCoordinateSampleStandardDeviation;
      result = dispersion ? value / receipt.zInputScale / coordinateDeviation : stableCenteredRatio(
        zCoordinate(value, receipt),
        receipt.zCoordinateMean,
        coordinateDeviation
      );
    } else {
      const deviation = receipt.sampleStandardDeviation;
      result = dispersion ? value / deviation : stableCenteredRatio(value, receipt.center, deviation);
    }
  } else if (receipt.method === "min_max") {
    const minimum = receipt.minimum;
    const maximum = receipt.maximum;
    result = exactBinary64AffineFraction(
      value,
      dispersion ? 0 : minimum,
      minimum,
      maximum
    );
  } else {
    result = receipt.baselineSumMinSubnormalUnits ? exactBinary64RatioToMean(
      value,
      receipt.baselineSumMinSubnormalUnits,
      receipt.basisCount
    ) : value / receipt.baselineMean;
  }
  const collapsedNonZero = result === 0 && (dispersion ? value !== 0 : receipt.method === "z_score" ? receipt.zCoordinateMode ? zCoordinate(value, receipt) !== receipt.zCoordinateMean : value !== receipt.center : receipt.method === "min_max" ? value !== receipt.minimum : value !== 0);
  if (!Number.isFinite(result) || collapsedNonZero) {
    throw new Error("trace normalization produced an unrepresentable binary64 value");
  }
  return result;
}
function normalizeValues(samples, normalization) {
  const basis = samples.filter((sample) => insideWindow(sample.displayTime, normalization.statisticsWindow)).map((sample) => sample.value).filter((value) => value !== null && Number.isFinite(value));
  if (normalization.method === "z_score") {
    if (basis.length < 2) throw new Error("z_score requires at least two non-missing samples");
    const coordinateSystem = zCoordinateSystem(basis);
    const coordinateMean = roundedBinary64Mean(coordinateSystem.coordinates);
    const squared = exactBinary64Sum(coordinateSystem.coordinates.map((coordinate) => {
      const difference = coordinate - coordinateMean;
      return difference * difference;
    }));
    const coordinateDeviation = Math.sqrt(squared / (basis.length - 1));
    if (!(coordinateDeviation > 0) || !Number.isFinite(coordinateDeviation)) {
      throw new Error("z_score requires a positive finite sample standard deviation");
    }
    const center = roundedBinary64Mean(basis);
    const deviation = coordinateSystem.scale * coordinateDeviation;
    const receipt2 = {
      method: normalization.method,
      statisticsWindow: normalization.statisticsWindow,
      basisCount: basis.length,
      center,
      ...deviation > 0 && Number.isFinite(deviation) ? { sampleStandardDeviation: deviation } : {},
      zCoordinateMode: coordinateSystem.mode,
      zCoordinateReference: coordinateSystem.reference,
      zInputScale: coordinateSystem.scale,
      zCoordinateMean: coordinateMean,
      zCoordinateSampleStandardDeviation: coordinateDeviation
    };
    const values2 = samples.map((sample) => sample.value === null ? null : applyTraceNormalization(sample.value, receipt2));
    assertStrictAffineOrdering(
      samples.filter((sample) => sample.value !== null).map((sample) => sample.value),
      values2.filter((value) => value !== null)
    );
    const transformedBasis2 = samples.map((sample, index) => ({ sample, value: values2[index] })).filter(
      (entry) => insideWindow(entry.sample.displayTime, normalization.statisticsWindow) && entry.sample.value !== null && entry.value !== null
    ).map((entry) => entry.value);
    assertNormalizationPostconditions(normalization.method, basis, transformedBasis2);
    return {
      values: values2,
      receipt: receipt2
    };
  }
  if (normalization.method === "min_max") {
    if (basis.length < 1) throw new Error("min_max requires a non-missing statistics sample");
    let minimum = basis[0];
    let maximum = basis[0];
    for (let i = 1; i < basis.length; i++) {
      if (basis[i] < minimum) minimum = basis[i];
      if (basis[i] > maximum) maximum = basis[i];
    }
    const magnitude = Math.max(Math.abs(minimum), Math.abs(maximum));
    const normalizedMinimum = magnitude === 0 ? 0 : minimum / magnitude;
    const normalizedMaximum = magnitude === 0 ? 0 : maximum / magnitude;
    const normalizedSpan = normalizedMaximum - normalizedMinimum;
    if (!(normalizedSpan > 0) || !Number.isFinite(normalizedSpan)) {
      throw new Error("min_max requires distinct finite extrema");
    }
    const receipt2 = {
      method: normalization.method,
      statisticsWindow: normalization.statisticsWindow,
      basisCount: basis.length,
      minimum,
      maximum
    };
    const values2 = samples.map((sample) => sample.value === null ? null : applyTraceNormalization(sample.value, receipt2));
    assertStrictAffineOrdering(
      samples.filter((sample) => sample.value !== null).map((sample) => sample.value),
      values2.filter((value) => value !== null)
    );
    const transformedBasis2 = samples.map((sample, index) => ({ sample, value: values2[index] })).filter(
      (entry) => insideWindow(entry.sample.displayTime, normalization.statisticsWindow) && entry.sample.value !== null && entry.value !== null
    ).map((entry) => entry.value);
    assertNormalizationPostconditions(normalization.method, basis, transformedBasis2);
    return {
      values: values2,
      receipt: receipt2
    };
  }
  if (basis.length < 1) throw new Error("baseline normalization requires a non-missing sample");
  const baselineSumMinSubnormalUnits = exactBinary64SumUnits(basis);
  if (BigInt(baselineSumMinSubnormalUnits) <= 0n) {
    throw new Error("baseline normalization requires a strictly positive finite mean");
  }
  const baseline = roundedBinary64Mean(basis);
  const receipt = {
    method: normalization.method,
    statisticsWindow: normalization.statisticsWindow,
    basisCount: basis.length,
    ...baseline > 0 && Number.isFinite(baseline) ? { baselineMean: baseline } : {},
    baselineSumMinSubnormalUnits
  };
  const values = samples.map((sample) => sample.value === null ? null : applyTraceNormalization(sample.value, receipt));
  assertStrictAffineOrdering(
    samples.filter((sample) => sample.value !== null).map((sample) => sample.value),
    values.filter((value) => value !== null)
  );
  const transformedBasis = samples.map((sample, index) => ({ sample, value: values[index] })).filter(
    (entry) => insideWindow(entry.sample.displayTime, normalization.statisticsWindow) && entry.sample.value !== null && entry.value !== null
  ).map((entry) => entry.value);
  assertNormalizationPostconditions(normalization.method, basis, transformedBasis);
  return {
    values,
    receipt
  };
}
function prepareTraceSeries(input, options) {
  if (input.observationKind !== "point_sample" && input.observationKind !== "piecewise_constant") {
    throw new Error(`unknown trace observation kind: ${String(input.observationKind)}`);
  }
  if (options.duplicatePolicy !== void 0 && options.duplicatePolicy !== "reject" && options.duplicatePolicy !== "keep_replicates" && options.duplicatePolicy !== "aggregate") {
    throw new Error(`unknown trace duplicate policy: ${String(options.duplicatePolicy)}`);
  }
  if (options.aggregateMethod !== void 0 && !["mean", "median", "min", "max"].includes(options.aggregateMethod)) {
    throw new Error(`unknown trace aggregate method: ${String(options.aggregateMethod)}`);
  }
  if (options.normalization && !["z_score", "min_max", "divide_by_baseline_mean"].includes(options.normalization.method)) {
    throw new Error(`unknown trace normalization method: ${String(options.normalization.method)}`);
  }
  if (input.times.length !== input.values.length) {
    throw new Error("trace time and value arrays must have equal length");
  }
  if (!Number.isFinite(options.window.start) || !Number.isFinite(options.window.stop) || !(options.window.stop > options.window.start)) {
    throw new Error("trace window must have finite increasing endpoints");
  }
  if (options.normalization && (!Number.isFinite(options.normalization.statisticsWindow.start) || !Number.isFinite(options.normalization.statisticsWindow.stop) || !(options.normalization.statisticsWindow.stop > options.normalization.statisticsWindow.start))) {
    throw new Error("trace normalization statistics window must have finite increasing endpoints");
  }
  if (!isKnownUnit(input.timeUnit) || !isKnownUnit(options.window.unit) || dimensionOf(input.timeUnit) !== "time" || dimensionOf(options.window.unit) !== "time") {
    throw new Error("trace time and display-window units must be registered time units");
  }
  if (!isKnownUnit(input.valueUnit)) {
    throw new Error("trace value unit must be registered");
  }
  if (options.targetValueUnit !== void 0 && !isKnownUnit(options.targetValueUnit)) {
    throw new Error("trace target value unit must be registered");
  }
  if (input.timeOffset && (!Number.isFinite(input.timeOffset.value) || !isKnownUnit(input.timeOffset.unit) || dimensionOf(input.timeOffset.unit) !== "time")) {
    throw new Error("trace clock offset must be finite and use a registered time unit");
  }
  if (options.normalization && options.normalization.statisticsWindow.unit !== options.window.unit) {
    throw new Error("trace normalization statistics window must use the prepared display-time unit");
  }
  const offset = input.timeOffset ? canonicalZero2(
    convertWithoutInventing(input.timeOffset.value, input.timeOffset.unit, options.window.unit)
  ) : 0;
  const ordered = input.times.map((sourceTime, ordinal) => {
    const rawSourceValue = input.values[ordinal];
    if (!Number.isFinite(sourceTime)) throw new Error("trace timestamps must be finite");
    if (rawSourceValue !== null && !Number.isFinite(rawSourceValue)) {
      throw new Error("trace values must be finite or null");
    }
    const canonicalSourceTime = canonicalZero2(sourceTime);
    const sourceValue = rawSourceValue === null ? null : canonicalZero2(rawSourceValue);
    const convertedTime = canonicalZero2(
      convertWithoutInventing(canonicalSourceTime, input.timeUnit, options.window.unit)
    );
    const displayTime = input.timeOffset ? canonicalZero2(convertExactUnitSum([
      { value: canonicalSourceTime, unit: input.timeUnit },
      { value: canonicalZero2(input.timeOffset.value), unit: input.timeOffset.unit }
    ], options.window.unit)) : convertedTime;
    if (!Number.isFinite(displayTime)) throw new Error("trace clock conversion produced a non-finite time");
    if (offset !== 0) {
      const appliedOffset = displayTime - convertedTime;
      if (!Number.isFinite(appliedOffset) || !binary64EffectWasPreserved(offset, appliedOffset)) {
        throw new Error("trace clock offset is distorted below binary64 resolution at this timestamp");
      }
    }
    const hasNonzeroOffset = input.timeOffset !== void 0 && input.timeOffset.value !== 0;
    if (input.timeUnit !== options.window.unit || hasNonzeroOffset) {
      const exactDisplayPosition = exactWindowPosition(
        canonicalSourceTime,
        input.timeUnit,
        input.timeOffset,
        options.window
      );
      if (exactDisplayPosition !== windowPosition(displayTime, options.window)) {
        throw new Error("trace timestamp rounds across a display-window boundary at binary64 resolution");
      }
    }
    if (options.normalization && (input.timeUnit !== options.normalization.statisticsWindow.unit || hasNonzeroOffset)) {
      const exactStatisticsPosition = exactWindowPosition(
        canonicalSourceTime,
        input.timeUnit,
        input.timeOffset,
        options.normalization.statisticsWindow
      );
      if (exactStatisticsPosition !== windowPosition(displayTime, options.normalization.statisticsWindow)) {
        throw new Error("trace timestamp rounds across a statistics-window boundary at binary64 resolution");
      }
    }
    return {
      sourceTime: canonicalSourceTime,
      convertedTime,
      displayTime,
      value: sourceValue,
      sourceValues: [sourceValue],
      sourceOrdinals: [ordinal],
      ordinal
    };
  }).sort((a, b) => a.sourceTime - b.sourceTime || a.ordinal - b.ordinal);
  assertStrictAffineOrdering(
    ordered.map((sample) => sample.sourceTime),
    ordered.map((sample) => sample.convertedTime),
    (lower, upper) => convertDifference(lower, upper, input.timeUnit, options.window.unit)
  );
  assertStrictAffineOrdering(
    ordered.map((sample) => sample.convertedTime),
    ordered.map((sample) => sample.displayTime),
    (lower, upper) => convertDifference(lower, upper, options.window.unit, options.window.unit)
  );
  const resolved = [];
  const reordered = ordered.some((sample, index) => sample.ordinal !== index);
  let duplicateGroups = 0;
  for (let start = 0; start < ordered.length; ) {
    let stop = start + 1;
    while (stop < ordered.length && ordered[stop].sourceTime === ordered[start].sourceTime) stop++;
    const count = stop - start;
    if (count > 1) {
      duplicateGroups++;
      if (options.duplicatePolicy === void 0 || options.duplicatePolicy === "reject") {
        throw new Error(`duplicate trace timestamp ${ordered[start].sourceTime} was rejected`);
      }
      if (options.duplicatePolicy === "aggregate") {
        if (!options.aggregateMethod) throw new Error("duplicate aggregation needs a declared method");
        resolved.push({
          sourceTime: ordered[start].sourceTime,
          displayTime: ordered[start].displayTime,
          value: aggregate(ordered.slice(start, stop).map((sample) => sample.value), options.aggregateMethod),
          sourceValues: ordered.slice(start, stop).flatMap((sample) => sample.sourceValues),
          sourceOrdinals: ordered.slice(start, stop).flatMap((sample) => sample.sourceOrdinals),
          replicateCount: count
        });
        start = stop;
        continue;
      }
    }
    for (let index = start; index < stop; index++) {
      resolved.push({
        sourceTime: ordered[index].sourceTime,
        displayTime: ordered[index].displayTime,
        value: ordered[index].value,
        sourceValues: ordered[index].sourceValues,
        sourceOrdinals: ordered[index].sourceOrdinals,
        replicateCount: 1
      });
    }
    start = stop;
  }
  for (let index = 1; index < resolved.length; index++) {
    const previous = resolved[index - 1];
    const current = resolved[index];
    if (current.sourceTime !== previous.sourceTime && current.displayTime === previous.displayTime) {
      throw new Error("time-unit conversion collapsed two distinct source timestamps");
    }
  }
  const targetUnit = options.normalization ? "1" : options.targetValueUnit ?? input.valueUnit;
  const converted2 = resolved.map((sample) => ({
    ...sample,
    value: sample.value === null || options.normalization ? sample.value : convertWithoutInventing(sample.value, input.valueUnit, targetUnit)
  }));
  if (!options.normalization) {
    assertStrictAffineOrdering(
      resolved.filter((sample) => sample.value !== null).map((sample) => sample.value),
      converted2.filter((sample) => sample.value !== null).map((sample) => sample.value),
      (lower, upper) => input.valueUnit === targetUnit ? exactBinary64Sum([upper, -lower]) : convertDifference(lower, upper, input.valueUnit, targetUnit)
    );
  }
  const normalizationResult = options.normalization ? normalizeValues(converted2, options.normalization) : { values: converted2.map((sample) => sample.value), receipt: void 0 };
  const retained = [];
  let excludedBelow = 0;
  let excludedAbove = 0;
  for (let index = 0; index < converted2.length; index++) {
    const sample = { ...converted2[index], value: normalizationResult.values[index] };
    if (sample.displayTime < options.window.start) excludedBelow += sample.replicateCount;
    else if (!insideWindow(sample.displayTime, options.window)) excludedAbove += sample.replicateCount;
    else retained.push(sample);
  }
  return {
    id: input.id,
    label: input.label,
    observationKind: input.observationKind,
    times: retained.map((sample) => sample.displayTime),
    values: retained.map((sample) => sample.value),
    replicateCounts: retained.map((sample) => sample.replicateCount),
    observations: retained.map((sample) => ({
      recordedTime: sample.sourceTime,
      sourceValues: sample.sourceValues,
      sourceOrdinals: sample.sourceOrdinals,
      time: sample.displayTime,
      value: sample.value,
      replicateCount: sample.replicateCount
    })),
    timeUnit: options.window.unit,
    valueUnit: targetUnit,
    sourceTimeUnit: input.timeUnit,
    sourceValueUnit: input.valueUnit,
    inputCount: input.times.length,
    outputCount: retained.length,
    excludedBelow,
    excludedAbove,
    missingCount: retained.reduce(
      (count, sample) => count + sample.sourceValues.filter((value) => value === null).length,
      0
    ),
    duplicateGroups,
    reordered,
    ...input.timeUnit !== options.window.unit ? {
      timeConversion: {
        ...conversionReceipt(input.timeUnit, options.window.unit)
      }
    } : {},
    ...!options.normalization && input.valueUnit !== targetUnit ? {
      valueConversion: {
        ...conversionReceipt(input.valueUnit, targetUnit)
      }
    } : {},
    ...input.timeOffset ? {
      timeOffset: {
        sourceValue: canonicalZero2(input.timeOffset.value),
        sourceUnit: input.timeOffset.unit,
        displayValue: offset,
        displayUnit: options.window.unit,
        conversion: conversionReceipt(input.timeOffset.unit, options.window.unit)
      }
    } : {},
    ...normalizationResult.receipt ? { normalization: normalizationResult.receipt } : {}
  };
}

// src/render/format.ts
function formatNumber(value, significantDigits = 6) {
  if (!Number.isFinite(value)) return "\u2014";
  if (value === 0 || Object.is(value, -0)) return "0";
  const magnitude = Math.abs(value);
  if (magnitude >= 1e6 || magnitude < 1e-4) {
    return trimExponential(value.toExponential(Math.max(0, significantDigits - 1)));
  }
  const precise = value.toPrecision(significantDigits);
  const trimmed = Number(precise).toString();
  return trimmed === "-0" ? "0" : trimmed;
}
function trimExponential(text) {
  const [mantissa, exponent] = text.split("e");
  const trimmedMantissa = mantissa.includes(".") ? mantissa.replace(/0+$/, "").replace(/\.$/, "") : mantissa;
  return `${trimmedMantissa}e${exponent}`;
}
function formatCoordinate(value) {
  if (!Number.isFinite(value)) return "0";
  const rounded = Math.round(value * 1e3) / 1e3;
  if (rounded === 0 || Object.is(rounded, -0)) return "0";
  const text = rounded.toString();
  return text === "-0" ? "0" : text;
}

// src/render/layout.ts
var LEGEND_ROW_HEIGHT = 18;
var MIN_PLOT_PANEL_HEIGHT = 48;
var DISCLOSURE_FONT_SIZE = 10;
var DISCLOSURE_LINE_HEIGHT = 14;
var DISCLOSURE_HORIZONTAL_INSET = 24;
var DISCLOSURE_BOTTOM_PADDING = 6;
var DISCLOSURE_PLOT_GAP = 10;
var DISCLOSURE_GLYPH_ADVANCE = 6;
function disclosureAvailableWidth(width) {
  return Math.max(1, width - 2 * DISCLOSURE_HORIZONTAL_INSET);
}
function wrapDisclosureText(text, width) {
  if (text.length === 0) return [""];
  const codePoints = Array.from(text);
  const capacity = Math.max(
    1,
    Math.floor(disclosureAvailableWidth(width) / DISCLOSURE_GLYPH_ADVANCE)
  );
  const lines = [];
  let start = 0;
  while (start < codePoints.length) {
    let end = Math.min(codePoints.length, start + capacity);
    if (end < codePoints.length) {
      for (let index = end - 1; index > start; index--) {
        if (/\s/u.test(codePoints[index])) {
          end = index;
          break;
        }
      }
    }
    lines.push(codePoints.slice(start, end).join(""));
    start = end;
  }
  return lines;
}
function disclosureRenderedTextLength(text, width) {
  return Math.min(
    disclosureAvailableWidth(width),
    Math.max(1, Array.from(text).length * DISCLOSURE_GLYPH_ADVANCE)
  );
}
function disclosureLineCount(width, disclosures) {
  let count = 0;
  for (const disclosure of disclosures) count += wrapDisclosureText(disclosure.text, width).length;
  return count;
}
function disclosureFooterHeight(width, disclosures) {
  return disclosureLineCount(width, disclosures) * DISCLOSURE_LINE_HEIGHT + DISCLOSURE_PLOT_GAP;
}
function legendStartY(hasSubtitle) {
  return hasSubtitle ? 64 : 48;
}
function legendColumnCount(width, itemCount) {
  if (itemCount <= 0) return 0;
  return Math.min(itemCount, width >= 640 ? 2 : 1);
}
function legendPlotInset(width, itemCount, hasSubtitle) {
  if (itemCount <= 0) return 0;
  const columns = legendColumnCount(width, itemCount);
  const rows = Math.ceil(itemCount / columns);
  return (hasSubtitle ? 16 : 0) + rows * LEGEND_ROW_HEIGHT;
}

// src/render/svg.ts
var RenderPlanGeometryError = class extends Error {
  constructor(panelId, markPath, message) {
    super(message);
    this.panelId = panelId;
    this.markPath = markPath;
    this.name = "RenderPlanGeometryError";
  }
  panelId;
  markPath;
};
function assertArrowGeometry(marks, panelId, prefix = "marks") {
  for (let markIndex = 0; markIndex < marks.length; markIndex++) {
    const mark = marks[markIndex];
    const markPath = `${prefix}/${markIndex}`;
    if (mark.type === "group") {
      assertArrowGeometry(mark.marks, panelId, `${markPath}/marks`);
      continue;
    }
    if (mark.type !== "arrow") continue;
    if (!Number.isFinite(mark.size) || !(mark.size > 0)) {
      throw new RenderPlanGeometryError(
        panelId,
        `${markPath}/size`,
        `arrow size must be finite and strictly positive; got ${String(mark.size)}`
      );
    }
    for (let arrowIndex = 0; arrowIndex < mark.arrows.length; arrowIndex++) {
      const arrow = mark.arrows[arrowIndex];
      if (![arrow.from.x, arrow.from.y, arrow.to.x, arrow.to.y].every(Number.isFinite)) {
        throw new RenderPlanGeometryError(
          panelId,
          `${markPath}/arrows/${arrowIndex}`,
          "arrow endpoints must contain four finite coordinates"
        );
      }
      if (arrow.from.x === arrow.to.x && arrow.from.y === arrow.to.y) {
        throw new RenderPlanGeometryError(
          panelId,
          `${markPath}/arrows/${arrowIndex}`,
          "arrow endpoints are identical, so direction is undefined"
        );
      }
    }
  }
}
function assertRenderPlanGeometry(plan) {
  for (const panel of plan.panels) assertArrowGeometry(panel.marks, panel.id);
}
function escapeText(value) {
  return value.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");
}
function escapeAttribute(value) {
  return value.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#39;");
}
var SvgWriter = class {
  out = [];
  indentLevel = 0;
  indent() {
    return "  ".repeat(this.indentLevel);
  }
  open(tag, attrs = []) {
    const rendered = attrs.map(([key, value]) => `${key}="${escapeAttribute(String(value))}"`).join(" ");
    this.out.push(`${this.indent()}<${tag}${rendered ? ` ${rendered}` : ""}>`);
    this.indentLevel++;
    return this;
  }
  leaf(tag, attrs = []) {
    const rendered = attrs.map(([key, value]) => `${key}="${escapeAttribute(String(value))}"`).join(" ");
    this.out.push(`${this.indent()}<${tag}${rendered ? ` ${rendered}` : ""}/>`);
    return this;
  }
  text(tag, content, attrs = []) {
    const rendered = attrs.map(([key, value]) => `${key}="${escapeAttribute(String(value))}"`).join(" ");
    this.out.push(
      `${this.indent()}<${tag}${rendered ? ` ${rendered}` : ""}>${escapeText(content)}</${tag}>`
    );
    return this;
  }
  close(tag) {
    this.indentLevel--;
    this.out.push(`${this.indent()}</${tag}>`);
    return this;
  }
  raw(line) {
    this.out.push(`${this.indent()}${line}`);
    return this;
  }
  toString() {
    return `${this.out.join("\n")}
`;
  }
};
function theme(themeId) {
  return THEMES[themeId] ?? THEMES.light;
}
function pointsToPath(points) {
  if (points.length === 0) return "";
  const commands = points.map(
    (point, index) => `${index === 0 ? "M" : "L"}${formatCoordinate(point.x)},${formatCoordinate(point.y)}`
  );
  return commands.join(" ");
}
var MARKER_PATHS = {
  circle: (x, y, r) => `M${formatCoordinate(x - r)},${formatCoordinate(y)}a${r},${r} 0 1,0 ${2 * r},0a${r},${r} 0 1,0 ${-2 * r},0`,
  square: (x, y, r) => `M${formatCoordinate(x - r)},${formatCoordinate(y - r)}h${2 * r}v${2 * r}h${-2 * r}z`,
  triangle: (x, y, r) => `M${formatCoordinate(x)},${formatCoordinate(y - r)}L${formatCoordinate(x + r)},${formatCoordinate(y + r)}L${formatCoordinate(x - r)},${formatCoordinate(y + r)}z`,
  diamond: (x, y, r) => `M${formatCoordinate(x)},${formatCoordinate(y - r)}L${formatCoordinate(x + r)},${formatCoordinate(y)}L${formatCoordinate(x)},${formatCoordinate(y + r)}L${formatCoordinate(x - r)},${formatCoordinate(y)}z`,
  cross: (x, y, r) => `M${formatCoordinate(x - r)},${formatCoordinate(y - r)}L${formatCoordinate(x + r)},${formatCoordinate(y + r)}M${formatCoordinate(x + r)},${formatCoordinate(y - r)}L${formatCoordinate(x - r)},${formatCoordinate(y + r)}`,
  plus: (x, y, r) => `M${formatCoordinate(x - r)},${formatCoordinate(y)}L${formatCoordinate(x + r)},${formatCoordinate(y)}M${formatCoordinate(x)},${formatCoordinate(y - r)}L${formatCoordinate(x)},${formatCoordinate(y + r)}`,
  star: (x, y, r) => {
    const points = [];
    for (let index = 0; index < 10; index++) {
      const radius = index % 2 === 0 ? r : r * 0.42;
      const angle = -Math.PI / 2 + index * Math.PI / 5;
      points.push({ x: x + radius * Math.cos(angle), y: y + radius * Math.sin(angle) });
    }
    return `${pointsToPath(points)} Z`;
  },
  hexagon: (x, y, r) => {
    const points = Array.from({ length: 6 }, (_value, index) => {
      const angle = Math.PI / 6 + index * Math.PI / 3;
      return { x: x + r * Math.cos(angle), y: y + r * Math.sin(angle) };
    });
    return `${pointsToPath(points)} Z`;
  }
};
function emitMark(writer, mark, colors) {
  switch (mark.type) {
    case "line": {
      const d = mark.subpaths.filter((subpath) => subpath.length > 0).map((subpath) => pointsToPath(subpath)).join(" ");
      if (d.length === 0) return;
      const attrs = [
        ["d", d],
        ["fill", "none"],
        ["stroke", mark.stroke],
        ["stroke-width", mark.strokeWidth]
      ];
      if (mark.dash && mark.dash !== "none") attrs.push(["stroke-dasharray", mark.dash]);
      writer.leaf("path", attrs);
      break;
    }
    case "arrow": {
      for (const arrow of mark.arrows) {
        const dx = arrow.to.x - arrow.from.x;
        const dy = arrow.to.y - arrow.from.y;
        const length = Math.hypot(dx, dy);
        if (!(length > 0) || !Number.isFinite(length)) {
          throw new RenderPlanGeometryError(
            "unknown",
            "marks/arrow",
            "arrow direction became non-finite or degenerate during serialization"
          );
        }
        const ux = dx / length;
        const uy = dy / length;
        const bx = arrow.to.x - ux * mark.size;
        const by = arrow.to.y - uy * mark.size;
        const halfWidth = mark.size * 0.58;
        const px = -uy * halfWidth;
        const py = ux * halfWidth;
        const d = [
          `M${formatCoordinate(arrow.to.x)},${formatCoordinate(arrow.to.y)}`,
          `L${formatCoordinate(bx + px)},${formatCoordinate(by + py)}`,
          `L${formatCoordinate(bx - px)},${formatCoordinate(by - py)} Z`
        ].join(" ");
        writer.leaf("path", [
          ["d", d],
          ["fill", mark.fill],
          ["stroke", mark.fill],
          ["stroke-width", 1]
        ]);
      }
      break;
    }
    case "path": {
      const d = mark.subpaths.filter((subpath) => subpath.length > 0).map((subpath) => pointsToPath(subpath)).join(" ");
      if (d.length === 0) return;
      writer.leaf("path", [
        ["d", d],
        ["fill", "none"],
        ["stroke", mark.stroke],
        ["stroke-width", mark.strokeWidth]
      ]);
      break;
    }
    case "point": {
      const build = MARKER_PATHS[mark.shape] ?? MARKER_PATHS.circle;
      for (const point of mark.points) {
        writer.leaf("path", [
          ["d", build(point.x, point.y, mark.radius)],
          ["fill", mark.fill],
          ["stroke", mark.fill],
          ["stroke-width", 1]
        ]);
      }
      break;
    }
    case "rect": {
      for (const rect of mark.rects) {
        const attrs = [
          ["x", formatCoordinate(rect.x)],
          ["y", formatCoordinate(rect.y)],
          ["width", formatCoordinate(Math.max(0, rect.width))],
          ["height", formatCoordinate(Math.max(0, rect.height))],
          ["fill", rect.fill]
        ];
        if (mark.stroke) attrs.push(["stroke", mark.stroke]);
        writer.leaf("rect", attrs);
      }
      break;
    }
    case "rule": {
      for (const line of mark.lines) {
        const attrs = mark.orientation === "vertical" ? [
          ["x1", formatCoordinate(line.position)],
          ["y1", formatCoordinate(line.from)],
          ["x2", formatCoordinate(line.position)],
          ["y2", formatCoordinate(line.to)]
        ] : [
          ["x1", formatCoordinate(line.from)],
          ["y1", formatCoordinate(line.position)],
          ["x2", formatCoordinate(line.to)],
          ["y2", formatCoordinate(line.position)]
        ];
        attrs.push(["stroke", mark.stroke], ["stroke-width", mark.strokeWidth]);
        if (mark.dash && mark.dash !== "none") attrs.push(["stroke-dasharray", mark.dash]);
        writer.leaf("line", attrs);
      }
      break;
    }
    case "area": {
      for (const subpath of mark.subpaths) {
        if (subpath.length === 0) continue;
        const top = subpath.map((p) => ({ x: p.x, y: p.y1 }));
        const bottom = subpath.map((p) => ({ x: p.x, y: p.y0 })).reverse();
        const d = `${pointsToPath(top)} ${pointsToPath(bottom).replace(/^M/, "L")} Z`;
        const attrs = [
          ["d", d],
          ["fill", mark.fill],
          ["fill-opacity", mark.opacity],
          ["stroke", mark.stroke ?? "none"]
        ];
        if (mark.stroke) attrs.push(["stroke-width", mark.strokeWidth ?? 1]);
        writer.leaf("path", attrs);
      }
      break;
    }
    case "text":
      emitText(writer, mark);
      break;
    case "group":
      writer.open("g", [["data-id", mark.id]]);
      for (const child of mark.marks) emitMark(writer, child, colors);
      writer.close("g");
      break;
  }
}
function emitText(writer, mark) {
  const attrs = [
    ["x", formatCoordinate(mark.x)],
    ["y", formatCoordinate(mark.y)],
    ["text-anchor", mark.anchor],
    ["font-size", mark.fontSize],
    ["fill", mark.fill],
    ["font-family", "sans-serif"]
  ];
  if (mark.decorative) attrs.push(["aria-hidden", "true"]);
  writer.text("text", mark.text, attrs);
}
function emitAxis(writer, axis, panel, colors) {
  writer.open("g", [["data-axis", axis.orientation], ["aria-hidden", "true"]]);
  const isBottom = axis.orientation === "bottom";
  const isLeft = axis.orientation === "left";
  const isTop = axis.orientation === "top";
  for (const tick of axis.ticks) {
    if (isBottom) {
      writer.leaf("line", [
        ["x1", formatCoordinate(tick.position)],
        ["y1", formatCoordinate(panel.y + panel.height)],
        ["x2", formatCoordinate(tick.position)],
        ["y2", formatCoordinate(panel.y + panel.height + 5)],
        ["stroke", colors.axis],
        ["stroke-width", 1]
      ]);
      emitText(writer, {
        type: "text",
        x: tick.position,
        y: panel.y + panel.height + 18,
        text: tick.label,
        anchor: "middle",
        fontSize: 11,
        fill: colors.mutedText,
        decorative: true
      });
    } else if (isLeft) {
      writer.leaf("line", [
        ["x1", formatCoordinate(panel.x - 5)],
        ["y1", formatCoordinate(tick.position)],
        ["x2", formatCoordinate(panel.x)],
        ["y2", formatCoordinate(tick.position)],
        ["stroke", colors.axis],
        ["stroke-width", 1]
      ]);
      emitText(writer, {
        type: "text",
        x: panel.x - 8,
        y: tick.position + 4,
        text: tick.label,
        anchor: "end",
        fontSize: 11,
        fill: colors.mutedText,
        decorative: true
      });
    } else if (isTop) {
      writer.leaf("line", [
        ["x1", formatCoordinate(tick.position)],
        ["y1", formatCoordinate(panel.y - 5)],
        ["x2", formatCoordinate(tick.position)],
        ["y2", formatCoordinate(panel.y)],
        ["stroke", colors.axis],
        ["stroke-width", 1]
      ]);
      emitText(writer, {
        type: "text",
        x: tick.position,
        y: panel.y - 8,
        text: tick.label,
        anchor: "middle",
        fontSize: 11,
        fill: colors.mutedText,
        decorative: true
      });
    } else {
      writer.leaf("line", [
        ["x1", formatCoordinate(panel.x + panel.width)],
        ["y1", formatCoordinate(tick.position)],
        ["x2", formatCoordinate(panel.x + panel.width + 5)],
        ["y2", formatCoordinate(tick.position)],
        ["stroke", colors.axis],
        ["stroke-width", 1]
      ]);
      emitText(writer, {
        type: "text",
        x: panel.x + panel.width + 8,
        y: tick.position + 4,
        text: tick.label,
        anchor: "start",
        fontSize: 11,
        fill: colors.mutedText,
        decorative: true
      });
    }
  }
  emitText(writer, {
    type: "text",
    x: isBottom || isTop ? panel.x + panel.width / 2 : isLeft ? panel.x - 44 : panel.x + panel.width + 44,
    y: isBottom ? panel.y + panel.height + 38 : isTop ? panel.y - 28 : panel.y + panel.height / 2,
    text: axis.label,
    anchor: "middle",
    fontSize: 12,
    fill: colors.text,
    decorative: true
  });
  writer.close("g");
}
function countMarks(marks) {
  let markCount = 0;
  let textCount = 0;
  const walk = (list) => {
    for (const mark of list) {
      switch (mark.type) {
        case "group":
          walk(mark.marks);
          break;
        case "line":
        case "path":
          if (mark.subpaths.some((subpath) => subpath.length > 0)) markCount++;
          break;
        case "arrow":
          markCount += mark.arrows.length;
          break;
        case "point":
          markCount += mark.points.length;
          break;
        case "rect":
          markCount += mark.rects.length;
          break;
        case "rule":
          markCount += mark.lines.length;
          break;
        case "area":
          markCount += mark.subpaths.filter((subpath) => subpath.length > 0).length;
          break;
        case "text":
          markCount++;
          textCount++;
          break;
      }
    }
  };
  walk(marks);
  return { marks: markCount, texts: textCount };
}
function countPlanResources(plan) {
  assertRenderPlanGeometry(plan);
  let markCount = 0;
  let textCount = 1 + (plan.subtitle ? 1 : 0) + disclosureLineCount(plan.width, plan.disclosures);
  for (const panel of plan.panels) {
    if (panel.label) textCount++;
    if (panel.noData) {
      textCount++;
      continue;
    }
    for (const axis of panel.axes) textCount += axis.ticks.length + 1;
    const counts = countMarks(panel.marks);
    markCount += counts.marks;
    textCount += counts.texts;
  }
  textCount += plan.legend?.length ?? 0;
  return { markCount, textCount };
}
function renderSvg(plan, digestOf) {
  assertRenderPlanGeometry(plan);
  const colors = theme(plan.themeId);
  const writer = new SvgWriter();
  writer.open("svg", [
    ["xmlns", "http://www.w3.org/2000/svg"],
    ["xmlns:cortexel", "urn:cortexel:metadata:1"],
    ["viewBox", `0 0 ${plan.width} ${plan.height}`],
    ["width", plan.width],
    ["height", plan.height],
    ["role", "img"],
    ["aria-labelledby", `${plan.figureId}-title ${plan.figureId}-desc`]
  ]);
  writer.text("title", plan.title, [["id", `${plan.figureId}-title`]]);
  writer.text("desc", plan.accessibility.summary, [["id", `${plan.figureId}-desc`]]);
  writer.open("metadata");
  writer.text("cortexel:contract", ARTIFACT_CONTRACT);
  writer.text("cortexel:skill", plan.skillId);
  writer.text("cortexel:requestDigest", plan.sourceRequestDigest);
  writer.close("metadata");
  writer.leaf("rect", [
    ["x", 0],
    ["y", 0],
    ["width", plan.width],
    ["height", plan.height],
    ["fill", colors.background]
  ]);
  emitText(writer, {
    type: "text",
    x: 24,
    y: 28,
    text: plan.title,
    anchor: "start",
    fontSize: 16,
    fill: colors.text,
    decorative: true
    // the <title> element already names the figure for AT
  });
  if (plan.subtitle) {
    emitText(writer, {
      type: "text",
      x: 24,
      y: 46,
      text: plan.subtitle,
      anchor: "start",
      fontSize: 12,
      fill: colors.mutedText,
      decorative: true
    });
  }
  if (plan.legend && plan.legend.length > 0) {
    const startY = legendStartY(plan.subtitle !== void 0);
    const columns = legendColumnCount(plan.width, plan.legend.length);
    const itemWidth = (plan.width - 48) / columns;
    writer.open("g", [["data-legend", "true"], ["aria-hidden", "true"]]);
    for (let index = 0; index < plan.legend.length; index++) {
      const item = plan.legend[index];
      const outlineColor = item.outlineColor ?? item.color;
      const column = index % columns;
      const row = Math.floor(index / columns);
      const x = 24 + column * itemWidth;
      const y = startY + row * LEGEND_ROW_HEIGHT;
      const glyph = item.glyph ?? "series";
      if (glyph === "band") {
        writer.leaf("rect", [
          ["x", x],
          ["y", y - 9],
          ["width", 24],
          ["height", 10],
          ["fill", item.color],
          ["fill-opacity", 0.18],
          ["stroke", outlineColor],
          ["stroke-width", 1.5]
        ]);
      } else if (glyph === "whisker") {
        writer.leaf("line", [
          ["x1", x + 12],
          ["y1", y - 10],
          ["x2", x + 12],
          ["y2", y + 2],
          ["stroke", outlineColor],
          ["stroke-width", 1.5]
        ]);
        for (const capY of [y - 10, y + 2]) {
          writer.leaf("line", [
            ["x1", x + 7],
            ["y1", capY],
            ["x2", x + 17],
            ["y2", capY],
            ["stroke", outlineColor],
            ["stroke-width", 1.5]
          ]);
        }
      } else {
        const attrs = [
          ["x1", x],
          ["y1", y - 4],
          ["x2", x + 24],
          ["y2", y - 4],
          ["stroke", item.color],
          ["stroke-width", 1.5]
        ];
        if (item.dash && item.dash !== "none") attrs.push(["stroke-dasharray", item.dash]);
        writer.leaf("line", attrs);
        if (item.marker) {
          const marker = MARKER_PATHS[item.marker] ?? MARKER_PATHS.circle;
          writer.leaf("path", [
            ["d", marker(x + 12, y - 4, 2.5)],
            ["fill", item.color],
            ["stroke", item.color],
            ["stroke-width", 1]
          ]);
        }
      }
      emitText(writer, {
        type: "text",
        x: x + 32,
        y,
        text: item.label,
        anchor: "start",
        fontSize: 10,
        fill: colors.text,
        decorative: true
      });
    }
    writer.close("g");
  }
  for (const panel of plan.panels) {
    writer.open("g", [["data-panel", panel.id]]);
    if (panel.label) {
      emitText(writer, {
        type: "text",
        x: panel.x,
        y: panel.y - 8,
        text: panel.label,
        anchor: "start",
        fontSize: 11,
        fill: colors.text,
        decorative: true
      });
    }
    if (panel.noData) {
      emitText(writer, {
        type: "text",
        x: panel.x + panel.width / 2,
        y: panel.y + panel.height / 2,
        text: `No data: ${panel.noData.reason}`,
        anchor: "middle",
        fontSize: 13,
        fill: colors.mutedText,
        decorative: true
      });
    } else {
      for (const axis of panel.axes) emitAxis(writer, axis, panel, colors);
      for (const mark of panel.marks) {
        emitMark(writer, mark, colors);
      }
    }
    writer.close("g");
  }
  const footerLineCount = disclosureLineCount(plan.width, plan.disclosures);
  let disclosureY = plan.height - footerLineCount * DISCLOSURE_LINE_HEIGHT - DISCLOSURE_BOTTOM_PADDING;
  writer.open("g", [["data-disclosures", "true"]]);
  for (const disclosure of plan.disclosures) {
    writer.open("g", [
      ["data-disclosure-id", disclosure.id],
      ["data-disclosure-text", disclosure.text]
    ]);
    const lines = wrapDisclosureText(disclosure.text, plan.width);
    for (let index = 0; index < lines.length; index++) {
      const line = lines[index];
      writer.text("text", line, [
        ["x", DISCLOSURE_HORIZONTAL_INSET],
        ["y", formatCoordinate(disclosureY)],
        ["text-anchor", "start"],
        ["font-size", DISCLOSURE_FONT_SIZE],
        ["fill", disclosure.severity === "critical" ? colors.error : colors.mutedText],
        ["font-family", "sans-serif"],
        ["textLength", formatCoordinate(disclosureRenderedTextLength(line, plan.width))],
        ["lengthAdjust", "spacingAndGlyphs"],
        ["data-disclosure-line", index],
        ["aria-hidden", "true"]
      ]);
      disclosureY += DISCLOSURE_LINE_HEIGHT;
    }
    writer.close("g");
  }
  writer.close("g");
  writer.close("svg");
  const svg = writer.toString();
  const counts = countPlanResources(plan);
  return {
    svg,
    digest: digestOf(svg),
    markCount: counts.markCount,
    textCount: counts.textCount,
    width: plan.width,
    height: plan.height
  };
}

// src/core/deterministic-transcendentals.ts
var bits = new DataView(new ArrayBuffer(8));
function highWord(value) {
  bits.setFloat64(0, value, false);
  return bits.getUint32(0, false);
}
function withHighWord(value, high) {
  bits.setFloat64(0, value, false);
  bits.setUint32(0, high >>> 0, false);
  return bits.getFloat64(0, false);
}
var LN2_HI = 0.6931471803691238;
var LN2_LO = 19082149292705877e-26;
var TWO54 = 18014398509481984;
var LOG_COEFFICIENTS = [
  0.6666666666666735,
  0.3999999999940942,
  0.2857142874366239,
  0.22222198432149784,
  0.1818357216161805,
  0.15313837699209373,
  0.14798198605116586
];
function deterministicLogOnePlus(delta) {
  if (Number.isNaN(delta) || delta < 0) return NaN;
  if (delta === Infinity) return Infinity;
  const hx = highWord(delta);
  const absoluteHigh = hx & 2147483647;
  let k = 1;
  let f = 0;
  let correction = 0;
  let reducedHigh = 0;
  if (hx < 1071284858) {
    if (absoluteHigh < 1042284544) {
      if (absoluteHigh < 1016070144) return delta;
      return delta - delta * delta * 0.5;
    }
    k = 0;
    f = delta;
    reducedHigh = 1;
  }
  if (k !== 0) {
    let u;
    if (absoluteHigh < 1128267776) {
      u = 1 + delta;
      reducedHigh = highWord(u);
      k = (reducedHigh >>> 20) - 1023;
      correction = k > 0 ? 1 - (u - delta) : delta - (u - 1);
      correction /= u;
    } else {
      u = delta;
      reducedHigh = highWord(u);
      k = (reducedHigh >>> 20) - 1023;
    }
    reducedHigh &= 1048575;
    if (reducedHigh < 434334) {
      u = withHighWord(u, reducedHigh | 1072693248);
    } else {
      k++;
      u = withHighWord(u, reducedHigh | 1071644672);
      reducedHigh = 1048576 - reducedHigh >>> 2;
    }
    f = u - 1;
  }
  const halfSquare = 0.5 * f * f;
  if (reducedHigh === 0) {
    if (f === 0) {
      if (k === 0) return 0;
      correction += k * LN2_LO;
      return k * LN2_HI + correction;
    }
    const remainder2 = halfSquare * (1 - 0.6666666666666666 * f);
    return k === 0 ? f - remainder2 : k * LN2_HI - (remainder2 - (k * LN2_LO + correction) - f);
  }
  const s = f / (2 + f);
  const z = s * s;
  const [lp1, lp2, lp3, lp4, lp5, lp6, lp7] = LOG_COEFFICIENTS;
  const remainder = z * (lp1 + z * (lp2 + z * (lp3 + z * (lp4 + z * (lp5 + z * (lp6 + z * lp7))))));
  return k === 0 ? f - (halfSquare - s * (halfSquare + remainder)) : k * LN2_HI - (halfSquare - (s * (halfSquare + remainder) + (k * LN2_LO + correction)) - f);
}
function positiveMantissaExponent(value) {
  let normalized = value;
  let exponentAdjustment = 0;
  let high = highWord(normalized);
  if ((high & 2146435072) === 0) {
    normalized *= TWO54;
    exponentAdjustment = -54;
    high = highWord(normalized);
  }
  const exponent = (high >>> 20) - 1023 + exponentAdjustment;
  const mantissa = withHighWord(normalized, high & 1048575 | 1072693248);
  return { mantissa, exponent };
}
function deterministicPositiveLogRatio(larger, smaller) {
  if (!(larger >= smaller) || !(smaller > 0) || !Number.isFinite(larger)) return NaN;
  if (larger === smaller) return 0;
  const difference = larger - smaller;
  if (Number.isFinite(difference)) {
    const delta = difference / smaller;
    if (Number.isFinite(delta) && delta <= 1) return deterministicLogOnePlus(delta);
  }
  const numerator = positiveMantissaExponent(larger);
  const denominator = positiveMantissaExponent(smaller);
  let reducedRatio = numerator.mantissa / denominator.mantissa;
  let exponent = numerator.exponent - denominator.exponent;
  if (reducedRatio < 1) {
    reducedRatio *= 2;
    exponent--;
  } else if (reducedRatio >= 2) {
    reducedRatio *= 0.5;
    exponent++;
  }
  const reducedLog = deterministicLogOnePlus(reducedRatio - 1);
  return exponent * LN2_HI + (reducedLog + exponent * LN2_LO);
}

// src/render/scale.ts
function linearScale(domainMin, domainMax, rangeMin, rangeMax) {
  if (!Number.isFinite(domainMin) || !Number.isFinite(domainMax) || domainMax < domainMin || !Number.isFinite(rangeMin) || !Number.isFinite(rangeMax)) {
    throw new Error("linear scale requires an ordered finite domain and finite range");
  }
  const lo = domainMin;
  const hi = domainMax;
  const rangeSpan = rangeMax - rangeMin;
  if (!Number.isFinite(rangeSpan)) {
    throw new Error("linear scale range span must be finite");
  }
  if (lo === hi) {
    const midpoint = rangeMin + rangeSpan / 2;
    return {
      domainMin: lo,
      domainMax: hi,
      rangeMin,
      rangeMax,
      map(value) {
        if (!Number.isFinite(value)) return NaN;
        if (value === lo) return midpoint;
        return value < lo ? rangeMin : rangeMax;
      }
    };
  }
  const directSpan = hi - lo;
  const normalizationScale = Math.max(Math.abs(lo), Math.abs(hi));
  const normalizedLo = lo / normalizationScale;
  const normalizedSpan = hi / normalizationScale - normalizedLo;
  return {
    domainMin: lo,
    domainMax: hi,
    rangeMin,
    rangeMax,
    map(value) {
      if (!Number.isFinite(value)) return NaN;
      if (value === lo) return rangeMin;
      if (value === hi) return rangeMax;
      const ratio = Number.isFinite(directSpan) ? (value - lo) / directSpan : (value / normalizationScale - normalizedLo) / normalizedSpan;
      return rangeMin + ratio * rangeSpan;
    }
  };
}
function linearNumericScale(domainMin, domainMax, rangeMin, rangeMax) {
  const scale = linearScale(domainMin, domainMax, rangeMin, rangeMax);
  return {
    ...scale,
    transform: "linear",
    ticks(target = 6) {
      return linearTicks(scale.domainMin, scale.domainMax, target);
    }
  };
}
function logNumericScale(domainMin, domainMax, rangeMin, rangeMax) {
  if (!(domainMin > 0) || !(domainMax > 0) || !Number.isFinite(domainMin) || !Number.isFinite(domainMax) || domainMax < domainMin || !Number.isFinite(rangeMin) || !Number.isFinite(rangeMax)) {
    throw new Error("log scale requires an ordered finite strictly positive domain and finite range");
  }
  const rangeSpan = rangeMax - rangeMin;
  if (!Number.isFinite(rangeSpan)) throw new Error("log scale range span must be finite");
  if (domainMin === domainMax) {
    const midpoint = rangeMin + rangeSpan / 2;
    return {
      domainMin,
      domainMax,
      rangeMin,
      rangeMax,
      transform: "log",
      map(value) {
        if (!(value > 0) || !Number.isFinite(value)) return NaN;
        if (value === domainMin) return midpoint;
        return value < domainMin ? rangeMin : rangeMax;
      },
      ticks() {
        return ticksFromValues([domainMin]);
      }
    };
  }
  const span = deterministicPositiveLogRatio(domainMax, domainMin);
  if (!(span > 0) || !Number.isFinite(span)) {
    throw new Error("log scale domain is unrepresentable at binary64 resolution");
  }
  return {
    domainMin,
    domainMax,
    rangeMin,
    rangeMax,
    transform: "log",
    map(value) {
      if (!(value > 0) || !Number.isFinite(value)) return NaN;
      if (value === domainMin) return rangeMin;
      if (value === domainMax) return rangeMax;
      const distance = value >= domainMin ? deterministicPositiveLogRatio(value, domainMin) : -deterministicPositiveLogRatio(domainMin, value);
      return rangeMin + distance / span * rangeSpan;
    },
    ticks(target = 6) {
      return logarithmicTicks(domainMin, domainMax, target);
    }
  };
}
function symlogTransform(value, threshold) {
  if (!(threshold > 0) || !Number.isFinite(threshold) || !Number.isFinite(value)) return NaN;
  const magnitude = Math.abs(value);
  if (magnitude <= threshold) {
    const transformed = value / threshold;
    if (value !== 0) {
      const restored = transformed * threshold;
      const scale = Math.max(Math.abs(value), Math.abs(restored));
      if (transformed === 0 || !Number.isFinite(restored) || Math.abs(value / scale - restored / scale) > 8 * Number.EPSILON) return NaN;
    }
    return transformed;
  }
  const logarithm = deterministicPositiveLogRatio(magnitude, threshold);
  return Math.sign(value) * (1 + logarithm);
}
function innerSymlogDistance(from, to, threshold) {
  const difference = to - from;
  return Number.isFinite(difference) ? difference / threshold : to / threshold - from / threshold;
}
function symlogDistance(from, to, threshold) {
  if (from === to) return 0;
  if (from > to) return -symlogDistance(to, from, threshold);
  let cursor = from;
  let distance = 0;
  if (cursor < -threshold) {
    const end = Math.min(to, -threshold);
    distance += deterministicPositiveLogRatio(Math.abs(cursor), Math.abs(end));
    cursor = end;
  }
  if (cursor < threshold && cursor < to) {
    const end = Math.min(to, threshold);
    distance += innerSymlogDistance(cursor, end, threshold);
    cursor = end;
  }
  if (cursor < to) {
    distance += deterministicPositiveLogRatio(to, cursor);
  }
  return distance;
}
function symlogNumericScale(domainMin, domainMax, rangeMin, rangeMax, threshold) {
  if (!Number.isFinite(domainMin) || !Number.isFinite(domainMax) || domainMax < domainMin || !(threshold > 0) || !Number.isFinite(threshold) || !Number.isFinite(rangeMin) || !Number.isFinite(rangeMax)) {
    throw new Error("symlog scale requires an ordered finite domain, finite range, and positive finite threshold");
  }
  const rangeSpan = rangeMax - rangeMin;
  if (!Number.isFinite(rangeSpan)) throw new Error("symlog scale range span must be finite");
  if (domainMin === domainMax) {
    const midpoint = rangeMin + rangeSpan / 2;
    return {
      domainMin,
      domainMax,
      rangeMin,
      rangeMax,
      transform: "symlog",
      map(value) {
        if (!Number.isFinite(value)) return NaN;
        if (value === domainMin) return midpoint;
        return value < domainMin ? rangeMin : rangeMax;
      },
      ticks() {
        return ticksFromValues([domainMin]);
      }
    };
  }
  const entirelyLinear = domainMin >= -threshold && domainMax <= threshold;
  const linear = entirelyLinear ? linearScale(domainMin, domainMax, rangeMin, rangeMax) : void 0;
  const span = entirelyLinear ? 1 : symlogDistance(domainMin, domainMax, threshold);
  if (!(span > 0) || !Number.isFinite(span)) {
    throw new Error("symlog scale domain is unrepresentable at binary64 resolution");
  }
  return {
    domainMin,
    domainMax,
    rangeMin,
    rangeMax,
    transform: "symlog",
    map(value) {
      if (!Number.isFinite(value)) return NaN;
      if (value === domainMin) return rangeMin;
      if (value === domainMax) return rangeMax;
      if (linear) return linear.map(value);
      return rangeMin + symlogDistance(domainMin, value, threshold) / span * rangeSpan;
    },
    ticks(target = 6) {
      if (linear) return linearTicks(domainMin, domainMax, target);
      const values = [domainMin, domainMax];
      if (domainMin < -threshold) {
        const closest = Math.max(domainMin, Math.min(domainMax, -threshold));
        for (const tick of logarithmicTicks(Math.abs(closest), Math.abs(domainMin), target)) {
          values.push(-tick.value);
        }
      }
      const innerMin = Math.max(domainMin, -threshold);
      const innerMax = Math.min(domainMax, threshold);
      if (innerMin <= innerMax) values.push(...linearTicks(innerMin, innerMax, target).map((tick) => tick.value));
      if (domainMax > threshold) {
        const first = Math.max(domainMin, threshold);
        for (const tick of logarithmicTicks(first, domainMax, target)) values.push(tick.value);
      }
      for (const special of [-threshold, 0, threshold]) {
        if (special >= domainMin && special <= domainMax) values.push(special);
      }
      values.sort((a, b) => a - b);
      const unique = [];
      for (const value of values) {
        if (unique.length === 0 || value !== unique[unique.length - 1]) unique.push(value);
      }
      return ticksFromValues(unique);
    }
  };
}
function niceStep(rawStep) {
  if (rawStep <= 0 || !Number.isFinite(rawStep)) return 1;
  const exponentText = rawStep.toExponential().split("e")[1];
  const exponent = Number(exponentText);
  const magnitude = Number(`1e${exponent}`) || Number.MIN_VALUE;
  const normalized = rawStep / magnitude;
  let nice;
  if (normalized <= 1) nice = 1;
  else if (normalized <= 2) nice = 2;
  else if (normalized <= 5) nice = 5;
  else nice = 10;
  return nice * magnitude;
}
function ticksFromValues(values) {
  const unique = [];
  for (const value of values) {
    if (!Number.isFinite(value)) continue;
    if (unique.length === 0 || value !== unique[unique.length - 1]) unique.push(value === 0 ? 0 : value);
  }
  let labels = unique.map((value) => formatNumber(value));
  if (new Set(labels).size !== labels.length) labels = unique.map((value) => formatNumber(value, 17));
  return unique.map((value, index) => ({ value, label: labels[index] }));
}
function decimalPower(exponent) {
  return Number(`1e${exponent}`);
}
function decimalExponent(value) {
  return Number(value.toExponential().split("e")[1]);
}
function logarithmicTicks(min, max, target = 6) {
  if (!(min > 0) || !(max > 0) || !Number.isFinite(min) || !Number.isFinite(max) || max < min || !Number.isFinite(target) || !(target > 0)) return [];
  if (min === max) return ticksFromValues([min]);
  let first = decimalExponent(min);
  if (!(decimalPower(first) >= min)) first++;
  let last = decimalExponent(max);
  if (!(decimalPower(last) <= max)) last--;
  const count = Math.max(0, last - first + 1);
  const step = Math.max(1, Math.ceil(count / Math.max(1, target)));
  const values = [];
  for (let exponent = first, guard = 0; exponent <= last && guard < 1e3; exponent += step, guard++) {
    const value = decimalPower(exponent);
    if (Number.isFinite(value) && value >= min && value <= max) values.push(value);
  }
  if (values.length < 2) values.push(min, max);
  values.sort((a, b) => a - b);
  return ticksFromValues(values);
}
function linearTicks(min, max, target = 6) {
  if (!Number.isFinite(min) || !Number.isFinite(max) || max < min || !Number.isFinite(target) || !(target > 0)) return [];
  if (min === max) {
    return ticksFromValues([min]);
  }
  const span = max - min;
  if (!Number.isFinite(span)) {
    const values = [min, ...min < 0 && max > 0 ? [0] : [], max];
    return ticksFromValues(values);
  }
  const rawStep = span / Math.max(1, target);
  const step = niceStep(rawStep);
  const start = Math.ceil(min / step) * step;
  if (!Number.isFinite(start)) {
    return ticksFromValues([min, max]);
  }
  const ticks = [];
  for (let value = start, guard = 0; value <= max + step * 1e-9 && guard < 1e3; guard++) {
    const snapped = Math.round(value / step) * step;
    if (Number.isFinite(snapped) && snapped >= min && snapped <= max && (ticks.length === 0 || snapped !== ticks[ticks.length - 1].value)) {
      ticks.push({ value: snapped, label: formatNumber(snapped) });
    }
    const next = value + step;
    if (next === value || !Number.isFinite(next)) break;
    value = next;
  }
  return ticks.length > 1 ? ticksFromValues(ticks.map((tick) => tick.value)) : ticksFromValues([min, max]);
}

// src/core/numeric.ts
function finiteExtent(values) {
  let min = Infinity;
  let max = -Infinity;
  let found = false;
  for (const value of values) {
    if (!Number.isFinite(value)) continue;
    if (value < min) min = value;
    if (value > max) max = value;
    found = true;
  }
  return found ? { min, max } : void 0;
}
function finiteExtentBy(values, project) {
  let min = Infinity;
  let max = -Infinity;
  let found = false;
  for (const item of values) {
    const value = project(item);
    if (!Number.isFinite(value)) continue;
    if (value < min) min = value;
    if (value > max) max = value;
    found = true;
  }
  return found ? { min, max } : void 0;
}

// src/render/compile.ts
var MARGIN = { top: 60, right: 32, bottom: 56, left: 64 };
function panelBox(context) {
  const disclosureSpace = disclosureFooterHeight(context.width, context.disclosures);
  return {
    x: MARGIN.left,
    y: MARGIN.top,
    width: context.width - MARGIN.left - MARGIN.right,
    height: context.height - MARGIN.top - MARGIN.bottom - disclosureSpace
  };
}
function seriesColor(themeId, index) {
  const focus = THEMES[themeId]?.focus ?? "#0072b2";
  return index === 0 ? focus : focus;
}
function buildTable(columns, rowsTotal, rowAt) {
  const rows = Array.from({ length: rowsTotal }, (_value, index) => rowAt(index));
  return {
    policy: "complete_returned",
    columns,
    rows,
    rowsInline: rows.length,
    rowsTotal
  };
}
function frame(context, disclosures) {
  return {
    version: 1,
    figureId: `fig-${context.sourceRequestDigest.slice(7, 19)}`,
    width: context.width,
    height: context.height,
    title: context.title,
    ...context.subtitle ? { subtitle: context.subtitle } : {},
    themeId: context.themeId,
    disclosures: disclosures.map((d) => ({ id: d.id, severity: d.severity, text: d.text })),
    sourceRequestDigest: context.sourceRequestDigest
  };
}
function compileStepFigure(context, binStart, binEnd, values, xLabel, yLabel, skillId, outputAuthorityClassId) {
  const box = panelBox(context);
  const noData = values.length === 0;
  let panel;
  if (noData) {
    panel = { id: "main", ...box, axes: [], marks: [], noData: { reason: "no bins to plot" } };
  } else {
    const xMin = binStart[0];
    const xMax = binEnd[binEnd.length - 1];
    const yMax = Math.max(0, finiteExtent(values)?.max ?? 0);
    const xScale = linearScale(xMin, xMax, box.x, box.x + box.width);
    const yScale = linearScale(0, yMax, box.y + box.height, box.y);
    const stepPoints = [];
    for (let i = 0; i < values.length; i++) {
      const yPixel = yScale.map(values[i]);
      stepPoints.push({
        x: xScale.map(binStart[i]),
        y: yPixel,
        ...outputAuthorityClassId ? {
          authority: {
            tag: "data_carrier",
            classId: outputAuthorityClassId,
            provenance: {
              binIndex: i,
              binStart: binStart[i],
              binEnd: binEnd[i]
            }
          }
        } : {}
      });
      stepPoints.push({
        x: xScale.map(binEnd[i]),
        y: yPixel,
        ...outputAuthorityClassId ? { authority: { tag: "connector" } } : {}
      });
    }
    const marks = [
      { type: "path", subpaths: [stepPoints], stroke: seriesColor(context.themeId, 0), strokeWidth: 1.5 }
    ];
    const axes = [
      {
        orientation: "bottom",
        label: xLabel,
        transform: "linear",
        ticks: linearTicks(xMin, xMax).map((t) => ({ position: xScale.map(t.value), label: t.label }))
      },
      {
        orientation: "left",
        label: yLabel,
        transform: "linear",
        ticks: linearTicks(0, yMax).map((t) => ({ position: yScale.map(t.value), label: t.label }))
      }
    ];
    panel = { id: "main", ...box, axes, marks };
  }
  return {
    ...frame(context, context.disclosures),
    skillId,
    panels: [panel],
    table: buildTable(
      [
        { key: "binStart", header: "Bin start" },
        { key: "binEnd", header: "Bin end" },
        { key: "value", header: yLabel }
      ],
      values.length,
      (i) => [binStart[i], binEnd[i], values[i]]
    ),
    accessibility: { summary: context.summary, panelSummaries: [] }
  };
}

// src/render/compileFamilies.ts
var MARGIN2 = { top: 60, right: 32, bottom: 56, left: 64 };
function panelBox2(context) {
  const disclosureSpace = disclosureFooterHeight(context.width, context.disclosures);
  return {
    x: MARGIN2.left,
    y: MARGIN2.top,
    width: context.width - MARGIN2.left - MARGIN2.right,
    height: context.height - MARGIN2.top - MARGIN2.bottom - disclosureSpace
  };
}
function accent(themeId) {
  return THEMES[themeId]?.focus ?? "#0072b2";
}
function gridColor(themeId) {
  return THEMES[themeId]?.grid ?? "#e2e6ea";
}
function uncertaintyStroke(themeId) {
  return THEMES[themeId]?.axis ?? "#3a4046";
}
function missingColor(themeId) {
  return THEMES[themeId]?.missing ?? "#767e87";
}
function categoricalStyle(index) {
  const style = CATEGORICAL_SERIES_STYLES[index % CATEGORICAL_SERIES_STYLES.length];
  return {
    color: style.color,
    dash: style.dash,
    marker: style.marker
  };
}
function frame2(context, skillId) {
  return {
    version: 1,
    figureId: `fig-${context.sourceRequestDigest.slice(7, 19)}`,
    skillId,
    width: context.width,
    height: context.height,
    title: context.title,
    ...context.subtitle ? { subtitle: context.subtitle } : {},
    themeId: context.themeId,
    disclosures: context.disclosures.map((d) => ({ id: d.id, severity: d.severity, text: d.text })),
    sourceRequestDigest: context.sourceRequestDigest,
    accessibility: { summary: context.summary, panelSummaries: [] }
  };
}
function emptyPanel(box, reason) {
  return { id: "main", ...box, axes: [], marks: [], noData: { reason } };
}
function traceMarkerObservationIndexes(entry) {
  return entry.markerObservationIndexes ?? entry.series.observations.map((_observation, index) => index);
}
function traceYValues(entry, includeSamplePoints = false) {
  const rendered = entry.renderRuns?.flatMap((run) => [
    ...run.values,
    ...run.uncertaintyLower ?? [],
    ...run.uncertaintyUpper ?? []
  ]);
  const markerValues = includeSamplePoints ? traceMarkerObservationIndexes(entry).map((index) => entry.series.observations[index]?.value ?? null) : [];
  return [
    ...rendered ?? entry.series.values,
    ...markerValues,
    ...entry.uncertaintyLower ?? [],
    ...entry.uncertaintyUpper ?? []
  ].filter((value) => value !== null && Number.isFinite(value));
}
function traceXValues(entry, includeSamplePoints = false) {
  const rendered = entry.renderRuns?.flatMap((run) => [...run.times]);
  const markerTimes = includeSamplePoints ? traceMarkerObservationIndexes(entry).map((index) => entry.series.observations[index]?.time) : [];
  return [
    ...rendered ?? entry.series.times,
    ...markerTimes
  ].filter((value) => Number.isFinite(value));
}
function cloneAuthorityJson(value) {
  if (value === null || typeof value !== "object") return value;
  if (Array.isArray(value)) return value.map(cloneAuthorityJson);
  return Object.fromEntries(
    Object.entries(value).map(([key, child]) => [key, cloneAuthorityJson(child)])
  );
}
function cloneAuthorityRole(role) {
  return role.tag === "data_carrier" ? {
    tag: "data_carrier",
    classId: role.classId,
    provenance: cloneAuthorityJson(role.provenance)
  } : { tag: role.tag };
}
function authorityCarrier(entry, observationIndex, classId, atomKind, extra = {}) {
  const provenance = entry.outputAuthority?.observationProvenance[observationIndex];
  if (provenance === void 0 || provenance === null || typeof provenance !== "object" || Array.isArray(provenance)) {
    return void 0;
  }
  return {
    tag: "data_carrier",
    classId,
    provenance: {
      ...cloneAuthorityJson(provenance),
      atomKind,
      ...cloneAuthorityJson(extra)
    }
  };
}
function retagCarrier(role, classId, atomKind) {
  if (role?.tag !== "data_carrier" || role.provenance === null || typeof role.provenance !== "object" || Array.isArray(role.provenance)) {
    return void 0;
  }
  return {
    tag: "data_carrier",
    classId,
    provenance: {
      ...cloneAuthorityJson(role.provenance),
      atomKind
    }
  };
}
function traceConnectorRole() {
  return { tag: "connector" };
}
function distributionDecorativeRole() {
  return { tag: "decorative_mark" };
}
function traceSubpaths(entry, xScale, yScale) {
  if (entry.renderRuns) {
    return entry.renderRuns.flatMap((run) => {
      const points = run.times.map((time, index) => ({
        x: xScale.map(time),
        y: yScale.map(run.values[index]),
        ...run.authority?.[index] ? { authority: cloneAuthorityRole(run.authority[index]) } : {}
      }));
      return points.length > 0 ? [points] : [];
    });
  }
  const series = entry.series;
  const subpaths = [];
  let current = [];
  for (let index = 0; index < series.times.length; index++) {
    const value = series.values[index];
    if (value === null || !Number.isFinite(value)) {
      if (current.length > 0) {
        if (series.observationKind === "piecewise_constant") {
          const previous = current[current.length - 1];
          current.push({
            x: xScale.map(series.times[index]),
            y: previous.y,
            authority: traceConnectorRole()
          });
        }
        subpaths.push(current);
      }
      current = [];
      continue;
    }
    const point = {
      x: xScale.map(series.times[index]),
      y: yScale.map(value),
      ...entry.outputAuthority ? {
        authority: authorityCarrier(
          entry,
          index,
          entry.outputAuthority.pathClassId,
          entry.authorityAtomKinds?.pathVertex ?? "observation_vertex"
        )
      } : {}
    };
    if (series.observationKind === "piecewise_constant" && current.length > 0) {
      const previous = current[current.length - 1];
      current.push({ x: point.x, y: previous.y, authority: traceConnectorRole() });
    }
    current.push(point);
  }
  if (current.length > 0) subpaths.push(current);
  return subpaths;
}
function traceAreaSubpaths(entry, xScale, yScale, includeSamplePoints = false) {
  if (entry.renderRuns) {
    const subpaths2 = entry.renderRuns.flatMap((run) => {
      const lower2 = run.uncertaintyLower;
      const upper2 = run.uncertaintyUpper;
      if (!lower2 || !upper2 || lower2.length !== run.times.length || upper2.length !== lower2.length) {
        return [];
      }
      const subpaths3 = [];
      let current2 = [];
      for (let index = 0; index < run.times.length; index++) {
        const lo = lower2[index];
        const hi = upper2[index];
        if (lo === null || hi === null) {
          if (current2.length > 0) subpaths3.push(current2);
          current2 = [];
          continue;
        }
        current2.push({
          x: xScale.map(run.times[index]),
          y0: yScale.map(lo),
          y1: yScale.map(hi),
          ...run.authority?.[index] ? {
            authority: entry.outputAuthority ? retagCarrier(
              run.authority[index],
              entry.outputAuthority.uncertaintyClassId,
              "uncertainty_band_vertex"
            ) : cloneAuthorityRole(run.authority[index])
          } : {}
        });
      }
      if (current2.length > 0) subpaths3.push(current2);
      return subpaths3;
    });
    if (includeSamplePoints) {
      const renderedLineage = /* @__PURE__ */ new Set();
      for (const run of entry.renderRuns) {
        for (const role of run.authority ?? []) {
          if (role.tag !== "data_carrier" || role.provenance === null || typeof role.provenance !== "object" || Array.isArray(role.provenance) || !Array.isArray(role.provenance.sourceOrdinals)) continue;
          renderedLineage.add(JSON.stringify(role.provenance.sourceOrdinals));
        }
      }
      const lower2 = entry.uncertaintyLower;
      const upper2 = entry.uncertaintyUpper;
      for (const index of traceMarkerObservationIndexes(entry)) {
        const observation = entry.series.observations[index];
        const lo = lower2?.[index];
        const hi = upper2?.[index];
        if (observation === void 0 || observation.value === null || renderedLineage.has(JSON.stringify(observation.sourceOrdinals)) || typeof lo !== "number" || !Number.isFinite(lo) || typeof hi !== "number" || !Number.isFinite(hi)) continue;
        subpaths2.push([{
          x: xScale.map(observation.time),
          y0: yScale.map(lo),
          y1: yScale.map(hi),
          ...entry.outputAuthority ? {
            authority: authorityCarrier(
              entry,
              index,
              entry.outputAuthority.uncertaintyClassId,
              "uncertainty_band_vertex"
            )
          } : {}
        }]);
      }
    }
    return subpaths2;
  }
  const lower = entry.uncertaintyLower;
  const upper = entry.uncertaintyUpper;
  if (!lower || !upper || lower.length !== entry.series.times.length || upper.length !== lower.length) {
    return [];
  }
  const subpaths = [];
  let current = [];
  for (let index = 0; index < lower.length; index++) {
    const lo = lower[index];
    const hi = upper[index];
    const x = xScale.map(entry.series.times[index]);
    if (lo === null || hi === null || !Number.isFinite(lo) || !Number.isFinite(hi)) {
      if (current.length > 0) {
        if (entry.series.observationKind === "piecewise_constant") {
          const previous = current[current.length - 1];
          current.push({ x, y0: previous.y0, y1: previous.y1, authority: traceConnectorRole() });
        }
        subpaths.push(current);
      }
      current = [];
      continue;
    }
    const point = {
      x,
      y0: yScale.map(lo),
      y1: yScale.map(hi),
      ...entry.outputAuthority ? {
        authority: authorityCarrier(
          entry,
          index,
          entry.outputAuthority.uncertaintyClassId,
          "uncertainty_band_vertex"
        )
      } : {}
    };
    if (entry.series.observationKind === "piecewise_constant" && current.length > 0) {
      const previous = current[current.length - 1];
      current.push({ x, y0: previous.y0, y1: previous.y1, authority: traceConnectorRole() });
    }
    current.push(point);
  }
  if (current.length > 0) subpaths.push(current);
  return subpaths;
}
function traceWhiskerMarks(entry, xScale, yScale, color) {
  const lower = entry.uncertaintyLower;
  const upper = entry.uncertaintyUpper;
  if (!lower || !upper || lower.length !== entry.series.times.length || upper.length !== lower.length) {
    return void 0;
  }
  const vertical = [];
  const caps = [];
  const halfCap = 3;
  for (let index = 0; index < lower.length; index++) {
    const lo = lower[index];
    const hi = upper[index];
    if (lo === null || hi === null || !Number.isFinite(lo) || !Number.isFinite(hi)) continue;
    const x = xScale.map(entry.series.times[index]);
    const y0 = yScale.map(lo);
    const y1 = yScale.map(hi);
    vertical.push({
      position: x,
      from: y0,
      to: y1,
      ...entry.outputAuthority ? {
        authority: authorityCarrier(
          entry,
          index,
          entry.outputAuthority.uncertaintyClassId,
          "uncertainty_whisker",
          { part: "shaft" }
        )
      } : {}
    });
    caps.push(
      {
        position: y0,
        from: x - halfCap,
        to: x + halfCap,
        ...entry.outputAuthority ? {
          authority: authorityCarrier(
            entry,
            index,
            entry.outputAuthority.uncertaintyClassId,
            "uncertainty_whisker",
            { part: "lower_cap" }
          )
        } : {}
      },
      {
        position: y1,
        from: x - halfCap,
        to: x + halfCap,
        ...entry.outputAuthority ? {
          authority: authorityCarrier(
            entry,
            index,
            entry.outputAuthority.uncertaintyClassId,
            "uncertainty_whisker",
            { part: "upper_cap" }
          )
        } : {}
      }
    );
  }
  if (vertical.length === 0) return void 0;
  return {
    type: "group",
    id: `uncertainty-${entry.styleIndex}`,
    marks: [
      { type: "rule", orientation: "vertical", lines: vertical, stroke: color, strokeWidth: 1.25 },
      { type: "rule", orientation: "horizontal", lines: caps, stroke: color, strokeWidth: 1.25 }
    ]
  };
}
function subpathPaintsLine(subpath) {
  if (subpath.length < 2) return false;
  const first = subpath[0];
  return subpath.some((point) => point.x !== first.x || point.y !== first.y);
}
function compileTraceFigure(context, panels, options, skillId) {
  const allSeries = panels.flatMap((panel) => panel.series);
  const legendById = /* @__PURE__ */ new Map();
  for (const entry of allSeries) {
    if (!legendById.has(entry.series.id)) legendById.set(entry.series.id, entry);
  }
  const legendEntries = [...legendById.values()].filter((entry) => entry.includeInLegend !== false);
  const uncertaintyLegendEntries = legendEntries.filter((entry) => entry.uncertainty !== void 0);
  const base = panelBox2(context);
  const legendInset = legendPlotInset(
    context.width,
    legendEntries.length + uncertaintyLegendEntries.length,
    context.subtitle !== void 0
  );
  const panelGap = panels.length > 1 ? options.sharedXAxis ? 22 : 50 : 0;
  const availableHeight = base.height - legendInset - panelGap * Math.max(0, panels.length - 1);
  const panelHeight = panels.length > 0 ? availableHeight / panels.length : availableHeight;
  const globalYValues = allSeries.flatMap((entry) => traceYValues(entry, options.showSamplePoints === true));
  const globalYExtent = options.sharedYDomain ? finiteExtent(globalYValues) : void 0;
  const entriesWithPointMarks = /* @__PURE__ */ new Set();
  const compiledPanels = panels.map((panelSpec, panelIndex) => {
    const box = {
      x: base.x,
      y: base.y + legendInset + panelIndex * (panelHeight + panelGap),
      width: base.width,
      height: panelHeight
    };
    const finiteTimes = panelSpec.series.flatMap((entry) => traceXValues(entry, options.showSamplePoints === true));
    const finiteValues = [
      ...panelSpec.series.flatMap((entry) => traceYValues(entry, options.showSamplePoints === true)),
      ...(panelSpec.referenceLines ?? []).map((reference) => reference.value)
    ];
    if (finiteTimes.length === 0 || finiteValues.length === 0) {
      return { ...emptyPanel(box, "no finite observations to plot"), id: panelSpec.id, label: panelSpec.label };
    }
    const xExtent = options.xDomain ? { min: options.xDomain[0], max: options.xDomain[1] } : finiteExtent(finiteTimes);
    const yExtent = globalYExtent ?? finiteExtent(finiteValues);
    const xScale = linearScale(xExtent.min, xExtent.max, box.x, box.x + box.width);
    const scaleKind = panelSpec.scale ?? "linear";
    const yScale = scaleKind === "log" ? logNumericScale(yExtent.min, yExtent.max, box.y + box.height, box.y) : scaleKind === "symlog" ? symlogNumericScale(
      yExtent.min,
      yExtent.max,
      box.y + box.height,
      box.y,
      panelSpec.symlogLinearThreshold
    ) : linearNumericScale(yExtent.min, yExtent.max, box.y + box.height, box.y);
    const marks = [];
    for (const entry of panelSpec.series) {
      const style = entry.style ?? categoricalStyle(entry.styleIndex);
      const boundaryColor = uncertaintyStroke(context.themeId);
      const seriesMarks = [];
      if (entry.uncertainty?.mark === "whisker") {
        const whiskers = traceWhiskerMarks(entry, xScale, yScale, boundaryColor);
        if (whiskers) seriesMarks.push(whiskers);
      } else if (entry.uncertainty?.mark === "band") {
        const uncertaintySubpaths = traceAreaSubpaths(
          entry,
          xScale,
          yScale,
          options.showSamplePoints === true
        );
        if (uncertaintySubpaths.length > 0) {
          seriesMarks.push({
            type: "area",
            subpaths: uncertaintySubpaths,
            fill: style.color,
            opacity: 0.18,
            stroke: boundaryColor,
            strokeWidth: 1.25
          });
          const degeneratePoints = uncertaintySubpaths.filter((subpath) => subpath.length > 0 && subpath.every(
            (point) => point.x === subpath[0].x && point.y0 === subpath[0].y0 && point.y1 === subpath[0].y1
          ) && subpath[0].y0 === subpath[0].y1).map((subpath) => ({
            x: subpath[0].x,
            y: subpath[0].y0,
            ...entry.outputAuthority ? {
              authority: retagCarrier(
                subpath[0].authority,
                entry.outputAuthority.uncertaintyClassId,
                "uncertainty_degenerate_marker"
              )
            } : {}
          }));
          if (degeneratePoints.length > 0) {
            seriesMarks.push({ type: "point", points: degeneratePoints, fill: boundaryColor, radius: 4, shape: "diamond" });
          }
        }
      }
      const lineSubpaths = traceSubpaths(entry, xScale, yScale);
      const drawableLines = lineSubpaths.filter(subpathPaintsLine);
      if (drawableLines.length > 0) {
        seriesMarks.push({
          type: "line",
          subpaths: drawableLines,
          stroke: style.color,
          strokeWidth: 1.5,
          dash: style.dash
        });
      }
      const points = [];
      if (options.showSamplePoints) {
        const markerIndexes = entry.markerObservationIndexes ?? entry.series.times.map((_time, index) => index);
        for (const index of markerIndexes) {
          const value = entry.series.values[index];
          if (value !== null && Number.isFinite(value)) {
            points.push({
              x: xScale.map(entry.series.times[index]),
              y: yScale.map(value),
              ...entry.outputAuthority ? {
                authority: authorityCarrier(
                  entry,
                  index,
                  entry.outputAuthority.sampleClassId,
                  entry.authorityAtomKinds?.sampleMarker ?? "observation_marker"
                )
              } : {}
            });
          }
        }
      } else {
        for (const subpath of lineSubpaths) {
          if (!subpathPaintsLine(subpath)) {
            const first = subpath[0];
            points.push({
              ...first,
              ...entry.outputAuthority ? {
                authority: retagCarrier(
                  first.authority,
                  entry.outputAuthority.sampleClassId,
                  entry.authorityAtomKinds?.isolatedMarker ?? "isolated_observation_marker"
                )
              } : {}
            });
          }
        }
      }
      if (points.length > 0) {
        seriesMarks.push({ type: "point", points, fill: style.color, radius: 2.4, shape: style.marker });
        entriesWithPointMarks.add(entry);
      }
      if (seriesMarks.length > 0) {
        if (options.groupSeriesMarks) {
          marks.push({ type: "group", id: `series-${entry.series.id}`, marks: seriesMarks });
        } else {
          marks.push(...seriesMarks);
        }
      }
    }
    for (const reference of panelSpec.referenceLines ?? []) {
      marks.push({
        type: "group",
        id: `reference-${reference.id}`,
        marks: [{
          type: "rule",
          orientation: "horizontal",
          lines: [{
            position: yScale.map(reference.value),
            from: box.x,
            to: box.x + box.width,
            ...reference.authority ? { authority: cloneAuthorityRole(reference.authority) } : {}
          }],
          stroke: reference.color,
          strokeWidth: 1,
          dash: reference.dash ?? "4 3"
        }]
      });
    }
    return {
      id: panelSpec.id,
      label: panelSpec.label,
      ...box,
      axes: [
        ...options.sharedXAxis && panels.length > 1 && panelIndex < panels.length - 1 ? [] : [xAxis(options.xLabel, xScale)],
        transformedYAxis(panelSpec.yLabel, yScale)
      ],
      marks
    };
  });
  const rowsTotal = allSeries.reduce((total, entry) => total + entry.series.outputCount, 0);
  const rows = [];
  for (const panel of panels) {
    for (const entry of panel.series) {
      for (let index = 0; index < entry.series.times.length; index++) {
        rows.push(options.tableRow ? [...options.tableRow(entry, index, panel)] : [
          entry.series.id,
          entry.series.label,
          entry.series.times[index],
          entry.series.values[index],
          entry.series.timeUnit,
          entry.series.valueUnit,
          entry.series.replicateCounts[index]
        ]);
      }
    }
  }
  return {
    ...frame2(context, skillId),
    panels: compiledPanels.length > 0 ? compiledPanels : [emptyPanel({ ...base, y: base.y + legendInset, height: availableHeight }, "no trace panels declared")],
    legend: [
      ...legendEntries.map((entry) => {
        const style = entry.style ?? categoricalStyle(entry.styleIndex);
        return {
          label: entry.series.label || entry.series.id,
          color: style.color,
          glyph: "series",
          dash: style.dash,
          ...entriesWithPointMarks.has(entry) ? { marker: style.marker } : {}
        };
      }),
      ...uncertaintyLegendEntries.map((entry) => {
        const style = entry.style ?? categoricalStyle(entry.styleIndex);
        return {
          label: `${entry.series.label || entry.series.id}: ${entry.uncertainty.label}`,
          color: style.color,
          outlineColor: uncertaintyStroke(context.themeId),
          glyph: entry.uncertainty.mark
        };
      }),
      ...panels.flatMap((panel) => (panel.referenceLines ?? []).map((reference) => ({
        label: reference.label,
        color: reference.color,
        glyph: "rule",
        dash: reference.dash ?? "4 3"
      })))
    ],
    accessibility: {
      summary: context.summary,
      panelSummaries: [
        ...options.summaryStatements ?? [],
        ...panels.map((panel) => {
          const extent = finiteExtent(panel.series.flatMap((entry) => traceYValues(entry, options.showSamplePoints === true)));
          return `${panel.label}: ${panel.series.length} ${panel.series.length === 1 ? "series" : "series"}; y axis ${panel.yLabel}; ${panel.scale ?? "linear"} value scale${extent ? `; displayed range ${formatNumber(extent.min)} to ${formatNumber(extent.max)}` : ""}.`;
        })
      ],
      ...panels.length > 1 ? { suppressGlobalValueRange: true } : {}
    },
    table: {
      policy: "complete_returned",
      columns: options.tableColumns ?? [
        { key: "seriesId", header: "Series" },
        { key: "seriesLabel", header: "Label" },
        { key: "time", header: "Time" },
        { key: "value", header: "Value" },
        { key: "timeUnit", header: "Time unit" },
        { key: "valueUnit", header: "Value unit" },
        { key: "replicateCount", header: "Replicates" }
      ],
      rows,
      rowsInline: rows.length,
      rowsTotal
    }
  };
}
function transformedYAxis(label, scale) {
  return {
    orientation: "left",
    label,
    transform: scale.transform,
    ticks: scale.ticks().map((tick) => ({ position: scale.map(tick.value), label: tick.label }))
  };
}
function xAxis(label, scale) {
  return {
    orientation: "bottom",
    label,
    transform: "linear",
    ticks: linearTicks(scale.domainMin, scale.domainMax).map((t) => ({ position: scale.map(t.value), label: t.label }))
  };
}
function yAxis(label, scale) {
  return {
    orientation: "left",
    label,
    transform: "linear",
    ticks: linearTicks(scale.domainMin, scale.domainMax).map((t) => ({ position: scale.map(t.value), label: t.label }))
  };
}
function compileBarFigure(context, edges, values, xLabel, yLabel, skillId, xScaleKind = "linear", options = {}) {
  const box = panelBox2(context);
  const table = barTable(
    edges,
    values,
    yLabel,
    options.tableMetadata ?? []
  );
  const accessibility = {
    // The global summary is the exact source-template materialization checked by
    // OutputAuthority.  Supplemental accounting belongs in its separately exposed
    // panel-summary channel; appending it here would make the final plan cease to be
    // an exact one-pass substitution even though no scientific fact was wrong.
    summary: context.summary,
    panelSummaries: [...options.summaryStatements ?? []]
  };
  if (values.length === 0 || edges.length < 2) {
    return {
      ...frame2(context, skillId),
      panels: [emptyPanel(box, "no bins to plot")],
      table,
      legend: [],
      accessibility
    };
  }
  const xMin = edges[0];
  const xMax = edges[edges.length - 1];
  const yMax = Math.max(0, finiteExtent(values)?.max ?? 0);
  const xScale = xScaleKind === "log" ? logNumericScale(xMin, xMax, box.x, box.x + box.width) : linearNumericScale(xMin, xMax, box.x, box.x + box.width);
  const yScale = linearScale(0, yMax, box.y + box.height, box.y);
  const baseline = yScale.map(0);
  const rects = values.map((value, i) => {
    const x0 = xScale.map(edges[i]);
    const x1 = xScale.map(edges[i + 1]);
    const yTop = yScale.map(value);
    return {
      x: x0,
      y: yTop,
      width: Math.max(0, x1 - x0 - 1),
      height: Math.max(0, baseline - yTop),
      fill: accent(context.themeId),
      ...options.outputAuthority ? {
        authority: {
          tag: "data_carrier",
          classId: options.outputAuthority.classId,
          provenance: {
            groupId: options.outputAuthority.groupId,
            binIndex: i,
            binStart: edges[i],
            binEnd: edges[i + 1]
          }
        }
      } : {}
    };
  });
  const marks = [{ type: "rect", rects }];
  const axes = [transformedXAxis(xLabel, xScale), yAxis(yLabel, yScale)];
  return {
    ...frame2(context, skillId),
    panels: [{ id: "main", ...box, axes, marks }],
    table,
    legend: [],
    accessibility
  };
}
var PSTH_TABLE_COLUMNS = [
  { key: "seriesId", header: "Series id" },
  { key: "seriesLabel", header: "Series" },
  { key: "alignmentLabel", header: "Alignment" },
  { key: "binStart", header: "Bin start (relative)" },
  { key: "binEnd", header: "Bin end (relative)" },
  { key: "binWidth", header: "Bin width" },
  { key: "binUnit", header: "Bin unit" },
  { key: "relativeWindowStart", header: "Relative-window start" },
  { key: "relativeWindowStop", header: "Relative-window stop" },
  { key: "relativeWindowUnit", header: "Relative-window unit" },
  { key: "relativeWindowBoundary", header: "Relative-window boundary" },
  { key: "binBoundary", header: "Bin boundary" },
  { key: "finalEdgeInclusive", header: "Final edge inclusive" },
  { key: "count", header: "Events" },
  { key: "trialDenominator", header: "Covering trials" },
  { key: "includedTrialCount", header: "Included trials" },
  { key: "excludedTrialCount", header: "Excluded trials" },
  { key: "excludedOutOfWindowCount", header: "Events excluded (out of window)" },
  { key: "selectedSenderCount", header: "Selected senders" },
  { key: "normalization", header: "Normalization" },
  { key: "denominatorPolicy", header: "Denominator policy" },
  { key: "senderExposurePolicy", header: "Sender-exposure policy" },
  { key: "value", header: "Value" },
  { key: "valueUnit", header: "Unit" },
  { key: "baselineCorrectedValue", header: "Value minus baseline" },
  { key: "baselineMode", header: "Baseline mode" },
  { key: "baselineRate", header: "Baseline rate" },
  { key: "baselineStart", header: "Baseline start" },
  { key: "baselineStop", header: "Baseline stop" },
  { key: "baselineUnit", header: "Baseline unit" }
];
function compilePsthFigure(context, psth, options, skillId) {
  const box = panelBox2(context);
  const rowsTotal = psth.counts.length;
  const rows = Array.from({ length: rowsTotal }, (_unused, index) => [
    options.seriesId,
    options.seriesLabel,
    options.alignmentLabel,
    psth.edges[index],
    psth.edges[index + 1],
    psth.binWidths[index],
    psth.binUnit,
    psth.relativeWindowStart,
    psth.relativeWindowStop,
    psth.relativeWindowUnit,
    psth.relativeWindowBoundary,
    psth.binBoundary,
    psth.finalEdgeInclusive ? "true" : "false",
    psth.counts[index],
    psth.trialDenominators[index],
    psth.includedTrialCount,
    psth.excludedTrialCount,
    psth.excludedOutOfWindowCount,
    psth.selectedSenderCount,
    options.normalization,
    options.denominatorPolicy,
    psth.senderExposurePolicy,
    psth.values[index],
    psth.valueUnit,
    psth.baselineRate === null ? null : psth.baselineCorrectedValues[index],
    psth.baselineRate === null ? null : "subtract_mean_rate",
    psth.baselineRate,
    psth.baselineStart,
    psth.baselineStop,
    psth.baselineUnit
  ]);
  const table = {
    policy: "complete_returned",
    columns: PSTH_TABLE_COLUMNS,
    rows,
    rowsInline: rows.length,
    rowsTotal
  };
  const finiteDisplay = psth.displayValues.filter(
    (value) => value !== null && Number.isFinite(value)
  );
  const plottedExtent = finiteExtent(finiteDisplay);
  const xScale = linearScale(
    psth.edges[0],
    psth.edges[psth.edges.length - 1],
    box.x,
    box.x + box.width
  );
  const xAxisModel = xAxis(
    `time from ${options.alignmentLabel} (${psth.binUnit})`,
    xScale
  );
  const normalizationLabel = options.normalization === "count" ? "event count" : options.normalization === "count_per_trial" ? "events / covering trial" : options.normalization === "total_event_rate_per_trial" ? `selected-group event rate / covering trial (${psth.valueUnit})` : `mean event rate / selected sender / covering trial (${psth.valueUnit})`;
  const yLabel = psth.baselineRate === null ? normalizationLabel : `${normalizationLabel} minus baseline`;
  const zeroOnDisplayedAxis = psth.edges[0] <= 0 && 0 <= psth.edges[psth.edges.length - 1];
  const zeroIncludedByMembership = zeroOnDisplayedAxis && (0 < psth.edges[psth.edges.length - 1] || 0 === psth.edges[psth.edges.length - 1] && psth.finalEdgeInclusive);
  const summaryStatements = [
    `${options.seriesLabel}, aligned to ${options.alignmentLabel}: relative window ${psth.relativeWindowStart} to ${psth.relativeWindowStop} ${psth.relativeWindowUnit} with boundary ${psth.relativeWindowBoundary}; ${psth.counts.length} explicit bins in ${psth.binUnit} with boundary ${psth.binBoundary}; final-edge inclusion ${psth.finalEdgeInclusive}.`,
    `${psth.exactCountTotal} counted events; selected-sender cardinality ${psth.selectedSenderCount}; included-trial cardinality ${psth.includedTrialCount}; excluded-trial cardinality ${psth.excludedTrialCount}; denominator policy ${options.denominatorPolicy}; normalization ${options.normalization}.`,
    psth.mode === "prebinned" ? "The pre-binned aggregate retains exact sender and trial cardinalities but not selected-sender identities or the membership partition between included and excluded trial ids; those identities cannot be recovered from this artifact." : "Events mode retains the complete selected-sender and included-trial identity universes; it has no excluded-trial partition.",
    psth.senderExposurePolicy === null ? "No per-selected-sender exposure divisor was applied." : "The per-selected-sender rate uses the explicit all-selected-senders/all-counted-trial-bins rectangular-exposure assertion; Cortexel cannot verify that exposure from event presence.",
    psth.excludedOutOfWindowCount === null ? "The pre-binned input does not expose an out-of-window event count." : `${psth.excludedOutOfWindowCount} source events were excluded by the relative window before bin placement.`,
    psth.missingBinCount > 0 ? psth.missingBinCount === 1 ? "1 bin has no covering trial and remains a missing gap, not a measured zero." : `${psth.missingBinCount} bins have no covering trial and remain missing gaps, not measured zeros.` : "Every bin has positive declared trial exposure.",
    psth.baselineRate === null ? "No baseline subtraction was requested." : `The aggregate baseline is ${formatNumber(psth.baselineRate)} ${psth.valueUnit}, re-derived from raw counts and summed exact exposure; plotted values subtract it while the table retains uncorrected values and counts.`,
    psth.baselineRate !== null && plottedExtent ? `The plotted baseline-corrected range is ${formatNumber(plottedExtent.min)} to ${formatNumber(plottedExtent.max)} ${psth.valueUnit}.` : psth.baselineRate !== null ? "No baseline-corrected value is observable because every displayed bin is missing." : "The plotted values are the uncorrected normalization.",
    zeroIncludedByMembership ? "Relative time zero is included by the displayed membership window and is shown as a vertical reference." : zeroOnDisplayedAxis ? "Relative time zero lies on the displayed half-open stop boundary and is shown as a boundary reference, but events at zero are excluded by membership." : "Relative time zero lies outside the displayed window, so no zero reference line is visible; the declared alignment still defines every relative coordinate."
  ];
  if (finiteDisplay.length === 0) {
    return {
      ...frame2(context, skillId),
      panels: [{
        id: "main",
        ...box,
        axes: [xAxisModel],
        marks: [],
        noData: { reason: "no included trial covered any PSTH bin" }
      }],
      table,
      legend: [],
      accessibility: {
        summary: context.summary,
        panelSummaries: summaryStatements,
        suppressGlobalValueRange: psth.baselineRate !== null
      }
    };
  }
  const displayExtent = finiteExtent(finiteDisplay);
  const yMinimum = Math.min(0, displayExtent.min);
  const yMaximum = Math.max(0, displayExtent.max);
  const yScale = psth.baselineRate === null && yMinimum === 0 && yMaximum === 0 ? {
    domainMin: 0,
    domainMax: 0,
    rangeMin: box.y + box.height,
    rangeMax: box.y,
    transform: "linear",
    map(value) {
      if (!Number.isFinite(value)) return Number.NaN;
      return value <= 0 ? box.y + box.height : box.y;
    },
    ticks() {
      return [{ value: 0, label: "0" }];
    }
  } : linearNumericScale(yMinimum, yMaximum, box.y + box.height, box.y);
  const zero = yScale.map(0);
  const rects = psth.displayValues.flatMap((value, index) => {
    if (value === null || !Number.isFinite(value)) return [];
    const x0 = xScale.map(psth.edges[index]);
    const x1 = xScale.map(psth.edges[index + 1]);
    const valueY = yScale.map(value);
    return [{
      x: x0,
      y: Math.min(zero, valueY),
      width: x1 - x0,
      height: Math.abs(zero - valueY),
      fill: accent(context.themeId),
      authority: {
        tag: "data_carrier",
        classId: "bins",
        provenance: {
          binIndex: index,
          binStart: psth.edges[index],
          binEnd: psth.edges[index + 1]
        }
      }
    }];
  });
  const marks = [{ type: "rect", rects }];
  if (psth.missingBinCount > 0) {
    const lines = psth.displayValues.flatMap((value, index) => value === null ? [{
      position: (xScale.map(psth.edges[index]) + xScale.map(psth.edges[index + 1])) / 2,
      from: box.y,
      to: box.y + box.height,
      authority: distributionDecorativeRole()
    }] : []);
    marks.push({
      type: "rule",
      orientation: "vertical",
      lines,
      stroke: missingColor(context.themeId),
      strokeWidth: 1,
      dash: "2 3"
    });
  }
  if (zeroOnDisplayedAxis) {
    marks.push({
      type: "rule",
      orientation: "vertical",
      lines: [{
        position: xScale.map(0),
        from: box.y,
        to: box.y + box.height,
        authority: distributionDecorativeRole()
      }],
      stroke: uncertaintyStroke(context.themeId),
      strokeWidth: 1.25,
      dash: "4 3"
    });
  }
  if (psth.baselineRate !== null) {
    marks.push({
      type: "rule",
      orientation: "horizontal",
      lines: [{
        position: zero,
        from: box.x,
        to: box.x + box.width,
        authority: distributionDecorativeRole()
      }],
      stroke: uncertaintyStroke(context.themeId),
      strokeWidth: 1.25,
      dash: "4 3"
    });
  }
  return {
    ...frame2(context, skillId),
    panels: [{
      id: "main",
      label: options.seriesLabel,
      ...box,
      axes: [xAxisModel, yAxis(yLabel, yScale)],
      marks
    }],
    table,
    legend: [
      { label: options.seriesLabel, color: accent(context.themeId), glyph: "series" },
      ...psth.missingBinCount > 0 ? [{
        label: "No included trial covered this bin",
        color: missingColor(context.themeId),
        glyph: "rule",
        dash: "2 3"
      }] : []
    ],
    accessibility: {
      summary: context.summary,
      panelSummaries: summaryStatements,
      suppressGlobalValueRange: psth.baselineRate !== null
    }
  };
}
function compileGroupedBarFigure(context, edges, groups, xLabel, yLabel, skillId, xScaleKind = "linear", options = {}) {
  const box = panelBox2(context);
  const accessibility = {
    // Keep group accounting discoverable without mutating the source-owned global
    // summary program after the compiler has closed its placeholder vocabulary.
    summary: context.summary,
    panelSummaries: [...options.summaryStatements ?? []]
  };
  if (groups.length === 0 || edges.length < 2) {
    return {
      ...frame2(context, skillId),
      panels: [emptyPanel(box, "no histogram groups to plot")],
      table: emptyTable(),
      legend: [],
      accessibility
    };
  }
  const xScale = xScaleKind === "log" ? logNumericScale(edges[0], edges[edges.length - 1], box.x, box.x + box.width) : linearNumericScale(edges[0], edges[edges.length - 1], box.x, box.x + box.width);
  const yMax = Math.max(
    0,
    finiteExtent(groups.flatMap((group) => group.values))?.max ?? 0
  );
  const yScale = linearScale(0, yMax, box.y + box.height, box.y);
  const baseline = yScale.map(0);
  const marks = groups.map((group, groupIndex) => {
    const style = categoricalStyle(groupIndex);
    const rects = group.values.map((value, binIndex2) => {
      const binX0 = xScale.map(edges[binIndex2]);
      const binX1 = xScale.map(edges[binIndex2 + 1]);
      const slotWidth = Math.max(0, binX1 - binX0) / groups.length;
      const x = binX0 + groupIndex * slotWidth;
      const y = yScale.map(value);
      return {
        x,
        y,
        width: Math.max(0, slotWidth - 0.75),
        height: Math.max(0, baseline - y),
        fill: style.color,
        ...options.outputAuthority ? {
          authority: {
            tag: "data_carrier",
            classId: options.outputAuthority.classId,
            provenance: {
              groupId: group.id,
              binIndex: binIndex2,
              binStart: edges[binIndex2],
              binEnd: edges[binIndex2 + 1]
            }
          }
        } : {}
      };
    });
    return { type: "rect", rects };
  });
  const rowsTotal = groups.reduce((sum, group) => sum + group.values.length, 0);
  const rows = [];
  for (const group of groups) {
    for (let index = 0; index < group.values.length; index++) {
      rows.push([
        group.id,
        group.connectionCount,
        group.observationCount,
        group.binnedObservationCount,
        group.underRangeCount,
        group.overRangeCount,
        group.missingMeasurementCount,
        group.missingObservationCount ?? null,
        edges[index],
        edges[index + 1],
        group.values[index]
      ]);
    }
  }
  return {
    ...frame2(context, skillId),
    panels: [{
      id: "main",
      ...box,
      axes: [transformedXAxis(xLabel, xScale), yAxis(yLabel, yScale)],
      marks
    }],
    table: {
      policy: "complete_returned",
      columns: [
        { key: "group", header: "Group" },
        { key: "connectionCount", header: "Connection rows" },
        { key: "observationCount", header: "Formed observations" },
        { key: "binnedObservationCount", header: "Binned observations" },
        { key: "underRangeCount", header: "Under range" },
        { key: "overRangeCount", header: "Over range" },
        { key: "missingMeasurementCount", header: "Missing measurements" },
        { key: "missingObservationCount", header: "Missing observations" },
        { key: "binStart", header: "Bin start" },
        { key: "binEnd", header: "Bin end" },
        { key: "value", header: yLabel }
      ],
      rows,
      rowsInline: rows.length,
      rowsTotal
    },
    legend: groups.map((group, index) => ({
      label: group.label,
      color: categoricalStyle(index).color,
      glyph: "series"
    })),
    accessibility
  };
}
function transformedXAxis(label, scale) {
  return {
    orientation: "bottom",
    label,
    transform: scale.transform,
    ticks: scale.ticks().map((tick) => ({ position: scale.map(tick.value), label: tick.label }))
  };
}
function barTable(edges, values, valueHeader = "Value", metadata = []) {
  const rows = Array.from(
    { length: values.length },
    (_value, i) => [
      edges[i],
      edges[i + 1],
      values[i],
      ...metadata.map((entry) => entry.value)
    ]
  );
  return {
    policy: "complete_returned",
    columns: [
      { key: "binStart", header: "Bin start" },
      { key: "binEnd", header: "Bin end" },
      { key: "value", header: valueHeader },
      ...metadata.map(({ key, header }) => ({ key, header }))
    ],
    rows,
    rowsInline: rows.length,
    rowsTotal: values.length
  };
}
function compileRasterFigure(context, events, rows, windowStart, windowStop, timeLabel, markStyle, skillId) {
  const box = panelBox2(context);
  if (rows.length === 0) {
    return { ...frame2(context, skillId), panels: [emptyPanel(box, "no declared event rows")], table: emptyTable(), legend: [] };
  }
  const rowIndex = /* @__PURE__ */ new Map();
  rows.forEach((row, i) => rowIndex.set(row.key, i));
  const xScale = linearScale(windowStart, windowStop, box.x, box.x + box.width);
  const rowHeight = box.height / rows.length;
  const tickHalf = Math.max(1, Math.min(rowHeight * 0.4, 4));
  const lines = [];
  const points = [];
  for (const event of events) {
    if (!event.inWindow) continue;
    const row = rowIndex.get(event.rowKey);
    if (row === void 0) continue;
    const x = xScale.map(event.time);
    const yCenter = box.y + (row + 0.5) * rowHeight;
    const authority = {
      tag: "data_carrier",
      classId: "events",
      provenance: {
        sourceOrdinal: event.sourceOrdinal,
        eventId: event.eventId,
        senderId: event.senderId,
        trialId: event.trialId,
        rowKey: event.rowKey
      }
    };
    if (markStyle === "point") points.push({ x, y: yCenter, authority });
    else lines.push({
      position: x,
      from: yCenter - tickHalf,
      to: yCenter + tickHalf,
      authority
    });
  }
  const marks = markStyle === "point" ? [{ type: "point", points, fill: accent(context.themeId), radius: 2, shape: "circle" }] : [{ type: "rule", orientation: "vertical", lines, stroke: accent(context.themeId), strokeWidth: 1 }];
  const axes = [
    xAxis(timeLabel, xScale),
    {
      orientation: "left",
      label: "sender / trial",
      transform: "band",
      ticks: yAxisRowTicks(rows.map((row) => row.label), box, rowHeight)
    }
  ];
  return {
    ...frame2(context, skillId),
    panels: [{ id: "main", ...box, axes, marks }],
    table: {
      policy: "complete_returned",
      columns: [
        { key: "sourceOrdinal", header: "Source ordinal" },
        { key: "eventId", header: "Event id" },
        { key: "time", header: "Event time" },
        { key: "timeUnit", header: "Time unit" },
        { key: "senderId", header: "Sender" },
        { key: "trialId", header: "Trial" },
        { key: "populationId", header: "Population" },
        { key: "rowKey", header: "Row key" },
        { key: "rowIndex", header: "Row index" },
        { key: "rowLabel", header: "Row label" },
        { key: "inWindow", header: "In window" }
      ],
      rows: events.map((event) => [
        event.sourceOrdinal,
        event.eventId,
        event.time,
        event.timeUnit,
        event.senderId,
        event.trialId,
        event.populationId,
        event.rowKey,
        event.rowIndex,
        event.rowLabel,
        event.inWindow ? "true" : "false"
      ]),
      rowsInline: events.length,
      rowsTotal: events.length
    },
    legend: []
  };
}
function yAxisRowTicks(order, box, rowHeight) {
  const step = Math.max(1, Math.ceil(order.length / 12));
  const ticks = [];
  for (let i = 0; i < order.length; i += step) {
    ticks.push({ position: box.y + (i + 0.5) * rowHeight, label: order[i] });
  }
  return ticks;
}
function compileMatrixFigure(context, spec, skillId) {
  const baseBox = panelBox2(context);
  if (spec.rowIds.length === 0 || spec.columnIds.length === 0) {
    return { ...frame2(context, skillId), panels: [emptyPanel(baseBox, "empty node universe")], table: emptyTable(), legend: [] };
  }
  const valued = spec.cells.filter(
    (cell) => cell.value !== null && (cell.state === "present" || cell.state === "valued")
  );
  const extent = finiteExtentBy(valued, (cell) => cell.value);
  const missingPartial = spec.cells.filter((cell) => cell.state === "present_with_missing_value");
  const missingAll = spec.cells.filter((cell) => cell.state === "present_without_value");
  const domainQualifier = spec.valueSemantics === "binary_presence" ? "painted presence values" : "complete painted values";
  const legend = [
    {
      label: `${spec.valueLabel}${extent ? `; ${domainQualifier} ${formatNumber(extent.min)} to ${formatNumber(extent.max)}` : "; no complete numeric cell value"}`,
      color: accent(context.themeId),
      outlineColor: uncertaintyStroke(context.themeId),
      glyph: "band"
    },
    ...spec.observedRows.some(Boolean) ? [{
      label: "Observed empty cell: measured absence inside a complete target row; it is not a measured numeric zero",
      color: gridColor(context.themeId),
      outlineColor: uncertaintyStroke(context.themeId),
      glyph: "band"
    }] : [],
    ...spec.observedRows.some((observed) => !observed) ? [{
      label: "not_observed: this scope cannot establish presence or absence for the target row",
      color: withOpacity(missingColor(context.themeId), 0.24),
      outlineColor: missingColor(context.themeId),
      glyph: "band"
    }] : [],
    ...missingPartial.length > 0 ? [{
      label: "Connection present, but at least one contributing weight is missing; no partial aggregate is painted",
      color: withOpacity(missingColor(context.themeId), 0.58),
      outlineColor: accent(context.themeId),
      glyph: "band"
    }] : [],
    ...missingAll.length > 0 ? [{
      label: "Connection present with no measured contributing weight; missing is not zero",
      color: missingColor(context.themeId),
      outlineColor: uncertaintyStroke(context.themeId),
      glyph: "band"
    }] : []
  ];
  const legendInset = legendPlotInset(context.width, legend.length, context.subtitle !== void 0);
  const box = {
    ...baseBox,
    y: baseBox.y + legendInset,
    height: baseBox.height - legendInset
  };
  const cellW = box.width / spec.columnIds.length;
  const cellH = box.height / spec.rowIds.length;
  const cellRect = (cell, fill) => ({
    x: box.x + cell.columnIndex * cellW,
    y: box.y + cell.rowIndex * cellH,
    width: Math.max(0, cellW - 0.5),
    height: Math.max(0, cellH - 0.5),
    fill,
    ...cell.authority === void 0 ? {} : { authority: cloneAuthorityRole(cell.authority) }
  });
  const colorFor = (value) => {
    if (spec.valueSemantics === "binary_presence") return accent(context.themeId);
    if (!extent || extent.min === extent.max) return sequentialColor(0.72);
    if (spec.colorScale?.class === "diverging") {
      return symmetricMatrixDivergingColor(
        value,
        extent.min,
        extent.max,
        spec.colorScale.center
      );
    }
    const t = spec.numericScale === "log" ? (Math.log(value) - Math.log(extent.min)) / (Math.log(extent.max) - Math.log(extent.min)) : (value - extent.min) / (extent.max - extent.min);
    return sequentialColor(t);
  };
  const backgrounds = [];
  if (spec.observedRows.some((observed) => !observed)) {
    backgrounds.push({
      x: box.x,
      y: box.y,
      width: box.width,
      height: box.height,
      fill: withOpacity(missingColor(context.themeId), 0.24),
      authority: distributionDecorativeRole()
    });
  }
  let runStart = null;
  for (let row = 0; row <= spec.observedRows.length; row++) {
    if (row < spec.observedRows.length && spec.observedRows[row]) {
      if (runStart === null) runStart = row;
      continue;
    }
    if (runStart !== null) {
      backgrounds.push({
        x: box.x,
        y: box.y + runStart * cellH,
        width: box.width,
        height: (row - runStart) * cellH,
        fill: gridColor(context.themeId),
        authority: distributionDecorativeRole()
      });
      runStart = null;
    }
  }
  const marks = [
    { type: "rect", rects: backgrounds, stroke: uncertaintyStroke(context.themeId) },
    {
      type: "rect",
      rects: valued.map((cell) => cellRect(cell, colorFor(cell.value))),
      // The outline makes an exact numeric zero visibly present even when its colour is
      // close to the observed-empty background or to a diverging neutral colour.
      stroke: uncertaintyStroke(context.themeId)
    },
    ...missingPartial.length > 0 ? [{
      type: "rect",
      rects: missingPartial.map((cell) => cellRect(cell, withOpacity(missingColor(context.themeId), 0.58))),
      stroke: accent(context.themeId)
    }] : [],
    ...missingAll.length > 0 ? [{
      type: "rect",
      rects: missingAll.map((cell) => cellRect(cell, missingColor(context.themeId))),
      stroke: uncertaintyStroke(context.themeId)
    }] : []
  ];
  const axes = [
    { orientation: "bottom", label: "source (column)", transform: "band", ticks: bandTicks(spec.columnIds, box.x, box.width) },
    { orientation: "left", label: "target (row)", transform: "band", ticks: bandTicks(spec.rowIds, box.y, box.height) }
  ];
  return {
    ...frame2(context, skillId),
    panels: [{ id: "main", ...box, axes, marks }],
    table: emptyTable(),
    legend,
    accessibility: {
      summary: spec.summary,
      panelSummaries: [spec.summary]
    }
  };
}
function bandTicks(ids, start, span) {
  const step = Math.max(1, Math.ceil(ids.length / 12));
  const band = span / ids.length;
  const ticks = [];
  for (let i = 0; i < ids.length; i += step) {
    ticks.push({ position: start + (i + 0.5) * band, label: ids[i] });
  }
  return ticks;
}
function interpolateChannel(start, stop, t) {
  return Math.round(start + (stop - start) * Math.max(0, Math.min(1, t)));
}
function rgbHex(red, green, blue) {
  return `#${[red, green, blue].map((channel) => Math.max(0, Math.min(255, channel)).toString(16).padStart(2, "0")).join("")}`;
}
function sequentialColor(t) {
  return rgbHex(
    interpolateChannel(230, 0, t),
    interpolateChannel(241, 114, t),
    interpolateChannel(248, 178, t)
  );
}
function symmetricMatrixDivergingColor(value, minimum, maximum, center) {
  if (![value, minimum, maximum, center].every(Number.isFinite)) {
    throw new Error("a diverging matrix colour requires finite value, extent, and center");
  }
  const valueUnits = finiteBinary64ToMinSubnormalUnits(value);
  const minimumUnits = finiteBinary64ToMinSubnormalUnits(minimum);
  const maximumUnits = finiteBinary64ToMinSubnormalUnits(maximum);
  const centerUnits = finiteBinary64ToMinSubnormalUnits(center);
  if (!(minimumUnits < centerUnits && centerUnits < maximumUnits)) {
    throw new Error(
      "a diverging matrix colour requires complete valued aggregates strictly below and above its center"
    );
  }
  const negativeRadius = centerUnits - minimumUnits;
  const positiveRadius = maximumUnits - centerUnits;
  const radius = negativeRadius > positiveRadius ? negativeRadius : positiveRadius;
  const signedDistance = valueUnits - centerUnits;
  const distance = signedDistance < 0n ? -signedDistance : signedDistance;
  const t = exactRationalToBinary64(distance, radius);
  if (signedDistance < 0n) {
    return rgbHex(
      interpolateChannel(247, 3, t),
      interpolateChannel(247, 115, t),
      interpolateChannel(247, 171, t)
    );
  }
  return rgbHex(
    interpolateChannel(247, 211, t),
    interpolateChannel(247, 99, t),
    interpolateChannel(247, 3, t)
  );
}
function divergingColor(value, minimum, maximum, center) {
  if (value < center) {
    const denominator2 = center - minimum;
    const t2 = denominator2 > 0 ? (center - value) / denominator2 : 1;
    return rgbHex(
      interpolateChannel(247, 0, t2),
      interpolateChannel(247, 114, t2),
      interpolateChannel(247, 178, t2)
    );
  }
  const denominator = maximum - center;
  const t = denominator > 0 ? (value - center) / denominator : 1;
  return rgbHex(
    interpolateChannel(247, 213, t),
    interpolateChannel(247, 94, t),
    interpolateChannel(247, 0, t)
  );
}
function equalScaleMapper(box, xs, ys, declaredDomain) {
  const xExtent = finiteExtent(xs);
  const yExtent = finiteExtent(ys);
  let xMin = declaredDomain?.xMin ?? xExtent.min;
  let xMax = declaredDomain?.xMax ?? xExtent.max;
  let yMin = declaredDomain?.yMin ?? yExtent.min;
  let yMax = declaredDomain?.yMax ?? yExtent.max;
  if (!declaredDomain) {
    const xSpan = xMax - xMin;
    const ySpan = yMax - yMin;
    const nonzero = Math.max(xSpan, ySpan);
    if (!(nonzero > 0)) throw new Error("a spatial map with no declared domain has zero extent on both axes");
    const xPadding = (xSpan > 0 ? xSpan : nonzero) * 0.02;
    const yPadding = (ySpan > 0 ? ySpan : nonzero) * 0.02;
    xMin -= xPadding;
    xMax += xPadding;
    yMin -= yPadding;
    yMax += yPadding;
  }
  const dataWidth = declaredDomain?.periodX ?? xMax - xMin;
  const dataHeight = declaredDomain?.periodY ?? yMax - yMin;
  const span = Math.max(dataWidth, dataHeight);
  if (!(span > 0)) throw new Error("spatial display domain has no positive extent");
  const scaleFactor = Math.min(box.width, box.height) / span;
  const dataCenterX = declaredDomain?.centerX ?? xMin / 2 + xMax / 2;
  const dataCenterY = declaredDomain?.centerY ?? yMin / 2 + yMax / 2;
  const pageCenterX = box.x + box.width / 2;
  const pageCenterY = box.y + box.height / 2;
  const map = (x, y) => ({
    x: pageCenterX + (x - dataCenterX) * scaleFactor,
    y: pageCenterY - (y - dataCenterY) * scaleFactor
  });
  const left = map(xMin, dataCenterY).x;
  const right = map(xMax, dataCenterY).x;
  const bottom = map(dataCenterX, yMin).y;
  const top = map(dataCenterX, yMax).y;
  return {
    map,
    xScale: linearScale(xMin, xMax, left, right),
    yScale: linearScale(yMin, yMax, bottom, top),
    bounds: { xMin, xMax, yMin, yMax }
  };
}
function insetArrow(from, target, radius) {
  const dx = target.x - from.x;
  const dy = target.y - from.y;
  const length = Math.hypot(dx, dy);
  if (!(length > 0)) return void 0;
  const ux = dx / length;
  const uy = dy / length;
  const to = { x: target.x - ux * radius, y: target.y - uy * radius };
  return { from: { x: to.x - ux * 10, y: to.y - uy * 10 }, to };
}
function compileSpatialMapFigure(context, spec, skillId) {
  const box = panelBox2(context);
  if (spec.nodes.length === 0) {
    return { ...frame2(context, skillId), panels: [emptyPanel(box, "no positioned nodes")], table: emptyTable(), legend: [] };
  }
  const mapper = equalScaleMapper(
    box,
    spec.nodes.map((node) => node.x),
    spec.nodes.map((node) => node.y),
    spec.domain
  );
  const pageById = new Map(spec.nodes.map((node) => [node.id, mapper.map(node.x, node.y)]));
  const scientificById = new Map(spec.nodes.map((node) => [node.id, { x: node.x, y: node.y }]));
  const nodeOrdinal = new Map(spec.nodes.map((node, index) => [node.id, index]));
  const canonicalPair = (source, target) => nodeOrdinal.get(source) <= nodeOrdinal.get(target) ? [source, target] : [target, source];
  const pairKey2 = (source, target) => {
    const [first, second] = canonicalPair(source, target);
    return `${first}\0${second}`;
  };
  const pairs = /* @__PURE__ */ new Map();
  for (const connection of spec.connections) {
    const key = pairKey2(connection.sourceId, connection.targetId);
    const entries = pairs.get(key);
    if (entries) entries.push(connection);
    else pairs.set(key, [connection]);
  }
  const pairValues = [...pairs.values()].map((entries) => aggregateTopologyScalar(
    entries.flatMap((entry) => typeof entry.value === "number" ? [entry.value] : []),
    entries.length === 1 ? "no_aggregation" : spec.multapseAggregation
  ));
  const finitePairValues = pairValues.filter((value) => value !== null);
  const pairExtent = finiteExtent(finitePairValues);
  const pairMagnitudeExtent = finiteExtent(finitePairValues.map((entry) => Math.abs(entry)));
  const nodeValues = spec.nodes.flatMap((node) => typeof node.value === "number" ? [node.value] : []);
  const nodeValueExtent = finiteExtent(nodeValues);
  const nodeTransform = (value) => {
    if (spec.nodeValueScale?.transform !== "symlog") return value;
    const threshold = spec.nodeValueScale.linearThreshold;
    const origin = spec.nodeValueScale.kind === "diverging" ? spec.nodeValueScale.center ?? 0 : 0;
    const delta = exactBinary64Sum([value, -origin]);
    const transformed = symlogTransform(delta, threshold);
    if (!Number.isFinite(transformed)) {
      throw new Error("spatial node symlog transform is unrepresentable in finite binary64");
    }
    return transformed;
  };
  const transformedNodeValues = nodeValues.map(nodeTransform);
  const nodeExtent = finiteExtent(transformedNodeValues);
  const marks = [];
  if (spec.domain) {
    const topLeft = mapper.map(spec.domain.xMin, spec.domain.yMax);
    const bottomRight = mapper.map(spec.domain.xMax, spec.domain.yMin);
    marks.push({
      type: "group",
      id: "declared-domain",
      marks: [{
        type: "rect",
        rects: [{
          x: topLeft.x,
          y: topLeft.y,
          width: bottomRight.x - topLeft.x,
          height: bottomRight.y - topLeft.y,
          fill: "#00000000",
          authority: { tag: "decorative_mark" }
        }],
        stroke: gridColor(context.themeId)
      }]
    });
  }
  let pairOrdinal = 0;
  for (const entries of pairs.values()) {
    const first = entries[0];
    const [physicalSourceId, physicalTargetId] = canonicalPair(first.sourceId, first.targetId);
    const sourceScientific = scientificById.get(physicalSourceId);
    const targetScientific = scientificById.get(physicalTargetId);
    const sourcePage = pageById.get(physicalSourceId);
    const targetPage = pageById.get(physicalTargetId);
    const value = pairValues[pairOrdinal++];
    const magnitudeT = value !== null && pairMagnitudeExtent && pairMagnitudeExtent.max !== pairMagnitudeExtent.min ? (Math.abs(value) - pairMagnitudeExtent.min) / (pairMagnitudeExtent.max - pairMagnitudeExtent.min) : 0.5;
    const colorT = value !== null && pairExtent && pairExtent.max !== pairExtent.min ? (value - pairExtent.min) / (pairExtent.max - pairExtent.min) : 0.5;
    const connectionUsesColor = spec.connectionEncoding?.channel === "color" || spec.connectionEncoding?.channel === "width_and_color";
    const color = spec.connectionEncoding && value === null ? missingColor(context.themeId) : connectionUsesColor && value !== null ? spec.connectionEncoding.colorKind === "diverging" ? divergingColor(value, pairExtent?.min ?? value, pairExtent?.max ?? value, spec.connectionEncoding.center ?? 0) : sequentialColor(colorT) : spec.connectionEncoding ? accent(context.themeId) : gridColor(context.themeId);
    const width = spec.connectionEncoding && value !== null && (spec.connectionEncoding.channel === "width" || spec.connectionEncoding.channel === "width_and_color") ? 1 + 4 * magnitudeT : 1.25;
    const pairMarks = [];
    if (physicalSourceId === physicalTargetId) {
      const radius = spec.markerRadius + 10;
      const loop = [
        sourcePage,
        { x: sourcePage.x + radius, y: sourcePage.y - radius },
        { x: sourcePage.x + 2 * radius, y: sourcePage.y },
        { x: sourcePage.x + radius, y: sourcePage.y + radius },
        sourcePage
      ];
      pairMarks.push({
        type: "line",
        subpaths: [loop.map((point) => ({ ...point, authority: { tag: "connector" } }))],
        stroke: color,
        strokeWidth: width
      });
      pairMarks.push({
        type: "arrow",
        arrows: [{
          from: loop[loop.length - 2],
          to: { x: sourcePage.x + 1, y: sourcePage.y + 1 },
          authority: {
            tag: "data_carrier",
            classId: "connections",
            provenance: {
              sourceId: first.sourceId,
              targetId: first.targetId,
              sourceOrdinals: entries.map((entry) => entry.sourceOrdinal),
              edgeIds: entries.map((entry) => entry.id)
            }
          }
        }],
        fill: color,
        size: 6
      });
    } else {
      const routed = routeSpatialChord(sourceScientific, targetScientific, spec.domain);
      pairMarks.push({
        type: "line",
        subpaths: routed.segments.map((segment) => segment.map((point) => ({
          ...mapper.map(point.x, point.y),
          authority: { tag: "connector" }
        }))),
        stroke: color,
        strokeWidth: width,
        ...value !== null && value < 0 ? { dash: "4 3" } : {}
      });
      const directions = /* @__PURE__ */ new Map();
      for (const entry of entries) {
        const key = `${entry.sourceId}\0${entry.targetId}`;
        const direction = directions.get(key);
        if (direction) direction.push(entry);
        else directions.set(key, [entry]);
      }
      const arrows = [...directions.values()].flatMap((direction) => {
        const entry = direction[0];
        const directionSegments = entry.sourceId === physicalSourceId ? routed.segments : reverseSpatialSegments(routed.segments);
        const final = directionSegments[directionSegments.length - 1];
        const from = mapper.map(final[0].x, final[0].y);
        const to = pageById.get(entry.targetId);
        const arrow = insetArrow(from, to, spec.markerRadius);
        return arrow ? [{
          ...arrow,
          authority: {
            tag: "data_carrier",
            classId: "connections",
            provenance: {
              sourceId: entry.sourceId,
              targetId: entry.targetId,
              sourceOrdinals: direction.map((candidate) => candidate.sourceOrdinal),
              edgeIds: direction.map((candidate) => candidate.id)
            }
          }
        }] : [];
      });
      if (arrows.length > 0) pairMarks.push({ type: "arrow", arrows, fill: color, size: 6 });
      const directionGroups = [...directions.values()];
      for (let directionIndex = 0; directionIndex < directionGroups.length; directionIndex++) {
        const direction = directionGroups[directionIndex];
        if (direction.length <= 1 && directionGroups.length <= 1) continue;
        const entry = direction[0];
        pairMarks.push({
          type: "text",
          x: (sourcePage.x + targetPage.x) / 2,
          y: (sourcePage.y + targetPage.y) / 2 - 7 + directionIndex * 14,
          text: `${entry.sourceId} -> ${entry.targetId}: x ${direction.length}`,
          anchor: "middle",
          fontSize: 9,
          fill: color,
          decorative: false
        });
      }
    }
    if (entries.length > 1 && physicalSourceId === physicalTargetId) {
      pairMarks.push({
        type: "text",
        x: (sourcePage.x + targetPage.x) / 2,
        y: (sourcePage.y + targetPage.y) / 2 - 5,
        text: `x ${entries.length}`,
        anchor: "middle",
        fontSize: 9,
        fill: color,
        decorative: true
      });
    }
    marks.push({ type: "group", id: `connection-pair-${first.id}`, marks: pairMarks });
  }
  for (let nodeIndex = 0; nodeIndex < spec.nodes.length; nodeIndex++) {
    const node = spec.nodes[nodeIndex];
    const point = pageById.get(node.id);
    const style = categoricalStyle(node.groupIndex ?? 0);
    const transformedNodeValue = typeof node.value === "number" ? nodeTransform(node.value) : null;
    const nodeT = transformedNodeValue !== null && nodeExtent && nodeExtent.max !== nodeExtent.min ? (transformedNodeValue - nodeExtent.min) / (nodeExtent.max - nodeExtent.min) : 0.5;
    const fill = spec.nodeEncoding === "group" ? style.color : spec.nodeEncoding === "value" && typeof node.value === "number" ? spec.nodeValueScale?.kind === "diverging" ? divergingColor(
      transformedNodeValue,
      nodeExtent?.min ?? transformedNodeValue,
      nodeExtent?.max ?? transformedNodeValue,
      spec.nodeValueScale.transform === "symlog" ? 0 : spec.nodeValueScale.center ?? 0
    ) : sequentialColor(nodeT) : spec.nodeEncoding === "value" ? missingColor(context.themeId) : accent(context.themeId);
    marks.push({
      type: "group",
      id: `node-${node.id}`,
      marks: [{
        type: "point",
        points: [{
          ...point,
          authority: {
            tag: "data_carrier",
            classId: "nodes",
            provenance: { nodeId: node.id, sourceOrdinal: nodeIndex }
          }
        }],
        fill,
        radius: spec.markerRadius,
        shape: spec.nodeEncoding === "group" ? style.marker : "circle"
      }]
    });
  }
  return {
    ...frame2(context, skillId),
    panels: [{
      id: "main",
      ...box,
      axes: [xAxis(spec.xLabel, mapper.xScale), yAxis(spec.yLabel, mapper.yScale)],
      marks
    }],
    table: emptyTable(),
    legend: [
      ...spec.nodeEncoding === "group" ? [...new Map(spec.nodes.map((node) => [node.groupIndex ?? -1, node])).values()].map((node) => {
        const style = categoricalStyle(node.groupIndex ?? 0);
        return {
          label: node.group ?? "ungrouped",
          color: style.color,
          glyph: "series",
          marker: style.marker
        };
      }) : [],
      ...spec.nodeEncoding === "value" ? [{
        label: `Node colour encodes the declared value over ${nodeValueExtent ? `${formatNumber(nodeValueExtent.min)} to ${formatNumber(nodeValueExtent.max)}` : "no finite range"}${spec.nodeValueScale?.transform === "symlog" ? ` using symlog with linear threshold ${formatNumber(spec.nodeValueScale.linearThreshold)}` : " linearly"}; missing values use the reserved colour`,
        color: accent(context.themeId),
        glyph: "series"
      }] : [],
      ...spec.connectionEncoding ? [{
        label: `Connection ${spec.connectionEncoding.channel} encodes the declared pair aggregate${spec.connectionEncoding.channel === "width" || spec.connectionEncoding.channel === "width_and_color" ? "; the observed |value| range maps to 1 to 5 px" : ""}${pairExtent ? ` (signed range ${formatNumber(pairExtent.min)} to ${formatNumber(pairExtent.max)})` : ""}${spec.multapseAggregation ? `; multapses use ${spec.multapseAggregation}` : ""}`,
        color: accent(context.themeId),
        glyph: "series"
      }] : []
    ]
  };
}
function compileCompartmentHeatmapFigure(context, rows, window, xLabel, colorLabel, colorSpec, skillId) {
  const box = panelBox2(context);
  const finite = rows.flatMap((row) => row.values.filter((value) => value !== null));
  const extent = finiteExtent(finite);
  const xScale = linearScale(window[0], window[1], box.x, box.x + box.width);
  const rowHeight = box.height / Math.max(1, rows.length);
  const marks = [];
  for (let rowIndex = 0; rowIndex < rows.length; rowIndex++) {
    const row = rows[rowIndex];
    const rects = row.times.map((time, index) => {
      const value = row.values[index];
      const t = value !== null && extent && extent.max !== extent.min ? (value - extent.min) / (extent.max - extent.min) : 0.5;
      const fill = value === null ? missingColor(context.themeId) : colorSpec.family === "diverging" ? divergingColor(value, extent?.min ?? value, extent?.max ?? value, colorSpec.center ?? 0) : sequentialColor(t);
      return {
        x: xScale.map(time) - 1.5,
        y: box.y + rowIndex * rowHeight + 1,
        width: 3,
        height: Math.max(1, rowHeight - 2),
        fill,
        ...row.outputAuthority?.provenance[index] !== void 0 ? {
          authority: {
            tag: "data_carrier",
            classId: row.outputAuthority.classId,
            provenance: row.outputAuthority.provenance[index]
          }
        } : {}
      };
    });
    marks.push({ type: "group", id: `compartment-${row.id}`, marks: [{ type: "rect", rects }] });
  }
  return {
    ...frame2(context, skillId),
    panels: [{
      id: "main",
      ...box,
      axes: [
        xAxis(xLabel, xScale),
        {
          orientation: "left",
          label: "compartment",
          transform: "band",
          ticks: rows.map((row, index) => ({
            position: box.y + (index + 0.5) * rowHeight,
            label: row.label
          }))
        }
      ],
      marks
    }],
    table: emptyTable(),
    legend: [{
      label: `${colorLabel}; global ${colorSpec.family} colour domain${extent ? ` ${formatNumber(extent.min)} to ${formatNumber(extent.max)}` : " unavailable"}`,
      color: accent(context.themeId),
      glyph: "series"
    }]
  };
}
function compileStemFigure(context, binCenters2, counts, xLabel, yLabel, skillId) {
  const box = panelBox2(context);
  if (counts.length === 0) {
    return { ...frame2(context, skillId), panels: [emptyPanel(box, "no pairs to plot")], table: emptyTable(), legend: [] };
  }
  const xMin = binCenters2[0];
  const xMax = binCenters2[binCenters2.length - 1];
  const yMax = Math.max(0, finiteExtent(counts)?.max ?? 0);
  const xScale = linearScale(xMin, xMax, box.x, box.x + box.width);
  const yScale = linearScale(0, yMax, box.y + box.height, box.y);
  const baseline = yScale.map(0);
  const stems = counts.map((c, i) => ({ position: xScale.map(binCenters2[i]), from: baseline, to: yScale.map(c) }));
  const dots = counts.map((c, i) => ({ x: xScale.map(binCenters2[i]), y: yScale.map(c) }));
  const marks = [
    { type: "rule", orientation: "vertical", lines: stems, stroke: accent(context.themeId), strokeWidth: 1.25 },
    { type: "point", points: dots, fill: accent(context.themeId), radius: 2, shape: "circle" }
  ];
  const axes = [xAxis(xLabel, xScale), yAxis(yLabel, yScale)];
  return {
    ...frame2(context, skillId),
    panels: [{ id: "main", ...box, axes, marks }],
    table: {
      policy: "complete_returned",
      columns: [{ key: "lag", header: xLabel }, { key: "count", header: yLabel }],
      rows: counts.map((c, i) => [binCenters2[i], c]),
      rowsInline: counts.length,
      rowsTotal: counts.length
    },
    legend: []
  };
}
function responseConditionTicks(curve, xValues, xScale) {
  const count = curve.conditions.length;
  if (count === 0) return [];
  const maximumLabels = 12;
  const step = count <= maximumLabels ? 1 : Math.ceil((count - 1) / (maximumLabels - 1));
  const ordinals = [];
  for (let index = 0; index < count; index += step) ordinals.push(index);
  if (ordinals[ordinals.length - 1] !== count - 1) ordinals.push(count - 1);
  const labelCounts = /* @__PURE__ */ new Map();
  for (const condition of curve.conditions) {
    labelCounts.set(
      condition.conditionLabel,
      (labelCounts.get(condition.conditionLabel) ?? 0) + 1
    );
  }
  const usedLabels = /* @__PURE__ */ new Set();
  const displayLabels = curve.conditions.map((condition, index) => {
    const base = condition.conditionLabel;
    let candidate = labelCounts.get(base) > 1 ? `${base} [${condition.conditionId}]` : base;
    let collision = 1;
    while (usedLabels.has(candidate)) {
      candidate = `${base} [condition ${condition.conditionId}; ordinal ${index + 1}; collision ${collision}]`;
      collision++;
    }
    usedLabels.add(candidate);
    return candidate;
  });
  return ordinals.map((index) => ({
    position: xScale.map(xValues[index]),
    label: displayLabels[index]
  }));
}
var RESPONSE_CURVE_TABLE_COLUMNS = [
  { key: "conditionId", header: "Condition" },
  { key: "conditionLabel", header: "Condition label" },
  { key: "input", header: "Input" },
  { key: "inputUnit", header: "Input unit" },
  { key: "repeatId", header: "Repeat" },
  { key: "response", header: "Response" },
  { key: "responseUnit", header: "Response unit" },
  { key: "responseMethod", header: "Method" },
  { key: "rateNormalization", header: "Rate normalization" },
  { key: "recordedSenderCount", header: "Recorded senders" },
  { key: "missing", header: "Undefined" },
  { key: "estimator", header: "Estimator" },
  { key: "sampleCount", header: "n retained" },
  { key: "excludedCount", header: "n excluded" },
  { key: "responseBasis", header: "Basis" },
  { key: "uncertaintyKind", header: "Uncertainty" },
  { key: "uncertaintyValue", header: "Uncertainty value" },
  { key: "uncertaintyLower", header: "Uncertainty lower" },
  { key: "uncertaintyUpper", header: "Uncertainty upper" },
  { key: "uncertaintyBasis", header: "Uncertainty basis" },
  { key: "estimatorRole", header: "Estimator role" },
  { key: "trimmedCount", header: "n trimmed" },
  { key: "peakBinCount", header: "Peak-bin count audit" },
  { key: "peakCountDerivationAlgorithm", header: "Peak-count derivation" },
  { key: "eventScopeKind", header: "Declared event scope" },
  { key: "eventSelectionId", header: "Declared event selection" },
  { key: "eventMembershipBinding", header: "Membership binding" },
  { key: "selectedEventTrainCount", header: "Selected event trains" }
];
function compileResponseCurveFigure(context, curve, options, skillId) {
  const box = panelBox2(context);
  const rowsTotal = options.tableRows.length;
  const table = {
    policy: "complete_returned",
    columns: RESPONSE_CURVE_TABLE_COLUMNS,
    rows: options.tableRows,
    rowsInline: rowsTotal,
    rowsTotal
  };
  const estimates = curve.conditions.map((condition) => condition.estimate).filter((value) => value !== null && Number.isFinite(value));
  const panelSummaries = [...options.summaryStatements ?? []];
  const xValues = curve.conditions.map(
    (condition) => curve.axis === "numeric" ? condition.input : condition.displayOrdinal
  );
  const xExtent = finiteExtent(xValues);
  const xScale = curve.axis === "numeric" && curve.inputScale === "log10" ? logNumericScale(xExtent.min, xExtent.max, box.x, box.x + box.width) : linearNumericScale(xExtent.min, xExtent.max, box.x, box.x + box.width);
  const xAxisModel = curve.axis === "numeric" ? transformedXAxis(options.xLabel, xScale) : {
    orientation: "bottom",
    label: options.xLabel,
    transform: "band",
    ticks: responseConditionTicks(curve, xValues, xScale)
  };
  const missingConditionLines = curve.conditions.map((condition, index) => condition.estimate === null ? {
    position: xScale.map(xValues[index]),
    from: box.y,
    to: box.y + box.height,
    authority: {
      tag: "data_carrier",
      classId: "conditions",
      provenance: {
        conditionId: condition.conditionId,
        displayOrdinal: condition.displayOrdinal,
        role: "undefined_condition"
      }
    }
  } : null).filter((line) => line !== null);
  const missingConditionMark = missingConditionLines.length > 0 ? {
    type: "rule",
    orientation: "vertical",
    lines: missingConditionLines,
    stroke: missingColor(context.themeId),
    strokeWidth: 1,
    dash: "2 3"
  } : void 0;
  const missingLegend = missingConditionLines.length > 0 ? [{
    label: "Declared condition with undefined response (x position only)",
    color: missingColor(context.themeId),
    glyph: "series",
    dash: "2 3"
  }] : [];
  if (estimates.length === 0) {
    return {
      ...frame2(context, skillId),
      panels: [{
        id: "main",
        ...box,
        axes: [xAxisModel],
        marks: missingConditionMark ? [missingConditionMark] : [],
        noData: { reason: "no declared condition has a usable response estimate" }
      }],
      table,
      legend: missingLegend,
      accessibility: {
        summary: context.summary,
        panelSummaries,
        suppressGlobalValueRange: true
      }
    };
  }
  const yExtent = finiteExtent(estimates);
  const yMinimum = Math.min(0, yExtent.min);
  const yScale = yExtent.max === 0 && yMinimum === 0 ? {
    domainMin: 0,
    domainMax: 0,
    rangeMin: box.y + box.height,
    rangeMax: box.y,
    transform: "linear",
    map(value) {
      if (!Number.isFinite(value)) return Number.NaN;
      if (value === 0) return box.y + box.height;
      return value < 0 ? box.y + box.height : box.y;
    },
    ticks() {
      return [{ value: 0, label: "0" }];
    }
  } : linearNumericScale(yMinimum, yExtent.max, box.y + box.height, box.y);
  const points = [];
  const guideSubpaths = [];
  let currentGuide = [];
  for (let index = 0; index < curve.conditions.length; index++) {
    const condition = curve.conditions[index];
    if (condition.estimate === null) {
      if (currentGuide.length >= 2) guideSubpaths.push(currentGuide);
      currentGuide = [];
      continue;
    }
    const point = {
      x: xScale.map(xValues[index]),
      y: yScale.map(condition.estimate),
      authority: {
        tag: "data_carrier",
        classId: "conditions",
        provenance: {
          conditionId: condition.conditionId,
          displayOrdinal: condition.displayOrdinal,
          role: "condition_estimate"
        }
      }
    };
    points.push(point);
    currentGuide.push({
      x: point.x,
      y: point.y,
      authority: {
        tag: "data_carrier",
        classId: "series_paths",
        provenance: {
          conditionId: condition.conditionId,
          displayOrdinal: condition.displayOrdinal,
          role: "ordered_guide_vertex"
        }
      }
    });
  }
  if (currentGuide.length >= 2) guideSubpaths.push(currentGuide);
  const marks = missingConditionMark ? [missingConditionMark] : [];
  if (curve.axis !== "nominal" && guideSubpaths.length > 0) {
    marks.push({
      type: "line",
      subpaths: guideSubpaths,
      stroke: missingColor(context.themeId),
      strokeWidth: 1,
      dash: "4 3"
    });
  }
  marks.push({ type: "point", points, fill: accent(context.themeId), radius: 3.5, shape: "circle" });
  return {
    ...frame2(context, skillId),
    panels: [{
      id: "main",
      ...box,
      axes: [xAxisModel, transformedYAxis(options.yLabel, yScale)],
      marks
    }],
    table,
    legend: [
      {
        label: options.curveLabel,
        color: accent(context.themeId),
        glyph: "series",
        marker: "circle"
      },
      ...curve.axis !== "nominal" && guideSubpaths.length > 0 ? [{
        label: "Ordered-condition guide (not a fit or interpolation)",
        color: missingColor(context.themeId),
        glyph: "series",
        dash: "4 3"
      }] : [],
      ...missingLegend
    ],
    accessibility: {
      summary: context.summary,
      panelSummaries,
      suppressGlobalValueRange: true
    }
  };
}
function splitStatePath(xs, ys) {
  const runs = [];
  const sourceRuns = [];
  let current = [];
  let currentOrdinals = [];
  for (let index = 0; index < Math.min(xs.length, ys.length); index++) {
    const x = xs[index];
    const y = ys[index];
    if (x === null || y === null || !Number.isFinite(x) || !Number.isFinite(y)) {
      if (current.length > 0) {
        runs.push(current);
        sourceRuns.push(currentOrdinals);
      }
      current = [];
      currentOrdinals = [];
      continue;
    }
    current.push({ x, y });
    currentOrdinals.push(index);
  }
  if (current.length > 0) {
    runs.push(current);
    sourceRuns.push(currentOrdinals);
  }
  return { runs, sourceRuns };
}
function compilePhasePlaneFigure(context, spec, skillId) {
  const box = panelBox2(context);
  const xs = [
    ...spec.trajectories?.xs ?? [],
    ...spec.vectorField?.xs ?? [],
    ...spec.nullclines?.xs ?? [],
    ...spec.fixedPoints?.xs ?? [],
    ...spec.vectorField?.domain ? [spec.vectorField.domain.xMin, spec.vectorField.domain.xMax] : []
  ].filter((value) => typeof value === "number" && Number.isFinite(value));
  const ys = [
    ...spec.trajectories?.ys ?? [],
    ...spec.vectorField?.ys ?? [],
    ...spec.nullclines?.ys ?? [],
    ...spec.fixedPoints?.ys ?? [],
    ...spec.vectorField?.domain ? [spec.vectorField.domain.yMin, spec.vectorField.domain.yMax] : []
  ].filter((value) => typeof value === "number" && Number.isFinite(value));
  if (xs.length === 0 || ys.length === 0) {
    return { ...frame2(context, skillId), panels: [emptyPanel(box, "no finite phase-plane carrier")], table: emptyTable(), legend: [] };
  }
  const xExtent = finiteExtent(xs);
  const yExtent = finiteExtent(ys);
  const xScale = linearScale(xExtent.min, xExtent.max, box.x, box.x + box.width);
  const yScale = linearScale(yExtent.min, yExtent.max, box.y + box.height, box.y);
  const marks = [];
  const legend = [];
  const field = spec.vectorField;
  if (field) {
    const normalized = field.xs.map((_x, index) => {
      const ux = field.normalizedDxs[index];
      const uy = field.normalizedDys[index];
      return { ux, uy, magnitude: Math.hypot(ux, uy) };
    });
    let transformedMaximum = Number.MIN_VALUE;
    for (const magnitude of field.magnitudes) {
      const transformed = field.scaling === "sqrt_magnitude" ? Math.sqrt(magnitude) : magnitude;
      if (transformed > transformedMaximum) transformedMaximum = transformed;
    }
    const maximumLength = Math.min(box.width, box.height) * field.maxArrowLengthFraction;
    for (let index = 0; index < field.xs.length; index++) {
      const origin = { x: xScale.map(field.xs[index]), y: yScale.map(field.ys[index]) };
      const direction = normalized[index];
      if (!(direction.magnitude > 0)) {
        marks.push({
          type: "group",
          id: `field-sample-${index}`,
          marks: [{
            type: "point",
            points: [{
              ...origin,
              authority: {
                tag: "data_carrier",
                classId: "field_vectors",
                provenance: { sourceOrdinal: index, role: "field_sample" }
              }
            }],
            fill: missingColor(context.themeId),
            radius: 2.8,
            shape: "cross"
          }]
        });
        continue;
      }
      const basisMagnitude = field.magnitudes[index];
      const transformed = field.scaling === "sqrt_magnitude" ? Math.sqrt(basisMagnitude) : basisMagnitude;
      const length = field.scaling === "unit_length" ? maximumLength : maximumLength * transformed / transformedMaximum;
      const endpoint = {
        x: origin.x + length * direction.ux / direction.magnitude,
        y: origin.y - length * direction.uy / direction.magnitude
      };
      const color = gridColor(context.themeId);
      marks.push({
        type: "group",
        id: `field-sample-${index}`,
        marks: [
          {
            type: "line",
            subpaths: [[
              { ...origin, authority: { tag: "connector" } },
              { ...endpoint, authority: { tag: "connector" } }
            ]],
            stroke: color,
            strokeWidth: 1
          },
          {
            type: "arrow",
            arrows: [{
              from: origin,
              to: endpoint,
              authority: {
                tag: "data_carrier",
                classId: "field_vectors",
                provenance: { sourceOrdinal: index, role: "field_sample" }
              }
            }],
            fill: color,
            size: 5
          }
        ]
      });
    }
    legend.push({
      label: `Caller-supplied vector field; ${field.scaling}${field.scaling === "sqrt_magnitude" ? " (compressed magnitude)" : ""}; ${field.magnitudeBasis} magnitude reference ${formatNumber(field.magnitudes.reduce((maximum, magnitude) => magnitude > maximum ? magnitude : maximum, 0))} ${field.magnitudeUnit} maps to at most ${formatNumber(field.maxArrowLengthFraction * 100)}% of the shorter plot axis`,
      color: gridColor(context.themeId),
      glyph: "series"
    });
  }
  const nullclines = spec.nullclines;
  if (nullclines) {
    for (let curveIndex = 0; curveIndex < nullclines.ids.length; curveIndex++) {
      const id = nullclines.ids[curveIndex];
      const selectedXs = [];
      const selectedYs = [];
      const selectedOrdinals = [];
      for (let index = 0; index < nullclines.pointIds.length; index++) {
        if (nullclines.pointIds[index] !== id) continue;
        selectedXs.push(nullclines.xs[index]);
        selectedYs.push(nullclines.ys[index]);
        selectedOrdinals.push(index);
      }
      const split = splitStatePath(selectedXs, selectedYs);
      const style = categoricalStyle(curveIndex + 3);
      const curveMarks = [];
      const drawable = split.runs.map((run, runIndex) => run.map((point, pointIndex) => ({
        x: xScale.map(point.x),
        y: yScale.map(point.y),
        authority: {
          tag: "data_carrier",
          classId: "nullclines",
          provenance: {
            curveId: id,
            sourceOrdinal: selectedOrdinals[split.sourceRuns[runIndex][pointIndex]]
          }
        }
      }))).filter((run) => run.length >= 2);
      if (drawable.length > 0) {
        curveMarks.push({ type: "line", subpaths: drawable, stroke: style.color, strokeWidth: 1.5, dash: "5 3" });
      }
      const isolated = split.runs.map((run, runIndex) => ({ run, runIndex })).filter(({ run }) => run.length === 1).map(({ run, runIndex }) => ({
        x: xScale.map(run[0].x),
        y: yScale.map(run[0].y),
        authority: {
          tag: "data_carrier",
          classId: "nullclines",
          provenance: {
            curveId: id,
            sourceOrdinal: selectedOrdinals[split.sourceRuns[runIndex][0]]
          }
        }
      }));
      if (isolated.length > 0) {
        curveMarks.push({ type: "point", points: isolated, fill: style.color, radius: 2.8, shape: style.marker });
      }
      if (curveMarks.length > 0) marks.push({ type: "group", id: `nullcline-${id}`, marks: curveMarks });
      legend.push({
        label: nullclines.labels[curveIndex] ?? id,
        color: style.color,
        glyph: "series",
        dash: "5 3"
      });
    }
  }
  const trajectories = spec.trajectories;
  if (trajectories) {
    for (let trajectoryIndex = 0; trajectoryIndex < trajectories.ids.length; trajectoryIndex++) {
      const id = trajectories.ids[trajectoryIndex];
      const selectedXs = [];
      const selectedYs = [];
      const selectedOrdinals = [];
      for (let index = 0; index < trajectories.pointIds.length; index++) {
        if (trajectories.pointIds[index] !== id) continue;
        selectedXs.push(trajectories.xs[index]);
        selectedYs.push(trajectories.ys[index]);
        selectedOrdinals.push(index);
      }
      const split = splitStatePath(selectedXs, selectedYs);
      const style = categoricalStyle(trajectoryIndex);
      const trajectoryMarks = [];
      const pageRuns = split.runs.map((run, runIndex) => run.map((point, pointIndex) => ({
        x: xScale.map(point.x),
        y: yScale.map(point.y),
        authority: {
          tag: "data_carrier",
          classId: "trajectories",
          provenance: {
            trajectoryId: id,
            sourceOrdinal: selectedOrdinals[split.sourceRuns[runIndex][pointIndex]]
          }
        }
      })));
      const drawable = pageRuns.filter((run) => run.length >= 2);
      if (drawable.length > 0) {
        trajectoryMarks.push({ type: "line", subpaths: drawable, stroke: style.color, strokeWidth: 1.75, dash: style.dash });
      }
      const isolated = pageRuns.filter((run) => run.length === 1).map((run) => run[0]);
      if (isolated.length > 0) {
        trajectoryMarks.push({ type: "point", points: isolated, fill: style.color, radius: 3, shape: style.marker });
      }
      const arrows = [];
      for (const run of pageRuns) {
        if (run.length < 2 || trajectories.directionMode === "none") continue;
        const every = trajectories.everyNPoints ?? 2;
        const indexes = trajectories.directionMode === "arrowhead_at_end" ? [run.length - 1] : Array.from(
          // "Every N points" means the Nth, 2Nth, ... point in each
          // continuous run (zero-based targets N-1, 2N-1, ...). Using N as
          // the first target would silently skip every two-point run.
          { length: Math.floor(run.length / every) },
          (_unused, index) => (index + 1) * every - 1
        );
        for (const index of indexes) {
          const earlier = run[index - 1];
          const later = run[index];
          const arrow = trajectories.timeDirection === "forward" ? { from: earlier, to: later } : { from: later, to: earlier };
          if (arrow.from.x !== arrow.to.x || arrow.from.y !== arrow.to.y) {
            arrows.push({
              from: { x: arrow.from.x, y: arrow.from.y },
              to: { x: arrow.to.x, y: arrow.to.y },
              authority: { tag: "connector" }
            });
          }
        }
      }
      if (arrows.length > 0) trajectoryMarks.push({ type: "arrow", arrows, fill: style.color, size: 6 });
      if (trajectoryMarks.length > 0) marks.push({ type: "group", id: `trajectory-${id}`, marks: trajectoryMarks });
      legend.push({
        label: `${trajectories.labels[trajectoryIndex] ?? id}${trajectoryMarks.length === 0 ? " (declared; no recorded points)" : ""}`,
        color: style.color,
        glyph: "series",
        dash: style.dash,
        marker: style.marker
      });
    }
  }
  const fixed = spec.fixedPoints;
  if (fixed) {
    for (let index = 0; index < fixed.ids.length; index++) {
      const style = fixed.converged[index] ? { color: uncertaintyStroke(context.themeId), marker: "diamond" } : { color: missingColor(context.themeId), marker: "cross" };
      marks.push({
        type: "group",
        id: `fixed-point-${fixed.ids[index]}`,
        marks: [{
          type: "point",
          points: [{
            x: xScale.map(fixed.xs[index]),
            y: yScale.map(fixed.ys[index]),
            authority: {
              tag: "data_carrier",
              classId: "fixed_points",
              provenance: { fixedPointId: fixed.ids[index], sourceOrdinal: index }
            }
          }],
          fill: style.color,
          radius: 4,
          shape: style.marker
        }]
      });
      legend.push({
        label: `${fixed.labels[index] ?? fixed.ids[index]} (${fixed.converged[index] ? "converged" : "unconverged candidate"})`,
        color: style.color,
        glyph: "series",
        marker: style.marker
      });
    }
  }
  return {
    ...frame2(context, skillId),
    panels: [{ id: "main", ...box, axes: [xAxis(spec.xLabel, xScale), yAxis(spec.yLabel, yScale)], marks }],
    table: emptyTable(),
    legend
  };
}
function compileGraphFigure(context, spec, skillId) {
  const box = panelBox2(context);
  if (spec.nodes.length === 0) {
    return { ...frame2(context, skillId), panels: [emptyPanel(box, "empty node universe")], table: emptyTable(), legend: [] };
  }
  const position = /* @__PURE__ */ new Map();
  let measuredMapper;
  if (spec.layout === "measured_positions") {
    measuredMapper = equalScaleMapper(
      box,
      spec.nodes.map((node) => node.x),
      spec.nodes.map((node) => node.y)
    );
    for (const node of spec.nodes) position.set(node.id, measuredMapper.map(node.x, node.y));
  } else if (spec.layout === "schematic_layered") {
    const grouped = /* @__PURE__ */ new Map();
    for (const node of spec.nodes) {
      const index = node.groupIndex ?? Number.MAX_SAFE_INTEGER;
      const entries = grouped.get(index);
      if (entries) entries.push(node);
      else grouped.set(index, [node]);
    }
    const columns = [...grouped.entries()].sort((left, right) => left[0] - right[0]);
    for (let column = 0; column < columns.length; column++) {
      const members = columns[column][1];
      for (let row = 0; row < members.length; row++) {
        position.set(members[row].id, {
          x: box.x + (column + 0.5) * box.width / columns.length,
          y: box.y + (row + 0.5) * box.height / members.length
        });
      }
    }
  } else if (spec.layout === "schematic_grouped_circular") {
    const grouped = /* @__PURE__ */ new Map();
    for (const node of spec.nodes) {
      const index = node.groupIndex ?? Number.MAX_SAFE_INTEGER;
      const entries = grouped.get(index);
      if (entries) entries.push(node);
      else grouped.set(index, [node]);
    }
    const sectors = [...grouped.entries()].sort((left, right) => left[0] - right[0]);
    const gap = 4 * Math.PI / 180;
    const available = Math.max(0, 2 * Math.PI - gap * sectors.length);
    let cursor = -Math.PI / 2;
    const radius = Math.max(12, Math.min(box.width, box.height) / 2 - 24);
    const cx = box.x + box.width / 2;
    const cy = box.y + box.height / 2;
    for (const [, members] of sectors) {
      const sector = available * members.length / spec.nodes.length;
      for (let index = 0; index < members.length; index++) {
        const angle = cursor + sector * (index + 0.5) / members.length;
        position.set(members[index].id, {
          x: cx + radius * Math.cos(angle),
          y: cy + radius * Math.sin(angle)
        });
      }
      cursor += sector + gap;
    }
  } else {
    const radius = Math.max(12, Math.min(box.width, box.height) / 2 - 24);
    const cx = box.x + box.width / 2;
    const cy = box.y + box.height / 2;
    spec.nodes.forEach((node, index) => {
      const angle = 2 * Math.PI * index / spec.nodes.length - Math.PI / 2;
      position.set(node.id, { x: cx + radius * Math.cos(angle), y: cy + radius * Math.sin(angle) });
    });
  }
  let maximumDegree = 1;
  for (const node of spec.nodes) {
    const degree = node.degree ?? 0;
    if (degree > maximumDegree) maximumDegree = degree;
  }
  const radiusById = new Map(spec.nodes.map((node) => [
    node.id,
    spec.encodeDegreeAsArea ? Math.sqrt(3 ** 2 + (8 ** 2 - 3 ** 2) * Math.max(0, node.degree ?? 0) / maximumDegree) : 5
  ]));
  const nodeOrdinal = new Map(spec.nodes.map((node, index) => [node.id, index]));
  const unorderedKey = (source, target) => {
    const sourceIndex = nodeOrdinal.get(source);
    const targetIndex = nodeOrdinal.get(target);
    return sourceIndex <= targetIndex ? `${source}\0${target}` : `${target}\0${source}`;
  };
  const unordered = /* @__PURE__ */ new Map();
  for (const edge of spec.edges) {
    const key = unorderedKey(edge.sourceId, edge.targetId);
    const entries = unordered.get(key);
    if (entries) entries.push(edge);
    else unordered.set(key, [edge]);
  }
  const paintGroups = [];
  if (spec.parallelDisplay === "separate_lanes") {
    for (const entries of unordered.values()) {
      for (let index = 0; index < entries.length; index++) {
        paintGroups.push({ id: entries[index].id, entries: [entries[index]], lane: index });
      }
    }
  } else {
    const directed = /* @__PURE__ */ new Map();
    for (const edge of spec.edges) {
      const key = `${edge.sourceId}\0${edge.targetId}`;
      const entries = directed.get(key);
      if (entries) entries.push(edge);
      else directed.set(key, [edge]);
    }
    const directedSeenByPair = /* @__PURE__ */ new Map();
    for (const entries of directed.values()) {
      const key = unorderedKey(entries[0].sourceId, entries[0].targetId);
      const lane = directedSeenByPair.get(key) ?? 0;
      directedSeenByPair.set(key, lane + 1);
      paintGroups.push({ id: `bundle-${entries[0].id}`, entries, lane });
    }
  }
  const paintValues = paintGroups.map((group) => aggregateTopologyScalar(
    group.entries.flatMap((entry) => typeof entry.value === "number" ? [entry.value] : []),
    group.entries.length > 1 ? spec.multapseAggregation : "no_aggregation"
  ));
  const centeredValue = (value, center) => {
    const centered = exactBinary64Sum([value, -center]);
    if (!Number.isFinite(centered)) {
      throw new Error("graph edge value minus its declared center is outside finite binary64");
    }
    return centered;
  };
  const transformValue = (value) => {
    if (spec.edgeEncoding?.scale !== "symlog") return value;
    const center = spec.edgeEncoding.colorKind === "diverging" ? spec.edgeEncoding.center ?? 0 : 0;
    const transformed = symlogTransform(centeredValue(value, center), 1);
    if (!Number.isFinite(transformed)) {
      throw new Error("graph edge symlog transform is outside finite binary64");
    }
    return transformed;
  };
  const transformedPaintValues = paintValues.map((value) => value === null ? null : transformValue(value));
  const valueExtent = finiteExtent(transformedPaintValues.filter((value) => value !== null));
  const magnitudeValues = paintValues.flatMap((value) => {
    if (value === null) return [];
    const center = spec.edgeEncoding?.colorKind === "diverging" ? spec.edgeEncoding.center ?? 0 : 0;
    const centered = centeredValue(value, center);
    const transformed = spec.edgeEncoding?.scale === "symlog" ? symlogTransform(centered, 1) : centered;
    if (!Number.isFinite(transformed)) {
      throw new Error("graph edge magnitude transform is outside finite binary64");
    }
    return [Math.abs(transformed)];
  });
  const magnitudeExtent = finiteExtent(magnitudeValues);
  const marks = [];
  for (let groupIndex = 0; groupIndex < paintGroups.length; groupIndex++) {
    const group = paintGroups[groupIndex];
    const first = group.entries[0];
    const source = position.get(first.sourceId);
    const target = position.get(first.targetId);
    const value = paintValues[groupIndex];
    const transformedValue = transformedPaintValues[groupIndex];
    const colorT = transformedValue !== null && valueExtent && valueExtent.max !== valueExtent.min ? (transformedValue - valueExtent.min) / (valueExtent.max - valueExtent.min) : 0.5;
    const center = spec.edgeEncoding?.colorKind === "diverging" ? spec.edgeEncoding.center ?? 0 : 0;
    const centered = value === null ? null : centeredValue(value, center);
    const magnitude = centered === null ? null : spec.edgeEncoding?.scale === "symlog" ? Math.abs(symlogTransform(centered, 1)) : Math.abs(centered);
    const magnitudeT = magnitude !== null && magnitudeExtent && magnitudeExtent.max !== magnitudeExtent.min ? (magnitude - magnitudeExtent.min) / (magnitudeExtent.max - magnitudeExtent.min) : 0.5;
    const edgeUsesColor = spec.edgeEncoding?.channel === "color" || spec.edgeEncoding?.channel === "width_and_color";
    const color = value === null && spec.edgeEncoding ? missingColor(context.themeId) : edgeUsesColor && value !== null ? spec.edgeEncoding.colorKind === "diverging" ? divergingColor(
      transformedValue,
      valueExtent?.min ?? transformedValue,
      valueExtent?.max ?? transformedValue,
      spec.edgeEncoding.scale === "symlog" ? 0 : spec.edgeEncoding.center ?? 0
    ) : sequentialColor(colorT) : spec.edgeEncoding ? accent(context.themeId) : gridColor(context.themeId);
    const width = spec.edgeEncoding && value !== null && (spec.edgeEncoding.channel === "width" || spec.edgeEncoding.channel === "width_and_color") ? 1 + 4 * magnitudeT : 1.25;
    const edgeMarks = [];
    if (first.sourceId === first.targetId) {
      const loopRadius = radiusById.get(first.sourceId) + 10 + group.lane * 5;
      const loop = [
        source,
        { x: source.x + loopRadius, y: source.y - loopRadius },
        { x: source.x + 2 * loopRadius, y: source.y },
        { x: source.x + loopRadius, y: source.y + loopRadius },
        source
      ];
      edgeMarks.push({
        type: "line",
        subpaths: [loop.map((point) => ({ ...point, authority: { tag: "connector" } }))],
        stroke: color,
        strokeWidth: width,
        ...value !== null && value < center ? { dash: "4 3" } : {}
      });
      edgeMarks.push({
        type: "arrow",
        arrows: [{
          from: loop[3],
          to: { x: source.x + 1, y: source.y + 1 },
          authority: {
            tag: "data_carrier",
            classId: "edges",
            provenance: {
              display: spec.parallelDisplay === "bundled" ? "bundled" : "separate_lane",
              sourceId: first.sourceId,
              targetId: first.targetId,
              sourceOrdinals: group.entries.map((entry) => entry.sourceOrdinal),
              edgeIds: group.entries.map((entry) => entry.id)
            }
          }
        }],
        fill: color,
        size: 6
      });
    } else {
      const dx = target.x - source.x;
      const dy = target.y - source.y;
      const length = Math.hypot(dx, dy);
      const laneMagnitude = Math.ceil(group.lane / 2) * 7;
      const laneSign = group.lane % 2 === 1 ? -1 : 1;
      const offsetX = length > 0 ? -dy / length * laneMagnitude * laneSign : 0;
      const offsetY = length > 0 ? dx / length * laneMagnitude * laneSign : 0;
      const start = { x: source.x + offsetX, y: source.y + offsetY };
      const end = { x: target.x + offsetX, y: target.y + offsetY };
      const arrow = insetArrow(start, end, radiusById.get(first.targetId));
      edgeMarks.push({
        type: "line",
        subpaths: [[
          { ...start, authority: { tag: "connector" } },
          { ...arrow?.to ?? end, authority: { tag: "connector" } }
        ]],
        stroke: color,
        strokeWidth: width,
        ...value !== null && value < center ? { dash: "4 3" } : {}
      });
      if (arrow) {
        edgeMarks.push({
          type: "arrow",
          arrows: [{
            ...arrow,
            authority: {
              tag: "data_carrier",
              classId: "edges",
              provenance: {
                display: spec.parallelDisplay === "bundled" ? "bundled" : "separate_lane",
                sourceId: first.sourceId,
                targetId: first.targetId,
                sourceOrdinals: group.entries.map((entry) => entry.sourceOrdinal),
                edgeIds: group.entries.map((entry) => entry.id)
              }
            }
          }],
          fill: color,
          size: 6
        });
      }
    }
    if (group.entries.length > 1) {
      edgeMarks.push({
        type: "text",
        x: (source.x + target.x) / 2,
        y: (source.y + target.y) / 2 - 5 - group.lane * 3,
        text: `x ${group.entries.length}`,
        anchor: "middle",
        fontSize: 9,
        fill: color,
        decorative: true
      });
    }
    marks.push({ type: "group", id: `edge-${group.id}`, marks: edgeMarks });
  }
  for (let nodeIndex = 0; nodeIndex < spec.nodes.length; nodeIndex++) {
    const node = spec.nodes[nodeIndex];
    const style = categoricalStyle(node.groupIndex ?? 0);
    const color = spec.nodeColorByGroup ? style.color : accent(context.themeId);
    const nodeMarks = [{
      type: "point",
      points: [{
        ...position.get(node.id),
        authority: {
          tag: "data_carrier",
          classId: "nodes",
          provenance: { nodeId: node.id, sourceOrdinal: nodeIndex }
        }
      }],
      fill: color,
      radius: radiusById.get(node.id),
      shape: spec.nodeColorByGroup ? style.marker : "circle"
    }];
    if (spec.degreeLabel && node.degree !== void 0) {
      const point = position.get(node.id);
      nodeMarks.push({
        type: "text",
        x: point.x,
        y: point.y - radiusById.get(node.id) - 4,
        text: `${spec.degreeLabel} ${node.degree}`,
        anchor: "middle",
        fontSize: 9,
        fill: color,
        decorative: true
      });
    }
    marks.push({ type: "group", id: `node-${node.id}`, marks: nodeMarks });
  }
  const axes = measuredMapper ? [
    xAxis(spec.xLabel ?? `x (${spec.positionUnit ?? ""})`, measuredMapper.xScale),
    yAxis(spec.yLabel ?? `y (${spec.positionUnit ?? ""})`, measuredMapper.yScale)
  ] : [];
  const groupLegend = spec.nodeColorByGroup ? [...new Map(spec.nodes.map((node) => [node.groupIndex ?? -1, node])).values()].map((node) => {
    const style = categoricalStyle(node.groupIndex ?? 0);
    return {
      label: node.group ?? "ungrouped",
      color: style.color,
      glyph: "series",
      marker: style.marker
    };
  }) : [];
  return {
    ...frame2(context, skillId),
    panels: [{ id: "main", ...box, axes, marks }],
    table: emptyTable(),
    legend: [
      ...groupLegend,
      ...spec.edgeEncoding ? [{
        label: `Edge ${spec.edgeEncoding.channel} encodes the declared value${spec.edgeEncoding.channel === "width" || spec.edgeEncoding.channel === "width_and_color" ? `; the observed magnitude from ${spec.edgeEncoding.colorKind === "diverging" ? `center ${formatNumber(spec.edgeEncoding.center ?? 0)}` : "zero"} maps to 1 to 5 px` : ""}${spec.edgeEncoding.scale === "symlog" ? ` after the contract-owned sign(value - reference) log1p(|value - reference| / 1 declared unit) transform` : ""}${paintValues.some((value) => value !== null && value < (spec.edgeEncoding.colorKind === "diverging" ? spec.edgeEncoding.center ?? 0 : 0)) ? "; values below the reference are dashed" : ""}`,
        color: accent(context.themeId),
        glyph: "series"
      }] : [],
      ...spec.encodeDegreeAsArea ? [{ label: "Node marker area above the 3 px-radius visibility baseline is proportional to the declared degree; zero-degree nodes retain that baseline marker", color: accent(context.themeId), glyph: "series" }] : []
    ]
  };
}
function emptyTable() {
  return { policy: "complete_returned", columns: [], rows: [], rowsInline: 0, rowsTotal: 0 };
}
function withOpacity(hex, opacity) {
  const clamped = Math.max(0, Math.min(1, opacity));
  const alpha = Math.round(clamped * 255).toString(16).padStart(2, "0");
  return `${hex}${alpha}`;
}

// src/render/plan-closure.ts
var CLOSED_RENDER_PLANS = /* @__PURE__ */ new WeakSet();
var MAX_CLOSURE_PROBLEMS = 32;
var MAX_CLOSURE_NODES = 2e6;
var MAX_CLOSURE_DEPTH = 192;
var ARRAY_INDEX = /^(?:0|[1-9][0-9]*)$/u;
function problem(problems, path, message) {
  if (problems.length < MAX_CLOSURE_PROBLEMS) problems.push({ path, message });
}
function inspectPlainTree(value) {
  const problems = [];
  const pending = [
    { value, path: "", depth: 0 }
  ];
  const seen = /* @__PURE__ */ new WeakSet();
  let nodes = 0;
  while (pending.length > 0 && problems.length < MAX_CLOSURE_PROBLEMS) {
    const current = pending.pop();
    const item = current.value;
    if (item === null || typeof item === "string" || typeof item === "boolean") continue;
    if (typeof item === "number") {
      if (!Number.isFinite(item)) problem(problems, current.path, "number is not finite");
      continue;
    }
    if (typeof item !== "object") {
      problem(problems, current.path, `non-plain value of type ${typeof item} is forbidden`);
      continue;
    }
    if (current.depth > MAX_CLOSURE_DEPTH) {
      problem(problems, current.path, `plain plan nesting exceeds ${MAX_CLOSURE_DEPTH}`);
      continue;
    }
    if (seen.has(item)) {
      problem(problems, current.path, "plan graph repeats or cycles through an object identity");
      continue;
    }
    seen.add(item);
    nodes++;
    if (nodes > MAX_CLOSURE_NODES) {
      problem(problems, current.path, `plain plan exceeds ${MAX_CLOSURE_NODES} object/array nodes`);
      break;
    }
    let prototype;
    let descriptors;
    try {
      prototype = Object.getPrototypeOf(item);
      descriptors = Object.getOwnPropertyDescriptors(item);
    } catch {
      problem(problems, current.path, "plan object refused prototype/descriptor inspection");
      continue;
    }
    const array5 = Array.isArray(item);
    if (array5 && prototype !== Array.prototype || !array5 && prototype !== Object.prototype && prototype !== null) {
      problem(problems, current.path, "plan contains a non-plain object or array prototype");
      continue;
    }
    const keys = Reflect.ownKeys(descriptors);
    if (keys.some((key) => typeof key === "symbol")) {
      problem(problems, current.path, "symbol-keyed plan state is forbidden");
      continue;
    }
    const lengthDescriptor = array5 ? descriptors.length : void 0;
    const arrayLength = array5 && lengthDescriptor && "value" in lengthDescriptor && Number.isSafeInteger(lengthDescriptor.value) && lengthDescriptor.value >= 0 ? lengthDescriptor.value : null;
    let arrayIndices = 0;
    for (const key of keys) {
      const descriptor = descriptors[key];
      if (!descriptor || !("value" in descriptor)) {
        problem(problems, `${current.path}/${key}`, "accessor properties are forbidden");
        continue;
      }
      if (array5 && key === "length") continue;
      if (!descriptor.enumerable) {
        problem(problems, `${current.path}/${key}`, "non-enumerable plan state is forbidden");
        continue;
      }
      if (array5) {
        if (!ARRAY_INDEX.test(key) || arrayLength === null || Number(key) >= arrayLength) {
          problem(problems, `${current.path}/${key}`, "array has a non-index or out-of-range own property");
          continue;
        }
        arrayIndices++;
      }
      pending.push({
        value: descriptor.value,
        path: `${current.path}/${key}`,
        depth: current.depth + 1
      });
    }
    if (array5 && (arrayLength === null || arrayIndices !== arrayLength)) {
      problem(problems, current.path, "plan arrays must be dense and have an ordinary safe length");
    }
  }
  return problems;
}
function closePlainRenderPlanForAuthorityV1(candidate) {
  const sourceProblems = inspectPlainTree(candidate);
  if (sourceProblems.length > 0) return { tag: "refused", problems: sourceProblems };
  let detached;
  try {
    detached = structuredClone(candidate);
  } catch {
    return {
      tag: "refused",
      problems: [{ path: "", message: "plan could not be detached; proxies and non-cloneable atoms are forbidden" }]
    };
  }
  const detachedProblems = inspectPlainTree(detached);
  if (detachedProblems.length > 0) return { tag: "refused", problems: detachedProblems };
  const plan = deepFreeze(detached);
  CLOSED_RENDER_PLANS.add(plan);
  return { tag: "closed", plan };
}
function isClosedPlainRenderPlanForAuthorityV1(value) {
  return value !== null && typeof value === "object" && CLOSED_RENDER_PLANS.has(value);
}

// src/core/output-authority.ts
var MAX_AUTHORITY_VIOLATIONS = 64;
var MAX_AUTHORITY_GEOMETRY_ENTRIES = 1e6;
var MAX_AUTHORITY_SUMMARY_FACT_CODE_UNITS = 8192;
var MAX_AUTHORITY_SUMMARY_CODE_UNITS = 65536;
var UNSAFE_SUMMARY_CODE_POINT = /[\u0000-\u001f\u007f-\u009f\u061c\u200b-\u200f\u2028-\u202e\u2060-\u2069\ufeff\ufdd0-\ufdef\ufffe\uffff]/u;
var UNPAIRED_SURROGATE = /[\ud800-\udbff](?![\udc00-\udfff])|(?<![\ud800-\udbff])[\udc00-\udfff]/u;
function summaryStringSafe(value, maximum) {
  if (value.length > maximum || UNSAFE_SUMMARY_CODE_POINT.test(value) || UNPAIRED_SURROGATE.test(value)) return false;
  for (const character of value) {
    const codePoint = character.codePointAt(0);
    if ((codePoint & 65535) === 65534 || (codePoint & 65535) === 65535) return false;
  }
  return true;
}
function isOutputAuthoritySummaryFactSafeV1(value) {
  return summaryStringSafe(value, MAX_AUTHORITY_SUMMARY_FACT_CODE_UNITS);
}
function isOutputAuthoritySummarySafeV1(value) {
  return summaryStringSafe(value, MAX_AUTHORITY_SUMMARY_CODE_UNITS);
}
function violation(code, path, message) {
  return { code, path, message };
}
function recordViolation(violations, entry) {
  if (violations.length < MAX_AUTHORITY_VIOLATIONS) violations.push(entry);
}
function fieldKind(value) {
  return value.tag;
}
function declaredFieldKind(authority, field) {
  return authority.derivationFields.find((entry) => entry.id === field)?.valueKind;
}
function resolveDerivationField(authority, evaluation, ref, requiredKind, path, violations) {
  const declaredKind = declaredFieldKind(authority, ref.field);
  if (declaredKind === void 0) {
    recordViolation(violations, violation(
      "AUTHORITY_FIELD_MISSING",
      path,
      `derivation field ${JSON.stringify(ref.field)} is not in the skill's closed vocabulary`
    ));
    return null;
  }
  if (declaredKind !== requiredKind) {
    recordViolation(violations, violation(
      "AUTHORITY_FIELD_TYPE_MISMATCH",
      path,
      `derivation field ${JSON.stringify(ref.field)} is declared ${declaredKind}, not ${requiredKind}`
    ));
    return null;
  }
  const value = evaluation.fields[ref.field];
  if (value === void 0) {
    recordViolation(violations, violation(
      "AUTHORITY_FIELD_MISSING",
      path,
      `independent evaluator omitted derivation field ${JSON.stringify(ref.field)}`
    ));
    return null;
  }
  if (fieldKind(value) !== requiredKind) {
    recordViolation(violations, violation(
      "AUTHORITY_FIELD_TYPE_MISMATCH",
      path,
      `independent evaluator returned ${fieldKind(value)} for ${JSON.stringify(ref.field)}, expected ${requiredKind}`
    ));
    return null;
  }
  return value;
}
function canonicalValue(value) {
  try {
    return canonicalize(value);
  } catch {
    return null;
  }
}
function canonicalEqual(left, right) {
  const leftCanonical = canonicalValue(left);
  const rightCanonical = canonicalValue(right);
  return leftCanonical !== null && rightCanonical !== null && leftCanonical === rightCanonical;
}
function expectedSummary(template, requiredPlaceholders, facts, disclosures) {
  if (!isOutputAuthoritySummarySafeV1(template)) return null;
  const withoutRecognizedTokens = template.replace(/\{[A-Za-z][A-Za-z0-9]*\}/gu, "");
  if (withoutRecognizedTokens.includes("{") || withoutRecognizedTokens.includes("}")) return null;
  const factKeys = Object.keys(facts);
  if (factKeys.length !== requiredPlaceholders.length || factKeys.some((key) => !requiredPlaceholders.includes(key))) return null;
  let missing = false;
  const rendered = template.replace(/\{([A-Za-z][A-Za-z0-9]*)\}/gu, (_whole, key) => {
    if (!Object.prototype.hasOwnProperty.call(facts, key)) {
      missing = true;
      return "";
    }
    const value = facts[key];
    if (typeof value !== "string" || !isOutputAuthoritySummaryFactSafeV1(value)) {
      missing = true;
      return "";
    }
    return value;
  });
  if (missing || rendered.length > MAX_AUTHORITY_SUMMARY_CODE_UNITS) return null;
  if (disclosures.some((disclosure) => !isOutputAuthoritySummaryFactSafeV1(disclosure.text))) return null;
  if (disclosures.length === 0) return rendered;
  const count = disclosures.length;
  const complete = `${rendered} ${count} ${count === 1 ? "disclosure applies" : "disclosures apply"}: ${disclosures.map((disclosure) => disclosure.text).join(" ")}`;
  return complete.length <= MAX_AUTHORITY_SUMMARY_CODE_UNITS ? complete : null;
}
function collectObservedGeometry(roots, violations) {
  const sequence = [];
  const stack = [];
  for (let index = roots.length - 1; index >= 0; index--) {
    stack.push({ node: roots[index], path: `/geometry/${index}` });
  }
  const seenGroups = /* @__PURE__ */ new WeakSet();
  while (stack.length > 0) {
    const { node, path } = stack.pop();
    if (node === null || typeof node !== "object") {
      recordViolation(violations, violation(
        "AUTHORITY_GEOMETRY_GRAPH_INVALID",
        path,
        "geometry node is not a tagged object"
      ));
      continue;
    }
    switch (node.tag) {
      case "group": {
        if (seenGroups.has(node)) {
          recordViolation(violations, violation(
            "AUTHORITY_GEOMETRY_GRAPH_INVALID",
            path,
            "nested geometry repeats or cycles through the same group object"
          ));
          break;
        }
        seenGroups.add(node);
        const children = node.children;
        if (!Array.isArray(children)) {
          recordViolation(violations, violation(
            "AUTHORITY_GEOMETRY_GRAPH_INVALID",
            `${path}/children`,
            "group children must be an array"
          ));
          break;
        }
        for (let index = children.length - 1; index >= 0; index--) {
          stack.push({ node: children[index], path: `${path}/children/${index}` });
        }
        break;
      }
      case "data_mark": {
        const mark = node;
        if (sequence.length >= MAX_AUTHORITY_GEOMETRY_ENTRIES) {
          recordViolation(violations, violation(
            "AUTHORITY_GEOMETRY_GRAPH_INVALID",
            path,
            `geometry carrier sequence exceeds ${MAX_AUTHORITY_GEOMETRY_ENTRIES}`
          ));
          return sequence;
        }
        sequence.push(mark.entry);
        break;
      }
      case "axis":
      case "text":
      case "disclosure":
      case "decorative_mark":
        break;
      default:
        recordViolation(violations, violation(
          "AUTHORITY_GEOMETRY_GRAPH_INVALID",
          path,
          "geometry node has an unknown or missing explicit tag"
        ));
    }
  }
  return sequence;
}
function interpretOutputAuthorityModelV1(authority, summaryTemplate, tableColumnContracts, expectedDisclosures, canonicalRequestDigest, evaluation, observed) {
  const violations = [];
  if (evaluation.evaluatorId !== authority.evaluator.id) {
    recordViolation(violations, violation(
      "AUTHORITY_EVALUATOR_MISMATCH",
      "/evaluatorId",
      `evaluation names ${JSON.stringify(evaluation.evaluatorId)}, expected ${JSON.stringify(authority.evaluator.id)}`
    ));
  }
  if (evaluation.canonicalRequestDigest !== canonicalRequestDigest) {
    recordViolation(violations, violation(
      "AUTHORITY_EVALUATOR_MISMATCH",
      "/canonicalRequestDigest",
      "authority evaluation is not bound to the validated canonical request being emitted"
    ));
  }
  if (!canonicalEqual(observed.disclosures, expectedDisclosures)) {
    recordViolation(violations, violation(
      "AUTHORITY_DISCLOSURE_MISMATCH",
      "/disclosures",
      "actual plan disclosures do not equal the independently derived mandatory disclosure set"
    ));
  }
  const declaredEvaluationFields = authority.derivationFields.map((field) => field.id).sort();
  const actualEvaluationFields = Object.keys(evaluation.fields).sort();
  if (!canonicalEqual(declaredEvaluationFields, actualEvaluationFields)) {
    recordViolation(violations, violation(
      "AUTHORITY_FIELD_MISSING",
      "/fields",
      "independent evaluator fields must exactly equal the skill-local closed derivation vocabulary; missing and unknown fields are refused"
    ));
  }
  const rowValue = resolveDerivationField(
    authority,
    evaluation,
    authority.table.expectedRows,
    "row_sequence",
    "/table/expectedRows",
    violations
  );
  const expectedRows = rowValue?.rows ?? [];
  const expectedRowsTotal = expectedRows.length;
  const sourceColumns = tableColumnContracts.map(({ key, header }) => ({ key, header }));
  const sourceKeys = tableColumnContracts.map(({ key }) => key);
  if (!canonicalEqual(sourceKeys, authority.table.carriedValueColumns) || !canonicalEqual(observed.table.columns, sourceColumns)) {
    recordViolation(violations, violation(
      "AUTHORITY_TABLE_COLUMNS_MISMATCH",
      "/table/columns",
      "observed columns are not the exact ordered source-owned carried-value columns"
    ));
  }
  const expectedBadWidth = expectedRows.findIndex(
    (row) => row.length !== authority.table.carriedValueColumns.length
  );
  const observedBadWidth = observed.table.rows.findIndex(
    (row) => row.length !== authority.table.carriedValueColumns.length
  );
  if (expectedBadWidth !== -1 || observedBadWidth !== -1) {
    recordViolation(violations, violation(
      "AUTHORITY_TABLE_ROW_WIDTH_MISMATCH",
      "/table/rows",
      expectedBadWidth !== -1 ? `independent expected row ${expectedBadWidth} does not carry every declared value column` : `observed row ${observedBadWidth} does not carry every declared value column`
    ));
  }
  if (!canonicalEqual(expectedRows, observed.table.rows)) {
    recordViolation(violations, violation(
      "AUTHORITY_TABLE_ROWS_MISMATCH",
      "/table/rows",
      "observed rows are not the exact evaluator-derived row sequence, including order, duplicate multiplicity, and every carried value"
    ));
  }
  let cellsValid = true;
  for (const row of observed.table.rows) {
    for (let columnIndex = 0; columnIndex < row.length; columnIndex++) {
      const cell = row[columnIndex];
      const column = tableColumnContracts[columnIndex];
      if (!column) {
        cellsValid = false;
        continue;
      }
      if (cell === null) {
        if (!column.nullable) cellsValid = false;
      } else if (typeof cell === "number") {
        if (!Number.isFinite(cell) || column.cellType === "string") cellsValid = false;
      } else if (typeof cell === "string") {
        if (column.cellType === "finite_number") cellsValid = false;
      } else {
        cellsValid = false;
      }
    }
  }
  if (!cellsValid) {
    recordViolation(violations, violation(
      "AUTHORITY_TABLE_CELL_DOMAIN_MISMATCH",
      "/table/rows",
      "observed table cells violate a source-owned type, nullability, or finite-number domain"
    ));
  }
  const keyIndices = tableColumnContracts.map((column, index) => column.keyPart ? index : -1).filter((index) => index >= 0);
  const rowKeys = observed.table.rows.map((row) => canonicalValue(keyIndices.map((index) => row[index])));
  if (keyIndices.length === 0 || rowKeys.some((key) => key === null) || new Set(rowKeys).size !== rowKeys.length) {
    recordViolation(violations, violation(
      "AUTHORITY_TABLE_KEY_MISMATCH",
      "/table/rows",
      "source-owned composite row keys must exist and be unique over the verified sequence"
    ));
  }
  if (observed.table.rowsTotal !== expectedRowsTotal || observed.table.rowsInline !== expectedRowsTotal || observed.table.rows.length !== expectedRowsTotal) {
    recordViolation(violations, violation(
      "AUTHORITY_TABLE_TOTAL_MISMATCH",
      "/table/rowsTotal",
      `rowsTotal must be ${expectedRowsTotal}, derived from verified expected rows; observed rowsTotal=${observed.table.rowsTotal}, rowsInline=${observed.table.rowsInline}, rows.length=${observed.table.rows.length}`
    ));
  }
  const observedGeometry = collectObservedGeometry(observed.geometry, violations);
  const declaredClassIds = new Set(authority.geometry.classes.map((entry) => entry.id));
  const unknownClassIds = /* @__PURE__ */ new Set();
  for (const entry of observedGeometry) {
    if (!declaredClassIds.has(entry.classId)) {
      unknownClassIds.add(entry.classId);
      if (unknownClassIds.size > MAX_AUTHORITY_VIOLATIONS) break;
      recordViolation(violations, violation(
        "AUTHORITY_GEOMETRY_CLASS_UNKNOWN",
        "/geometry",
        `observed data geometry uses undeclared class ${JSON.stringify(entry.classId)}`
      ));
    }
  }
  const geometryValue = resolveDerivationField(
    authority,
    evaluation,
    authority.geometry.expectedSequence,
    "geometry_sequence",
    "/geometry/expectedSequence",
    violations
  );
  if (geometryValue !== null) {
    const classById = new Map(authority.geometry.classes.map((entry) => [entry.id, entry]));
    const comparable = (entries) => entries.map((entry) => {
      const geometryClass = classById.get(entry.classId);
      return geometryClass?.payloadAssurance === "canonical_geometry_exact" ? entry : { tag: "carrier", classId: entry.classId, provenance: entry.provenance };
    });
    const exactPayloadsAvailable = [...geometryValue.entries, ...observedGeometry].every((entry) => {
      const geometryClass = classById.get(entry.classId);
      return geometryClass?.payloadAssurance !== "canonical_geometry_exact" || entry.tag === "canonical_geometry";
    });
    if (!exactPayloadsAvailable || !canonicalEqual(comparable(geometryValue.entries), comparable(observedGeometry))) {
      recordViolation(violations, violation(
        "AUTHORITY_GEOMETRY_SEQUENCE_MISMATCH",
        "/geometry",
        "global data-geometry sequence differs in exact cardinality, DFS interleaving, class order, provenance, or independently assured geometry payload"
      ));
    }
  }
  const factValue = resolveDerivationField(
    authority,
    evaluation,
    authority.summary.expectedFacts,
    "summary_fact_map",
    "/summary/expectedFacts",
    violations
  );
  if (factValue !== null) {
    const rendered = expectedSummary(
      summaryTemplate,
      authority.summary.requiredPlaceholders,
      factValue.facts,
      expectedDisclosures
    );
    if (rendered === null) {
      recordViolation(violations, violation(
        "AUTHORITY_SUMMARY_FACTS_MISMATCH",
        "/summary/facts",
        "summary fact keys must exactly equal the source-owned placeholder vocabulary and every value must be a bounded control-safe string"
      ));
    } else if (rendered !== observed.summary) {
      recordViolation(violations, violation(
        "AUTHORITY_SUMMARY_MISMATCH",
        "/summary",
        "observed summary is not exact fail-closed substitution of independently evaluated facts into the skill template"
      ));
    }
  }
  return violations.length === 0 ? { tag: "valid", expectedRowsTotal } : { tag: "invalid", violations };
}

// src/render/output-authority-gate.ts
import { isValidatedRequest } from "#cortexel-request-capability";

// src/authority/evaluators/model.ts
function authorityEvaluatorId(skillId, revision) {
  return `${skillId}.output_authority.v${revision}`;
}
function rowSequence(rows) {
  return { tag: "row_sequence", rows };
}
function geometrySequence(entries) {
  return { tag: "geometry_sequence", entries };
}
function summaryFactMap(facts) {
  return { tag: "summary_fact_map", facts };
}
function disclosureFactMap(facts) {
  return { tag: "disclosure_fact_map", facts };
}
function defineAuthorityEvaluator(id, deriveFields) {
  return Object.freeze({
    id,
    evaluateCanonicalRequest(canonicalRequest) {
      return {
        evaluatorId: id,
        canonicalRequestDigest: canonicalDigest(canonicalRequest),
        fields: deriveFields(canonicalRequest)
      };
    }
  });
}
function carrier(classId, provenance) {
  return { tag: "carrier", classId, provenance };
}

// src/authority/evaluators/distributions.ts
var MAX_DISTRIBUTION_AUTHORITY_ROWS = 500;
var DELAY_AUTHORITY;
var WEIGHT_AUTHORITY;
var ISI_AUTHORITY;
var CORRELOGRAM_AUTHORITY;
var PSTH_AUTHORITY;
function record(value) {
  return value !== null && value !== void 0 && typeof value === "object" && !Array.isArray(value) ? value : {};
}
function array(value) {
  return Array.isArray(value) ? value : [];
}
function numbers(value) {
  return array(value).map(Number);
}
function nullableNumbers(value) {
  return array(value).map((entry) => entry === null ? null : Number(entry));
}
function strings(value) {
  return array(value).map(String);
}
function exactText(value) {
  if (typeof value === "bigint") return value.toString(10);
  return value === 0 ? "0" : String(value);
}
function finiteExtrema(values) {
  if (values.length === 0) return null;
  let min = values[0];
  let max = values[0];
  for (let index = 1; index < values.length; index++) {
    const value = values[index];
    if (value < min) min = value;
    if (value > max) max = value;
  }
  return { min, max };
}
function maximumNonnegativeInteger(values) {
  let maximum = 0;
  for (const value of values) {
    if (value > maximum) maximum = value;
  }
  return maximum;
}
function structuredCell(value) {
  return value === void 0 ? "unknown" : canonicalize(value);
}
function distributionScopeSummaryCell(value) {
  const scope = record(value);
  const summary = { kind: String(scope.kind) };
  if (scope.snapshotTime !== void 0) summary.snapshotTime = scope.snapshotTime;
  switch (scope.kind) {
    case "single_process":
      break;
    case "global_merged":
      summary.mergedRanksCoverage = "all_ranks_0_through_worldSize_minus_1";
      summary.worldSize = scope.worldSize;
      break;
    case "mpi_target_rank_local":
      summary.localTargetUniverseComplete = scope.localTargetUniverseComplete;
      summary.rank = scope.rank;
      summary.worldSize = scope.worldSize;
      break;
    case "sampled":
      summary.method = scope.method;
      summary.parentScope = scope.parentScope;
      summary.retainedConnectionCount = scope.retainedConnectionCount;
      summary.sourceConnectionCount = scope.sourceConnectionCount;
      break;
    default:
      throw new Error(`unsupported NetworkScope kind ${String(scope.kind)}`);
  }
  return canonicalize(summary);
}
function compareIdentifiers(left, right) {
  const a = left[Symbol.iterator]();
  const b = right[Symbol.iterator]();
  while (true) {
    const x = a.next();
    const y = b.next();
    if (x.done || y.done) {
      if (x.done && y.done) return 0;
      return x.done ? -1 : 1;
    }
    const xc = x.value.codePointAt(0);
    const yc = y.value.codePointAt(0);
    if (xc !== yc) return xc < yc ? -1 : 1;
  }
}
function tupleKey(parts) {
  return JSON.stringify(parts);
}
function finalEdgeInclusive(node) {
  if (typeof node.finalEdgeInclusive === "boolean") return node.finalEdgeInclusive;
  return node.boundary === "[lo,hi]" || node.boundary === "[start,stop]";
}
function binsFrom(nodeValue, fallbackUnit) {
  const node = record(nodeValue);
  const unit = typeof node.unit === "string" ? node.unit : fallbackUnit;
  if (!unit) throw new Error("authority evaluator requires a declared bin unit");
  if (node.mode === "edges" || Array.isArray(node.edges)) {
    const edges = numbers(node.edges);
    if (edges.length < 2) throw new Error("authority evaluator requires at least two edges");
    return { edges, unit, finalEdgeInclusive: finalEdgeInclusive(node) };
  }
  const materialized = materializeWidthBins(
    Number(node.start),
    Number(node.stop),
    Number(node.width)
  );
  if (!materialized.ok) {
    throw new Error(`authority evaluator cannot materialize bins (${materialized.reason})`);
  }
  return {
    edges: materialized.edges,
    unit,
    finalEdgeInclusive: finalEdgeInclusive(node)
  };
}
function sameUnitWidth(lower, upper, unit) {
  try {
    return convertDifference(lower, upper, unit, unit);
  } catch {
    const width = upper - lower;
    if (!Number.isFinite(width) || !(width > 0)) {
      throw new Error(`bin in ${unit} has no positive finite width`);
    }
    return width;
  }
}
function converted(value, from, to) {
  return from === to ? value : convert(value, from, to);
}
function ordinaryBinIndex(value, valueUnit, bins, edgeToleranceUlps = 0) {
  const candidate = converted(value, valueUnit, bins.unit);
  const compare = (edge) => {
    const tolerance = edgeToleranceUlps * Number.EPSILON * Math.max(
      Number.MIN_VALUE,
      Math.abs(candidate),
      Math.abs(edge)
    );
    return Math.abs(candidate - edge) <= tolerance ? 0 : candidate < edge ? -1 : 1;
  };
  const last = bins.edges.length - 1;
  if (compare(bins.edges[0]) < 0) return -1;
  const againstLast = compare(bins.edges[last]);
  if (againstLast > 0 || againstLast === 0 && !bins.finalEdgeInclusive) return -1;
  if (againstLast === 0) return last - 1;
  let low = 0;
  let high = last;
  while (low < high) {
    const middle = Math.floor((low + high + 1) / 2);
    if (compare(bins.edges[middle]) >= 0) low = middle;
    else high = middle - 1;
  }
  return low;
}
function normalizedHistogramValues(counts, bins, normalization) {
  if (normalization === "count") return [...counts];
  const total = counts.reduce((sum, count) => sum + BigInt(count), 0n);
  if (total <= 0n || total > BigInt(Number.MAX_SAFE_INTEGER)) {
    throw new Error("normalized histogram has no positive safe-integer denominator");
  }
  if (normalization === "probability") {
    return counts.map((count) => exactRationalToBinary64(BigInt(count), total));
  }
  const denominator = Number(total);
  return counts.map((count, index) => divideExactIntegerByConvertedDifference(
    count,
    denominator,
    bins.edges[index],
    bins.edges[index + 1],
    bins.unit,
    bins.unit
  ));
}
function recoverCounts(values, bins, normalization, total) {
  if (normalization === "count") return values.map((value) => {
    if (!Number.isSafeInteger(value) || value < 0) throw new Error("count is not exact");
    return value;
  });
  const output = values.map((value, index) => {
    const estimate = normalization === "probability" ? value * total : value * total * sameUnitWidth(bins.edges[index], bins.edges[index + 1], bins.unit);
    const candidates = /* @__PURE__ */ new Set();
    for (let offset = -3; offset <= 3; offset++) {
      candidates.add(Math.floor(estimate) + offset);
      candidates.add(Math.ceil(estimate) + offset);
    }
    const matches = [...candidates].filter((candidate) => {
      if (!Number.isSafeInteger(candidate) || candidate < 0 || candidate > total) return false;
      const expected = normalization === "probability" ? exactRationalToBinary64(BigInt(candidate), BigInt(total)) : divideExactIntegerByConvertedDifference(
        candidate,
        total,
        bins.edges[index],
        bins.edges[index + 1],
        bins.unit,
        bins.unit
      );
      return Object.is(expected, value);
    });
    if (matches.length !== 1) throw new Error("normalized bin does not identify one count");
    return matches[0];
  });
  if (output.reduce((sum, count) => sum + count, 0) !== total) {
    throw new Error("recovered counts do not conserve the declared total");
  }
  return output;
}
function conversionDisclosure(label, from, to) {
  const receipt = conversionReceipt(from, to);
  return `${label}: ${from} -> ${to} (factor ${receipt.factor})`;
}
function baseDisclosureFacts(request, extra = {}) {
  const source = record(request.source);
  const data = record(request.data);
  const parameters = record(request.parameters);
  const scope = record(data.scope);
  const nodeUniverse = record(data.nodeUniverse);
  const uncertainty = record(parameters.uncertainty ?? data.uncertainty);
  return {
    sourceKind: typeof source.kind === "string" ? source.kind : "unknown",
    sourceAuthenticityVerified: false,
    referenceComparisonRun: false,
    callerNotePresent: typeof source.declaredNote === "string",
    uncertaintyKind: typeof uncertainty.kind === "string" ? uncertainty.kind : "none",
    uncertaintyReason: typeof uncertainty.reason === "string" ? uncertainty.reason : "not_provided",
    ...typeof scope.kind === "string" ? { scopeKind: scope.kind } : {},
    ...typeof scope.rank === "number" ? { rank: scope.rank } : {},
    ...typeof scope.worldSize === "number" ? { worldSize: scope.worldSize } : {},
    ...typeof scope.retainedConnectionCount === "number" ? {
      retainedConnectionCount: scope.retainedConnectionCount,
      sampledRetained: scope.retainedConnectionCount
    } : {},
    ...typeof scope.sourceConnectionCount === "number" ? {
      sourceConnectionCount: scope.sourceConnectionCount,
      sampledSource: scope.sourceConnectionCount
    } : {},
    ...typeof nodeUniverse.complete === "boolean" ? { nodeUniverseComplete: nodeUniverse.complete } : {},
    ...extra
  };
}
function modelFields(model) {
  return {
    "table.rows": rowSequence(model.rows),
    "geometry.sequence": geometrySequence(model.geometry),
    "summary.facts": summaryFactMap(model.summary),
    "disclosure.facts": disclosureFactMap(model.disclosures)
  };
}
function binCarriers(groups, bins) {
  return groups.flatMap((group) => group.counts.map((_count, binIndex2) => carrier("bins", {
    groupId: group.id,
    binIndex: binIndex2,
    binStart: bins.edges[binIndex2],
    binEnd: bins.edges[binIndex2 + 1]
  })));
}
function degreeModel(requestValue) {
  const request = record(requestValue);
  const data = record(request.data);
  const parameters = record(request.parameters);
  const universe = strings(record(data.nodeUniverse).ids);
  const direction = String(parameters.direction);
  const countingPolicy = String(parameters.countingPolicy);
  const autapsePolicy = String(parameters.autapsePolicy);
  const normalization = String(parameters.normalization);
  let degrees;
  let countedConnectionCount;
  let countedIncidenceCount;
  let excludedAutapseCount = 0;
  if (data.mode === "node_degrees") {
    const declared = record(data.nodeDegrees);
    const byId = new Map(strings(declared.nodeIds).map((id, index) => [id, numbers(declared.degrees)[index]]));
    degrees = universe.map((id) => {
      const value = byId.get(id);
      if (value === void 0) throw new Error("degree authority lost a declared node");
      return value;
    });
    countedConnectionCount = Number(data.countedConnectionCount);
    countedIncidenceCount = Number(data.countedIncidenceCount);
    excludedAutapseCount = typeof data.excludedAutapseCount === "number" ? data.excludedAutapseCount : 0;
  } else {
    const connections = record(data.connections);
    const sources = strings(connections.sourceIds);
    const targets = strings(connections.targetIds);
    const index = new Map(universe.map((id, ordinal) => [id, ordinal]));
    degrees = universe.map(() => 0);
    const neighbours = universe.map(() => /* @__PURE__ */ new Set());
    countedConnectionCount = 0;
    for (let ordinal = 0; ordinal < sources.length; ordinal++) {
      if (sources[ordinal] === targets[ordinal] && autapsePolicy === "exclude") {
        excludedAutapseCount++;
        continue;
      }
      countedConnectionCount++;
      const id = direction === "in" ? targets[ordinal] : sources[ordinal];
      const counterpart = direction === "in" ? sources[ordinal] : targets[ordinal];
      const owner = index.get(id);
      if (owner === void 0) throw new Error("connection endpoint is outside degree universe");
      if (countingPolicy === "count_unique_neighbors") neighbours[owner].add(counterpart);
      else degrees[owner]++;
    }
    if (countingPolicy === "count_unique_neighbors") {
      degrees = neighbours.map((entry) => entry.size);
    }
    countedIncidenceCount = degrees.reduce((sum, degree) => sum + degree, 0);
  }
  const maximum = maximumNonnegativeInteger(degrees);
  if (maximum + 1 > MAX_DISTRIBUTION_AUTHORITY_ROWS) {
    throw new Error(
      `degree authority requires ${maximum + 1} rows, over its ${MAX_DISTRIBUTION_AUTHORITY_ROWS}-row contract budget`
    );
  }
  const nodeCounts = new Array(maximum + 1).fill(0);
  for (const degree of degrees) nodeCounts[degree]++;
  const probabilities = nodeCounts.map((count) => exactRationalToBinary64(BigInt(count), BigInt(universe.length)));
  const rows = nodeCounts.map((count, degree) => [
    degree,
    degree,
    count,
    normalization === "probability" ? probabilities[degree] : null,
    universe.length,
    countedConnectionCount,
    countedIncidenceCount,
    "1"
  ]);
  const groups = [{ id: "all", counts: nodeCounts }];
  const degreeEdges = new Array(nodeCounts.length + 1);
  degreeEdges[0] = -0.5;
  for (let degree = 0; degree < nodeCounts.length; degree++) {
    degreeEdges[degree + 1] = degree + 0.5;
  }
  const scopeSummary = distributionScopeSummaryCell(data.scope);
  return {
    rows,
    geometry: binCarriers(groups, {
      edges: degreeEdges,
      unit: "1",
      finalEdgeInclusive: true
    }),
    summary: {
      direction,
      selectionLabel: String(parameters.selectionLabel ?? parameters.selectionId),
      countingPolicy,
      autapsePolicy,
      excludedAutapseStatement: autapsePolicy === "exclude" ? ` (${excludedAutapseCount} excluded)` : "",
      universeNodeCount: exactText(universe.length),
      scopeStatement: scopeSummary,
      countedConnectionCount: exactText(countedConnectionCount),
      countedIncidenceCount: exactText(countedIncidenceCount),
      minDegree: "0",
      maxDegree: exactText(maximum),
      zeroDegreeNodeCount: exactText(nodeCounts[0]),
      normalization
    },
    disclosures: baseDisclosureFacts(request, {
      ...countingPolicy === "count_unique_neighbors" && countedIncidenceCount < countedConnectionCount ? {
        multapseAggregated: true,
        multapseAggregation: "count_unique_neighbors"
      } : {}
    })
  };
}
var DEGREE_AUTHORITY = defineAuthorityEvaluator(
  authorityEvaluatorId("network.degree_distribution", 2),
  (request) => modelFields(degreeModel(request))
);
function populationRateModel(requestValue) {
  const request = record(requestValue);
  const data = record(request.data);
  const parameters = record(request.parameters);
  const normalization = String(parameters.normalization);
  let bins;
  let counts;
  let recordedSenderCount;
  let sourceEventCount;
  let eventTimeConversion;
  let suppliedRateConversion;
  if (data.mode === "events") {
    bins = binsFrom(parameters.bins);
    const eventTimes = record(data.eventTimes);
    const eventUnit = String(eventTimes.unit);
    counts = new Array(bins.edges.length - 1).fill(0);
    for (const value of numbers(eventTimes.values)) {
      const index = ordinaryBinIndex(value, eventUnit, bins);
      if (index < 0) throw new Error("population-rate event is outside declared bins");
      counts[index]++;
    }
    recordedSenderCount = strings(data.recordedSenderIds).length;
    sourceEventCount = numbers(eventTimes.values).length;
    if (eventUnit !== bins.unit) {
      eventTimeConversion = conversionDisclosure("population-rate event times", eventUnit, bins.unit);
    }
  } else {
    const edgeRecord = record(data.binEdges);
    bins = { edges: numbers(edgeRecord.edges), unit: String(edgeRecord.unit), finalEdgeInclusive: false };
    counts = numbers(data.counts);
    recordedSenderCount = strings(data.recordedSenderIds).length;
    sourceEventCount = Number(data.sourceEventCount);
    const suppliedRates = record(data.rates);
    const suppliedRateUnit = typeof suppliedRates.unit === "string" ? suppliedRates.unit : void 0;
    if (suppliedRateUnit !== void 0 && suppliedRateUnit !== "Hz") {
      suppliedRateConversion = conversionDisclosure(
        "supplied population rates",
        suppliedRateUnit,
        "Hz"
      );
    }
  }
  const rateDivisor = normalization === "mean_rate_per_recorded_sender" ? recordedSenderCount : 1;
  const widths = counts.map((_count, index) => sameUnitWidth(
    bins.edges[index],
    bins.edges[index + 1],
    bins.unit
  ));
  const rates = counts.map((count, index) => divideExactIntegerByConvertedDifference(
    count,
    rateDivisor,
    bins.edges[index],
    bins.edges[index + 1],
    bins.unit,
    "s"
  ));
  const rows = counts.map((count, index) => [
    bins.edges[index],
    bins.edges[index + 1],
    widths[index],
    count,
    recordedSenderCount,
    rates[index],
    "Hz"
  ]);
  const geometry = counts.map((_count, binIndex2) => carrier("bins", {
    binIndex: binIndex2,
    binStart: bins.edges[binIndex2],
    binEnd: bins.edges[binIndex2 + 1]
  }));
  const window = record(data.window);
  const rateExtent = finiteExtrema(rates);
  if (rateExtent === null) throw new Error("population-rate bins unexpectedly empty");
  const unitConversions = [
    ...eventTimeConversion ? [eventTimeConversion] : [],
    ...bins.unit !== "s" ? [conversionDisclosure("population-rate bin widths", bins.unit, "s")] : [],
    ...suppliedRateConversion ? [suppliedRateConversion] : []
  ];
  return {
    rows,
    geometry,
    summary: {
      populationLabel: String(parameters.populationLabel ?? parameters.populationId),
      windowStart: exactText(Number(window.start)),
      windowStop: exactText(Number(window.stop)),
      timeUnit: String(window.unit),
      binCount: exactText(counts.length),
      eventCount: exactText(sourceEventCount),
      recordedSenderCount: exactText(recordedSenderCount),
      normalization,
      rateMin: exactText(rateExtent.min),
      rateMax: exactText(rateExtent.max)
    },
    disclosures: baseDisclosureFacts(request, {
      ...data.mode === "prebinned" ? { preBinned: true } : {},
      ...unitConversions.length > 0 ? { unitConversions } : {}
    })
  };
}
var POPULATION_RATE_AUTHORITY = defineAuthorityEvaluator(
  authorityEvaluatorId("neuro.population_rate", 2),
  (request) => modelFields(populationRateModel(request))
);
function rasterPartition(data) {
  const eventTimes = record(data.eventTimes);
  const window = record(data.window);
  const times = numbers(eventTimes.values);
  const timeUnit = String(eventTimes.unit);
  const windowUnit = String(window.unit);
  const nest = window.kind === "nest_recording_device_origin_relative";
  const lower = nest ? [
    { value: Number(window.origin), unit: windowUnit },
    { value: Number(window.start), unit: windowUnit }
  ] : [{ value: Number(window.start), unit: windowUnit }];
  const upper = nest ? [
    { value: Number(window.origin), unit: windowUnit },
    { value: Number(window.stop), unit: windowUnit }
  ] : [{ value: Number(window.stop), unit: windowUnit }];
  const boundary = nest ? "(origin+start,origin+stop]" : String(window.boundary);
  const openStart = boundary.startsWith("(");
  const closedStop = boundary.endsWith("]");
  const inWindow2 = times.map((time) => {
    const event = { value: time, unit: timeUnit };
    const lowerVsEvent = compareExactUnitSumToValue(lower, event);
    const upperVsEvent = compareExactUnitSumToValue(upper, event);
    return !(openStart ? lowerVsEvent >= 0 : lowerVsEvent > 0) && !(closedStop ? upperVsEvent < 0 : upperVsEvent <= 0);
  });
  return {
    times,
    timeUnit,
    windowUnit,
    boundary,
    inWindow: inWindow2,
    displayStart: convertExactUnitSum(lower, timeUnit),
    displayStop: convertExactUnitSum(upper, timeUnit),
    excludedCount: inWindow2.filter((accepted) => !accepted).length
  };
}
function rasterModel(requestValue) {
  const request = record(requestValue);
  const data = record(request.data);
  const parameters = record(request.parameters);
  const partition = rasterPartition(data);
  const senders = strings(data.eventSenderIds);
  const recorded = strings(data.recordedSenderIds);
  const eventTrials = Array.isArray(data.eventTrialIds) ? strings(data.eventTrialIds) : void 0;
  const trials = strings(data.trialIds);
  const suppliedEventIds = Array.isArray(data.eventIds) ? strings(data.eventIds) : void 0;
  const populations = strings(data.senderPopulationIds);
  const populationBySender = new Map(recorded.map((sender, index) => [sender, populations[index]]));
  let orderedSenders = [...recorded];
  if (parameters.rowOrder === "canonical_sender_id") {
    orderedSenders.sort(compareIdentifiers);
  } else if (parameters.rowOrder === "grouped_by_population" && populations.length > 0) {
    const groups = /* @__PURE__ */ new Map();
    for (const sender of recorded) {
      const population = populationBySender.get(sender);
      if (population === void 0) continue;
      const members = groups.get(population);
      if (members) members.push(sender);
      else groups.set(population, [sender]);
    }
    orderedSenders = [...groups.values()].flat();
  }
  const rowKey = (sender, trial) => JSON.stringify([sender, trial]);
  const rasterRows = orderedSenders.flatMap((sender) => (eventTrials ? trials : [null]).map((trial) => ({
    key: rowKey(sender, trial),
    label: trial === null ? sender : `${sender} / ${trial}`
  })));
  const rowIndex = new Map(rasterRows.map((row, index) => [row.key, index]));
  const rowLabel = new Map(rasterRows.map((row) => [row.key, row.label]));
  const events = partition.times.map((time, sourceOrdinal) => {
    const senderId = senders[sourceOrdinal];
    const trialId = eventTrials?.[sourceOrdinal] ?? null;
    const key = rowKey(senderId, trialId);
    return {
      sourceOrdinal,
      eventId: suppliedEventIds?.[sourceOrdinal] ?? `source-ordinal-${sourceOrdinal}`,
      time,
      timeUnit: partition.timeUnit,
      senderId,
      trialId,
      populationId: populationBySender.get(senderId) ?? null,
      rowKey: key,
      rowIndex: rowIndex.get(key),
      rowLabel: rowLabel.get(key),
      inWindow: partition.inWindow[sourceOrdinal]
    };
  }).sort((left, right) => left.time - right.time || left.rowIndex - right.rowIndex || left.sourceOrdinal - right.sourceOrdinal);
  const rows = events.map((event) => [
    event.sourceOrdinal,
    event.eventId,
    event.time,
    event.timeUnit,
    event.senderId,
    event.trialId,
    event.populationId,
    event.rowKey,
    event.rowIndex,
    event.rowLabel,
    event.inWindow ? "true" : "false"
  ]);
  const geometry = events.flatMap((event) => event.inWindow ? [carrier("events", {
    sourceOrdinal: event.sourceOrdinal,
    eventId: event.eventId,
    senderId: event.senderId,
    trialId: event.trialId,
    rowKey: event.rowKey
  })] : []);
  const active = new Set(events.filter((event) => event.inWindow).map((event) => event.senderId));
  const accepted = events.length - partition.excludedCount;
  return {
    rows,
    geometry,
    summary: {
      eventCount: exactText(events.length),
      excludedCount: exactText(partition.excludedCount),
      activeSenderCount: exactText(active.size),
      recordedSenderCount: exactText(recorded.length),
      trialCount: exactText(eventTrials ? trials.length : 1),
      windowStart: exactText(partition.displayStart),
      windowStop: exactText(partition.displayStop),
      timeUnit: partition.timeUnit,
      windowBoundary: partition.boundary,
      timeBase: String(data.timeBase),
      rowCount: exactText(rasterRows.length),
      rowOrder: String(parameters.rowOrder),
      senderUniverseComplete: String(data.senderUniverseComplete),
      markCount: exactText(accepted)
    },
    disclosures: baseDisclosureFacts(request, {
      ...partition.excludedCount > 0 ? { excludedOutOfWindow: partition.excludedCount } : {},
      ...partition.windowUnit !== partition.timeUnit ? {
        unitConversions: [conversionDisclosure(
          "spike-raster window endpoints",
          partition.windowUnit,
          partition.timeUnit
        )]
      } : {},
      ...record(data.window).kind === "nest_recording_device_origin_relative" ? { nestSerializedClock: true } : {}
    })
  };
}
var RASTER_AUTHORITY = defineAuthorityEvaluator(
  authorityEvaluatorId("neuro.spike_raster", 2),
  (request) => modelFields(rasterModel(request))
);
function aggregate2(values, method) {
  const ordered = [...values].sort((left, right) => left - right);
  if (ordered.length === 0) throw new Error("cannot aggregate an empty ordered pair");
  if (method === "no_aggregation") {
    if (ordered.length !== 1) throw new Error("no_aggregation encountered a multapse");
    return ordered[0];
  }
  if (method === "sum") return exactBinary64Sum(ordered);
  if (method === "mean") return exactBinary64Mean(ordered);
  if (method === "min") return ordered[0];
  if (method === "max") return ordered[ordered.length - 1];
  throw new Error(`unknown aggregation ${method}`);
}
function histogramGroup(id, observations, bins, normalization, connectionCount, missingMeasurementCount, missingObservationCount, zeroObservationCount, edgeToleranceUlps = 0) {
  const counts = new Array(bins.edges.length - 1).fill(0);
  let underRangeCount = 0;
  let overRangeCount = 0;
  for (const observation of observations) {
    const index = ordinaryBinIndex(observation, bins.unit, bins, edgeToleranceUlps);
    if (index >= 0) counts[index]++;
    else if (observation < bins.edges[0]) underRangeCount++;
    else overRangeCount++;
  }
  return {
    id,
    counts,
    values: normalizedHistogramValues(counts, bins, normalization),
    connectionCount,
    observationCount: observations.length,
    binnedObservationCount: counts.reduce((sum, count) => sum + count, 0),
    underRangeCount,
    overRangeCount,
    missingMeasurementCount,
    missingObservationCount,
    zeroObservationCount
  };
}
function prebinnedGroup(data, bins, normalization, isDelay) {
  const histogram = record(data.histogram);
  const declaredValues = numbers(histogram.values);
  const observationCount = Number(data.totalObservationCount);
  const underRangeCount = Number(data.underRangeCount ?? data.excludedUnderRangeCount ?? 0);
  const overRangeCount = Number(data.overRangeCount ?? data.excludedOverRangeCount ?? 0);
  const inRange = observationCount - underRangeCount - overRangeCount;
  const suppliedCounts = Array.isArray(data.counts) ? numbers(data.counts) : void 0;
  const counts = suppliedCounts ?? recoverCounts(declaredValues, bins, normalization, inRange);
  const values = normalizedHistogramValues(counts, bins, normalization);
  const connectionCount = Number(
    isDelay ? data.consideredConnectionCount ?? observationCount : data.sourceConnectionCount ?? observationCount
  );
  return {
    id: "all",
    counts,
    values,
    connectionCount,
    observationCount,
    binnedObservationCount: counts.reduce((sum, count) => sum + count, 0),
    underRangeCount,
    overRangeCount,
    missingMeasurementCount: isDelay ? 0 : Number(data.missingWeightCount ?? 0),
    missingObservationCount: isDelay ? 0 : Number(data.missingObservationCount ?? 0),
    zeroObservationCount: isDelay ? 0 : Number(data.zeroWeightCount ?? 0)
  };
}
function delayGroups(data, parameters, bins) {
  const connections = record(data.connections);
  const delays = record(connections.delays);
  const sourceUnit = String(delays.unit);
  const sourceIds = strings(connections.sourceIds);
  const targetIds = strings(connections.targetIds);
  const sourceValues = numbers(delays.values);
  const models = strings(connections.synapseModels);
  const groupByModel = data.groupBy === "synapse_model";
  const groupIds = groupByModel ? [...new Set(models)].sort(compareIdentifiers) : ["all"];
  const observations = new Map(groupIds.map((id) => [id, []]));
  const rows = new Map(groupIds.map((id) => [id, 0]));
  if (parameters.countingPolicy === "per_connection") {
    for (let index = 0; index < sourceValues.length; index++) {
      const group = groupByModel ? models[index] : "all";
      rows.set(group, rows.get(group) + 1);
      observations.get(group).push(converted(sourceValues[index], sourceUnit, bins.unit));
    }
  } else {
    const pairs = /* @__PURE__ */ new Map();
    for (let index = 0; index < sourceValues.length; index++) {
      const group = groupByModel ? models[index] : "all";
      rows.set(group, rows.get(group) + 1);
      const key = tupleKey([group, sourceIds[index], targetIds[index]]);
      const existing = pairs.get(key);
      const value = converted(sourceValues[index], sourceUnit, bins.unit);
      if (existing) existing.values.push(value);
      else pairs.set(key, { group, values: [value] });
    }
    for (const key of [...pairs.keys()].sort(compareIdentifiers)) {
      const pair = pairs.get(key);
      observations.get(pair.group).push(aggregate2(
        pair.values,
        String(parameters.multapseAggregation)
      ));
    }
  }
  return {
    sourceUnit,
    raw: sourceValues,
    groups: groupIds.map((id) => histogramGroup(
      id,
      observations.get(id),
      bins,
      String(parameters.normalization),
      rows.get(id),
      0,
      0,
      0,
      8
    ))
  };
}
function delayModel(requestValue) {
  const request = record(requestValue);
  const data = record(request.data);
  const parameters = record(request.parameters);
  const bins = binsFrom(parameters.bins);
  let groups;
  let sourceUnit = bins.unit;
  let raw = [];
  if (data.mode === "prebinned") {
    groups = [prebinnedGroup(data, bins, String(parameters.normalization), true)];
  } else {
    const derived = delayGroups(data, parameters, bins);
    groups = derived.groups;
    sourceUnit = derived.sourceUnit;
    raw = derived.raw;
  }
  const normalization = String(parameters.normalization);
  const observationKind = parameters.countingPolicy === "per_ordered_pair" ? "ordered_pair" : "connection";
  const scopeSummary = distributionScopeSummaryCell(data.scope);
  const densityUnit = reciprocalUnit(bins.unit) ?? null;
  const rows = [];
  for (const group of groups) {
    for (let index = 0; index < group.counts.length; index++) {
      rows.push([
        group.id,
        bins.edges[index],
        bins.edges[index + 1],
        sameUnitWidth(bins.edges[index], bins.edges[index + 1], bins.unit),
        bins.unit,
        group.counts[index],
        observationKind,
        normalization,
        scopeSummary,
        normalization === "probability" ? group.values[index] : null,
        normalization === "density" ? group.values[index] : null,
        normalization === "density" ? densityUnit : null,
        group.binnedObservationCount,
        group.connectionCount,
        group.underRangeCount,
        group.overRangeCount
      ]);
    }
  }
  const connectionCount = groups.reduce((sum, group) => sum + group.connectionCount, 0);
  const observationCount = groups.reduce((sum, group) => sum + group.observationCount, 0);
  const under = groups.reduce((sum, group) => sum + group.underRangeCount, 0);
  const over = groups.reduce((sum, group) => sum + group.overRangeCount, 0);
  const aggregated = Math.max(0, connectionCount - observationCount);
  const resolution = record(data.sourceResolution);
  const valueUnit = normalization === "density" ? String(densityUnit) : "1";
  const rawExtent = finiteExtrema(raw);
  const delayMin = rawExtent !== null ? exactText(rawExtent.min) : "not retained by pre-binned input";
  const delayMax = rawExtent !== null ? exactText(rawExtent.max) : "not retained by pre-binned input";
  return {
    rows,
    geometry: binCarriers(groups, bins),
    summary: {
      selectionLabel: String(parameters.selectionLabel ?? parameters.selectionId),
      consideredConnectionCount: exactText(connectionCount),
      countingPolicy: String(parameters.countingPolicy),
      pairAggregationStatement: parameters.countingPolicy === "per_ordered_pair" ? ` with ${String(parameters.multapseAggregation)} aggregation` : "",
      observationCount: exactText(observationCount),
      observationKind,
      groupCount: exactText(groups.length),
      groupByStatement: String(data.groupBy),
      scopeStatement: scopeSummary,
      delayMin,
      delayMax,
      delayUnit: sourceUnit,
      sourceResolution: `${exactText(Number(resolution.value))} ${String(resolution.unit)}`,
      binCount: exactText(bins.edges.length - 1),
      binMin: exactText(bins.edges[0]),
      binMax: exactText(bins.edges[bins.edges.length - 1]),
      binUnit: bins.unit,
      xScale: String(parameters.xScale ?? "linear"),
      underRangeCount: exactText(under),
      overRangeCount: exactText(over),
      normalization,
      valueUnit
    },
    disclosures: baseDisclosureFacts(request, {
      ...data.mode === "prebinned" ? { preBinned: true } : {},
      ...sourceUnit !== bins.unit ? { unitConversions: [conversionDisclosure("delay observations", sourceUnit, bins.unit)] } : {},
      ...aggregated > 0 ? {
        multapseAggregated: true,
        multapseAggregation: String(parameters.multapseAggregation)
      } : {}
    })
  };
}
DELAY_AUTHORITY = defineAuthorityEvaluator(
  authorityEvaluatorId("network.delay_distribution", 2),
  (request) => modelFields(delayModel(request))
);
function weightGroups(data, parameters, bins) {
  const connections = record(data.connections);
  const weights = record(connections.weights);
  const sourceUnit = String(weights.unit);
  const sourceIds = strings(connections.sourceIds);
  const targetIds = strings(connections.targetIds);
  const sourceValues = nullableNumbers(weights.values);
  const models = strings(connections.synapseModels);
  const grouped = parameters.grouping === "by_synapse_model";
  const groupIds = grouped ? [...new Set(models)].sort(compareIdentifiers) : ["all"];
  const observations = new Map(groupIds.map((id) => [id, []]));
  const connectionCounts = new Map(groupIds.map((id) => [id, 0]));
  const missingRows = new Map(groupIds.map((id) => [id, 0]));
  const missingObservations = new Map(groupIds.map((id) => [id, 0]));
  const zeros = new Map(groupIds.map((id) => [id, 0]));
  const transform = (value) => {
    const signed = parameters.signTreatment === "magnitude" ? Math.abs(value) : value;
    return converted(signed, sourceUnit, bins.unit);
  };
  if (parameters.observationUnit === "synapse") {
    for (let index = 0; index < sourceValues.length; index++) {
      const group = grouped ? models[index] : "all";
      connectionCounts.set(group, connectionCounts.get(group) + 1);
      const raw = sourceValues[index];
      if (raw === null) {
        missingRows.set(group, missingRows.get(group) + 1);
        missingObservations.set(group, missingObservations.get(group) + 1);
        continue;
      }
      const value = transform(raw);
      if (value === 0) zeros.set(group, zeros.get(group) + 1);
      observations.get(group).push(value);
    }
  } else {
    const pairs = /* @__PURE__ */ new Map();
    for (let index = 0; index < sourceValues.length; index++) {
      const group = grouped ? models[index] : "all";
      connectionCounts.set(group, connectionCounts.get(group) + 1);
      const key = tupleKey([group, sourceIds[index], targetIds[index]]);
      const pair = pairs.get(key) ?? { group, values: [], missing: false, connections: 0 };
      pair.connections++;
      const raw = sourceValues[index];
      if (raw === null) {
        pair.missing = true;
        missingRows.set(group, missingRows.get(group) + 1);
      } else {
        pair.values.push(raw);
      }
      pairs.set(key, pair);
    }
    for (const key of [...pairs.keys()].sort(compareIdentifiers)) {
      const pair = pairs.get(key);
      if (pair.missing) {
        missingObservations.set(pair.group, missingObservations.get(pair.group) + 1);
        continue;
      }
      const value = transform(aggregate2(pair.values, String(parameters.aggregation)));
      if (value === 0) zeros.set(pair.group, zeros.get(pair.group) + 1);
      observations.get(pair.group).push(value);
    }
  }
  return {
    sourceUnit,
    models,
    groups: groupIds.map((id) => histogramGroup(
      id,
      observations.get(id),
      bins,
      String(parameters.normalization),
      connectionCounts.get(id),
      missingRows.get(id),
      missingObservations.get(id),
      zeros.get(id)
    ))
  };
}
function weightModel(requestValue) {
  const request = record(requestValue);
  const data = record(request.data);
  const parameters = record(request.parameters);
  const bins = data.mode === "prebinned" ? binsFrom(data.binEdges) : binsFrom(parameters.bins);
  let groups;
  let sourceUnit = bins.unit;
  let models;
  if (data.mode === "prebinned") {
    groups = [prebinnedGroup(data, bins, String(parameters.normalization), false)];
    models = strings(data.contributingSynapseModels);
  } else {
    const derived = weightGroups(data, parameters, bins);
    groups = derived.groups;
    sourceUnit = derived.sourceUnit;
    models = derived.models;
  }
  const normalization = String(parameters.normalization);
  const densityUnit = reciprocalUnit(bins.unit) ?? null;
  const rows = [];
  for (const group of groups) {
    for (let index = 0; index < group.counts.length; index++) {
      rows.push([
        group.id,
        bins.edges[index],
        bins.edges[index + 1],
        sameUnitWidth(bins.edges[index], bins.edges[index + 1], bins.unit),
        bins.edges[index + 1] <= 0 ? "negative" : "non_negative",
        group.counts[index],
        group.values[index],
        normalization === "density" ? densityUnit : "1",
        group.binnedObservationCount,
        group.missingObservationCount,
        group.connectionCount
      ]);
    }
  }
  const connectionCount = groups.reduce((sum, group) => sum + group.connectionCount, 0);
  const binned = groups.reduce((sum, group) => sum + group.binnedObservationCount, 0);
  const missingMeasurements = groups.reduce((sum, group) => sum + group.missingMeasurementCount, 0);
  const missingObservations = groups.reduce((sum, group) => sum + group.missingObservationCount, 0);
  const zeros = groups.reduce((sum, group) => sum + group.zeroObservationCount, 0);
  const under = groups.reduce((sum, group) => sum + group.underRangeCount, 0);
  const over = groups.reduce((sum, group) => sum + group.overRangeCount, 0);
  const aggregated = Math.max(0, connectionCount - missingObservations - groups.reduce((sum, group) => sum + group.observationCount, 0));
  const sourceUniverse = strings(record(data.sourceUniverse).ids);
  const targetUniverse = strings(record(data.targetUniverse).ids);
  const comparability = structuredCell(parameters.weightComparability);
  const zeroEdgeStatement = bins.edges.includes(0) ? "Zero is an explicit bin edge." : "Zero is not an explicit bin edge.";
  return {
    rows,
    geometry: binCarriers(groups, bins),
    summary: {
      selectionLabel: String(parameters.selectionLabel ?? parameters.selectionId),
      inRangeObservationCount: exactText(binned),
      observationUnit: String(parameters.observationUnit),
      sourceConnectionCount: exactText(connectionCount),
      sourceNodeCount: exactText(sourceUniverse.length),
      targetNodeCount: exactText(targetUniverse.length),
      scopeStatement: distributionScopeSummaryCell(data.scope),
      synapseModels: [...new Set(models)].sort(compareIdentifiers).join(", "),
      weightComparability: comparability,
      signTreatment: String(parameters.signTreatment),
      weightUnit: sourceUnit,
      missingWeightCount: exactText(missingMeasurements),
      missingObservationCount: exactText(missingObservations),
      zeroWeightCount: exactText(zeros),
      binCount: exactText(bins.edges.length - 1),
      binMin: exactText(bins.edges[0]),
      binMax: exactText(bins.edges[bins.edges.length - 1]),
      binUnit: bins.unit,
      xScale: String(parameters.xScale ?? "linear"),
      zeroEdgeStatement,
      underRangeCount: exactText(under),
      overRangeCount: exactText(over),
      normalization,
      valueUnit: normalization === "density" ? String(densityUnit) : "1"
    },
    disclosures: baseDisclosureFacts(request, {
      ...data.mode === "prebinned" ? { preBinned: true } : {},
      missingValueCount: missingMeasurements,
      ...sourceUnit !== bins.unit ? { unitConversions: [conversionDisclosure("weight observations", sourceUnit, bins.unit)] } : {},
      ...aggregated > 0 ? {
        multapseAggregated: true,
        multapseAggregation: String(parameters.aggregation)
      } : {}
    })
  };
}
WEIGHT_AUTHORITY = defineAuthorityEvaluator(
  authorityEvaluatorId("network.weight_distribution", 2),
  (request) => modelFields(weightModel(request))
);
function isiTrainKey(sender, trial) {
  return tupleKey(trial === void 0 ? ["sender", sender] : ["sender-trial", sender, trial]);
}
function declaredIsiTrainKeys(senders, trials) {
  return trials ? senders.flatMap((sender) => trials.map((trial) => isiTrainKey(sender, trial))) : senders.map((sender) => isiTrainKey(sender, void 0));
}
function isiFromEvents(data) {
  const eventTimes = record(data.eventTimes);
  const unit = String(eventTimes.unit);
  const times = numbers(eventTimes.values);
  const senders = strings(data.eventSenderIds);
  const eventTrials = Array.isArray(data.eventTrialIds) ? strings(data.eventTrialIds) : void 0;
  const recorded = strings(data.recordedSenderIds);
  const trials = Array.isArray(data.trialIds) ? strings(data.trialIds) : void 0;
  const keys = declaredIsiTrainKeys(recorded, trials);
  const byTrain = new Map(keys.map((key) => [key, []]));
  for (let index = 0; index < times.length; index++) {
    const key = isiTrainKey(senders[index], eventTrials?.[index]);
    const train = byTrain.get(key);
    if (!train) throw new Error("ISI event is outside the declared train universe");
    train.push(times[index]);
  }
  const intervals = [];
  let trainsWithoutIntervalCount = 0;
  let zeroIntervalCount = 0;
  for (const key of keys) {
    const train = byTrain.get(key).sort((left, right) => left - right);
    if (train.length < 2) trainsWithoutIntervalCount++;
    for (let index = 1; index < train.length; index++) {
      const interval = exactBinary64Sum([train[index], -train[index - 1]]);
      if (interval === 0) zeroIntervalCount++;
      intervals.push(interval);
    }
  }
  return {
    intervals,
    intervalUnit: unit,
    intervalCount: intervals.length,
    spikeCount: times.length,
    trainCount: keys.length,
    trainsWithoutIntervalCount,
    zeroIntervalCount,
    excludedOutOfWindowCount: 0
  };
}
function isiFromIntervals(data) {
  const intervalRecord = record(data.intervals);
  const intervals = numbers(intervalRecord.values);
  const senders = strings(data.recordedSenderIds);
  const trials = Array.isArray(data.trialIds) ? strings(data.trialIds) : void 0;
  const keys = declaredIsiTrainKeys(senders, trials);
  const table = array(data.trains).map(record);
  let spikeCount = 0;
  let trainsWithoutIntervalCount = 0;
  for (const train of table) {
    const count = Number(train.spikeCount);
    spikeCount += count;
    if (count < 2) trainsWithoutIntervalCount++;
  }
  return {
    intervals,
    intervalUnit: String(intervalRecord.unit),
    intervalCount: intervals.length,
    spikeCount,
    trainCount: keys.length,
    trainsWithoutIntervalCount,
    zeroIntervalCount: intervals.filter((value) => value === 0).length,
    excludedOutOfWindowCount: null
  };
}
function isiModel(requestValue) {
  const request = record(requestValue);
  const data = record(request.data);
  const parameters = record(request.parameters);
  const bins = binsFrom(parameters.bins);
  const facts = data.mode === "intervals" ? isiFromIntervals(data) : isiFromEvents(data);
  const normalization = String(parameters.normalization);
  const counts = new Array(bins.edges.length - 1).fill(0);
  let under = 0;
  let over = 0;
  for (const interval of facts.intervals) {
    const index = ordinaryBinIndex(interval, facts.intervalUnit, bins);
    if (index >= 0) counts[index]++;
    else if (converted(interval, facts.intervalUnit, bins.unit) < bins.edges[0]) under++;
    else over++;
  }
  const values = normalizedHistogramValues(counts, bins, normalization);
  const binned = counts.reduce((sum, count) => sum + count, 0);
  const densityUnit = reciprocalUnit(bins.unit) ?? null;
  const rows = counts.map((count, index) => [
    bins.edges[index],
    bins.edges[index + 1],
    sameUnitWidth(bins.edges[index], bins.edges[index + 1], bins.unit),
    bins.unit,
    count,
    normalization === "probability" ? values[index] : null,
    normalization === "density" ? values[index] : null,
    normalization === "density" ? densityUnit : null,
    binned,
    facts.intervalCount,
    under,
    over,
    facts.trainCount,
    facts.spikeCount,
    facts.excludedOutOfWindowCount
  ]);
  const window = record(data.window);
  const duration = convertDifference(
    Number(window.start),
    Number(window.stop),
    String(window.unit),
    String(window.unit)
  );
  const senderCount = strings(data.recordedSenderIds).length;
  const trialCount = Array.isArray(data.trialIds) ? strings(data.trialIds).length : 1;
  return {
    rows,
    geometry: binCarriers([{ id: "all", counts }], bins),
    summary: {
      selectionLabel: String(parameters.selectionLabel ?? parameters.selectionId),
      intervalCount: exactText(facts.intervalCount),
      trainCount: exactText(facts.trainCount),
      senderCount: exactText(senderCount),
      trialCount: exactText(trialCount),
      spikeCount: exactText(facts.spikeCount),
      windowStart: exactText(Number(window.start)),
      windowStop: exactText(Number(window.stop)),
      timeUnit: String(window.unit),
      trainsWithoutIntervalCount: exactText(facts.trainsWithoutIntervalCount),
      excludedOutOfWindowCount: facts.excludedOutOfWindowCount === null ? "not retained by interval input" : exactText(facts.excludedOutOfWindowCount),
      binCount: exactText(counts.length),
      binMin: exactText(bins.edges[0]),
      binMax: exactText(bins.edges[bins.edges.length - 1]),
      intervalUnit: bins.unit,
      xScale: String(parameters.xScale ?? "linear"),
      underRangeCount: exactText(under),
      overRangeCount: exactText(over),
      normalization,
      valueUnit: normalization === "density" ? String(densityUnit) : "1",
      windowDuration: exactText(duration),
      zeroIntervalStatement: facts.zeroIntervalCount > 0 ? `${facts.zeroIntervalCount} exact zero intervals were retained by the declared policy.` : "No exact zero interval was observed."
    },
    disclosures: baseDisclosureFacts(request, {
      ...facts.intervalUnit !== bins.unit ? {
        unitConversions: [conversionDisclosure(
          "inter-spike intervals",
          facts.intervalUnit,
          bins.unit
        )]
      } : {}
    })
  };
}
ISI_AUTHORITY = defineAuthorityEvaluator(
  authorityEvaluatorId("neuro.isi_distribution", 2),
  (request) => modelFields(isiModel(request))
);
function correlogramAxis(parameters) {
  const range = record(parameters.lagRange);
  const declaration = record(parameters.bins);
  const lagUnit = String(range.unit);
  const typedWidthUnit = String(declaration.unit);
  const typedWidth = Number(declaration.width);
  const widthInLagUnit = converted(typedWidth, typedWidthUnit, lagUnit);
  const materialized = materializeCenteredLagBins(
    Number(range.min),
    Number(range.max),
    widthInLagUnit,
    20001
  );
  if (!materialized.ok) throw new Error("cannot independently materialize lag ladder");
  const edges = materialized.edges;
  const centers = edges.slice(0, -1).map((lower, index) => lower + (edges[index + 1] - lower) / 2);
  return {
    bins: { edges, unit: lagUnit, finalEdgeInclusive: false },
    centers,
    typedWidth,
    typedWidthUnit,
    widthInLagUnit,
    lagUnit,
    conversions: typedWidthUnit === lagUnit ? [] : [conversionDisclosure("correlogram bin width", typedWidthUnit, lagUnit)]
  };
}
function eligibleReferenceCounts(referenceTimes, referenceUnit, bins, window) {
  const binCount = bins.edges.length - 1;
  const changes = new Array(binCount + 1).fill(0);
  const firstTrue = (predicate) => {
    let low = 0;
    let high = bins.edges.length;
    while (low < high) {
      const middle = low + Math.floor((high - low) / 2);
      if (predicate(bins.edges[middle])) high = middle;
      else low = middle + 1;
    }
    return low;
  };
  const openStart = window.boundary === "(start,stop]";
  for (const time of referenceTimes) {
    const firstLower = firstTrue((edge) => {
      const comparison = compareExactUnitSumToValue(
        [
          { value: time, unit: referenceUnit },
          { value: edge, unit: bins.unit }
        ],
        { value: Number(window.start), unit: String(window.unit) }
      );
      return openStart ? comparison > 0 : comparison >= 0;
    });
    const firstUpperBeyond = firstTrue((edge) => compareExactUnitSumToValue(
      [
        { value: time, unit: referenceUnit },
        { value: edge, unit: bins.unit }
      ],
      { value: Number(window.stop), unit: String(window.unit) }
    ) > 0);
    const lower = Math.min(firstLower, binCount);
    const upper = Math.max(0, Math.min(firstUpperBeyond - 1, binCount));
    if (lower < upper) {
      changes[lower]++;
      changes[upper]--;
    }
  }
  let active = 0;
  return changes.slice(0, binCount).map((change) => {
    active += change;
    return active;
  });
}
function correlogramModel(requestValue) {
  const request = record(requestValue);
  const data = record(request.data);
  const parameters = record(request.parameters);
  const axis = correlogramAxis(parameters);
  const mode = String(data.mode);
  const preBinned = mode.startsWith("prebinned_");
  const kind = mode.endsWith("_auto") ? "auto" : "cross";
  const referenceTrain = record(kind === "auto" ? data.train : data.referenceTrain);
  const targetTrain = kind === "auto" ? referenceTrain : record(data.targetTrain);
  const referenceSourceTimes = numbers(record(referenceTrain.eventTimes).values);
  const targetSourceTimes = kind === "auto" ? referenceSourceTimes : numbers(record(targetTrain.eventTimes).values);
  const referenceSourceUnit = preBinned ? axis.lagUnit : String(record(referenceTrain.eventTimes).unit);
  const targetSourceUnit = preBinned ? axis.lagUnit : String(record(targetTrain.eventTimes).unit);
  const referenceTimes = preBinned ? [] : referenceSourceTimes.map((value) => converted(value, referenceSourceUnit, axis.lagUnit));
  const targetTimes = preBinned ? [] : kind === "auto" ? referenceTimes : targetSourceTimes.map((value) => converted(value, targetSourceUnit, axis.lagUnit));
  let pairCounts;
  let referenceEventCount;
  let targetEventCount;
  if (preBinned) {
    pairCounts = numbers(data.pairCounts);
    referenceEventCount = Number(data.referenceEventCount);
    targetEventCount = kind === "auto" ? referenceEventCount : Number(data.targetEventCount);
  } else {
    const sortedReference = [...referenceTimes].sort((left, right) => left - right);
    const sortedTarget = kind === "auto" ? sortedReference : [...targetTimes].sort((left, right) => left - right);
    pairCounts = new Array(axis.centers.length).fill(0);
    const minimumLag = axis.bins.edges[0];
    const maximumLag = axis.bins.edges[axis.bins.edges.length - 1];
    let firstEligibleTarget = 0;
    let firstTargetPastWindow = 0;
    for (let referenceIndex = 0; referenceIndex < sortedReference.length; referenceIndex++) {
      const referenceTime = sortedReference[referenceIndex];
      while (firstEligibleTarget < sortedTarget.length && sortedTarget[firstEligibleTarget] - referenceTime < minimumLag) {
        firstEligibleTarget++;
      }
      if (firstTargetPastWindow < firstEligibleTarget) {
        firstTargetPastWindow = firstEligibleTarget;
      }
      while (firstTargetPastWindow < sortedTarget.length && sortedTarget[firstTargetPastWindow] - referenceTime < maximumLag) {
        firstTargetPastWindow++;
      }
      for (let targetIndex = firstEligibleTarget; targetIndex < firstTargetPastWindow; targetIndex++) {
        if (kind === "auto" && referenceIndex === targetIndex) continue;
        const lag = sortedTarget[targetIndex] - referenceTime;
        const index = ordinaryBinIndex(lag, axis.lagUnit, axis.bins);
        if (index >= 0) pairCounts[index]++;
      }
    }
    referenceEventCount = referenceTimes.length;
    targetEventCount = kind === "auto" ? referenceEventCount : targetTimes.length;
  }
  const countedPairCount = pairCounts.reduce((sum, count) => sum + count, 0);
  const candidatePairCount = referenceEventCount * targetEventCount;
  const sameEventSelfPairCountExcluded = kind === "auto" ? referenceEventCount : 0;
  const outOfRangePairCount = candidatePairCount - countedPairCount - sameEventSelfPairCountExcluded;
  const statistic = String(parameters.statistic);
  const edgeCorrection = String(parameters.edgeCorrection);
  let eligible;
  let denominators;
  let values;
  let statuses;
  if (statistic === "raw_pair_count") {
    eligible = pairCounts.map(() => null);
    denominators = pairCounts.map(() => null);
    values = [...pairCounts];
    statuses = pairCounts.map(() => "defined");
  } else {
    const exactEligible = edgeCorrection === "none" ? pairCounts.map(() => referenceEventCount) : preBinned ? numbers(data.eligibleReferenceEventCounts) : eligibleReferenceCounts(
      referenceSourceTimes,
      referenceSourceUnit,
      axis.bins,
      record(data.window)
    );
    const widthSeconds = convertDifference(
      0,
      axis.typedWidth,
      axis.typedWidthUnit,
      "s"
    );
    eligible = exactEligible;
    denominators = exactEligible.map((count) => count * widthSeconds);
    values = pairCounts.map((count, index) => exactEligible[index] === 0 ? null : deriveExactCountRateInUnit(
      count,
      exactEligible[index],
      0,
      axis.typedWidth,
      axis.typedWidthUnit,
      "Hz"
    ));
    statuses = exactEligible.map((count) => count === 0 ? "undefined_zero_eligible_reference_events" : "defined");
  }
  const rows = pairCounts.map((pairCount, index) => [
    axis.bins.edges[index],
    axis.centers[index],
    axis.bins.edges[index + 1],
    pairCount,
    eligible[index],
    denominators[index],
    values[index],
    statistic === "raw_pair_count" ? "1" : "Hz",
    statuses[index],
    null,
    null
  ]);
  const geometry = values.flatMap((value, binIndex2) => value === null ? [] : [carrier("bins", {
    binIndex: binIndex2,
    lagBinStart: axis.bins.edges[binIndex2],
    lagBinCenter: axis.centers[binIndex2],
    lagBinEnd: axis.bins.edges[binIndex2 + 1]
  })]);
  const undefinedCount = values.filter((value) => value === null).length;
  const window = record(data.window);
  const duration = convertDifference(
    Number(window.start),
    Number(window.stop),
    String(window.unit),
    String(window.unit)
  );
  const conversions = [...axis.conversions];
  if (!preBinned && referenceSourceUnit !== axis.lagUnit) {
    conversions.push(conversionDisclosure(
      "correlogram reference-event times",
      referenceSourceUnit,
      axis.lagUnit
    ));
  }
  if (!preBinned && kind === "cross" && targetSourceUnit !== axis.lagUnit) {
    conversions.push(conversionDisclosure(
      "correlogram target-event times",
      targetSourceUnit,
      axis.lagUnit
    ));
  }
  if (preBinned) {
    const edgeUnit = String(record(data.binEdges).unit);
    if (edgeUnit !== axis.lagUnit) {
      conversions.push(conversionDisclosure(
        "pre-binned correlogram lag edges",
        edgeUnit,
        axis.lagUnit
      ));
    }
  }
  const denominatorStatement = statistic === "raw_pair_count" ? "none" : edgeCorrection === "none" ? "reference event count multiplied by the typed bin width in seconds" : "the per-bin eligible-reference count multiplied by the typed bin width in seconds; zero exposure yields null";
  const sourceAuthorityStatement = preBinned ? "Source event counts and exact pair numerators were declared by the pre-binned product; duration was derived from its window" : "Source event counts were derived from the explicit event arrays, and duration was derived from their shared window";
  return {
    rows,
    geometry,
    summary: {
      correlationKind: kind,
      targetLabel: String(targetTrain.label),
      referenceLabel: String(referenceTrain.label),
      referenceRecordedSenderCount: exactText(strings(referenceTrain.recordedSenderIds).length),
      targetRecordedSenderCount: exactText(strings(targetTrain.recordedSenderIds).length),
      binCount: exactText(pairCounts.length),
      binWidth: exactText(axis.widthInLagUnit),
      lagUnit: axis.lagUnit,
      lagMin: exactText(axis.centers[0]),
      lagMax: exactText(axis.centers[axis.centers.length - 1]),
      statistic,
      valueUnit: statistic === "raw_pair_count" ? "1" : "Hz",
      denominatorStatement,
      referenceEventCount: exactText(referenceEventCount),
      targetEventCount: exactText(targetEventCount),
      observationDuration: exactText(duration),
      timeUnit: String(window.unit),
      sourceAuthorityStatement,
      candidatePairCount: exactText(candidatePairCount),
      countedPairCount: exactText(countedPairCount),
      outOfRangePairCount: exactText(outOfRangePairCount),
      sameEventSelfPairCountExcluded: exactText(sameEventSelfPairCountExcluded),
      undefinedRateBinCount: exactText(undefinedCount),
      uncertaintyStatement: "No uncertainty interval is estimated or drawn."
    },
    disclosures: baseDisclosureFacts(request, {
      ...preBinned ? { preBinned: true } : {},
      ...conversions.length > 0 ? { unitConversions: conversions } : {},
      ...undefinedCount > 0 ? { missingValueCount: undefinedCount } : {}
    })
  };
}
CORRELOGRAM_AUTHORITY = defineAuthorityEvaluator(
  authorityEvaluatorId("neuro.correlogram", 2),
  (request) => modelFields(correlogramModel(request))
);
function psthNormalizedValues(counts, denominators, bins, selectedSenderCount, normalization, valueUnit) {
  return counts.map((count, index) => {
    const denominator = denominators[index];
    if (count === null || denominator === null) return null;
    if (normalization === "count") return count;
    if (normalization === "count_per_trial") {
      return exactRationalToBinary64(BigInt(count), BigInt(denominator));
    }
    const factors = normalization === "mean_rate_per_selected_sender_per_trial" ? [denominator, selectedSenderCount] : [denominator];
    return deriveExactCountRateWithIntegerFactorsInUnit(
      count,
      factors,
      bins.edges[index],
      bins.edges[index + 1],
      bins.unit,
      valueUnit
    );
  });
}
function psthBaseline(parameters, relativeWindow, bins, counts, denominators, selectedSenderCount, normalization, valueUnit) {
  const baseline = record(parameters.baseline);
  if (Object.keys(baseline).length === 0) {
    return {
      rate: null,
      corrected: counts.map(() => null),
      start: null,
      stop: null,
      unit: null
    };
  }
  const windowUnit = String(relativeWindow.unit);
  const start = Number(baseline.start);
  const stop = Number(baseline.stop);
  const startInBins = converted(start, windowUnit, bins.unit);
  const stopInBins = converted(stop, windowUnit, bins.unit);
  const startIndex = bins.edges.findIndex((edge) => Object.is(edge, startInBins));
  const stopIndex = bins.edges.findIndex((edge) => Object.is(edge, stopInBins));
  if (startIndex < 0 || stopIndex <= startIndex) {
    throw new Error("PSTH baseline does not identify exact bin edges");
  }
  let baselineCount = 0n;
  const intervals = [];
  for (let index = startIndex; index < stopIndex; index++) {
    const count = counts[index];
    const denominator = denominators[index];
    if (count === null || denominator === null) continue;
    baselineCount += BigInt(count);
    intervals.push({
      lower: bins.edges[index],
      upper: bins.edges[index + 1],
      integerWeight: denominator
    });
  }
  const aggregateFactors = normalization === "mean_rate_per_selected_sender_per_trial" ? [selectedSenderCount] : [];
  const rate = deriveExactAggregateCountRateOverIntervalsInUnit(
    baselineCount,
    aggregateFactors,
    intervals,
    bins.unit,
    valueUnit
  );
  const corrected = counts.map((count, index) => {
    const denominator = denominators[index];
    if (count === null || denominator === null) return null;
    const factors = normalization === "mean_rate_per_selected_sender_per_trial" ? [denominator, selectedSenderCount] : [denominator];
    return deriveExactCountRateMinusAggregateRateOverIntervalsInUnit(
      count,
      factors,
      bins.edges[index],
      bins.edges[index + 1],
      baselineCount,
      aggregateFactors,
      intervals,
      bins.unit,
      valueUnit
    );
  });
  return { rate, corrected, start, stop, unit: windowUnit };
}
function psthIndependent(data, parameters) {
  const bins = binsFrom(parameters.bins);
  const relativeWindow = record(data.relativeWindow);
  const normalization = String(parameters.normalization);
  let counts;
  let denominators;
  let selectedSenderCount;
  let includedTrialCount;
  let excludedTrialCount;
  let excludedOutOfWindowCount;
  if (data.mode === "prebinned") {
    counts = nullableNumbers(data.counts);
    denominators = nullableNumbers(data.trialDenominators);
    selectedSenderCount = Number(data.recordedSenderCount);
    includedTrialCount = Number(data.includedTrialCount);
    excludedTrialCount = Number(data.excludedTrialCount);
    excludedOutOfWindowCount = null;
  } else {
    const eventTimes = record(data.eventTimes);
    const sourceTimes = numbers(eventTimes.values);
    const senders = strings(data.eventSenderIds);
    const eventTrials = strings(data.eventTrialIds);
    const recorded = new Set(strings(data.recordedSenderIds));
    const trialIds = strings(data.trialIds);
    const alignments = numbers(data.alignmentTimes);
    const alignmentByTrial = new Map(trialIds.map((trial, index) => [trial, alignments[index]]));
    counts = new Array(bins.edges.length - 1).fill(0);
    const windowStart = converted(Number(relativeWindow.start), String(relativeWindow.unit), bins.unit);
    const windowStop = converted(Number(relativeWindow.stop), String(relativeWindow.unit), bins.unit);
    const closedStop = relativeWindow.boundary === "[start,stop]";
    excludedOutOfWindowCount = 0;
    for (let index = 0; index < sourceTimes.length; index++) {
      const alignment = alignmentByTrial.get(eventTrials[index]);
      if (!recorded.has(senders[index]) || alignment === void 0) {
        throw new Error("PSTH event does not bind to the selected sender/trial universes");
      }
      const relative = convertExactUnitSum(
        [
          { value: sourceTimes[index], unit: String(eventTimes.unit) },
          { value: -alignment, unit: String(data.alignmentUnit) }
        ],
        bins.unit
      );
      if (relative < windowStart || (closedStop ? relative > windowStop : relative >= windowStop)) {
        excludedOutOfWindowCount++;
        continue;
      }
      const bin = ordinaryBinIndex(relative, bins.unit, bins);
      if (bin < 0) throw new Error("PSTH in-window event has no authoritative bin");
      counts[bin] = (counts[bin] ?? 0) + 1;
    }
    denominators = counts.map(() => trialIds.length);
    selectedSenderCount = recorded.size;
    includedTrialCount = trialIds.length;
    excludedTrialCount = 0;
  }
  const supplied = record(data.rates);
  const rateNormalization = normalization === "total_event_rate_per_trial" || normalization === "mean_rate_per_selected_sender_per_trial";
  const valueUnit = rateNormalization ? typeof supplied.unit === "string" ? supplied.unit : "Hz" : "1";
  const values = psthNormalizedValues(
    counts,
    denominators,
    bins,
    selectedSenderCount,
    normalization,
    valueUnit
  );
  const baseline = psthBaseline(
    parameters,
    relativeWindow,
    bins,
    counts,
    denominators,
    selectedSenderCount,
    normalization,
    valueUnit
  );
  const pairs = [];
  if (String(relativeWindow.unit) !== bins.unit) pairs.push([String(relativeWindow.unit), bins.unit]);
  if (data.mode === "events") {
    const eventUnit = String(record(data.eventTimes).unit);
    const alignmentUnit = String(data.alignmentUnit);
    if (eventUnit !== bins.unit) pairs.push([eventUnit, bins.unit]);
    if (alignmentUnit !== bins.unit) pairs.push([alignmentUnit, bins.unit]);
  }
  if (rateNormalization) {
    if (bins.unit !== "s") pairs.push([bins.unit, "s"]);
    if (valueUnit !== "Hz") pairs.push(["Hz", valueUnit]);
  }
  const conversions = [...new Map(pairs.map((pair) => [`${pair[0]}\0${pair[1]}`, pair])).values()].map(([from, to]) => `${from} -> ${to}`);
  return {
    bins,
    counts,
    denominators,
    values,
    displayValues: baseline.rate === null ? values : baseline.corrected,
    corrected: baseline.corrected,
    valueUnit,
    baselineRate: baseline.rate,
    baselineStart: baseline.start,
    baselineStop: baseline.stop,
    baselineUnit: baseline.unit,
    selectedSenderCount,
    includedTrialCount,
    excludedTrialCount,
    excludedOutOfWindowCount,
    conversions
  };
}
function psthModel(requestValue) {
  const request = record(requestValue);
  const data = record(request.data);
  const parameters = record(request.parameters);
  const result = psthIndependent(data, parameters);
  const relativeWindow = record(data.relativeWindow);
  const normalization = String(parameters.normalization);
  const denominatorPolicy = String(parameters.denominatorPolicy);
  const widths = result.counts.map((_count, index) => sameUnitWidth(
    result.bins.edges[index],
    result.bins.edges[index + 1],
    result.bins.unit
  ));
  const rows = result.counts.map((count, index) => [
    String(parameters.seriesId),
    String(parameters.seriesLabel ?? parameters.seriesId),
    String(parameters.alignmentLabel ?? "alignment"),
    result.bins.edges[index],
    result.bins.edges[index + 1],
    widths[index],
    result.bins.unit,
    Number(relativeWindow.start),
    Number(relativeWindow.stop),
    String(relativeWindow.unit),
    String(relativeWindow.boundary),
    String(record(parameters.bins).boundary),
    result.bins.finalEdgeInclusive ? "true" : "false",
    count,
    result.denominators[index],
    result.includedTrialCount,
    result.excludedTrialCount,
    result.excludedOutOfWindowCount,
    result.selectedSenderCount,
    normalization,
    denominatorPolicy,
    parameters.senderExposurePolicy === void 0 ? null : String(parameters.senderExposurePolicy),
    result.values[index],
    result.valueUnit,
    result.baselineRate === null ? null : result.corrected[index],
    result.baselineRate === null ? null : "subtract_mean_rate",
    result.baselineRate,
    result.baselineStart,
    result.baselineStop,
    result.baselineUnit
  ]);
  const geometry = result.displayValues.flatMap((value, binIndex2) => value === null ? [] : [carrier("bins", {
    binIndex: binIndex2,
    binStart: result.bins.edges[binIndex2],
    binEnd: result.bins.edges[binIndex2 + 1]
  })]);
  const finiteValues = result.values.filter((value) => value !== null);
  const countTotal = result.counts.reduce(
    (sum, count) => sum + (count === null ? 0n : BigInt(count)),
    0n
  );
  const missing = result.counts.filter((count) => count === null).length;
  const zeroOnAxis = result.bins.edges[0] <= 0 && 0 <= result.bins.edges[result.bins.edges.length - 1];
  const zeroIncluded = zeroOnAxis && (0 < result.bins.edges[result.bins.edges.length - 1] || result.bins.finalEdgeInclusive);
  const baselineStatement = result.baselineRate === null ? "No baseline subtraction was requested." : `Baseline ${exactText(result.baselineRate)} ${result.valueUnit} was subtracted from displayed values; the table retains both uncorrected and corrected values.`;
  const excludedEventStatement = result.excludedOutOfWindowCount === null ? "The pre-binned input does not retain an out-of-window event count." : `${result.excludedOutOfWindowCount} source events were excluded by the relative window.`;
  const valueExtent = finiteExtrema(finiteValues);
  return {
    rows,
    geometry,
    summary: {
      seriesLabel: String(parameters.seriesLabel ?? parameters.seriesId),
      alignmentLabel: String(parameters.alignmentLabel ?? "alignment"),
      windowStart: exactText(Number(relativeWindow.start)),
      windowStop: exactText(Number(relativeWindow.stop)),
      timeUnit: String(relativeWindow.unit),
      windowBoundary: String(relativeWindow.boundary),
      zeroVisibilityStatement: zeroIncluded ? "Relative time zero is included and shown as a reference." : zeroOnAxis ? "Relative time zero is an excluded stop-boundary coordinate shown only as a boundary reference." : "Relative time zero lies outside the displayed window and no zero reference is drawn.",
      binCount: exactText(result.counts.length),
      eventCount: exactText(countTotal),
      selectedSenderCount: exactText(result.selectedSenderCount),
      includedTrialCount: exactText(result.includedTrialCount),
      excludedTrialCount: exactText(result.excludedTrialCount),
      denominatorPolicy,
      normalization,
      valueMin: valueExtent === null ? "null" : exactText(valueExtent.min),
      valueMax: valueExtent === null ? "null" : exactText(valueExtent.max),
      valueUnit: result.valueUnit,
      baselineStatement,
      missingBinStatement: missing > 0 ? missing === 1 ? "1 bin has no covering trial and remains missing, not zero." : `${missing} bins have no covering trial and remain missing, not zero.` : "Every bin has positive declared trial exposure.",
      excludedEventStatement,
      uncertaintyStatement: "No uncertainty interval was supplied or rendered."
    },
    disclosures: baseDisclosureFacts(request, {
      ...data.mode === "prebinned" ? { preBinned: true } : {},
      ...missing > 0 ? { missingValueCount: missing } : {},
      ...result.excludedOutOfWindowCount !== null ? { excludedOutOfWindow: result.excludedOutOfWindowCount } : {},
      ...result.conversions.length > 0 ? { unitConversions: result.conversions } : {},
      ...normalization === "mean_rate_per_selected_sender_per_trial" ? { rectangularSenderExposureAsserted: true } : {}
    })
  };
}
PSTH_AUTHORITY = defineAuthorityEvaluator(
  authorityEvaluatorId("neuro.psth", 2),
  (request) => modelFields(psthModel(request))
);
var DISTRIBUTION_AUTHORITY_EVALUATORS = [
  DEGREE_AUTHORITY,
  DELAY_AUTHORITY,
  WEIGHT_AUTHORITY,
  ISI_AUTHORITY,
  CORRELOGRAM_AUTHORITY,
  POPULATION_RATE_AUTHORITY,
  PSTH_AUTHORITY,
  RASTER_AUTHORITY
];

// src/authority/evaluators/implementation-ids.ts
var OUTPUT_AUTHORITY_IMPLEMENTATION_IDS_V1 = Object.freeze([
  "network.adjacency_matrix.output_authority.v2",
  "network.connection_graph.output_authority.v2",
  "network.degree_distribution.output_authority.v2",
  "network.delay_distribution.output_authority.v2",
  "network.delay_matrix.output_authority.v2",
  "network.spatial_map_2d.output_authority.v2",
  "network.synaptic_weight_trace.output_authority.v2",
  "network.weight_distribution.output_authority.v2",
  "network.weight_matrix.output_authority.v2",
  "neuro.analog_trace.output_authority.v2",
  "neuro.compartment_trace.output_authority.v2",
  "neuro.correlogram.output_authority.v2",
  "neuro.isi_distribution.output_authority.v2",
  "neuro.multisignal_trace.output_authority.v2",
  "neuro.phase_plane.output_authority.v2",
  "neuro.population_rate.output_authority.v2",
  "neuro.psth.output_authority.v2",
  "neuro.response_curve.output_authority.v2",
  "neuro.spike_raster.output_authority.v2"
]);
var IMPLEMENTATION_ID = /^[a-z][a-z0-9_.-]*$/u;
var DANGEROUS_MAP_KEYS = /* @__PURE__ */ new Set(["__proto__", "constructor", "prototype"]);
function outputAuthorityImplementationInventoryProblemsV1(ids) {
  const problems = [];
  const seen = /* @__PURE__ */ new Set();
  let previous = null;
  ids.forEach((candidate, index) => {
    if (typeof candidate !== "string" || candidate.length === 0) {
      problems.push(`implementation id at index ${index} is not a non-empty string`);
      return;
    }
    if (!IMPLEMENTATION_ID.test(candidate) || DANGEROUS_MAP_KEYS.has(candidate)) {
      problems.push(`implementation id ${JSON.stringify(candidate)} is not a safe closed identifier`);
    }
    if (seen.has(candidate)) {
      problems.push(`duplicate implementation id ${JSON.stringify(candidate)}`);
    }
    if (previous !== null && !(previous < candidate)) {
      problems.push(
        `implementation ids are not strictly ascending at ${JSON.stringify(previous)} and ${JSON.stringify(candidate)}`
      );
    }
    seen.add(candidate);
    previous = candidate;
  });
  return Object.freeze(problems);
}

// src/authority/evaluators/matrices.ts
function record2(value) {
  return value !== null && value !== void 0 && typeof value === "object" && !Array.isArray(value) ? value : {};
}
function array2(value) {
  return Array.isArray(value) ? value : [];
}
function strings2(value) {
  return array2(value).map(String);
}
function nullableNumbers2(value) {
  return array2(value).map((entry) => entry === null ? null : Number(entry));
}
function numbers2(value) {
  return array2(value).map(Number);
}
function compareUnicodeCodePoints(left, right) {
  let leftIndex = 0;
  let rightIndex = 0;
  while (leftIndex < left.length && rightIndex < right.length) {
    const leftPoint = left.codePointAt(leftIndex);
    const rightPoint = right.codePointAt(rightIndex);
    if (leftPoint !== rightPoint) return leftPoint < rightPoint ? -1 : 1;
    leftIndex += leftPoint > 65535 ? 2 : 1;
    rightIndex += rightPoint > 65535 ? 2 : 1;
  }
  if (leftIndex === left.length && rightIndex === right.length) return 0;
  return leftIndex === left.length ? -1 : 1;
}
function canonicalStrings(values) {
  if (values.length === 0) return null;
  return [...new Set(values)].sort(compareUnicodeCodePoints);
}
function canonicalZero3(value) {
  return value === 0 ? 0 : value;
}
function summaryNumber(value) {
  return value === 0 ? "0" : String(value);
}
function structuredCell2(value) {
  return value === void 0 ? null : canonicalize(value);
}
function booleanCell(value) {
  return value ? "true" : "false";
}
function requestParts(value) {
  const request = record2(value);
  const data = record2(request.data);
  const connections = record2(data.connections);
  const edgeIds = Array.isArray(connections.edgeIds) ? strings2(connections.edgeIds) : void 0;
  const synapseModels = Array.isArray(connections.synapseModels) ? strings2(connections.synapseModels) : void 0;
  const observedTargetIds = Array.isArray(data.observedTargetIds) ? strings2(data.observedTargetIds) : void 0;
  return {
    request,
    data,
    parameters: record2(request.parameters),
    nodeIds: strings2(record2(data.nodeUniverse).ids),
    connections,
    sourceIds: strings2(connections.sourceIds),
    targetIds: strings2(connections.targetIds),
    ...edgeIds === void 0 ? {} : { edgeIds },
    ...synapseModels === void 0 ? {} : { synapseModels },
    scope: record2(data.scope),
    ...observedTargetIds === void 0 ? {} : { observedTargetIds }
  };
}
function topology(parts, dense) {
  const nodeIndex = /* @__PURE__ */ new Map();
  for (let index = 0; index < parts.nodeIds.length; index++) {
    nodeIndex.set(parts.nodeIds[index], index);
  }
  const scopeKind = String(parts.scope.kind);
  const observedTargets = scopeKind === "mpi_target_rank_local" ? new Set(parts.observedTargetIds ?? []) : scopeKind === "sampled" ? /* @__PURE__ */ new Set() : new Set(parts.nodeIds);
  const connectionSetComplete = scopeKind !== "sampled" && (scopeKind !== "mpi_target_rank_local" || parts.scope.localTargetUniverseComplete === true);
  const contributors = /* @__PURE__ */ new Map();
  for (let ordinal = 0; ordinal < parts.sourceIds.length; ordinal++) {
    const rowIndex = nodeIndex.get(parts.targetIds[ordinal]);
    const columnIndex = nodeIndex.get(parts.sourceIds[ordinal]);
    if (rowIndex === void 0 || columnIndex === void 0) {
      throw new Error("matrix authority encountered an endpoint outside the validated universe");
    }
    const key = `${rowIndex}:${columnIndex}`;
    const existing = contributors.get(key);
    if (existing === void 0) contributors.set(key, [ordinal]);
    else existing.push(ordinal);
  }
  const makeCell = (rowIndex, columnIndex) => {
    const targetId = parts.nodeIds[rowIndex];
    const sourceId = parts.nodeIds[columnIndex];
    const ordinals = contributors.get(`${rowIndex}:${columnIndex}`) ?? [];
    const completelyObserved = observedTargets.has(targetId) && connectionSetComplete;
    const state = ordinals.length > 0 ? "present" : completelyObserved ? "absent" : "not_observed";
    const contributingEdgeIds = parts.edgeIds === void 0 ? null : ordinals.map((ordinal) => parts.edgeIds[ordinal]).sort(compareUnicodeCodePoints);
    const models = parts.synapseModels === void 0 ? null : canonicalStrings(ordinals.map((ordinal) => parts.synapseModels[ordinal]));
    return {
      rowIndex,
      targetId,
      columnIndex,
      sourceId,
      state,
      ordinals: [...ordinals],
      retainedConnectionRows: ordinals.length,
      contributingConnectionCount: completelyObserved ? ordinals.length : null,
      contributingEdgeIds,
      synapseModels: models,
      isAutapse: targetId === sourceId,
      connectionSetComplete: completelyObserved
    };
  };
  const presentCoordinates = [];
  for (const key of contributors.keys()) {
    const separator = key.indexOf(":");
    presentCoordinates.push([
      Number(key.slice(0, separator)),
      Number(key.slice(separator + 1))
    ]);
  }
  presentCoordinates.sort((left, right) => left[0] - right[0] || left[1] - right[1]);
  const presentCells = presentCoordinates.map(([rowIndex, columnIndex]) => makeCell(rowIndex, columnIndex));
  const nodeCount = parts.nodeIds.length;
  const totalCellCount = nodeCount * nodeCount;
  const observedRowCount = connectionSetComplete ? observedTargets.size : 0;
  let presentInObservedRows = 0;
  for (const cell of presentCells) {
    if (observedTargets.has(cell.targetId)) presentInObservedRows++;
  }
  const absentCellCount = observedRowCount * nodeCount - presentInObservedRows;
  const notObservedCellCount = totalCellCount - presentCells.length - absentCellCount;
  let tableCells = presentCells;
  if (dense) {
    const materialized = [];
    for (let rowIndex = 0; rowIndex < nodeCount; rowIndex++) {
      for (let columnIndex = 0; columnIndex < nodeCount; columnIndex++) {
        materialized.push(makeCell(rowIndex, columnIndex));
      }
    }
    tableCells = materialized;
  }
  return {
    nodeIds: [...parts.nodeIds],
    connectionCount: parts.sourceIds.length,
    presentCells,
    tableCells,
    presentCellCount: presentCells.length,
    absentCellCount,
    notObservedCellCount,
    observedRowCount
  };
}
function aggregate3(values, method) {
  if (values.length === 0) throw new Error("matrix authority cannot aggregate an empty cell");
  if (method === "sum") return canonicalZero3(exactBinary64Sum(values));
  if (method === "mean") return canonicalZero3(exactBinary64Mean(values));
  if (method === "no_aggregation") {
    if (values.length !== 1) throw new Error("matrix authority no_aggregation encountered a multapse");
    return canonicalZero3(values[0]);
  }
  let result = values[0];
  for (let index = 1; index < values.length; index++) {
    if (method === "min" ? values[index] < result : values[index] > result) {
      result = values[index];
    }
  }
  return canonicalZero3(result);
}
function extrema(values) {
  if (values.length === 0) return null;
  let min = values[0];
  let max = values[0];
  for (let index = 1; index < values.length; index++) {
    if (values[index] < min) min = values[index];
    if (values[index] > max) max = values[index];
  }
  return { min: canonicalZero3(min), max: canonicalZero3(max) };
}
function cellProvenance(cell, state) {
  return {
    rowIndex: cell.rowIndex,
    targetId: cell.targetId,
    columnIndex: cell.columnIndex,
    sourceId: cell.sourceId,
    cellState: state
  };
}
function adjacencyMultapseStatement(cells, cellMode, aggregation) {
  let count = 0;
  for (const cell of cells) if (cell.retainedConnectionRows > 1) count++;
  if (count === 0) return "No present cell contains multiple retained connection rows.";
  if (cellMode === "binary_presence") {
    return `${count} present ${count === 1 ? "cell contains" : "cells contain"} multiple retained connection rows; each maps to one binary-presence mark, while retained-row and complete-cell counts remain in the table.`;
  }
  return `${count} present ${count === 1 ? "cell aggregates" : "cells aggregate"} multiple retained connection rows using ${aggregation}.`;
}
function noCompactionStatement() {
  return "No compaction was applied; every accepted row is returned.";
}
function scopeStatement(scope) {
  const kind = String(scope.kind);
  if (kind === "single_process") return "single_process complete";
  if (kind === "global_merged") {
    return `global_merged across all ${String(scope.worldSize)} declared ranks`;
  }
  if (kind === "mpi_target_rank_local") {
    return `mpi_target_rank_local rank ${String(scope.rank)} in worldSize ${String(scope.worldSize)}; localTargetUniverseComplete=${String(scope.localTargetUniverseComplete)}`;
  }
  if (kind === "sampled") {
    return `sampled ${String(scope.method)} from ${String(scope.parentScope)}; retained ${String(scope.retainedConnectionCount)} of ${String(scope.sourceConnectionCount)} connection rows`;
  }
  throw new Error("matrix authority encountered an unknown validated scope kind");
}
function scopeSummaryCell(scope) {
  const kind = String(scope.kind);
  if (kind === "single_process") {
    return canonicalize({ kind: "single_process", snapshotTime: scope.snapshotTime });
  }
  if (kind === "global_merged") {
    return canonicalize({
      kind: "global_merged",
      mergedRanksCoverage: "all_ranks_0_through_worldSize_minus_1",
      snapshotTime: scope.snapshotTime,
      worldSize: scope.worldSize
    });
  }
  if (kind === "mpi_target_rank_local") {
    return canonicalize({
      kind: "mpi_target_rank_local",
      localTargetUniverseComplete: true,
      rank: scope.rank,
      snapshotTime: scope.snapshotTime,
      worldSize: scope.worldSize
    });
  }
  if (kind === "sampled") {
    return canonicalize({
      kind: "sampled",
      method: scope.method,
      parentScope: scope.parentScope,
      retainedConnectionCount: scope.retainedConnectionCount,
      snapshotTime: scope.snapshotTime,
      sourceConnectionCount: scope.sourceConnectionCount
    });
  }
  throw new Error("matrix authority encountered an unknown validated scope kind");
}
function baseDisclosureFacts2(parts, extra = {}) {
  const source = record2(parts.request.source);
  const uncertainty = record2(parts.parameters.uncertainty ?? parts.data.uncertainty);
  const nodeUniverse = record2(parts.data.nodeUniverse);
  return {
    sourceKind: typeof source.kind === "string" ? source.kind : "unknown",
    sourceAuthenticityVerified: false,
    referenceComparisonRun: false,
    callerNotePresent: typeof source.declaredNote === "string",
    uncertaintyKind: typeof uncertainty.kind === "string" ? uncertainty.kind : "none",
    uncertaintyReason: typeof uncertainty.reason === "string" ? uncertainty.reason : "not_provided",
    ...typeof parts.scope.kind === "string" ? { scopeKind: parts.scope.kind } : {},
    ...typeof parts.scope.rank === "number" ? { rank: parts.scope.rank } : {},
    ...typeof parts.scope.worldSize === "number" ? { worldSize: parts.scope.worldSize } : {},
    ...typeof parts.scope.retainedConnectionCount === "number" ? {
      retainedConnectionCount: parts.scope.retainedConnectionCount,
      sampledRetained: parts.scope.retainedConnectionCount
    } : {},
    ...typeof parts.scope.sourceConnectionCount === "number" ? {
      sourceConnectionCount: parts.scope.sourceConnectionCount,
      sampledSource: parts.scope.sourceConnectionCount
    } : {},
    ...typeof nodeUniverse.complete === "boolean" ? { nodeUniverseComplete: nodeUniverse.complete } : {},
    ...extra
  };
}
function modelFields2(model) {
  return {
    "table.rows": rowSequence(model.rows),
    "geometry.sequence": geometrySequence(model.geometry),
    "summary.facts": summaryFactMap(model.summary),
    "disclosure.facts": disclosureFactMap(model.disclosures)
  };
}
function adjacencyModel(requestValue) {
  const parts = requestParts(requestValue);
  const matrix = topology(parts, parts.parameters.tableCellEnumeration === "dense");
  const cellMode = String(parts.parameters.cellMode);
  const aggregation = String(parts.parameters.multapseAggregation);
  const weightRecord = record2(parts.connections.weights);
  const delayRecord = record2(parts.connections.delays);
  const weightValues = array2(weightRecord.values);
  const delayValues = array2(delayRecord.values);
  const scopeCell = scopeSummaryCell(parts.scope);
  const rows = [];
  for (const cell of matrix.tableCells) {
    const count = cell.ordinals.length;
    const cellValue = cell.state === "not_observed" ? null : cellMode === "binary_presence" ? count > 0 ? 1 : 0 : count;
    const multiplicity = cell.connectionSetComplete ? count : null;
    const carried = [];
    for (const ordinal of cell.ordinals) {
      const entry = {};
      if (Object.keys(weightRecord).length > 0) {
        entry.weight = weightValues[ordinal] ?? null;
        entry.weightUnit = weightRecord.unit ?? null;
      }
      if (Object.keys(delayRecord).length > 0) {
        entry.delay = delayValues[ordinal] ?? null;
        entry.delayUnit = delayRecord.unit ?? null;
      }
      if (Object.keys(entry).length > 0) {
        if (parts.edgeIds !== void 0) entry.edgeId = parts.edgeIds[ordinal];
        if (parts.synapseModels !== void 0) {
          entry.synapseModel = parts.synapseModels[ordinal];
        }
        carried.push(entry);
      }
    }
    rows.push([
      cell.rowIndex,
      cell.targetId,
      cell.columnIndex,
      cell.sourceId,
      cell.state,
      cellValue,
      multiplicity,
      cell.retainedConnectionRows,
      booleanCell(cell.connectionSetComplete),
      booleanCell(cell.isAutapse),
      cell.contributingEdgeIds === null ? null : structuredCell2([...cell.contributingEdgeIds]),
      cell.synapseModels === null ? null : structuredCell2([...cell.synapseModels]),
      carried.length === 0 ? null : structuredCell2(carried),
      scopeCell
    ]);
  }
  const geometry = matrix.presentCells.map((cell) => carrier("cells", cellProvenance(cell, "present")));
  let autapseCellCount = 0;
  let multapseCellCount = 0;
  let missingValueCount = 0;
  for (const cell of matrix.presentCells) {
    if (cell.isAutapse) autapseCellCount++;
    if (cell.retainedConnectionRows > 1) multapseCellCount++;
  }
  for (const value of weightValues) if (value === null) missingValueCount++;
  for (const value of delayValues) if (value === null) missingValueCount++;
  const snapshotTime = record2(parts.scope.snapshotTime);
  return {
    rows,
    geometry,
    summary: {
      selectionLabel: String(parts.parameters.selectionLabel ?? parts.parameters.selectionId),
      nodeCount: String(parts.nodeIds.length),
      cellMode,
      presentCellCount: String(matrix.presentCellCount),
      connectionCount: String(matrix.connectionCount),
      observedRowCount: String(matrix.observedRowCount),
      absentCellCount: String(matrix.absentCellCount),
      notObservedCellCount: String(matrix.notObservedCellCount),
      autapseCellCount: String(autapseCellCount),
      scopeStatement: scopeStatement(parts.scope),
      snapshotTime: summaryNumber(Number(snapshotTime.value)),
      snapshotTimeUnit: String(snapshotTime.unit),
      multapseStatement: adjacencyMultapseStatement(
        matrix.presentCells,
        cellMode,
        aggregation
      ),
      compactionStatement: noCompactionStatement()
    },
    disclosures: baseDisclosureFacts2(parts, {
      ...cellMode === "multiplicity" && multapseCellCount > 0 ? { multapseAggregated: true, multapseAggregation: aggregation } : {},
      ...missingValueCount > 0 ? { missingValueCount } : {}
    })
  };
}
function weightMultapseStatement(cells, aggregation) {
  let aggregated = 0;
  let unavailable = 0;
  for (const cell of cells) {
    if (cell.topology.retainedConnectionRows <= 1) continue;
    if (cell.state === "valued") aggregated++;
    else unavailable++;
  }
  if (aggregated === 0 && unavailable === 0) {
    return "No present cell contains multiple retained connection rows.";
  }
  const statements = [];
  if (aggregated > 0) {
    statements.push(
      `${aggregated} valued ${aggregated === 1 ? "cell aggregates" : "cells aggregate"} multiple retained connection rows using ${aggregation}.`
    );
  }
  if (unavailable > 0) {
    statements.push(
      `${unavailable} present ${unavailable === 1 ? "cell contains" : "cells contain"} multiple retained connection rows, but no complete aggregate is reported because at least one contributing weight is missing.`
    );
  }
  return statements.join(" ");
}
function synapseModelGroupStatement(parts) {
  if (parts.synapseModels === void 0) return "no synapse-model channel was declared";
  const models = canonicalStrings(parts.synapseModels) ?? [];
  if (models.length === 0) return "the declared synapse-model channel is empty";
  if (models.length === 1) return `one declared model ${models[0]}; no cross-model pooling`;
  return `caller-declared comparability group ${String(parts.parameters.synapseModelGroup)} across ${String(models.length)} distinct declared models`;
}
function colorScaleStatement(parameters) {
  const scale = record2(parameters.colorScale);
  return scale.class === "diverging" ? `diverging about declared center ${summaryNumber(Number(scale.center))}` : "sequential";
}
function weightModel2(requestValue) {
  const parts = requestParts(requestValue);
  const matrix = topology(parts, parts.parameters.tableCellEnumeration === "dense");
  const weights = nullableNumbers2(record2(parts.connections.weights).values);
  const aggregation = String(parts.parameters.multapseAggregation);
  const weightUnit = String(record2(parts.connections.weights).unit);
  const scopeCell = scopeSummaryCell(parts.scope);
  const weightCells = [];
  for (const cell of matrix.tableCells) {
    const measured = [];
    for (const ordinal of cell.ordinals) {
      const value = weights[ordinal];
      if (value !== null) measured.push(canonicalZero3(value));
    }
    const state = cell.state === "absent" ? "absent" : cell.state === "not_observed" ? "not_observed" : measured.length === 0 ? "present_without_value" : measured.length < cell.ordinals.length ? "present_with_missing_value" : "valued";
    weightCells.push({
      topology: cell,
      state,
      aggregate: state === "valued" ? aggregate3(measured, aggregation) : null,
      contributingWeightCount: cell.state === "not_observed" ? null : measured.length,
      missingWeightCount: cell.state === "not_observed" ? null : cell.ordinals.length - measured.length,
      weightMin: measured.length === 0 ? null : aggregate3(measured, "min"),
      weightMax: measured.length === 0 ? null : aggregate3(measured, "max")
    });
  }
  const rows = [];
  const aggregates = [];
  let valuedCellCount = 0;
  let presentWithMissingValueCellCount = 0;
  let presentWithoutValueCellCount = 0;
  let aggregatedMultapseCellCount = 0;
  for (const cell of weightCells) {
    if (cell.aggregate !== null) aggregates.push(cell.aggregate);
    if (cell.state === "valued") valuedCellCount++;
    if (cell.state === "present_with_missing_value") presentWithMissingValueCellCount++;
    if (cell.state === "present_without_value") presentWithoutValueCellCount++;
    if (cell.state === "valued" && cell.topology.retainedConnectionRows > 1) {
      aggregatedMultapseCellCount++;
    }
    const models = cell.topology.synapseModels === null ? null : structuredCell2({
      models: [...cell.topology.synapseModels],
      ...typeof parts.parameters.synapseModelGroup === "string" ? { comparabilityGroup: parts.parameters.synapseModelGroup } : {}
    });
    rows.push([
      cell.topology.rowIndex,
      cell.topology.targetId,
      cell.topology.columnIndex,
      cell.topology.sourceId,
      cell.state,
      cell.aggregate,
      aggregation,
      weightUnit,
      cell.topology.contributingConnectionCount,
      cell.contributingWeightCount,
      cell.missingWeightCount,
      cell.weightMin,
      cell.weightMax,
      models,
      cell.topology.contributingEdgeIds === null ? null : structuredCell2([...cell.topology.contributingEdgeIds]),
      scopeCell
    ]);
  }
  const byState = (state) => {
    const result = [];
    for (const cell of weightCells) {
      if (cell.state === state) {
        result.push(carrier("cells", cellProvenance(cell.topology, cell.state)));
      }
    }
    return result;
  };
  const geometry = [
    ...byState("valued"),
    ...byState("present_with_missing_value"),
    ...byState("present_without_value")
  ];
  const aggregateExtent = extrema(aggregates);
  let missingValueCount = 0;
  for (const weight of weights) if (weight === null) missingValueCount++;
  const snapshotTime = record2(parts.scope.snapshotTime);
  return {
    rows,
    geometry,
    summary: {
      nodeCount: String(parts.nodeIds.length),
      valuedCellCount: String(valuedCellCount),
      presentWithMissingValueCellCount: String(presentWithMissingValueCellCount),
      presentWithoutValueCellCount: String(presentWithoutValueCellCount),
      absentCellCount: String(matrix.absentCellCount),
      notObservedCellCount: String(matrix.notObservedCellCount),
      aggregation,
      connectionCount: String(matrix.connectionCount),
      weightUnit,
      aggregateMin: aggregateExtent === null ? "not available" : summaryNumber(aggregateExtent.min),
      aggregateMax: aggregateExtent === null ? "not available" : summaryNumber(aggregateExtent.max),
      synapseModelGroupStatement: synapseModelGroupStatement(parts),
      colorScaleStatement: colorScaleStatement(parts.parameters),
      scopeStatement: scopeStatement(parts.scope),
      snapshotTime: `${summaryNumber(Number(snapshotTime.value))} ${String(snapshotTime.unit)}`,
      multapseStatement: weightMultapseStatement(weightCells, aggregation),
      compactionStatement: noCompactionStatement()
    },
    disclosures: baseDisclosureFacts2(parts, {
      ...aggregatedMultapseCellCount > 0 ? { multapseAggregated: true, multapseAggregation: aggregation } : {},
      ...missingValueCount > 0 ? { missingValueCount } : {}
    })
  };
}
function delayModel2(requestValue) {
  const parts = requestParts(requestValue);
  const matrix = topology(parts, parts.parameters.tableCellEnumeration === "dense");
  const delayRecord = record2(parts.connections.delays);
  const delays = numbers2(delayRecord.values);
  const sourceUnit = String(delayRecord.unit);
  const displayUnit = String(record2(parts.parameters.displayUnit).unit ?? sourceUnit);
  const aggregation = String(parts.parameters.multapseAggregation);
  const delaySemantics = String(parts.parameters.delaySemantics);
  const scopeCell = scopeSummaryCell(parts.scope);
  const rows = [];
  const geometry = [];
  const aggregates = [];
  let multapseCellCount = 0;
  for (const cell of matrix.tableCells) {
    const measured = [];
    for (const ordinal of cell.ordinals) measured.push(delays[ordinal]);
    const hasDelay = cell.state === "present";
    const convertOnce = (value) => sourceUnit === displayUnit ? value : canonicalZero3(convert(value, sourceUnit, displayUnit));
    const delayAggregate = hasDelay ? convertOnce(aggregate3(measured, aggregation)) : null;
    const delayMin = hasDelay ? convertOnce(aggregate3(measured, "min")) : null;
    const delayMax = hasDelay ? convertOnce(aggregate3(measured, "max")) : null;
    if (delayAggregate !== null) aggregates.push(delayAggregate);
    if (cell.retainedConnectionRows > 1) multapseCellCount++;
    rows.push([
      cell.rowIndex,
      cell.targetId,
      cell.columnIndex,
      cell.sourceId,
      cell.state,
      delayAggregate,
      displayUnit,
      aggregation,
      delaySemantics,
      cell.contributingConnectionCount,
      delayMin,
      delayMax,
      cell.contributingEdgeIds === null ? null : structuredCell2([...cell.contributingEdgeIds]),
      cell.synapseModels === null ? null : structuredCell2([...cell.synapseModels]),
      booleanCell(cell.isAutapse),
      scopeCell
    ]);
    if (cell.state === "present") {
      geometry.push(carrier("cells", cellProvenance(cell, "present")));
    }
  }
  const delayExtent = extrema(aggregates);
  const snapshotTime = record2(parts.scope.snapshotTime);
  const unitConversions = sourceUnit === displayUnit || matrix.presentCells.length === 0 ? [] : [`delay: ${sourceUnit} -> ${displayUnit} (factor ${conversionFactor(sourceUnit, displayUnit)})`];
  return {
    rows,
    geometry,
    summary: {
      matrixLabel: String(parts.parameters.matrixLabel ?? "declared node universe"),
      rowCount: String(parts.nodeIds.length),
      columnCount: String(parts.nodeIds.length),
      presentCellCount: String(matrix.presentCellCount),
      absentCellCount: String(matrix.absentCellCount),
      notObservedCellCount: String(matrix.notObservedCellCount),
      multapseAggregation: aggregation,
      delaySemantics,
      connectionCount: String(matrix.connectionCount),
      displayUnit,
      delayMin: delayExtent === null ? "not available" : summaryNumber(delayExtent.min),
      delayMax: delayExtent === null ? "not available" : summaryNumber(delayExtent.max),
      scopeKind: String(parts.scope.kind),
      snapshotTime: summaryNumber(Number(snapshotTime.value)),
      snapshotTimeUnit: String(snapshotTime.unit),
      compactionStatement: noCompactionStatement()
    },
    disclosures: baseDisclosureFacts2(parts, {
      ...multapseCellCount > 0 ? { multapseAggregated: true, multapseAggregation: aggregation } : {},
      ...unitConversions.length > 0 ? { unitConversions } : {}
    })
  };
}
var ADJACENCY_AUTHORITY = defineAuthorityEvaluator(
  authorityEvaluatorId("network.adjacency_matrix", 2),
  (request) => modelFields2(adjacencyModel(request))
);
var WEIGHT_AUTHORITY2 = defineAuthorityEvaluator(
  authorityEvaluatorId("network.weight_matrix", 2),
  (request) => modelFields2(weightModel2(request))
);
var DELAY_AUTHORITY2 = defineAuthorityEvaluator(
  authorityEvaluatorId("network.delay_matrix", 2),
  (request) => modelFields2(delayModel2(request))
);
var MATRIX_AUTHORITY_EVALUATORS = [
  ADJACENCY_AUTHORITY,
  WEIGHT_AUTHORITY2,
  DELAY_AUTHORITY2
];

// src/authority/evaluators/topology-dynamics.ts
function record3(value) {
  return value !== null && value !== void 0 && typeof value === "object" && !Array.isArray(value) ? value : {};
}
function array3(value) {
  return Array.isArray(value) ? value : [];
}
function numbers3(value) {
  return array3(value).map(Number);
}
function nullableNumbers3(value) {
  return array3(value).map((entry) => entry === null ? null : Number(entry));
}
function strings3(value) {
  return array3(value).map(String);
}
function exactText2(value) {
  return value === 0 ? "0" : String(value);
}
function booleanCell2(value) {
  return value ? "true" : "false";
}
function structuredCell3(value) {
  return value === void 0 ? "unknown" : canonicalize(value);
}
function topologyScopeSummaryCell(value) {
  const scope = record3(value);
  const summary = { kind: String(scope.kind) };
  if (scope.snapshotTime !== void 0) summary.snapshotTime = scope.snapshotTime;
  switch (scope.kind) {
    case "single_process":
      break;
    case "global_merged":
      summary.mergedRanksCoverage = "all_ranks_0_through_worldSize_minus_1";
      summary.worldSize = scope.worldSize;
      break;
    case "mpi_target_rank_local":
      summary.localTargetUniverseComplete = scope.localTargetUniverseComplete;
      summary.rank = scope.rank;
      summary.worldSize = scope.worldSize;
      break;
    case "sampled":
      summary.method = scope.method;
      summary.parentScope = scope.parentScope;
      summary.retainedConnectionCount = scope.retainedConnectionCount;
      summary.sourceConnectionCount = scope.sourceConnectionCount;
      break;
    default:
      throw new Error(`unsupported NetworkScope kind ${String(scope.kind)}`);
  }
  return canonicalize(summary);
}
function compareUnicodeCodePoints2(left, right) {
  const leftIterator = left[Symbol.iterator]();
  const rightIterator = right[Symbol.iterator]();
  while (true) {
    const leftNext = leftIterator.next();
    const rightNext = rightIterator.next();
    if (leftNext.done || rightNext.done) {
      if (leftNext.done && rightNext.done) return 0;
      return leftNext.done ? -1 : 1;
    }
    const leftCodePoint = leftNext.value.codePointAt(0);
    const rightCodePoint = rightNext.value.codePointAt(0);
    if (leftCodePoint !== rightCodePoint) return leftCodePoint < rightCodePoint ? -1 : 1;
  }
}
function compareUtf16CodeUnits(left, right) {
  return left < right ? -1 : left > right ? 1 : 0;
}
function finiteExtrema2(values) {
  if (values.length === 0) return null;
  let min = values[0];
  let max = values[0];
  for (let index = 1; index < values.length; index++) {
    if (values[index] < min) min = values[index];
    if (values[index] > max) max = values[index];
  }
  return { min, max };
}
function countNulls(values) {
  let total = 0;
  for (const value of values) if (value === null) total++;
  return total;
}
function pairKey(source, target) {
  return canonicalize(compareUnicodeCodePoints2(source, target) <= 0 ? [source, target] : [target, source]);
}
function conversionText(label, from, to) {
  return `${label}: ${from} -> ${to} (factor ${conversionFactor(from, to)})`;
}
function baseDisclosureFacts3(request, extra = {}) {
  const source = record3(request.source);
  const data = record3(request.data);
  const parameters = record3(request.parameters);
  const scope = record3(data.scope);
  const nodeUniverse = record3(data.nodeUniverse);
  const positions = record3(data.positions);
  const uncertainty = record3(parameters.uncertainty ?? data.uncertainty);
  const universeIds = strings3(nodeUniverse.ids);
  const positionIds = strings3(positions.nodeIds);
  return {
    sourceKind: typeof source.kind === "string" ? source.kind : "unknown",
    sourceAuthenticityVerified: false,
    referenceComparisonRun: false,
    callerNotePresent: typeof source.declaredNote === "string",
    uncertaintyKind: typeof uncertainty.kind === "string" ? uncertainty.kind : "none",
    uncertaintyReason: typeof uncertainty.reason === "string" ? uncertainty.reason : "not_provided",
    ...typeof scope.kind === "string" ? { scopeKind: scope.kind } : {},
    ...typeof scope.rank === "number" ? { rank: scope.rank } : {},
    ...typeof scope.worldSize === "number" ? { worldSize: scope.worldSize } : {},
    ...typeof scope.retainedConnectionCount === "number" ? {
      retainedConnectionCount: scope.retainedConnectionCount,
      sampledRetained: scope.retainedConnectionCount
    } : {},
    ...typeof scope.sourceConnectionCount === "number" ? {
      sourceConnectionCount: scope.sourceConnectionCount,
      sampledSource: scope.sourceConnectionCount
    } : {},
    ...typeof nodeUniverse.complete === "boolean" ? { nodeUniverseComplete: nodeUniverse.complete } : {},
    ...typeof record3(parameters.layout).mode === "string" && String(record3(parameters.layout).mode).startsWith("schematic") ? { schematicLayout: true } : {},
    ...universeIds.length > positionIds.length && positions.nodeIds !== void 0 ? { positionsMissing: universeIds.length - positionIds.length, positionsTotal: universeIds.length } : {},
    ...extra
  };
}
function modelFields3(model) {
  return {
    "table.rows": rowSequence(model.rows),
    "geometry.sequence": geometrySequence(model.geometry),
    "summary.facts": summaryFactMap(model.summary),
    "disclosure.facts": disclosureFactMap(model.disclosures)
  };
}
function graphDegrees(ids, sources, targets, policy, autapseContribution) {
  const indexById = new Map(ids.map((id, index) => [id, index]));
  const incoming = new Array(ids.length).fill(0);
  const outgoing = new Array(ids.length).fill(0);
  const autapseCounts = new Array(ids.length).fill(0);
  if (policy === "per_unique_neighbour") {
    const incomingNeighbours = ids.map(() => /* @__PURE__ */ new Set());
    const outgoingNeighbours = ids.map(() => /* @__PURE__ */ new Set());
    const autapses = /* @__PURE__ */ new Set();
    for (let ordinal = 0; ordinal < sources.length; ordinal++) {
      const source = sources[ordinal];
      const target = targets[ordinal];
      const sourceIndex = indexById.get(source);
      const targetIndex = indexById.get(target);
      if (source === target) {
        if (!autapses.has(source)) autapseCounts[sourceIndex] = 1;
        autapses.add(source);
        if (autapseContribution === "excluded") continue;
      }
      outgoingNeighbours[sourceIndex].add(target);
      incomingNeighbours[targetIndex].add(source);
    }
    for (let index = 0; index < ids.length; index++) {
      incoming[index] = incomingNeighbours[index].size;
      outgoing[index] = outgoingNeighbours[index].size;
    }
  } else {
    for (let ordinal = 0; ordinal < sources.length; ordinal++) {
      const sourceIndex = indexById.get(sources[ordinal]);
      const targetIndex = indexById.get(targets[ordinal]);
      if (sources[ordinal] === targets[ordinal]) {
        autapseCounts[sourceIndex]++;
        if (autapseContribution === "excluded") continue;
      }
      outgoing[sourceIndex]++;
      incoming[targetIndex]++;
    }
  }
  return { incoming, outgoing, autapseCounts };
}
function graphModel(requestValue) {
  const request = record3(requestValue);
  const data = record3(request.data);
  const parameters = record3(request.parameters);
  const universe = record3(data.nodeUniverse);
  const connections = record3(data.connections);
  const ids = strings3(universe.ids);
  const sources = strings3(connections.sourceIds);
  const targets = strings3(connections.targetIds);
  const groups = array3(universe.groups).map(record3);
  const groupByNode = /* @__PURE__ */ new Map();
  for (const group of groups) {
    for (const member of strings3(group.memberIds)) groupByNode.set(member, String(group.id));
  }
  const degree = record3(parameters.degreeAnnotation);
  const degreePolicy = String(degree.countingPolicy);
  const degreeValues = graphDegrees(
    ids,
    sources,
    targets,
    degreePolicy,
    String(degree.autapseContribution)
  );
  const positions = record3(data.positions);
  const positionedIds = strings3(positions.nodeIds);
  const positionIndex = new Map(positionedIds.map((id, index) => [id, index]));
  const sourceXs = numbers3(record3(positions.x).values);
  const sourceYs = numbers3(record3(positions.y).values);
  const xUnit = positions.x === void 0 ? null : String(record3(positions.x).unit);
  const yUnit = positions.y === void 0 ? null : String(record3(positions.y).unit);
  const positionUnit = xUnit !== null && yUnit !== null ? xUnit === yUnit || conversionFactor(yUnit, xUnit) >= 1 ? xUnit : yUnit : null;
  const xs = positionUnit !== null && xUnit !== positionUnit ? sourceXs.map((value) => convert(value, xUnit, positionUnit)) : sourceXs;
  const ys = positionUnit !== null && yUnit !== positionUnit ? sourceYs.map((value) => convert(value, yUnit, positionUnit)) : sourceYs;
  const layoutMode = String(record3(parameters.layout).mode);
  const measured = layoutMode === "measured_positions";
  const scopeSummary = topologyScopeSummaryCell(data.scope);
  const scopeKind = String(record3(data.scope).kind);
  const nodeRows = ids.map((id, index) => {
    const position = positionIndex.get(id);
    const totalDegree = degree.mode === void 0 ? null : degreeValues.incoming[index] + degreeValues.outgoing[index] - (degree.autapseContribution === "counts_once" ? degreeValues.autapseCounts[index] : 0);
    return [
      "node",
      id,
      groupByNode.get(id) ?? null,
      null,
      null,
      null,
      null,
      null,
      null,
      null,
      null,
      null,
      null,
      degree.mode === "in_degree" || degree.mode === "total_degree" ? degreeValues.incoming[index] : null,
      (degree.mode === "out_degree" || degree.mode === "total_degree") && scopeKind !== "mpi_target_rank_local" ? degreeValues.outgoing[index] : null,
      degree.mode === void 0 ? null : structuredCell3({
        mode: degree.mode,
        countingPolicy: degree.countingPolicy,
        autapseContribution: degree.autapseContribution,
        ...degree.mode === "total_degree" ? { totalDegree } : {}
      }),
      measured && position !== void 0 ? xs[position] : null,
      measured && position !== void 0 ? ys[position] : null,
      measured ? positionUnit : null,
      measured ? "measured" : "schematic (non-spatial)",
      scopeSummary
    ];
  });
  const edgeIds = strings3(connections.edgeIds);
  const weights = array3(record3(connections.weights).values);
  const delays = array3(record3(connections.delays).values);
  const models = strings3(connections.synapseModels);
  const weightUnit = connections.weights === void 0 ? null : String(record3(connections.weights).unit);
  const delayUnit = connections.delays === void 0 ? null : String(record3(connections.delays).unit);
  const pairTotals = /* @__PURE__ */ new Map();
  for (let ordinal = 0; ordinal < sources.length; ordinal++) {
    const key = pairKey(sources[ordinal], targets[ordinal]);
    pairTotals.set(key, (pairTotals.get(key) ?? 0) + 1);
  }
  const pairSeen = /* @__PURE__ */ new Map();
  const edgeRows = sources.map((source, ordinal) => {
    const key = pairKey(source, targets[ordinal]);
    const parallelIndex = (pairSeen.get(key) ?? 0) + 1;
    pairSeen.set(key, parallelIndex);
    return [
      "connection",
      edgeIds[ordinal] ?? null,
      null,
      source,
      targets[ordinal],
      booleanCell2(source === targets[ordinal]),
      parallelIndex,
      pairTotals.get(key),
      typeof weights[ordinal] === "number" ? weights[ordinal] : null,
      weightUnit,
      typeof delays[ordinal] === "number" ? delays[ordinal] : null,
      delayUnit,
      models[ordinal] ?? null,
      null,
      null,
      null,
      null,
      null,
      null,
      measured ? "measured" : "schematic (non-spatial)",
      scopeSummary
    ];
  });
  const edges = sources.map((sourceId, sourceOrdinal) => ({
    id: edgeIds[sourceOrdinal] ?? `source-row-${sourceOrdinal}`,
    sourceId,
    targetId: targets[sourceOrdinal],
    sourceOrdinal
  }));
  const geometry = [];
  if (record3(parameters.parallelEdges).display === "bundled") {
    const directed = /* @__PURE__ */ new Map();
    for (const edge of edges) {
      const key = canonicalize([edge.sourceId, edge.targetId]);
      const existing = directed.get(key);
      if (existing) existing.push(edge);
      else directed.set(key, [edge]);
    }
    for (const entries of directed.values()) {
      geometry.push(carrier("edges", {
        display: "bundled",
        sourceId: entries[0].sourceId,
        targetId: entries[0].targetId,
        sourceOrdinals: entries.map((entry) => entry.sourceOrdinal),
        edgeIds: entries.map((entry) => entry.id)
      }));
    }
  } else {
    const unordered = /* @__PURE__ */ new Map();
    for (const edge of edges) {
      const key = pairKey(edge.sourceId, edge.targetId);
      const existing = unordered.get(key);
      if (existing) existing.push(edge);
      else unordered.set(key, [edge]);
    }
    for (const entries of unordered.values()) {
      for (const edge of entries) {
        geometry.push(carrier("edges", {
          display: "separate_lane",
          sourceId: edge.sourceId,
          targetId: edge.targetId,
          sourceOrdinals: [edge.sourceOrdinal],
          edgeIds: [edge.id]
        }));
      }
    }
  }
  for (let index = 0; index < ids.length; index++) {
    geometry.push(carrier("nodes", { nodeId: ids[index], sourceOrdinal: index }));
  }
  const connected = /* @__PURE__ */ new Set();
  let autapseCount = 0;
  for (let ordinal = 0; ordinal < sources.length; ordinal++) {
    connected.add(sources[ordinal]);
    connected.add(targets[ordinal]);
    if (sources[ordinal] === targets[ordinal]) autapseCount++;
  }
  const edgeEncoding = record3(parameters.edgeValueEncoding);
  const encodedValues = edgeEncoding.mode === "weight" ? weights : edgeEncoding.mode === "delay" ? delays : [];
  const missingEncoded = countNulls(encodedValues);
  const conversions = measured && positionUnit !== null ? [
    ...xUnit !== positionUnit ? [conversionText("x", xUnit, positionUnit)] : [],
    ...yUnit !== positionUnit ? [conversionText("y", yUnit, positionUnit)] : []
  ] : [];
  const bundled = record3(parameters.parallelEdges).display === "bundled";
  const directedGroups = /* @__PURE__ */ new Set();
  for (let index = 0; index < sources.length; index++) {
    directedGroups.add(canonicalize([sources[index], targets[index]]));
  }
  const aggregated = bundled && directedGroups.size < sources.length;
  const edgeValueStatement = edgeEncoding.mode === void 0 ? "no edge value channel is encoded" : `${String(edgeEncoding.mode)} on ${String(edgeEncoding.channel)} with ${String(edgeEncoding.scale ?? "linear")} scale${typeof edgeEncoding.colorScale === "string" ? ` and ${edgeEncoding.colorScale} colour` : ""}${typeof parameters.multapseAggregation === "string" ? `; bundled multapses use ${parameters.multapseAggregation}` : ""}`;
  const degreeStatement = degree.mode === void 0 ? "no degree annotation" : structuredCell3(degree);
  return {
    rows: [...nodeRows, ...edgeRows],
    geometry,
    summary: {
      graphLabel: String(parameters.graphLabel ?? parameters.graphId),
      nodeCount: exactText2(ids.length),
      isolateCount: exactText2(ids.filter((id) => !connected.has(id)).length),
      edgeCount: exactText2(sources.length),
      multapseRowCount: exactText2(sources.length - pairTotals.size),
      autapseCount: exactText2(autapseCount),
      scopeStatement: scopeSummary,
      layoutMode,
      layoutSpatialStatement: measured ? `measured positions in ${String(positionUnit)}` : "schematic and non-spatial",
      nodeOrder: String(universe.order ?? "as_declared"),
      edgeValueStatement,
      degreeStatement,
      missingValueStatement: edgeEncoding.mode === void 0 ? "No edge value channel is encoded." : missingEncoded === 0 ? "No encoded edge value is missing." : `${missingEncoded} encoded edge values are missing and retain the reserved missing-value encoding.`,
      compactionStatement: "No declared node or connection row was compacted.",
      tableStatement: `The complete table contains ${ids.length} node rows followed by ${sources.length} source-order connection rows.`
    },
    disclosures: baseDisclosureFacts3(request, {
      ...conversions.length > 0 ? { unitConversions: conversions } : {},
      ...missingEncoded > 0 ? { missingValueCount: missingEncoded } : {},
      ...aggregated && typeof parameters.multapseAggregation === "string" ? { multapseAggregated: true, multapseAggregation: parameters.multapseAggregation } : {}
    })
  };
}
var GRAPH_AUTHORITY = defineAuthorityEvaluator(
  authorityEvaluatorId("network.connection_graph", 2),
  (request) => modelFields3(graphModel(request))
);
function spatialModel(requestValue) {
  const request = record3(requestValue);
  const data = record3(request.data);
  const parameters = record3(request.parameters);
  const universe = record3(data.nodeUniverse);
  const positions = record3(data.positions);
  const universeIds = strings3(universe.ids);
  const sourcePositionIds = strings3(positions.nodeIds);
  const sourcePositionIndex = new Map(
    sourcePositionIds.map((id, index) => [id, index])
  );
  const ids = [...universeIds];
  const sourceXs = numbers3(record3(positions.x).values);
  const sourceYs = numbers3(record3(positions.y).values);
  const xUnit = String(record3(positions.x).unit);
  const yUnit = String(record3(positions.y).unit);
  const positionUnit = xUnit === yUnit || conversionFactor(yUnit, xUnit) >= 1 ? xUnit : yUnit;
  const convertedSourceXs = xUnit === positionUnit ? sourceXs : sourceXs.map((value2) => convert(value2, xUnit, positionUnit));
  const convertedSourceYs = yUnit === positionUnit ? sourceYs : sourceYs.map((value2) => convert(value2, yUnit, positionUnit));
  const xs = ids.map((id) => convertedSourceXs[sourcePositionIndex.get(id)]);
  const ys = ids.map((id) => convertedSourceYs[sourcePositionIndex.get(id)]);
  const positionIndex = new Map(ids.map((id, index) => [id, index]));
  const groupByNode = /* @__PURE__ */ new Map();
  for (const group of array3(universe.groups).map(record3)) {
    for (const member of strings3(group.memberIds)) groupByNode.set(member, String(group.id));
  }
  const domain = record3(positions.domain);
  const center = record3(domain.center);
  const extent = record3(domain.extent);
  const hasDomain = domain.center !== void 0 && domain.extent !== void 0;
  const canonicalCenterX = hasDomain ? convert(Number(record3(center.x).value), String(record3(center.x).unit), positionUnit) : 0;
  const canonicalCenterY = hasDomain ? convert(Number(record3(center.y).value), String(record3(center.y).unit), positionUnit) : 0;
  const canonicalWidth = hasDomain ? convert(Number(record3(extent.width).value), String(record3(extent.width).unit), positionUnit) : 0;
  const canonicalHeight = hasDomain ? convert(Number(record3(extent.height).value), String(record3(extent.height).unit), positionUnit) : 0;
  const domainAxisX = hasDomain ? spatialDomainAxis(canonicalCenterX, canonicalWidth) : null;
  const domainAxisY = hasDomain ? spatialDomainAxis(canonicalCenterY, canonicalHeight) : null;
  const boundary = record3(domain.boundary);
  const domainBounds = domainAxisX && domainAxisY ? {
    xMin: domainAxisX.lower,
    xMax: domainAxisX.upper,
    yMin: domainAxisY.lower,
    yMax: domainAxisY.upper,
    centerX: canonicalCenterX,
    centerY: canonicalCenterY,
    periodX: canonicalWidth,
    periodY: canonicalHeight,
    periodicX: boundary.kind === "periodic" && boundary.x === true,
    periodicY: boundary.kind === "periodic" && boundary.y === true,
    edgeChordRule: boundary.kind === "periodic" ? String(boundary.edgeChordRule) : "open"
  } : null;
  const coincident = /* @__PURE__ */ new Map();
  for (let index = 0; index < ids.length; index++) {
    const key = canonicalize([xs[index], ys[index]]);
    const existing = coincident.get(key);
    if (existing) existing.push(ids[index]);
    else coincident.set(key, [ids[index]]);
  }
  const value = record3(positions.value);
  const sourceValueValues = array3(value.values);
  const valueValues = positions.value === void 0 ? [] : ids.map((id) => sourceValueValues[sourcePositionIndex.get(id)]);
  const scopeSummary = topologyScopeSummaryCell(data.scope);
  const nodeRows = universeIds.map((nodeId) => {
    const index = positionIndex.get(nodeId);
    const missing = index === void 0;
    const x = missing ? null : xs[index];
    const y = missing ? null : ys[index];
    const sharing = missing ? [] : (coincident.get(canonicalize([x, y])) ?? []).filter((id) => id !== nodeId);
    return [
      "node",
      nodeId,
      groupByNode.get(nodeId) ?? "ungrouped",
      x,
      y,
      missing ? null : positionUnit,
      missing ? null : String(positions.status),
      booleanCell2(missing),
      domainBounds === null || missing ? null : booleanCell2(
        spatialDomainAxisContains(domainAxisX, x) && spatialDomainAxisContains(domainAxisY, y)
      ),
      sharing.length > 0 ? structuredCell3(sharing) : null,
      missing || positions.value === void 0 || valueValues[index] === null ? null : valueValues[index],
      positions.value === void 0 ? null : String(value.unit),
      null,
      null,
      null,
      null,
      null,
      null,
      null,
      null,
      null,
      scopeSummary
    ];
  });
  const connections = record3(data.connections);
  const sources = strings3(connections.sourceIds);
  const targets = strings3(connections.targetIds);
  const callerEdgeIds = strings3(connections.edgeIds);
  const edgeIds = sources.map((_source, index) => callerEdgeIds[index] ?? `connection-row-${index}`);
  const weights = array3(record3(connections.weights).values);
  const delays = array3(record3(connections.delays).values);
  const models = strings3(connections.synapseModels);
  const weightUnit = connections.weights === void 0 ? null : String(record3(connections.weights).unit);
  const delayUnit = connections.delays === void 0 ? null : String(record3(connections.delays).unit);
  const universeOrdinal = new Map(universeIds.map((id, index) => [id, index]));
  const canonicalPair = (source, target) => universeOrdinal.get(source) <= universeOrdinal.get(target) ? [source, target] : [target, source];
  const spatialPairKey = (source, target) => canonicalize(canonicalPair(source, target));
  const totals = /* @__PURE__ */ new Map();
  for (let index = 0; index < sources.length; index++) {
    const key = spatialPairKey(sources[index], targets[index]);
    totals.set(key, (totals.get(key) ?? 0) + 1);
  }
  const routes = /* @__PURE__ */ new Map();
  for (let index = 0; index < sources.length; index++) {
    const key = spatialPairKey(sources[index], targets[index]);
    if (routes.has(key)) continue;
    const [physicalSourceId, physicalTargetId] = canonicalPair(
      sources[index],
      targets[index]
    );
    const sourceIndex = positionIndex.get(physicalSourceId);
    const targetIndex = positionIndex.get(physicalTargetId);
    routes.set(key, classifySpatialChord(
      { x: xs[sourceIndex], y: ys[sourceIndex] },
      { x: xs[targetIndex], y: ys[targetIndex] },
      domainBounds ?? void 0,
      physicalSourceId === physicalTargetId
    ));
  }
  let minimumImageTieChordCount = 0;
  let minimumImageTieAxisCount = 0;
  for (const route of routes.values()) {
    const axes = Number(route.halfPeriodTieX) + Number(route.halfPeriodTieY);
    if (axes > 0) minimumImageTieChordCount++;
    minimumImageTieAxisCount += axes;
  }
  const edgeRows = sources.map((source, index) => [
    "connection",
    edgeIds[index],
    "not_applicable",
    null,
    null,
    null,
    null,
    "not_applicable",
    null,
    null,
    null,
    null,
    source,
    targets[index],
    routes.get(spatialPairKey(source, targets[index])).pathKind,
    totals.get(spatialPairKey(source, targets[index])),
    typeof weights[index] === "number" ? weights[index] : null,
    weightUnit,
    typeof delays[index] === "number" ? delays[index] : null,
    delayUnit,
    models[index] ?? null,
    scopeSummary
  ]);
  const geometry = [];
  const pairs = /* @__PURE__ */ new Map();
  for (let index = 0; index < sources.length; index++) {
    const key = spatialPairKey(sources[index], targets[index]);
    const existing = pairs.get(key);
    if (existing) existing.push(index);
    else pairs.set(key, [index]);
  }
  for (const ordinals of pairs.values()) {
    const directions = /* @__PURE__ */ new Map();
    for (const ordinal of ordinals) {
      const key = canonicalize([sources[ordinal], targets[ordinal]]);
      const existing = directions.get(key);
      if (existing) existing.push(ordinal);
      else directions.set(key, [ordinal]);
    }
    for (const directionOrdinals of directions.values()) {
      const first = directionOrdinals[0];
      geometry.push(carrier("connections", {
        sourceId: sources[first],
        targetId: targets[first],
        sourceOrdinals: directionOrdinals,
        edgeIds: directionOrdinals.map((ordinal) => edgeIds[ordinal])
      }));
    }
  }
  for (let index = 0; index < ids.length; index++) {
    geometry.push(carrier("nodes", { nodeId: ids[index], sourceOrdinal: index }));
  }
  let outside = 0;
  if (domainBounds) {
    for (let index = 0; index < ids.length; index++) {
      if (!spatialDomainAxisContains(domainAxisX, xs[index]) || !spatialDomainAxisContains(domainAxisY, ys[index])) outside++;
    }
  }
  let coincidentCount = 0;
  for (const members of coincident.values()) if (members.length > 1) coincidentCount += members.length;
  const connectionDisplay = record3(parameters.connectionDisplay);
  const encodedValues = connectionDisplay.valueEncoding === "weight" ? weights : connectionDisplay.valueEncoding === "delay" ? delays : [];
  const explicitMissing = countNulls(valueValues) + countNulls(encodedValues);
  const conversions = [
    ...xUnit !== positionUnit ? [conversionText("x", xUnit, positionUnit)] : [],
    ...yUnit !== positionUnit ? [conversionText("y", yUnit, positionUnit)] : []
  ];
  const hasAggregatedPair = encodedValues.length > 0 && totals.size < sources.length;
  const frame3 = record3(positions.frame);
  const uncertainty = record3(parameters.uncertainty);
  return {
    rows: [...nodeRows, ...edgeRows],
    geometry,
    summary: {
      mapLabel: String(parameters.mapLabel ?? parameters.mapId),
      drawnNodeCount: exactText2(ids.length),
      declaredNodeCount: exactText2(universeIds.length),
      positionStatus: String(positions.status),
      frameId: String(frame3.id),
      missingPositionCount: exactText2(universeIds.length - ids.length),
      xAxisLabel: String(frame3.xAxisLabel ?? "x"),
      yAxisLabel: String(frame3.yAxisLabel ?? "y"),
      positionUnit,
      domainStatement: domainBounds ? `closed x [${exactText2(domainBounds.xMin)}, ${exactText2(domainBounds.xMax)}] and y [${exactText2(domainBounds.yMin)}, ${exactText2(domainBounds.yMax)}] ${positionUnit}` : "no domain declared",
      boundaryStatement: domainBounds ? boundary.kind === "periodic" ? `periodic x=${String(boundary.x)} y=${String(boundary.y)} with ${String(boundary.edgeChordRule)} edge chords` : String(boundary.kind) : "not declared",
      minimumImageTieChordCount: exactText2(minimumImageTieChordCount),
      minimumImageTieAxisCount: exactText2(minimumImageTieAxisCount),
      coincidentNodeCount: exactText2(coincidentCount),
      outsideDomainCount: exactText2(outside),
      nodeEncodingStatement: structuredCell3(parameters.nodeEncoding),
      connectionStatement: sources.length === 0 ? "no connections were supplied" : `${sources.length} source connection rows form ${pairs.size} unordered measured endpoint chords; direction groups remain explicit`,
      nodeUniverseStatement: `${universe.complete === true ? "complete" : "incomplete"}; order ${String(universe.order ?? "as_declared")}`,
      scopeStatement: scopeSummary,
      uncertaintyStatement: uncertainty.kind === "none" ? `No uncertainty was supplied (${String(uncertainty.reason ?? "not_provided")}).` : `Uncertainty kind ${String(uncertainty.kind)} was declared.`,
      compactionStatement: "No declared node or connection row was compacted; nodes without positions are omitted and disclosed.",
      tableStatement: `The complete table contains ${universeIds.length} node rows followed by ${sources.length} source-order connection rows.`
    },
    disclosures: baseDisclosureFacts3(request, {
      ...conversions.length > 0 ? { unitConversions: conversions } : {},
      ...explicitMissing > 0 ? { missingValueCount: explicitMissing } : {},
      ...hasAggregatedPair && typeof parameters.multapseAggregation === "string" ? { multapseAggregated: true, multapseAggregation: parameters.multapseAggregation } : {}
    })
  };
}
var SPATIAL_AUTHORITY = defineAuthorityEvaluator(
  authorityEvaluatorId("network.spatial_map_2d", 2),
  (request) => modelFields3(spatialModel(request))
);
function convertedCarrier(value, targetUnit) {
  const node = record3(value);
  const sourceUnit = String(node.unit);
  return nullableNumbers3(node.values).map((entry) => entry === null ? null : sourceUnit === targetUnit ? entry : convert(entry, sourceUnit, targetUnit));
}
function phaseConvergenceFlags(fixed) {
  const residualDx = numbers3(record3(fixed.residualDxDt).values);
  const residualDy = numbers3(record3(fixed.residualDyDt).values);
  const toleranceDx = numbers3(record3(fixed.toleranceDxDt).values);
  const toleranceDy = numbers3(record3(fixed.toleranceDyDt).values);
  const residualDxUnit = String(record3(fixed.residualDxDt).unit);
  const residualDyUnit = String(record3(fixed.residualDyDt).unit);
  const toleranceDxUnit = String(record3(fixed.toleranceDxDt).unit);
  const toleranceDyUnit = String(record3(fixed.toleranceDyDt).unit);
  const within = (residual, tolerance) => residual <= tolerance || residual - tolerance <= 1e-9 * Math.max(
    Math.abs(residual),
    Math.abs(tolerance),
    Number.MIN_VALUE
  );
  return strings3(fixed.ids).map((_id, index) => within(
    Math.abs(convert(residualDx[index], residualDxUnit, toleranceDxUnit)),
    toleranceDx[index]
  ) && within(
    Math.abs(convert(residualDy[index], residualDyUnit, toleranceDyUnit)),
    toleranceDy[index]
  ));
}
function phaseExtents(data, xUnit, yUnit) {
  const trajectories = record3(data.trajectories);
  const field = record3(data.vectorField);
  const nullclines = record3(data.nullclines);
  const fixed = record3(data.fixedPoints);
  const domain = record3(field.domain);
  const domainX = record3(domain.x);
  const domainY = record3(domain.y);
  const finite = (values) => values.filter((value) => value !== null);
  const xs = [
    ...finite(convertedCarrier(trajectories.x, xUnit)),
    ...finite(convertedCarrier(field.x, xUnit)),
    ...finite(convertedCarrier(nullclines.x, xUnit)),
    ...finite(convertedCarrier(fixed.x, xUnit)),
    ...domain.x === void 0 ? [] : [
      convert(Number(domainX.start), String(domainX.unit), xUnit),
      convert(Number(domainX.stop), String(domainX.unit), xUnit)
    ]
  ];
  const ys = [
    ...finite(convertedCarrier(trajectories.y, yUnit)),
    ...finite(convertedCarrier(field.y, yUnit)),
    ...finite(convertedCarrier(nullclines.y, yUnit)),
    ...finite(convertedCarrier(fixed.y, yUnit)),
    ...domain.y === void 0 ? [] : [
      convert(Number(domainY.start), String(domainY.unit), yUnit),
      convert(Number(domainY.stop), String(domainY.unit), yUnit)
    ]
  ];
  return { xExtent: finiteExtrema2(xs), yExtent: finiteExtrema2(ys) };
}
function phaseRows(data, parameters, convergence) {
  const axes = record3(data.axes);
  const xUnit = String(record3(axes.x).unit);
  const yUnit = String(record3(axes.y).unit);
  const trajectories = record3(data.trajectories);
  const field = record3(data.vectorField);
  const nullclines = record3(data.nullclines);
  const fixed = record3(data.fixedPoints);
  const extents = phaseExtents(data, xUnit, yUnit);
  const magnitudeBasis = String(parameters.magnitudeBasis ?? "axis_normalized");
  const speed = (dx, dy, dxUnit, dyUnit) => {
    if (dx === null || dy === null || dxUnit === null || dyUnit === null) return null;
    if (magnitudeBasis === "physical") {
      if (dimensionOf(xUnit) !== dimensionOf(yUnit)) return null;
      const canonicalStateUnit = canonicalUnitFor(xUnit);
      const canonicalTimeUnit = canonicalUnitFor(dxUnit);
      if (!canonicalStateUnit || !canonicalTimeUnit) return null;
      return Math.hypot(
        dx * conversionFactor(xUnit, canonicalStateUnit) * conversionFactor(dxUnit, canonicalTimeUnit),
        dy * conversionFactor(yUnit, canonicalStateUnit) * conversionFactor(dyUnit, canonicalTimeUnit)
      );
    }
    if (!extents.xExtent || !extents.yExtent || !(extents.xExtent.max > extents.xExtent.min) || !(extents.yExtent.max > extents.yExtent.min)) return null;
    return Math.hypot(
      exactBinary64RatioToDifference(dx, extents.xExtent.min, extents.xExtent.max),
      exactBinary64RatioToDifference(
        convert(dy, dyUnit, dxUnit),
        extents.yExtent.min,
        extents.yExtent.max
      )
    );
  };
  const derivativeUnitCell = (dxUnit, dyUnit) => dxUnit === null && dyUnit === null ? null : structuredCell3({
    x: dxUnit === null ? null : `${xUnit} ${dxUnit}`,
    y: dyUnit === null ? null : `${yUnit} ${dyUnit}`,
    magnitudeBasis,
    ...magnitudeBasis === "physical" ? { magnitudeUnit: `${canonicalUnitFor(xUnit) ?? xUnit} ${canonicalUnitFor(dxUnit ?? "") ?? dxUnit}` } : { magnitudeUnit: dxUnit }
  });
  const rows = [];
  if (data.trajectories !== void 0) {
    const pointIds = strings3(trajectories.pointTrajectoryIds);
    const declaredIds = strings3(record3(trajectories.universe).ids);
    const times = numbers3(record3(trajectories.times).values);
    const xs = convertedCarrier(trajectories.x, xUnit);
    const ys = convertedCarrier(trajectories.y, yUnit);
    const dxs = array3(record3(trajectories.dxdt).values);
    const dys = array3(record3(trajectories.dydt).values);
    const dxUnit = trajectories.dxdt === void 0 ? null : String(record3(trajectories.dxdt).unit);
    const dyUnit = trajectories.dydt === void 0 ? null : String(record3(trajectories.dydt).unit);
    for (let index = 0; index < pointIds.length; index++) {
      const rawDx = dxs[index];
      const rawDy = dys[index];
      const dx = typeof rawDx === "number" ? rawDx : null;
      const dy = typeof rawDy === "number" ? rawDy : null;
      rows.push([
        "trajectory_point",
        pointIds[index],
        index,
        times[index],
        xs[index] ?? null,
        ys[index] ?? null,
        dx,
        dy,
        derivativeUnitCell(dxUnit, dyUnit),
        speed(dx, dy, dxUnit, dyUnit),
        null,
        null,
        null
      ]);
    }
    const represented = new Set(pointIds);
    for (const id of declaredIds) {
      if (!represented.has(id)) {
        rows.push(["trajectory_point", id, null, null, null, null, null, null, null, null, null, null, null]);
      }
    }
  }
  if (data.vectorField !== void 0) {
    const xs = convertedCarrier(field.x, xUnit).filter((value) => value !== null);
    const ys = convertedCarrier(field.y, yUnit).filter((value) => value !== null);
    const dxs = numbers3(record3(field.dx).values);
    const dys = numbers3(record3(field.dy).values);
    const dxUnit = String(record3(field.dx).unit);
    const dyUnit = String(record3(field.dy).unit);
    for (let index = 0; index < xs.length; index++) {
      rows.push([
        "field_sample",
        null,
        index,
        null,
        xs[index],
        ys[index],
        dxs[index],
        dys[index],
        derivativeUnitCell(dxUnit, dyUnit),
        speed(dxs[index], dys[index], dxUnit, dyUnit),
        null,
        null,
        null
      ]);
    }
  }
  if (data.nullclines !== void 0) {
    const pointIds = strings3(nullclines.pointCurveIds);
    const xs = convertedCarrier(nullclines.x, xUnit);
    const ys = convertedCarrier(nullclines.y, yUnit);
    const curveIds = strings3(nullclines.curveIds);
    const methods = array3(nullclines.methods).map(record3);
    const methodById = new Map(curveIds.map((id, index) => [id, methods[index]]));
    for (let index = 0; index < pointIds.length; index++) {
      const method = methodById.get(pointIds[index]) ?? {};
      rows.push([
        "nullcline_point",
        pointIds[index],
        index,
        null,
        xs[index] ?? null,
        ys[index] ?? null,
        null,
        null,
        null,
        null,
        String(method.kind),
        structuredCell3(method.residualTolerance),
        null
      ]);
    }
  }
  if (data.fixedPoints !== void 0) {
    const ids = strings3(fixed.ids);
    const xs = convertedCarrier(fixed.x, xUnit).filter((value) => value !== null);
    const ys = convertedCarrier(fixed.y, yUnit).filter((value) => value !== null);
    const methods = strings3(fixed.methods);
    const residualDx = numbers3(record3(fixed.residualDxDt).values);
    const residualDy = numbers3(record3(fixed.residualDyDt).values);
    const toleranceDx = numbers3(record3(fixed.toleranceDxDt).values);
    const toleranceDy = numbers3(record3(fixed.toleranceDyDt).values);
    const residualDxUnit = String(record3(fixed.residualDxDt).unit);
    const residualDyUnit = String(record3(fixed.residualDyDt).unit);
    const toleranceDxUnit = String(record3(fixed.toleranceDxDt).unit);
    const toleranceDyUnit = String(record3(fixed.toleranceDyDt).unit);
    for (let index = 0; index < ids.length; index++) {
      rows.push([
        "fixed_point",
        ids[index],
        index,
        null,
        xs[index],
        ys[index],
        null,
        null,
        null,
        null,
        methods[index],
        structuredCell3({
          dxdt: { value: residualDx[index], unit: residualDxUnit },
          dydt: { value: residualDy[index], unit: residualDyUnit },
          toleranceDxDt: { value: toleranceDx[index], unit: toleranceDxUnit },
          toleranceDyDt: { value: toleranceDy[index], unit: toleranceDyUnit }
        }),
        booleanCell2(convergence[index])
      ]);
    }
  }
  return rows;
}
function orderedPathCarrierOrdinals(ownerIds, ownerId, xs, ys) {
  const runs = [];
  let current = [];
  for (let sourceOrdinal = 0; sourceOrdinal < ownerIds.length; sourceOrdinal++) {
    if (ownerIds[sourceOrdinal] !== ownerId) continue;
    if (xs[sourceOrdinal] === null || ys[sourceOrdinal] === null) {
      if (current.length > 0) runs.push(current);
      current = [];
    } else current.push(sourceOrdinal);
  }
  if (current.length > 0) runs.push(current);
  return [
    ...runs.filter((run) => run.length >= 2).flat(),
    ...runs.filter((run) => run.length === 1).flat()
  ];
}
function phaseModel(requestValue) {
  const request = record3(requestValue);
  const data = record3(request.data);
  const parameters = record3(request.parameters);
  const axes = record3(data.axes);
  const xAxis2 = record3(axes.x);
  const yAxis2 = record3(axes.y);
  const xUnit = String(xAxis2.unit);
  const yUnit = String(yAxis2.unit);
  const trajectories = record3(data.trajectories);
  const field = record3(data.vectorField);
  const nullclines = record3(data.nullclines);
  const fixed = record3(data.fixedPoints);
  const convergence = phaseConvergenceFlags(fixed);
  const rows = phaseRows(data, parameters, convergence);
  const geometry = [];
  if (data.vectorField !== void 0) {
    const dxs = numbers3(record3(field.dx).values);
    for (let index = 0; index < dxs.length; index++) {
      geometry.push(carrier("field_vectors", { sourceOrdinal: index, role: "field_sample" }));
    }
  }
  if (data.nullclines !== void 0) {
    const ids = strings3(nullclines.curveIds);
    const ownerIds = strings3(nullclines.pointCurveIds);
    const xs = nullableNumbers3(record3(nullclines.x).values);
    const ys = nullableNumbers3(record3(nullclines.y).values);
    for (const id of ids) {
      for (const sourceOrdinal of orderedPathCarrierOrdinals(ownerIds, id, xs, ys)) {
        geometry.push(carrier("nullclines", { curveId: id, sourceOrdinal }));
      }
    }
  }
  if (data.trajectories !== void 0) {
    const ids = strings3(record3(trajectories.universe).ids);
    const ownerIds = strings3(trajectories.pointTrajectoryIds);
    const xs = nullableNumbers3(record3(trajectories.x).values);
    const ys = nullableNumbers3(record3(trajectories.y).values);
    for (const id of ids) {
      for (const sourceOrdinal of orderedPathCarrierOrdinals(ownerIds, id, xs, ys)) {
        geometry.push(carrier("trajectories", { trajectoryId: id, sourceOrdinal }));
      }
    }
  }
  if (data.fixedPoints !== void 0) {
    const ids = strings3(fixed.ids);
    for (let index = 0; index < ids.length; index++) {
      geometry.push(carrier("fixed_points", { fixedPointId: ids[index], sourceOrdinal: index }));
    }
  }
  const conversions = [];
  const noteConversion = (label, value, target) => {
    if (value === void 0) return;
    const source = String(record3(value).unit);
    if (source !== target) conversions.push(conversionText(label, source, target));
  };
  noteConversion("trajectory x", trajectories.x, xUnit);
  noteConversion("trajectory y", trajectories.y, yUnit);
  noteConversion("vector-field x", field.x, xUnit);
  noteConversion("vector-field y", field.y, yUnit);
  noteConversion("nullcline x", nullclines.x, xUnit);
  noteConversion("nullcline y", nullclines.y, yUnit);
  noteConversion("fixed-point x", fixed.x, xUnit);
  noteConversion("fixed-point y", fixed.y, yUnit);
  const trajectoryXs = nullableNumbers3(record3(trajectories.x).values);
  const trajectoryYs = nullableNumbers3(record3(trajectories.y).values);
  const nullclineXs = nullableNumbers3(record3(nullclines.x).values);
  const nullclineYs = nullableNumbers3(record3(nullclines.y).values);
  let missingPointCount = 0;
  for (let index = 0; index < trajectoryXs.length; index++) {
    if (trajectoryXs[index] === null || trajectoryYs[index] === null) missingPointCount++;
  }
  for (let index = 0; index < nullclineXs.length; index++) {
    if (nullclineXs[index] === null || nullclineYs[index] === null) missingPointCount++;
  }
  const times = numbers3(record3(trajectories.times).values);
  const fieldDomain = record3(field.domain);
  const domainX = record3(fieldDomain.x);
  const domainY = record3(fieldDomain.y);
  const timeDirection = data.trajectories === void 0 ? "not supplied" : String(trajectories.timeDirection);
  const uncertainty = record3(parameters.uncertainty);
  return {
    rows,
    geometry,
    summary: {
      yLabel: String(yAxis2.label ?? "y"),
      yUnit,
      xLabel: String(xAxis2.label ?? "x"),
      xUnit,
      trajectoryCount: exactText2(strings3(record3(trajectories.universe).ids).length),
      trajectoryPointCount: exactText2(strings3(trajectories.pointTrajectoryIds).length),
      timeStart: times.length === 0 ? "not supplied" : exactText2(times[0]),
      timeStop: times.length === 0 ? "not supplied" : exactText2(times[times.length - 1]),
      timeUnit: times.length === 0 ? "not supplied" : String(record3(trajectories.times).unit),
      timeDirection,
      fieldSampleCount: exactText2(numbers3(record3(field.x).values).length),
      latticeKind: data.vectorField === void 0 ? "not supplied" : String(record3(field.lattice).kind),
      xDomainStart: field.domain === void 0 ? "not supplied" : exactText2(convert(Number(domainX.start), String(domainX.unit), xUnit)),
      xDomainStop: field.domain === void 0 ? "not supplied" : exactText2(convert(Number(domainX.stop), String(domainX.unit), xUnit)),
      yDomainStart: field.domain === void 0 ? "not supplied" : exactText2(convert(Number(domainY.start), String(domainY.unit), yUnit)),
      yDomainStop: field.domain === void 0 ? "not supplied" : exactText2(convert(Number(domainY.stop), String(domainY.unit), yUnit)),
      arrowScalingMode: data.vectorField === void 0 ? "not supplied" : String(record3(parameters.arrowScaling).mode),
      magnitudeBasis: data.vectorField === void 0 ? "not supplied" : String(parameters.magnitudeBasis),
      nullclineCount: exactText2(strings3(nullclines.curveIds).length),
      fixedPointCount: exactText2(strings3(fixed.ids).length),
      missingStatement: missingPointCount === 0 ? "No supplied trajectory or nullcline point has a missing coordinate." : `${missingPointCount} supplied path points have a missing coordinate and break their path; missing is never zero.`,
      uncertaintyStatement: uncertainty.kind === "none" ? `No uncertainty was supplied (${String(uncertainty.reason ?? "not_provided")}).` : `Uncertainty kind ${String(uncertainty.kind)} was declared.`
    },
    disclosures: baseDisclosureFacts3(request, {
      ...conversions.length > 0 ? { unitConversions: conversions } : {},
      ...missingPointCount > 0 ? { missingValueCount: missingPointCount } : {}
    })
  };
}
var PHASE_AUTHORITY = defineAuthorityEvaluator(
  authorityEvaluatorId("neuro.phase_plane", 2),
  (request) => modelFields3(phaseModel(request))
);
function responseEstimate(values, estimator, trimFraction) {
  if (values.length === 0) return null;
  if (estimator === "mean") return exactBinary64Mean(values);
  const ordered = [...values].sort((left, right) => left - right);
  if (estimator === "median") {
    const middle = Math.floor(ordered.length / 2);
    return ordered.length % 2 === 1 ? ordered[middle] : exactBinary64Mean([ordered[middle - 1], ordered[middle]]);
  }
  const trim = floorExactBinary64TimesSafeInteger(trimFraction, ordered.length);
  return exactBinary64Mean(ordered.slice(trim, ordered.length - trim));
}
function independentResponseCurve(data, parameters, response) {
  const conditions = record3(data.conditions);
  const axis = String(conditions.axis);
  const conditionIds = strings3(conditions.ids);
  const labels = strings3(conditions.labels);
  const input = record3(conditions.input);
  const inputs = numbers3(input.values);
  const order = conditionIds.map((_id, index) => index);
  if (axis === "numeric") {
    order.sort((left, right) => inputs[left] - inputs[right] || left - right);
  }
  const displayOrdinal = /* @__PURE__ */ new Map();
  order.forEach((declared, display) => displayOrdinal.set(declared, display));
  const indexById = new Map(conditionIds.map((id, index) => [id, index]));
  const estimator = String(parameters.estimator);
  const responseMethod = String(response.method);
  const mode = String(data.mode);
  const observations = record3(data.observations);
  const aggregates = record3(data.aggregates);
  const rawResponses = nullableNumbers3(response.values).map((value) => value === 0 ? 0 : value);
  const repeatsByCondition = conditionIds.map(() => []);
  const peakCounts = nullableNumbers3(record3(response.audit).peakBinCounts);
  const peakAuditPresent = record3(response.audit).peakBinCounts !== void 0;
  const basis = record3(response.basis);
  const eventScope = verifyResponseEventScope(data.eventScope);
  if (!eventScope.ok) throw new Error("validated response event scope was not independently recognized");
  const rate = responseMethod === "mean_firing_rate" || responseMethod === "peak_firing_rate" ? verifyResponseRateAuthority(response.rateNormalization, data.eventScope) : null;
  const hasBinnedPeakAudit = peakAuditPresent && mode === "repeats" && responseMethod === "peak_firing_rate" && basis.estimator === "binned_count" && peakCounts.length === rawResponses.length && rate?.ok === true;
  if (mode === "repeats") {
    const ownerIds = strings3(observations.conditionIds);
    const repeatIds = strings3(observations.repeatIds);
    for (let sourceOrdinal = 0; sourceOrdinal < rawResponses.length; sourceOrdinal++) {
      const declared = indexById.get(ownerIds[sourceOrdinal]);
      if (declared === void 0) throw new Error("validated repeat references an unknown condition");
      repeatsByCondition[declared].push({
        conditionId: ownerIds[sourceOrdinal],
        repeatId: repeatIds[sourceOrdinal],
        response: rawResponses[sourceOrdinal],
        sourceOrdinal,
        estimatorRole: rawResponses[sourceOrdinal] === null ? "undefined" : "retained"
      });
    }
    for (const rows of repeatsByCondition) {
      rows.sort((left, right) => compareUtf16CodeUnits(left.repeatId, right.repeatId) || left.sourceOrdinal - right.sourceOrdinal);
    }
  }
  const results = [];
  let attemptedCount = 0;
  let retainedCount = 0;
  let trimmedCount = 0;
  let excludedCount = 0;
  let conditionsWithoutEstimate = 0;
  const aggregateSamples = nullableNumbers3(aggregates.sampleCounts);
  const aggregateExcluded = numbers3(aggregates.excludedCounts);
  const aggregateTrimmed = numbers3(aggregates.trimmedCounts);
  for (const declaredOrdinal of order) {
    const rows = repeatsByCondition[declaredOrdinal];
    let estimate;
    let sampleCount;
    let retained;
    let trimmed;
    let excluded;
    let attempted;
    let resultRows;
    if (mode === "repeats") {
      const defined = rows.filter((row) => row.response !== null).sort((left, right) => {
        const leftValue = hasBinnedPeakAudit ? peakCounts[left.sourceOrdinal] : left.response;
        const rightValue = hasBinnedPeakAudit ? peakCounts[right.sourceOrdinal] : right.response;
        return leftValue - rightValue || compareUtf16CodeUnits(left.repeatId, right.repeatId) || left.sourceOrdinal - right.sourceOrdinal;
      });
      const trimPerTail = estimator === "trimmed_mean" ? floorExactBinary64TimesSafeInteger(Number(parameters.trimFraction), defined.length) : 0;
      const roleByOrdinal = /* @__PURE__ */ new Map();
      for (let index = 0; index < defined.length; index++) {
        roleByOrdinal.set(
          defined[index].sourceOrdinal,
          index < trimPerTail ? "trimmed_low" : index >= defined.length - trimPerTail ? "trimmed_high" : "retained"
        );
      }
      resultRows = rows.map((row) => ({
        ...row,
        estimatorRole: row.response === null ? "undefined" : roleByOrdinal.get(row.sourceOrdinal)
      }));
      retained = defined.length - 2 * trimPerTail;
      trimmed = 2 * trimPerTail;
      excluded = rows.length - defined.length;
      attempted = rows.length;
      sampleCount = retained;
      if (hasBinnedPeakAudit) {
        const orderedCounts = defined.map((row) => peakCounts[row.sourceOrdinal]);
        let estimatorCounts;
        if (estimator === "median") {
          const middle = Math.floor(orderedCounts.length / 2);
          estimatorCounts = orderedCounts.length === 0 ? [] : orderedCounts.length % 2 === 1 ? [orderedCounts[middle]] : [orderedCounts[middle - 1], orderedCounts[middle]];
        } else {
          estimatorCounts = orderedCounts.slice(trimPerTail, orderedCounts.length - trimPerTail);
        }
        if (estimatorCounts.length === 0) estimate = null;
        else {
          let total = 0n;
          for (const count of estimatorCounts) total += BigInt(count);
          const width = record3(basis.binWidth);
          estimate = deriveExactAggregateCountRateInUnit(
            total,
            rate.integerDivisor,
            estimatorCounts.length,
            Number(width.value),
            String(width.unit),
            String(response.unit)
          );
        }
      } else {
        estimate = responseEstimate(
          defined.map((row) => row.response),
          estimator,
          typeof parameters.trimFraction === "number" ? parameters.trimFraction : void 0
        );
      }
    } else {
      estimate = rawResponses[declaredOrdinal];
      sampleCount = aggregateSamples[declaredOrdinal];
      retained = sampleCount ?? 0;
      trimmed = aggregateTrimmed[declaredOrdinal] ?? 0;
      excluded = aggregateExcluded[declaredOrdinal];
      attempted = retained + trimmed + excluded;
      resultRows = [];
    }
    attemptedCount += attempted;
    retainedCount += retained;
    trimmedCount += trimmed;
    excludedCount += excluded;
    if (estimate === null) conditionsWithoutEstimate++;
    results.push({
      conditionId: conditionIds[declaredOrdinal],
      conditionLabel: labels[declaredOrdinal] ?? conditionIds[declaredOrdinal],
      input: axis === "numeric" ? inputs[declaredOrdinal] : null,
      displayOrdinal: displayOrdinal.get(declaredOrdinal),
      estimate,
      sampleCount,
      retainedCount: retained,
      trimmedCount: trimmed,
      excludedCount: excluded,
      attemptedCount: attempted,
      repeats: resultRows
    });
  }
  return {
    axis,
    inputScale: axis === "numeric" ? String(input.scale ?? "linear") : null,
    conditions: results,
    attemptedCount,
    retainedCount,
    trimmedCount,
    excludedCount,
    conditionsWithoutEstimate
  };
}
function responseRateText(authority) {
  if (authority.normalization === "single_train_rate") {
    return "single_train_rate (one event train; integer divisor 1)";
  }
  const count = authority.eventScope.recordedSenderCount;
  if (authority.normalization === "total_event_rate") {
    return `total_event_rate (pooled total over ${count} recorded sender${count === 1 ? "" : "s"}; integer divisor 1)`;
  }
  return `mean_rate_per_recorded_sender (pooled total divided by ${count} recorded sender${count === 1 ? "" : "s"})`;
}
function responseMembershipText(authority) {
  if (authority.membershipKind === "single_train_selection_rule") {
    return "single_train_selection_rule (source composition not bound)";
  }
  const binding = record3(authority.normalizedScope.membershipBinding);
  if (authority.membershipKind === "explicit_sender_ids") {
    const ids = strings3(binding.senderIds);
    return `explicit_sender_ids (${ids.length} unique ids; ${RESPONSE_EVENT_MEMBERSHIP_CANONICALIZATION_ID}; canonical membership digest ${responseEventMembershipDigest(ids)})`;
  }
  if (authority.membershipKind === "canonical_sender_ids_digest") {
    return `canonical_sender_ids_digest (${String(binding.canonicalization)}; ${String(binding.digest)}; preimage unavailable)`;
  }
  return "cardinality_only (member identities not bound)";
}
function responseScopeText(authority) {
  const cardinality = authority.recordedSenderCount === null ? `${authority.selectedEventTrainCount} selected event train; no recorded-sender cardinality declared` : `${authority.selectedEventTrainCount} selected sender event train${authority.selectedEventTrainCount === 1 ? "" : "s"} (including silent senders)`;
  return `${authority.kind} selection ${authority.selectionId}; ${cardinality}; ${responseMembershipText(authority)}; declared pooling ${authority.poolingOperator}; declared completeness ${authority.eventCompleteness}`;
}
function peakSupportText(value) {
  const support = record3(value);
  if (support.kind === "analytic_infinite") return "analytic infinite support";
  if (support.kind === "finite_full_width") {
    return `finite full-width support, ${String(support.supportBoundary)} boundary`;
  }
  if (support.kind === "finite_cutoff") {
    const cutoff = record3(support.cutoff);
    return `finite ${String(support.geometry)} cutoff ${exactText2(Number(cutoff.value))} ${String(cutoff.unit ?? "")}, ${String(support.cutoffBoundary)}, ${String(support.tailPolicy)}`;
  }
  return "unrecognized support";
}
function peakEvaluationText(value) {
  const evaluation = record3(value);
  if (evaluation.mode === "continuous_supremum") {
    return `continuous supremum over ${String(evaluation.domain)} ${String(evaluation.boundary)}`;
  }
  const step = record3(evaluation.step);
  return `sampled grid of ${String(evaluation.sampleCount)} points from measurement-window start, step ${exactText2(Number(step.value ?? 0))} ${String(step.unit ?? "")}, ${String(evaluation.boundary)}, stop excluded, ${String(evaluation.tilingPolicy)}, partial steps ${String(evaluation.partialStepPolicy)}`;
}
function peakBasisText(value) {
  const basis = record3(value);
  if (basis.estimator === "binned_count") {
    const width = record3(basis.binWidth);
    return `binned-count peak over ${String(basis.binCount)} bins with exact physical exposure ${exactText2(Number(width.value))} ${String(width.unit ?? "")} verified for every emitted interval, origin ${String(basis.origin)}, ${String(basis.boundary)}, ${String(basis.tilingPolicy)}, partial bins ${String(basis.partialBinPolicy)}`;
  }
  const bandwidth = record3(basis.bandwidth);
  return `${String(basis.shape)} ${String(basis.kernelForm)} kernel, ${String(basis.bandwidthDefinition)} ${exactText2(Number(bandwidth.value ?? 0))} ${String(bandwidth.unit ?? "")}, ${peakSupportText(basis.support)}, ${String(basis.normalization)}, ${String(basis.evaluationOperator)}, edge policy ${String(basis.edgePolicy)}, ${peakEvaluationText(basis.evaluation)}`;
}
function responseBasisText(data, parameters, response, authority, rate) {
  const window = record3(data.measurementWindow);
  const boundary = String(window.boundary ?? "[start,stop)");
  const windowBasis = `${boundary} ${exactText2(Number(window.start))} to ${exactText2(Number(window.stop))} ${String(window.unit)}`;
  const scope = responseScopeText(authority);
  const rateText = rate ? responseRateText(rate) : null;
  const method = String(response.method);
  const values = nullableNumbers3(response.values);
  const auditRecord = record3(response.audit);
  const auditPresent = auditRecord.peakBinCounts !== void 0;
  const audit = nullableNumbers3(auditRecord.peakBinCounts);
  let definedAudit = 0;
  for (const count of audit) if (count !== null) definedAudit++;
  const definedValues = values.length - values.filter((value) => value === null).length;
  if (method === "first_spike_latency") {
    return `caller-declared event scope ${scope}; latency is the first selected spike (the minimum over the superposed union for a pooled scope) from ${String(response.latencyReference ?? "unspecified reference")}; measurement window ${windowBasis}`;
  }
  if (method === "peak_firing_rate") {
    if (auditPresent) {
      return definedAudit > 0 ? `caller-declared event scope ${scope}; caller-supplied exact max-bin counts after the declared sender pooling; defined repeat rates and defined ${String(parameters.estimator)} condition estimates re-derived at count level with one final rounding; ${String(rateText)}; ${peakBasisText(response.basis)}; measurement window ${windowBasis}` : `caller-declared event scope ${scope}; caller-supplied peak-bin count audit contained only nulls; null-mask alignment was verified, but no repeat rate or condition estimate existed to re-derive; ${String(rateText)}; ${peakBasisText(response.basis)}; measurement window ${windowBasis}`;
    }
    return definedValues > 0 ? `caller-declared event scope ${scope}; caller-supplied peak value after the declared sender pooling; ${String(rateText)}; ${peakBasisText(response.basis)}; measurement window ${windowBasis}` : `caller-declared event scope ${scope}; no defined peak value was supplied; ${String(rateText)}; ${peakBasisText(response.basis)}; measurement window ${windowBasis}`;
  }
  return rateText ? `caller-declared event scope ${scope}; rate normalization ${rateText}; measurement window ${windowBasis}` : `caller-declared event scope ${scope}; measurement window ${windowBasis}`;
}
function responseModel(requestValue) {
  const request = record3(requestValue);
  const data = record3(request.data);
  const parameters = record3(request.parameters);
  const mode = String(data.mode);
  const conditions = record3(data.conditions);
  const input = record3(conditions.input);
  const response = record3(mode === "repeats" ? record3(data.observations).response : record3(data.aggregates).response);
  const curve = independentResponseCurve(data, parameters, response);
  const eventScopeResult = verifyResponseEventScope(data.eventScope);
  if (!eventScopeResult.ok) throw new Error("validated response event scope was not independently recognized");
  const eventScope = eventScopeResult.authority;
  const method = String(response.method);
  const rateResult = method === "mean_firing_rate" || method === "peak_firing_rate" ? verifyResponseRateAuthority(response.rateNormalization, data.eventScope) : null;
  if (rateResult !== null && !rateResult.ok) {
    throw new Error("validated response rate normalization was not independently recognized");
  }
  const rate = rateResult?.ok === true ? rateResult : null;
  const responseBasis = responseBasisText(data, parameters, response, eventScope, rate);
  const responseUnit = String(response.unit);
  const inputUnit = curve.axis === "numeric" ? String(input.unit) : null;
  const auditPeakCounts = nullableNumbers3(record3(response.audit).peakBinCounts);
  let definedPeakCount = 0;
  for (const count of auditPeakCounts) if (count !== null) definedPeakCount++;
  const hasDefinedPeakAudit = definedPeakCount > 0;
  const rows = [];
  for (const condition of curve.conditions) {
    if (mode === "repeats") {
      for (const repeat of condition.repeats) {
        rows.push([
          condition.conditionId,
          condition.conditionLabel,
          condition.input,
          inputUnit,
          repeat.repeatId,
          repeat.response,
          responseUnit,
          method,
          rate?.normalization ?? null,
          eventScope.recordedSenderCount,
          booleanCell2(repeat.response === null),
          null,
          null,
          null,
          responseBasis,
          null,
          null,
          null,
          null,
          null,
          repeat.estimatorRole,
          null,
          auditPeakCounts[repeat.sourceOrdinal] ?? null,
          repeat.response !== null && hasDefinedPeakAudit ? "exact_peak_bin_count_to_condition_estimator_rate_one_round" : null,
          eventScope.kind,
          eventScope.selectionId,
          responseMembershipText(eventScope),
          eventScope.selectedEventTrainCount
        ]);
      }
    }
    rows.push([
      condition.conditionId,
      condition.conditionLabel,
      condition.input,
      inputUnit,
      null,
      condition.estimate,
      responseUnit,
      method,
      rate?.normalization ?? null,
      eventScope.recordedSenderCount,
      booleanCell2(condition.estimate === null),
      String(parameters.estimator),
      condition.sampleCount,
      condition.excludedCount,
      responseBasis,
      String(record3(parameters.uncertainty).kind ?? "none"),
      null,
      null,
      null,
      null,
      "aggregate_estimate",
      condition.trimmedCount,
      null,
      condition.estimate !== null && hasDefinedPeakAudit ? "exact_peak_bin_count_to_condition_estimator_rate_one_round" : null,
      eventScope.kind,
      eventScope.selectionId,
      responseMembershipText(eventScope),
      eventScope.selectedEventTrainCount
    ]);
  }
  const geometry = [];
  for (const condition of curve.conditions) {
    if (condition.estimate === null) {
      geometry.push(carrier("conditions", {
        conditionId: condition.conditionId,
        displayOrdinal: condition.displayOrdinal,
        role: "undefined_condition"
      }));
    }
  }
  if (curve.axis !== "nominal") {
    let run = [];
    const runs = [];
    for (const condition of curve.conditions) {
      if (condition.estimate === null) {
        if (run.length >= 2) runs.push(run);
        run = [];
      } else run.push(condition);
    }
    if (run.length >= 2) runs.push(run);
    for (const path of runs) {
      for (const condition of path) {
        geometry.push(carrier("series_paths", {
          conditionId: condition.conditionId,
          displayOrdinal: condition.displayOrdinal,
          role: "ordered_guide_vertex"
        }));
      }
    }
  }
  for (const condition of curve.conditions) {
    if (condition.estimate !== null) {
      geometry.push(carrier("conditions", {
        conditionId: condition.conditionId,
        displayOrdinal: condition.displayOrdinal,
        role: "condition_estimate"
      }));
    }
  }
  const estimates = curve.conditions.map((condition) => condition.estimate).filter((value) => value !== null);
  const extent = finiteExtrema2(estimates);
  const window = record3(data.measurementWindow);
  const uncertainty = record3(parameters.uncertainty);
  const aggregateCounts = curve.conditions.map((condition) => condition.retainedCount).filter((count) => count > 0);
  let aggregateSampleBase = "0 retained in every condition";
  if (aggregateCounts.length > 0) {
    const aggregateExtent = finiteExtrema2(aggregateCounts);
    aggregateSampleBase = aggregateExtent.min === aggregateExtent.max ? exactText2(aggregateExtent.min) : `${aggregateExtent.min}-${aggregateExtent.max} by condition`;
  }
  const aggregateSampleCount = curve.conditionsWithoutEstimate > 0 && aggregateCounts.length > 0 ? `${aggregateSampleBase} where defined; ${curve.conditionsWithoutEstimate} condition${curve.conditionsWithoutEstimate === 1 ? "" : "s"} undefined` : aggregateSampleBase;
  const inputLabel = curve.axis === "numeric" ? String(input.label ?? "input") : String(conditions.inputLabel ?? "condition");
  const lineStatement = curve.axis === "nominal" ? "No guide line is drawn because the condition axis is nominal." : "A straight guide joins only adjacent defined conditions; gaps break it, and it is neither a fit nor interpolation.";
  return {
    rows,
    geometry,
    summary: {
      curveLabel: String(parameters.curveLabel ?? parameters.curveId ?? method.replaceAll("_", " ")),
      responseMethod: method,
      responseUnit,
      inputLabel,
      inputUnit: inputUnit ?? "not applicable",
      inputScale: curve.inputScale ?? curve.axis,
      conditionCount: exactText2(curve.conditions.length),
      axisKind: curve.axis,
      eventScopeKind: eventScope.kind,
      eventSelectionId: eventScope.selectionId,
      eventMembershipBinding: responseMembershipText(eventScope),
      selectedEventTrainCount: exactText2(eventScope.selectedEventTrainCount),
      recordedSenderCount: eventScope.recordedSenderCount === null ? "not declared" : exactText2(eventScope.recordedSenderCount),
      rateNormalization: rate?.normalization ?? "not applicable",
      estimator: String(parameters.estimator),
      retainedCount: exactText2(curve.retainedCount),
      attemptedCount: exactText2(curve.attemptedCount),
      trimmedCount: exactText2(curve.trimmedCount),
      excludedCount: exactText2(curve.excludedCount),
      repeatDesign: String(parameters.repeatDesign),
      responseBasis,
      windowStart: exactText2(Number(window.start)),
      windowStop: exactText2(Number(window.stop)),
      timeUnit: String(window.unit),
      responseMin: extent ? exactText2(extent.min) : "undefined",
      responseMax: extent ? exactText2(extent.max) : "undefined",
      uncertaintyStatement: uncertainty.kind === "none" ? `No uncertainty was supplied (${String(uncertainty.reason ?? "not_provided")}).` : `Uncertainty kind ${String(uncertainty.kind)} was declared.`,
      aggregationStatement: mode === "aggregates" ? "Only per-condition aggregates were supplied; raw repeats and pairing cannot be inspected." : "Every attempted raw repeat is retained in deterministic condition and repeat order in the complete table.",
      lineStatement
    },
    disclosures: baseDisclosureFacts3(request, {
      ...curve.excludedCount > 0 ? { missingValueCount: curve.excludedCount } : {},
      eventScopeMembershipCardinalityOnly: eventScope.membershipKind === "cardinality_only",
      eventScopeExternalAuthorityDeclared: true,
      ...mode === "aggregates" ? {
        aggregateEstimator: String(parameters.estimator),
        aggregateSampleCount
      } : {}
    })
  };
}
var RESPONSE_AUTHORITY = defineAuthorityEvaluator(
  authorityEvaluatorId("neuro.response_curve", 2),
  (request) => modelFields3(responseModel(request))
);
var TOPOLOGY_DYNAMICS_AUTHORITY_EVALUATORS = [
  GRAPH_AUTHORITY,
  SPATIAL_AUTHORITY,
  PHASE_AUTHORITY,
  RESPONSE_AUTHORITY
];

// src/authority/evaluators/traces.ts
function record4(value) {
  return value !== null && value !== void 0 && typeof value === "object" && !Array.isArray(value) ? value : {};
}
function array4(value) {
  return Array.isArray(value) ? value : [];
}
function numberArray(value) {
  return array4(value).map((entry) => Number(entry));
}
function nullableNumberArray(value) {
  return array4(value).map((entry) => entry === null ? null : Number(entry));
}
function stringArray(value) {
  return array4(value).map(String);
}
function canonicalZero4(value) {
  return value === 0 ? 0 : value;
}
function sequenceMinimum(values) {
  if (values.length === 0) throw new Error("minimum of an empty sequence is undefined");
  let result = values[0];
  for (let index = 1; index < values.length; index++) result = Math.min(result, values[index]);
  return result;
}
function sequenceMaximum(values) {
  if (values.length === 0) throw new Error("maximum of an empty sequence is undefined");
  let result = values[0];
  for (let index = 1; index < values.length; index++) result = Math.max(result, values[index]);
  return result;
}
function sequenceMaximumAbsolute(values) {
  if (values.length === 0) throw new Error("maximum magnitude of an empty sequence is undefined");
  let result = Math.abs(values[0]);
  for (let index = 1; index < values.length; index++) result = Math.max(result, Math.abs(values[index]));
  return result;
}
function windowFrom(value, targetUnit) {
  const source = record4(value);
  const sourceUnit = String(source.unit);
  const unit = targetUnit ?? sourceUnit;
  return {
    start: sourceUnit === unit ? Number(source.start) : convert(Number(source.start), sourceUnit, unit),
    stop: sourceUnit === unit ? Number(source.stop) : convert(Number(source.stop), sourceUnit, unit),
    unit,
    finalEdgeInclusive: typeof source.boundary === "string" && source.boundary.endsWith("]")
  };
}
function inWindow(value, window) {
  return value >= window.start && (window.finalEdgeInclusive ? value <= window.stop : value < window.stop);
}
function duplicateOptions(parameters) {
  const node = parameters.duplicateTimePolicy;
  const structured = record4(node);
  const duplicatePolicy = typeof node === "string" ? node : structured.policy;
  const nestedAggregate = structured.aggregate;
  const aggregateMethod = parameters.aggregateMethod ?? parameters.duplicateTimeAggregate ?? structured.method ?? (typeof nestedAggregate === "string" ? nestedAggregate : record4(nestedAggregate).method);
  return {
    ...duplicatePolicy === void 0 ? {} : { duplicatePolicy },
    ...aggregateMethod === void 0 ? {} : { aggregateMethod }
  };
}
function independentAggregate(values, method) {
  const present = values.filter((value) => value !== null);
  if (present.length === 0) return null;
  if (method === "mean") return canonicalZero4(exactBinary64Mean(present));
  if (method === "min") return canonicalZero4(sequenceMinimum(present));
  if (method === "max") return canonicalZero4(sequenceMaximum(present));
  const sorted = [...present].sort((left, right) => left - right);
  const middle = Math.floor(sorted.length / 2);
  return canonicalZero4(sorted.length % 2 === 1 ? sorted[middle] : exactBinary64Mean([sorted[middle - 1], sorted[middle]]));
}
function stableCenteredRatio2(value, center, denominator) {
  const direct = value - center;
  if (Number.isFinite(direct)) return direct / denominator;
  const scale = Math.max(Math.abs(value), Math.abs(center), Math.abs(denominator));
  return (value / scale - center / scale) / (denominator / scale);
}
function zCoordinates(values) {
  const reference = sequenceMinimum(values);
  const differences = values.map((value) => value - reference);
  const differenceScale = sequenceMaximumAbsolute(differences);
  if (differenceScale > 0 && Number.isFinite(differenceScale) && differences.every(Number.isFinite)) {
    return {
      mode: "centered_difference",
      reference,
      scale: differenceScale,
      values: differences.map((value) => value / differenceScale)
    };
  }
  const scale = sequenceMaximumAbsolute(values);
  return {
    mode: "scaled_value",
    reference: 0,
    scale,
    values: values.map((value) => value / scale)
  };
}
function normalizeValue(value, receipt, dispersion = false) {
  if (receipt.method === "z_score") {
    if (receipt.zInputScale && receipt.zCoordinateSampleStandardDeviation) {
      if (dispersion) {
        return value / receipt.zInputScale / receipt.zCoordinateSampleStandardDeviation;
      }
      const coordinate = receipt.zCoordinateMode === "centered_difference" ? stableCenteredRatio2(value, receipt.zCoordinateReference, receipt.zInputScale) : value / receipt.zInputScale;
      return stableCenteredRatio2(
        coordinate,
        receipt.zCoordinateMean,
        receipt.zCoordinateSampleStandardDeviation
      );
    }
    return dispersion ? value / receipt.sampleStandardDeviation : stableCenteredRatio2(value, receipt.center, receipt.sampleStandardDeviation);
  }
  if (receipt.method === "min_max") {
    return exactBinary64AffineFraction(
      value,
      dispersion ? 0 : receipt.minimum,
      receipt.minimum,
      receipt.maximum
    );
  }
  return receipt.baselineSumMinSubnormalUnits ? exactBinary64RatioToMean(value, receipt.baselineSumMinSubnormalUnits, receipt.basisCount) : value / receipt.baselineMean;
}
function normalizeSamples(observations, declaration) {
  const basis = observations.filter((observation) => inWindow(observation.time, declaration.statisticsWindow)).map((observation) => observation.value).filter((value) => value !== null);
  if (declaration.method === "z_score") {
    const coordinate = zCoordinates(basis);
    const coordinateMean = roundedBinary64Mean(coordinate.values);
    const sumSquares = exactBinary64Sum(coordinate.values.map((value) => {
      const difference = value - coordinateMean;
      return difference * difference;
    }));
    const coordinateDeviation = Math.sqrt(sumSquares / (basis.length - 1));
    const deviation = coordinate.scale * coordinateDeviation;
    const receipt2 = {
      method: declaration.method,
      statisticsWindow: declaration.statisticsWindow,
      basisCount: basis.length,
      center: roundedBinary64Mean(basis),
      ...deviation > 0 && Number.isFinite(deviation) ? { sampleStandardDeviation: deviation } : {},
      zCoordinateMode: coordinate.mode,
      zCoordinateReference: coordinate.reference,
      zInputScale: coordinate.scale,
      zCoordinateMean: coordinateMean,
      zCoordinateSampleStandardDeviation: coordinateDeviation
    };
    return {
      receipt: receipt2,
      values: observations.map((observation) => observation.value === null ? null : normalizeValue(observation.value, receipt2))
    };
  }
  if (declaration.method === "min_max") {
    const receipt2 = {
      method: declaration.method,
      statisticsWindow: declaration.statisticsWindow,
      basisCount: basis.length,
      minimum: sequenceMinimum(basis),
      maximum: sequenceMaximum(basis)
    };
    return {
      receipt: receipt2,
      values: observations.map((observation) => observation.value === null ? null : normalizeValue(observation.value, receipt2))
    };
  }
  const units = exactBinary64SumUnits(basis);
  const mean2 = roundedBinary64Mean(basis);
  const receipt = {
    method: declaration.method,
    statisticsWindow: declaration.statisticsWindow,
    basisCount: basis.length,
    ...mean2 > 0 && Number.isFinite(mean2) ? { baselineMean: mean2 } : {},
    baselineSumMinSubnormalUnits: units
  };
  return {
    receipt,
    values: observations.map((observation) => observation.value === null ? null : normalizeValue(observation.value, receipt))
  };
}
function prepareIndependentTrace(input, options) {
  const sorted = input.times.map((rawTime, ordinal) => {
    const recordedTime = canonicalZero4(rawTime);
    const sourceValue = input.values[ordinal];
    const convertedTime = input.timeUnit === options.window.unit ? recordedTime : convert(recordedTime, input.timeUnit, options.window.unit);
    const time = input.timeOffset ? convertExactUnitSum([
      { value: recordedTime, unit: input.timeUnit },
      { value: canonicalZero4(input.timeOffset.value), unit: input.timeOffset.unit }
    ], options.window.unit) : convertedTime;
    return {
      recordedTime,
      time: canonicalZero4(time),
      sourceValue: sourceValue === null ? null : canonicalZero4(sourceValue),
      ordinal
    };
  }).sort((left, right) => left.recordedTime - right.recordedTime || left.ordinal - right.ordinal);
  const resolved = [];
  let duplicateGroups = 0;
  for (let first = 0; first < sorted.length; ) {
    let after = first + 1;
    while (after < sorted.length && sorted[after].recordedTime === sorted[first].recordedTime) after++;
    const tied = sorted.slice(first, after);
    if (tied.length > 1) duplicateGroups++;
    if (tied.length > 1 && options.duplicatePolicy === "aggregate") {
      resolved.push({
        recordedTime: tied[0].recordedTime,
        time: tied[0].time,
        sourceValues: tied.map((entry) => entry.sourceValue),
        sourceOrdinals: tied.map((entry) => entry.ordinal),
        value: independentAggregate(tied.map((entry) => entry.sourceValue), options.aggregateMethod),
        replicateCount: tied.length
      });
    } else {
      for (const entry of tied) {
        resolved.push({
          recordedTime: entry.recordedTime,
          time: entry.time,
          sourceValues: [entry.sourceValue],
          sourceOrdinals: [entry.ordinal],
          value: entry.sourceValue,
          replicateCount: 1
        });
      }
    }
    first = after;
  }
  const targetValueUnit = options.normalization ? "1" : options.targetValueUnit ?? input.valueUnit;
  const converted2 = resolved.map((observation) => ({
    ...observation,
    value: observation.value === null || options.normalization || input.valueUnit === targetValueUnit ? observation.value : canonicalZero4(convert(observation.value, input.valueUnit, targetValueUnit))
  }));
  const normalized = options.normalization ? normalizeSamples(converted2, options.normalization) : void 0;
  const transformed = converted2.map((observation, index) => ({
    ...observation,
    value: normalized ? normalized.values[index] : observation.value
  }));
  let excludedBelow = 0;
  let excludedAbove = 0;
  const retained = [];
  for (const observation of transformed) {
    if (observation.time < options.window.start) excludedBelow += observation.replicateCount;
    else if (!inWindow(observation.time, options.window)) excludedAbove += observation.replicateCount;
    else retained.push(observation);
  }
  return {
    id: input.id,
    label: input.label,
    observationKind: input.observationKind,
    observations: retained,
    times: retained.map((observation) => observation.time),
    values: retained.map((observation) => observation.value),
    sourceTimeUnit: input.timeUnit,
    sourceValueUnit: input.valueUnit,
    timeUnit: options.window.unit,
    valueUnit: targetValueUnit,
    excludedBelow,
    excludedAbove,
    duplicateGroups,
    ...normalized ? { normalization: normalized.receipt } : {},
    ...input.timeOffset ? { timeOffset: { sourceValue: canonicalZero4(input.timeOffset.value), sourceUnit: input.timeOffset.unit } } : {}
  };
}
function traceCell(values) {
  return values.length === 1 ? values[0] : JSON.stringify(values);
}
function structuredCell4(value) {
  return value === void 0 ? null : canonicalize(value);
}
function weightTraceScopeSummaryCell(value) {
  const scope = record4(value);
  const summary = { kind: String(scope.kind) };
  if (scope.snapshotTime !== void 0) summary.snapshotTime = scope.snapshotTime;
  switch (scope.kind) {
    case "single_process":
      break;
    case "global_merged":
      summary.mergedRanksCoverage = "all_ranks_0_through_worldSize_minus_1";
      summary.worldSize = scope.worldSize;
      break;
    case "mpi_target_rank_local":
      summary.localTargetUniverseComplete = scope.localTargetUniverseComplete;
      summary.rank = scope.rank;
      summary.worldSize = scope.worldSize;
      break;
    case "sampled":
      summary.method = scope.method;
      summary.parentScope = scope.parentScope;
      summary.retainedConnectionCount = scope.retainedConnectionCount;
      summary.sourceConnectionCount = scope.sourceConnectionCount;
      break;
    default:
      throw new Error(`unsupported NetworkScope kind ${String(scope.kind)}`);
  }
  return canonicalize(summary);
}
function uncertaintyCells(uncertainty, sourceOrdinal, trace, displayedValue) {
  const kind = typeof uncertainty?.kind === "string" ? uncertainty.kind : void 0;
  if (!kind || kind === "none" || sourceOrdinal === void 0) {
    const reason = typeof uncertainty?.reason === "string" ? uncertainty.reason : void 0;
    return [null, null, kind === "none" ? `none${reason ? ` (${reason})` : ""}` : null];
  }
  const declared = uncertainty;
  const sourceUnit = typeof declared.unit === "string" ? declared.unit : trace.valueUnit;
  const transform = (value, dispersion) => {
    if (typeof value !== "number") return null;
    if (!trace.normalization) {
      return sourceUnit === trace.valueUnit ? value : convert(value, sourceUnit, trace.valueUnit);
    }
    const source = sourceUnit === trace.sourceValueUnit ? value : convert(value, sourceUnit, trace.sourceValueUnit);
    return normalizeValue(source, trace.normalization, dispersion);
  };
  const sampleCount = array4(declared.sampleCount)[sourceOrdinal];
  const extra = [];
  if (kind === "standard_deviation" || kind === "standard_error") {
    extra.push(`basis=${String(declared.basis ?? "unspecified")}`);
    if (typeof sampleCount === "number") extra.push(`sampleCount=${sampleCount}`);
    const dispersion = transform(array4(declared.values)[sourceOrdinal], true);
    const description = [kind, ...extra].join("; ");
    return displayedValue === null || dispersion === null ? [null, null, description] : [displayedValue - dispersion, displayedValue + dispersion, description];
  }
  const lower = transform(array4(declared.lower)[sourceOrdinal], false);
  const upper = transform(array4(declared.upper)[sourceOrdinal], false);
  if (typeof declared.method === "string") extra.push(`method=${declared.method}`);
  if (typeof declared.level === "number") extra.push(`level=${declared.level}`);
  if (typeof declared.coverage === "string") extra.push(`coverage=${declared.coverage}`);
  if (typeof declared.lowerQuantile === "number") extra.push(`lowerQuantile=${declared.lowerQuantile}`);
  if (typeof declared.upperQuantile === "number") extra.push(`upperQuantile=${declared.upperQuantile}`);
  if (typeof declared.basis === "string") extra.push(`basis=${declared.basis}`);
  if (typeof sampleCount === "number") extra.push(`sampleCount=${sampleCount}`);
  return [lower, upper, [kind, ...extra].join("; ")];
}
function originText(raw) {
  const origin = record4(raw.origin);
  return origin.kind === "derived" ? `derived: ${String(origin.method ?? "unspecified")}` : String(origin.kind ?? "unknown");
}
function sourceProvenance(trace, observation, atomKind, extra = {}) {
  return {
    seriesId: trace.id,
    sourceOrdinals: [...observation.sourceOrdinals],
    atomKind,
    ...extra
  };
}
function sourceProvenanceForView(view, observation, atomKind, extra = {}) {
  const lineage = canonicalize([...observation.sourceOrdinals]);
  let renderRunOrdinal;
  for (let runOrdinal = 0; runOrdinal < (view.renderRuns?.length ?? 0); runOrdinal++) {
    if (view.renderRuns[runOrdinal].vertices.some((vertex) => canonicalize(array4(record4(vertex.provenance).sourceOrdinals)) === lineage)) {
      if (renderRunOrdinal !== void 0 && renderRunOrdinal !== runOrdinal) {
        throw new Error("one independent observation lineage appears in multiple render runs");
      }
      renderRunOrdinal = runOrdinal;
    }
  }
  return sourceProvenance(view.trace, observation, atomKind, {
    ...renderRunOrdinal === void 0 ? {} : { renderRunOrdinal },
    ...extra
  });
}
function joinUnique(values) {
  return [...new Set(values)].join(", ");
}
function noCompactionStatement2() {
  return "No compaction was applied; every accepted row is returned.";
}
function summaryNumber2(value) {
  return value === 0 ? "0" : String(value);
}
function unitConversionStatement(traces) {
  const conversions = [];
  for (const trace of traces) {
    if (trace.sourceTimeUnit !== trace.timeUnit) {
      conversions.push(`${trace.id} time ${trace.sourceTimeUnit} to ${trace.timeUnit}`);
    }
    if (!trace.normalization && trace.sourceValueUnit !== trace.valueUnit) {
      conversions.push(`${trace.id} value ${trace.sourceValueUnit} to ${trace.valueUnit}`);
    }
    if (trace.timeOffset && trace.timeOffset.sourceUnit !== trace.timeUnit) {
      conversions.push(`${trace.id} offset ${trace.timeOffset.sourceUnit} to ${trace.timeUnit}`);
    }
  }
  return conversions.length === 0 ? "No unit conversion was applied." : `Unit conversions: ${conversions.join("; ")}.`;
}
function duplicateStatement(parameters, traces) {
  const options = duplicateOptions(parameters);
  const groups = traces.reduce((sum, trace) => sum + trace.duplicateGroups, 0);
  if (groups === 0) return `${String(options.duplicatePolicy ?? "reject_if_present")} (no duplicate groups present)`;
  return options.duplicatePolicy === "aggregate" ? `aggregate (${String(options.aggregateMethod)}; ${groups} duplicate groups)` : `${String(options.duplicatePolicy)} (${groups} duplicate groups)`;
}
function uncertaintyStatement(uncertainties) {
  const descriptions = uncertainties.map((value) => {
    const kind = typeof value?.kind === "string" ? value.kind : "none";
    return kind === "none" && typeof value?.reason === "string" ? `none (${value.reason})` : kind;
  });
  return `Uncertainty declarations: ${joinUnique(descriptions)}.`;
}
function weightUncertaintyStatement(uncertainties) {
  const descriptions = uncertainties.map((uncertainty) => {
    const kind = typeof uncertainty?.kind === "string" ? uncertainty.kind : "none";
    if (kind === "none") {
      return typeof uncertainty?.reason === "string" ? `none (${uncertainty.reason})` : "none";
    }
    const basis = String(uncertainty?.basis ?? "ensemble_members");
    if (kind === "standard_deviation") {
      return `standard_deviation (descriptive sample SD across exact ${basis}; center=arithmetic mean; divisor=n-1; no sampling-population or coverage claim)`;
    }
    if (kind === "quantile_interval") {
      return `quantile_interval (descriptive empirical Type-7 linear q=${String(uncertainty?.lowerQuantile)}\u2013${String(uncertainty?.upperQuantile)} across exact ${basis}; no coverage claim)`;
    }
    if (kind === "ensemble_range") {
      return `ensemble_range (descriptive observed minimum\u2013maximum across exact ${basis}; no sampling-population or coverage claim)`;
    }
    throw new Error(`unsupported independent synaptic-weight uncertainty summary kind ${kind}`);
  });
  return `Uncertainty declarations: ${joinUnique(descriptions)}.`;
}
function hasCompleteDrawableUncertainty(trace, uncertainty) {
  const kind = typeof uncertainty?.kind === "string" ? uncertainty.kind : "none";
  if (kind === "none") return false;
  let sawValue = false;
  for (const observation of trace.observations) {
    if (observation.value === null) continue;
    sawValue = true;
    const ordinal = observation.sourceOrdinals.length === 1 ? observation.sourceOrdinals[0] : void 0;
    const [lower, upper] = uncertaintyCells(uncertainty, ordinal, trace, observation.value);
    if (lower === null || upper === null) return false;
  }
  return sawValue;
}
function independentViewHasCompleteDrawableUncertainty(view, showSamplePoints) {
  const uncertainty = view.uncertainty;
  const kind = typeof uncertainty?.kind === "string" ? uncertainty.kind : "none";
  if (kind === "none") return false;
  let eligibleIndexes;
  if (kind === "standard_deviation" || kind === "standard_error" || view.renderRuns === void 0) {
    eligibleIndexes = view.trace.observations.map((_observation, index) => index);
  } else {
    const renderedLineage = new Set(
      view.renderRuns.flatMap((run) => run.vertices.map((vertex) => canonicalize(array4(record4(vertex.provenance).sourceOrdinals))))
    );
    const eligible = new Set(
      view.trace.observations.flatMap((observation, index) => renderedLineage.has(canonicalize([...observation.sourceOrdinals])) ? [index] : [])
    );
    if (showSamplePoints) {
      for (const index of view.markerObservationIndexes ?? view.trace.observations.map((_observation, observationIndex) => observationIndex)) {
        eligible.add(index);
      }
    }
    eligibleIndexes = [...eligible].sort((left, right) => left - right);
  }
  let observed = false;
  for (const index of eligibleIndexes) {
    const observation = view.trace.observations[index];
    if (observation?.value === null || observation === void 0) continue;
    observed = true;
    const ordinal = observation.sourceOrdinals.length === 1 ? observation.sourceOrdinals[0] : void 0;
    const [lower, upper] = uncertaintyCells(
      uncertainty,
      ordinal,
      view.trace,
      observation.value
    );
    if (lower === null || upper === null) return false;
  }
  return observed;
}
function exactConversionFacts(traces, uncertainties) {
  const conversions = [];
  for (const trace of traces) {
    if (trace.sourceTimeUnit !== trace.timeUnit) {
      conversions.push(
        `${trace.id}: ${trace.sourceTimeUnit} -> ${trace.timeUnit} (factor ${conversionFactor(trace.sourceTimeUnit, trace.timeUnit)})`
      );
    }
    if (!trace.normalization && trace.sourceValueUnit !== trace.valueUnit) {
      conversions.push(
        `${trace.id}: ${trace.sourceValueUnit} -> ${trace.valueUnit} (factor ${conversionFactor(trace.sourceValueUnit, trace.valueUnit)})`
      );
    }
    if (trace.timeOffset && trace.timeOffset.sourceUnit !== trace.timeUnit) {
      conversions.push(
        `${trace.id} offset: ${trace.timeOffset.sourceUnit} -> ${trace.timeUnit} (factor ${conversionFactor(trace.timeOffset.sourceUnit, trace.timeUnit)})`
      );
    }
  }
  for (let index = 0; index < Math.min(traces.length, uncertainties.length); index++) {
    const uncertainty = uncertainties[index];
    const sourceUnit = typeof uncertainty?.unit === "string" ? uncertainty.unit : void 0;
    if (!sourceUnit || uncertainty?.kind === "none") continue;
    const targetUnit = traces[index].normalization ? traces[index].sourceValueUnit : traces[index].valueUnit;
    if (sourceUnit !== targetUnit) {
      conversions.push(
        `${traces[index].id} uncertainty: ${sourceUnit} -> ${targetUnit} (factor ${conversionFactor(sourceUnit, targetUnit)})`
      );
    }
  }
  return conversions;
}
function traceDisclosureFacts(request, parameters, traces, uncertainties, extras = {}) {
  const source = record4(request.source);
  const scope = record4(record4(request.data).scope);
  const conversions = exactConversionFacts(traces, uncertainties);
  const aggregateMethod = duplicateOptions(parameters).aggregateMethod;
  const duplicateGroups = traces.reduce((sum, trace) => sum + trace.duplicateGroups, 0);
  const missingValueCount = traces.reduce(
    (sum, trace) => sum + trace.observations.filter((observation) => observation.value === null).length,
    0
  );
  const missingAggregateReplicateCount = traces.reduce(
    (sum, trace) => sum + trace.observations.reduce(
      (traceSum, observation) => traceSum + (observation.replicateCount > 1 && observation.value !== null ? observation.sourceValues.filter((value) => value === null).length : 0),
      0
    ),
    0
  );
  const declared = uncertainties.filter((uncertainty) => uncertainty?.kind !== void 0 && uncertainty.kind !== "none").length;
  const shown = traces.filter((trace, index) => hasCompleteDrawableUncertainty(trace, uncertainties[index])).length;
  const reasons = uncertainties.map((uncertainty) => uncertainty?.reason).filter((reason) => typeof reason === "string");
  return {
    sourceKind: typeof source.kind === "string" ? source.kind : "unknown",
    sourceAuthenticityVerified: false,
    referenceComparisonRun: false,
    callerNotePresent: typeof source.declaredNote === "string",
    ...typeof scope.kind === "string" ? { scopeKind: scope.kind } : {},
    ...typeof scope.rank === "number" ? { rank: scope.rank } : {},
    ...typeof scope.worldSize === "number" ? { worldSize: scope.worldSize } : {},
    ...typeof scope.retainedConnectionCount === "number" ? {
      retainedConnectionCount: scope.retainedConnectionCount,
      sampledRetained: scope.retainedConnectionCount
    } : {},
    ...typeof scope.sourceConnectionCount === "number" ? {
      sourceConnectionCount: scope.sourceConnectionCount,
      sampledSource: scope.sourceConnectionCount
    } : {},
    excludedOutOfWindow: traces.reduce(
      (sum, trace) => sum + trace.excludedBelow + trace.excludedAbove,
      0
    ),
    missingValueCount,
    ...conversions.length > 0 ? { unitConversions: conversions } : {},
    ...duplicateGroups > 0 && aggregateMethod ? { duplicateTimeAggregateMethod: aggregateMethod } : {},
    ...missingAggregateReplicateCount > 0 ? { missingAggregateReplicateCount } : {},
    uncertaintyKind: declared > 0 ? "provided" : "none",
    uncertaintySeriesDeclared: declared,
    uncertaintySeriesShown: shown,
    uncertaintySeriesTotal: uncertainties.length,
    ...declared === 0 && reasons.length > 0 ? { uncertaintyReason: joinUnique(reasons) } : declared === 0 ? { uncertaintyReason: "not_provided" } : {},
    ...extras
  };
}
function observationPath(trace) {
  const subpaths = [];
  let current = [];
  for (const observation of trace.observations) {
    if (observation.value === null) {
      if (current.length > 0) {
        if (trace.observationKind === "piecewise_constant") {
          current.push({ observation, connector: true });
        }
        subpaths.push(current);
      }
      current = [];
      continue;
    }
    if (trace.observationKind === "piecewise_constant" && current.length > 0) {
      current.push({ observation, connector: true });
    }
    current.push({ observation, connector: false });
  }
  if (current.length > 0) subpaths.push(current);
  return subpaths;
}
function pathPaints(path) {
  if (path.length < 2) return false;
  const first = path[0];
  const firstValue = first.connector ? path[Math.max(0, path.indexOf(first) - 1)]?.observation.value : first.observation.value;
  let previousValue = firstValue;
  const coordinates = path.map((entry) => {
    const value = entry.connector ? previousValue : entry.observation.value;
    if (!entry.connector) previousValue = entry.observation.value;
    return [entry.observation.time, value];
  });
  return coordinates.some(([time, value]) => time !== coordinates[0][0] || value !== coordinates[0][1]);
}
function uncertaintyGeometry(view, classId, showSamplePoints) {
  const uncertainty = view.uncertainty;
  const kind = typeof uncertainty?.kind === "string" ? uncertainty.kind : "none";
  if (kind === "none") return [];
  const usable = view.trace.observations.map((observation) => {
    const ordinal = observation.sourceOrdinals.length === 1 ? observation.sourceOrdinals[0] : void 0;
    const [lower, upper] = uncertaintyCells(uncertainty, ordinal, view.trace, observation.value);
    return { observation, lower, upper };
  });
  if (kind === "standard_deviation" || kind === "standard_error") {
    const finite = usable.filter(({ observation, lower, upper }) => observation.value !== null && lower !== null && upper !== null);
    return [
      ...finite.map(({ observation }) => carrier(
        classId,
        sourceProvenanceForView(view, observation, "uncertainty_whisker", { part: "shaft" })
      )),
      ...finite.flatMap(({ observation }) => [
        carrier(classId, sourceProvenanceForView(view, observation, "uncertainty_whisker", { part: "lower_cap" })),
        carrier(classId, sourceProvenanceForView(view, observation, "uncertainty_whisker", { part: "upper_cap" }))
      ])
    ];
  }
  if (view.renderRuns) {
    const subpaths2 = [];
    for (const run of view.renderRuns) {
      let current2 = [];
      for (const vertex of run.vertices) {
        const provenance = record4(vertex.provenance);
        const ordinals = array4(provenance.sourceOrdinals);
        const ordinal = ordinals.length === 1 && typeof ordinals[0] === "number" ? ordinals[0] : void 0;
        const [lower, upper] = uncertaintyCells(
          uncertainty,
          ordinal,
          view.trace,
          vertex.value
        );
        if (lower === null || upper === null) {
          if (current2.length > 0) subpaths2.push(current2);
          current2 = [];
          continue;
        }
        current2.push({ time: vertex.time, lower, upper, provenance });
      }
      if (current2.length > 0) subpaths2.push(current2);
    }
    if (showSamplePoints) {
      const renderedLineage = new Set(
        view.renderRuns.flatMap((run) => run.vertices.map((vertex) => canonicalize(array4(record4(vertex.provenance).sourceOrdinals))))
      );
      const markerIndexes = view.markerObservationIndexes ?? view.trace.observations.map((_observation, index) => index);
      for (const index of markerIndexes) {
        const observation = view.trace.observations[index];
        if (observation === void 0 || observation.value === null || renderedLineage.has(canonicalize([...observation.sourceOrdinals]))) continue;
        const ordinal = observation.sourceOrdinals.length === 1 ? observation.sourceOrdinals[0] : void 0;
        const [lower, upper] = uncertaintyCells(
          uncertainty,
          ordinal,
          view.trace,
          observation.value
        );
        if (lower === null || upper === null) continue;
        subpaths2.push([{
          time: observation.time,
          lower,
          upper,
          provenance: record4(sourceProvenanceForView(
            view,
            observation,
            "uncertainty_band_vertex"
          ))
        }]);
      }
    }
    const vertices2 = subpaths2.flatMap((path) => path.map((entry) => carrier(classId, {
      ...entry.provenance,
      atomKind: "uncertainty_band_vertex"
    })));
    const degenerate2 = subpaths2.flatMap((path) => {
      if (path.length === 0) return [];
      const first = path[0];
      return path.every(
        (entry) => entry.time === first.time && entry.lower === first.lower && entry.upper === first.upper
      ) && first.lower === first.upper ? [carrier(classId, {
        ...first.provenance,
        atomKind: "uncertainty_degenerate_marker"
      })] : [];
    });
    return [...vertices2, ...degenerate2];
  }
  const subpaths = [];
  let current = [];
  for (const entry of usable) {
    if (entry.lower === null || entry.upper === null || entry.observation.value === null) {
      if (current.length > 0) {
        if (view.trace.observationKind === "piecewise_constant") {
          const previous = current[current.length - 1];
          current.push({
            observation: entry.observation,
            connector: true,
            lower: previous.lower,
            upper: previous.upper
          });
        }
        subpaths.push(current);
      }
      current = [];
      continue;
    }
    if (view.trace.observationKind === "piecewise_constant" && current.length > 0) {
      const previous = current[current.length - 1];
      current.push({
        observation: entry.observation,
        connector: true,
        lower: previous.lower,
        upper: previous.upper
      });
    }
    current.push({
      observation: entry.observation,
      connector: false,
      lower: entry.lower,
      upper: entry.upper
    });
  }
  if (current.length > 0) subpaths.push(current);
  const vertices = subpaths.flatMap((path) => path.flatMap((entry) => entry.connector ? [] : [carrier(classId, sourceProvenance(view.trace, entry.observation, "uncertainty_band_vertex"))]));
  const degenerate = subpaths.flatMap((path) => {
    if (path.length === 0) return [];
    const first = path[0];
    const same = path.every(
      (entry) => entry.observation.time === first.observation.time && entry.lower === first.lower && entry.upper === first.upper
    );
    return same && first.lower === first.upper ? [carrier(classId, sourceProvenance(view.trace, first.observation, "uncertainty_degenerate_marker"))] : [];
  });
  return [...vertices, ...degenerate];
}
function normalTraceGeometry(view, classes, showSamplePoints, atomKinds = {
  pathVertex: "observation_vertex",
  sampleMarker: "observation_marker",
  isolatedMarker: "isolated_observation_marker"
}) {
  const output = [];
  if (classes.uncertainty) {
    output.push(...uncertaintyGeometry(view, classes.uncertainty, showSamplePoints));
  }
  if (view.renderRuns) {
    for (const run of view.renderRuns) {
      if (run.vertices.length < 2) continue;
      const first = run.vertices[0];
      if (!run.vertices.some((vertex) => vertex.time !== first.time || vertex.value !== first.value)) continue;
      for (const vertex of run.vertices) output.push(carrier(classes.paths, vertex.provenance));
    }
  } else {
    const paths = observationPath(view.trace);
    for (const path of paths) {
      if (!pathPaints(path)) continue;
      for (const entry of path) {
        if (!entry.connector) {
          output.push(carrier(
            classes.paths,
            sourceProvenance(view.trace, entry.observation, atomKinds.pathVertex)
          ));
        }
      }
    }
  }
  if (showSamplePoints) {
    const markerIndexes = view.markerObservationIndexes ?? view.trace.observations.map((_observation, index) => index);
    for (const index of markerIndexes) {
      const observation = view.trace.observations[index];
      if (observation.value !== null) {
        output.push(carrier(
          classes.samples,
          sourceProvenanceForView(view, observation, atomKinds.sampleMarker)
        ));
      }
    }
  } else if (view.renderRuns) {
    for (const run of view.renderRuns) {
      if (run.vertices.length === 0) continue;
      const first = run.vertices[0];
      const paints = run.vertices.length >= 2 && run.vertices.some(
        (vertex) => vertex.time !== first.time || vertex.value !== first.value
      );
      if (!paints) {
        output.push(carrier(
          classes.samples,
          { ...record4(first.provenance), atomKind: atomKinds.isolatedMarker }
        ));
      }
    }
  } else {
    for (const path of observationPath(view.trace)) {
      if (!pathPaints(path) && path.length > 0) {
        output.push(carrier(
          classes.samples,
          sourceProvenance(view.trace, path[0].observation, atomKinds.isolatedMarker)
        ));
      }
    }
  }
  return output;
}
function analogModel(requestValue) {
  const request = record4(requestValue);
  const data = record4(request.data);
  const parameters = record4(request.parameters);
  const window = windowFrom(data.window);
  const rawSeries = array4(data.series).map(record4);
  const ids = stringArray(data.seriesIds);
  const inputs = rawSeries.map((raw, index) => {
    const time = record4(raw.time);
    const values = record4(raw.values);
    return {
      id: ids[index] ?? `series-${index + 1}`,
      label: typeof raw.label === "string" ? raw.label : ids[index] ?? `Series ${index + 1}`,
      observationKind: raw.observationKind ?? "point_sample",
      times: numberArray(time.values),
      timeUnit: typeof time.unit === "string" ? time.unit : window.unit,
      values: nullableNumberArray(values.values),
      valueUnit: typeof values.unit === "string" ? values.unit : "1"
    };
  });
  const layout = String(parameters.layout);
  const panelMembers = [];
  if (layout === "small_multiples") {
    const groupBy = typeof parameters.groupBy === "string" ? parameters.groupBy : "series";
    const groups = /* @__PURE__ */ new Map();
    for (let index = 0; index < inputs.length; index++) {
      const key = groupBy === "quantity_kind" ? String(record4(rawSeries[index].values).kind) : inputs[index].id;
      const group = groups.get(key);
      if (group) group.push(index);
      else groups.set(key, [index]);
    }
    for (const [key, indexes] of groups) panelMembers.push({ key, indexes });
  } else {
    panelMembers.push({ key: "main", indexes: inputs.map((_input, index) => index) });
  }
  const panels = panelMembers.map((panel) => {
    const targetUnit = layout === "small_multiples" ? inputs[panel.indexes[0]].valueUnit : typeof parameters.valueUnit === "string" ? parameters.valueUnit : inputs[0].valueUnit;
    return {
      id: panel.key,
      series: panel.indexes.map((sourceIndex) => ({
        raw: rawSeries[sourceIndex],
        sourceIndex,
        uncertainty: record4(parameters.uncertainty),
        trace: prepareIndependentTrace(inputs[sourceIndex], {
          window,
          ...duplicateOptions(parameters),
          targetValueUnit: targetUnit
        })
      }))
    };
  });
  const uncertainty = record4(parameters.uncertainty);
  const views = panels.flatMap((panel) => panel.series);
  const traces = views.map((view) => view.trace);
  const rows = [];
  for (const panel of panels) {
    for (const view of panel.series) {
      const rawValues = record4(view.raw.values);
      for (const observation of view.trace.observations) {
        const sourceOrdinal = observation.sourceOrdinals.length === 1 ? observation.sourceOrdinals[0] : void 0;
        const [lower, upper, method] = uncertaintyCells(
          uncertainty,
          sourceOrdinal,
          view.trace,
          observation.value
        );
        rows.push([
          view.trace.id,
          view.trace.label,
          String(rawValues.kind ?? "unknown"),
          view.trace.observationKind,
          originText(view.raw),
          observation.time,
          view.trace.timeUnit,
          observation.value,
          view.trace.valueUnit,
          observation.value === null ? "true" : "false",
          observation.replicateCount,
          lower,
          upper,
          method,
          traceCell(observation.sourceOrdinals)
        ]);
      }
    }
  }
  const geometry = panels.flatMap((panel) => panel.series.flatMap((view) => normalTraceGeometry(view, {
    paths: "series_paths",
    samples: "samples",
    uncertainty: "uncertainty"
  }, parameters.showSamplePoints === true)));
  const missing = traces.reduce(
    (sum, trace) => sum + trace.observations.filter((observation) => observation.value === null).length,
    0
  );
  const excluded = traces.reduce(
    (sum, trace) => sum + trace.excludedBelow + trace.excludedAbove,
    0
  );
  const quantitySummary = joinUnique(views.map((view) => `${String(record4(view.raw.values).kind ?? "unknown")} (${view.trace.valueUnit})`));
  const summary = {
    seriesCount: String(traces.length),
    windowStart: summaryNumber2(window.start),
    windowStop: summaryNumber2(window.stop),
    timeUnit: window.unit,
    layoutMode: layout,
    quantitySummary,
    sampleCount: String(rows.length),
    missingCount: String(missing),
    excludedCount: String(excluded),
    duplicateTimePolicy: duplicateStatement(parameters, traces),
    unitConversionStatement: unitConversionStatement(traces),
    uncertaintyStatement: uncertaintyStatement(traces.map(() => uncertainty)),
    compactionStatement: noCompactionStatement2()
  };
  return {
    rows,
    geometry,
    summary,
    disclosures: traceDisclosureFacts(
      request,
      parameters,
      traces,
      traces.map(() => uncertainty)
    )
  };
}
var ANALOG_AUTHORITY = defineAuthorityEvaluator(
  authorityEvaluatorId("neuro.analog_trace", 2),
  (request) => {
    const model = analogModel(request);
    return {
      "table.rows": rowSequence(model.rows),
      "geometry.sequence": geometrySequence(model.geometry),
      "summary.facts": summaryFactMap(model.summary),
      "disclosure.facts": disclosureFactMap(model.disclosures)
    };
  }
);
function multisignalModel(requestValue) {
  const request = record4(requestValue);
  const data = record4(request.data);
  const parameters = record4(request.parameters);
  const window = windowFrom(data.window);
  const rawSeries = array4(data.series).map(record4);
  const sharedTime = record4(data.eventTimes);
  const inputs = rawSeries.map((raw, index) => {
    const time = data.timeBase === "shared" ? sharedTime : record4(raw.time);
    const values = record4(raw.values);
    const offset = record4(raw.timeOffset);
    return {
      id: typeof raw.seriesId === "string" ? raw.seriesId : `series-${index + 1}`,
      label: typeof raw.label === "string" ? raw.label : typeof raw.seriesId === "string" ? raw.seriesId : `Series ${index + 1}`,
      panelId: typeof raw.panelId === "string" ? raw.panelId : "main",
      observationKind: raw.observationKind ?? "point_sample",
      times: numberArray(time.values),
      timeUnit: typeof time.unit === "string" ? time.unit : window.unit,
      values: nullableNumberArray(values.values),
      valueUnit: typeof values.unit === "string" ? values.unit : "1",
      ...typeof offset.value === "number" && typeof offset.unit === "string" ? { timeOffset: { value: offset.value, unit: offset.unit } } : {}
    };
  });
  const normalizationNode = record4(parameters.normalization);
  const normalization = typeof normalizationNode.method === "string" ? {
    method: normalizationNode.method,
    statisticsWindow: windowFrom(normalizationNode.statisticsWindow, window.unit)
  } : void 0;
  const declaredPanels = array4(parameters.panels).map((value, declaredIndex) => ({
    panel: record4(value),
    declaredIndex
  }));
  if (parameters.panelOrder === "by_panel_id") {
    declaredPanels.sort((left, right) => {
      const a = String(left.panel.panelId ?? "");
      const b = String(right.panel.panelId ?? "");
      return a < b ? -1 : a > b ? 1 : left.declaredIndex - right.declaredIndex;
    });
  }
  const panels = declaredPanels.map(({ panel, declaredIndex }) => {
    const panelId = typeof panel.panelId === "string" ? panel.panelId : `panel-${declaredIndex + 1}`;
    const targetUnit = normalization ? "1" : String(panel.unit ?? "1");
    return {
      id: panelId,
      series: inputs.flatMap((input, sourceIndex) => {
        if (input.panelId !== panelId) return [];
        const uncertainty = record4(rawSeries[sourceIndex].uncertainty);
        return [{
          raw: rawSeries[sourceIndex],
          sourceIndex,
          panelId,
          uncertainty,
          trace: prepareIndependentTrace(input, {
            window,
            ...duplicateOptions(parameters),
            targetValueUnit: targetUnit,
            ...normalization ? { normalization } : {}
          })
        }];
      })
    };
  });
  const views = panels.flatMap((panel) => panel.series);
  const traces = views.map((view) => view.trace);
  const uncertainties = views.map((view) => view.uncertainty);
  const rows = [];
  for (const panel of panels) {
    for (const view of panel.series) {
      const raw = view.raw;
      const origin = record4(raw.origin);
      const rawValues = record4(raw.values);
      const uncertainty = view.uncertainty;
      for (const observation of view.trace.observations) {
        const ordinal = observation.sourceOrdinals.length === 1 ? observation.sourceOrdinals[0] : void 0;
        const [lower, upper, method] = uncertaintyCells(
          uncertainty,
          ordinal,
          view.trace,
          observation.value
        );
        rows.push([
          view.trace.id,
          view.trace.label,
          String(raw.entityId ?? ""),
          String(raw.entityKind ?? ""),
          typeof raw.compartmentId === "string" ? raw.compartmentId : null,
          typeof raw.pathwayId === "string" ? raw.pathwayId : null,
          String(raw.variableId ?? ""),
          panel.id,
          view.trace.observationKind,
          String(origin.kind ?? "unknown"),
          typeof origin.method === "string" ? origin.method : null,
          observation.recordedTime,
          view.trace.sourceTimeUnit,
          view.trace.timeOffset?.sourceValue ?? 0,
          view.trace.timeOffset?.sourceUnit ?? view.trace.sourceTimeUnit,
          observation.time,
          view.trace.timeUnit,
          traceCell(observation.sourceValues),
          String(rawValues.unit ?? view.trace.valueUnit),
          observation.value,
          view.trace.valueUnit,
          observation.value === null ? "true" : "false",
          observation.replicateCount,
          String(uncertainty?.kind ?? "none"),
          lower,
          upper,
          method,
          structuredCell4(view.trace.normalization),
          traceCell(observation.sourceOrdinals)
        ]);
      }
    }
  }
  const geometry = panels.flatMap((panel) => panel.series.flatMap((view) => normalTraceGeometry(view, {
    paths: "series_paths",
    samples: "samples",
    uncertainty: "uncertainty"
  }, parameters.showSamplePoints === true)));
  const panelSummary = panels.map((panel) => {
    const panelDeclaration = declaredPanels.find(({ panel: candidate, declaredIndex }) => String(candidate.panelId ?? `panel-${declaredIndex + 1}`) === panel.id)?.panel ?? {};
    return `${panel.id}: ${panel.series.length} series, unit ${normalization ? "1" : String(panelDeclaration.unit ?? "1")}, scale ${String(panelDeclaration.scale ?? "linear")}`;
  }).join("; ");
  const seriesSummary = views.map((view) => {
    const values = record4(view.raw.values);
    return `${view.trace.id}: ${String(view.raw.variableId ?? "unknown")} (${String(values.kind ?? "unknown")}, ${view.trace.valueUnit})`;
  }).join("; ");
  const missing = traces.reduce(
    (sum, trace) => sum + trace.observations.filter((observation) => observation.value === null).length,
    0
  );
  const summary = {
    seriesCount: String(traces.length),
    panelCount: String(panels.length),
    windowStart: summaryNumber2(window.start),
    windowStop: summaryNumber2(window.stop),
    timeUnit: window.unit,
    windowBoundary: window.finalEdgeInclusive ? "[start,stop]" : "[start,stop)",
    layout: String(parameters.layout),
    timeAlignment: String(structuredCell4(data.timeAlignment) ?? "unknown"),
    panelSummary,
    seriesSummary,
    observationKindStatement: `Observation kinds: ${joinUnique(traces.map((trace) => trace.observationKind))}.`,
    originStatement: `Origins: ${joinUnique(views.map((view) => originText(view.raw)))}.`,
    sampleCount: String(rows.length),
    missingCount: String(missing),
    duplicateTimePolicy: duplicateStatement(parameters, traces),
    normalizationStatement: normalization ? `Normalization: ${normalization.method} using ${normalization.statisticsWindow.start} to ${normalization.statisticsWindow.stop} ${normalization.statisticsWindow.unit}.` : "No normalization was applied.",
    uncertaintyStatement: uncertaintyStatement(uncertainties),
    unitConversionStatement: unitConversionStatement(traces),
    compactionStatement: noCompactionStatement2()
  };
  return {
    rows,
    geometry,
    summary,
    disclosures: traceDisclosureFacts(request, parameters, traces, uncertainties)
  };
}
var MULTISIGNAL_AUTHORITY = defineAuthorityEvaluator(
  authorityEvaluatorId("neuro.multisignal_trace", 2),
  (request) => {
    const model = multisignalModel(request);
    return {
      "table.rows": rowSequence(model.rows),
      "geometry.sequence": geometrySequence(model.geometry),
      "summary.facts": summaryFactMap(model.summary),
      "disclosure.facts": disclosureFactMap(model.disclosures)
    };
  }
);
function quantile(values, probability) {
  return exactBinary64EmpiricalQuantileType7(values, probability);
}
function deriveIndependentCompartmentAggregate(declaration, views, window) {
  const selectedIds = stringArray(declaration.compartmentIds);
  const viewsByCompartment = /* @__PURE__ */ new Map();
  for (const view of views) {
    const id2 = String(view.raw.compartmentId);
    const existing = viewsByCompartment.get(id2);
    if (existing) existing.push(view);
    else viewsByCompartment.set(id2, [view]);
  }
  const selected = selectedIds.map((id2) => {
    const matches = viewsByCompartment.get(id2) ?? [];
    if (matches.length !== 1) throw new Error(`ambiguous compartment aggregate member ${id2}`);
    return matches[0];
  });
  const targetUnit = selected[0].trace.valueUnit;
  const times = [...new Set(selected.flatMap((view) => [...view.trace.times]))].sort((left, right) => left - right);
  const byMember = selected.map((view) => {
    const values2 = /* @__PURE__ */ new Map();
    for (const observation of view.trace.observations) {
      const existing = values2.get(observation.time);
      if (existing) existing.push(observation.value);
      else values2.set(observation.time, [observation.value]);
    }
    return values2;
  });
  const weights = declaration.weighting === "declared" ? numberArray(declaration.weights) : selected.map(() => 1);
  const method = String(declaration.method);
  const values = times.map((time) => {
    const memberValues = [];
    for (let index = 0; index < selected.length; index++) {
      const candidates = byMember[index].get(time) ?? [];
      if (candidates.length !== 1 || candidates[0] === null) return null;
      memberValues.push(selected[index].trace.valueUnit === targetUnit ? candidates[0] : convert(candidates[0], selected[index].trace.valueUnit, targetUnit));
    }
    if (method === "mean") {
      return exactBinary64WeightedMean(memberValues, weights);
    }
    if (method === "sum") {
      return exactBinary64WeightedSum(memberValues, weights);
    }
    if (method === "median") return quantile(memberValues, 0.5);
    if (method === "min") return sequenceMinimum(memberValues);
    return sequenceMaximum(memberValues);
  });
  const id = `aggregate-${selectedIds.join("-")}`;
  const label = typeof declaration.label === "string" ? declaration.label : `${method} over ${selected.length} declared compartments`;
  const trace = prepareIndependentTrace({
    id,
    label,
    observationKind: "point_sample",
    times,
    timeUnit: window.unit,
    values,
    valueUnit: targetUnit
  }, {
    window,
    duplicatePolicy: "reject",
    targetValueUnit: targetUnit
  });
  const source = selected.length === 1 ? selected[0] : void 0;
  const uncertainty = source?.uncertainty;
  const scale = method === "sum" ? weights[0] : 1;
  let derivedUncertainty;
  if (source && uncertainty && uncertainty.kind !== "none") {
    const sourceLower = [];
    const sourceUpper = [];
    for (const observation of source.trace.observations) {
      const ordinal = observation.sourceOrdinals.length === 1 ? observation.sourceOrdinals[0] : void 0;
      const [lower2, upper2] = uncertaintyCells(uncertainty, ordinal, source.trace, observation.value);
      sourceLower.push(lower2);
      sourceUpper.push(upper2);
    }
    const uniqueSourceIndex = /* @__PURE__ */ new Map();
    for (let sourceIndex = 0; sourceIndex < source.trace.times.length; sourceIndex++) {
      const time = source.trace.times[sourceIndex];
      uniqueSourceIndex.set(time, uniqueSourceIndex.has(time) ? null : sourceIndex);
    }
    const lower = [];
    const upper = [];
    for (let index = 0; index < times.length; index++) {
      const sourceIndex = uniqueSourceIndex.get(times[index]);
      lower.push(
        typeof sourceIndex === "number" && values[index] !== null && sourceLower[sourceIndex] !== null ? exactBinary64WeightedSum([sourceLower[sourceIndex]], [scale]) : null
      );
      upper.push(
        typeof sourceIndex === "number" && values[index] !== null && sourceUpper[sourceIndex] !== null ? exactBinary64WeightedSum([sourceUpper[sourceIndex]], [scale]) : null
      );
    }
    derivedUncertainty = {
      kind: String(uncertainty.kind),
      unit: targetUnit,
      lower,
      upper,
      ...uncertainty.method !== void 0 ? { method: uncertainty.method } : {},
      ...uncertainty.level !== void 0 ? { level: uncertainty.level } : {},
      ...uncertainty.coverage !== void 0 ? { coverage: uncertainty.coverage } : {},
      ...uncertainty.lowerQuantile !== void 0 ? { lowerQuantile: uncertainty.lowerQuantile } : {},
      ...uncertainty.upperQuantile !== void 0 ? { upperQuantile: uncertainty.upperQuantile } : {},
      ...uncertainty.basis !== void 0 ? { basis: uncertainty.basis } : {}
    };
  }
  return {
    raw: {
      compartmentId: id,
      signalId: String(selected[0].raw.signalId),
      signalLabel: label,
      values: { kind: String(record4(selected[0].raw.values).kind), unit: targetUnit, values }
    },
    sourceIndex: views.length,
    aggregate: true,
    trace,
    ...derivedUncertainty ? { uncertainty: derivedUncertainty } : {}
  };
}
function compartmentModel(requestValue) {
  const request = record4(requestValue);
  const data = record4(request.data);
  const parameters = record4(request.parameters);
  const window = windowFrom(data.window);
  const rawSeries = array4(data.series).map(record4);
  const compartmentIds = stringArray(data.compartmentIds);
  const compartmentIndexById = new Map(compartmentIds.map((id, index) => [id, index]));
  const labels = stringArray(data.compartmentLabels);
  const parents = array4(data.compartmentParentIds);
  const distances = record4(data.compartmentPathDistances);
  const distanceValues = array4(distances.values);
  const targetValueUnit = String(record4(rawSeries[0].values).unit);
  const uncertainty = record4(parameters.uncertainty);
  const views = rawSeries.map((raw, sourceIndex) => {
    const time = record4(raw.time);
    const values = record4(raw.values);
    const compartmentId = String(raw.compartmentId);
    const signalId = String(raw.signalId);
    const compartmentIndex = compartmentIndexById.get(compartmentId) ?? -1;
    return {
      raw,
      sourceIndex,
      uncertainty,
      trace: prepareIndependentTrace({
        id: `compartment-source-${String(sourceIndex)}`,
        label: `${labels[compartmentIndex] ?? compartmentId}: ${String(raw.signalLabel ?? signalId)}`,
        observationKind: "point_sample",
        times: numberArray(time.values),
        timeUnit: String(time.unit),
        values: nullableNumberArray(values.values),
        valueUnit: String(values.unit)
      }, {
        window,
        ...duplicateOptions(parameters),
        targetValueUnit
      })
    };
  });
  const aggregateDeclaration = record4(parameters.compartmentAggregate);
  const aggregate4 = Object.keys(aggregateDeclaration).length > 0 ? deriveIndependentCompartmentAggregate(aggregateDeclaration, views, window) : void 0;
  const byCompartment = /* @__PURE__ */ new Map();
  for (const view of views) {
    const id = String(view.raw.compartmentId);
    const existing = byCompartment.get(id);
    if (existing) existing.push(view);
    else byCompartment.set(id, [view]);
  }
  const rows = [];
  for (let rowIndex = 0; rowIndex < compartmentIds.length; rowIndex++) {
    const compartmentId = compartmentIds[rowIndex];
    const common = [
      String(data.cellId),
      rowIndex,
      compartmentId,
      labels[rowIndex] ?? compartmentId,
      typeof parents[rowIndex] === "string" ? parents[rowIndex] : null,
      typeof distanceValues[rowIndex] === "number" ? distanceValues[rowIndex] : null,
      typeof distances.unit === "string" ? distances.unit : null
    ];
    const members = byCompartment.get(compartmentId) ?? [];
    if (members.length === 0) {
      rows.push([...common, "no", null, null, null, null, null, null, null]);
      continue;
    }
    for (const view of members) {
      for (const observation of view.trace.observations) {
        rows.push([
          ...common,
          "yes",
          String(view.raw.signalId),
          observation.recordedTime,
          view.trace.sourceTimeUnit,
          observation.value,
          view.trace.valueUnit,
          observation.value === null ? "true" : "false",
          traceCell(observation.sourceOrdinals)
        ]);
      }
    }
  }
  const layout = String(parameters.layout);
  let geometry = [];
  if (layout === "heatmap") {
    const heatmapRows = [];
    for (const id of compartmentIds) heatmapRows.push(...byCompartment.get(id) ?? []);
    if (aggregate4) heatmapRows.push(aggregate4);
    geometry = heatmapRows.flatMap((view) => view.trace.observations.map((observation) => carrier(
      view.aggregate ? "aggregate" : "heatmap_cells",
      sourceProvenance(
        view.trace,
        observation,
        view.aggregate ? "aggregate_heatmap_cell" : "heatmap_cell"
      )
    )));
  } else {
    const panels = [];
    if (layout === "small_multiples") {
      for (const id of compartmentIds) panels.push({ id, series: byCompartment.get(id) ?? [] });
      if (aggregate4) panels.push({ id: aggregate4.trace.id, series: [aggregate4] });
    } else {
      const selected = new Set(stringArray(parameters.overlayCompartmentIds));
      panels.push({
        id: "overlay",
        series: [
          ...views.filter((view) => selected.has(String(view.raw.compartmentId))),
          ...aggregate4 ? [aggregate4] : []
        ]
      });
    }
    geometry = panels.flatMap((panel) => panel.series.flatMap((view) => normalTraceGeometry(
      view,
      view.aggregate ? { paths: "aggregate", samples: "aggregate", uncertainty: "uncertainty" } : { paths: "series_paths", samples: "samples", uncertainty: "uncertainty" },
      false
    )));
  }
  const traces = views.map((view) => view.trace);
  const finiteValues = traces.flatMap((trace) => trace.values).filter((value) => value !== null);
  const recorded = new Set(views.map((view) => String(view.raw.compartmentId))).size;
  const missing = traces.reduce(
    (sum, trace) => sum + trace.observations.filter((observation) => observation.value === null).length,
    0
  );
  const quantityKind = joinUnique(rawSeries.map((raw) => String(record4(raw.values).kind)));
  const summary = {
    cellLabel: String(data.cellLabel ?? data.cellId),
    signalLabel: joinUnique(rawSeries.map((raw) => String(raw.signalLabel ?? raw.signalId))),
    quantityKind,
    unit: targetValueUnit,
    compartmentCount: String(compartmentIds.length),
    windowStart: summaryNumber2(window.start),
    windowStop: summaryNumber2(window.stop),
    timeUnit: window.unit,
    layoutMode: layout,
    scaleStatement: layout === "heatmap" ? "one global colour domain over all accepted cells" : layout === "small_multiples" ? `${String(parameters.yScale)} y domains` : "one shared y domain for the declared overlay selection",
    compartmentOrderBasis: String(data.compartmentOrderBasis),
    recordedCompartmentCount: String(recorded),
    notRecordedCount: String(compartmentIds.length - recorded),
    sampleCount: String(traces.reduce((sum, trace) => sum + trace.observations.length, 0)),
    missingSampleCount: String(missing),
    valueMin: finiteValues.length > 0 ? summaryNumber2(sequenceMinimum(finiteValues)) : "not available",
    valueMax: finiteValues.length > 0 ? summaryNumber2(sequenceMaximum(finiteValues)) : "not available",
    duplicateTimeStatement: `Duplicate timestamps: ${duplicateStatement(parameters, traces)}.`,
    aggregateStatement: aggregate4 ? `Aggregate: ${String(aggregateDeclaration.method)} over ${stringArray(aggregateDeclaration.compartmentIds).length} explicitly selected compartments with ${String(aggregateDeclaration.weighting)} weighting.` : "No compartment aggregate was requested.",
    uncertaintyStatement: uncertaintyStatement(traces.map(() => uncertainty)),
    universeStatement: data.compartmentUniverseComplete === true ? "The declared compartment universe is complete." : "The declared compartments are an incomplete subset of the cell.",
    compactionStatement: noCompactionStatement2()
  };
  const declared = uncertainty.kind !== void 0 && uncertainty.kind !== "none" ? 1 : 0;
  const shownViews = [
    ...views,
    ...aggregate4 ? [aggregate4] : []
  ];
  const shown = shownViews.filter((view) => hasCompleteDrawableUncertainty(view.trace, view.uncertainty)).length;
  return {
    rows,
    geometry,
    summary,
    disclosures: traceDisclosureFacts(
      request,
      parameters,
      traces,
      traces.map(() => uncertainty),
      {
        nodeUniverseComplete: data.compartmentUniverseComplete === true,
        uncertaintyKind: declared > 0 ? "provided" : "none",
        uncertaintySeriesDeclared: declared,
        uncertaintySeriesShown: shown,
        uncertaintySeriesTotal: views.length + (aggregate4 ? 1 : 0),
        ...declared === 0 && typeof uncertainty.reason === "string" ? { uncertaintyReason: uncertainty.reason } : {}
      }
    )
  };
}
var COMPARTMENT_AUTHORITY = defineAuthorityEvaluator(
  authorityEvaluatorId("neuro.compartment_trace", 2),
  (request) => {
    const model = compartmentModel(request);
    return {
      "table.rows": rowSequence(model.rows),
      "geometry.sequence": geometrySequence(model.geometry),
      "summary.facts": summaryFactMap(model.summary),
      "disclosure.facts": disclosureFactMap(model.disclosures)
    };
  }
);
function emptyIndependentWeightCarrierCounts() {
  return {
    source_observation: { total: 0, missing: 0 },
    source_state_witness: { total: 0, missing: 0 },
    caller_reconstruction_point: { total: 0, missing: 0 },
    derived_aggregate_evaluation: { total: 0, missing: 0 },
    declared_aggregate_point: { total: 0, missing: 0 },
    declared_initial_state: { total: 0, missing: 0 }
  };
}
function independentWeightCarrierStatement(counts, sourceReadingCount, missingSourceReadingCount, excludedSourceReadingCount, reconstructionPointCount, retainedReconstructionRowCount, missingReconstructionPointCount, excludedReconstructionPointCount) {
  const clause = (kind, label) => `${label} ${counts[kind].total} rows (${counts[kind].missing} missing rows)`;
  return `Carriers: ${[
    `source readings ${sourceReadingCount} in ${counts.source_observation.total} retained rows (${missingSourceReadingCount} missing readings; ${excludedSourceReadingCount} excluded outside the window)`,
    clause("source_state_witness", "source state witnesses"),
    clause("caller_reconstruction_point", "caller reconstruction point carriers"),
    `reconstruction semantics ${reconstructionPointCount} raw points in ${retainedReconstructionRowCount} retained rows (${missingReconstructionPointCount} missing retained points; ${excludedReconstructionPointCount} excluded outside the window)`,
    clause("derived_aggregate_evaluation", "Cortexel-derived aggregate evaluations"),
    clause("declared_aggregate_point", "caller-declared aggregate points"),
    clause("declared_initial_state", "influential declared initial states")
  ].join("; ")}.`;
}
function coalesceIndependentWeightIntervals(intervals) {
  const output = [];
  for (const interval of intervals) {
    const previous = output[output.length - 1];
    if (previous && interval.start <= previous.stop) {
      output[output.length - 1] = {
        start: previous.start,
        stop: Math.max(previous.stop, interval.stop)
      };
    } else {
      output.push({ start: interval.start, stop: interval.stop });
    }
  }
  return output;
}
function independentDeclaredInitialHold(view, observation, membershipLifetimes) {
  if (observation.kind !== "event_updated" || observation.updateSemantics !== "value_after_update" || view.initialValue === void 0) return void 0;
  const lifetime = membershipLifetimes?.find((candidate) => view.initialStateTime >= candidate.start && view.initialStateTime < candidate.stop);
  if (membershipLifetimes && !lifetime) return void 0;
  const stop = Math.min(
    view.stateTrace.observations[0]?.time ?? view.stateStop,
    view.recordedStop,
    lifetime?.stop ?? view.recordedStop
  );
  const start = Math.max(
    view.stateStart,
    view.recordedStart,
    lifetime?.start ?? view.recordedStart
  );
  return stop > start ? { start, stop, value: view.initialValue } : void 0;
}
function independentEventUpdatedMaterialization(view, updateSemantics, membershipLifetimes) {
  const segments = [];
  const clippedInitialHold = independentDeclaredInitialHold(view, {
    kind: "event_updated",
    updateSemantics
  }, membershipLifetimes);
  if (clippedInitialHold && clippedInitialHold.stop > clippedInitialHold.start) {
    segments.push({
      start: clippedInitialHold.start,
      stop: clippedInitialHold.stop,
      value: clippedInitialHold.value,
      provenance: {
        seriesId: view.trace.id,
        sourceOrdinals: [],
        atomKind: "hold_segment",
        segmentSource: "declared_initial_weight"
      }
    });
  }
  for (let index = 0; index < view.stateTrace.observations.length; index++) {
    const observation = view.stateTrace.observations[index];
    const sourceStart = updateSemantics === "value_after_update" ? observation.time : index === 0 ? view.stateStart : view.stateTrace.observations[index - 1].time;
    const sourceStop = updateSemantics === "value_after_update" ? view.stateTrace.observations[index + 1]?.time ?? view.stateStop : observation.time;
    const lifetime = membershipLifetimes?.find((candidate) => updateSemantics === "value_after_update" ? observation.time >= candidate.start && observation.time < candidate.stop : observation.time > candidate.start && observation.time <= candidate.stop);
    if (membershipLifetimes && !lifetime) continue;
    const start = Math.max(sourceStart, view.recordedStart, lifetime?.start ?? sourceStart);
    const stop = Math.min(sourceStop, view.recordedStop, lifetime?.stop ?? sourceStop);
    if (!(stop > start)) continue;
    segments.push({
      start,
      stop,
      value: observation.value,
      observation,
      provenance: sourceProvenance(view.stateTrace, observation, "hold_segment")
    });
  }
  const runs = [];
  const retainedOrdinals = new Set(
    view.trace.observations.flatMap((observation) => observation.sourceOrdinals)
  );
  const witnesses = /* @__PURE__ */ new Map();
  let paintedInitial = false;
  for (let segmentIndex = 0; segmentIndex < segments.length; segmentIndex++) {
    const segment = segments[segmentIndex];
    if (segment.observation && segment.observation.sourceOrdinals.some(
      (ordinal) => !retainedOrdinals.has(ordinal)
    )) {
      const role = segment.observation.time < view.recordedStart ? "carry_in" : "look_ahead";
      witnesses.set(
        `${role}:${segment.observation.sourceOrdinals.join(",")}`,
        { observation: segment.observation, role }
      );
    }
    if (record4(segment.provenance).segmentSource === "declared_initial_weight") paintedInitial = true;
    if (segment.value === null || segment.provenance === void 0) continue;
    const startProvenance = {
      ...record4(segment.provenance),
      segmentIndex,
      endpoint: "start"
    };
    const stopProvenance = {
      ...record4(segment.provenance),
      segmentIndex,
      endpoint: "stop"
    };
    const previous = runs[runs.length - 1];
    if (previous && previous.vertices[previous.vertices.length - 1].time === segment.start) {
      previous.vertices.push(
        { time: segment.start, value: segment.value, provenance: startProvenance },
        { time: segment.stop, value: segment.value, provenance: stopProvenance }
      );
    } else {
      runs.push({
        vertices: [
          { time: segment.start, value: segment.value, provenance: startProvenance },
          { time: segment.stop, value: segment.value, provenance: stopProvenance }
        ]
      });
    }
  }
  if (updateSemantics === "value_after_update" && view.recordedFinalEdgeInclusive) {
    const terminalObservation = view.stateTrace.observations.find((observation) => observation.time === view.recordedStop && observation.value !== null);
    const terminalLifetime = membershipLifetimes?.find((lifetime) => terminalObservation !== void 0 && terminalObservation.time >= lifetime.start && terminalObservation.time < lifetime.stop);
    if (terminalObservation && (!membershipLifetimes || terminalLifetime)) {
      runs.push({
        vertices: [{
          time: terminalObservation.time,
          value: terminalObservation.value,
          provenance: {
            seriesId: view.stateTrace.id,
            sourceOrdinals: [...terminalObservation.sourceOrdinals],
            atomKind: "observation_vertex",
            terminalClosedStop: true
          }
        }]
      });
    }
  }
  return {
    runs,
    witnesses: [...witnesses.values()],
    initialStatePainted: paintedInitial
  };
}
function eventUpdatedRuns(view, updateSemantics) {
  return [...independentEventUpdatedMaterialization(view, updateSemantics).runs];
}
function mergeIndependentWeightStateWitnesses(...groups) {
  const merged = /* @__PURE__ */ new Map();
  for (const group of groups) {
    for (const witness of group ?? []) {
      const key = `${witness.role}:${witness.observation.sourceOrdinals.join(",")}`;
      const prior = merged.get(key);
      merged.set(key, {
        observation: witness.observation,
        role: witness.role,
        ...prior?.consultedByDerivedAggregate === true || witness.consultedByDerivedAggregate === true ? { consultedByDerivedAggregate: true } : {}
      });
    }
  }
  return [...merged.values()].sort((left, right) => left.observation.time - right.observation.time || left.observation.sourceOrdinals[0] - right.observation.sourceOrdinals[0]);
}
function prepareWeightMember(raw, observation, targetValueUnit, data, parameters, window, hasRecordedInterval, sourceIndex) {
  const time = record4(raw.time);
  const values = record4(raw.values);
  const interval = hasRecordedInterval ? record4(raw.recordedInterval) : record4(data.window);
  const intervalUnit = String(interval.unit);
  const intervalStart = intervalUnit === window.unit ? Number(interval.start) : convert(Number(interval.start), intervalUnit, window.unit);
  const intervalStop = intervalUnit === window.unit ? Number(interval.stop) : convert(Number(interval.stop), intervalUnit, window.unit);
  const recordedStart = Math.max(window.start, intervalStart);
  const recordedStop = Math.min(window.stop, intervalStop);
  const intervalFinalEdgeInclusive = typeof interval.boundary === "string" && interval.boundary.endsWith("]");
  const recordedFinalEdgeInclusive = (recordedStop !== window.stop || window.finalEdgeInclusive) && (recordedStop !== intervalStop || intervalFinalEdgeInclusive);
  const input = {
    id: String(raw.edgeId ?? raw.groupId),
    label: String(raw.label ?? raw.groupLabel ?? raw.edgeId ?? raw.groupId),
    observationKind: observation.kind === "event_updated" ? "piecewise_constant" : "point_sample",
    times: numberArray(time.values),
    timeUnit: String(time.unit),
    values: nullableNumberArray(values.values),
    valueUnit: String(values.unit)
  };
  const trace = prepareIndependentTrace(input, {
    window: {
      start: recordedStart,
      stop: recordedStop,
      unit: window.unit,
      finalEdgeInclusive: recordedFinalEdgeInclusive
    },
    ...duplicateOptions(parameters),
    targetValueUnit
  });
  const stateTrace = prepareIndependentTrace(input, {
    window: {
      start: intervalStart,
      stop: intervalStop,
      unit: window.unit,
      finalEdgeInclusive: intervalFinalEdgeInclusive
    },
    ...duplicateOptions(parameters),
    targetValueUnit
  });
  const initialQuantity = record4(record4(raw.initialWeight).quantity);
  const initialValue = typeof initialQuantity.value === "number" ? String(initialQuantity.unit) === targetValueUnit ? initialQuantity.value : convert(initialQuantity.value, String(initialQuantity.unit), targetValueUnit) : void 0;
  const view = {
    raw,
    sourceIndex,
    uncertainty: record4(raw.uncertainty),
    trace,
    stateTrace,
    recordedStart,
    recordedStop,
    recordedFinalEdgeInclusive,
    stateStart: intervalStart,
    stateStop: intervalStop,
    initialStateTime: intervalStart,
    ...initialValue === void 0 ? {} : { initialValue }
  };
  if (observation.kind === "event_updated") {
    return {
      ...view,
      renderRuns: eventUpdatedRuns(
        view,
        observation.updateSemantics === "value_before_update" ? "value_before_update" : "value_after_update"
      )
    };
  }
  if (observation.kind === "interpolated_trajectory") {
    if (independentTrajectoryNeedsExcludedKnot(view)) {
      throw new Error("the accepted reconstruction depends on an excluded boundary knot");
    }
    return { ...view, renderRuns: independentTrajectoryRuns(view) };
  }
  return view;
}
function countForObservation(counts, ordinals) {
  const selected = ordinals.map((ordinal) => counts[ordinal]);
  return selected.length > 0 && selected.every((value) => value === selected[0]) ? selected[0] : null;
}
function independentWeightRenderTopology(runs, sourceOrdinals, timeUnit, declaredInitialState = false) {
  let matchedRunOrdinal = null;
  let paintedFrom;
  let paintedUntil;
  for (let renderRunOrdinal = 0; renderRunOrdinal < (runs?.length ?? 0); renderRunOrdinal++) {
    let matchedRun = false;
    for (const vertex of runs[renderRunOrdinal].vertices) {
      const provenance = record4(vertex.provenance);
      const provenanceOrdinals = array4(provenance.sourceOrdinals);
      const matches = declaredInitialState ? provenance.segmentSource === "declared_initial_weight" : provenanceOrdinals.length === sourceOrdinals.length && provenanceOrdinals.every((value, index) => value === sourceOrdinals[index]);
      if (!matches) continue;
      matchedRun = true;
      if (provenance.atomKind === "hold_segment") {
        if (provenance.endpoint === "start") paintedFrom = vertex.time;
        if (provenance.endpoint === "stop") paintedUntil = vertex.time;
      }
    }
    if (!matchedRun) continue;
    if (matchedRunOrdinal !== null && matchedRunOrdinal !== renderRunOrdinal) {
      throw new Error("one independently derived weight carrier belongs to multiple render runs");
    }
    matchedRunOrdinal = renderRunOrdinal;
  }
  return {
    paintedInterval: paintedFrom !== void 0 && paintedUntil !== void 0 && paintedUntil > paintedFrom ? structuredCell4({ from: paintedFrom, until: paintedUntil, unit: timeUnit }) : null,
    renderRunOrdinal: matchedRunOrdinal
  };
}
function independentDuplicateResolutionMetadata(observation, parameters) {
  if (observation.replicateCount <= 1) return {};
  const options = duplicateOptions(parameters);
  if (options.duplicatePolicy !== "aggregate" || typeof options.aggregateMethod !== "string") {
    throw new Error("independent weight duplicate carrier lacks its aggregate method");
  }
  return {
    duplicateResolution: {
      policy: "aggregate",
      method: options.aggregateMethod
    }
  };
}
function independentPreparedWeightMetadata(semantics, observation, parameters) {
  const metadata = {
    ...semantics.kind === "interpolated_trajectory" ? {
      reconstruction: {
        method: semantics.method ?? null,
        interpolant: semantics.interpolant ?? null,
        reconstructedBy: semantics.reconstructedBy ?? null
      }
    } : {},
    ...independentDuplicateResolutionMetadata(observation, parameters)
  };
  return Object.keys(metadata).length === 0 ? null : structuredCell4(metadata);
}
function bindIndependentWeightRunOrdinals(runs) {
  return runs.map((run, renderRunOrdinal) => ({
    vertices: run.vertices.map((vertex) => ({
      ...vertex,
      provenance: {
        ...record4(vertex.provenance),
        renderRunOrdinal
      }
    }))
  }));
}
function independentWeightModelsInData(data) {
  const records = data.mode === "preaggregated" ? [record4(data.aggregate)] : array4(data.series).map(record4);
  return [...new Set(records.flatMap((raw) => {
    const aggregateModels = array4(raw.synapseModels).filter(
      (model) => typeof model === "string"
    );
    return aggregateModels.length > 0 ? aggregateModels : typeof raw.synapseModel === "string" ? [raw.synapseModel] : [];
  }))];
}
function independentWeightModelCell(raw, data, parameters) {
  const aggregateModels = array4(raw.synapseModels).filter(
    (model) => typeof model === "string"
  );
  const actualModels = aggregateModels.length > 0 ? aggregateModels : typeof raw.synapseModel === "string" ? [raw.synapseModel] : [];
  const comparability = record4(parameters.weightComparability);
  const mode = String(comparability.mode);
  const declaredModels = mode === "declared_comparable_models" ? array4(comparability.comparableModels).filter(
    (model) => typeof model === "string"
  ) : independentWeightModelsInData(data);
  return structuredCell4({
    kind: "weight_model_comparability",
    actualModels,
    declaredModels,
    comparabilityMode: mode
  });
}
function independentWeightRows(data, parameters, entries) {
  const scopeSummary = weightTraceScopeSummaryCell(data.scope);
  const rows = [];
  const carrierCounts = emptyIndependentWeightCarrierCounts();
  let sourceReadingCount = 0;
  let missingSourceReadingCount = 0;
  const account = (kind, value) => {
    carrierCounts[kind].total++;
    if (value === null) carrierCounts[kind].missing++;
  };
  const append = (entry) => {
    const { view, observation: semantics, carrierKind, counts } = entry;
    const endpoints = record4(view.raw.endpoints);
    const eventKinds = array4(view.raw.eventKinds);
    const initialInfluential = entry.initialStatePainted === true || entry.initialStateConsumedByDerivedAggregate === true;
    const initialHold = initialInfluential ? independentDeclaredInitialHold(view, semantics, entry.membershipLifetimes) : void 0;
    const appendWitness = (witness, carrierOrdinal) => {
      const sample = witness.observation;
      const ordinal = sample.sourceOrdinals.length === 1 ? sample.sourceOrdinals[0] : void 0;
      const [lower, upper, uncertaintyMethod] = uncertaintyCells(
        view.uncertainty,
        ordinal,
        view.stateTrace,
        sample.value
      );
      const rowEvents = sample.sourceOrdinals.flatMap((sourceOrdinal) => typeof eventKinds[sourceOrdinal] === "string" ? [eventKinds[sourceOrdinal]] : []);
      const topology2 = independentWeightRenderTopology(
        entry.renderRuns,
        sample.sourceOrdinals,
        view.stateTrace.timeUnit
      );
      account("source_state_witness", sample.value);
      rows.push([
        String(view.raw.edgeId ?? view.raw.groupId),
        String(view.raw.label ?? view.raw.groupLabel ?? view.raw.edgeId ?? view.raw.groupId),
        typeof endpoints.sourceId === "string" ? endpoints.sourceId : null,
        typeof endpoints.targetId === "string" ? endpoints.targetId : null,
        independentWeightModelCell(view.raw, data, parameters),
        sample.time,
        view.stateTrace.timeUnit,
        sample.value,
        view.stateTrace.valueUnit,
        String(semantics.kind),
        typeof semantics.updateSemantics === "string" ? semantics.updateSemantics : null,
        topology2.paintedInterval,
        topology2.renderRunOrdinal,
        rowEvents.length === 0 ? null : traceCell(rowEvents),
        sample.value === null ? "true" : "false",
        null,
        null,
        null,
        lower,
        upper,
        uncertaintyMethod,
        structuredCell4(view.raw.initialWeight),
        structuredCell4(view.raw.bounds),
        "source_state_witness",
        carrierOrdinal,
        structuredCell4({
          role: witness.role,
          directlyPainted: topology2.renderRunOrdinal !== null,
          consultedByDerivedAggregate: witness.consultedByDerivedAggregate === true,
          ...independentDuplicateResolutionMetadata(sample, parameters)
        }),
        traceCell(sample.sourceOrdinals),
        scopeSummary
      ]);
    };
    const witnesses = [...entry.stateWitnesses ?? []];
    for (let index = 0; index < witnesses.length; index++) {
      if (witnesses[index].role === "carry_in") appendWitness(witnesses[index], index);
    }
    if (initialHold) {
      const topology2 = independentWeightRenderTopology(
        entry.renderRuns,
        [],
        view.trace.timeUnit,
        true
      );
      account("declared_initial_state", initialHold.value);
      rows.push([
        String(view.raw.edgeId ?? view.raw.groupId),
        String(view.raw.label ?? view.raw.groupLabel ?? view.raw.edgeId ?? view.raw.groupId),
        typeof endpoints.sourceId === "string" ? endpoints.sourceId : null,
        typeof endpoints.targetId === "string" ? endpoints.targetId : null,
        independentWeightModelCell(view.raw, data, parameters),
        initialHold.start,
        view.trace.timeUnit,
        initialHold.value,
        view.trace.valueUnit,
        String(semantics.kind),
        String(semantics.updateSemantics),
        topology2.paintedInterval,
        topology2.renderRunOrdinal,
        null,
        "false",
        null,
        null,
        null,
        null,
        null,
        null,
        structuredCell4(view.raw.initialWeight),
        structuredCell4(view.raw.bounds),
        "declared_initial_state",
        0,
        structuredCell4({
          directlyPainted: topology2.renderRunOrdinal !== null,
          consumedByDerivedAggregate: entry.initialStateConsumedByDerivedAggregate === true,
          declaredAt: view.initialStateTime,
          declaredAtUnit: view.trace.timeUnit
        }),
        null,
        scopeSummary
      ]);
    }
    for (let index = 0; index < view.trace.observations.length; index++) {
      const sample = view.trace.observations[index];
      const ordinal = sample.sourceOrdinals.length === 1 ? sample.sourceOrdinals[0] : void 0;
      const [lower, upper, uncertaintyMethod] = uncertaintyCells(
        view.uncertainty,
        ordinal,
        view.trace,
        sample.value
      );
      const observationKind = String(semantics.kind);
      const updateSemantics = typeof semantics.updateSemantics === "string" ? semantics.updateSemantics : null;
      const topology2 = independentWeightRenderTopology(
        entry.renderRuns,
        sample.sourceOrdinals,
        view.trace.timeUnit
      );
      const isSourceObservation = carrierKind === "source_observation";
      const hasSourceLineage = isSourceObservation || semantics.kind === "interpolated_trajectory";
      const rowEvents = isSourceObservation ? sample.sourceOrdinals.flatMap((sourceOrdinal) => typeof eventKinds[sourceOrdinal] === "string" ? [eventKinds[sourceOrdinal]] : []) : [];
      account(carrierKind, sample.value);
      if (isSourceObservation) {
        sourceReadingCount += sample.replicateCount;
        missingSourceReadingCount += sample.sourceValues.filter((value) => value === null).length;
      }
      rows.push([
        String(view.raw.edgeId ?? view.raw.groupId),
        String(view.raw.label ?? view.raw.groupLabel ?? view.raw.edgeId ?? view.raw.groupId),
        typeof endpoints.sourceId === "string" ? endpoints.sourceId : null,
        typeof endpoints.targetId === "string" ? endpoints.targetId : null,
        independentWeightModelCell(view.raw, data, parameters),
        sample.time,
        view.trace.timeUnit,
        sample.value,
        view.trace.valueUnit,
        observationKind,
        updateSemantics,
        topology2.paintedInterval,
        topology2.renderRunOrdinal,
        rowEvents.length === 0 ? null : traceCell(rowEvents),
        sample.value === null ? "true" : "false",
        hasSourceLineage ? sample.replicateCount : null,
        counts ? countForObservation(counts.memberCounts, sample.sourceOrdinals) : null,
        counts ? countForObservation(counts.contributingCounts, sample.sourceOrdinals) : null,
        lower,
        upper,
        uncertaintyMethod,
        structuredCell4(view.raw.initialWeight),
        structuredCell4(view.raw.bounds),
        carrierKind,
        index,
        independentPreparedWeightMetadata(semantics, sample, parameters),
        hasSourceLineage ? traceCell(sample.sourceOrdinals) : null,
        scopeSummary
      ]);
    }
    for (let index = 0; index < witnesses.length; index++) {
      if (witnesses[index].role === "look_ahead") appendWitness(witnesses[index], index);
    }
  };
  for (const entry of entries) append(entry);
  return { rows, carrierCounts, sourceReadingCount, missingSourceReadingCount };
}
function weightAggregateValue(values, method) {
  if (values.length === 0) return null;
  if (method === "mean") return exactBinary64Mean(values);
  if (method === "median") return quantile(values, 0.5);
  if (method === "min") return sequenceMinimum(values);
  return sequenceMaximum(values);
}
function independentMemberAvailability(member, lifetimes) {
  return lifetimes.flatMap((interval) => {
    const start = Math.max(interval.start, member.recordedStart);
    const stop = Math.min(interval.stop, member.recordedStop);
    return stop > start ? [{ start, stop }] : [];
  });
}
function independentUnionAvailability(members, intervals) {
  const pieces = members.flatMap((member) => independentMemberAvailability(member, intervals.get(member.trace.id) ?? []));
  pieces.sort((left, right) => left.start - right.start || left.stop - right.stop);
  return coalesceIndependentWeightIntervals(pieces);
}
function independentAvailabilityTransitions(members, intervals) {
  const transitions = members.flatMap((member) => independentMemberAvailability(member, intervals.get(member.trace.id) ?? []).flatMap((interval) => [interval.start, interval.stop]));
  transitions.sort((left, right) => left - right);
  return transitions.filter((time, index) => index === 0 || time !== transitions[index - 1]);
}
function independentAvailabilityConnects(availability, from, to) {
  if (!(to > from)) return true;
  return availability.some((interval) => interval.start <= from && interval.stop >= to);
}
function crossesIndependentAvailabilityTransition(transitions, from, to) {
  let lower = 0;
  let upper = transitions.length;
  while (lower < upper) {
    const middle = lower + Math.floor((upper - lower) / 2);
    if (transitions[middle] <= from) lower = middle + 1;
    else upper = middle;
  }
  return lower < transitions.length && transitions[lower] < to;
}
function independentPointRuns(view, availability, filterPointsByAvailability, membershipLifetimes = [], availabilityTransitions = []) {
  const runs = [];
  const markerObservationIndexes = [];
  let current;
  for (let index = 0; index < view.trace.observations.length; index++) {
    const observation = view.trace.observations[index];
    const belongs = membershipLifetimes.some((interval) => observation.time >= interval.start && observation.time < interval.stop);
    const recorded = observation.time >= view.recordedStart && (observation.time < view.recordedStop || view.recordedFinalEdgeInclusive && observation.time === view.recordedStop);
    const available = !filterPointsByAvailability || belongs && recorded;
    if (!available || observation.value === null) {
      current = void 0;
      continue;
    }
    markerObservationIndexes.push(index);
    const previousTime = current?.vertices[current.vertices.length - 1]?.time;
    if (!current || previousTime === void 0 || crossesIndependentAvailabilityTransition(
      availabilityTransitions,
      previousTime,
      observation.time
    ) || !independentAvailabilityConnects(availability, previousTime, observation.time)) {
      current = { vertices: [] };
      runs.push(current);
    }
    current.vertices.push({
      time: observation.time,
      value: observation.value,
      provenance: sourceProvenance(view.trace, observation, "observation_vertex")
    });
  }
  return { runs, markerObservationIndexes };
}
function independentMarkerIndexesForRuns(view, runs) {
  const lineages = /* @__PURE__ */ new Set();
  for (const run of runs) {
    for (const vertex of run.vertices) {
      const ordinals = array4(record4(vertex.provenance).sourceOrdinals);
      if (ordinals.length > 0) lineages.add(canonicalize(ordinals));
    }
  }
  return view.trace.observations.flatMap((observation, index) => lineages.has(canonicalize([...observation.sourceOrdinals])) ? [index] : []);
}
function independentTrajectoryNeedsExcludedKnot(view) {
  const retained = new Set(
    view.trace.observations.flatMap((observation) => observation.sourceOrdinals)
  );
  const observations = view.stateTrace.observations;
  for (let index = 1; index < observations.length; index++) {
    const left = observations[index - 1];
    const right = observations[index];
    if (left.value === null || right.value === null || !(right.time > left.time)) continue;
    const overlapStart = Math.max(left.time, view.recordedStart);
    const overlapStop = Math.min(right.time, view.recordedStop);
    if (overlapStop > overlapStart && (left.sourceOrdinals.some((ordinal) => !retained.has(ordinal)) || right.sourceOrdinals.some((ordinal) => !retained.has(ordinal)))) return true;
  }
  return false;
}
function independentTrajectoryRuns(view, atomKind = "reconstruction_vertex") {
  const runs = [];
  let current;
  for (const observation of view.trace.observations) {
    if (observation.value === null) {
      current = void 0;
      continue;
    }
    if (!current) {
      current = { vertices: [] };
      runs.push(current);
    }
    current.vertices.push({
      time: observation.time,
      value: observation.value,
      provenance: sourceProvenance(view.trace, observation, atomKind)
    });
  }
  return runs;
}
function deriveIndependentWeightAggregate(data, parameters, window, members) {
  const membership = record4(data.membership);
  const aggregate4 = record4(parameters.aggregate);
  const evaluation = record4(aggregate4.evaluation);
  const observation = record4(data.observation);
  const membershipUnit = String(membership.unit);
  const intervals = /* @__PURE__ */ new Map();
  const membershipEndpointSet = /* @__PURE__ */ new Set();
  for (const memberValue of array4(membership.members)) {
    const member = record4(memberValue);
    const convertedIntervals = array4(member.intervals).map((value) => {
      const interval = record4(value);
      const converted2 = {
        start: membershipUnit === window.unit ? Number(interval.start) : convert(Number(interval.start), membershipUnit, window.unit),
        stop: membershipUnit === window.unit ? Number(interval.stop) : convert(Number(interval.stop), membershipUnit, window.unit)
      };
      membershipEndpointSet.add(converted2.start);
      membershipEndpointSet.add(converted2.stop);
      return converted2;
    });
    intervals.set(String(member.edgeId), coalesceIndependentWeightIntervals(convertedIntervals));
  }
  const selected = members.filter((member) => intervals.has(member.trace.id));
  const timeSet = /* @__PURE__ */ new Set();
  const addTime = (time) => {
    if (inWindow(time, window)) timeSet.add(time);
  };
  switch (evaluation.mode) {
    case "hold_last_observed_at_declared_times": {
      const declared = record4(evaluation.times);
      for (const value of array4(declared.values)) {
        const numericValue = Number(value);
        addTime(
          declared.unit === window.unit ? numericValue : convert(numericValue, String(declared.unit), window.unit)
        );
      }
      break;
    }
    case "shared_sample_grid":
      for (const time of selected[0]?.trace.times ?? []) addTime(time);
      break;
    case "hold_last_observed_at_union_times":
      addTime(window.start);
      if (window.finalEdgeInclusive) addTime(window.stop);
      for (const time of membershipEndpointSet) addTime(time);
      for (const member of selected) {
        for (const time of member.trace.times) addTime(time);
        addTime(member.recordedStart);
        addTime(member.recordedStop);
      }
      break;
    default:
      throw new Error(
        `unsupported independent synaptic-weight aggregate evaluation mode ${String(evaluation.mode)}`
      );
  }
  const times = [...timeSet].sort((left, right) => left - right);
  const states = selected.map((member) => {
    const lifetimes = intervals.get(member.trace.id);
    const stateTrace = observation.kind === "point_sample" ? member.trace : member.stateTrace;
    const pointIndexByTime = /* @__PURE__ */ new Map();
    for (let index = 0; index < member.trace.times.length; index++) {
      if (!pointIndexByTime.has(member.trace.times[index])) {
        pointIndexByTime.set(member.trace.times[index], index);
      }
    }
    return {
      member,
      stateTrace,
      lifetimes,
      lifetimeIndex: 0,
      before: 0,
      after: -1,
      pointIndexByTime,
      retainedSourceOrdinals: new Set(
        member.trace.observations.flatMap((candidate) => candidate.sourceOrdinals)
      )
    };
  });
  const memberCounts = [];
  const contributingCounts = [];
  const dispersion = record4(aggregate4.dispersion);
  const values = [];
  const dispersionValues = [];
  const lowerValues = [];
  const upperValues = [];
  const stateWitnessMaps = /* @__PURE__ */ new Map();
  const initialStateContributorIds = /* @__PURE__ */ new Set();
  for (const time of times) {
    const present = [];
    let memberCount = 0;
    for (const state of states) {
      while (state.lifetimeIndex < state.lifetimes.length && time >= state.lifetimes[state.lifetimeIndex].stop) state.lifetimeIndex++;
      const lifetime = state.lifetimes[state.lifetimeIndex];
      const belongs = lifetime !== void 0 && time >= lifetime.start && time < lifetime.stop;
      if (belongs) memberCount++;
      const member = state.member;
      if (!lifetime || !belongs || time < member.recordedStart || time > member.recordedStop || !member.recordedFinalEdgeInclusive && time === member.recordedStop) continue;
      let candidate;
      let candidateObservation;
      let usedInitialState = false;
      if (observation.kind === "point_sample") {
        const index = state.pointIndexByTime.get(time);
        candidate = index === void 0 ? null : member.trace.values[index];
      } else if (observation.updateSemantics === "value_before_update") {
        while (state.before < state.stateTrace.times.length && state.stateTrace.times[state.before] < time) {
          state.before++;
        }
        const candidateTime = state.stateTrace.times[state.before];
        candidateObservation = state.stateTrace.observations[state.before];
        const usable = state.before < state.stateTrace.values.length && candidateTime >= Math.max(lifetime.start, member.stateStart) && candidateTime <= lifetime.stop;
        candidate = usable ? state.stateTrace.values[state.before] : null;
        if (!usable) candidateObservation = void 0;
      } else {
        while (state.after + 1 < state.stateTrace.times.length && state.stateTrace.times[state.after + 1] <= time) state.after++;
        const candidateTime = state.stateTrace.times[state.after];
        if (state.after >= 0 && candidateTime >= Math.max(lifetime.start, member.stateStart)) {
          candidate = state.stateTrace.values[state.after];
          candidateObservation = state.stateTrace.observations[state.after];
        } else {
          const initialBelongsToLifetime = member.initialValue !== void 0 && member.initialStateTime >= lifetime.start && member.initialStateTime < lifetime.stop;
          candidate = initialBelongsToLifetime ? member.initialValue : null;
          usedInitialState = initialBelongsToLifetime;
        }
      }
      if (candidateObservation && candidateObservation.sourceOrdinals.some(
        (ordinal) => !state.retainedSourceOrdinals.has(ordinal)
      )) {
        const role = candidateObservation.time < member.recordedStart ? "carry_in" : "look_ahead";
        const byMember = stateWitnessMaps.get(member.trace.id) ?? /* @__PURE__ */ new Map();
        byMember.set(
          `${role}:${candidateObservation.sourceOrdinals.join(",")}`,
          {
            observation: candidateObservation,
            role,
            consultedByDerivedAggregate: true
          }
        );
        stateWitnessMaps.set(member.trace.id, byMember);
      }
      if (usedInitialState) initialStateContributorIds.add(member.trace.id);
      if (typeof candidate === "number") present.push(candidate);
    }
    memberCounts.push(memberCount);
    contributingCounts.push(present.length);
    const aggregateValue = weightAggregateValue(present, String(aggregate4.method));
    values.push(aggregateValue);
    if (dispersion.kind === "standard_deviation") {
      if (present.length < 2 || aggregateValue === null) {
        dispersionValues.push(null);
      } else {
        dispersionValues.push(exactBinary64SampleStandardDeviation(present));
      }
    } else if (dispersion.kind === "quantile_interval") {
      if (present.length === 0) {
        lowerValues.push(null);
        upperValues.push(null);
      } else {
        lowerValues.push(quantile(present, Number(dispersion.lowerQuantile)));
        upperValues.push(quantile(present, Number(dispersion.upperQuantile)));
      }
    } else if (dispersion.kind === "ensemble_range") {
      lowerValues.push(present.length > 0 ? sequenceMinimum(present) : null);
      upperValues.push(present.length > 0 ? sequenceMaximum(present) : null);
    }
  }
  let uncertainty;
  if (dispersion.kind === "standard_deviation") {
    uncertainty = {
      kind: "standard_deviation",
      unit: selected[0]?.trace.valueUnit ?? "nest:weight",
      values: dispersionValues,
      sampleCount: dispersionValues.map((value, index) => value === null ? null : contributingCounts[index]),
      basis: "ensemble_members"
    };
  } else if (dispersion.kind === "quantile_interval") {
    uncertainty = {
      kind: "quantile_interval",
      unit: selected[0]?.trace.valueUnit ?? "nest:weight",
      lower: lowerValues,
      upper: upperValues,
      lowerQuantile: Number(dispersion.lowerQuantile),
      upperQuantile: Number(dispersion.upperQuantile),
      method: "empirical_type_7_linear",
      sampleCount: contributingCounts.map((count) => count > 0 ? count : null),
      basis: "ensemble_members"
    };
  } else if (dispersion.kind === "ensemble_range") {
    uncertainty = {
      kind: "ensemble_range",
      unit: selected[0]?.trace.valueUnit ?? "nest:weight",
      lower: lowerValues,
      upper: upperValues,
      sampleCount: contributingCounts.map((count) => count > 0 ? count : null),
      basis: "ensemble_members"
    };
  } else {
    uncertainty = { kind: "none", reason: String(dispersion.reason ?? "not_computed") };
  }
  const groupId = String(membership.groupId);
  const groupLabel = String(membership.groupLabel ?? groupId);
  const valueUnit = selected[0]?.trace.valueUnit ?? "nest:weight";
  const raw = {
    groupId,
    groupLabel,
    synapseModels: [...new Set(selected.map((member) => String(member.raw.synapseModel)))],
    weightComparabilityMode: String(record4(parameters.weightComparability).mode),
    method: String(aggregate4.method),
    observation,
    time: { kind: "time", unit: window.unit, values: times },
    values: { kind: "synaptic_weight", unit: valueUnit, values },
    memberCounts,
    contributingCounts,
    uncertainty
  };
  let view = prepareWeightMember(
    raw,
    observation,
    valueUnit,
    data,
    parameters,
    window,
    false,
    members.length
  );
  view = {
    ...view,
    aggregate: true,
    uncertainty
  };
  return {
    view,
    memberCounts,
    contributingCounts,
    selected,
    intervals,
    stateWitnessesByMember: new Map(
      [...stateWitnessMaps].map(([id, byKey]) => [id, [...byKey.values()]])
    ),
    initialStateContributorIds
  };
}
function weightReferenceGeometry(members, parameters) {
  if (parameters.showReferenceLines !== true) return [];
  const output = [];
  for (const member of members) {
    const initial = record4(member.raw.initialWeight);
    if (typeof record4(initial.quantity).value === "number") {
      output.push(carrier("references", {
        seriesId: member.trace.id,
        atomKind: "reference_rule",
        reference: "initial"
      }));
    }
    const bounds = record4(member.raw.bounds);
    if (typeof record4(bounds.lower).value === "number") {
      output.push(carrier("references", {
        seriesId: member.trace.id,
        atomKind: "reference_rule",
        reference: "lower"
      }));
    }
    if (typeof record4(bounds.upper).value === "number") {
      output.push(carrier("references", {
        seriesId: member.trace.id,
        atomKind: "reference_rule",
        reference: "upper"
      }));
    }
  }
  return output;
}
function weightModel3(requestValue) {
  const request = record4(requestValue);
  const data = record4(request.data);
  const parameters = record4(request.parameters);
  const window = windowFrom(data.window);
  const dataObservation = record4(data.observation);
  let members = [];
  let aggregate4;
  let derived;
  let counts;
  let table;
  let drawn;
  let references;
  let modelMembers;
  let sourceMembers;
  let reconstructionMembers;
  let identifiedMembers;
  if (data.mode === "preaggregated") {
    const raw = record4(data.aggregate);
    const observation2 = record4(raw.observation);
    aggregate4 = prepareWeightMember(
      raw,
      observation2,
      String(record4(raw.values).unit),
      data,
      parameters,
      window,
      false,
      0
    );
    aggregate4 = { ...aggregate4, aggregate: true };
    counts = {
      memberCounts: numberArray(raw.memberCounts),
      contributingCounts: numberArray(raw.contributingCounts)
    };
    const eventMaterialization = observation2.kind === "event_updated" ? independentEventUpdatedMaterialization(
      aggregate4,
      observation2.updateSemantics === "value_before_update" ? "value_before_update" : "value_after_update"
    ) : void 0;
    const aggregateRenderRuns = bindIndependentWeightRunOrdinals(
      eventMaterialization?.runs ?? independentTrajectoryRuns(
        aggregate4,
        observation2.kind === "interpolated_trajectory" ? "reconstruction_vertex" : "observation_vertex"
      )
    );
    aggregate4 = { ...aggregate4, renderRuns: aggregateRenderRuns };
    table = independentWeightRows(data, parameters, [{
      view: aggregate4,
      observation: observation2,
      carrierKind: "declared_aggregate_point",
      counts,
      stateWitnesses: eventMaterialization?.witnesses,
      initialStatePainted: eventMaterialization?.initialStatePainted,
      renderRuns: aggregateRenderRuns
    }]);
    drawn = [aggregate4];
    references = [aggregate4];
    modelMembers = [aggregate4];
    sourceMembers = [];
    reconstructionMembers = observation2.kind === "interpolated_trajectory" ? [aggregate4] : [];
    identifiedMembers = [];
  } else {
    const rawSeries = array4(data.series).map(record4);
    const derivedDisplay = parameters.display === "aggregate_derived" || parameters.display === "aggregate_derived_with_members";
    const membershipIds = new Set(
      array4(record4(data.membership).members).map((candidate) => String(record4(candidate).edgeId))
    );
    const targetRaw = derivedDisplay ? rawSeries.find((candidate) => membershipIds.has(String(candidate.edgeId))) : rawSeries[0];
    const targetUnit = String(record4(targetRaw?.values).unit);
    members = rawSeries.map((raw, index) => prepareWeightMember(
      raw,
      dataObservation,
      targetUnit,
      data,
      parameters,
      window,
      true,
      index
    ));
    if (parameters.display === "aggregate_derived" || parameters.display === "aggregate_derived_with_members") {
      derived = deriveIndependentWeightAggregate(data, parameters, window, members);
      aggregate4 = derived.view;
      counts = {
        memberCounts: derived.memberCounts,
        contributingCounts: derived.contributingCounts
      };
    }
    const selected = derived?.selected ?? [];
    const paintedMemberIds = parameters.display === "individual" ? new Set(members.map((member) => member.trace.id)) : parameters.display === "aggregate_derived_with_members" ? new Set(selected.map((member) => member.trace.id)) : /* @__PURE__ */ new Set();
    const eventSemantics = dataObservation.updateSemantics === "value_before_update" ? "value_before_update" : "value_after_update";
    const globalAvailabilityTransitions = derived ? independentAvailabilityTransitions(selected, derived.intervals) : [];
    const paintedEventMaterializations = /* @__PURE__ */ new Map();
    const memberRuns = /* @__PURE__ */ new Map();
    const memberMarkerIndexes = /* @__PURE__ */ new Map();
    if (dataObservation.kind === "event_updated") {
      for (const member of members) {
        if (!paintedMemberIds.has(member.trace.id)) continue;
        const materialization = parameters.display === "aggregate_derived_with_members" ? independentEventUpdatedMaterialization(
          member,
          eventSemantics,
          derived?.intervals.get(member.trace.id)
        ) : independentEventUpdatedMaterialization(member, eventSemantics);
        paintedEventMaterializations.set(member.trace.id, materialization);
        const runs = bindIndependentWeightRunOrdinals(materialization.runs);
        memberRuns.set(member.trace.id, runs);
        if (parameters.display === "aggregate_derived_with_members") {
          memberMarkerIndexes.set(
            member.trace.id,
            independentMarkerIndexesForRuns(member, runs)
          );
        }
      }
    } else {
      for (const member of members) {
        if (!paintedMemberIds.has(member.trace.id)) continue;
        if (dataObservation.kind === "point_sample" && parameters.display === "aggregate_derived_with_members") {
          const lifetimes = derived?.intervals.get(member.trace.id) ?? [];
          const point = independentPointRuns(
            member,
            independentMemberAvailability(member, lifetimes),
            true,
            lifetimes,
            globalAvailabilityTransitions
          );
          memberRuns.set(
            member.trace.id,
            bindIndependentWeightRunOrdinals(point.runs)
          );
          memberMarkerIndexes.set(
            member.trace.id,
            point.markerObservationIndexes
          );
        } else {
          memberRuns.set(
            member.trace.id,
            bindIndependentWeightRunOrdinals(independentTrajectoryRuns(
              member,
              dataObservation.kind === "interpolated_trajectory" ? "reconstruction_vertex" : "observation_vertex"
            ))
          );
        }
      }
    }
    if (aggregate4 && derived) {
      const aggregateRuns = dataObservation.kind === "event_updated" ? independentEventUpdatedMaterialization(aggregate4, eventSemantics).runs : independentPointRuns(
        aggregate4,
        independentUnionAvailability(selected, derived.intervals),
        false,
        [],
        globalAvailabilityTransitions
      ).runs;
      aggregate4 = {
        ...aggregate4,
        renderRuns: bindIndependentWeightRunOrdinals(aggregateRuns)
      };
    }
    const sourceCarrierKind = dataObservation.kind === "interpolated_trajectory" ? "caller_reconstruction_point" : "source_observation";
    const tableEntries = [
      ...members.map((member) => ({
        view: member,
        observation: dataObservation,
        carrierKind: sourceCarrierKind,
        stateWitnesses: mergeIndependentWeightStateWitnesses(
          paintedEventMaterializations.get(member.trace.id)?.witnesses,
          derived?.stateWitnessesByMember.get(member.trace.id)
        ),
        initialStatePainted: paintedEventMaterializations.get(member.trace.id)?.initialStatePainted === true,
        initialStateConsumedByDerivedAggregate: derived?.initialStateContributorIds.has(member.trace.id) === true,
        ...derived?.intervals.get(member.trace.id) ? { membershipLifetimes: derived.intervals.get(member.trace.id) } : {},
        ...memberRuns.get(member.trace.id) ? { renderRuns: memberRuns.get(member.trace.id) } : {}
      })),
      ...aggregate4 ? [{
        view: aggregate4,
        observation: dataObservation,
        carrierKind: "derived_aggregate_evaluation",
        counts,
        renderRuns: aggregate4.renderRuns
      }] : []
    ];
    table = independentWeightRows(data, parameters, tableEntries);
    const displayedSelected = parameters.display === "aggregate_derived_with_members" ? selected.map((member) => ({
      ...member,
      renderRuns: memberRuns.get(member.trace.id),
      ...memberMarkerIndexes.get(member.trace.id) ? { markerObservationIndexes: memberMarkerIndexes.get(member.trace.id) } : {}
    })) : [];
    drawn = parameters.display === "individual" ? members.map((member) => ({
      ...member,
      renderRuns: memberRuns.get(member.trace.id),
      ...memberMarkerIndexes.get(member.trace.id) ? { markerObservationIndexes: memberMarkerIndexes.get(member.trace.id) } : {}
    })) : [
      ...displayedSelected,
      ...aggregate4 ? [aggregate4] : []
    ];
    references = parameters.display === "individual" ? members : parameters.display === "aggregate_derived_with_members" ? selected : [];
    modelMembers = parameters.display === "individual" ? members : selected;
    sourceMembers = sourceCarrierKind === "source_observation" ? members : [];
    reconstructionMembers = sourceCarrierKind === "caller_reconstruction_point" ? members : [];
    identifiedMembers = parameters.display === "individual" ? members : selected;
  }
  const rows = table.rows;
  const observation = data.mode === "preaggregated" ? record4(record4(data.aggregate).observation) : dataObservation;
  const reconstruction = observation.kind === "interpolated_trajectory";
  const geometry = [
    ...drawn.flatMap((view) => normalTraceGeometry(view, {
      paths: "series_paths",
      samples: reconstruction ? "reconstruction" : "observations",
      uncertainty: "uncertainty"
    }, parameters.showObservationMarkers === true, reconstruction ? {
      pathVertex: "reconstruction_vertex",
      sampleMarker: "reconstruction_marker",
      isolatedMarker: "isolated_reconstruction_marker"
    } : void 0)),
    ...weightReferenceGeometry(references, parameters)
  ];
  const factViews = drawn;
  const traces = factViews.map((view) => view.trace);
  const uncertainties = factViews.map((view) => view.uncertainty);
  const rawModels = modelMembers.flatMap((view) => {
    const modelSet2 = array4(view.raw.synapseModels).filter(
      (model) => typeof model === "string"
    );
    return modelSet2.length > 0 ? modelSet2 : typeof view.raw.synapseModel === "string" ? [view.raw.synapseModel] : [];
  });
  const scope = record4(data.scope);
  const declaredMemberCounts = data.mode === "preaggregated" ? numberArray(record4(data.aggregate).memberCounts) : [];
  const maximumConcurrentMembers = declaredMemberCounts.length === 0 ? 0 : Math.max(0, ...declaredMemberCounts);
  const excluded = sourceMembers.reduce(
    (sum, view) => sum + view.trace.excludedBelow + view.trace.excludedAbove,
    0
  );
  const excludedReconstruction = reconstructionMembers.reduce(
    (sum, view) => sum + view.trace.excludedBelow + view.trace.excludedAbove,
    0
  );
  const reconstructionPointCount = reconstructionMembers.reduce(
    (sum, view) => sum + array4(record4(view.raw.values).values).length,
    0
  );
  const missingReconstructionPointCount = reconstructionMembers.reduce(
    (sum, view) => sum + view.trace.observations.reduce(
      (memberSum, candidate) => memberSum + candidate.sourceValues.filter(
        (value) => value === null
      ).length,
      0
    ),
    0
  );
  const retainedReconstructionRowCount = reconstructionMembers.reduce(
    (sum, view) => sum + view.trace.observations.length,
    0
  );
  const membership = record4(data.membership);
  const comparability = record4(parameters.weightComparability);
  const modelSet = [...new Set(rawModels)];
  const comparabilityClaim = canonicalize({
    mode: String(comparability.mode),
    models: modelSet
  });
  const summary = {
    synapseCardinalityStatement: data.mode === "preaggregated" ? `Unique synapse cardinality was not supplied; maximum concurrent memberCount is ${maximumConcurrentMembers}.` : `${identifiedMembers.length} identified synapse${identifiedMembers.length === 1 ? "" : "s"}.`,
    synapseModels: joinUnique(modelSet),
    weightUnit: traces[0]?.valueUnit ?? "unknown",
    weightDimensionStatement: dimensionOf(traces[0]?.valueUnit ?? "") === "simulator_defined" ? `simulator-defined; exact comparability claim ${comparabilityClaim}` : `registered ${String(dimensionOf(traces[0]?.valueUnit ?? "") ?? "unknown")} dimension; exact comparability claim ${comparabilityClaim}`,
    windowStart: summaryNumber2(window.start),
    windowStop: summaryNumber2(window.stop),
    timeUnit: window.unit,
    observationStatement: observation.kind === "interpolated_trajectory" ? `interpolated_trajectory (method=${String(observation.method)}; interpolant=${String(observation.interpolant)}; reconstructedBy=${String(observation.reconstructedBy)})` : `${String(observation.kind)}${typeof observation.updateSemantics === "string" ? ` (${observation.updateSemantics})` : ""}`,
    duplicateTimeStatement: `Duplicate timestamps: ${duplicateStatement(
      parameters,
      (data.mode === "preaggregated" ? [aggregate4] : members).map((view) => view.trace)
    )}.`,
    retainedSourceRowCount: String(table.carrierCounts.source_observation.total),
    sourceReadingCount: String(table.sourceReadingCount),
    missingCount: String(table.missingSourceReadingCount),
    excludedCount: String(excluded),
    reconstructionPointCount: String(reconstructionPointCount),
    retainedReconstructionRowCount: String(retainedReconstructionRowCount),
    missingReconstructionPointCount: String(missingReconstructionPointCount),
    excludedReconstructionPointCount: String(excludedReconstruction),
    carrierStatement: independentWeightCarrierStatement(
      table.carrierCounts,
      table.sourceReadingCount,
      table.missingSourceReadingCount,
      excluded,
      reconstructionPointCount,
      retainedReconstructionRowCount,
      missingReconstructionPointCount,
      excludedReconstruction
    ),
    displayMode: String(parameters.display),
    aggregateStatement: parameters.display === "aggregate_declared" ? `The caller declared a ${String(record4(data.aggregate).method)} aggregate using interval method ${String(record4(data.aggregate).intervalMethod)}; Cortexel did not receive member observations and cannot re-derive either claim.` : aggregate4 ? `Cortexel-derived ${String(record4(parameters.aggregate).method)} over the declared membership; denominator is contributingCount and zero contributors yield missing.` : "No aggregate was requested.",
    membershipStatement: Object.keys(membership).length > 0 ? `${String(membership.groupId)} with ${array4(membership.members).length} identified members represented by declared half-open membership intervals.` : data.mode === "preaggregated" ? "Member identities were not supplied with the caller-declared aggregate." : "No aggregate membership was declared.",
    referenceStatement: parameters.showReferenceLines === true ? `${weightReferenceGeometry(references, parameters).length} declared initial-weight/bound reference lines are shown; they never clamp observations.` : "Declared initial weights and bounds are not drawn as reference lines.",
    scopeStatement: weightTraceScopeSummaryCell(data.scope),
    uncertaintyStatement: weightUncertaintyStatement(uncertainties),
    compactionStatement: noCompactionStatement2()
  };
  const uncertaintySeriesShown = factViews.filter((view) => independentViewHasCompleteDrawableUncertainty(
    view,
    parameters.showObservationMarkers === true
  )).length;
  return {
    rows,
    geometry,
    summary,
    disclosures: traceDisclosureFacts(request, parameters, traces, uncertainties, {
      ...typeof scope.kind === "string" ? { scopeKind: scope.kind } : {},
      ...typeof scope.rank === "number" ? { rank: scope.rank } : {},
      ...typeof scope.worldSize === "number" ? { worldSize: scope.worldSize } : {},
      uncertaintySeriesShown
    })
  };
}
var WEIGHT_AUTHORITY3 = defineAuthorityEvaluator(
  authorityEvaluatorId("network.synaptic_weight_trace", 2),
  (request) => {
    const model = weightModel3(request);
    return {
      "table.rows": rowSequence(model.rows),
      "geometry.sequence": geometrySequence(model.geometry),
      "summary.facts": summaryFactMap(model.summary),
      "disclosure.facts": disclosureFactMap(model.disclosures)
    };
  }
);
var TRACE_AUTHORITY_EVALUATORS = [
  ANALOG_AUTHORITY,
  COMPARTMENT_AUTHORITY,
  MULTISIGNAL_AUTHORITY,
  WEIGHT_AUTHORITY3
];

// src/authority/evaluators/registry.ts
var ALL = [
  ...TRACE_AUTHORITY_EVALUATORS,
  ...DISTRIBUTION_AUTHORITY_EVALUATORS,
  ...MATRIX_AUTHORITY_EVALUATORS,
  ...TOPOLOGY_DYNAMICS_AUTHORITY_EVALUATORS
];
function outputAuthorityEvaluatorRegistryProblemsV1(evaluators, expectedIds = OUTPUT_AUTHORITY_IMPLEMENTATION_IDS_V1) {
  const problems = [...outputAuthorityImplementationInventoryProblemsV1(expectedIds)];
  const actualIds = evaluators.map((evaluator) => evaluator.id);
  const stringActualIds = actualIds.filter((id) => typeof id === "string");
  problems.push(...outputAuthorityImplementationInventoryProblemsV1([...stringActualIds].sort()));
  actualIds.forEach((id, index) => {
    if (typeof id !== "string") problems.push(`registered evaluator id at index ${index} is not a string`);
  });
  const expectedCounts = /* @__PURE__ */ new Map();
  const actualCounts = /* @__PURE__ */ new Map();
  for (const id of expectedIds) {
    if (typeof id === "string") expectedCounts.set(id, (expectedCounts.get(id) ?? 0) + 1);
  }
  for (const id of stringActualIds) actualCounts.set(id, (actualCounts.get(id) ?? 0) + 1);
  for (const id of [.../* @__PURE__ */ new Set([...expectedCounts.keys(), ...actualCounts.keys()])].sort()) {
    const expected = expectedCounts.get(id) ?? 0;
    const actual = actualCounts.get(id) ?? 0;
    if (actual < expected) {
      problems.push(`missing registered evaluator ${JSON.stringify(id)} (${expected - actual})`);
    } else if (actual > expected) {
      problems.push(`extra registered evaluator ${JSON.stringify(id)} (${actual - expected})`);
    }
  }
  return Object.freeze(problems);
}
var REGISTRY_PROBLEMS = outputAuthorityEvaluatorRegistryProblemsV1(ALL);
if (REGISTRY_PROBLEMS.length > 0) {
  throw new Error(`invalid OutputAuthority evaluator registry: ${REGISTRY_PROBLEMS.join("; ")}`);
}
var BY_ID = /* @__PURE__ */ new Map();
for (const evaluator of ALL) {
  if (BY_ID.has(evaluator.id)) {
    throw new Error(`duplicate OutputAuthority evaluator id ${JSON.stringify(evaluator.id)}`);
  }
  BY_ID.set(evaluator.id, evaluator);
}
var OUTPUT_AUTHORITY_EVALUATOR_IDS_V1 = Object.freeze(
  [...BY_ID.keys()].sort()
);
for (const id of OUTPUT_AUTHORITY_IMPLEMENTATION_IDS_V1) {
  if (BY_ID.get(id)?.id !== id) {
    throw new Error(`OutputAuthority evaluator round-trip failed for ${JSON.stringify(id)}`);
  }
}
function resolveOutputAuthorityEvaluatorV1(id) {
  return BY_ID.get(id) ?? null;
}

// src/render/output-authority-extract.ts
var MAX_AUTHORITY_MARK_DEPTH = 128;
var MAX_AUTHORITY_ATOMS = 1e6;
var MAX_AUTHORITY_PROBLEMS = 32;
function recordProblem(problems, problem2) {
  if (problems.length < MAX_AUTHORITY_PROBLEMS) problems.push(problem2);
}
function classifiedAtom(role, geometry, path, problems) {
  if (role === void 0) {
    recordProblem(problems, {
      path,
      message: "atomic plan geometry has no explicit data_carrier/connector/decorative_mark role"
    });
    return null;
  }
  switch (role.tag) {
    case "data_carrier":
      return {
        tag: "data_mark",
        entry: {
          tag: "canonical_geometry",
          classId: role.classId,
          provenance: role.provenance,
          geometry
        }
      };
    case "connector":
      return { tag: "decorative_mark" };
    case "decorative_mark":
      return { tag: "decorative_mark" };
    default:
      recordProblem(problems, { path, message: "atomic plan geometry has an unknown explicit role tag" });
      return null;
  }
}
function preflightPlanMarks(plan) {
  const problems = [];
  const pending = [];
  for (let panelIndex = plan.panels.length - 1; panelIndex >= 0; panelIndex--) {
    const marks = plan.panels[panelIndex].marks;
    for (let markIndex = marks.length - 1; markIndex >= 0; markIndex--) {
      pending.push({ mark: marks[markIndex], path: `/panels/${panelIndex}/marks/${markIndex}`, depth: 1 });
    }
  }
  const seen = /* @__PURE__ */ new WeakSet();
  let atoms = 0;
  while (pending.length > 0) {
    const { mark, path, depth } = pending.pop();
    if (mark === null || typeof mark !== "object" || seen.has(mark)) {
      recordProblem(problems, { path, message: "mark graph repeats/cycles or contains a non-object mark" });
      continue;
    }
    seen.add(mark);
    if (depth > MAX_AUTHORITY_MARK_DEPTH) {
      recordProblem(problems, { path, message: `mark nesting exceeds ${MAX_AUTHORITY_MARK_DEPTH}` });
      continue;
    }
    switch (mark.type) {
      case "group":
        for (let index = mark.marks.length - 1; index >= 0; index--) {
          pending.push({ mark: mark.marks[index], path: `${path}/marks/${index}`, depth: depth + 1 });
        }
        break;
      case "line":
      case "area":
      case "path":
        for (const subpath of mark.subpaths) atoms += subpath.length;
        break;
      case "arrow":
        atoms += mark.arrows.length;
        break;
      case "point":
        atoms += mark.points.length;
        break;
      case "rect":
        atoms += mark.rects.length;
        break;
      case "rule":
        atoms += mark.lines.length;
        break;
      case "text":
        break;
      default:
        recordProblem(problems, { path, message: "mark has an unknown or missing closed-union type" });
    }
    if (atoms > MAX_AUTHORITY_ATOMS) {
      recordProblem(problems, { path, message: `authority extraction exceeds ${MAX_AUTHORITY_ATOMS} atomic primitives` });
      break;
    }
  }
  return problems;
}
function validateRenderRunSubpath(roles, subpath, problems, ownership) {
  const identities = /* @__PURE__ */ new Set();
  let untaggedDataCarrierCount = 0;
  for (const role of roles) {
    if (role?.tag !== "data_carrier") continue;
    const provenance = role.provenance;
    if (provenance === null || typeof provenance !== "object" || Array.isArray(provenance)) {
      untaggedDataCarrierCount++;
      continue;
    }
    const renderRunOrdinal = provenance.renderRunOrdinal;
    if (renderRunOrdinal === void 0) {
      untaggedDataCarrierCount++;
      continue;
    }
    if (typeof provenance.seriesId !== "string" || !Number.isSafeInteger(renderRunOrdinal) || renderRunOrdinal < 0) {
      recordProblem(problems, {
        path: subpath,
        message: "render-run provenance requires a string seriesId and non-negative safe-integer renderRunOrdinal"
      });
      continue;
    }
    identities.add(JSON.stringify([
      role.classId,
      provenance.seriesId,
      renderRunOrdinal
    ]));
  }
  if (identities.size === 0) return;
  if (untaggedDataCarrierCount > 0) {
    recordProblem(problems, {
      path: subpath,
      message: "one tagged RenderPlan line/path subpath mixes declared render-run carriers with data carriers lacking renderRunOrdinal"
    });
  }
  if (identities.size !== 1) {
    recordProblem(problems, {
      path: subpath,
      message: "one RenderPlan line/path subpath contains carriers from multiple declared render runs"
    });
    return;
  }
  const identity = [...identities][0];
  const prior = ownership.subpathByIdentity.get(identity);
  if (prior !== void 0 && prior !== subpath) {
    recordProblem(problems, {
      path: subpath,
      message: `one declared render run is split across multiple RenderPlan line/path subpaths (first at ${prior})`
    });
    return;
  }
  ownership.subpathByIdentity.set(identity, subpath);
}
function lineNodes(mark, path, problems, runOwnership) {
  const output = [];
  for (let subpathIndex = 0; subpathIndex < mark.subpaths.length; subpathIndex++) {
    const subpath = mark.subpaths[subpathIndex];
    validateRenderRunSubpath(
      subpath.map((point) => point.authority),
      `${path}/subpaths/${subpathIndex}`,
      problems,
      runOwnership
    );
    for (let pointIndex = 0; pointIndex < subpath.length; pointIndex++) {
      const point = subpath[pointIndex];
      const node = classifiedAtom(point.authority, {
        tag: "line_vertex",
        x: point.x,
        y: point.y,
        stroke: mark.stroke,
        strokeWidth: mark.strokeWidth,
        ...mark.dash === void 0 ? {} : { dash: mark.dash }
      }, `${path}/subpaths/${subpathIndex}/${pointIndex}`, problems);
      if (node) output.push(node);
    }
  }
  return output;
}
function arrowNodes(mark, path, problems) {
  return mark.arrows.flatMap((arrow, index) => {
    const node = classifiedAtom(arrow.authority, {
      tag: "arrow",
      from: arrow.from,
      to: arrow.to,
      fill: mark.fill,
      size: mark.size
    }, `${path}/arrows/${index}`, problems);
    return node ? [node] : [];
  });
}
function pointNodes(mark, path, problems) {
  return mark.points.flatMap((point, index) => {
    const node = classifiedAtom(point.authority, {
      tag: "point",
      x: point.x,
      y: point.y,
      fill: mark.fill,
      radius: mark.radius,
      shape: mark.shape
    }, `${path}/points/${index}`, problems);
    return node ? [node] : [];
  });
}
function rectNodes(mark, path, problems) {
  return mark.rects.flatMap((rect, index) => {
    const node = classifiedAtom(rect.authority, {
      tag: "rect",
      x: rect.x,
      y: rect.y,
      width: rect.width,
      height: rect.height,
      fill: rect.fill,
      ...mark.stroke === void 0 ? {} : { stroke: mark.stroke }
    }, `${path}/rects/${index}`, problems);
    return node ? [node] : [];
  });
}
function ruleNodes(mark, path, problems) {
  return mark.lines.flatMap((line, index) => {
    const node = classifiedAtom(line.authority, {
      tag: "rule",
      orientation: mark.orientation,
      position: line.position,
      from: line.from,
      to: line.to,
      stroke: mark.stroke,
      strokeWidth: mark.strokeWidth,
      ...mark.dash === void 0 ? {} : { dash: mark.dash }
    }, `${path}/lines/${index}`, problems);
    return node ? [node] : [];
  });
}
function areaNodes(mark, path, problems) {
  const output = [];
  for (let subpathIndex = 0; subpathIndex < mark.subpaths.length; subpathIndex++) {
    const subpath = mark.subpaths[subpathIndex];
    for (let pointIndex = 0; pointIndex < subpath.length; pointIndex++) {
      const point = subpath[pointIndex];
      const node = classifiedAtom(point.authority, {
        tag: "area_vertex",
        x: point.x,
        y0: point.y0,
        y1: point.y1,
        fill: mark.fill,
        opacity: mark.opacity,
        ...mark.stroke === void 0 ? {} : { stroke: mark.stroke },
        ...mark.strokeWidth === void 0 ? {} : { strokeWidth: mark.strokeWidth }
      }, `${path}/subpaths/${subpathIndex}/${pointIndex}`, problems);
      if (node) output.push(node);
    }
  }
  return output;
}
function pathNodes(mark, path, problems, runOwnership) {
  const output = [];
  for (let subpathIndex = 0; subpathIndex < mark.subpaths.length; subpathIndex++) {
    const subpath = mark.subpaths[subpathIndex];
    validateRenderRunSubpath(
      subpath.map((point) => point.authority),
      `${path}/subpaths/${subpathIndex}`,
      problems,
      runOwnership
    );
    for (let pointIndex = 0; pointIndex < subpath.length; pointIndex++) {
      const point = subpath[pointIndex];
      const node = classifiedAtom(point.authority, {
        tag: "path_vertex",
        x: point.x,
        y: point.y,
        stroke: mark.stroke,
        strokeWidth: mark.strokeWidth
      }, `${path}/subpaths/${subpathIndex}/${pointIndex}`, problems);
      if (node) output.push(node);
    }
  }
  return output;
}
function markNode(mark, path, problems, runOwnership) {
  switch (mark.type) {
    case "group":
      return {
        tag: "group",
        children: mark.marks.map((child, index) => markNode(child, `${path}/marks/${index}`, problems, runOwnership))
      };
    case "text":
      return { tag: "text" };
    case "line":
      return { tag: "group", children: lineNodes(mark, path, problems, runOwnership) };
    case "arrow":
      return { tag: "group", children: arrowNodes(mark, path, problems) };
    case "point":
      return { tag: "group", children: pointNodes(mark, path, problems) };
    case "rect":
      return { tag: "group", children: rectNodes(mark, path, problems) };
    case "rule":
      return { tag: "group", children: ruleNodes(mark, path, problems) };
    case "area":
      return { tag: "group", children: areaNodes(mark, path, problems) };
    case "path":
      return { tag: "group", children: pathNodes(mark, path, problems, runOwnership) };
  }
}
function extractObservedOutputAuthorityV1(plan) {
  const problems = preflightPlanMarks(plan);
  if (problems.length > 0) return { tag: "invalid_plan_roles", problems };
  const runOwnership = {
    subpathByIdentity: /* @__PURE__ */ new Map()
  };
  const geometry = plan.panels.map((panel, panelIndex) => ({
    tag: "group",
    children: [
      ...panel.axes.map(() => ({ tag: "axis" })),
      ...panel.marks.map((mark, markIndex) => markNode(
        mark,
        `/panels/${panelIndex}/marks/${markIndex}`,
        problems,
        runOwnership
      ))
    ]
  }));
  for (const _disclosure of plan.disclosures) geometry.push({ tag: "disclosure" });
  return problems.length > 0 ? { tag: "invalid_plan_roles", problems } : {
    tag: "extracted",
    observed: {
      table: plan.table,
      geometry,
      summary: plan.accessibility.summary,
      disclosures: plan.disclosures
    }
  };
}

// src/render/output-authority-gate.ts
var ENVIRONMENT_DISCLOSURE_FACT_KEYS = Object.freeze([
  "budgetProfileId",
  "nonStandardBudgetProfile"
]);
function record5(value) {
  return value !== null && typeof value === "object" && !Array.isArray(value) ? value : null;
}
function forcedDisclosureIds(skillId, request) {
  const data = record5(request.data) ?? {};
  const parameters = record5(request.parameters) ?? {};
  const forced = [];
  if (skillId === "neuro.correlogram") {
    forced.push("LAG_ORIENTATION");
    if (data.mode === "events_auto" || data.mode === "prebinned_auto") {
      forced.push("ZERO_LAG_SELF_PAIRS_EXCLUDED");
    }
  }
  if (skillId === "network.adjacency_matrix" || skillId === "network.weight_matrix" || skillId === "network.delay_matrix") forced.push("ABSENT_IS_NOT_ZERO");
  if (skillId === "network.connection_graph" && typeof record5(parameters.layout)?.mode === "string" && String(record5(parameters.layout).mode).startsWith("schematic")) forced.push("SCHEMATIC_LAYOUT");
  if (skillId === "neuro.response_curve" && data.mode === "aggregates") {
    forced.push("AGGREGATE_WITHOUT_RAW_REPEATS");
  }
  return forced;
}
function authorityEffectiveBudgetProfile(validated) {
  const presentation = record5(validated.canonicalRequest.presentation) ?? {};
  const requestedProfile = presentation.budgetProfile ?? DEFAULT_PROFILE;
  const hostProfile = validated.inputAssurance.budgetProfile;
  if (typeof requestedProfile !== "string" || typeof hostProfile !== "string") return null;
  const requested = tryGetBudgetLimits(requestedProfile);
  const host = tryGetBudgetLimits(hostProfile);
  if (!requested || !host) return null;
  const requestedKeys = Object.keys(requested).sort();
  const hostKeys = Object.keys(host).sort();
  if (requestedKeys.length === 0 || requestedKeys.length !== hostKeys.length || requestedKeys.some((key, index) => key !== hostKeys[index])) return null;
  const noGreaterThan = (left, right) => requestedKeys.every((key) => Number.isFinite(left[key]) && Number.isFinite(right[key]) && left[key] <= right[key]);
  if (noGreaterThan(requested, host)) return requestedProfile;
  if (noGreaterThan(host, requested)) return hostProfile;
  return null;
}
function checkOutputAuthorityEmissionV1(validated, plan) {
  if (!isValidatedRequest(validated)) {
    return {
      tag: "refused",
      messages: ["OutputAuthority emission gate requires Cortexel's live validated-request capability token"]
    };
  }
  if (!isClosedPlainRenderPlanForAuthorityV1(plan)) {
    return {
      tag: "refused",
      messages: [
        "OutputAuthority translation gate requires Cortexel's detached, plain, deeply frozen RenderPlan capability"
      ]
    };
  }
  const catalog = SKILL_CATALOG[validated.skillId];
  const authority = catalog?.outputAuthority;
  if (!authority) {
    return { tag: "refused", messages: ["stable skill has no generated OutputAuthority declaration"] };
  }
  if (plan.sourceRequestDigest !== validated.requestDigest || plan.skillId !== validated.skillId || authority.evaluator.id !== `${validated.skillId}.output_authority.v${validated.skillRevision}`) {
    return {
      tag: "refused",
      messages: ["OutputAuthority request, skill declaration, and actual RenderPlan identities do not match"]
    };
  }
  try {
    const evaluator = resolveOutputAuthorityEvaluatorV1(authority.evaluator.id);
    if (evaluator === null) {
      return {
        tag: "refused",
        messages: [`no library-owned OutputAuthority evaluator is registered for ${authority.evaluator.id}`]
      };
    }
    const extracted = extractObservedOutputAuthorityV1(plan);
    if (extracted.tag !== "extracted") {
      return {
        tag: "refused",
        messages: extracted.problems.map((problem2) => `${problem2.path}: ${problem2.message}`)
      };
    }
    const evaluation = evaluator.evaluateCanonicalRequest(validated.canonicalRequest);
    const disclosureValue = evaluation.fields[authority.disclosures.expectedFacts.field];
    if (disclosureValue?.tag !== "disclosure_fact_map") {
      return {
        tag: "refused",
        messages: ["independent evaluator omitted the typed disclosure fact map"]
      };
    }
    const collidingEnvironmentFact = ENVIRONMENT_DISCLOSURE_FACT_KEYS.find((key) => Object.prototype.hasOwnProperty.call(disclosureValue.facts, key));
    if (collidingEnvironmentFact !== void 0) {
      return {
        tag: "refused",
        messages: [
          `request-only evaluator attempted to author library-environment disclosure fact ${collidingEnvironmentFact}`
        ]
      };
    }
    const effectiveBudgetProfile = authorityEffectiveBudgetProfile(validated);
    if (effectiveBudgetProfile === null) {
      return {
        tag: "refused",
        messages: ["OutputAuthority could not independently compose the host and requested budget profiles"]
      };
    }
    const expectedDisclosures = deriveDisclosures(
      { ...disclosureValue.facts, budgetProfileId: effectiveBudgetProfile },
      catalog.disclosures,
      forcedDisclosureIds(validated.skillId, validated.canonicalRequest)
    );
    const interpreted = interpretOutputAuthorityModelV1(
      authority,
      catalog.accessibility.summaryTemplate,
      catalog.accessibility.tableColumns,
      expectedDisclosures,
      validated.requestDigest,
      evaluation,
      extracted.observed
    );
    return interpreted.tag === "valid" ? { tag: "passed" } : {
      tag: "refused",
      messages: interpreted.violations.map((entry) => `${entry.path}: ${entry.message}`)
    };
  } catch {
    return {
      tag: "refused",
      messages: ["OutputAuthority evaluator, extractor, or interpreter failed closed before emission"]
    };
  }
}

// src/render/buildFigure.ts
function num(value) {
  return typeof value === "number" && Number.isFinite(value) ? value : void 0;
}
function exactNumberText(value) {
  return value === 0 ? "0" : String(value);
}
function arr(value) {
  return Array.isArray(value) ? value : void 0;
}
function rec(value) {
  return value !== null && typeof value === "object" && !Array.isArray(value) ? value : void 0;
}
function responseRateAuthorityText(authority) {
  if (authority.normalization === "single_train_rate") {
    return "single_train_rate (one event train; integer divisor 1)";
  }
  const recordedSenderCount = authority.eventScope.recordedSenderCount;
  if (authority.normalization === "total_event_rate") {
    return `total_event_rate (pooled total over ${recordedSenderCount} recorded sender${recordedSenderCount === 1 ? "" : "s"}; integer divisor 1)`;
  }
  return `mean_rate_per_recorded_sender (pooled total divided by ${recordedSenderCount} recorded sender${recordedSenderCount === 1 ? "" : "s"})`;
}
function responseRateAxisQualifier(authority) {
  if (authority.normalization === "single_train_rate") return "single train";
  const recordedSenderCount = authority.eventScope.recordedSenderCount;
  if (authority.normalization === "total_event_rate") {
    return `pooled total, ${recordedSenderCount} sender${recordedSenderCount === 1 ? "" : "s"}`;
  }
  return `mean per sender, ${recordedSenderCount} sender${recordedSenderCount === 1 ? "" : "s"}`;
}
function responseEventMembershipText(authority) {
  if (authority.membershipKind === "single_train_selection_rule") {
    return "single_train_selection_rule (source composition not bound)";
  }
  const binding = rec(authority.normalizedScope.membershipBinding);
  if (authority.membershipKind === "explicit_sender_ids") {
    const normalized = rec(authority.normalizedScope.membershipBinding);
    const senderIds = arr(normalized?.senderIds) ?? [];
    const membershipDigest = responseEventMembershipDigest(senderIds);
    return `explicit_sender_ids (${senderIds.length} unique ids; ${RESPONSE_EVENT_MEMBERSHIP_CANONICALIZATION_ID}; canonical membership digest ${membershipDigest})`;
  }
  if (authority.membershipKind === "canonical_sender_ids_digest") {
    return `canonical_sender_ids_digest (${String(binding?.canonicalization)}; ${String(binding?.digest)}; preimage unavailable)`;
  }
  return "cardinality_only (member identities not bound)";
}
function responseEventAxisQualifier(method, authority) {
  const selection = `declared selection ${authority.selectionId}`;
  if (authority.kind === "single_train") {
    if (method === "first_spike_latency") return `${selection}; first event in one selected train`;
    if (method === "event_count") return `${selection}; count in one selected train`;
    return `${selection}; one selected event train`;
  }
  const count = authority.recordedSenderCount;
  if (method === "first_spike_latency") {
    return `${selection}; minimum first-event latency over pooled union of ${count} declared sender trains`;
  }
  if (method === "event_count") {
    return `${selection}; pooled total count over ${count} declared sender trains`;
  }
  return `${selection}; pooled union of ${count} declared sender trains`;
}
function responseEventScopeText(authority) {
  const cardinality = authority.recordedSenderCount === null ? `${authority.selectedEventTrainCount} selected event train; no recorded-sender cardinality declared` : `${authority.selectedEventTrainCount} selected sender event train${authority.selectedEventTrainCount === 1 ? "" : "s"} (including silent senders)`;
  return `${authority.kind} selection ${authority.selectionId}; ${cardinality}; ${responseEventMembershipText(authority)}; declared pooling ${authority.poolingOperator}; declared completeness ${authority.eventCompleteness}`;
}
function peakSupportText2(supportValue) {
  const support = rec(supportValue);
  if (support?.kind === "analytic_infinite") return "analytic infinite support";
  if (support?.kind === "finite_full_width") {
    return `finite full-width support, ${String(support.supportBoundary)} boundary`;
  }
  if (support?.kind === "finite_cutoff") {
    const cutoff = rec(support.cutoff);
    return `finite ${String(support.geometry)} cutoff ${exactNumberText(num(cutoff?.value) ?? 0)} ${String(cutoff?.unit ?? "")}, ${String(support.cutoffBoundary)}, ${String(support.tailPolicy)}`;
  }
  return "unrecognized support";
}
function peakEvaluationText2(evaluationValue) {
  const evaluation = rec(evaluationValue);
  if (evaluation?.mode === "continuous_supremum") {
    return `continuous supremum over ${String(evaluation.domain)} ${String(evaluation.boundary)}`;
  }
  const step = rec(evaluation?.step);
  return `sampled grid of ${String(evaluation?.sampleCount)} points from measurement-window start, step ${exactNumberText(num(step?.value) ?? 0)} ${String(step?.unit ?? "")}, ${String(evaluation?.boundary)}, stop excluded, ${String(evaluation?.tilingPolicy)}, partial steps ${String(evaluation?.partialStepPolicy)}`;
}
function peakEstimatorBasisText(basisValue) {
  const basis = rec(basisValue);
  if (basis?.estimator === "binned_count") {
    const width = rec(basis.binWidth);
    return `binned-count peak over ${String(basis.binCount)} bins with exact physical exposure ${exactNumberText(num(width?.value) ?? 0)} ${String(width?.unit ?? "")} verified for every emitted interval, origin ${String(basis.origin)}, ${String(basis.boundary)}, ${String(basis.tilingPolicy)}, partial bins ${String(basis.partialBinPolicy)}`;
  }
  const bandwidth = rec(basis?.bandwidth);
  return `${String(basis?.shape)} ${String(basis?.kernelForm)} kernel, ${String(basis?.bandwidthDefinition)} ${exactNumberText(num(bandwidth?.value) ?? 0)} ${String(bandwidth?.unit ?? "")}, ${peakSupportText2(basis?.support)}, ${String(basis?.normalization)}, ${String(basis?.evaluationOperator)}, edge policy ${String(basis?.edgePolicy)}, ${peakEvaluationText2(basis?.evaluation)}`;
}
function disclosureFacts(request, extra) {
  const source = rec(request.source) ?? {};
  const data = rec(request.data) ?? {};
  const parameters = rec(request.parameters) ?? {};
  const uncertainty = rec(parameters.uncertainty) ?? rec(data.uncertainty);
  const eventWindow = rec(data.window);
  const scope = rec(data.scope);
  const nodeUniverse = rec(data.nodeUniverse);
  let missing = 0;
  const countMissing = (node) => {
    if (node === null || typeof node !== "object") return;
    const pending = [node];
    while (pending.length > 0) {
      const current = pending.pop();
      const values = Array.isArray(current) ? current : Object.values(current);
      for (const value of values) {
        if (value === null) missing++;
        else if (typeof value === "object") pending.push(value);
      }
    }
  };
  countMissing(data.series);
  const facts = {
    sourceKind: source.kind ?? "unknown",
    sourceAuthenticityVerified: false,
    referenceComparisonRun: false,
    callerNotePresent: typeof source.declaredNote === "string",
    nestSerializedClock: eventWindow?.kind === "nest_recording_device_origin_relative",
    uncertaintyKind: uncertainty ? uncertainty.kind : void 0,
    uncertaintyReason: uncertainty ? uncertainty.reason : void 0,
    ...scope ? { scopeKind: scope.kind } : {},
    ...scope && typeof scope.rank === "number" ? { rank: scope.rank } : {},
    ...scope && typeof scope.worldSize === "number" ? { worldSize: scope.worldSize } : {},
    ...scope && typeof scope.retainedConnectionCount === "number" ? { retainedConnectionCount: scope.retainedConnectionCount, sampledRetained: scope.retainedConnectionCount } : {},
    ...scope && typeof scope.sourceConnectionCount === "number" ? { sourceConnectionCount: scope.sourceConnectionCount, sampledSource: scope.sourceConnectionCount } : {},
    ...nodeUniverse && typeof nodeUniverse.complete === "boolean" ? { nodeUniverseComplete: nodeUniverse.complete } : {},
    ...missing > 0 ? { missingValueCount: missing } : {},
    // A schematic layout carries no spatial meaning; disclose it whenever the layout mode
    // says so, in addition to the compiler-forced case for the connection graph.
    ...typeof rec(parameters.layout)?.mode === "string" && rec(parameters.layout).mode.startsWith("schematic") ? { schematicLayout: true } : {},
    ...extra
  };
  if (facts.uncertaintyKind === void 0) {
    facts.uncertaintyKind = "none";
    facts.uncertaintyReason = "not_provided";
  }
  const positions = rec(data.positions);
  const universeIds = arr(nodeUniverse?.ids);
  const positionIds = arr(positions?.nodeIds);
  if (universeIds && positionIds && positionIds.length < universeIds.length) {
    facts.positionsMissing = universeIds.length - positionIds.length;
    facts.positionsTotal = universeIds.length;
  }
  return facts;
}
function forcedDisclosures(skillId, request) {
  const data = rec(request.data) ?? {};
  const forced = [];
  if (skillId === "neuro.correlogram") {
    forced.push("LAG_ORIENTATION");
    if (data.mode === "events_auto" || data.mode === "prebinned_auto") {
      forced.push("ZERO_LAG_SELF_PAIRS_EXCLUDED");
    }
  }
  if (skillId === "network.adjacency_matrix" || skillId === "network.weight_matrix" || skillId === "network.delay_matrix") {
    forced.push("ABSENT_IS_NOT_ZERO");
  }
  if (skillId === "network.connection_graph") {
    const layoutMode = rec(rec(request.parameters)?.layout)?.mode;
    if (typeof layoutMode === "string" && layoutMode.startsWith("schematic")) {
      forced.push("SCHEMATIC_LAYOUT");
    }
  }
  if (skillId === "neuro.response_curve" && data.mode === "aggregates") {
    forced.push("AGGREGATE_WITHOUT_RAW_REPEATS");
  }
  return forced;
}
function numbers4(value) {
  return (arr(value) ?? []).filter((v) => typeof v === "number" && Number.isFinite(v));
}
function strings4(value) {
  return (arr(value) ?? []).filter((v) => typeof v === "string");
}
function withContractTable(plan, _context, skillId, rows) {
  const columns = SKILL_CATALOG[skillId].accessibility.tableColumns.map(
    ({ key, header }) => ({ key, header })
  );
  for (let index = 0; index < rows.length; index++) {
    if (rows[index].length !== columns.length) {
      throw new Error(
        `accessibility table row ${index} for ${skillId} has ${rows[index].length} cells for ${columns.length} contract columns`
      );
    }
  }
  const returnedRows = rows.map((row) => [...row]);
  return {
    ...plan,
    table: {
      policy: "complete_returned",
      columns,
      rows: returnedRows,
      rowsInline: returnedRows.length,
      rowsTotal: rows.length
    }
  };
}
function structuredCell5(value) {
  return value === void 0 ? null : canonicalize(value);
}
function compilerNetworkScopeSummaryCell(value) {
  const scope = rec(value);
  if (!scope) throw new Error("NetworkScope must be an object");
  const summary = { kind: String(scope.kind) };
  if (scope.snapshotTime !== void 0) summary.snapshotTime = scope.snapshotTime;
  switch (scope.kind) {
    case "single_process":
      break;
    case "global_merged":
      summary.mergedRanksCoverage = "all_ranks_0_through_worldSize_minus_1";
      summary.worldSize = scope.worldSize;
      break;
    case "mpi_target_rank_local":
      summary.localTargetUniverseComplete = scope.localTargetUniverseComplete;
      summary.rank = scope.rank;
      summary.worldSize = scope.worldSize;
      break;
    case "sampled":
      summary.method = scope.method;
      summary.parentScope = scope.parentScope;
      summary.retainedConnectionCount = scope.retainedConnectionCount;
      summary.sourceConnectionCount = scope.sourceConnectionCount;
      break;
    default:
      throw new Error(`unsupported NetworkScope kind ${String(scope.kind)}`);
  }
  return canonicalize(summary);
}
function booleanCell3(value) {
  return value ? "true" : "false";
}
function countExplicitNulls(values) {
  let total = 0;
  for (const value of values) if (value === null) total++;
  return total;
}
function contractOrderedTableRow(skillId, values) {
  return SKILL_CATALOG[skillId].accessibility.tableColumns.map(({ key }) => {
    if (!Object.prototype.hasOwnProperty.call(values, key)) {
      throw new Error(`compiler did not materialize contract table cell ${key}`);
    }
    return values[key];
  });
}
function compareUnicodeCodePoints3(left, right) {
  const leftIterator = left[Symbol.iterator]();
  const rightIterator = right[Symbol.iterator]();
  while (true) {
    const leftNext = leftIterator.next();
    const rightNext = rightIterator.next();
    if (leftNext.done || rightNext.done) {
      if (leftNext.done && rightNext.done) return 0;
      return leftNext.done ? -1 : 1;
    }
    const leftCodePoint = leftNext.value.codePointAt(0);
    const rightCodePoint = rightNext.value.codePointAt(0);
    if (leftCodePoint !== rightCodePoint) return leftCodePoint < rightCodePoint ? -1 : 1;
  }
}
function exactSpikeWindowPartition(data) {
  const eventTimes = rec(data.eventTimes) ?? {};
  const times = numbers4(eventTimes.values);
  const window = rec(data.window) ?? {};
  const timeUnit = typeof eventTimes.unit === "string" ? eventTimes.unit : "ms";
  const windowUnit = typeof window.unit === "string" ? window.unit : timeUnit;
  const nestOriginRelative = window.kind === "nest_recording_device_origin_relative";
  const origin = nestOriginRelative ? num(window.origin) : void 0;
  const start = num(window.start);
  const stop = num(window.stop);
  if (start === void 0 || stop === void 0 || nestOriginRelative && origin === void 0) {
    throw new Error("a validated spike-raster window did not retain its finite endpoint terms");
  }
  const lowerTerms = nestOriginRelative ? [
    { value: origin, unit: windowUnit },
    { value: start, unit: windowUnit }
  ] : [{ value: start, unit: windowUnit }];
  const upperTerms = nestOriginRelative ? [
    { value: origin, unit: windowUnit },
    { value: stop, unit: windowUnit }
  ] : [{ value: stop, unit: windowUnit }];
  const boundary = nestOriginRelative ? "(origin+start,origin+stop]" : String(window.boundary);
  const openStart = boundary.startsWith("(");
  const closedStop = boundary.endsWith("]");
  const displayStart = convertExactUnitSum(lowerTerms, timeUnit);
  const displayStop = convertExactUnitSum(upperTerms, timeUnit);
  if (!(displayStop > displayStart)) {
    throw new Error(
      "the exact event-window endpoints collapse or invert after their one permitted binary64 display conversion"
    );
  }
  const inWindow2 = new Array(times.length).fill(false);
  let excludedBelow = 0;
  let excludedAbove = 0;
  for (let index = 0; index < times.length; index++) {
    const event = { value: times[index], unit: timeUnit };
    const lowerVsEvent = compareExactUnitSumToValue(lowerTerms, event);
    const upperVsEvent = compareExactUnitSumToValue(upperTerms, event);
    const below = openStart ? lowerVsEvent >= 0 : lowerVsEvent > 0;
    const above = closedStop ? upperVsEvent < 0 : upperVsEvent <= 0;
    if (below) excludedBelow++;
    else if (above) excludedAbove++;
    else inWindow2[index] = true;
  }
  const excludedCount = excludedBelow + excludedAbove;
  return {
    times,
    timeUnit,
    windowUnit,
    nestOriginRelative,
    lowerTerms,
    upperTerms,
    boundary,
    displayStart,
    displayStop,
    inWindow: inWindow2,
    excludedBelow,
    excludedAbove,
    excludedCount,
    acceptedCount: times.length - excludedCount
  };
}
function binBoundaryInclusive(spec) {
  if (!spec) return void 0;
  if (typeof spec.finalEdgeInclusive === "boolean") return spec.finalEdgeInclusive;
  if (spec.boundary === "[start,stop]" || spec.boundary === "[lo,hi]") return true;
  if (spec.boundary === "[start,stop)" || spec.boundary === "[lo,hi)") return false;
  return void 0;
}
function declaredBins(node, fallbackUnit) {
  if (!node) return void 0;
  const unit = typeof node.unit === "string" ? node.unit : fallbackUnit;
  if (!unit) return void 0;
  const finalEdgeInclusive2 = binBoundaryInclusive(node) ?? true;
  if (node.mode === "edges" || arr(node.edges)) {
    const edges = numbers4(node.edges);
    return edges.length >= 2 ? { spec: { edges, finalEdgeInclusive: finalEdgeInclusive2 }, unit } : void 0;
  }
  const start = num(node.start);
  const stop = num(node.stop);
  const width = num(node.width);
  if (start === void 0 || stop === void 0 || width === void 0) return void 0;
  return {
    spec: { edges: edgesFromWidth(start, stop, width), finalEdgeInclusive: finalEdgeInclusive2 },
    unit
  };
}
var EmptyHistogramNormalizationError = class extends Error {
};
function sameUnitDifference(lower, upper, unit) {
  try {
    return convertDifference(lower, upper, unit, unit);
  } catch {
    const difference = upper - lower;
    if (!Number.isFinite(difference) || !(difference > 0)) {
      throw new Error(`the declared ${unit} interval has no finite positive binary64 width`);
    }
    return difference;
  }
}
function histogramValues(counts, edges, binUnit, normalization) {
  let total = 0;
  for (const count of counts) {
    if (!Number.isSafeInteger(count) || count < 0) {
      throw new Error("histogram counts must be non-negative safe integers");
    }
    total += count;
    if (!Number.isSafeInteger(total)) {
      throw new Error("histogram observation total exceeds the safe-integer domain");
    }
  }
  if (normalization === "count") return [...counts];
  if (total === 0) {
    throw new EmptyHistogramNormalizationError(
      "probability and density are undefined for an empty histogram"
    );
  }
  if (normalization === "probability") {
    return counts.map((count) => exactBinary64DivideByIntegerProduct(count, total, 1));
  }
  if (normalization === "density") {
    return counts.map((count, index) => divideExactIntegerByConvertedDifference(
      count,
      total,
      edges[index],
      edges[index + 1],
      binUnit,
      binUnit
    ));
  }
  throw new Error(`unsupported histogram normalization ${normalization}`);
}
function recoverHistogramCounts(values, edges, binUnit, normalization, total) {
  if (!Number.isSafeInteger(total) || total < 0) {
    throw new Error("prebinned histogram has no non-negative safe-integer denominator");
  }
  if (normalization === "count") {
    if (values.some((value) => !Number.isSafeInteger(value) || value < 0)) {
      throw new Error("prebinned count histogram contains a non-integer count");
    }
    return [...values];
  }
  const counts = values.map((value, index) => {
    const width = sameUnitDifference(edges[index], edges[index + 1], binUnit);
    const estimate = normalization === "probability" ? value * total : value * total * width;
    const nearby = /* @__PURE__ */ new Set();
    for (let offset = -3; offset <= 3; offset++) {
      nearby.add(Math.floor(estimate) + offset);
      nearby.add(Math.ceil(estimate) + offset);
    }
    const matches = [...nearby].filter((candidate) => {
      if (!Number.isSafeInteger(candidate) || candidate < 0 || candidate > total) return false;
      const forward2 = normalization === "probability" ? exactBinary64DivideByIntegerProduct(candidate, total, 1) : divideExactIntegerByConvertedDifference(
        candidate,
        total,
        edges[index],
        edges[index + 1],
        binUnit,
        binUnit
      );
      return Object.is(forward2, value);
    });
    if (matches.length !== 1) {
      throw new Error(
        `prebinned ${normalization} value at bin ${index} does not identify one unique exact integer count`
      );
    }
    return matches[0];
  });
  const countTotal = counts.reduce((sum, count) => sum + count, 0);
  if (!Number.isSafeInteger(countTotal) || countTotal !== total) {
    throw new Error(
      `prebinned normalized histogram recovers ${countTotal} counts but declares ${total} in-range observations`
    );
  }
  const forward = histogramValues(counts, edges, binUnit, normalization);
  if (forward.some((value, index) => !Object.is(value, values[index]))) {
    throw new Error("prebinned normalized histogram is not on the exact integer-count estimator lattice");
  }
  return counts;
}
function histogramYAxisLabel(normalization, binUnit) {
  if (normalization === "count") return "count";
  if (normalization === "probability") return "probability";
  if (normalization === "density") {
    const unit = reciprocalUnit(binUnit);
    if (!unit) {
      throw new Error(`density requires a registered reciprocal unit for ${binUnit}`);
    }
    return `density (${unitLabel(unit) || unit})`;
  }
  throw new Error(`unsupported histogram normalization ${normalization}`);
}
function conversionDisclosureText(label, receipt) {
  return `${label}: ${receipt.from} -> ${receipt.to} (factor ${receipt.factor})`;
}
function derivationOperation(id, algorithm, parameters, input, output, receipt) {
  return {
    id,
    algorithm,
    algorithmRevision: 1,
    parameters,
    inputDigest: canonicalDigest(input),
    outputDigest: canonicalDigest(output),
    receipt
  };
}
function correlogramAxis2(parameters) {
  const lag = rec(parameters.lagRange) ?? {};
  const bins = rec(parameters.bins) ?? {};
  const min = num(lag.min);
  const max = num(lag.max);
  const width = num(bins.width);
  const lagUnit = typeof lag.unit === "string" ? lag.unit : void 0;
  const binUnit = typeof bins.unit === "string" ? bins.unit : void 0;
  if (min === void 0 || max === void 0 || width === void 0 || lagUnit === void 0 || binUnit === void 0) {
    throw new Error("validated correlogram request has no complete typed centred-lag axis");
  }
  const widthInLagUnit = binUnit === lagUnit ? width : convert(width, binUnit, lagUnit);
  const materialized = materializeCenteredLagBins(min, max, widthInLagUnit, 20001);
  if (!materialized.ok) {
    throw new Error(`validated correlogram centred-lag axis cannot be materialized (${materialized.reason})`);
  }
  const spec = { edges: [...materialized.edges], finalEdgeInclusive: false };
  const widthConversion = binUnit === lagUnit ? void 0 : conversionReceipt(binUnit, lagUnit);
  return {
    lagUnit,
    binWidth: { value: width, unit: binUnit },
    binWidthInLagUnit: widthInLagUnit,
    spec,
    centers: binCenters(spec),
    unitConversions: widthConversion ? [conversionDisclosureText("correlogram bin width", widthConversion)] : []
  };
}
function eventCorrelogramInputs(data, parameters) {
  if (data.mode !== "events_auto" && data.mode !== "events_cross") return void 0;
  const axis = correlogramAxis2(parameters);
  const window = rec(data.window) ?? {};
  const windowStart = num(window.start);
  const windowStop = num(window.stop);
  const windowUnit = typeof window.unit === "string" ? window.unit : void 0;
  const windowBoundary = window.boundary;
  if (windowStart === void 0 || windowStop === void 0 || windowUnit === void 0 || windowBoundary !== "[start,stop)" && windowBoundary !== "[start,stop]" && windowBoundary !== "(start,stop]") {
    throw new Error("validated correlogram request has no complete typed observation window");
  }
  const kind = data.mode === "events_auto" ? "auto" : "cross";
  const referenceTrain = rec(kind === "auto" ? data.train : data.referenceTrain) ?? {};
  const targetTrain = kind === "auto" ? referenceTrain : rec(data.targetTrain) ?? {};
  const referenceTimes = rec(referenceTrain.eventTimes) ?? {};
  const targetTimes = rec(targetTrain.eventTimes) ?? {};
  const referenceSourceValues = numbers4(referenceTimes.values);
  const targetSourceValues = kind === "auto" ? referenceSourceValues : numbers4(targetTimes.values);
  const referenceTimeUnit = typeof referenceTimes.unit === "string" ? referenceTimes.unit : void 0;
  const targetTimeUnit = typeof targetTimes.unit === "string" ? targetTimes.unit : void 0;
  if (referenceTimeUnit === void 0 || targetTimeUnit === void 0) {
    throw new Error("validated correlogram event train has no explicit event-time unit");
  }
  const ref = referenceTimeUnit === axis.lagUnit ? [...referenceSourceValues] : referenceSourceValues.map((value) => convert(value, referenceTimeUnit, axis.lagUnit));
  const tgt = kind === "auto" ? ref : targetTimeUnit === axis.lagUnit ? [...targetSourceValues] : targetSourceValues.map((value) => convert(value, targetTimeUnit, axis.lagUnit));
  const referenceConversion = referenceTimeUnit === axis.lagUnit ? void 0 : conversionReceipt(referenceTimeUnit, axis.lagUnit);
  const targetConversion = kind === "cross" && targetTimeUnit !== axis.lagUnit ? conversionReceipt(targetTimeUnit, axis.lagUnit) : void 0;
  return {
    ...axis,
    ref,
    tgt,
    referenceTimesInSourceUnit: referenceSourceValues,
    referenceTimeUnit,
    kind,
    referenceLabel: String(referenceTrain.label),
    targetLabel: String(targetTrain.label),
    referenceRecordedSenderCount: strings4(referenceTrain.recordedSenderIds).length,
    targetRecordedSenderCount: strings4(targetTrain.recordedSenderIds).length,
    window: {
      start: windowStart,
      stop: windowStop,
      unit: windowUnit,
      boundary: windowBoundary
    },
    unitConversions: [
      ...axis.unitConversions,
      ...referenceConversion ? [conversionDisclosureText("correlogram reference-event times", referenceConversion)] : [],
      ...targetConversion ? [conversionDisclosureText("correlogram target-event times", targetConversion)] : []
    ]
  };
}
function compileNullableCorrelogramStemFigure(context, edges, centers, values, xLabel, yLabel, skillId) {
  if (centers.length !== values.length) {
    throw new Error("correlogram geometry has different centre and value cardinalities");
  }
  if (edges.length !== centers.length + 1) {
    throw new Error("correlogram geometry has no exact edge pair for every centre");
  }
  const defined = values.map((value) => value !== null);
  const definedCount = defined.filter(Boolean).length;
  const scaffold = compileStemFigure(
    context,
    centers,
    values.map((value) => value ?? 0),
    xLabel,
    yLabel,
    skillId
  );
  const panels = scaffold.panels.map((panel) => {
    const marks = panel.marks.map((mark) => {
      if (mark.type === "rule" && mark.orientation === "vertical") {
        if (mark.lines.length !== defined.length) {
          throw new Error("correlogram stem scaffold lost its one-line-per-bin invariant");
        }
        return {
          ...mark,
          lines: mark.lines.flatMap((line, index) => defined[index] ? [{ ...line, authority: { tag: "connector" } }] : [])
        };
      }
      if (mark.type === "point") {
        if (mark.points.length !== defined.length) {
          throw new Error("correlogram stem scaffold lost its one-point-per-bin invariant");
        }
        return {
          ...mark,
          points: mark.points.flatMap((point, index) => defined[index] ? [{
            ...point,
            authority: {
              tag: "data_carrier",
              classId: "bins",
              provenance: {
                binIndex: index,
                lagBinStart: edges[index],
                lagBinCenter: centers[index],
                lagBinEnd: edges[index + 1]
              }
            }
          }] : [])
        };
      }
      throw new Error(`correlogram stem scaffold emitted unexpected ${mark.type} geometry`);
    });
    return {
      ...panel,
      marks,
      ...definedCount === 0 ? {
        noData: {
          reason: "every target-rate bin is undefined because its eligible-reference count is zero"
        }
      } : {}
    };
  });
  return { ...scaffold, panels };
}
function traceWindowFrom(node, targetUnit) {
  const start = num(node?.start);
  const stop = num(node?.stop);
  const sourceUnit = typeof node?.unit === "string" ? node.unit : void 0;
  if (start === void 0 || stop === void 0 || sourceUnit === void 0) return void 0;
  const unit = targetUnit ?? sourceUnit;
  const convertedStart = sourceUnit === unit ? start : convert(start, sourceUnit, unit);
  const convertedStop = sourceUnit === unit ? stop : convert(stop, sourceUnit, unit);
  if (sourceUnit !== unit) {
    const expectedWidth = convertDifference(start, stop, sourceUnit, unit);
    const actualWidth = convertedStop - convertedStart;
    if (!(expectedWidth > 0) || !Number.isFinite(expectedWidth) || !Number.isFinite(actualWidth) || !binary64EffectWasPreserved(expectedWidth, actualWidth)) {
      throw new Error("trace window conversion materially distorted its width at binary64 resolution");
    }
  }
  return {
    start: convertedStart,
    stop: convertedStop,
    unit,
    finalEdgeInclusive: typeof node?.boundary === "string" && node.boundary.endsWith("]")
  };
}
function traceDuplicateOptions(parameters) {
  const policyNode = parameters.duplicateTimePolicy;
  const structuredPolicy = rec(policyNode);
  const duplicatePolicy = typeof policyNode === "string" ? policyNode : structuredPolicy?.policy;
  const structuredAggregate = structuredPolicy?.aggregate;
  const aggregateMethod = parameters.aggregateMethod ?? parameters.duplicateTimeAggregate ?? structuredPolicy?.method ?? (typeof structuredAggregate === "string" ? structuredAggregate : rec(structuredAggregate)?.method);
  return {
    ...duplicatePolicy ? { duplicatePolicy } : {},
    ...aggregateMethod ? { aggregateMethod } : {}
  };
}
function traceValues(node) {
  return (arr(node?.values) ?? []).map(
    (value) => value === null ? null : typeof value === "number" && Number.isFinite(value) ? value : null
  );
}
function traceNormalizationFrom(parameters, displayWindow) {
  const normalization = rec(parameters.normalization);
  const method = normalization?.method;
  if (!normalization || !method) return void 0;
  const statisticsWindow = traceWindowFrom(rec(normalization.statisticsWindow), displayWindow.unit);
  return statisticsWindow ? { method, statisticsWindow } : void 0;
}
function prepareTrace(input, window, parameters, targetValueUnit, normalization) {
  return prepareTraceSeries(input, {
    window,
    ...traceDuplicateOptions(parameters),
    ...targetValueUnit ? { targetValueUnit } : {},
    ...normalization ? { normalization } : {}
  });
}
function traceOutputAuthority(series, pathClassId, sampleClassId, uncertaintyClassId) {
  return {
    pathClassId,
    sampleClassId,
    uncertaintyClassId,
    observationProvenance: series.observations.map((observation) => ({
      seriesId: series.id,
      sourceOrdinals: [...observation.sourceOrdinals]
    }))
  };
}
function traceCell2(values) {
  return values.length === 1 ? values[0] : JSON.stringify(values);
}
function traceCompilerSourceSummary(skillId, facts) {
  const template = SKILL_CATALOG[skillId].accessibility.summaryTemplate;
  const placeholderPattern = /\{([A-Za-z][A-Za-z0-9]*)\}/g;
  const required = [];
  for (const match of template.matchAll(placeholderPattern)) required.push(match[1]);
  const requiredSet = new Set(required);
  const templateLiterals = template.replace(placeholderPattern, "");
  if (required.length === 0 || templateLiterals.includes("{") || templateLiterals.includes("}")) {
    throw new Error(`${skillId} accessibility summary template has no placeholders or contains a malformed token`);
  }
  const supplied = Object.keys(facts);
  const missing = [...requiredSet].filter((key) => !Object.prototype.hasOwnProperty.call(facts, key));
  const extra = supplied.filter((key) => !requiredSet.has(key));
  if (missing.length > 0 || extra.length > 0) {
    throw new Error(
      `${skillId} compiler summary facts do not exactly match its source template (missing ${missing.join(", ") || "none"}; extra ${extra.join(", ") || "none"})`
    );
  }
  for (const [key, value] of Object.entries(facts)) {
    if (!isOutputAuthoritySummaryFactSafeV1(value)) {
      throw new Error(`${skillId} compiler summary fact ${key} is not safe, well-formed display text`);
    }
  }
  const summary = template.replace(placeholderPattern, (_placeholder, key) => facts[key]);
  if (!isOutputAuthoritySummarySafeV1(summary)) {
    throw new Error(`${skillId} compiler produced an unsafe summary`);
  }
  return summary;
}
function traceCompilerContext(base, skillId, facts) {
  return { ...base, summary: traceCompilerSourceSummary(skillId, facts) };
}
function distributionCompilerSourceSummary(skillId, facts) {
  const template = SKILL_CATALOG[skillId].accessibility.summaryTemplate;
  const placeholderPattern = /\{([A-Za-z][A-Za-z0-9]*)\}/g;
  const required = [];
  for (const match of template.matchAll(placeholderPattern)) required.push(match[1]);
  const requiredSet = new Set(required);
  const templateLiterals = template.replace(placeholderPattern, "");
  if (required.length === 0 || templateLiterals.includes("{") || templateLiterals.includes("}")) {
    throw new Error(`${skillId} accessibility summary template has no placeholders or contains a malformed token`);
  }
  const supplied = Object.keys(facts);
  const missing = [...requiredSet].filter(
    (key) => !Object.prototype.hasOwnProperty.call(facts, key)
  );
  const extra = supplied.filter((key) => !requiredSet.has(key));
  if (missing.length > 0 || extra.length > 0) {
    throw new Error(
      `${skillId} distribution compiler summary facts do not exactly match its source template (missing ${missing.join(", ") || "none"}; extra ${extra.join(", ") || "none"})`
    );
  }
  for (const [key, value] of Object.entries(facts)) {
    if (!isOutputAuthoritySummaryFactSafeV1(value)) {
      throw new Error(
        `${skillId} distribution compiler summary fact ${key} is not safe, well-formed display text`
      );
    }
  }
  const summary = template.replace(placeholderPattern, (_placeholder, key) => facts[key]);
  if (!isOutputAuthoritySummarySafeV1(summary)) {
    throw new Error(`${skillId} distribution compiler produced an unsafe summary`);
  }
  return summary;
}
function distributionCompilerContext(base, skillId, facts) {
  return { ...base, summary: distributionCompilerSourceSummary(skillId, facts) };
}
function topologyDynamicsCompilerSourceSummary(skillId, facts) {
  const template = SKILL_CATALOG[skillId].accessibility.summaryTemplate;
  if (!isOutputAuthoritySummarySafeV1(template)) {
    throw new Error(
      `${skillId} topology/dynamics compiler source template is not bounded control-safe display text`
    );
  }
  const placeholderPattern = /\{([A-Za-z][A-Za-z0-9]*)\}/gu;
  const withoutRecognizedTokens = template.replace(/\{[A-Za-z][A-Za-z0-9]*\}/gu, "");
  if (withoutRecognizedTokens.includes("{") || withoutRecognizedTokens.includes("}")) {
    throw new Error(
      `${skillId} topology/dynamics compiler source template contains an unrecognized brace token`
    );
  }
  const required = [];
  for (const match of template.matchAll(placeholderPattern)) required.push(match[1]);
  const requiredSet = new Set(required);
  const supplied = Object.keys(facts);
  const missing = [...requiredSet].filter(
    (key) => !Object.prototype.hasOwnProperty.call(facts, key)
  );
  const extra = supplied.filter((key) => !requiredSet.has(key));
  if (required.length === 0 || missing.length > 0 || extra.length > 0) {
    throw new Error(
      `${skillId} topology/dynamics compiler summary facts do not exactly match its source template (missing ${missing.join(", ") || "none"}; extra ${extra.join(", ") || "none"})`
    );
  }
  for (const [key, value] of Object.entries(facts)) {
    if (!isOutputAuthoritySummaryFactSafeV1(value)) {
      throw new Error(
        `${skillId} topology/dynamics compiler summary fact ${key} is not bounded control-safe display text`
      );
    }
  }
  const summary = template.replace(placeholderPattern, (_placeholder, key) => facts[key]);
  if (!isOutputAuthoritySummarySafeV1(summary)) {
    throw new Error(
      `${skillId} topology/dynamics compiler produced an unsafe summary`
    );
  }
  return summary;
}
function topologyDynamicsCompilerContext(base, skillId, facts) {
  return { ...base, summary: topologyDynamicsCompilerSourceSummary(skillId, facts) };
}
function distributionCompilerNumber(value) {
  return value === 0 ? "0" : String(value);
}
function distributionCompilerExtrema(values) {
  if (values.length === 0) return null;
  let min = values[0];
  let max = values[0];
  for (let index = 1; index < values.length; index++) {
    const value = values[index];
    if (value < min) min = value;
    if (value > max) max = value;
  }
  return { min, max };
}
function distributionCompilerSortedUnique(values) {
  return [...new Set(values)].sort(compareUnicodeCodePoints3).join(", ");
}
function traceCompilerSummaryNumber(value) {
  return value === 0 ? "0" : String(value);
}
function traceCompilerJoinUnique(values) {
  return [...new Set(values)].join(", ");
}
function traceCompilerOriginText(raw) {
  const origin = rec(raw.origin) ?? {};
  return origin.kind === "derived" ? `derived: ${String(origin.method ?? "unspecified")}` : String(origin.kind ?? "unknown");
}
function traceCompilerDuplicateStatement(parameters, traces) {
  const options = traceDuplicateOptions(parameters);
  const groups = traces.reduce((sum, trace) => sum + trace.duplicateGroups, 0);
  if (groups === 0) {
    return `${String(options.duplicatePolicy ?? "reject_if_present")} (no duplicate groups present)`;
  }
  return options.duplicatePolicy === "aggregate" ? `aggregate (${String(options.aggregateMethod)}; ${groups} duplicate groups)` : `${String(options.duplicatePolicy)} (${groups} duplicate groups)`;
}
function traceCompilerUnitConversionStatement(traces) {
  const conversions = [];
  for (const trace of traces) {
    if (trace.sourceTimeUnit !== trace.timeUnit) {
      conversions.push(`${trace.id} time ${trace.sourceTimeUnit} to ${trace.timeUnit}`);
    }
    if (!trace.normalization && trace.sourceValueUnit !== trace.valueUnit) {
      conversions.push(`${trace.id} value ${trace.sourceValueUnit} to ${trace.valueUnit}`);
    }
    if (trace.timeOffset && trace.timeOffset.sourceUnit !== trace.timeUnit) {
      conversions.push(`${trace.id} offset ${trace.timeOffset.sourceUnit} to ${trace.timeUnit}`);
    }
  }
  return conversions.length === 0 ? "No unit conversion was applied." : `Unit conversions: ${conversions.join("; ")}.`;
}
function traceCompilerUncertaintyStatement(uncertainties) {
  const descriptions = uncertainties.map((uncertainty) => {
    const kind = typeof uncertainty?.kind === "string" ? uncertainty.kind : "none";
    return kind === "none" && typeof uncertainty?.reason === "string" ? `none (${uncertainty.reason})` : kind;
  });
  return `Uncertainty declarations: ${traceCompilerJoinUnique(descriptions)}.`;
}
function traceCompilerWeightUncertaintyStatement(uncertainties) {
  const descriptions = uncertainties.map((uncertainty) => {
    const kind = typeof uncertainty?.kind === "string" ? uncertainty.kind : "none";
    if (kind === "none") {
      return typeof uncertainty?.reason === "string" ? `none (${uncertainty.reason})` : "none";
    }
    const basis = String(uncertainty?.basis ?? "ensemble_members");
    if (kind === "standard_deviation") {
      return `standard_deviation (descriptive sample SD across exact ${basis}; center=arithmetic mean; divisor=n-1; no sampling-population or coverage claim)`;
    }
    if (kind === "quantile_interval") {
      return `quantile_interval (descriptive empirical Type-7 linear q=${String(uncertainty?.lowerQuantile)}\u2013${String(uncertainty?.upperQuantile)} across exact ${basis}; no coverage claim)`;
    }
    if (kind === "ensemble_range") {
      return `ensemble_range (descriptive observed minimum\u2013maximum across exact ${basis}; no sampling-population or coverage claim)`;
    }
    throw new Error(`unsupported synaptic-weight uncertainty summary kind ${kind}`);
  });
  return `Uncertainty declarations: ${traceCompilerJoinUnique(descriptions)}.`;
}
function traceCompilerNoCompactionStatement() {
  return "No compaction was applied; every accepted row is returned.";
}
function traceCompilerMinimum(values) {
  if (values.length === 0) throw new Error("minimum of an empty sequence is undefined");
  let result = values[0];
  for (let index = 1; index < values.length; index++) result = Math.min(result, values[index]);
  return result;
}
function traceCompilerMaximum(values) {
  if (values.length === 0) throw new Error("maximum of an empty sequence is undefined");
  let result = values[0];
  for (let index = 1; index < values.length; index++) result = Math.max(result, values[index]);
  return result;
}
function traceUncertaintyTransform(declared, prepared, value, dispersion) {
  if (typeof value !== "number" || !Number.isFinite(value)) return null;
  const sourceUnit = typeof declared.unit === "string" ? declared.unit : prepared.valueUnit;
  if (!prepared.normalization) {
    const converted2 = sourceUnit === prepared.valueUnit ? value : convert(value, sourceUnit, prepared.valueUnit);
    return Number.isFinite(converted2) ? converted2 : null;
  }
  const sourceValue = sourceUnit === prepared.sourceValueUnit ? value : convert(value, sourceUnit, prepared.sourceValueUnit);
  return applyTraceNormalization(sourceValue, prepared.normalization, dispersion);
}
function traceUncertaintyDifferenceTransform(declared, prepared, lower, upper) {
  const sourceUnit = typeof declared.unit === "string" ? declared.unit : prepared.valueUnit;
  const targetUnit = prepared.normalization ? prepared.sourceValueUnit : prepared.valueUnit;
  const convertedDifference = sourceUnit === targetUnit ? exactBinary64Sum([upper, -lower]) : convertDifference(lower, upper, sourceUnit, targetUnit);
  return prepared.normalization ? applyTraceNormalization(convertedDifference, prepared.normalization, true) : convertedDifference;
}
function assertUncertaintyAffineSequence(sourceValues, transformedValues, transformDifference, effectEpsilonMultiples) {
  const pairs = sourceValues.flatMap((source, index) => typeof source === "number" && Number.isFinite(source) && typeof transformedValues[index] === "number" && Number.isFinite(transformedValues[index]) ? [{ source, transformed: transformedValues[index] }] : []);
  pairs.sort((left, right) => left.source - right.source || left.transformed - right.transformed);
  for (let index = 1; index < pairs.length; index++) {
    const previous = pairs[index - 1];
    const current = pairs[index];
    if (current.source === previous.source) continue;
    const actualDifference = current.transformed - previous.transformed;
    if (!(actualDifference > 0) || !Number.isFinite(actualDifference)) {
      throw new Error("uncertainty affine transform collapsed distinct values below binary64 resolution");
    }
    if (effectEpsilonMultiples === void 0 || !transformDifference) continue;
    const expectedDifference = transformDifference(previous.source, current.source);
    if (expectedDifference === null || !(expectedDifference > 0) || !Number.isFinite(expectedDifference) || !binary64EffectWasPreserved(expectedDifference, actualDifference, effectEpsilonMultiples)) {
      throw new Error("uncertainty affine transform materially distorted distinct values at binary64 resolution");
    }
  }
}
function traceUncertaintyCells(uncertainty, sourceOrdinal, prepared, displayedValue) {
  const kind = typeof uncertainty?.kind === "string" ? uncertainty.kind : void 0;
  if (!kind || kind === "none" || sourceOrdinal === void 0) {
    const reason = typeof uncertainty?.reason === "string" ? uncertainty.reason : void 0;
    return [null, null, kind === "none" ? `none${reason ? ` (${reason})` : ""}` : null];
  }
  const declared = uncertainty ?? {};
  const effectEpsilonMultiples = prepared.normalization ? 32 : 8;
  const transform = (value, dispersion) => traceUncertaintyTransform(declared, prepared, value, dispersion);
  const describe = (extra = []) => [kind, ...extra].join("; ");
  const sampleCount = arr(declared.sampleCount)?.[sourceOrdinal];
  if (kind === "standard_deviation" || kind === "standard_error") {
    const description2 = describe([
      `basis=${String(declared.basis ?? "unspecified")}`,
      ...typeof sampleCount === "number" ? [`sampleCount=${sampleCount}`] : []
    ]);
    const dispersion = transform(arr(declared.values)?.[sourceOrdinal], true);
    if (displayedValue === null || dispersion === null) return [null, null, description2];
    const lower2 = displayedValue - dispersion;
    const upper2 = displayedValue + dispersion;
    if (!Number.isFinite(lower2) || !Number.isFinite(upper2)) {
      throw new Error("uncertainty transformation produced a non-finite bound");
    }
    if (dispersion !== 0 && (!binary64EffectWasPreserved(dispersion, displayedValue - lower2, effectEpsilonMultiples) || !binary64EffectWasPreserved(dispersion, upper2 - displayedValue, effectEpsilonMultiples))) {
      throw new Error("uncertainty dispersion is distorted below binary64 resolution");
    }
    return [lower2, upper2, description2];
  }
  const sourceLower = arr(declared.lower)?.[sourceOrdinal];
  const sourceUpper = arr(declared.upper)?.[sourceOrdinal];
  const lower = transform(sourceLower, false);
  const upper = transform(sourceUpper, false);
  if (typeof sourceLower === "number" && typeof sourceUpper === "number" && sourceLower < sourceUpper && lower !== null && upper !== null && lower === upper) {
    throw new Error("uncertainty interval collapsed below binary64 resolution");
  }
  if (typeof sourceLower === "number" && typeof sourceUpper === "number" && sourceLower < sourceUpper && lower !== null && upper !== null) {
    const expectedWidth = traceUncertaintyDifferenceTransform(
      declared,
      prepared,
      sourceLower,
      sourceUpper
    );
    const actualWidth = upper - lower;
    if (expectedWidth !== null && expectedWidth > 0 && Number.isFinite(expectedWidth) && (!Number.isFinite(actualWidth) || !binary64EffectWasPreserved(expectedWidth, actualWidth, effectEpsilonMultiples))) {
      throw new Error("uncertainty interval width is distorted below binary64 resolution");
    }
  }
  const description = describe([
    ...typeof declared.method === "string" ? [`method=${declared.method}`] : [],
    ...typeof declared.level === "number" ? [`level=${declared.level}`] : [],
    ...typeof declared.coverage === "string" ? [`coverage=${declared.coverage}`] : [],
    ...typeof declared.lowerQuantile === "number" ? [`lowerQuantile=${declared.lowerQuantile}`] : [],
    ...typeof declared.upperQuantile === "number" ? [`upperQuantile=${declared.upperQuantile}`] : [],
    ...typeof declared.basis === "string" ? [`basis=${declared.basis}`] : [],
    ...typeof sampleCount === "number" ? [`sampleCount=${sampleCount}`] : []
  ]);
  return [lower, upper, description];
}
function traceUncertaintyPresentation(uncertainty, prepared) {
  const kind = typeof uncertainty?.kind === "string" ? uncertainty.kind : "none";
  if (kind === "none") return void 0;
  const registeredStyle = UNCERTAINTY_STYLES_BY_KIND[kind];
  if (!registeredStyle || registeredStyle.mark !== "band" && registeredStyle.mark !== "whisker") {
    throw new Error(`uncertainty kind ${kind} has no registered drawable mark`);
  }
  const basis = String(uncertainty?.basis ?? "unspecified basis");
  const counts = prepared.observations.flatMap((observation) => observation.sourceOrdinals.length === 1 ? [arr(uncertainty?.sampleCount)?.[observation.sourceOrdinals[0]]] : []).filter((value) => typeof value === "number" && Number.isInteger(value));
  const uniqueCounts = [...new Set(counts)].sort((a, b) => a - b);
  const countLabel = uniqueCounts.length === 0 ? "not supplied" : uniqueCounts.length === 1 ? String(uniqueCounts[0]) : `${uniqueCounts[0]}\u2013${uniqueCounts[uniqueCounts.length - 1]}, varying by point; exact n is in the table`;
  const substitutions = {
    level: typeof uncertainty?.level === "number" ? formatNumber(uncertainty.level * 100) : "not supplied",
    coverage: String(uncertainty?.coverage ?? "not supplied"),
    method: String(uncertainty?.method ?? "not supplied"),
    lowerQuantile: typeof uncertainty?.lowerQuantile === "number" ? formatNumber(uncertainty.lowerQuantile) : "not supplied",
    upperQuantile: typeof uncertainty?.upperQuantile === "number" ? formatNumber(uncertainty.upperQuantile) : "not supplied",
    basis,
    sampleCount: countLabel,
    reason: String(uncertainty?.reason ?? "not supplied")
  };
  const label = registeredStyle.label.replace(/\{(\w+)\}/g, (_whole, key) => {
    const replacement = substitutions[key];
    if (replacement === void 0) {
      throw new Error(`uncertainty style ${kind} has unresolved label placeholder ${key}`);
    }
    return replacement;
  });
  if (/\{\w+\}/.test(label)) {
    throw new Error(`uncertainty style ${kind} has an unresolved label placeholder`);
  }
  return {
    kind,
    mark: registeredStyle.mark,
    label
  };
}
function traceUncertaintyArrays(uncertainty, prepared) {
  const kind = typeof uncertainty?.kind === "string" ? uncertainty.kind : "none";
  if (kind === "none") return {};
  const lower = [];
  const upper = [];
  for (const observation of prepared.observations) {
    if (observation.sourceOrdinals.length !== 1) {
      throw new Error("uncertainty cannot be synthesized for an aggregated duplicate-time group");
    }
    const [lo, hi] = traceUncertaintyCells(
      uncertainty,
      observation.sourceOrdinals[0],
      prepared,
      observation.value
    );
    lower.push(observation.value === null ? null : lo);
    upper.push(observation.value === null ? null : hi);
  }
  const transformDifference = (lowerValue, upperValue) => traceUncertaintyDifferenceTransform(uncertainty ?? {}, prepared, lowerValue, upperValue);
  const effectEpsilonMultiples = prepared.normalization ? void 0 : 8;
  if (kind === "standard_deviation" || kind === "standard_error") {
    const sourceDispersions = prepared.observations.map((observation) => observation.sourceOrdinals.length === 1 ? arr(uncertainty?.values)?.[observation.sourceOrdinals[0]] : null);
    const transformedDispersions = sourceDispersions.map((value) => traceUncertaintyTransform(uncertainty ?? {}, prepared, value, true));
    assertUncertaintyAffineSequence(
      sourceDispersions,
      transformedDispersions,
      transformDifference,
      effectEpsilonMultiples
    );
  } else {
    const sourceLower = prepared.observations.map((observation) => observation.sourceOrdinals.length === 1 ? arr(uncertainty?.lower)?.[observation.sourceOrdinals[0]] : null);
    const sourceUpper = prepared.observations.map((observation) => observation.sourceOrdinals.length === 1 ? arr(uncertainty?.upper)?.[observation.sourceOrdinals[0]] : null);
    assertUncertaintyAffineSequence(sourceLower, lower, transformDifference, effectEpsilonMultiples);
    assertUncertaintyAffineSequence(sourceUpper, upper, transformDifference, effectEpsilonMultiples);
  }
  return {
    uncertaintyLower: lower,
    uncertaintyUpper: upper,
    uncertainty: traceUncertaintyPresentation(uncertainty, prepared)
  };
}
function traceSeriesHasCompleteDrawableUncertainty(entry, showSamplePoints = false) {
  const lower = entry.uncertaintyLower;
  const upper = entry.uncertaintyUpper;
  if (!lower || !upper || !entry.uncertainty) return false;
  let eligibleIndexes;
  if (entry.uncertainty.mark === "whisker" || entry.renderRuns === void 0) {
    eligibleIndexes = entry.series.observations.map((_observation, index) => index);
  } else {
    const renderedLineage = /* @__PURE__ */ new Set();
    for (const run of entry.renderRuns) {
      for (const role of run.authority ?? []) {
        if (role.tag !== "data_carrier") continue;
        const provenance = rec(role.provenance);
        const sourceOrdinals = arr(provenance?.sourceOrdinals);
        if (sourceOrdinals) renderedLineage.add(canonicalize(sourceOrdinals));
      }
    }
    const eligible = new Set(
      entry.series.observations.flatMap((observation, index) => renderedLineage.has(canonicalize(observation.sourceOrdinals)) ? [index] : [])
    );
    if (showSamplePoints) {
      for (const index of entry.markerObservationIndexes ?? entry.series.observations.map((_observation, observationIndex) => observationIndex)) {
        eligible.add(index);
      }
    }
    eligibleIndexes = [...eligible].sort((left, right) => left - right);
  }
  let observed = false;
  for (const index of eligibleIndexes) {
    if (entry.series.values[index] === null) continue;
    observed = true;
    if (typeof lower[index] !== "number" || !Number.isFinite(lower[index]) || typeof upper[index] !== "number" || !Number.isFinite(upper[index])) return false;
  }
  return observed;
}
function traceUncertaintyLengthMismatch(uncertainty, expected) {
  if (!uncertainty || uncertainty.kind === "none") return void 0;
  for (const key of ["values", "lower", "upper", "sampleCount"]) {
    const values = arr(uncertainty[key]);
    if (values && values.length !== expected) return { key, observed: values.length };
  }
  return void 0;
}
function traceUncertaintyValidationError(uncertainty, valueUnit, sourceValues, skillId, instancePath) {
  const kind = typeof uncertainty?.kind === "string" ? uncertainty.kind : "none";
  if (kind === "none") return void 0;
  if (kind === "credible_interval") {
    return makeError({
      code: "SCIENCE_UNCERTAINTY_UNSUPPORTED_FOR_SKILL",
      stage: "science",
      instancePath: `${instancePath}/kind`,
      skillId,
      message: "every stable Artifact 1.0 skill refuses credible intervals because the request and artifact contracts have no verified-attestation input or verifier; Cortexel cannot promote caller-supplied bounds into posterior evidence."
    });
  }
  if (!SKILL_CATALOG[skillId].uncertaintySupport.includes(kind)) {
    return makeError({
      code: "SCIENCE_UNCERTAINTY_UNSUPPORTED_FOR_SKILL",
      stage: "science",
      instancePath: `${instancePath}/kind`,
      skillId,
      message: `${skillId} does not support uncertainty kind ${kind}.`
    });
  }
  const uncertaintyUnit = typeof uncertainty?.unit === "string" ? uncertainty.unit : void 0;
  if (uncertaintyUnit && (dimensionOf(uncertaintyUnit) !== dimensionOf(valueUnit) || dimensionOf(valueUnit) === "simulator_defined" && uncertaintyUnit !== valueUnit)) {
    return makeError({
      code: "SCIENCE_UNIT_DIMENSION_MISMATCH",
      stage: "science",
      instancePath: `${instancePath}/unit`,
      skillId,
      message: `uncertainty unit ${uncertaintyUnit} is not compatible with series unit ${valueUnit}.`
    });
  }
  if (kind === "standard_deviation" || kind === "standard_error") {
    const values = arr(uncertainty?.values) ?? [];
    const sampleCount = arr(uncertainty?.sampleCount) ?? [];
    const invalid = values.findIndex((value) => typeof value === "number" && value < 0);
    if (invalid >= 0) {
      return makeError({
        code: "SCIENCE_UNCERTAINTY_BOUNDS_INVALID",
        stage: "science",
        instancePath: `${instancePath}/values/${invalid}`,
        skillId,
        message: `${kind} is a non-negative distance; a negative dispersion cannot be drawn.`
      });
    }
    for (let index = 0; index < Math.min(values.length, sampleCount.length); index++) {
      if (values[index] === null !== (sampleCount[index] === null)) {
        return makeError({
          code: "SCIENCE_UNCERTAINTY_BOUNDS_INVALID",
          stage: "science",
          instancePath: `${instancePath}/sampleCount/${index}`,
          skillId,
          message: `${kind} and sampleCount must be present or missing together at every point.`
        });
      }
      if (sourceValues[index] === null && values[index] !== null) {
        return makeError({
          code: "SCIENCE_UNCERTAINTY_BOUNDS_INVALID",
          stage: "science",
          instancePath: `${instancePath}/values/${index}`,
          skillId,
          message: `uncertainty at index ${index} cannot qualify a missing central observation. The uncertainty value and sample count must also be null.`
        });
      }
    }
  }
  const lower = arr(uncertainty?.lower);
  const upper = arr(uncertainty?.upper);
  if (lower && upper) {
    const sampleCount = arr(uncertainty?.sampleCount);
    for (let index = 0; index < Math.min(lower.length, upper.length); index++) {
      const lo = lower[index];
      const hi = upper[index];
      if (lo === null !== (hi === null)) {
        return makeError({
          code: "SCIENCE_UNCERTAINTY_BOUNDS_INVALID",
          stage: "science",
          instancePath: `${instancePath}/${hi === null ? "upper" : "lower"}/${index}`,
          skillId,
          message: `lower and upper uncertainty bounds must be present or missing together at index ${index}.`
        });
      }
      if (sampleCount && index < sampleCount.length && lo === null !== (sampleCount[index] === null)) {
        return makeError({
          code: "SCIENCE_UNCERTAINTY_BOUNDS_INVALID",
          stage: "science",
          instancePath: `${instancePath}/sampleCount/${index}`,
          skillId,
          message: `uncertainty bounds and sampleCount must share one missingness mask at index ${index}.`
        });
      }
      if (sourceValues[index] === null && lo !== null) {
        return makeError({
          code: "SCIENCE_UNCERTAINTY_BOUNDS_INVALID",
          stage: "science",
          instancePath: `${instancePath}/lower/${index}`,
          skillId,
          message: `uncertainty bounds at index ${index} cannot qualify a missing central observation. Both bounds${sampleCount ? " and sampleCount" : ""} must also be null.`
        });
      }
      if (typeof lo === "number" && typeof hi === "number" && lo > hi) {
        return makeError({
          code: "SCIENCE_UNCERTAINTY_BOUNDS_INVALID",
          stage: "science",
          instancePath: `${instancePath}/lower/${index}`,
          skillId,
          message: `uncertainty lower bound ${lo} exceeds upper bound ${hi}.`
        });
      }
    }
  }
  if (kind === "quantile_interval" && typeof uncertainty?.lowerQuantile === "number" && typeof uncertainty?.upperQuantile === "number" && !(uncertainty.lowerQuantile < uncertainty.upperQuantile)) {
    return makeError({
      code: "SCIENCE_UNCERTAINTY_LEVEL_INVALID",
      stage: "science",
      instancePath: `${instancePath}/upperQuantile`,
      skillId,
      message: "the lower uncertainty quantile must be strictly below the upper quantile."
    });
  }
  return void 0;
}
function traceOperation(inputPayload, prepared, window, parameters, uncertainty, stateEvidence) {
  const duplicate = traceDuplicateOptions(parameters);
  const retainedSourceCount = prepared.replicateCounts.reduce((sum, count) => sum + count, 0);
  return {
    id: `trace.prepare.${prepared.id}`,
    algorithm: "cortexel.trace.prepare_series",
    algorithmRevision: stateEvidence ? 2 : 1,
    parameters: {
      window,
      duplicateTimePolicy: duplicate.duplicatePolicy ?? "reject_if_present",
      ...duplicate.aggregateMethod ? { aggregateMethod: duplicate.aggregateMethod } : {},
      targetTimeUnit: prepared.timeUnit,
      targetValueUnit: prepared.valueUnit,
      ...prepared.normalization ? { normalization: prepared.normalization.method } : {},
      ...stateEvidence ? {
        observation: stateEvidence.observation,
        stateWindow: stateEvidence.stateWindow,
        ...stateEvidence.membershipLifetimes ? { membershipLifetimes: stateEvidence.membershipLifetimes } : {}
      } : {}
    },
    inputDigest: canonicalDigest(stateEvidence ? {
      inputPayload,
      observation: stateEvidence.observation,
      analysisWindow: window,
      stateWindow: stateEvidence.stateWindow,
      membershipLifetimes: stateEvidence.membershipLifetimes ?? null
    } : inputPayload),
    outputDigest: canonicalDigest({
      id: prepared.id,
      observations: prepared.observations,
      timeUnit: prepared.timeUnit,
      valueUnit: prepared.valueUnit,
      sourceTimeUnit: prepared.sourceTimeUnit,
      sourceValueUnit: prepared.sourceValueUnit,
      ...uncertainty?.lower ? { uncertaintyLower: uncertainty.lower } : {},
      ...uncertainty?.upper ? { uncertaintyUpper: uncertainty.upper } : {},
      ...stateEvidence ? {
        stateObservations: stateEvidence.statePrepared.observations,
        stateTimeUnit: stateEvidence.statePrepared.timeUnit,
        stateValueUnit: stateEvidence.statePrepared.valueUnit,
        fullMaterialization: stateEvidence.fullMaterialization ?? null,
        displayedMaterialization: stateEvidence.displayedMaterialization ?? null,
        stateWitnesses: stateEvidence.stateWitnesses ?? [],
        initialStateConsumedByDerivedAggregate: stateEvidence.initialStateConsumedByDerivedAggregate === true
      } : {}
    }),
    receipt: {
      inputObservationCount: prepared.inputCount,
      retainedObservationCount: retainedSourceCount,
      outputRowCount: prepared.outputCount,
      excludedBelowWindow: prepared.excludedBelow,
      excludedAboveWindow: prepared.excludedAbove,
      excludedOutOfWindow: prepared.excludedBelow + prepared.excludedAbove,
      missingObservationCount: prepared.missingCount,
      duplicateGroupCount: prepared.duplicateGroups,
      reordered: prepared.reordered,
      sourceOrderBoundByInputDigest: true,
      retainedSourceOrdinalDigest: canonicalDigest(
        prepared.observations.flatMap((observation) => observation.sourceOrdinals)
      ),
      ...prepared.timeConversion ? { timeConversion: prepared.timeConversion } : {},
      ...prepared.valueConversion ? { valueConversion: prepared.valueConversion } : {},
      ...prepared.timeOffset ? { timeOffset: prepared.timeOffset } : {},
      ...prepared.normalization ? { normalization: prepared.normalization } : {},
      ...uncertainty?.lower || uncertainty?.upper ? {
        uncertaintyTransformed: true,
        ...uncertainty.conversion ? { uncertaintyConversion: uncertainty.conversion } : {}
      } : {},
      ...stateEvidence ? {
        fullStateBeforeDisplayClip: true,
        stateObservationCount: stateEvidence.statePrepared.observations.reduce(
          (sum, observation) => sum + observation.replicateCount,
          0
        ),
        stateOutputRowCount: stateEvidence.statePrepared.outputCount,
        stateObservationDigest: canonicalDigest(stateEvidence.statePrepared.observations),
        displayedObservationDigest: canonicalDigest(prepared.observations),
        fullMaterializationDigest: canonicalDigest(stateEvidence.fullMaterialization ?? null),
        displayedMaterializationDigest: canonicalDigest(
          stateEvidence.displayedMaterialization ?? null
        ),
        stateWitnessCount: stateEvidence.stateWitnesses?.length ?? 0,
        stateWitnessDigest: canonicalDigest(stateEvidence.stateWitnesses ?? []),
        initialStateConsumedByDerivedAggregate: stateEvidence.initialStateConsumedByDerivedAggregate === true
      } : {}
    }
  };
}
var WEIGHT_TRACE_PREPARATION_BATCH_DOMAIN = "cortexel.weight_trace.prepare_series_batch/v1";
var COMPARTMENT_TRACE_PREPARATION_BATCH_DOMAIN = "cortexel.compartment_trace.prepare_series_batch/v1";
var MAX_TRACE_PREPARATION_VIEW_ROWS = 25e4;
function traceBatchDigest(digestDomain, suffix, payload) {
  return canonicalDigest({
    digestDomain: `${digestDomain}/${suffix}`,
    payload
  });
}
function nonIdentityConversion(from, to) {
  return from !== void 0 && to !== void 0 && from !== to ? conversionReceipt(from, to) : null;
}
function tracePreparationBatchViewReceipt(digestDomain, sourceIndex, seriesIdentity, view) {
  const prepared = view.prepared;
  const boundedCounters = [
    prepared.inputCount,
    prepared.outputCount,
    prepared.excludedBelow,
    prepared.excludedAbove,
    prepared.missingCount,
    prepared.duplicateGroups
  ];
  if (!Number.isFinite(view.window.start) || !Number.isFinite(view.window.stop) || !(view.window.stop > view.window.start) || view.window.unit !== prepared.timeUnit || boundedCounters.some((count) => !Number.isSafeInteger(count) || count < 0 || count > MAX_TRACE_PREPARATION_VIEW_ROWS) || prepared.times.length !== prepared.outputCount || prepared.values.length !== prepared.outputCount || prepared.replicateCounts.length !== prepared.outputCount || prepared.observations.length !== prepared.outputCount || view.uncertaintyLower === void 0 !== (view.uncertaintyUpper === void 0) || view.uncertaintyLower !== void 0 && view.uncertaintyLower.length !== prepared.outputCount || view.uncertaintyUpper !== void 0 && view.uncertaintyUpper.length !== prepared.outputCount) {
    throw new Error("trace preparation batch view violates its bounded structural postconditions");
  }
  const sourceOrdinals = /* @__PURE__ */ new Set();
  let recomputedMissingSourceCount = 0;
  for (let index = 0; index < prepared.observations.length; index++) {
    const observation = prepared.observations[index];
    if (!Number.isSafeInteger(observation.replicateCount) || observation.replicateCount < 1 || observation.replicateCount > MAX_TRACE_PREPARATION_VIEW_ROWS || observation.replicateCount !== prepared.replicateCounts[index] || observation.sourceOrdinals.length !== observation.replicateCount || observation.sourceValues.length !== observation.replicateCount || observation.time !== prepared.times[index] || observation.value !== prepared.values[index]) {
      throw new Error("trace preparation batch view contains an inconsistent prepared observation");
    }
    for (const ordinal of observation.sourceOrdinals) {
      if (!Number.isSafeInteger(ordinal) || ordinal < 0 || ordinal >= prepared.inputCount || sourceOrdinals.has(ordinal)) {
        throw new Error("trace preparation batch view contains an invalid or repeated source ordinal");
      }
      sourceOrdinals.add(ordinal);
    }
    recomputedMissingSourceCount += observation.sourceValues.filter(
      (value) => value === null
    ).length;
  }
  const retainedSourceCount = prepared.observations.reduce(
    (sum, observation) => sum + observation.replicateCount,
    0
  );
  if (!Number.isSafeInteger(retainedSourceCount) || retainedSourceCount !== sourceOrdinals.size || prepared.inputCount !== retainedSourceCount + prepared.excludedBelow + prepared.excludedAbove || prepared.outputCount !== prepared.observations.length || retainedSourceCount > MAX_TRACE_PREPARATION_VIEW_ROWS || prepared.missingCount !== recomputedMissingSourceCount || prepared.missingCount > retainedSourceCount) {
    throw new Error("trace preparation batch view violates exact count conservation or the 250000-row bound");
  }
  const retainedOrdinalGroups = prepared.observations.map((observation) => [...observation.sourceOrdinals]);
  const outputPayload = {
    sourceIndex,
    seriesIdentity,
    kind: view.kind,
    window: view.window,
    prepared,
    uncertaintyLower: view.uncertaintyLower ?? null,
    uncertaintyUpper: view.uncertaintyUpper ?? null
  };
  return {
    kind: view.kind,
    window: view.window,
    inputSourceCount: prepared.inputCount,
    retainedSourceCount,
    outputRowCount: prepared.outputCount,
    excludedBelowWindow: prepared.excludedBelow,
    excludedAboveWindow: prepared.excludedAbove,
    missingRetainedSourceCount: prepared.missingCount,
    duplicateGroupCount: prepared.duplicateGroups,
    reordered: prepared.reordered,
    sourceOrderBoundByInputDigest: true,
    retainedSourceOrdinalDigest: traceBatchDigest(
      digestDomain,
      "retained-source-ordinals",
      {
        sourceIndex,
        seriesIdentity,
        kind: view.kind,
        retainedOrdinalGroups
      }
    ),
    outputDigest: traceBatchDigest(digestDomain, "view-output", outputPayload)
  };
}
function tracePreparationContextWitnessReceipts(digestDomain, sourceIndex, seriesIdentity, displayView, statePrepared, witnesses) {
  if (witnesses.length === 0) return [];
  if (!statePrepared || witnesses.length > 2) {
    throw new Error("trace preparation context witnesses require one bounded state view");
  }
  const seenRoles = /* @__PURE__ */ new Set();
  const displaySourceOrdinals = new Set(
    displayView.prepared.observations.flatMap((observation) => observation.sourceOrdinals)
  );
  return witnesses.map((candidate) => {
    const witness = rec(candidate) ?? {};
    const role = String(witness.role);
    const observation = rec(witness.observation);
    if (role !== "carry_in" && role !== "look_ahead" || seenRoles.has(role) || !observation) {
      throw new Error("trace preparation context witnesses must have unique carry-in/look-ahead roles");
    }
    seenRoles.add(role);
    const witnessSourceOrdinals = numbers4(observation.sourceOrdinals);
    const witnessTime = num(observation.time);
    const observationKey = canonicalize(observation);
    const stateObservationIndex = statePrepared.observations.findIndex(
      (stateObservation) => canonicalize(stateObservation) === observationKey
    );
    if (stateObservationIndex < 0 || stateObservationIndex >= MAX_TRACE_PREPARATION_VIEW_ROWS || witnessTime === void 0 || witnessSourceOrdinals.some((ordinal) => displaySourceOrdinals.has(ordinal)) || role === "carry_in" && !(witnessTime < displayView.window.start) || role === "look_ahead" && !(witnessTime > displayView.window.stop || witnessTime === displayView.window.stop && !displayView.window.finalEdgeInclusive)) {
      throw new Error(
        "trace preparation context witness is not a role-correct out-of-display observation in the bounded state view"
      );
    }
    return {
      role,
      stateObservationIndex,
      consultedByDerivedAggregate: witness.consultedByDerivedAggregate === true,
      observationDigest: traceBatchDigest(
        digestDomain,
        "context-witness",
        {
          sourceIndex,
          seriesIdentity,
          role,
          stateObservationIndex,
          observation
        }
      )
    };
  });
}
function tracePreparationBatchOperation(family, globalContextPayload, entryInputs) {
  const digestDomain = family === "weight" ? WEIGHT_TRACE_PREPARATION_BATCH_DOMAIN : COMPARTMENT_TRACE_PREPARATION_BATCH_DOMAIN;
  const maximumSeries = family === "weight" ? 1024 : 8192;
  if (entryInputs.length < 1 || entryInputs.length > maximumSeries) {
    throw new Error(
      `${family} trace preparation batch requires 1..${maximumSeries} canonical source entries`
    );
  }
  const roles = new Set(entryInputs.map((entry) => entry.role));
  if (family === "compartment" && (roles.size !== 1 || !roles.has("source_series")) || family === "weight" && roles.has("declared_aggregate") && (entryInputs.length !== 1 || roles.size !== 1)) {
    throw new Error("trace preparation batch mixes an impossible family/role combination");
  }
  const identityKeys = /* @__PURE__ */ new Set();
  for (let index = 0; index < entryInputs.length; index++) {
    const entry = entryInputs[index];
    if (entry.sourceIndex !== index) {
      throw new Error("trace preparation batch sourceIndex must be contiguous canonical request order");
    }
    const identityKey = canonicalize(entry.seriesIdentity);
    if (identityKeys.has(identityKey)) {
      throw new Error("trace preparation batch contains a duplicate structured series identity");
    }
    identityKeys.add(identityKey);
    const viewKinds = entry.views.map((view) => view.kind).join(",");
    const expectedViewKinds = family === "weight" && entry.role === "source_series" ? "display,state" : "display";
    if (viewKinds !== expectedViewKinds) {
      throw new Error(
        `trace preparation batch ${family}/${entry.role} requires exact view order ${expectedViewKinds}`
      );
    }
  }
  const globalContextDigest = traceBatchDigest(
    digestDomain,
    "global-context",
    globalContextPayload
  );
  const seriesReceipts = entryInputs.map((entry) => {
    const inputDigest2 = traceBatchDigest(digestDomain, "series-input", {
      globalContextDigest,
      sourceIndex: entry.sourceIndex,
      seriesIdentity: entry.seriesIdentity,
      role: entry.role,
      inputPayload: entry.inputPayload
    });
    const views = entry.views.map((view) => tracePreparationBatchViewReceipt(
      digestDomain,
      entry.sourceIndex,
      entry.seriesIdentity,
      view
    ));
    const displayView = entry.views.find((view) => view.kind === "display");
    const statePrepared = entry.views.find((view) => view.kind === "state")?.prepared;
    const contextWitnesses = tracePreparationContextWitnessReceipts(
      digestDomain,
      entry.sourceIndex,
      entry.seriesIdentity,
      displayView,
      statePrepared,
      entry.contextWitnesses ?? []
    );
    const materialization = entry.materialization ? {
      kind: "event_updated",
      fullOutputDigest: traceBatchDigest(
        digestDomain,
        "materialization-full",
        {
          sourceIndex: entry.sourceIndex,
          seriesIdentity: entry.seriesIdentity,
          materialization: entry.materialization.full
        }
      ),
      displayedOutputDigest: traceBatchDigest(
        digestDomain,
        "materialization-displayed",
        {
          sourceIndex: entry.sourceIndex,
          seriesIdentity: entry.seriesIdentity,
          materialization: entry.materialization.displayed
        }
      )
    } : null;
    return {
      sourceIndex: entry.sourceIndex,
      seriesIdentity: entry.seriesIdentity,
      role: entry.role,
      inputDigest: inputDigest2,
      views,
      transforms: entry.transforms,
      contextWitnesses,
      materialization,
      initialStatePainted: entry.initialStatePainted,
      initialStateConsumedByDerivedAggregate: entry.initialStateConsumedByDerivedAggregate
    };
  });
  const receipt = {
    globalContextDigest,
    seriesCount: seriesReceipts.length,
    seriesReceipts
  };
  const inputDigest = traceBatchDigest(digestDomain, "operation-input", {
    globalContextDigest,
    seriesInputDigests: seriesReceipts.map((entry) => entry.inputDigest)
  });
  const outputDigest = traceBatchDigest(
    digestDomain,
    "operation-output",
    receipt
  );
  return {
    id: family === "weight" ? "weight.trace.prepare_series_batch" : "compartment.trace.prepare_series_batch",
    algorithm: family === "weight" ? "cortexel.weight_trace.prepare_series_batch" : "cortexel.compartment_trace.prepare_series_batch",
    algorithmRevision: 1,
    parameters: {
      seriesOrder: "canonical_request_order",
      digestCanonicalization: "rfc8785",
      digestDomain
    },
    inputDigest,
    outputDigest,
    receipt
  };
}
function chainAggregateToPreparationBatch(operation, preparationBatch) {
  const preparationBatchOutputDigest = preparationBatch.outputDigest;
  if (operation.algorithm !== "cortexel.weight_trace.aggregate_members" && operation.algorithm !== "cortexel.compartment_trace.aggregate_explicit_selection" || operation.inputDigest === void 0 || operation.outputDigest === void 0 || preparationBatchOutputDigest === void 0) {
    throw new Error("only a fully digested registered trace aggregate can be chained to a preparation batch");
  }
  const algorithmRevision = 4;
  const digestDomain = `${operation.algorithm}/v${String(algorithmRevision)}`;
  const outputReceipt = rec(operation.receipt.output);
  const outputUnits = operation.algorithm === "cortexel.weight_trace.aggregate_members" ? {
    timeUnit: String(rec(operation.parameters.analysisWindow)?.unit),
    valueUnit: String(operation.parameters.targetValueUnit)
  } : {
    timeUnit: String(rec(outputReceipt?.time)?.unit),
    valueUnit: String(rec(outputReceipt?.value)?.unit)
  };
  const scientificInputDigest = operation.inputDigest;
  const scientificOutputDigest = operation.outputDigest;
  const stateWitnessDigest = operation.algorithm === "cortexel.weight_trace.aggregate_members" ? canonicalDigest({
    digestDomain: "cortexel.weight_trace.aggregate_members/v4/state-witnesses",
    payload: (arr(rec(preparationBatch.receipt)?.seriesReceipts) ?? []).flatMap((candidate) => {
      const entry = rec(candidate) ?? {};
      return (arr(entry.contextWitnesses) ?? []).map((witness) => rec(witness) ?? {}).filter((witness) => witness.consultedByDerivedAggregate === true).map((witness) => ({
        seriesIdentity: entry.seriesIdentity,
        role: witness.role,
        stateObservationIndex: witness.stateObservationIndex,
        observationDigest: witness.observationDigest
      }));
    })
  }) : void 0;
  return {
    ...operation,
    algorithmRevision,
    parameters: {
      ...operation.parameters,
      digestCanonicalization: "rfc8785",
      digestDomain,
      preparationBatchOutputDigest
    },
    inputDigest: canonicalDigest({
      digestDomain: `${digestDomain}/operation-input`,
      payload: {
        preparationBatchOutputDigest,
        scientificInputDigest
      }
    }),
    outputDigest: canonicalDigest({
      digestDomain: `${digestDomain}/operation-output`,
      payload: {
        scientificOutputDigest,
        outputUnits
      }
    }),
    receipt: {
      ...operation.receipt,
      ...stateWitnessDigest === void 0 ? {} : { stateWitnessDigest },
      scientificInputDigest,
      scientificOutputDigest,
      outputUnits,
      preparationBatchOutputDigest
    }
  };
}
function derivationOperationIdsAreUnique(operationIds) {
  return operationIds.length <= 64 && operationIds.every((id) => id.trim().length > 0) && new Set(operationIds).size === operationIds.length;
}
function uncertaintyConversionForPreparedTrace(rawUncertainty, prepared) {
  return rawUncertainty?.kind !== void 0 && rawUncertainty.kind !== "none" ? nonIdentityConversion(
    typeof rawUncertainty.unit === "string" ? rawUncertainty.unit : void 0,
    prepared.valueUnit
  ) : null;
}
function weightPreparationBatchTransforms(member, referenceRendered) {
  const interval = rec(member.raw.recordedInterval);
  const initialQuantity = rec(rec(member.raw.initialWeight)?.quantity);
  const bounds = rec(member.raw.bounds);
  const lowerBound = rec(bounds?.lower);
  const upperBound = rec(bounds?.upper);
  return {
    time: member.prepared.timeConversion ?? null,
    value: member.prepared.valueConversion ?? null,
    recordedInterval: nonIdentityConversion(
      typeof interval?.unit === "string" ? interval.unit : void 0,
      member.prepared.timeUnit
    ),
    initialValue: nonIdentityConversion(
      typeof initialQuantity?.unit === "string" ? initialQuantity.unit : void 0,
      member.prepared.valueUnit
    ),
    uncertainty: null,
    normalization: null,
    renderedLowerBound: referenceRendered ? nonIdentityConversion(
      typeof lowerBound?.unit === "string" ? lowerBound.unit : void 0,
      member.prepared.valueUnit
    ) : null,
    renderedUpperBound: referenceRendered ? nonIdentityConversion(
      typeof upperBound?.unit === "string" ? upperBound.unit : void 0,
      member.prepared.valueUnit
    ) : null
  };
}
function declaredWeightPreparationBatchTransforms(member) {
  return {
    time: member.prepared.timeConversion ?? null,
    value: null,
    recordedInterval: null,
    initialValue: null,
    uncertainty: uncertaintyConversionForPreparedTrace(
      rec(member.raw.uncertainty),
      member.prepared
    ),
    normalization: null,
    renderedLowerBound: null,
    renderedUpperBound: null
  };
}
function compartmentPreparationBatchTransforms(entry, uncertainty) {
  return {
    time: entry.prepared.timeConversion ?? null,
    value: entry.prepared.valueConversion ?? null,
    recordedInterval: null,
    initialValue: null,
    uncertainty: uncertaintyConversionForPreparedTrace(
      uncertainty,
      entry.prepared
    ),
    normalization: null,
    renderedLowerBound: null,
    renderedUpperBound: null
  };
}
function traceDerivedFacts(series, aggregateMethod, uncertainties = []) {
  const conversions = [];
  for (const prepared of series) {
    if (prepared.timeConversion) {
      conversions.push(
        `${prepared.id}: ${prepared.timeConversion.from} -> ${prepared.timeConversion.to} (factor ${prepared.timeConversion.factor})`
      );
    }
    if (prepared.valueConversion) {
      conversions.push(
        `${prepared.id}: ${prepared.valueConversion.from} -> ${prepared.valueConversion.to} (factor ${prepared.valueConversion.factor})`
      );
    }
    if (prepared.timeOffset && prepared.timeOffset.sourceUnit !== prepared.timeOffset.displayUnit) {
      conversions.push(
        `${prepared.id} offset: ${prepared.timeOffset.sourceUnit} -> ${prepared.timeOffset.displayUnit} (factor ${conversionFactor(prepared.timeOffset.sourceUnit, prepared.timeOffset.displayUnit)})`
      );
    }
  }
  for (let index = 0; index < Math.min(series.length, uncertainties.length); index++) {
    const uncertainty = uncertainties[index];
    const sourceUnit = typeof uncertainty?.unit === "string" ? uncertainty.unit : void 0;
    if (!sourceUnit || uncertainty?.kind === "none") continue;
    const targetUnit = series[index].normalization ? series[index].sourceValueUnit : series[index].valueUnit;
    if (sourceUnit !== targetUnit) {
      conversions.push(
        `${series[index].id} uncertainty: ${sourceUnit} -> ${targetUnit} (factor ${conversionFactor(sourceUnit, targetUnit)})`
      );
    }
  }
  const duplicateGroups = series.reduce((sum, prepared) => sum + prepared.duplicateGroups, 0);
  const missingOutputRows = series.reduce(
    (sum, prepared) => sum + prepared.observations.filter((observation) => observation.value === null).length,
    0
  );
  const missingAggregateReplicateCount = series.reduce(
    (sum, prepared) => sum + prepared.observations.reduce(
      (seriesSum, observation) => seriesSum + (observation.replicateCount > 1 && observation.value !== null ? observation.sourceValues.filter((value) => value === null).length : 0),
      0
    ),
    0
  );
  return {
    excludedOutOfWindow: series.reduce(
      (sum, prepared) => sum + prepared.excludedBelow + prepared.excludedAbove,
      0
    ),
    missingValueCount: missingOutputRows,
    ...missingAggregateReplicateCount > 0 ? { missingAggregateReplicateCount } : {},
    ...conversions.length > 0 ? { unitConversions: conversions } : {},
    ...duplicateGroups > 0 && aggregateMethod ? { duplicateTimeAggregateMethod: aggregateMethod } : {}
  };
}
function tracePreparationError(error, skillId, instancePath) {
  const message = error instanceof Error ? error.message : "trace preparation failed";
  let code = "INTERNAL_INVARIANT_VIOLATED";
  let stage = "internal";
  if (message.includes("equal length")) {
    code = "SEMANTIC_LENGTH_MISMATCH";
    stage = "semantic";
  } else if (message.includes("duplicate")) {
    code = "SCIENCE_DUPLICATE_TIME_POLICY";
    stage = "science";
  } else if (message.includes("binary64 resolution") || message.includes("overflowed") || message.includes("overflows") || message.includes("underflowed") || message.includes("underflows to zero") || message.includes("unrepresentable binary64") || message.includes("collapsed") || message.includes("clock conversion produced a non-finite time")) {
    code = "SCIENCE_NUMERIC_RESOLUTION_UNREPRESENTABLE";
    stage = "science";
  } else if (message.includes("normalization") || message.includes("z_score") || message.includes("min_max")) {
    code = "SCIENCE_NORMALIZATION_UNVERIFIABLE";
    stage = "science";
  } else if (message.includes("uncertainty cannot be synthesized")) {
    code = "SCIENCE_UNCERTAINTY_UNSUPPORTED_FOR_SKILL";
    stage = "science";
  } else if (message.includes("uncertainty")) {
    code = "SCIENCE_UNCERTAINTY_BOUNDS_INVALID";
    stage = "science";
  } else if (message.includes("convert") || message.includes("dimension") || message.includes("unit")) {
    code = "SCIENCE_UNIT_DIMENSION_MISMATCH";
    stage = "science";
  }
  return makeError({
    code,
    stage,
    instancePath,
    skillId,
    message: code === "INTERNAL_INVARIANT_VIOLATED" ? "validated trace data could not be transformed without violating a finite-output invariant" : message
  });
}
function compartmentTraceTableRows(data, preparedEntries) {
  const cellId = String(data.cellId);
  const compartmentIds = strings4(data.compartmentIds);
  const labels = strings4(data.compartmentLabels);
  const parents = arr(data.compartmentParentIds) ?? [];
  const distances = rec(data.compartmentPathDistances);
  const distanceValues = arr(distances?.values) ?? [];
  const distanceUnit = typeof distances?.unit === "string" ? distances.unit : null;
  const seriesByCompartment = /* @__PURE__ */ new Map();
  for (const entry of preparedEntries) {
    const compartmentId = String(entry.raw.compartmentId);
    const entries = seriesByCompartment.get(compartmentId);
    if (entries) entries.push(entry);
    else seriesByCompartment.set(compartmentId, [entry]);
  }
  const rows = [];
  for (let rowIndex = 0; rowIndex < compartmentIds.length; rowIndex++) {
    const compartmentId = compartmentIds[rowIndex];
    const entries = seriesByCompartment.get(compartmentId) ?? [];
    const common = [
      cellId,
      rowIndex,
      compartmentId,
      labels[rowIndex] ?? compartmentId,
      typeof parents[rowIndex] === "string" ? parents[rowIndex] : null,
      typeof distanceValues[rowIndex] === "number" ? distanceValues[rowIndex] : null,
      distanceUnit
    ];
    if (entries.length === 0) {
      rows.push([...common, "no", null, null, null, null, null, null, null]);
      continue;
    }
    for (const entry of entries) {
      for (const observation of entry.prepared.observations) {
        rows.push([
          ...common,
          "yes",
          String(entry.raw.signalId),
          observation.recordedTime,
          entry.prepared.sourceTimeUnit,
          observation.value,
          entry.prepared.valueUnit,
          booleanCell3(observation.value === null),
          traceCell2(observation.sourceOrdinals)
        ]);
      }
    }
  }
  return rows;
}
function deriveCompartmentAggregate(aggregate4, entries, window) {
  const selectedIds = strings4(aggregate4.compartmentIds);
  const selected = selectedIds.map((compartmentId) => {
    const candidates = entries.filter((entry) => String(entry.raw.compartmentId) === compartmentId);
    if (candidates.length !== 1) {
      throw new Error(
        `compartment aggregate selection ${compartmentId} resolves to ${candidates.length} series; exactly one signal series per selected compartment is required`
      );
    }
    return candidates[0];
  });
  if (selected.length === 0) throw new Error("compartment aggregate selection is empty");
  const signalIds = new Set(selected.map((entry) => String(entry.raw.signalId)));
  if (signalIds.size !== 1) {
    throw new Error("compartment aggregate selection spans more than one signal identity");
  }
  const targetUnit = selected[0].prepared.valueUnit;
  for (const entry of selected.slice(1)) {
    if (!axesAreCompatible(targetUnit, entry.prepared.valueUnit)) {
      throw new Error(`compartment aggregate cannot combine ${targetUnit} with ${entry.prepared.valueUnit}`);
    }
  }
  const times = [...new Set(selected.flatMap((entry) => [...entry.prepared.times]))].sort((left, right) => left - right);
  const byMember = selected.map((entry) => {
    const values2 = /* @__PURE__ */ new Map();
    for (let index = 0; index < entry.prepared.times.length; index++) {
      const time = entry.prepared.times[index];
      const existing = values2.get(time);
      if (existing) existing.push(entry.prepared.values[index]);
      else values2.set(time, [entry.prepared.values[index]]);
    }
    return values2;
  });
  const weighting = String(aggregate4.weighting);
  const weights = weighting === "declared" ? numbers4(aggregate4.weights) : selected.map(() => 1);
  const method = String(aggregate4.method);
  const values = times.map((time) => {
    const memberValues = [];
    for (let index = 0; index < selected.length; index++) {
      const candidates = byMember[index].get(time) ?? [];
      if (candidates.length !== 1 || candidates[0] === null) return null;
      const value = selected[index].prepared.valueUnit === targetUnit ? candidates[0] : convert(candidates[0], selected[index].prepared.valueUnit, targetUnit);
      memberValues.push(value);
    }
    if (method === "mean") {
      return exactBinary64WeightedMean(memberValues, weights);
    }
    if (method === "sum") {
      return exactBinary64WeightedSum(memberValues, weights);
    }
    if (method === "median") return empiricalQuantile(memberValues, 0.5);
    if (method === "min") return traceCompilerMinimum(memberValues);
    if (method === "max") return traceCompilerMaximum(memberValues);
    throw new Error(`unsupported compartment aggregate method ${method}`);
  });
  const id = `aggregate-${selectedIds.join("-")}`;
  const label = typeof aggregate4.label === "string" ? aggregate4.label : `${method} over ${selected.length} declared compartments`;
  const prepared = prepareTraceSeries({
    id,
    label,
    observationKind: "point_sample",
    times,
    timeUnit: window.unit,
    values,
    valueUnit: targetUnit
  }, { window, duplicatePolicy: "reject", targetValueUnit: targetUnit });
  const uncertaintySource = selected.length === 1 ? selected[0] : void 0;
  const uncertaintyScale = method === "sum" ? weights[0] : 1;
  const propagatedBounds = (source) => {
    if (!source || !uncertaintySource) return void 0;
    return times.map((time, timeIndex) => {
      const matching = uncertaintySource.prepared.times.map((candidate, index) => candidate === time ? index : -1).filter((index) => index >= 0);
      if (matching.length !== 1 || values[timeIndex] === null) return null;
      const bound = source[matching[0]];
      return bound === null ? null : exactBinary64WeightedSum([bound], [uncertaintyScale]);
    });
  };
  const propagatedLower = propagatedBounds(uncertaintySource?.uncertaintyLower);
  const propagatedUpper = propagatedBounds(uncertaintySource?.uncertaintyUpper);
  return {
    entry: {
      series: prepared,
      styleIndex: entries.length,
      outputAuthority: traceOutputAuthority(prepared, "aggregate", "aggregate", "uncertainty"),
      ...propagatedLower ? { uncertaintyLower: propagatedLower } : {},
      ...propagatedUpper ? { uncertaintyUpper: propagatedUpper } : {},
      ...uncertaintySource?.uncertainty ? { uncertainty: uncertaintySource.uncertainty } : {}
    },
    operation: {
      id: "compartment.aggregate.explicit_selection",
      algorithm: "cortexel.compartment_trace.aggregate_explicit_selection",
      algorithmRevision: 2,
      parameters: {
        selectedCompartmentIds: selectedIds,
        method,
        weighting,
        weights,
        binary64Arithmetic: "exact_products_and_cancellation_then_one_final_round",
        ...aggregate4.weightBasis !== void 0 ? { weightBasis: aggregate4.weightBasis } : {},
        alignment: "exact_accepted_time_only",
        duplicateReplicateAlignment: "undefined_yields_missing"
      },
      inputDigest: canonicalDigest(selected.map((entry) => entry.raw)),
      outputDigest: canonicalDigest({ times, values, unit: targetUnit }),
      receipt: {
        selectedCompartmentCount: selected.length,
        evaluationCount: times.length,
        missingBecauseAbsentOrAmbiguousCount: values.filter((value) => value === null).length,
        noInterpolation: true,
        noSurvivorOnlyDenominator: true,
        output: {
          time: { unit: window.unit, values: times },
          value: { unit: targetUnit, values }
        }
      }
    }
  };
}
function countForPreparedObservation(sourceCounts, sourceOrdinals) {
  const counts = sourceOrdinals.map((ordinal) => sourceCounts[ordinal]);
  if (counts.length === 0 || counts.some((count) => typeof count !== "number")) return null;
  if (counts.some((count) => count !== counts[0])) {
    throw new Error("duplicate aggregate observations disagree on their declared member count");
  }
  return counts[0];
}
function emptyWeightCarrierCounts() {
  return {
    source_observation: { total: 0, missing: 0 },
    source_state_witness: { total: 0, missing: 0 },
    caller_reconstruction_point: { total: 0, missing: 0 },
    derived_aggregate_evaluation: { total: 0, missing: 0 },
    declared_aggregate_point: { total: 0, missing: 0 },
    declared_initial_state: { total: 0, missing: 0 }
  };
}
function weightCarrierStatement(counts, sourceReadingCount, missingSourceReadingCount, excludedSourceReadingCount, reconstructionPointCount, retainedReconstructionRowCount, missingReconstructionPointCount, excludedReconstructionPointCount) {
  const clause = (kind, label) => `${label} ${counts[kind].total} rows (${counts[kind].missing} missing rows)`;
  return `Carriers: ${[
    `source readings ${sourceReadingCount} in ${counts.source_observation.total} retained rows (${missingSourceReadingCount} missing readings; ${excludedSourceReadingCount} excluded outside the window)`,
    clause("source_state_witness", "source state witnesses"),
    clause("caller_reconstruction_point", "caller reconstruction point carriers"),
    `reconstruction semantics ${reconstructionPointCount} raw points in ${retainedReconstructionRowCount} retained rows (${missingReconstructionPointCount} missing retained points; ${excludedReconstructionPointCount} excluded outside the window)`,
    clause("derived_aggregate_evaluation", "Cortexel-derived aggregate evaluations"),
    clause("declared_aggregate_point", "caller-declared aggregate points"),
    clause("declared_initial_state", "influential declared initial states")
  ].join("; ")}.`;
}
function weightModelsInData(data) {
  const records = data.mode === "preaggregated" ? [rec(data.aggregate) ?? {}] : (arr(data.series) ?? []).map((candidate) => rec(candidate) ?? {});
  return [...new Set(records.flatMap((series) => {
    const aggregateModels = (arr(series.synapseModels) ?? []).filter((model) => typeof model === "string");
    return aggregateModels.length > 0 ? aggregateModels : typeof series.synapseModel === "string" ? [series.synapseModel] : [];
  }))];
}
function weightSynapseModelCell(series, data, parameters) {
  const aggregateModels = (arr(series.synapseModels) ?? []).filter((model) => typeof model === "string");
  const actualModels = aggregateModels.length > 0 ? aggregateModels : typeof series.synapseModel === "string" ? [series.synapseModel] : [];
  const comparability = rec(parameters.weightComparability) ?? {};
  const mode = String(comparability.mode);
  const declaredModels = mode === "declared_comparable_models" ? (arr(comparability.comparableModels) ?? []).filter((model) => typeof model === "string") : weightModelsInData(data);
  return structuredCell5({
    kind: "weight_model_comparability",
    actualModels,
    declaredModels,
    comparabilityMode: mode
  });
}
function saturatingWeightRowSum(increments, limit) {
  if (!Number.isSafeInteger(limit) || limit < 0 || limit >= Number.MAX_SAFE_INTEGER) {
    throw new Error("weight returned-table limit must be a non-negative safe integer below MAX_SAFE_INTEGER");
  }
  let count = 0;
  for (const increment of increments) {
    if (!Number.isSafeInteger(increment) || increment < 0) {
      throw new Error("weight carrier increment must be a non-negative safe integer");
    }
    if (increment > limit - count) {
      return { tag: "saturated_over_budget", saturatedRowCount: limit + 1 };
    }
    count += increment;
  }
  return { tag: "exact_within_budget", exactRowCount: count };
}
function preflightPreparedWeightObservationRows(members, limit) {
  return saturatingWeightRowSum(
    members.map((member) => member.prepared.observations.length),
    limit
  );
}
function preflightWeightTableCarriers(seriesEntries, limit) {
  if (!Number.isSafeInteger(limit) || limit < 0 || limit >= Number.MAX_SAFE_INTEGER) {
    throw new Error("weight returned-table limit must be a non-negative safe integer below MAX_SAFE_INTEGER");
  }
  const carriers = [];
  const saturationSentinel = limit + 1;
  const append = (carrier2) => {
    if (carriers.length === limit) return false;
    carriers.push(carrier2);
    return true;
  };
  for (const entry of seriesEntries) {
    const witnesses = [...entry.stateWitnesses ?? []];
    for (let index = 0; index < witnesses.length; index++) {
      if (witnesses[index].role === "carry_in" && !append({
        tag: "source_state_witness",
        entry,
        witness: witnesses[index],
        carrierOrdinal: index
      })) return { tag: "saturated_over_budget", saturatedRowCount: saturationSentinel };
    }
    const initialInfluential = entry.initialStatePainted === true || entry.initialStateConsumedByDerivedAggregate === true;
    const initialHold = initialInfluential ? declaredInitialHold(entry.member, entry.observation, entry.membershipLifetimes) : void 0;
    if (initialHold && !append({ tag: "declared_initial_state", entry, hold: initialHold })) return { tag: "saturated_over_budget", saturatedRowCount: saturationSentinel };
    for (let observationIndex = 0; observationIndex < entry.member.prepared.observations.length; observationIndex++) {
      if (!append({ tag: "prepared_observation", entry, observationIndex })) {
        return { tag: "saturated_over_budget", saturatedRowCount: saturationSentinel };
      }
    }
    for (let index = 0; index < witnesses.length; index++) {
      if (witnesses[index].role === "look_ahead" && !append({
        tag: "source_state_witness",
        entry,
        witness: witnesses[index],
        carrierOrdinal: index
      })) return { tag: "saturated_over_budget", saturatedRowCount: saturationSentinel };
    }
  }
  return { tag: "exact_within_budget", exactRowCount: carriers.length, carriers };
}
function synapticWeightTraceTableRows(data, parameters, preflight, renderedRunsByEntry = /* @__PURE__ */ new Map()) {
  const scopeSummary = compilerNetworkScopeSummaryCell(data.scope);
  const rows = [];
  const carrierCounts = emptyWeightCarrierCounts();
  let sourceReadingCount = 0;
  let missingSourceReadingCount = 0;
  const account = (kind, value) => {
    carrierCounts[kind].total++;
    if (value === null) carrierCounts[kind].missing++;
  };
  for (const carrier2 of preflight.carriers) {
    const { entry } = carrier2;
    const { member, observation, carrierKind, counts } = entry;
    const series = member.raw;
    const prepared = member.prepared;
    const endpoints = rec(series.endpoints);
    const uncertainty = rec(series.uncertainty);
    const eventKinds = arr(series.eventKinds) ?? [];
    const modelCell = weightSynapseModelCell(series, data, parameters);
    if (carrier2.tag === "source_state_witness") {
      const stateObservation = carrier2.witness.observation;
      const witnessOrdinal = stateObservation.sourceOrdinals.length === 1 ? stateObservation.sourceOrdinals[0] : void 0;
      const [lower, upper, uncertaintyMethod] = traceUncertaintyCells(
        uncertainty,
        witnessOrdinal,
        member.statePrepared,
        stateObservation.value
      );
      const witnessEvents = stateObservation.sourceOrdinals.flatMap((ordinal) => typeof eventKinds[ordinal] === "string" ? [eventKinds[ordinal]] : []);
      const topology2 = weightCarrierRenderTopology(
        renderedRunsByEntry.get(entry),
        stateObservation.sourceOrdinals,
        member.statePrepared.timeUnit
      );
      account("source_state_witness", stateObservation.value);
      rows.push([
        String(series.edgeId ?? series.groupId),
        String(series.label ?? series.groupLabel ?? series.edgeId ?? series.groupId),
        typeof endpoints?.sourceId === "string" ? endpoints.sourceId : null,
        typeof endpoints?.targetId === "string" ? endpoints.targetId : null,
        modelCell,
        stateObservation.time,
        member.statePrepared.timeUnit,
        stateObservation.value,
        member.statePrepared.valueUnit,
        String(observation.kind),
        typeof observation.updateSemantics === "string" ? observation.updateSemantics : null,
        topology2.paintedInterval,
        topology2.renderRunOrdinal,
        witnessEvents.length === 0 ? null : traceCell2(witnessEvents),
        booleanCell3(stateObservation.value === null),
        null,
        null,
        null,
        lower,
        upper,
        uncertaintyMethod,
        structuredCell5(series.initialWeight),
        structuredCell5(series.bounds),
        "source_state_witness",
        carrier2.carrierOrdinal,
        structuredCell5({
          role: carrier2.witness.role,
          directlyPainted: topology2.renderRunOrdinal !== null,
          consultedByDerivedAggregate: carrier2.witness.consultedByDerivedAggregate === true,
          ...weightDuplicateResolutionMetadata(stateObservation, parameters)
        }),
        traceCell2(stateObservation.sourceOrdinals),
        scopeSummary
      ]);
      continue;
    }
    if (carrier2.tag === "declared_initial_state") {
      const initialHold = carrier2.hold;
      const topology2 = weightCarrierRenderTopology(
        renderedRunsByEntry.get(entry),
        [],
        prepared.timeUnit,
        true
      );
      account("declared_initial_state", initialHold.value);
      rows.push([
        String(series.edgeId ?? series.groupId),
        String(series.label ?? series.groupLabel ?? series.edgeId ?? series.groupId),
        typeof endpoints?.sourceId === "string" ? endpoints.sourceId : null,
        typeof endpoints?.targetId === "string" ? endpoints.targetId : null,
        modelCell,
        initialHold.start,
        prepared.timeUnit,
        initialHold.value,
        prepared.valueUnit,
        String(observation.kind),
        String(observation.updateSemantics),
        topology2.paintedInterval,
        topology2.renderRunOrdinal,
        null,
        booleanCell3(false),
        null,
        null,
        null,
        null,
        null,
        null,
        structuredCell5(series.initialWeight),
        structuredCell5(series.bounds),
        "declared_initial_state",
        0,
        structuredCell5({
          directlyPainted: topology2.renderRunOrdinal !== null,
          consumedByDerivedAggregate: entry.initialStateConsumedByDerivedAggregate === true,
          declaredAt: member.initialStateTime,
          declaredAtUnit: prepared.timeUnit
        }),
        null,
        scopeSummary
      ]);
      continue;
    }
    {
      const rowIndex = carrier2.observationIndex;
      const preparedObservation = prepared.observations[rowIndex];
      const sourceOrdinals = preparedObservation.sourceOrdinals;
      const value = preparedObservation.value;
      const [uncertaintyLower, uncertaintyUpper, uncertaintyMethod] = traceUncertaintyCells(
        uncertainty,
        sourceOrdinals.length === 1 ? sourceOrdinals[0] : void 0,
        prepared,
        value
      );
      const observationKind = String(observation.kind);
      const updateSemantics = typeof observation.updateSemantics === "string" ? observation.updateSemantics : null;
      const topology2 = weightCarrierRenderTopology(
        renderedRunsByEntry.get(entry),
        sourceOrdinals,
        prepared.timeUnit
      );
      const isSourceObservation = carrierKind === "source_observation";
      const hasSourceLineage = isSourceObservation || observation.kind === "interpolated_trajectory";
      const rowEventKinds = isSourceObservation ? sourceOrdinals.flatMap((ordinal) => typeof eventKinds[ordinal] === "string" ? [eventKinds[ordinal]] : []) : [];
      account(carrierKind, value);
      if (isSourceObservation) {
        sourceReadingCount += preparedObservation.replicateCount;
        missingSourceReadingCount += preparedObservation.sourceValues.filter(
          (sourceValue) => sourceValue === null
        ).length;
      }
      rows.push([
        String(series.edgeId ?? series.groupId),
        String(series.label ?? series.groupLabel ?? series.edgeId ?? series.groupId),
        typeof endpoints?.sourceId === "string" ? endpoints.sourceId : null,
        typeof endpoints?.targetId === "string" ? endpoints.targetId : null,
        modelCell,
        preparedObservation.time,
        prepared.timeUnit,
        value,
        prepared.valueUnit,
        observationKind,
        updateSemantics,
        topology2.paintedInterval,
        topology2.renderRunOrdinal,
        rowEventKinds.length === 0 ? null : traceCell2(rowEventKinds),
        booleanCell3(value === null),
        hasSourceLineage ? preparedObservation.replicateCount : null,
        counts ? countForPreparedObservation(counts.memberCounts, sourceOrdinals) : null,
        counts ? countForPreparedObservation(counts.contributingCounts, sourceOrdinals) : null,
        uncertaintyLower,
        uncertaintyUpper,
        uncertaintyMethod,
        structuredCell5(series.initialWeight),
        structuredCell5(series.bounds),
        carrierKind,
        rowIndex,
        weightPreparedCarrierMetadata(
          observation,
          preparedObservation,
          parameters
        ),
        hasSourceLineage ? traceCell2(sourceOrdinals) : null,
        scopeSummary
      ]);
    }
  }
  return { rows, carrierCounts, sourceReadingCount, missingSourceReadingCount };
}
function sameWeightSourceOrdinals(left, right) {
  return left.length === right.length && left.every((value, index) => value === right[index]);
}
function weightCarrierRenderTopology(runs, sourceOrdinals, timeUnit, declaredInitialState = false) {
  let matchedRunOrdinal = null;
  let paintedFrom;
  let paintedUntil;
  for (let runOrdinal = 0; runOrdinal < (runs?.length ?? 0); runOrdinal++) {
    const run = runs[runOrdinal];
    let matchedRun = false;
    for (let vertexIndex = 0; vertexIndex < run.times.length; vertexIndex++) {
      const role = run.authority?.[vertexIndex];
      if (role?.tag !== "data_carrier") continue;
      const provenance = rec(role.provenance) ?? {};
      const matches = declaredInitialState ? provenance.segmentSource === "declared_initial_weight" : sameWeightSourceOrdinals(
        arr(provenance.sourceOrdinals) ?? [],
        sourceOrdinals
      );
      if (!matches) continue;
      matchedRun = true;
      if (provenance.atomKind === "hold_segment") {
        if (provenance.endpoint === "start") paintedFrom = run.times[vertexIndex];
        if (provenance.endpoint === "stop") paintedUntil = run.times[vertexIndex];
      }
    }
    if (!matchedRun) continue;
    if (matchedRunOrdinal !== null && matchedRunOrdinal !== runOrdinal) {
      throw new Error("one weight carrier is assigned to more than one explicit render run");
    }
    matchedRunOrdinal = runOrdinal;
  }
  return {
    paintedInterval: paintedFrom !== void 0 && paintedUntil !== void 0 && paintedUntil > paintedFrom ? structuredCell5({ from: paintedFrom, until: paintedUntil, unit: timeUnit }) : null,
    renderRunOrdinal: matchedRunOrdinal
  };
}
function weightDuplicateResolutionMetadata(observation, parameters) {
  if (observation.replicateCount <= 1) return {};
  const duplicate = traceDuplicateOptions(parameters);
  if (duplicate.duplicatePolicy !== "aggregate" || typeof duplicate.aggregateMethod !== "string") {
    throw new Error("a collapsed weight carrier lacks its named duplicate-resolution method");
  }
  return {
    duplicateResolution: {
      policy: "aggregate",
      method: duplicate.aggregateMethod
    }
  };
}
function weightPreparedCarrierMetadata(observation, preparedObservation, parameters) {
  const metadata = {
    ...observation.kind === "interpolated_trajectory" ? {
      reconstruction: {
        method: observation.method,
        interpolant: observation.interpolant,
        reconstructedBy: observation.reconstructedBy
      }
    } : {},
    ...weightDuplicateResolutionMetadata(preparedObservation, parameters)
  };
  return Object.keys(metadata).length === 0 ? null : structuredCell5(metadata);
}
function weightSourceOrdinalKey(sourceOrdinals) {
  return canonicalize(sourceOrdinals);
}
function bindWeightRenderRunOrdinals(runs) {
  return runs.map((run, renderRunOrdinal) => ({
    times: [...run.times],
    values: [...run.values],
    ...run.uncertaintyLower ? { uncertaintyLower: [...run.uncertaintyLower] } : {},
    ...run.uncertaintyUpper ? { uncertaintyUpper: [...run.uncertaintyUpper] } : {},
    ...run.authority ? {
      authority: run.authority.map((role) => role.tag === "data_carrier" ? {
        ...role,
        provenance: {
          ...rec(role.provenance) ?? {},
          renderRunOrdinal
        }
      } : { ...role })
    } : {}
  }));
}
function weightTraceOutputAuthority(series, runs, pathClassId, sampleClassId, uncertaintyClassId) {
  const base = traceOutputAuthority(
    series,
    pathClassId,
    sampleClassId,
    uncertaintyClassId
  );
  const runOrdinalByLineage = /* @__PURE__ */ new Map();
  for (let renderRunOrdinal = 0; renderRunOrdinal < runs.length; renderRunOrdinal++) {
    for (const role of runs[renderRunOrdinal].authority ?? []) {
      if (role.tag !== "data_carrier") continue;
      const sourceOrdinals = arr((rec(role.provenance) ?? {}).sourceOrdinals);
      if (!sourceOrdinals) continue;
      const key = weightSourceOrdinalKey(sourceOrdinals);
      const prior = runOrdinalByLineage.get(key);
      if (prior !== void 0 && prior !== renderRunOrdinal) {
        throw new Error("one weight observation lineage appears in multiple explicit render runs");
      }
      runOrdinalByLineage.set(key, renderRunOrdinal);
    }
  }
  return {
    ...base,
    observationProvenance: series.observations.map((observation) => {
      const renderRunOrdinal = runOrdinalByLineage.get(
        weightSourceOrdinalKey(observation.sourceOrdinals)
      );
      return {
        seriesId: series.id,
        sourceOrdinals: [...observation.sourceOrdinals],
        ...renderRunOrdinal === void 0 ? {} : { renderRunOrdinal }
      };
    })
  };
}
function weightPanelSeriesHasPaintableCarrier(entry, showObservationMarkers) {
  if (entry.renderRuns !== void 0) {
    if (entry.renderRuns.some((run) => run.values.some(Number.isFinite))) return true;
    if (!showObservationMarkers) return false;
    const markerIndexes = entry.markerObservationIndexes ?? entry.series.values.map((_value, index) => index);
    return markerIndexes.some((index) => {
      const value = entry.series.values[index];
      return typeof value === "number" && Number.isFinite(value);
    });
  }
  return entry.series.values.some((value) => typeof value === "number" && Number.isFinite(value));
}
function coalesceWeightIntervals(intervals) {
  const output = [];
  for (const interval of intervals) {
    const previous = output[output.length - 1];
    if (previous && interval.start <= previous.stop) {
      output[output.length - 1] = {
        start: previous.start,
        stop: Math.max(previous.stop, interval.stop)
      };
    } else {
      output.push({ start: interval.start, stop: interval.stop });
    }
  }
  return output;
}
function declaredInitialHold(member, observation, membershipLifetimes) {
  if (observation.kind !== "event_updated" || observation.updateSemantics !== "value_after_update" || member.initialValue === void 0) return void 0;
  const lifetime = membershipLifetimes?.find((candidate) => member.initialStateTime >= candidate.start && member.initialStateTime < candidate.stop);
  if (membershipLifetimes && !lifetime) return void 0;
  const stop = Math.min(
    member.statePrepared.observations[0]?.time ?? member.stateStop,
    member.recordedStop,
    lifetime?.stop ?? member.recordedStop
  );
  const start = Math.max(
    member.stateStart,
    member.recordedStart,
    lifetime?.start ?? member.recordedStart
  );
  return stop > start ? { start, stop, value: member.initialValue } : void 0;
}
function eventUpdatedMaterialization(member, updateSemantics, membershipLifetimes, includeRenderRuns = true) {
  const observations = member.statePrepared.observations;
  const runs = [];
  const witnesses = /* @__PURE__ */ new Map();
  const retainedOrdinals = new Set(
    member.prepared.observations.flatMap((observation) => observation.sourceOrdinals)
  );
  let paintedInitial = false;
  let segmentCount = 0;
  const emitSegment = (segment) => {
    const segmentIndex = segmentCount++;
    if (segment.observation && segment.observation.sourceOrdinals.some(
      (ordinal) => !retainedOrdinals.has(ordinal)
    )) {
      const role = segment.observation.time < member.recordedStart ? "carry_in" : "look_ahead";
      witnesses.set(
        `${role}:${segment.observation.sourceOrdinals.join(",")}`,
        { observation: segment.observation, role }
      );
    }
    if (segment.provenance?.segmentSource === "declared_initial_weight") paintedInitial = true;
    if (!includeRenderRuns || segment.value === null || !segment.provenance) return;
    const startAuthority = {
      tag: "data_carrier",
      classId: "series_paths",
      provenance: {
        ...segment.provenance,
        segmentIndex,
        endpoint: "start"
      }
    };
    const stopAuthority = {
      tag: "data_carrier",
      classId: "series_paths",
      provenance: {
        ...segment.provenance,
        segmentIndex,
        endpoint: "stop"
      }
    };
    const previous = runs[runs.length - 1];
    if (previous && previous.times[previous.times.length - 1] === segment.start) {
      previous.times.push(segment.start, segment.stop);
      previous.values.push(segment.value, segment.value);
      previous.uncertaintyLower.push(segment.lower, segment.lower);
      previous.uncertaintyUpper.push(segment.upper, segment.upper);
      previous.authority.push(startAuthority, stopAuthority);
    } else {
      runs.push({
        times: [segment.start, segment.stop],
        values: [segment.value, segment.value],
        uncertaintyLower: [segment.lower, segment.lower],
        uncertaintyUpper: [segment.upper, segment.upper],
        authority: [startAuthority, stopAuthority]
      });
    }
  };
  const clippedInitialHold = declaredInitialHold(member, {
    kind: "event_updated",
    updateSemantics
  }, membershipLifetimes);
  if (clippedInitialHold && clippedInitialHold.stop > clippedInitialHold.start) {
    emitSegment({
      start: clippedInitialHold.start,
      stop: clippedInitialHold.stop,
      value: clippedInitialHold.value,
      lower: null,
      upper: null,
      provenance: {
        seriesId: member.prepared.id,
        sourceOrdinals: [],
        atomKind: "hold_segment",
        segmentSource: "declared_initial_weight"
      }
    });
  }
  for (let index = 0; index < observations.length; index++) {
    const observation = observations[index];
    const sourceStart = updateSemantics === "value_after_update" ? observation.time : index === 0 ? member.stateStart : observations[index - 1].time;
    const sourceStop = updateSemantics === "value_after_update" ? observations[index + 1]?.time ?? member.stateStop : observation.time;
    const lifetime = membershipLifetimes?.find((candidate) => updateSemantics === "value_after_update" ? observation.time >= candidate.start && observation.time < candidate.stop : observation.time > candidate.start && observation.time <= candidate.stop);
    if (membershipLifetimes && !lifetime) continue;
    const start = Math.max(sourceStart, member.recordedStart, lifetime?.start ?? sourceStart);
    const stop = Math.min(sourceStop, member.recordedStop, lifetime?.stop ?? sourceStop);
    if (!(stop > start)) continue;
    emitSegment({
      start,
      stop,
      value: observation.value,
      lower: member.stateUncertaintyLower?.[index] ?? null,
      upper: member.stateUncertaintyUpper?.[index] ?? null,
      observation,
      provenance: {
        seriesId: member.statePrepared.id,
        sourceOrdinals: [...observation.sourceOrdinals],
        atomKind: "hold_segment"
      }
    });
  }
  if (includeRenderRuns && updateSemantics === "value_after_update" && member.recordedFinalEdgeInclusive) {
    const terminalIndex = observations.findIndex((observation) => observation.time === member.recordedStop && observation.value !== null);
    const terminalObservation = observations[terminalIndex];
    const terminalLifetime = membershipLifetimes?.find((lifetime) => terminalObservation !== void 0 && terminalObservation.time >= lifetime.start && terminalObservation.time < lifetime.stop);
    if (terminalObservation && (!membershipLifetimes || terminalLifetime)) {
      runs.push({
        times: [terminalObservation.time],
        values: [terminalObservation.value],
        uncertaintyLower: [member.stateUncertaintyLower?.[terminalIndex] ?? null],
        uncertaintyUpper: [member.stateUncertaintyUpper?.[terminalIndex] ?? null],
        authority: [{
          tag: "data_carrier",
          classId: "series_paths",
          provenance: {
            seriesId: member.statePrepared.id,
            sourceOrdinals: [...terminalObservation.sourceOrdinals],
            atomKind: "observation_vertex",
            terminalClosedStop: true
          }
        }]
      });
    }
  }
  return {
    runs,
    witnesses: [...witnesses.values()],
    initialStatePainted: paintedInitial
  };
}
function mergeWeightStateWitnesses(...groups) {
  const merged = /* @__PURE__ */ new Map();
  for (const group of groups) {
    for (const witness of group ?? []) {
      const key = `${witness.role}:${witness.observation.sourceOrdinals.join(",")}`;
      const prior = merged.get(key);
      merged.set(key, {
        observation: witness.observation,
        role: witness.role,
        ...prior?.consultedByDerivedAggregate === true || witness.consultedByDerivedAggregate === true ? { consultedByDerivedAggregate: true } : {}
      });
    }
  }
  return [...merged.values()].sort((left, right) => left.observation.time - right.observation.time || left.observation.sourceOrdinals[0] - right.observation.sourceOrdinals[0]);
}
function markerIndexesForWeightRuns(member, runs) {
  const lineage = /* @__PURE__ */ new Set();
  for (const run of runs) {
    for (const role of run.authority ?? []) {
      if (role.tag !== "data_carrier") continue;
      const provenance = rec(role.provenance);
      const ordinals = arr(provenance?.sourceOrdinals) ?? [];
      if (ordinals.length > 0) lineage.add(canonicalize(ordinals));
    }
  }
  return member.prepared.observations.flatMap((observation, index) => lineage.has(canonicalize(observation.sourceOrdinals)) ? [index] : []);
}
function unionWeightAvailability(selectedMembers, membershipById) {
  const pieces = selectedMembers.flatMap((member) => (membershipById.get(member.prepared.id) ?? []).flatMap((interval) => {
    const start = Math.max(interval.start, member.recordedStart);
    const stop = Math.min(interval.stop, member.recordedStop);
    return stop > start ? [{ start, stop }] : [];
  }));
  pieces.sort((left, right) => left.start - right.start || left.stop - right.stop);
  return coalesceWeightIntervals(pieces);
}
function weightAvailabilityTransitions(selectedMembers, membershipById) {
  const transitions = selectedMembers.flatMap((member) => memberWeightAvailability(member, membershipById.get(member.prepared.id) ?? []).flatMap((interval) => [interval.start, interval.stop]));
  transitions.sort((left, right) => left - right);
  return transitions.filter((time, index) => index === 0 || time !== transitions[index - 1]);
}
function memberWeightAvailability(member, membershipLifetimes) {
  return membershipLifetimes.flatMap((interval) => {
    const start = Math.max(interval.start, member.recordedStart);
    const stop = Math.min(interval.stop, member.recordedStop);
    return stop > start ? [{ start, stop }] : [];
  });
}
function availabilityConnects(availability, from, to) {
  if (!(to > from)) return true;
  return availability.some((interval) => interval.start <= from && interval.stop >= to);
}
function crossesWeightAvailabilityTransition(transitions, from, to) {
  let lower = 0;
  let upper = transitions.length;
  while (lower < upper) {
    const middle = lower + Math.floor((upper - lower) / 2);
    if (transitions[middle] <= from) lower = middle + 1;
    else upper = middle;
  }
  return lower < transitions.length && transitions[lower] < to;
}
function pointSampleAggregateRenderRuns(prepared, uncertaintyLower, uncertaintyUpper, availability, availabilityTransitions) {
  const runs = [];
  let current;
  for (let index = 0; index < prepared.observations.length; index++) {
    const observation = prepared.observations[index];
    if (observation.value === null) {
      current = void 0;
      continue;
    }
    const previousTime = current?.times[current.times.length - 1];
    if (current === void 0 || previousTime === void 0 || crossesWeightAvailabilityTransition(
      availabilityTransitions,
      previousTime,
      observation.time
    ) || !availabilityConnects(availability, previousTime, observation.time)) {
      current = {
        times: [],
        values: [],
        uncertaintyLower: [],
        uncertaintyUpper: [],
        authority: []
      };
      runs.push(current);
    }
    current.times.push(observation.time);
    current.values.push(observation.value);
    current.uncertaintyLower.push(uncertaintyLower?.[index] ?? null);
    current.uncertaintyUpper.push(uncertaintyUpper?.[index] ?? null);
    current.authority.push({
      tag: "data_carrier",
      classId: "series_paths",
      provenance: {
        seriesId: prepared.id,
        sourceOrdinals: [...observation.sourceOrdinals],
        atomKind: "observation_vertex"
      }
    });
  }
  return runs;
}
function pointSampleMembershipRenderRuns(member, membershipLifetimes, availabilityTransitions = []) {
  const availability = memberWeightAvailability(member, membershipLifetimes);
  const runs = [];
  const markerObservationIndexes = [];
  let current;
  for (let index = 0; index < member.prepared.observations.length; index++) {
    const observation = member.prepared.observations[index];
    const belongs = membershipLifetimes.some((interval) => observation.time >= interval.start && observation.time < interval.stop);
    const recorded = observation.time >= member.recordedStart && (observation.time < member.recordedStop || member.recordedFinalEdgeInclusive && observation.time === member.recordedStop);
    const available = belongs && recorded;
    if (!available || observation.value === null) {
      current = void 0;
      continue;
    }
    markerObservationIndexes.push(index);
    const previousTime = current?.times[current.times.length - 1];
    if (!current || previousTime === void 0 || crossesWeightAvailabilityTransition(
      availabilityTransitions,
      previousTime,
      observation.time
    ) || !availabilityConnects(availability, previousTime, observation.time)) {
      current = {
        times: [],
        values: [],
        uncertaintyLower: [],
        uncertaintyUpper: [],
        authority: []
      };
      runs.push(current);
    }
    current.times.push(observation.time);
    current.values.push(observation.value);
    current.uncertaintyLower.push(member.uncertaintyLower?.[index] ?? null);
    current.uncertaintyUpper.push(member.uncertaintyUpper?.[index] ?? null);
    current.authority.push({
      tag: "data_carrier",
      classId: "series_paths",
      provenance: {
        seriesId: member.prepared.id,
        sourceOrdinals: [...observation.sourceOrdinals],
        atomKind: "observation_vertex"
      }
    });
  }
  return { runs, markerObservationIndexes };
}
function interpolatedTrajectoryNeedsExcludedKnot(member) {
  const retained = new Set(
    member.prepared.observations.flatMap((observation) => observation.sourceOrdinals)
  );
  const observations = member.statePrepared.observations;
  for (let index = 1; index < observations.length; index++) {
    const left = observations[index - 1];
    const right = observations[index];
    if (left.value === null || right.value === null || !(right.time > left.time)) continue;
    const overlapStart = Math.max(left.time, member.recordedStart);
    const overlapStop = Math.min(right.time, member.recordedStop);
    if (overlapStop > overlapStart && (left.sourceOrdinals.some((ordinal) => !retained.has(ordinal)) || right.sourceOrdinals.some((ordinal) => !retained.has(ordinal)))) return true;
  }
  return false;
}
function interpolatedTrajectoryRenderRuns(member, atomKind = "reconstruction_vertex") {
  const runs = [];
  let current;
  for (let index = 0; index < member.prepared.observations.length; index++) {
    const observation = member.prepared.observations[index];
    if (observation.value === null) {
      current = void 0;
      continue;
    }
    if (!current) {
      current = {
        times: [],
        values: [],
        uncertaintyLower: [],
        uncertaintyUpper: [],
        authority: []
      };
      runs.push(current);
    }
    current.times.push(observation.time);
    current.values.push(observation.value);
    current.uncertaintyLower.push(member.uncertaintyLower?.[index] ?? null);
    current.uncertaintyUpper.push(member.uncertaintyUpper?.[index] ?? null);
    current.authority.push({
      tag: "data_carrier",
      classId: "series_paths",
      provenance: {
        seriesId: member.prepared.id,
        sourceOrdinals: [...observation.sourceOrdinals],
        atomKind
      }
    });
  }
  return runs;
}
function empiricalQuantile(values, probability) {
  return exactBinary64EmpiricalQuantileType7(values, probability);
}
function aggregateWeightValues(values, method) {
  if (values.length === 0) return null;
  if (method === "mean") return exactBinary64Mean(values);
  if (method === "median") return empiricalQuantile(values, 0.5);
  if (method === "min") return traceCompilerMinimum(values);
  if (method === "max") return traceCompilerMaximum(values);
  throw new Error(`unsupported synaptic-weight aggregate method ${method}`);
}
function prepareWeightAggregateGrid(data, parameters, window, members, maximumEvaluationTimes) {
  if (!Number.isSafeInteger(maximumEvaluationTimes) || maximumEvaluationTimes < 0 || maximumEvaluationTimes >= Number.MAX_SAFE_INTEGER) {
    throw new Error("weight aggregate-grid limit must be a non-negative safe integer below MAX_SAFE_INTEGER");
  }
  const membership = rec(data.membership) ?? {};
  const evaluation = rec(rec(parameters.aggregate)?.evaluation) ?? {};
  const membershipUnit = String(membership.unit);
  const membershipById = /* @__PURE__ */ new Map();
  const evaluationTimeSet = /* @__PURE__ */ new Set();
  const saturated = () => ({
    tag: "saturated_over_budget",
    saturatedEvaluationCount: maximumEvaluationTimes + 1
  });
  const addEvaluationTime = (time) => {
    if (time < window.start || time > window.stop || !window.finalEdgeInclusive && time === window.stop) return true;
    evaluationTimeSet.add(time);
    return evaluationTimeSet.size <= maximumEvaluationTimes;
  };
  let evaluationMode;
  switch (evaluation.mode) {
    case "hold_last_observed_at_declared_times":
      evaluationMode = "declared";
      break;
    case "shared_sample_grid":
      evaluationMode = "shared";
      break;
    case "hold_last_observed_at_union_times":
      evaluationMode = "union";
      break;
    default:
      throw new Error(
        `unsupported synaptic-weight aggregate evaluation mode ${String(evaluation.mode)}`
      );
  }
  if (evaluationMode === "union") {
    if (!addEvaluationTime(window.start)) {
      return saturated();
    }
    if (window.finalEdgeInclusive && !addEvaluationTime(window.stop)) {
      return saturated();
    }
  }
  const membershipMembers = arr(membership.members) ?? [];
  const membershipIds = new Set(
    membershipMembers.map((candidate) => String((rec(candidate) ?? {}).edgeId))
  );
  const selectedMembers = members.filter((candidate) => membershipIds.has(candidate.prepared.id));
  if (selectedMembers.length !== membershipIds.size) {
    const unresolved = [...membershipIds].filter((id) => !selectedMembers.some((member) => member.prepared.id === id));
    throw new Error(`aggregate membership references undeclared edge ${unresolved.join(", ")}`);
  }
  if (evaluationMode === "declared") {
    const times = rec(evaluation.times) ?? {};
    const unit = String(times.unit);
    for (const candidate of arr(times.values) ?? []) {
      if (typeof candidate !== "number") {
        throw new Error("declared synaptic-weight aggregate time must be a number");
      }
      const converted2 = unit === window.unit ? candidate : convert(candidate, unit, window.unit);
      if (!addEvaluationTime(converted2)) return saturated();
    }
  } else if (evaluationMode === "shared") {
    const sharedTimes = selectedMembers[0]?.prepared.times ?? [];
    for (const member of selectedMembers.slice(1)) {
      if (member.prepared.times.length !== sharedTimes.length || member.prepared.times.some((time, index) => time !== sharedTimes[index])) {
        throw new Error("shared_sample_grid requires exact elementwise-identical member times");
      }
    }
    for (const time of sharedTimes) {
      if (!addEvaluationTime(time)) return saturated();
    }
  }
  for (const candidate of membershipMembers) {
    const record6 = rec(candidate) ?? {};
    const convertedIntervals = [];
    for (const interval of arr(record6.intervals) ?? []) {
      const span = rec(interval) ?? {};
      const converted2 = {
        start: membershipUnit === window.unit ? num(span.start) : convert(num(span.start), membershipUnit, window.unit),
        stop: membershipUnit === window.unit ? num(span.stop) : convert(num(span.stop), membershipUnit, window.unit)
      };
      convertedIntervals.push(converted2);
      if (evaluationMode === "union" && (!addEvaluationTime(converted2.start) || !addEvaluationTime(converted2.stop))) {
        return saturated();
      }
    }
    membershipById.set(
      String(record6.edgeId),
      coalesceWeightIntervals(convertedIntervals)
    );
  }
  if (evaluationMode === "union") {
    for (const member of selectedMembers) {
      for (const time of member.prepared.times) {
        if (!addEvaluationTime(time)) return saturated();
      }
      if (!addEvaluationTime(member.recordedStart) || !addEvaluationTime(member.recordedStop)) {
        return saturated();
      }
    }
  }
  const evaluationTimes = [...evaluationTimeSet].sort((left, right) => left - right);
  return {
    tag: "exact_within_budget",
    grid: { membershipById, selectedMembers, evaluationTimes }
  };
}
function deriveWeightAggregate(data, parameters, window, preparedGrid) {
  const membership = rec(data.membership) ?? {};
  const aggregate4 = rec(parameters.aggregate) ?? {};
  const evaluation = rec(aggregate4.evaluation) ?? {};
  const observation = rec(data.observation) ?? {};
  const { membershipById, selectedMembers, evaluationTimes } = preparedGrid;
  const membershipUnit = String(membership.unit);
  const selectedIds = selectedMembers.map((member) => member.prepared.id);
  const states = selectedMembers.map((member) => {
    const lifetimes = membershipById.get(member.prepared.id);
    const stateTrace = observation.kind === "point_sample" ? member.prepared : member.statePrepared;
    const pointIndexByTime = /* @__PURE__ */ new Map();
    for (let index = 0; index < member.prepared.times.length; index++) {
      if (!pointIndexByTime.has(member.prepared.times[index])) {
        pointIndexByTime.set(member.prepared.times[index], index);
      }
    }
    return {
      member,
      stateTrace,
      lifetimes,
      lifetimeIndex: 0,
      before: 0,
      after: -1,
      pointIndexByTime,
      retainedSourceOrdinals: new Set(
        member.prepared.observations.flatMap((candidate) => candidate.sourceOrdinals)
      )
    };
  });
  const memberCounts = [];
  const contributingCounts = [];
  const aggregateValues = [];
  const dispersion = rec(aggregate4.dispersion) ?? { kind: "none", reason: "not_computed" };
  const dispersionValues = [];
  const lowerValues = [];
  const upperValues = [];
  const stateWitnessMaps = /* @__PURE__ */ new Map();
  const initialStateContributorIds = /* @__PURE__ */ new Set();
  for (const time of evaluationTimes) {
    let memberCount = 0;
    const finite = [];
    for (const state of states) {
      while (state.lifetimeIndex < state.lifetimes.length && time >= state.lifetimes[state.lifetimeIndex].stop) state.lifetimeIndex++;
      const lifetime = state.lifetimes[state.lifetimeIndex];
      const belongs = lifetime !== void 0 && time >= lifetime.start && time < lifetime.stop;
      if (belongs) memberCount++;
      const member = state.member;
      if (!lifetime || !belongs || time < member.recordedStart || time > member.recordedStop || !member.recordedFinalEdgeInclusive && time === member.recordedStop) continue;
      let candidate;
      let candidateObservation;
      let usedInitialState = false;
      if (observation.kind === "point_sample") {
        const index = state.pointIndexByTime.get(time);
        candidate = index === void 0 ? null : member.prepared.values[index];
      } else if (observation.updateSemantics === "value_before_update") {
        while (state.before < state.stateTrace.times.length && state.stateTrace.times[state.before] < time) state.before++;
        const candidateTime = state.stateTrace.times[state.before];
        candidateObservation = state.stateTrace.observations[state.before];
        candidate = state.before < state.stateTrace.values.length && candidateTime >= Math.max(lifetime.start, member.stateStart) && candidateTime <= lifetime.stop ? state.stateTrace.values[state.before] : null;
        if (!(state.before < state.stateTrace.values.length && candidateTime >= Math.max(lifetime.start, member.stateStart) && candidateTime <= lifetime.stop)) candidateObservation = void 0;
      } else {
        while (state.after + 1 < state.stateTrace.times.length && state.stateTrace.times[state.after + 1] <= time) state.after++;
        const candidateTime = state.stateTrace.times[state.after];
        if (state.after >= 0 && candidateTime >= Math.max(lifetime.start, member.stateStart)) {
          candidate = state.stateTrace.values[state.after];
          candidateObservation = state.stateTrace.observations[state.after];
        } else {
          const initialBelongsToLifetime = member.initialValue !== void 0 && member.initialStateTime >= lifetime.start && member.initialStateTime < lifetime.stop;
          candidate = initialBelongsToLifetime ? member.initialValue : null;
          usedInitialState = initialBelongsToLifetime;
        }
      }
      if (candidateObservation && candidateObservation.sourceOrdinals.some(
        (ordinal) => !state.retainedSourceOrdinals.has(ordinal)
      )) {
        const role = candidateObservation.time < member.recordedStart ? "carry_in" : "look_ahead";
        const byMember = stateWitnessMaps.get(member.prepared.id) ?? /* @__PURE__ */ new Map();
        byMember.set(
          `${role}:${candidateObservation.sourceOrdinals.join(",")}`,
          {
            observation: candidateObservation,
            role,
            consultedByDerivedAggregate: true
          }
        );
        stateWitnessMaps.set(member.prepared.id, byMember);
      }
      if (usedInitialState) initialStateContributorIds.add(member.prepared.id);
      if (typeof candidate === "number") finite.push(candidate);
    }
    memberCounts.push(memberCount);
    contributingCounts.push(finite.length);
    const aggregateValue = aggregateWeightValues(finite, String(aggregate4.method));
    aggregateValues.push(aggregateValue);
    if (dispersion.kind === "standard_deviation") {
      dispersionValues.push(
        finite.length < 2 || aggregateValue === null ? null : exactBinary64SampleStandardDeviation(finite)
      );
    } else if (dispersion.kind === "quantile_interval") {
      lowerValues.push(finite.length > 0 ? empiricalQuantile(finite, num(dispersion.lowerQuantile)) : null);
      upperValues.push(finite.length > 0 ? empiricalQuantile(finite, num(dispersion.upperQuantile)) : null);
    } else if (dispersion.kind === "ensemble_range") {
      lowerValues.push(finite.length > 0 ? traceCompilerMinimum(finite) : null);
      upperValues.push(finite.length > 0 ? traceCompilerMaximum(finite) : null);
    }
  }
  const valueUnit = selectedMembers[0]?.prepared.valueUnit ?? "nest:weight";
  let uncertainty;
  if (dispersion.kind === "standard_deviation") {
    uncertainty = {
      kind: "standard_deviation",
      unit: valueUnit,
      values: dispersionValues,
      sampleCount: dispersionValues.map((value, index) => value === null ? null : contributingCounts[index]),
      basis: "ensemble_members"
    };
  } else if (dispersion.kind === "quantile_interval") {
    uncertainty = {
      kind: "quantile_interval",
      unit: valueUnit,
      lower: lowerValues,
      upper: upperValues,
      lowerQuantile: dispersion.lowerQuantile,
      upperQuantile: dispersion.upperQuantile,
      method: "empirical_type_7_linear",
      sampleCount: contributingCounts.map((count) => count > 0 ? count : null),
      basis: "ensemble_members"
    };
  } else if (dispersion.kind === "ensemble_range") {
    uncertainty = {
      kind: "ensemble_range",
      unit: valueUnit,
      lower: lowerValues,
      upper: upperValues,
      sampleCount: contributingCounts.map((count) => count > 0 ? count : null),
      basis: "ensemble_members"
    };
  } else {
    uncertainty = { kind: "none", reason: String(dispersion.reason ?? "not_computed") };
  }
  const groupId = String(membership.groupId);
  const groupLabel = String(membership.groupLabel ?? groupId);
  const initialStateContributorIdList = selectedMembers.filter((member) => initialStateContributorIds.has(member.prepared.id)).map((member) => member.prepared.id);
  const aggregateObservationKind = observation.kind === "event_updated" ? "piecewise_constant" : "point_sample";
  const prepared = prepareTraceSeries({
    id: groupId,
    label: groupLabel,
    observationKind: aggregateObservationKind,
    times: evaluationTimes,
    timeUnit: window.unit,
    values: aggregateValues,
    valueUnit
  }, { window, duplicatePolicy: "reject", targetValueUnit: valueUnit });
  const uncertaintyArrays = traceUncertaintyArrays(uncertainty, prepared);
  const models = [...new Set(selectedMembers.map((member) => String(member.raw.synapseModel)))];
  const record6 = {
    groupId,
    groupLabel,
    synapseModels: models,
    weightComparabilityMode: String(rec(parameters.weightComparability)?.mode),
    method: aggregate4.method,
    time: { kind: "time", unit: window.unit, values: evaluationTimes },
    values: { kind: "synaptic_weight", unit: valueUnit, values: aggregateValues },
    memberCounts,
    contributingCounts,
    uncertainty
  };
  return {
    record: record6,
    prepared,
    ...uncertaintyArrays,
    memberCounts,
    contributingCounts,
    selectedMembers,
    membershipById,
    stateWitnessesByMember: new Map(
      [...stateWitnessMaps].map(([id, byKey]) => [id, [...byKey.values()]])
    ),
    initialStateContributorIds,
    operation: {
      id: "weight.aggregate.membership_bound",
      algorithm: "cortexel.weight_trace.aggregate_members",
      algorithmRevision: 2,
      parameters: {
        method: aggregate4.method,
        evaluation,
        dispersion,
        membershipUnit,
        observation,
        analysisWindow: window,
        weightComparability: rec(parameters.weightComparability),
        targetValueUnit: valueUnit
      },
      inputDigest: canonicalDigest({
        membership,
        members: selectedMembers.map((member) => member.raw),
        observation,
        analysisWindow: window,
        weightComparability: rec(parameters.weightComparability),
        targetValueUnit: valueUnit
      }),
      outputDigest: canonicalDigest({ evaluationTimes, aggregateValues, memberCounts, contributingCounts, uncertainty }),
      receipt: {
        selectedMemberIds: selectedIds,
        evaluationCount: evaluationTimes.length,
        meanDenominator: "contributing_count",
        zeroContributorsYieldNull: true,
        membershipTimeConversion: nonIdentityConversion(membershipUnit, window.unit),
        evaluationTimeConversion: evaluation.mode === "hold_last_observed_at_declared_times" ? nonIdentityConversion(
          typeof rec(evaluation.times)?.unit === "string" ? String(rec(evaluation.times)?.unit) : void 0,
          window.unit
        ) : null,
        quantileAlgorithm: dispersion.kind === "quantile_interval" ? "empirical_type_7_linear" : null,
        initialStateContributorIds: initialStateContributorIdList,
        output: {
          evaluationTimes,
          aggregateValues,
          memberCounts,
          contributingCounts,
          uncertainty
        }
      }
    }
  };
}
function phaseConvergenceFlags2(fixedPoints) {
  if (!fixedPoints) return [];
  const residualDx = numbers4(rec(fixedPoints.residualDxDt)?.values);
  const residualDy = numbers4(rec(fixedPoints.residualDyDt)?.values);
  const toleranceDx = numbers4(rec(fixedPoints.toleranceDxDt)?.values);
  const toleranceDy = numbers4(rec(fixedPoints.toleranceDyDt)?.values);
  const residualDxUnit = String(rec(fixedPoints.residualDxDt)?.unit);
  const residualDyUnit = String(rec(fixedPoints.residualDyDt)?.unit);
  const toleranceDxUnit = String(rec(fixedPoints.toleranceDxDt)?.unit);
  const toleranceDyUnit = String(rec(fixedPoints.toleranceDyDt)?.unit);
  const within = (residual, tolerance) => residual <= tolerance || residual - tolerance <= 1e-9 * Math.max(Math.abs(residual), Math.abs(tolerance), Number.MIN_VALUE);
  return strings4(fixedPoints.ids).map((_id, index) => within(
    Math.abs(convert(residualDx[index], residualDxUnit, toleranceDxUnit)),
    toleranceDx[index]
  ) && within(
    Math.abs(convert(residualDy[index], residualDyUnit, toleranceDyUnit)),
    toleranceDy[index]
  ));
}
function phasePlaneTableRows(data, parameters, convergenceFlags = phaseConvergenceFlags2(rec(data.fixedPoints))) {
  const axes = rec(data.axes) ?? {};
  const xAxis2 = rec(axes.x) ?? {};
  const yAxis2 = rec(axes.y) ?? {};
  const xUnit = String(xAxis2.unit);
  const yUnit = String(yAxis2.unit);
  const trajectories = rec(data.trajectories);
  const vectorField = rec(data.vectorField);
  const nullclines = rec(data.nullclines);
  const fixedPoints = rec(data.fixedPoints);
  const fieldDomain = rec(vectorField?.domain);
  const convertedCarrier2 = (carrier2, targetUnit) => {
    const sourceUnit = String(carrier2?.unit);
    return (arr(carrier2?.values) ?? []).map((value) => value === null ? null : sourceUnit === targetUnit ? value : convert(value, sourceUnit, targetUnit));
  };
  const convertedFinite = (carrier2, targetUnit) => convertedCarrier2(carrier2, targetUnit).filter(
    (value) => value !== null
  );
  const allX = [
    ...convertedFinite(rec(trajectories?.x), xUnit),
    ...convertedFinite(rec(vectorField?.x), xUnit),
    ...convertedFinite(rec(nullclines?.x), xUnit),
    ...convertedFinite(rec(fixedPoints?.x), xUnit),
    ...num(rec(fieldDomain?.x)?.start) !== void 0 ? [convert(num(rec(fieldDomain?.x)?.start), String(rec(fieldDomain?.x)?.unit), xUnit)] : [],
    ...num(rec(fieldDomain?.x)?.stop) !== void 0 ? [convert(num(rec(fieldDomain?.x)?.stop), String(rec(fieldDomain?.x)?.unit), xUnit)] : []
  ];
  const allY = [
    ...convertedFinite(rec(trajectories?.y), yUnit),
    ...convertedFinite(rec(vectorField?.y), yUnit),
    ...convertedFinite(rec(nullclines?.y), yUnit),
    ...convertedFinite(rec(fixedPoints?.y), yUnit),
    ...num(rec(fieldDomain?.y)?.start) !== void 0 ? [convert(num(rec(fieldDomain?.y)?.start), String(rec(fieldDomain?.y)?.unit), yUnit)] : [],
    ...num(rec(fieldDomain?.y)?.stop) !== void 0 ? [convert(num(rec(fieldDomain?.y)?.stop), String(rec(fieldDomain?.y)?.unit), yUnit)] : []
  ];
  const xExtent = finiteExtent(allX);
  const yExtent = finiteExtent(allY);
  const magnitudeBasis = String(parameters.magnitudeBasis ?? "axis_normalized");
  const speed = (dx, dy, dxUnit, dyUnit) => {
    if (dx === null || dy === null || dxUnit === null || dyUnit === null) return null;
    if (magnitudeBasis === "physical") {
      if (dimensionOf(xUnit) !== dimensionOf(yUnit)) return null;
      const canonicalStateUnit = canonicalUnitFor(xUnit);
      const canonicalTimeUnit = canonicalUnitFor(dxUnit);
      if (!canonicalStateUnit || !canonicalTimeUnit) return null;
      const canonicalDx = dx * conversionFactor(xUnit, canonicalStateUnit) * conversionFactor(dxUnit, canonicalTimeUnit);
      const canonicalDy = dy * conversionFactor(yUnit, canonicalStateUnit) * conversionFactor(dyUnit, canonicalTimeUnit);
      return Math.hypot(canonicalDx, canonicalDy);
    }
    if (!xExtent || !yExtent || !(xExtent.max > xExtent.min) || !(yExtent.max > yExtent.min)) {
      return null;
    }
    return Math.hypot(
      exactBinary64RatioToDifference(dx, xExtent.min, xExtent.max),
      exactBinary64RatioToDifference(
        convert(dy, dyUnit, dxUnit),
        yExtent.min,
        yExtent.max
      )
    );
  };
  const derivativeUnitCell = (dxUnit, dyUnit) => dxUnit === null && dyUnit === null ? null : structuredCell5({
    x: dxUnit === null ? null : `${xUnit} ${dxUnit}`,
    y: dyUnit === null ? null : `${yUnit} ${dyUnit}`,
    magnitudeBasis,
    ...magnitudeBasis === "physical" ? { magnitudeUnit: `${canonicalUnitFor(xUnit) ?? xUnit} ${canonicalUnitFor(dxUnit ?? "") ?? dxUnit}` } : { magnitudeUnit: dxUnit }
  });
  const rows = [];
  if (trajectories) {
    const pointIds = strings4(trajectories.pointTrajectoryIds);
    const declaredIds = strings4(rec(trajectories.universe)?.ids);
    const times = numbers4(rec(trajectories.times)?.values);
    const xs = convertedCarrier2(rec(trajectories.x), xUnit);
    const ys = convertedCarrier2(rec(trajectories.y), yUnit);
    const dxs = arr(rec(trajectories.dxdt)?.values) ?? [];
    const dys = arr(rec(trajectories.dydt)?.values) ?? [];
    const dxUnit = typeof rec(trajectories.dxdt)?.unit === "string" ? String(rec(trajectories.dxdt)?.unit) : null;
    const dyUnit = typeof rec(trajectories.dydt)?.unit === "string" ? String(rec(trajectories.dydt)?.unit) : null;
    for (let index = 0; index < pointIds.length; index++) {
      const x = xs[index] ?? null;
      const y = ys[index] ?? null;
      const dx = typeof dxs[index] === "number" ? dxs[index] : null;
      const dy = typeof dys[index] === "number" ? dys[index] : null;
      rows.push([
        "trajectory_point",
        pointIds[index],
        index,
        times[index],
        x,
        y,
        dx,
        dy,
        derivativeUnitCell(dxUnit, dyUnit),
        speed(dx, dy, dxUnit, dyUnit),
        null,
        null,
        null
      ]);
    }
    const represented = new Set(pointIds);
    for (const id of declaredIds) {
      if (!represented.has(id)) {
        rows.push(["trajectory_point", id, null, null, null, null, null, null, null, null, null, null, null]);
      }
    }
  }
  if (vectorField) {
    const xs = convertedFinite(rec(vectorField.x), xUnit);
    const ys = convertedFinite(rec(vectorField.y), yUnit);
    const dxs = numbers4(rec(vectorField.dx)?.values);
    const dys = numbers4(rec(vectorField.dy)?.values);
    const dxUnit = String(rec(vectorField.dx)?.unit);
    const dyUnit = String(rec(vectorField.dy)?.unit);
    for (let index = 0; index < xs.length; index++) {
      rows.push([
        "field_sample",
        null,
        index,
        null,
        xs[index],
        ys[index],
        dxs[index],
        dys[index],
        derivativeUnitCell(dxUnit, dyUnit),
        speed(dxs[index], dys[index], dxUnit, dyUnit),
        null,
        null,
        null
      ]);
    }
  }
  if (nullclines) {
    const pointIds = strings4(nullclines.pointCurveIds);
    const xs = convertedCarrier2(rec(nullclines.x), xUnit);
    const ys = convertedCarrier2(rec(nullclines.y), yUnit);
    const curveIds = strings4(nullclines.curveIds);
    const methods = arr(nullclines.methods) ?? [];
    const methodById = new Map(curveIds.map((id, index) => [id, rec(methods[index]) ?? {}]));
    for (let index = 0; index < pointIds.length; index++) {
      const method = methodById.get(pointIds[index]) ?? {};
      rows.push([
        "nullcline_point",
        pointIds[index],
        index,
        null,
        xs[index] ?? null,
        ys[index] ?? null,
        null,
        null,
        null,
        null,
        String(method.kind),
        structuredCell5(method.residualTolerance),
        null
      ]);
    }
  }
  if (fixedPoints) {
    const ids = strings4(fixedPoints.ids);
    const xs = convertedFinite(rec(fixedPoints.x), xUnit);
    const ys = convertedFinite(rec(fixedPoints.y), yUnit);
    const methods = strings4(fixedPoints.methods);
    const residualDx = numbers4(rec(fixedPoints.residualDxDt)?.values);
    const residualDy = numbers4(rec(fixedPoints.residualDyDt)?.values);
    const toleranceDx = numbers4(rec(fixedPoints.toleranceDxDt)?.values);
    const toleranceDy = numbers4(rec(fixedPoints.toleranceDyDt)?.values);
    const residualDxUnit = String(rec(fixedPoints.residualDxDt)?.unit);
    const residualDyUnit = String(rec(fixedPoints.residualDyDt)?.unit);
    const toleranceDxUnit = String(rec(fixedPoints.toleranceDxDt)?.unit);
    const toleranceDyUnit = String(rec(fixedPoints.toleranceDyDt)?.unit);
    for (let index = 0; index < ids.length; index++) {
      const converged = convergenceFlags[index];
      rows.push([
        "fixed_point",
        ids[index],
        index,
        null,
        xs[index],
        ys[index],
        null,
        null,
        null,
        null,
        methods[index],
        structuredCell5({
          dxdt: { value: residualDx[index], unit: residualDxUnit },
          dydt: { value: residualDy[index], unit: residualDyUnit },
          toleranceDxDt: { value: toleranceDx[index], unit: toleranceDxUnit },
          toleranceDyDt: { value: toleranceDy[index], unit: toleranceDyUnit }
        }),
        booleanCell3(converged)
      ]);
    }
  }
  return rows;
}
function compile(validated, context, pairwiseOperations, returnedTableRows) {
  const request = validated.canonicalRequest;
  const data = rec(request.data) ?? {};
  const parameters = rec(request.parameters) ?? {};
  const skillId = validated.skillId;
  const rendererId = SKILL_CATALOG[skillId].renderer.id;
  const done = (plan, derivationOperations = [], derivedDisclosureFacts = {}) => ({ plan, rendererId, derivationOperations, derivedDisclosureFacts });
  const fail = (code, stage, message, instancePath = "") => ({
    errors: [makeError({ code, stage, message, instancePath, skillId })]
  });
  const failDistribution = (error) => {
    const stage = error.code.startsWith("RESOURCE_") ? "budget" : error.code.startsWith("RENDER_") ? "render" : error.code.startsWith("SCOPE_") ? "scope" : error.code.startsWith("SCIENCE_") ? "science" : "semantic";
    const instancePath = error.path.length === 0 ? "/data" : `/${error.path.map((part) => String(part).replaceAll("~", "~0").replaceAll("/", "~1")).join("/")}`;
    return fail(error.code, stage, error.message, instancePath);
  };
  if (skillId === "neuro.population_rate") {
    const mode = data.mode;
    if (mode === "events") {
      if (parameters.rateMode !== "binned_count") {
        return fail(
          "RENDER_UNSUPPORTED_SKILL",
          "render",
          `population rate mode ${String(parameters.rateMode)} has no contract-faithful compiler in this build. Cortexel will not substitute a binned count.`,
          "/parameters/rateMode"
        );
      }
      try {
        const timeRecord = rec(data.eventTimes) ?? {};
        const sourceUnit = timeRecord.unit ?? "ms";
        const resolvedBins = declaredBins(rec(parameters.bins));
        if (!resolvedBins) return { pending: rendererId };
        const times = numbers4(timeRecord.values);
        const recorded = strings4(data.recordedSenderIds);
        const normalization = parameters.normalization ?? "total_event_rate";
        const result = derivePopulationRateCounts({
          eventTimes: times,
          eventUnit: sourceUnit,
          bins: {
            edges: resolvedBins.spec.edges,
            unit: resolvedBins.unit,
            finalEdgeInclusive: resolvedBins.spec.finalEdgeInclusive
          },
          recordedSenderCount: recorded.length,
          normalization
        });
        const eventTimeConversion = sourceUnit !== resolvedBins.unit ? conversionReceipt(sourceUnit, resolvedBins.unit) : void 0;
        const binWidthConversion = resolvedBins.unit !== "s" ? conversionReceipt(resolvedBins.unit, "s") : void 0;
        const binWidths2 = result.counts.map((_, index) => sameUnitDifference(
          resolvedBins.spec.edges[index],
          resolvedBins.spec.edges[index + 1],
          resolvedBins.unit
        ));
        const operation = derivationOperation(
          "population_rate.binned",
          "cortexel.population_rate.binned_count",
          { bins: resolvedBins, normalization },
          data,
          {
            counts: result.counts,
            binWidths: binWidths2,
            binWidthUnit: resolvedBins.unit,
            ratesHz: result.ratesHz
          },
          {
            sourceEventCount: result.sourceEventCount,
            recordedSenderCount: result.recordedSenderCount,
            ...eventTimeConversion ? { eventTimeConversion } : {},
            ...binWidthConversion ? { binWidthConversion } : {}
          }
        );
        const unitConversions = [
          ...eventTimeConversion ? [
            conversionDisclosureText("population-rate event times", eventTimeConversion)
          ] : [],
          ...binWidthConversion ? [
            conversionDisclosureText("population-rate bin widths", binWidthConversion)
          ] : []
        ];
        const derivedFacts = unitConversions.length > 0 ? { unitConversions } : {};
        const tableRows = result.counts.map((count, index) => [
          resolvedBins.spec.edges[index],
          resolvedBins.spec.edges[index + 1],
          binWidths2[index],
          count,
          result.recordedSenderCount,
          result.ratesHz[index],
          "Hz"
        ]);
        const rateExtent = distributionCompilerExtrema(result.ratesHz);
        if (rateExtent === null) throw new Error("population-rate bins unexpectedly empty");
        const populationWindow = rec(data.window) ?? {};
        const compileContext = distributionCompilerContext(
          context(tableRows.length, derivedFacts),
          skillId,
          {
            populationLabel: String(parameters.populationLabel ?? parameters.populationId),
            windowStart: distributionCompilerNumber(num(populationWindow.start)),
            windowStop: distributionCompilerNumber(num(populationWindow.stop)),
            timeUnit: String(populationWindow.unit),
            binCount: distributionCompilerNumber(result.counts.length),
            eventCount: distributionCompilerNumber(result.sourceEventCount),
            recordedSenderCount: distributionCompilerNumber(result.recordedSenderCount),
            normalization,
            rateMin: distributionCompilerNumber(rateExtent.min),
            rateMax: distributionCompilerNumber(rateExtent.max)
          }
        );
        return done(
          withContractTable(compileStepFigure(
            compileContext,
            resolvedBins.spec.edges.slice(0, -1),
            resolvedBins.spec.edges.slice(1),
            result.ratesHz,
            `time (${unitLabel(resolvedBins.unit)})`,
            "rate (Hz)",
            skillId,
            "bins"
          ), compileContext, skillId, tableRows),
          [operation],
          derivedFacts
        );
      } catch (error) {
        if (error instanceof DistributionDerivationError) return failDistribution(error);
        const message = error instanceof Error ? error.message : "population-rate derivation failed";
        return fail(
          "SCIENCE_NUMERIC_RESOLUTION_UNREPRESENTABLE",
          "science",
          message,
          "/data"
        );
      }
    }
    try {
      const edgeRecord = rec(data.binEdges) ?? {};
      const edges = numbers4(edgeRecord.edges);
      const timeUnit = edgeRecord.unit ?? "ms";
      const counts = numbers4(data.counts);
      const recordedSenderIds = strings4(data.recordedSenderIds);
      const declaredRecordedSenderCount = num(data.recordedSenderCount);
      const recordedSenderCount = recordedSenderIds.length;
      if (declaredRecordedSenderCount !== recordedSenderCount) {
        return fail(
          "SCIENCE_NORMALIZATION_UNVERIFIABLE",
          "science",
          `recordedSenderCount ${String(declaredRecordedSenderCount)} does not equal the complete recordedSenderIds cardinality ${recordedSenderCount}.`,
          "/data/recordedSenderCount"
        );
      }
      const sourceEventCount = num(data.sourceEventCount);
      const exactCountTotal2 = counts.reduce((total, count) => total + BigInt(count), 0n);
      if (sourceEventCount === void 0 || exactCountTotal2 !== BigInt(sourceEventCount)) {
        return fail(
          "SCIENCE_NORMALIZATION_UNVERIFIABLE",
          "science",
          `sum(counts) is ${exactCountTotal2}, not sourceEventCount ${String(sourceEventCount)}.`,
          "/data/sourceEventCount"
        );
      }
      const normalization = parameters.normalization ?? "total_event_rate";
      const integerFactor = normalization === "mean_rate_per_recorded_sender" ? recordedSenderCount : 1;
      const binWidthConversion = timeUnit !== "s" ? conversionReceipt(timeUnit, "s") : void 0;
      const suppliedRates = rec(data.rates);
      const suppliedRateUnit = typeof suppliedRates?.unit === "string" ? suppliedRates.unit : void 0;
      const suppliedRateConversion = suppliedRateUnit && suppliedRateUnit !== "Hz" ? conversionReceipt(suppliedRateUnit, "Hz") : void 0;
      const binWidths2 = counts.map((_, index) => convertDifference(edges[index], edges[index + 1], timeUnit, timeUnit));
      const ratesHz = counts.map((count, index) => {
        return divideExactIntegerByConvertedDifference(
          count,
          integerFactor,
          edges[index],
          edges[index + 1],
          timeUnit,
          "s"
        );
      });
      const operation = derivationOperation(
        "population_rate.prebinned_verify",
        "cortexel.population_rate.rederive_prebinned",
        { normalization, timeUnit },
        data,
        { counts, binWidths: binWidths2, binWidthUnit: timeUnit, ratesHz },
        {
          recordedSenderCount,
          sourceEventCount,
          suppliedRatesReverified: suppliedRates !== void 0,
          ...binWidthConversion ? { binWidthConversion } : {},
          ...suppliedRateConversion ? { suppliedRateConversion } : {}
        }
      );
      const unitConversions = [
        ...binWidthConversion ? [conversionDisclosureText("population-rate bin widths", binWidthConversion)] : [],
        ...suppliedRateConversion ? [conversionDisclosureText("supplied population rates", suppliedRateConversion)] : []
      ];
      const derivedFacts = {
        preBinned: true,
        ...unitConversions.length > 0 ? { unitConversions } : {}
      };
      const tableRows = ratesHz.map((rate, index) => [
        edges[index],
        edges[index + 1],
        binWidths2[index],
        counts[index],
        recordedSenderCount,
        rate,
        "Hz"
      ]);
      const rateExtent = distributionCompilerExtrema(ratesHz);
      if (rateExtent === null) throw new Error("population-rate bins unexpectedly empty");
      const populationWindow = rec(data.window) ?? {};
      const compileContext = distributionCompilerContext(
        context(tableRows.length, derivedFacts),
        skillId,
        {
          populationLabel: String(parameters.populationLabel ?? parameters.populationId),
          windowStart: distributionCompilerNumber(num(populationWindow.start)),
          windowStop: distributionCompilerNumber(num(populationWindow.stop)),
          timeUnit: String(populationWindow.unit),
          binCount: distributionCompilerNumber(counts.length),
          eventCount: distributionCompilerNumber(sourceEventCount),
          recordedSenderCount: distributionCompilerNumber(recordedSenderCount),
          normalization,
          rateMin: distributionCompilerNumber(rateExtent.min),
          rateMax: distributionCompilerNumber(rateExtent.max)
        }
      );
      return done(
        withContractTable(compileStepFigure(
          compileContext,
          edges.slice(0, -1),
          edges.slice(1),
          ratesHz,
          `time (${unitLabel(timeUnit)})`,
          "rate (Hz)",
          skillId,
          "bins"
        ), compileContext, skillId, tableRows),
        [operation],
        derivedFacts
      );
    } catch (error) {
      return fail(
        "SCIENCE_NUMERIC_RESOLUTION_UNREPRESENTABLE",
        "science",
        error instanceof Error ? error.message : "prebinned population-rate derivation failed",
        "/data"
      );
    }
  }
  if (skillId === "neuro.analog_trace") {
    const window = traceWindowFrom(rec(data.window));
    const rawSeries = arr(data.series) ?? [];
    const seriesIds = strings4(data.seriesIds);
    if (!window || rawSeries.length === 0) return { pending: rendererId };
    const inputs = rawSeries.map((value, index) => {
      const entry = rec(value) ?? {};
      const time = rec(entry.time) ?? {};
      const values = rec(entry.values) ?? {};
      return {
        id: seriesIds[index] ?? `series-${index + 1}`,
        label: typeof entry.label === "string" ? entry.label : seriesIds[index] ?? `Series ${index + 1}`,
        observationKind: entry.observationKind ?? "point_sample",
        times: numbers4(time.values),
        timeUnit: time.unit ?? window.unit,
        values: traceValues(values),
        valueUnit: values.unit ?? "1"
      };
    });
    const analogUncertainty = rec(parameters.uncertainty);
    if (analogUncertainty?.kind !== void 0 && analogUncertainty.kind !== "none" && inputs.length !== 1) {
      return fail(
        "SCIENCE_UNCERTAINTY_UNSUPPORTED_FOR_SKILL",
        "science",
        "registry 1.0 analog uncertainty is figure-level and therefore unambiguous only for exactly one series. Use separate figures or declare uncertainty:none for a multi-series trace.",
        "/parameters/uncertainty"
      );
    }
    if (inputs.length === 1) {
      const uncertaintyError = traceUncertaintyValidationError(
        analogUncertainty,
        inputs[0].valueUnit,
        inputs[0].values,
        skillId,
        "/parameters/uncertainty"
      );
      if (uncertaintyError) return { errors: [uncertaintyError] };
      const mismatch = traceUncertaintyLengthMismatch(analogUncertainty, inputs[0].values.length);
      if (mismatch) {
        return fail(
          "SEMANTIC_LENGTH_MISMATCH",
          "semantic",
          `analog uncertainty.${mismatch.key} has ${mismatch.observed} entries for ${inputs[0].values.length} samples.`,
          `/parameters/uncertainty/${mismatch.key}`
        );
      }
    }
    const layout = parameters.layout;
    if (layout === "shared_axis" && inputs.length > CATEGORICAL_SERIES_STYLES.length) {
      return fail(
        "RENDER_SERIES_LIMIT_EXCEEDED",
        "render",
        `${inputs.length} series were assigned to one shared axis, but the registered palette has only ${CATEGORICAL_SERIES_STYLES.length} stable distinguishable style tuples.`,
        "/data/series"
      );
    }
    const panels = [];
    if (layout === "small_multiples") {
      const groupBy = parameters.groupBy ?? "series";
      const groups = /* @__PURE__ */ new Map();
      for (let index = 0; index < inputs.length; index++) {
        const key = groupBy === "quantity_kind" ? String(rec(rawSeries[index]) && rec(rec(rawSeries[index]).values)?.kind) : inputs[index].id;
        const members = groups.get(key) ?? [];
        members.push(index);
        groups.set(key, members);
      }
      for (const [key, indexes] of groups) {
        if (indexes.length > CATEGORICAL_SERIES_STYLES.length) {
          return fail(
            "RENDER_SERIES_LIMIT_EXCEEDED",
            "render",
            `panel ${key} would overlay ${indexes.length} series, above the ${CATEGORICAL_SERIES_STYLES.length}-style distinguishability limit.`,
            "/data/series"
          );
        }
        if (groupBy === "quantity_kind") {
          const dimensions = new Set(indexes.map((index) => dimensionOf(inputs[index].valueUnit)));
          const incompatible = indexes.length > 1 && indexes.slice(1).some((index) => !axesAreCompatible(inputs[indexes[0]].valueUnit, inputs[index].valueUnit));
          if (dimensions.size > 1 || incompatible) {
            return fail(
              "SCIENCE_UNIT_DIMENSION_MISMATCH",
              "science",
              `quantity-kind group ${key} cannot share one numeric axis. Use groupBy=series; Cortexel will not force dimensionally incompatible or simulator-defined state variables into a comparison.`,
              "/parameters/groupBy"
            );
          }
        }
        const targetUnit = inputs[indexes[0]].valueUnit;
        const prepared = [];
        for (const index of indexes) {
          try {
            const series = prepareTrace(inputs[index], window, parameters, targetUnit);
            prepared.push({
              series,
              styleIndex: index,
              tableMetadata: { sourceIndex: index },
              outputAuthority: traceOutputAuthority(series, "series_paths", "samples", "uncertainty"),
              ...traceUncertaintyArrays(analogUncertainty, series)
            });
          } catch (error) {
            return { errors: [tracePreparationError(error, skillId, `/data/series/${index}`)] };
          }
        }
        panels.push({
          id: `panel-${panels.length + 1}`,
          label: groupBy === "series" ? prepared[0].series.label : key,
          yLabel: `value (${unitLabel(targetUnit)})`,
          series: prepared
        });
      }
    } else {
      const targetUnit = parameters.valueUnit ?? inputs[0].valueUnit;
      const prepared = [];
      for (let index = 0; index < inputs.length; index++) {
        try {
          const series = prepareTrace(inputs[index], window, parameters, targetUnit);
          prepared.push({
            series,
            styleIndex: index,
            tableMetadata: { sourceIndex: index },
            outputAuthority: traceOutputAuthority(series, "series_paths", "samples", "uncertainty"),
            ...traceUncertaintyArrays(analogUncertainty, series)
          });
        } catch (error) {
          return { errors: [tracePreparationError(error, skillId, `/data/series/${index}`)] };
        }
      }
      panels.push({
        id: "main",
        label: "Shared value axis",
        yLabel: `value (${unitLabel(targetUnit)})`,
        series: prepared
      });
    }
    const allEntries = panels.flatMap((panel) => panel.series);
    const allPrepared = allEntries.map((entry) => entry.series);
    const empty = allPrepared.find((prepared) => prepared.outputCount === 0);
    if (empty) {
      return fail(
        "RENDER_NO_DATA",
        "render",
        `series ${empty.id} has no observation inside the declared display window. Cortexel refuses an empty trace because it is visually indistinguishable from a measured flat or silent signal.`,
        "/data/window"
      );
    }
    const rowsTotal = allPrepared.reduce(
      (total, entry) => total + entry.outputCount,
      0
    );
    const duplicate = traceDuplicateOptions(parameters);
    const uncertaintyDeclared = analogUncertainty?.kind !== void 0 && analogUncertainty.kind !== "none";
    const derivedFacts = {
      ...traceDerivedFacts(
        allPrepared,
        duplicate.aggregateMethod,
        allPrepared.map(() => analogUncertainty)
      ),
      uncertaintySeriesDeclared: uncertaintyDeclared ? 1 : 0,
      uncertaintySeriesShown: allEntries.filter((entry) => traceSeriesHasCompleteDrawableUncertainty(entry)).length,
      uncertaintySeriesTotal: allEntries.length
    };
    const analogSummaryFacts = {
      seriesCount: String(allPrepared.length),
      windowStart: traceCompilerSummaryNumber(window.start),
      windowStop: traceCompilerSummaryNumber(window.stop),
      timeUnit: window.unit,
      layoutMode: layout,
      quantitySummary: traceCompilerJoinUnique(allEntries.map((entry) => {
        const sourceIndex = Number(entry.tableMetadata?.sourceIndex ?? 0);
        const raw = rec(rawSeries[sourceIndex]) ?? {};
        return `${String(rec(raw.values)?.kind ?? "unknown")} (${entry.series.valueUnit})`;
      })),
      sampleCount: String(rowsTotal),
      missingCount: String(allPrepared.reduce(
        (sum, trace) => sum + trace.observations.filter((observation) => observation.value === null).length,
        0
      )),
      excludedCount: String(allPrepared.reduce(
        (sum, trace) => sum + trace.excludedBelow + trace.excludedAbove,
        0
      )),
      duplicateTimePolicy: traceCompilerDuplicateStatement(parameters, allPrepared),
      unitConversionStatement: traceCompilerUnitConversionStatement(allPrepared),
      uncertaintyStatement: traceCompilerUncertaintyStatement(
        allPrepared.map(() => analogUncertainty)
      ),
      compactionStatement: traceCompilerNoCompactionStatement()
    };
    const analogCompileContext = traceCompilerContext(
      context(rowsTotal, derivedFacts),
      skillId,
      analogSummaryFacts
    );
    const operations = allEntries.map((entry) => {
      const sourceIndex = Number(entry.tableMetadata?.sourceIndex ?? 0);
      const uncertaintySourceUnit = typeof analogUncertainty?.unit === "string" ? analogUncertainty.unit : entry.series.valueUnit;
      const uncertaintyTargetUnit = entry.series.normalization ? entry.series.sourceValueUnit : entry.series.valueUnit;
      return traceOperation({ series: rawSeries[sourceIndex], uncertainty: analogUncertainty }, entry.series, window, parameters, {
        lower: entry.uncertaintyLower,
        upper: entry.uncertaintyUpper,
        ...analogUncertainty?.kind && analogUncertainty.kind !== "none" && uncertaintySourceUnit !== uncertaintyTargetUnit ? { conversion: conversionReceipt(uncertaintySourceUnit, uncertaintyTargetUnit) } : {}
      });
    });
    return done(compileTraceFigure(
      analogCompileContext,
      panels,
      {
        xLabel: `time (${unitLabel(window.unit)})`,
        ...layout !== "small_multiples" || parameters.sharedTimeAxis !== false ? { xDomain: [window.start, window.stop] } : {},
        sharedXAxis: layout !== "small_multiples" || parameters.sharedTimeAxis !== false,
        showSamplePoints: parameters.showSamplePoints === true,
        summaryStatements: [
          `Display window [${formatNumber(window.start)}, ${formatNumber(window.stop)}${window.finalEdgeInclusive ? "]" : ")"} ${unitLabel(window.unit)}.`,
          `Layout ${layout}; ${layout === "small_multiples" && parameters.sharedTimeAxis === false ? "panel time domains are independent" : "all panels share the declared time domain"}.`,
          ...layout === "small_multiples" ? ["Panel y domains are independent; curve heights are not comparable across panels."] : []
        ],
        tableColumns: [
          { key: "seriesId", header: "Series" },
          { key: "seriesLabel", header: "Label" },
          { key: "quantityKind", header: "Quantity" },
          { key: "observationKind", header: "Observation" },
          { key: "origin", header: "Origin" },
          { key: "time", header: "Time" },
          { key: "timeUnit", header: "Time unit" },
          { key: "value", header: "Value" },
          { key: "valueUnit", header: "Unit" },
          { key: "missing", header: "Missing" },
          { key: "replicateCount", header: "Replicates" },
          { key: "uncertaintyLower", header: "Uncertainty lower" },
          { key: "uncertaintyUpper", header: "Uncertainty upper" },
          { key: "uncertaintyMethod", header: "Uncertainty declaration" },
          { key: "sourceOrdinal", header: "Source row" }
        ],
        tableRow: (entry, observationIndex) => {
          const sourceIndex = Number(entry.tableMetadata?.sourceIndex ?? 0);
          const raw = rec(rawSeries[sourceIndex]) ?? {};
          const origin = rec(raw.origin) ?? {};
          const observation = entry.series.observations[observationIndex];
          const sourceOrdinal = observation.sourceOrdinals.length === 1 ? observation.sourceOrdinals[0] : void 0;
          const [uncertaintyLower, uncertaintyUpper, uncertaintyMethod] = traceUncertaintyCells(
            rec(parameters.uncertainty),
            sourceOrdinal,
            entry.series,
            observation.value
          );
          return [
            entry.series.id,
            entry.series.label,
            String(rec(raw.values)?.kind ?? "unknown"),
            entry.series.observationKind,
            origin.kind === "derived" ? `derived: ${String(origin.method ?? "unspecified")}` : String(origin.kind ?? "unknown"),
            observation.time,
            entry.series.timeUnit,
            observation.value,
            entry.series.valueUnit,
            observation.value === null ? "true" : "false",
            observation.replicateCount,
            uncertaintyLower,
            uncertaintyUpper,
            uncertaintyMethod,
            traceCell2(observation.sourceOrdinals)
          ];
        }
      },
      skillId
    ), operations, derivedFacts);
  }
  if (skillId === "neuro.multisignal_trace") {
    const window = traceWindowFrom(rec(data.window));
    const rawSeries = arr(data.series) ?? [];
    const declaredPanels = arr(parameters.panels) ?? [];
    if (!window || rawSeries.length === 0 || declaredPanels.length === 0) return { pending: rendererId };
    const sharedTime = rec(data.eventTimes);
    const inputs = rawSeries.map((value, index) => {
      const entry = rec(value) ?? {};
      const time = data.timeBase === "shared" ? sharedTime ?? {} : rec(entry.time) ?? {};
      const values = rec(entry.values) ?? {};
      const offset = rec(entry.timeOffset);
      return {
        id: entry.seriesId ?? `series-${index + 1}`,
        label: entry.label ?? entry.seriesId ?? `Series ${index + 1}`,
        panelId: entry.panelId ?? "main",
        observationKind: entry.observationKind ?? "point_sample",
        times: numbers4(time.values),
        timeUnit: time.unit ?? window.unit,
        values: traceValues(values),
        valueUnit: values.unit ?? "1",
        ...offset && num(offset.value) !== void 0 && typeof offset.unit === "string" ? { timeOffset: { value: num(offset.value), unit: offset.unit } } : {}
      };
    });
    let normalization;
    try {
      normalization = traceNormalizationFrom(parameters, window);
    } catch (error) {
      return { errors: [tracePreparationError(error, skillId, "/parameters/normalization/statisticsWindow")] };
    }
    const seriesIds = inputs.map((input) => input.id);
    if (new Set(seriesIds).size !== seriesIds.length) {
      return fail(
        "SEMANTIC_DUPLICATE_ID",
        "semantic",
        "multi-signal series ids must be unique; duplicate identities cannot bind an unambiguous legend or table.",
        "/data/series"
      );
    }
    const orderedPanelRecords = declaredPanels.map((value, panelIndex) => ({
      panel: rec(value) ?? {},
      declaredIndex: panelIndex
    }));
    const panelIds = orderedPanelRecords.map(({ panel, declaredIndex }) => panel.panelId ?? `panel-${declaredIndex + 1}`);
    if (new Set(panelIds).size !== panelIds.length) {
      return fail(
        "SEMANTIC_DUPLICATE_ID",
        "semantic",
        "multi-signal panel ids must be unique; duplicate panels would duplicate observations in the artifact.",
        "/parameters/panels"
      );
    }
    const panelIdSet = new Set(panelIds);
    const undeclared = inputs.find((input) => !panelIdSet.has(input.panelId));
    if (undeclared) {
      return fail(
        "RENDER_NO_DATA",
        "render",
        `series ${undeclared.id} names undeclared panel ${undeclared.panelId}. Cortexel refuses rather than silently dropping the series.`,
        "/data/series"
      );
    }
    if (parameters.panelOrder === "by_panel_id") {
      orderedPanelRecords.sort((a, b) => {
        const left = String(a.panel.panelId ?? "");
        const right = String(b.panel.panelId ?? "");
        return left < right ? -1 : left > right ? 1 : a.declaredIndex - b.declaredIndex;
      });
    }
    const panels = [];
    for (const { panel, declaredIndex } of orderedPanelRecords) {
      const panelId = panel.panelId ?? `panel-${declaredIndex + 1}`;
      const memberInputs = inputs.map((input, index) => ({ input, index })).filter(({ input }) => input.panelId === panelId);
      if (memberInputs.length === 0) {
        return fail(
          "RENDER_NO_DATA",
          "render",
          `declared panel ${panelId} has no member series. Cortexel refuses an empty coordinate system.`,
          "/parameters/panels"
        );
      }
      if (memberInputs.length > CATEGORICAL_SERIES_STYLES.length) {
        return fail(
          "RENDER_SERIES_LIMIT_EXCEEDED",
          "render",
          `panel ${panelId} contains ${memberInputs.length} overlaid series, above the ${CATEGORICAL_SERIES_STYLES.length}-style distinguishability limit.`,
          "/data/series"
        );
      }
      const targetUnit = normalization ? "1" : panel.unit ?? "1";
      const members = [];
      if (memberInputs.length > 1 && memberInputs.some(({ input }) => dimensionOf(input.valueUnit) === "simulator_defined")) {
        return fail(
          "SCIENCE_UNIT_DIMENSION_MISMATCH",
          "science",
          `panel ${panelId} contains a simulator-defined unit alongside another series. Its meaning is model-dependent even when the unit codes are identical, so it must occupy a panel of its own.`,
          `/parameters/panels/${declaredIndex}`
        );
      }
      for (const { input, index } of memberInputs) {
        if (input.times.length !== input.values.length) {
          return fail(
            "SEMANTIC_LENGTH_MISMATCH",
            "semantic",
            `series ${input.id} has ${input.times.length} timestamps and ${input.values.length} values.`,
            `/data/series/${index}`
          );
        }
        const seriesUncertainty = rec(rec(rawSeries[index])?.uncertainty);
        const uncertaintyError = traceUncertaintyValidationError(
          seriesUncertainty,
          input.valueUnit,
          input.values,
          skillId,
          `/data/series/${index}/uncertainty`
        );
        if (uncertaintyError) return { errors: [uncertaintyError] };
        const uncertaintyMismatch = traceUncertaintyLengthMismatch(
          seriesUncertainty,
          input.values.length
        );
        if (uncertaintyMismatch) {
          return fail(
            "SEMANTIC_LENGTH_MISMATCH",
            "semantic",
            `series ${input.id} uncertainty.${uncertaintyMismatch.key} has ${uncertaintyMismatch.observed} entries for ${input.values.length} samples.`,
            `/data/series/${index}/uncertainty/${uncertaintyMismatch.key}`
          );
        }
        let prepared;
        let uncertaintyArrays;
        try {
          prepared = prepareTrace(input, window, parameters, targetUnit, normalization);
          uncertaintyArrays = traceUncertaintyArrays(seriesUncertainty, prepared);
        } catch (error) {
          return { errors: [tracePreparationError(error, skillId, `/data/series/${index}`)] };
        }
        if (prepared.excludedBelow + prepared.excludedAbove > 0) {
          return fail(
            "SCIENCE_EVENT_OUT_OF_WINDOW",
            "science",
            `series ${input.id} has ${prepared.excludedBelow + prepared.excludedAbove} samples outside the declared display window after applying its clock offset. Multi-signal traces refuse this contradiction rather than cropping it.`,
            `/data/series/${index}`
          );
        }
        if (prepared.outputCount === 0) {
          return fail(
            "RENDER_NO_DATA",
            "render",
            `series ${input.id} has no observation to draw.`,
            `/data/series/${index}`
          );
        }
        members.push({
          series: prepared,
          styleIndex: index,
          tableMetadata: { sourceIndex: index, panelId },
          outputAuthority: traceOutputAuthority(prepared, "series_paths", "samples", "uncertainty"),
          ...uncertaintyArrays
        });
      }
      const scale = panel.scale ?? "linear";
      if (scale === "log" && members.some((entry) => [
        ...entry.series.values,
        ...entry.uncertaintyLower ?? [],
        ...entry.uncertaintyUpper ?? []
      ].some((value) => value !== null && value <= 0))) {
        return fail(
          "RENDER_LOG_SCALE_NONPOSITIVE_DOMAIN",
          "render",
          `panel ${panelId} requests a logarithmic scale but contains a zero or negative displayed value. Cortexel never clips those observations.`,
          `/parameters/panels/${declaredIndex}/scale`
        );
      }
      panels.push({
        id: panelId,
        label: panel.label ?? panelId,
        yLabel: normalization ? `${normalization.method} (${unitLabel("1")})` : `value (${unitLabel(targetUnit)})`,
        series: members,
        scale,
        ...scale === "symlog" && typeof panel.symlogLinearThreshold === "number" ? { symlogLinearThreshold: panel.symlogLinearThreshold } : {}
      });
    }
    const allEntries = panels.flatMap((panel) => panel.series);
    const allPrepared = allEntries.map((entry) => entry.series);
    const rowsTotal = allPrepared.reduce((total, entry) => total + entry.outputCount, 0);
    const duplicate = traceDuplicateOptions(parameters);
    const declaredUncertainties = allEntries.map((entry) => {
      const sourceIndex = Number(entry.tableMetadata?.sourceIndex ?? 0);
      return rec(rec(rawSeries[sourceIndex])?.uncertainty);
    });
    const declaredUncertaintyCount = declaredUncertainties.filter(
      (uncertainty) => uncertainty?.kind !== void 0 && uncertainty.kind !== "none"
    ).length;
    const shownUncertaintyCount = allEntries.filter((entry) => traceSeriesHasCompleteDrawableUncertainty(entry)).length;
    const uncertaintyReasons = declaredUncertainties.map((uncertainty) => uncertainty?.reason).filter((reason) => typeof reason === "string");
    const derivedFacts = {
      ...traceDerivedFacts(allPrepared, duplicate.aggregateMethod, declaredUncertainties),
      uncertaintyKind: declaredUncertaintyCount > 0 ? "provided" : "none",
      uncertaintySeriesDeclared: declaredUncertaintyCount,
      uncertaintySeriesShown: shownUncertaintyCount,
      uncertaintySeriesTotal: declaredUncertainties.length,
      ...declaredUncertaintyCount === 0 && uncertaintyReasons.length > 0 ? { uncertaintyReason: [...new Set(uncertaintyReasons)].join(", ") } : {}
    };
    const multisignalSummaryFacts = {
      seriesCount: String(allPrepared.length),
      panelCount: String(panels.length),
      windowStart: traceCompilerSummaryNumber(window.start),
      windowStop: traceCompilerSummaryNumber(window.stop),
      timeUnit: window.unit,
      windowBoundary: window.finalEdgeInclusive ? "[start,stop]" : "[start,stop)",
      layout: String(parameters.layout),
      timeAlignment: String(structuredCell5(data.timeAlignment) ?? "unknown"),
      panelSummary: panels.map((panel) => {
        const sourceIndex = declaredPanels.findIndex((candidate, candidateIndex) => {
          const declaration2 = rec(candidate) ?? {};
          return String(declaration2.panelId ?? `panel-${candidateIndex + 1}`) === panel.id;
        });
        const declaration = sourceIndex >= 0 ? rec(declaredPanels[sourceIndex]) ?? {} : {};
        return `${panel.id}: ${panel.series.length} series, unit ${normalization ? "1" : String(declaration.unit ?? "1")}, scale ${String(declaration.scale ?? "linear")}`;
      }).join("; "),
      seriesSummary: allEntries.map((entry) => {
        const sourceIndex = Number(entry.tableMetadata?.sourceIndex ?? 0);
        const raw = rec(rawSeries[sourceIndex]) ?? {};
        const values = rec(raw.values) ?? {};
        return `${entry.series.id}: ${String(raw.variableId ?? "unknown")} (${String(values.kind ?? "unknown")}, ${entry.series.valueUnit})`;
      }).join("; "),
      observationKindStatement: `Observation kinds: ${traceCompilerJoinUnique(
        allPrepared.map((trace) => trace.observationKind)
      )}.`,
      originStatement: `Origins: ${traceCompilerJoinUnique(allEntries.map((entry) => {
        const sourceIndex = Number(entry.tableMetadata?.sourceIndex ?? 0);
        return traceCompilerOriginText(rec(rawSeries[sourceIndex]) ?? {});
      }))}.`,
      sampleCount: String(rowsTotal),
      missingCount: String(allPrepared.reduce(
        (sum, trace) => sum + trace.observations.filter((observation) => observation.value === null).length,
        0
      )),
      duplicateTimePolicy: traceCompilerDuplicateStatement(parameters, allPrepared),
      normalizationStatement: normalization ? `Normalization: ${normalization.method} using ${traceCompilerSummaryNumber(normalization.statisticsWindow.start)} to ${traceCompilerSummaryNumber(normalization.statisticsWindow.stop)} ${normalization.statisticsWindow.unit}.` : "No normalization was applied.",
      uncertaintyStatement: traceCompilerUncertaintyStatement(declaredUncertainties),
      unitConversionStatement: traceCompilerUnitConversionStatement(allPrepared),
      compactionStatement: traceCompilerNoCompactionStatement()
    };
    const multisignalCompileContext = traceCompilerContext(
      context(rowsTotal, derivedFacts),
      skillId,
      multisignalSummaryFacts
    );
    const operations = allEntries.map((entry) => {
      const sourceIndex = Number(entry.tableMetadata?.sourceIndex ?? 0);
      const raw = rawSeries[sourceIndex];
      const rawRecord = rec(raw) ?? {};
      const uncertainty = rec(rawRecord.uncertainty);
      const uncertaintySourceUnit = typeof uncertainty?.unit === "string" ? uncertainty.unit : entry.series.valueUnit;
      const uncertaintyTargetUnit = entry.series.normalization ? entry.series.sourceValueUnit : entry.series.valueUnit;
      return traceOperation(
        data.timeBase === "shared" ? { series: raw, eventTimes: sharedTime } : raw,
        entry.series,
        window,
        parameters,
        {
          lower: entry.uncertaintyLower,
          upper: entry.uncertaintyUpper,
          ...uncertainty?.kind && uncertainty.kind !== "none" && uncertaintySourceUnit !== uncertaintyTargetUnit ? { conversion: conversionReceipt(uncertaintySourceUnit, uncertaintyTargetUnit) } : {}
        }
      );
    });
    return done(compileTraceFigure(
      multisignalCompileContext,
      panels,
      {
        xLabel: `time (${unitLabel(window.unit)})`,
        xDomain: [window.start, window.stop],
        sharedXAxis: true,
        showSamplePoints: parameters.showSamplePoints === true,
        summaryStatements: [
          `Display window [${formatNumber(window.start)}, ${formatNumber(window.stop)}${window.finalEdgeInclusive ? "]" : ")"} ${unitLabel(window.unit)}.`,
          `Layout ${String(parameters.layout)}; panel order ${String(parameters.panelOrder ?? "as_declared")}; time alignment ${String(rec(data.timeAlignment)?.kind ?? "unknown")}.`,
          ...parameters.layout === "small_multiples" ? ["Panel y domains and units are independent; curve heights are not comparable across panels."] : []
        ],
        tableColumns: [
          { key: "seriesId", header: "Series" },
          { key: "seriesLabel", header: "Label" },
          { key: "entityId", header: "Entity" },
          { key: "entityKind", header: "Entity kind" },
          { key: "compartmentId", header: "Compartment" },
          { key: "pathwayId", header: "Pathway" },
          { key: "variableId", header: "Variable" },
          { key: "panelId", header: "Panel" },
          { key: "observationKind", header: "Observation kind" },
          { key: "origin", header: "Origin" },
          { key: "originMethod", header: "Derivation method" },
          { key: "recordedTime", header: "Recorded time" },
          { key: "recordedTimeUnit", header: "Recorded time unit" },
          { key: "timeOffset", header: "Declared offset" },
          { key: "timeOffsetUnit", header: "Declared offset unit" },
          { key: "time", header: "Time" },
          { key: "timeUnit", header: "Time unit" },
          { key: "value", header: "Value" },
          { key: "unit", header: "Unit" },
          { key: "displayValue", header: "Drawn value" },
          { key: "displayUnit", header: "Drawn unit" },
          { key: "missing", header: "Missing" },
          { key: "replicateCount", header: "Replicates" },
          { key: "uncertaintyKind", header: "Uncertainty kind" },
          { key: "uncertaintyLower", header: "Uncertainty lower" },
          { key: "uncertaintyUpper", header: "Uncertainty upper" },
          { key: "uncertaintyMethod", header: "Uncertainty declaration" },
          { key: "normalizationParameters", header: "Normalization parameters" },
          { key: "sourceRowIndex", header: "Source row" }
        ],
        tableRow: (entry, observationIndex, panel) => {
          const sourceIndex = Number(entry.tableMetadata?.sourceIndex ?? 0);
          const raw = rec(rawSeries[sourceIndex]) ?? {};
          const origin = rec(raw.origin) ?? {};
          const uncertainty = rec(raw.uncertainty);
          const rawValues = rec(raw.values) ?? {};
          const observation = entry.series.observations[observationIndex];
          const sourceOrdinal = observation.sourceOrdinals.length === 1 ? observation.sourceOrdinals[0] : void 0;
          const [uncertaintyLower, uncertaintyUpper, uncertaintyMethod] = traceUncertaintyCells(
            uncertainty,
            sourceOrdinal,
            entry.series,
            observation.value
          );
          return [
            entry.series.id,
            entry.series.label,
            String(raw.entityId ?? ""),
            String(raw.entityKind ?? ""),
            typeof raw.compartmentId === "string" ? raw.compartmentId : null,
            typeof raw.pathwayId === "string" ? raw.pathwayId : null,
            String(raw.variableId ?? ""),
            panel.id,
            entry.series.observationKind,
            String(origin.kind ?? "unknown"),
            typeof origin.method === "string" ? origin.method : null,
            observation.recordedTime,
            entry.series.sourceTimeUnit,
            entry.series.timeOffset?.sourceValue ?? 0,
            entry.series.timeOffset?.sourceUnit ?? entry.series.sourceTimeUnit,
            observation.time,
            entry.series.timeUnit,
            traceCell2(observation.sourceValues),
            String(rawValues.unit ?? entry.series.valueUnit),
            observation.value,
            entry.series.valueUnit,
            observation.value === null ? "true" : "false",
            observation.replicateCount,
            String(uncertainty?.kind ?? "none"),
            uncertaintyLower,
            uncertaintyUpper,
            uncertaintyMethod,
            structuredCell5(entry.series.normalization),
            traceCell2(observation.sourceOrdinals)
          ];
        }
      },
      skillId
    ), operations, derivedFacts);
  }
  if (skillId === "neuro.compartment_trace") {
    let window;
    try {
      window = traceWindowFrom(rec(data.window));
    } catch (error) {
      return { errors: [tracePreparationError(error, skillId, "/data/window")] };
    }
    const rawSeries = arr(data.series) ?? [];
    if (!window || rawSeries.length === 0) return { pending: rendererId };
    const firstValues = rec(rec(rawSeries[0])?.values) ?? {};
    const targetValueUnit = String(firstValues.unit);
    const declaredUncertainty = rec(parameters.uncertainty);
    if (declaredUncertainty?.kind !== void 0 && declaredUncertainty.kind !== "none" && rawSeries.length !== 1) {
      return fail(
        "SCIENCE_UNCERTAINTY_UNSUPPORTED_FOR_SKILL",
        "science",
        "compartment-trace uncertainty is declared once for the whole figure in contract 1.0 and is therefore unambiguous only when exactly one source series is present.",
        "/parameters/uncertainty"
      );
    }
    if (parameters.layout === "heatmap" && declaredUncertainty?.kind !== void 0 && declaredUncertainty.kind !== "none") {
      return fail(
        "SCIENCE_UNCERTAINTY_UNSUPPORTED_FOR_SKILL",
        "science",
        "a heatmap cell has one colour channel and cannot simultaneously encode a declared interval without a registered uncertainty mark; choose a trace layout or uncertainty:none.",
        "/parameters/uncertainty"
      );
    }
    const compartmentIds = strings4(data.compartmentIds);
    const labels = strings4(data.compartmentLabels);
    const compartmentIndex = new Map(compartmentIds.map((id, index) => [id, index]));
    const entries = [];
    const operations = [];
    try {
      for (let seriesIndex = 0; seriesIndex < rawSeries.length; seriesIndex++) {
        const raw = rec(rawSeries[seriesIndex]) ?? {};
        const time = rec(raw.time) ?? {};
        const values = rec(raw.values) ?? {};
        const sourceValues = traceValues(values);
        const uncertaintyError = traceUncertaintyValidationError(
          declaredUncertainty,
          String(values.unit),
          sourceValues,
          skillId,
          "/parameters/uncertainty"
        );
        if (uncertaintyError) return { errors: [uncertaintyError] };
        const mismatch = traceUncertaintyLengthMismatch(declaredUncertainty, sourceValues.length);
        if (mismatch) {
          return fail(
            "SEMANTIC_LENGTH_MISMATCH",
            "semantic",
            `compartment uncertainty.${mismatch.key} has ${mismatch.observed} entries for ${sourceValues.length} samples.`,
            `/parameters/uncertainty/${mismatch.key}`
          );
        }
        const compartmentId = String(raw.compartmentId);
        const signalId = String(raw.signalId);
        const prepared = prepareTrace({
          id: `compartment-source-${String(seriesIndex)}`,
          label: `${labels[compartmentIndex.get(compartmentId) ?? -1] ?? compartmentId}: ${String(raw.signalLabel ?? signalId)}`,
          observationKind: "point_sample",
          times: numbers4(time.values),
          timeUnit: String(time.unit),
          values: sourceValues,
          valueUnit: String(values.unit)
        }, window, parameters, targetValueUnit);
        const uncertaintyArrays = traceUncertaintyArrays(declaredUncertainty, prepared);
        entries.push({
          raw,
          prepared,
          compartmentIndex: compartmentIndex.get(compartmentId) ?? seriesIndex,
          seriesIndex,
          ...uncertaintyArrays
        });
      }
    } catch (error) {
      return { errors: [tracePreparationError(error, skillId, "/data/series")] };
    }
    let preparationBatch;
    try {
      preparationBatch = tracePreparationBatchOperation(
        "compartment",
        {
          skillId,
          dataContext: { ...data, series: null },
          parameters,
          analysisWindow: window,
          targetValueUnit
        },
        entries.map((entry) => ({
          sourceIndex: entry.seriesIndex,
          seriesIdentity: {
            kind: "compartment_series",
            compartmentId: String(entry.raw.compartmentId),
            signalId: String(entry.raw.signalId)
          },
          role: "source_series",
          inputPayload: entry.raw,
          views: [{
            kind: "display",
            prepared: entry.prepared,
            window,
            uncertaintyLower: entry.uncertaintyLower,
            uncertaintyUpper: entry.uncertaintyUpper
          }],
          transforms: compartmentPreparationBatchTransforms(
            entry,
            declaredUncertainty
          ),
          initialStatePainted: false,
          initialStateConsumedByDerivedAggregate: false
        }))
      );
      operations.push(preparationBatch);
    } catch (error) {
      return { errors: [tracePreparationError(error, skillId, "/data/series")] };
    }
    let derivedAggregate;
    const aggregate4 = rec(parameters.compartmentAggregate);
    if (aggregate4) {
      try {
        derivedAggregate = deriveCompartmentAggregate(aggregate4, entries, window);
        operations.push(chainAggregateToPreparationBatch(
          derivedAggregate.operation,
          preparationBatch
        ));
      } catch (error) {
        return { errors: [tracePreparationError(
          error,
          skillId,
          "/parameters/compartmentAggregate"
        )] };
      }
    }
    const asPanelEntry = (entry, styleIndex = entry.seriesIndex) => ({
      series: entry.prepared,
      styleIndex,
      outputAuthority: traceOutputAuthority(entry.prepared, "series_paths", "samples", "uncertainty"),
      uncertaintyLower: entry.uncertaintyLower,
      uncertaintyUpper: entry.uncertaintyUpper,
      uncertainty: entry.uncertainty
    });
    const valueKind = String(firstValues.kind);
    const yLabel = `${valueKind.replaceAll("_", " ")} (${unitLabel(targetValueUnit)})`;
    const layout = String(parameters.layout);
    let plan;
    const tableRows = compartmentTraceTableRows(data, entries);
    const recordedCompartmentIds = new Set(entries.map((entry) => String(entry.raw.compartmentId)));
    const expectedCompartmentRows = entries.reduce(
      (sum, entry) => sum + entry.prepared.outputCount,
      0
    ) + compartmentIds.filter((id) => !recordedCompartmentIds.has(id)).length;
    if (tableRows.length !== expectedCompartmentRows) {
      return fail(
        "INTERNAL_INVARIANT_VIOLATED",
        "internal",
        `compartment table materialized ${tableRows.length} rows for ${expectedCompartmentRows} accepted sample/unrecorded-compartment carriers.`,
        "/data"
      );
    }
    const derivedFacts = {
      ...traceDerivedFacts(entries.map((entry) => entry.prepared), traceDuplicateOptions(parameters).aggregateMethod, entries.map(() => declaredUncertainty)),
      nodeUniverseComplete: data.compartmentUniverseComplete === true,
      uncertaintyKind: declaredUncertainty?.kind && declaredUncertainty.kind !== "none" ? "provided" : "none",
      uncertaintySeriesDeclared: declaredUncertainty?.kind && declaredUncertainty.kind !== "none" ? 1 : 0,
      uncertaintySeriesShown: [
        ...entries.map((entry) => asPanelEntry(entry)),
        ...derivedAggregate ? [derivedAggregate.entry] : []
      ].filter((entry) => traceSeriesHasCompleteDrawableUncertainty(entry)).length,
      uncertaintySeriesTotal: entries.length + (derivedAggregate ? 1 : 0),
      ...declaredUncertainty?.kind === "none" && typeof declaredUncertainty.reason === "string" ? { uncertaintyReason: declaredUncertainty.reason } : declaredUncertainty === void 0 ? { uncertaintyReason: "not_provided" } : {}
    };
    const compartmentTraces = entries.map((entry) => entry.prepared);
    const compartmentFiniteValues = compartmentTraces.flatMap((trace) => trace.values).filter((value) => value !== null);
    const recordedCompartmentCount = recordedCompartmentIds.size;
    const compartmentSummaryFacts = {
      cellLabel: String(data.cellLabel ?? data.cellId),
      signalLabel: traceCompilerJoinUnique(rawSeries.map((candidate) => {
        const raw = rec(candidate) ?? {};
        return String(raw.signalLabel ?? raw.signalId);
      })),
      quantityKind: traceCompilerJoinUnique(rawSeries.map((candidate) => String(rec(rec(candidate)?.values)?.kind))),
      unit: targetValueUnit,
      compartmentCount: String(compartmentIds.length),
      windowStart: traceCompilerSummaryNumber(window.start),
      windowStop: traceCompilerSummaryNumber(window.stop),
      timeUnit: window.unit,
      layoutMode: layout,
      scaleStatement: layout === "heatmap" ? "one global colour domain over all accepted cells" : layout === "small_multiples" ? `${String(parameters.yScale)} y domains` : "one shared y domain for the declared overlay selection",
      compartmentOrderBasis: String(data.compartmentOrderBasis),
      recordedCompartmentCount: String(recordedCompartmentCount),
      notRecordedCount: String(compartmentIds.length - recordedCompartmentCount),
      sampleCount: String(compartmentTraces.reduce(
        (sum, trace) => sum + trace.observations.length,
        0
      )),
      missingSampleCount: String(compartmentTraces.reduce(
        (sum, trace) => sum + trace.observations.filter((observation) => observation.value === null).length,
        0
      )),
      valueMin: compartmentFiniteValues.length > 0 ? traceCompilerSummaryNumber(traceCompilerMinimum(compartmentFiniteValues)) : "not available",
      valueMax: compartmentFiniteValues.length > 0 ? traceCompilerSummaryNumber(traceCompilerMaximum(compartmentFiniteValues)) : "not available",
      duplicateTimeStatement: `Duplicate timestamps: ${traceCompilerDuplicateStatement(parameters, compartmentTraces)}.`,
      aggregateStatement: aggregate4 ? `Aggregate: ${String(aggregate4.method)} over ${strings4(aggregate4.compartmentIds).length} explicitly selected compartments with ${String(aggregate4.weighting)} weighting.` : "No compartment aggregate was requested.",
      uncertaintyStatement: traceCompilerUncertaintyStatement(
        compartmentTraces.map(() => declaredUncertainty)
      ),
      universeStatement: data.compartmentUniverseComplete === true ? "The declared compartment universe is complete." : "The declared compartments are an incomplete subset of the cell.",
      compactionStatement: traceCompilerNoCompactionStatement()
    };
    const compileContext = traceCompilerContext(
      context(tableRows.length, derivedFacts),
      skillId,
      compartmentSummaryFacts
    );
    if (layout === "heatmap") {
      const signalIds = new Set(entries.map((entry) => String(entry.raw.signalId)));
      if (signalIds.size !== 1) {
        return fail(
          "RENDER_SERIES_LIMIT_EXCEEDED",
          "render",
          `a heatmap has one global colour quantity, but ${signalIds.size} signal identities were supplied.`,
          "/data/series"
        );
      }
      const rowEntries = compartmentIds.map((id, index) => {
        const matches = entries.filter((entry) => String(entry.raw.compartmentId) === id);
        if (matches.length > 1) {
          throw new Error(`compartment ${id} has more than one ${[...signalIds][0]} series`);
        }
        return {
          id,
          label: labels[index] ?? id,
          times: matches[0]?.prepared.times ?? [],
          values: matches[0]?.prepared.values ?? [],
          ...matches[0] ? {
            outputAuthority: {
              classId: "heatmap_cells",
              provenance: matches[0].prepared.observations.map((observation) => ({
                seriesId: matches[0].prepared.id,
                sourceOrdinals: [...observation.sourceOrdinals],
                atomKind: "heatmap_cell"
              }))
            }
          } : {}
        };
      });
      if (derivedAggregate) {
        rowEntries.push({
          id: derivedAggregate.entry.series.id,
          label: derivedAggregate.entry.series.label,
          times: derivedAggregate.entry.series.times,
          values: derivedAggregate.entry.series.values,
          outputAuthority: {
            classId: "aggregate",
            provenance: derivedAggregate.entry.series.observations.map((observation) => ({
              seriesId: derivedAggregate.entry.series.id,
              sourceOrdinals: [...observation.sourceOrdinals],
              atomKind: "aggregate_heatmap_cell"
            }))
          }
        });
      }
      const colorScale = rec(parameters.colorScale) ?? {};
      plan = compileCompartmentHeatmapFigure(
        compileContext,
        rowEntries,
        [window.start, window.stop],
        `time (${unitLabel(window.unit)})`,
        yLabel,
        {
          family: colorScale.family === "diverging" ? "diverging" : "sequential",
          ...typeof colorScale.center === "number" ? { center: colorScale.center } : {}
        },
        skillId
      );
    } else {
      const panels = [];
      if (layout === "small_multiples") {
        for (let index = 0; index < compartmentIds.length; index++) {
          const id = compartmentIds[index];
          const panelEntries = entries.filter((entry) => String(entry.raw.compartmentId) === id).map((entry, entryIndex) => asPanelEntry(entry, entryIndex));
          if (panelEntries.length > CATEGORICAL_SERIES_STYLES.length) {
            return fail(
              "RENDER_SERIES_LIMIT_EXCEEDED",
              "render",
              `compartment ${id} has ${panelEntries.length} overlaid signals but only ${CATEGORICAL_SERIES_STYLES.length} stable style tuples exist.`,
              "/data/series"
            );
          }
          const panelLabel = panelEntries.length === 1 ? `${labels[index] ?? id}: ${panelEntries[0].series.label.split(": ").slice(1).join(": ")}` : labels[index] ?? id;
          panels.push({
            id,
            label: panelLabel,
            yLabel,
            series: panelEntries.length === 1 && panelEntries[0].uncertainty === void 0 ? [{ ...panelEntries[0], includeInLegend: false }] : panelEntries
          });
        }
        if (derivedAggregate) {
          panels.push({
            id: derivedAggregate.entry.series.id,
            label: derivedAggregate.entry.series.label,
            yLabel,
            series: [{ ...derivedAggregate.entry, includeInLegend: false }]
          });
        }
      } else {
        const selected = new Set(strings4(parameters.overlayCompartmentIds));
        const selectedEntries = entries.filter((entry) => selected.has(String(entry.raw.compartmentId)));
        const drawnCount = selectedEntries.length + (derivedAggregate ? 1 : 0);
        if (drawnCount > CATEGORICAL_SERIES_STYLES.length) {
          return fail(
            "RENDER_SERIES_LIMIT_EXCEEDED",
            "render",
            `${drawnCount} overlay series exceed the ${CATEGORICAL_SERIES_STYLES.length} registered distinguishable style tuples.`,
            "/parameters/overlayCompartmentIds"
          );
        }
        panels.push({
          id: "overlay",
          label: `${String(data.cellLabel ?? data.cellId)} \u2014 declared compartment selection`,
          yLabel,
          series: [
            ...selectedEntries.map((entry, index) => asPanelEntry(entry, index)),
            ...derivedAggregate ? [{ ...derivedAggregate.entry, styleIndex: selectedEntries.length }] : []
          ]
        });
      }
      plan = compileTraceFigure(
        compileContext,
        panels,
        {
          xLabel: `time (${unitLabel(window.unit)})`,
          xDomain: [window.start, window.stop],
          sharedXAxis: true,
          sharedYDomain: layout === "small_multiples" && parameters.yScale === "shared",
          showSamplePoints: false,
          groupSeriesMarks: true,
          summaryStatements: [
            `Compartment order is caller-declared as ${String(data.compartmentOrderBasis)} and is never inferred.`,
            `${compartmentIds.length} compartments declared; ${new Set(entries.map((entry) => String(entry.raw.compartmentId))).size} recorded.`,
            ...layout === "small_multiples" ? [`Small-multiple value domains are ${String(parameters.yScale)}.`] : ["Only the explicitly selected compartments are overlaid."]
          ]
        },
        skillId
      );
    }
    return done(
      withContractTable(plan, compileContext, skillId, tableRows),
      operations,
      derivedFacts
    );
  }
  if (skillId === "network.synaptic_weight_trace") {
    let window;
    try {
      window = traceWindowFrom(rec(data.window));
    } catch (error) {
      return { errors: [tracePreparationError(error, skillId, "/data/window")] };
    }
    if (!window) return { pending: rendererId };
    const display = String(parameters.display);
    const dataObservation = rec(data.observation) ?? {};
    const refuseSaturatedWeightTable = (preflight, instancePath) => ({
      errors: [makeError({
        code: "RESOURCE_COMPACTION_UNAVAILABLE",
        stage: "budget",
        instancePath,
        skillId,
        message: `the complete synaptic-weight carrier table requires more than ${returnedTableRows} rows. Cortexel saturated the canonical carrier preflight at ${preflight.saturatedRowCount} (a lower-bound sentinel, not an exact count) and refused before allocating table cells or render geometry.`,
        limit: {
          name: "returnedTableRows",
          limit: returnedTableRows,
          observed: preflight.saturatedRowCount
        }
      })]
    });
    const prepareMember = (raw, observation, targetValueUnit, instancePath, hasRecordedInterval) => {
      const time = rec(raw.time) ?? {};
      const values = rec(raw.values) ?? {};
      const sourceValues = traceValues(values);
      const uncertainty = rec(raw.uncertainty);
      const uncertaintyError = traceUncertaintyValidationError(
        uncertainty,
        String(values.unit),
        sourceValues,
        skillId,
        `${instancePath}/uncertainty`
      );
      if (uncertaintyError) return uncertaintyError;
      const mismatch = traceUncertaintyLengthMismatch(uncertainty, sourceValues.length);
      if (mismatch) {
        return makeError({
          code: "SEMANTIC_LENGTH_MISMATCH",
          stage: "semantic",
          instancePath: `${instancePath}/uncertainty/${mismatch.key}`,
          skillId,
          message: `uncertainty.${mismatch.key} has ${mismatch.observed} entries for ${sourceValues.length} weight observations.`
        });
      }
      const interval = hasRecordedInterval ? rec(raw.recordedInterval) ?? {} : rec(data.window) ?? {};
      const intervalUnit = String(interval.unit);
      const intervalStart = intervalUnit === window.unit ? num(interval.start) : convert(num(interval.start), intervalUnit, window.unit);
      const intervalStop = intervalUnit === window.unit ? num(interval.stop) : convert(num(interval.stop), intervalUnit, window.unit);
      const recordedStart = Math.max(window.start, intervalStart);
      const recordedStop = Math.min(window.stop, intervalStop);
      if (!(recordedStop > recordedStart)) {
        return makeError({
          code: "SCIENCE_WINDOW_INVALID",
          stage: "science",
          instancePath: `${instancePath}/recordedInterval`,
          skillId,
          message: "the recorded interval does not overlap the requested analysis window."
        });
      }
      const intervalFinalEdgeInclusive = typeof interval.boundary === "string" && interval.boundary.endsWith("]");
      const recordedFinalEdgeInclusive = (recordedStop !== window.stop || window.finalEdgeInclusive) && (recordedStop !== intervalStop || intervalFinalEdgeInclusive);
      const memberWindow = {
        start: recordedStart,
        stop: recordedStop,
        unit: window.unit,
        finalEdgeInclusive: recordedFinalEdgeInclusive
      };
      const observationKind = observation.kind === "event_updated" ? "piecewise_constant" : "point_sample";
      const prepared = prepareTrace({
        id: String(raw.edgeId ?? raw.groupId),
        label: String(raw.label ?? raw.groupLabel ?? raw.edgeId ?? raw.groupId),
        observationKind,
        times: numbers4(time.values),
        timeUnit: String(time.unit),
        values: sourceValues,
        valueUnit: String(values.unit)
      }, memberWindow, parameters, targetValueUnit);
      const statePrepared = prepareTrace({
        id: String(raw.edgeId ?? raw.groupId),
        label: String(raw.label ?? raw.groupLabel ?? raw.edgeId ?? raw.groupId),
        observationKind,
        times: numbers4(time.values),
        timeUnit: String(time.unit),
        values: sourceValues,
        valueUnit: String(values.unit)
      }, {
        start: intervalStart,
        stop: intervalStop,
        unit: window.unit,
        finalEdgeInclusive: intervalFinalEdgeInclusive
      }, parameters, targetValueUnit);
      const uncertaintyArrays = traceUncertaintyArrays(uncertainty, prepared);
      const stateUncertaintyArrays = traceUncertaintyArrays(uncertainty, statePrepared);
      const initialQuantity = rec(rec(raw.initialWeight)?.quantity);
      const initialValue = initialQuantity && typeof initialQuantity.value === "number" ? String(initialQuantity.unit) === targetValueUnit ? initialQuantity.value : convert(initialQuantity.value, String(initialQuantity.unit), targetValueUnit) : void 0;
      const member = {
        raw,
        prepared,
        statePrepared,
        ...uncertaintyArrays,
        stateUncertaintyLower: stateUncertaintyArrays.uncertaintyLower,
        stateUncertaintyUpper: stateUncertaintyArrays.uncertaintyUpper,
        recordedStart,
        recordedStop,
        recordedFinalEdgeInclusive,
        stateStart: intervalStart,
        stateStop: intervalStop,
        initialStateTime: intervalStart,
        ...initialValue !== void 0 ? { initialValue } : {}
      };
      if (observation.kind === "interpolated_trajectory" && interpolatedTrajectoryNeedsExcludedKnot(member)) {
        return makeError({
          code: "SCIENCE_NORMALIZATION_UNVERIFIABLE",
          stage: "science",
          instancePath: `${instancePath}/time/values`,
          skillId,
          message: "the accepted reconstruction would depend on an excluded boundary knot; revision 1 refuses rather than synthesize an un-audited clipped interpolation point."
        });
      }
      return member;
    };
    const referenceLines = (members, targetValueUnit) => {
      if (parameters.showReferenceLines !== true) return [];
      const lines = [];
      const addQuantity = (member, idSuffix, quantity, label) => {
        if (!quantity || typeof quantity.value !== "number" || typeof quantity.unit !== "string") return;
        const value = quantity.unit === targetValueUnit ? quantity.value : convert(quantity.value, quantity.unit, targetValueUnit);
        lines.push({
          id: `${member.prepared.id}-${idSuffix}`,
          value,
          label,
          color: "#6b7280",
          dash: idSuffix === "initial" ? "2 2" : "6 3",
          authority: {
            tag: "data_carrier",
            classId: "references",
            provenance: {
              seriesId: member.prepared.id,
              atomKind: "reference_rule",
              reference: idSuffix
            }
          }
        });
      };
      for (const member of members) {
        const initial = rec(member.raw.initialWeight);
        addQuantity(
          member,
          "initial",
          rec(initial?.quantity),
          `${member.prepared.label}: declared initial weight (${String(initial?.origin ?? "unknown origin")})`
        );
        const bounds = rec(member.raw.bounds);
        addQuantity(
          member,
          "lower",
          rec(bounds?.lower),
          `${member.prepared.label}: declared ${String(bounds?.boundKind ?? "")} lower bound (${String(bounds?.origin ?? "unknown origin")})`
        );
        addQuantity(
          member,
          "upper",
          rec(bounds?.upper),
          `${member.prepared.label}: declared ${String(bounds?.boundKind ?? "")} upper bound (${String(bounds?.origin ?? "unknown origin")})`
        );
      }
      return lines;
    };
    const summaryFacts = (identifiedMembers, sourceMembers, reconstructionMembers, displayedMembers, modelMembers, aggregateMember, carrierCounts, sourceReadingCount, missingSourceReadingCount, referenceMembers) => {
      const traces = displayedMembers.map((member) => member.prepared);
      const uncertainties = displayedMembers.map((member) => rec(member.raw.uncertainty));
      const models = modelMembers.flatMap((member) => {
        const modelSet2 = (arr(member.raw.synapseModels) ?? []).filter((model) => typeof model === "string");
        return modelSet2.length > 0 ? modelSet2 : typeof member.raw.synapseModel === "string" ? [member.raw.synapseModel] : [];
      });
      const observation = data.mode === "preaggregated" ? rec(rec(data.aggregate)?.observation) ?? {} : dataObservation;
      const memberCounts = data.mode === "preaggregated" ? numbers4(rec(data.aggregate)?.memberCounts) : [];
      const maximumConcurrentMembers = memberCounts.length === 0 ? 0 : Math.max(0, traceCompilerMaximum(memberCounts));
      const membership = rec(data.membership) ?? {};
      const comparability = rec(parameters.weightComparability) ?? {};
      const weightUnit = traces[0]?.valueUnit ?? "unknown";
      const modelSet = [...new Set(models)];
      const comparabilityClaim = canonicalize({
        mode: String(comparability.mode),
        models: modelSet
      });
      const referencesShown = parameters.showReferenceLines === true ? referenceMembers.reduce((count, member) => {
        const initial = rec(member.raw.initialWeight) ?? {};
        const bounds = rec(member.raw.bounds) ?? {};
        return count + (typeof rec(initial.quantity)?.value === "number" ? 1 : 0) + (typeof rec(bounds.lower)?.value === "number" ? 1 : 0) + (typeof rec(bounds.upper)?.value === "number" ? 1 : 0);
      }, 0) : 0;
      const excludedSourceReadingCount = sourceMembers.reduce(
        (sum, member) => sum + member.prepared.excludedBelow + member.prepared.excludedAbove,
        0
      );
      const excludedReconstructionPointCount = reconstructionMembers.reduce(
        (sum, member) => sum + member.prepared.excludedBelow + member.prepared.excludedAbove,
        0
      );
      const reconstructionPointCount = reconstructionMembers.reduce(
        (sum, member) => sum + member.prepared.inputCount,
        0
      );
      const missingReconstructionPointCount = reconstructionMembers.reduce(
        (sum, member) => sum + member.prepared.missingCount,
        0
      );
      const retainedReconstructionRowCount = reconstructionMembers.reduce(
        (sum, member) => sum + member.prepared.outputCount,
        0
      );
      return {
        synapseCardinalityStatement: data.mode === "preaggregated" ? `Unique synapse cardinality was not supplied; maximum concurrent memberCount is ${maximumConcurrentMembers}.` : `${identifiedMembers.length} identified synapse${identifiedMembers.length === 1 ? "" : "s"}.`,
        synapseModels: traceCompilerJoinUnique(modelSet),
        weightUnit,
        weightDimensionStatement: dimensionOf(weightUnit) === "simulator_defined" ? `simulator-defined; exact comparability claim ${comparabilityClaim}` : `registered ${String(dimensionOf(weightUnit) ?? "unknown")} dimension; exact comparability claim ${comparabilityClaim}`,
        windowStart: traceCompilerSummaryNumber(window.start),
        windowStop: traceCompilerSummaryNumber(window.stop),
        timeUnit: window.unit,
        observationStatement: observation.kind === "interpolated_trajectory" ? `interpolated_trajectory (method=${String(observation.method)}; interpolant=${String(observation.interpolant)}; reconstructedBy=${String(observation.reconstructedBy)})` : `${String(observation.kind)}${typeof observation.updateSemantics === "string" ? ` (${observation.updateSemantics})` : ""}`,
        duplicateTimeStatement: `Duplicate timestamps: ${traceCompilerDuplicateStatement(
          parameters,
          (data.mode === "preaggregated" ? displayedMembers : [...sourceMembers, ...reconstructionMembers]).map((candidate) => candidate.prepared)
        )}.`,
        retainedSourceRowCount: String(carrierCounts.source_observation.total),
        sourceReadingCount: String(sourceReadingCount),
        missingCount: String(missingSourceReadingCount),
        excludedCount: String(excludedSourceReadingCount),
        reconstructionPointCount: String(reconstructionPointCount),
        retainedReconstructionRowCount: String(retainedReconstructionRowCount),
        missingReconstructionPointCount: String(missingReconstructionPointCount),
        excludedReconstructionPointCount: String(excludedReconstructionPointCount),
        carrierStatement: weightCarrierStatement(
          carrierCounts,
          sourceReadingCount,
          missingSourceReadingCount,
          excludedSourceReadingCount,
          reconstructionPointCount,
          retainedReconstructionRowCount,
          missingReconstructionPointCount,
          excludedReconstructionPointCount
        ),
        displayMode: String(parameters.display),
        aggregateStatement: parameters.display === "aggregate_declared" ? `The caller declared a ${String(rec(data.aggregate)?.method)} aggregate using interval method ${String(rec(data.aggregate)?.intervalMethod)}; Cortexel did not receive member observations and cannot re-derive either claim.` : aggregateMember ? `Cortexel-derived ${String(rec(parameters.aggregate)?.method)} over the declared membership; denominator is contributingCount and zero contributors yield missing.` : "No aggregate was requested.",
        membershipStatement: Object.keys(membership).length > 0 ? `${String(membership.groupId)} with ${(arr(membership.members) ?? []).length} identified members represented by declared half-open membership intervals.` : data.mode === "preaggregated" ? "Member identities were not supplied with the caller-declared aggregate." : "No aggregate membership was declared.",
        referenceStatement: parameters.showReferenceLines === true ? `${referencesShown} declared initial-weight/bound reference lines are shown; they never clamp observations.` : "Declared initial weights and bounds are not drawn as reference lines.",
        scopeStatement: compilerNetworkScopeSummaryCell(data.scope),
        uncertaintyStatement: traceCompilerWeightUncertaintyStatement(uncertainties),
        compactionStatement: traceCompilerNoCompactionStatement()
      };
    };
    const weightDisclosureFacts = (factMembers, exactPresentationEntries) => {
      const uncertainties = factMembers.map((member) => rec(member.raw.uncertainty));
      const declared = uncertainties.filter(
        (uncertainty) => uncertainty?.kind !== void 0 && uncertainty.kind !== "none"
      ).length;
      const reasons = uncertainties.map((uncertainty) => uncertainty?.reason).filter((reason) => typeof reason === "string");
      const presentationEntries = exactPresentationEntries ?? factMembers.map(
        (member, index) => ({
          series: member.prepared,
          styleIndex: index,
          uncertaintyLower: member.uncertaintyLower,
          uncertaintyUpper: member.uncertaintyUpper,
          uncertainty: member.uncertainty
        })
      );
      return {
        ...traceDerivedFacts(
          factMembers.map((member) => member.prepared),
          traceDuplicateOptions(parameters).aggregateMethod,
          uncertainties
        ),
        uncertaintyKind: declared > 0 ? "provided" : "none",
        uncertaintySeriesDeclared: declared,
        uncertaintySeriesShown: presentationEntries.filter(
          (entry) => traceSeriesHasCompleteDrawableUncertainty(
            entry,
            parameters.showObservationMarkers === true
          )
        ).length,
        uncertaintySeriesTotal: factMembers.length,
        ...declared === 0 ? { uncertaintyReason: reasons.length > 0 ? traceCompilerJoinUnique(reasons) : "not_provided" } : {}
      };
    };
    try {
      if (data.mode === "preaggregated") {
        const rawAggregate = rec(data.aggregate) ?? {};
        const values = rec(rawAggregate.values) ?? {};
        const aggregateObservation = rec(rawAggregate.observation) ?? {};
        const preparedOrError = prepareMember(
          rawAggregate,
          aggregateObservation,
          String(values.unit),
          "/data/aggregate",
          false
        );
        if ("code" in preparedOrError) return { errors: [preparedOrError] };
        const member = preparedOrError;
        const eventEvidence = aggregateObservation.kind === "event_updated" ? eventUpdatedMaterialization(
          member,
          aggregateObservation.updateSemantics === "value_before_update" ? "value_before_update" : "value_after_update",
          void 0,
          false
        ) : void 0;
        const memberCounts = numbers4(rawAggregate.memberCounts);
        const contributingCounts = numbers4(rawAggregate.contributingCounts);
        const tableEntries2 = [{
          member,
          observation: aggregateObservation,
          carrierKind: "declared_aggregate_point",
          counts: { memberCounts, contributingCounts },
          stateWitnesses: eventEvidence?.witnesses,
          initialStatePainted: eventEvidence?.initialStatePainted
        }];
        const rowPreflight2 = preflightWeightTableCarriers(tableEntries2, returnedTableRows);
        if (rowPreflight2.tag === "saturated_over_budget") {
          return refuseSaturatedWeightTable(rowPreflight2, "/data/aggregate");
        }
        const eventMaterialization = aggregateObservation.kind === "event_updated" ? eventUpdatedMaterialization(
          member,
          aggregateObservation.updateSemantics === "value_before_update" ? "value_before_update" : "value_after_update"
        ) : void 0;
        const renderRuns = bindWeightRenderRunOrdinals(
          eventMaterialization?.runs ?? interpolatedTrajectoryRenderRuns(
            member,
            aggregateObservation.kind === "interpolated_trajectory" ? "reconstruction_vertex" : "observation_vertex"
          )
        );
        const panelEntry = {
          series: member.prepared,
          styleIndex: 0,
          outputAuthority: weightTraceOutputAuthority(
            member.prepared,
            renderRuns,
            "series_paths",
            aggregateObservation.kind === "interpolated_trajectory" ? "reconstruction" : "observations",
            "uncertainty"
          ),
          ...member,
          renderRuns,
          ...aggregateObservation.kind === "interpolated_trajectory" ? {
            authorityAtomKinds: {
              pathVertex: "reconstruction_vertex",
              sampleMarker: "reconstruction_marker",
              isolatedMarker: "isolated_reconstruction_marker"
            }
          } : {}
        };
        if (!weightPanelSeriesHasPaintableCarrier(
          panelEntry,
          parameters.showObservationMarkers === true
        )) {
          return fail(
            "RENDER_NO_DATA",
            "render",
            "the declared aggregate has no finite retained point, reconstructed vertex, event-held state, carry-in state, or painted initial state in the requested window.",
            "/data/aggregate"
          );
        }
        const table2 = synapticWeightTraceTableRows(
          data,
          parameters,
          rowPreflight2,
          /* @__PURE__ */ new Map([[tableEntries2[0], renderRuns]])
        );
        const tableRows2 = table2.rows;
        if (tableRows2.length !== rowPreflight2.exactRowCount) {
          return fail(
            "INTERNAL_INVARIANT_VIOLATED",
            "internal",
            `declared aggregate table materialized ${tableRows2.length} rows after an exact ${rowPreflight2.exactRowCount}-row canonical carrier preflight.`,
            "/data/aggregate"
          );
        }
        const facts2 = weightDisclosureFacts([member], [panelEntry]);
        const compileContext2 = traceCompilerContext(
          context(tableRows2.length, facts2),
          skillId,
          summaryFacts(
            [],
            [],
            aggregateObservation.kind === "interpolated_trajectory" ? [member] : [],
            [member],
            [member],
            member,
            table2.carrierCounts,
            table2.sourceReadingCount,
            table2.missingSourceReadingCount,
            [member]
          )
        );
        const plan2 = compileTraceFigure(
          compileContext2,
          [{
            id: "aggregate",
            label: member.prepared.label,
            yLabel: `synaptic weight (${unitLabel(member.prepared.valueUnit)})`,
            series: [panelEntry],
            referenceLines: referenceLines([member], member.prepared.valueUnit)
          }],
          {
            xLabel: `time (${unitLabel(window.unit)})`,
            xDomain: [window.start, window.stop],
            showSamplePoints: parameters.showObservationMarkers === true,
            groupSeriesMarks: true,
            summaryStatements: ["This is a caller-declared aggregate; Cortexel did not receive its member observations and cannot re-derive it."]
          },
          skillId
        );
        return done(
          withContractTable(plan2, compileContext2, skillId, tableRows2),
          [tracePreparationBatchOperation(
            "weight",
            {
              skillId,
              dataContext: { ...data, aggregate: null },
              parameters,
              analysisWindow: window,
              observation: aggregateObservation,
              targetValueUnit: member.prepared.valueUnit
            },
            [{
              sourceIndex: 0,
              seriesIdentity: {
                kind: "declared_weight_aggregate",
                groupId: String(rawAggregate.groupId)
              },
              role: "declared_aggregate",
              inputPayload: rawAggregate,
              views: [{
                kind: "display",
                prepared: member.prepared,
                window,
                uncertaintyLower: member.uncertaintyLower,
                uncertaintyUpper: member.uncertaintyUpper
              }],
              transforms: declaredWeightPreparationBatchTransforms(member),
              ...eventMaterialization ? {
                materialization: {
                  full: eventMaterialization,
                  displayed: eventMaterialization
                }
              } : {},
              initialStatePainted: false,
              initialStateConsumedByDerivedAggregate: false
            }]
          )],
          facts2
        );
      }
      const rawSeries = arr(data.series) ?? [];
      if (rawSeries.length === 0) return { pending: rendererId };
      const derivedDisplay = display === "aggregate_derived" || display === "aggregate_derived_with_members";
      const membershipIds = new Set(
        (arr(rec(data.membership)?.members) ?? []).map((candidate) => String(rec(candidate)?.edgeId))
      );
      const targetRawSeries = derivedDisplay ? rawSeries.find((candidate) => membershipIds.has(String(rec(candidate)?.edgeId))) : rawSeries[0];
      const targetValueUnit = String(rec(rec(targetRawSeries)?.values)?.unit);
      const members = [];
      const operations = [];
      for (let index = 0; index < rawSeries.length; index++) {
        const raw = rec(rawSeries[index]) ?? {};
        const preparedOrError = prepareMember(
          raw,
          dataObservation,
          targetValueUnit,
          `/data/series/${index}`,
          true
        );
        if ("code" in preparedOrError) return { errors: [preparedOrError] };
        if ((display === "aggregate_derived" || display === "aggregate_derived_with_members") && rec(raw.uncertainty)?.kind !== "none") {
          return fail(
            "SCIENCE_UNCERTAINTY_UNSUPPORTED_FOR_SKILL",
            "science",
            "derived weight aggregates require uncertainty:none on every member; across-trial edge uncertainty cannot be propagated across synapses without an error model.",
            `/data/series/${index}/uncertainty`
          );
        }
        members.push(preparedOrError);
      }
      if (display === "individual" && members.length > CATEGORICAL_SERIES_STYLES.length) {
        return fail(
          "RENDER_SERIES_LIMIT_EXCEEDED",
          "render",
          `${members.length} individual synapses exceed the ${CATEGORICAL_SERIES_STYLES.length} registered distinguishable style tuples.`,
          "/data/series"
        );
      }
      const sourceRowPreflight = preflightPreparedWeightObservationRows(
        members,
        returnedTableRows
      );
      if (sourceRowPreflight.tag === "saturated_over_budget") {
        return refuseSaturatedWeightTable(sourceRowPreflight, "/data/series");
      }
      const aggregateGridPreflight = derivedDisplay ? prepareWeightAggregateGrid(
        data,
        parameters,
        window,
        members,
        returnedTableRows - sourceRowPreflight.exactRowCount
      ) : void 0;
      if (aggregateGridPreflight?.tag === "saturated_over_budget") {
        return refuseSaturatedWeightTable(
          {
            tag: "saturated_over_budget",
            saturatedRowCount: sourceRowPreflight.exactRowCount + aggregateGridPreflight.saturatedEvaluationCount
          },
          "/parameters/aggregate/evaluation"
        );
      }
      const preparedAggregateGrid = aggregateGridPreflight?.grid;
      const memberEventEvidence = /* @__PURE__ */ new Map();
      if (dataObservation.kind === "event_updated") {
        const semantics = dataObservation.updateSemantics === "value_before_update" ? "value_before_update" : "value_after_update";
        for (const member of members) {
          memberEventEvidence.set(
            member.prepared.id,
            eventUpdatedMaterialization(member, semantics, void 0, false)
          );
        }
      }
      const makeMemberEntry = (member, styleIndex, renderRuns, markerObservationIndexes) => {
        return {
          series: member.prepared,
          styleIndex,
          outputAuthority: weightTraceOutputAuthority(
            member.prepared,
            renderRuns,
            "series_paths",
            dataObservation.kind === "interpolated_trajectory" ? "reconstruction" : "observations",
            "uncertainty"
          ),
          uncertaintyLower: member.uncertaintyLower,
          uncertaintyUpper: member.uncertaintyUpper,
          uncertainty: member.uncertainty,
          renderRuns,
          ...markerObservationIndexes ? { markerObservationIndexes } : {},
          ...dataObservation.kind === "interpolated_trajectory" ? {
            authorityAtomKinds: {
              pathVertex: "reconstruction_vertex",
              sampleMarker: "reconstruction_marker",
              isolatedMarker: "isolated_reconstruction_marker"
            }
          } : {}
        };
      };
      let derived;
      let aggregateMember;
      if (display === "aggregate_derived" || display === "aggregate_derived_with_members") {
        if (!preparedAggregateGrid) {
          return fail(
            "INTERNAL_INVARIANT_VIOLATED",
            "internal",
            "derived weight display reached aggregate evaluation without an exact bounded grid preflight.",
            "/parameters/aggregate/evaluation"
          );
        }
        derived = deriveWeightAggregate(
          data,
          parameters,
          window,
          preparedAggregateGrid
        );
        aggregateMember = {
          raw: derived.record,
          prepared: derived.prepared,
          statePrepared: derived.prepared,
          uncertaintyLower: derived.uncertaintyLower,
          uncertaintyUpper: derived.uncertaintyUpper,
          stateUncertaintyLower: derived.uncertaintyLower,
          stateUncertaintyUpper: derived.uncertaintyUpper,
          uncertainty: derived.uncertainty,
          recordedStart: window.start,
          recordedStop: window.stop,
          recordedFinalEdgeInclusive: window.finalEdgeInclusive,
          stateStart: window.start,
          stateStop: window.stop,
          initialStateTime: window.start
        };
      }
      const selectedMembers = derived?.selectedMembers ?? [];
      const selectedIds = new Set(selectedMembers.map((member) => member.prepared.id));
      const paintedMemberIds = display === "individual" ? new Set(members.map((member) => member.prepared.id)) : display === "aggregate_derived_with_members" ? selectedIds : /* @__PURE__ */ new Set();
      const paintedEventEvidence = /* @__PURE__ */ new Map();
      if (dataObservation.kind === "event_updated") {
        const semantics = dataObservation.updateSemantics === "value_before_update" ? "value_before_update" : "value_after_update";
        for (const member of members) {
          if (!paintedMemberIds.has(member.prepared.id)) continue;
          paintedEventEvidence.set(
            member.prepared.id,
            display === "aggregate_derived_with_members" ? eventUpdatedMaterialization(
              member,
              semantics,
              derived?.membershipById.get(member.prepared.id),
              false
            ) : memberEventEvidence.get(member.prepared.id)
          );
        }
      }
      const sourceCarrierKind = dataObservation.kind === "interpolated_trajectory" ? "caller_reconstruction_point" : "source_observation";
      const tableEntries = [
        ...members.map((member) => ({
          member,
          observation: dataObservation,
          carrierKind: sourceCarrierKind,
          stateWitnesses: mergeWeightStateWitnesses(
            paintedMemberIds.has(member.prepared.id) ? paintedEventEvidence.get(member.prepared.id)?.witnesses : void 0,
            derived?.stateWitnessesByMember.get(member.prepared.id)
          ),
          initialStatePainted: paintedMemberIds.has(member.prepared.id) && paintedEventEvidence.get(member.prepared.id)?.initialStatePainted === true,
          initialStateConsumedByDerivedAggregate: derived?.initialStateContributorIds.has(member.prepared.id) === true,
          ...derived?.membershipById.get(member.prepared.id) ? { membershipLifetimes: derived.membershipById.get(member.prepared.id) } : {}
        })),
        ...aggregateMember ? [{
          member: aggregateMember,
          observation: dataObservation,
          carrierKind: "derived_aggregate_evaluation",
          counts: {
            memberCounts: [...derived.memberCounts],
            contributingCounts: [...derived.contributingCounts]
          }
        }] : []
      ];
      const rowPreflight = preflightWeightTableCarriers(tableEntries, returnedTableRows);
      if (rowPreflight.tag === "saturated_over_budget") {
        return refuseSaturatedWeightTable(rowPreflight, "/data");
      }
      const fullEventMaterializations = /* @__PURE__ */ new Map();
      const paintedEventMaterializations = /* @__PURE__ */ new Map();
      if (dataObservation.kind === "event_updated") {
        const semantics = dataObservation.updateSemantics === "value_before_update" ? "value_before_update" : "value_after_update";
        for (const member of members) {
          const full = eventUpdatedMaterialization(member, semantics);
          fullEventMaterializations.set(member.prepared.id, full);
          if (!paintedMemberIds.has(member.prepared.id)) continue;
          paintedEventMaterializations.set(
            member.prepared.id,
            display === "aggregate_derived_with_members" ? eventUpdatedMaterialization(
              member,
              semantics,
              derived?.membershipById.get(member.prepared.id)
            ) : full
          );
        }
      }
      const globalAvailabilityTransitions = derived ? weightAvailabilityTransitions(selectedMembers, derived.membershipById) : [];
      const memberRenderRuns = /* @__PURE__ */ new Map();
      const memberMarkerIndexes = /* @__PURE__ */ new Map();
      for (const member of members) {
        if (!paintedMemberIds.has(member.prepared.id)) continue;
        let rawRuns;
        if (dataObservation.kind === "event_updated") {
          const materialization = paintedEventMaterializations.get(member.prepared.id);
          rawRuns = materialization.runs;
          if (display === "aggregate_derived_with_members") {
            memberMarkerIndexes.set(
              member.prepared.id,
              markerIndexesForWeightRuns(member, materialization.runs)
            );
          }
        } else if (dataObservation.kind === "point_sample" && display === "aggregate_derived_with_members") {
          const materialization = pointSampleMembershipRenderRuns(
            member,
            derived?.membershipById.get(member.prepared.id) ?? [],
            globalAvailabilityTransitions
          );
          rawRuns = materialization.runs;
          memberMarkerIndexes.set(
            member.prepared.id,
            materialization.markerObservationIndexes
          );
        } else {
          rawRuns = interpolatedTrajectoryRenderRuns(
            member,
            dataObservation.kind === "interpolated_trajectory" ? "reconstruction_vertex" : "observation_vertex"
          );
        }
        memberRenderRuns.set(
          member.prepared.id,
          bindWeightRenderRunOrdinals(rawRuns)
        );
      }
      let aggregateRenderRuns;
      if (aggregateMember && derived) {
        const rawRuns = dataObservation.kind === "event_updated" ? eventUpdatedMaterialization(
          aggregateMember,
          dataObservation.updateSemantics === "value_before_update" ? "value_before_update" : "value_after_update"
        ).runs : pointSampleAggregateRenderRuns(
          derived.prepared,
          derived.uncertaintyLower,
          derived.uncertaintyUpper,
          unionWeightAvailability(selectedMembers, derived.membershipById),
          globalAvailabilityTransitions
        );
        aggregateRenderRuns = bindWeightRenderRunOrdinals(rawRuns);
      }
      const renderedRunsByEntry = /* @__PURE__ */ new Map();
      for (let index = 0; index < members.length; index++) {
        const runs = memberRenderRuns.get(members[index].prepared.id);
        if (runs) renderedRunsByEntry.set(tableEntries[index], runs);
      }
      if (aggregateRenderRuns && tableEntries.length > members.length) {
        renderedRunsByEntry.set(tableEntries[tableEntries.length - 1], aggregateRenderRuns);
      }
      const table = synapticWeightTraceTableRows(
        data,
        parameters,
        rowPreflight,
        renderedRunsByEntry
      );
      const tableRows = table.rows;
      if (tableRows.length !== rowPreflight.exactRowCount) {
        return fail(
          "INTERNAL_INVARIANT_VIOLATED",
          "internal",
          `weight table materialized ${tableRows.length} rows after an exact ${rowPreflight.exactRowCount}-row canonical carrier preflight.`,
          "/data"
        );
      }
      const panelSeries = display === "individual" ? members.map((member, index) => makeMemberEntry(
        member,
        index,
        memberRenderRuns.get(member.prepared.id),
        memberMarkerIndexes.get(member.prepared.id)
      )) : [
        ...display === "aggregate_derived_with_members" ? selectedMembers.map((member, index) => ({
          ...makeMemberEntry(
            member,
            index,
            memberRenderRuns.get(member.prepared.id),
            memberMarkerIndexes.get(member.prepared.id)
          ),
          style: { color: "#9ca3af", dash: "none", marker: "circle" },
          includeInLegend: false
        })) : [],
        ...aggregateMember ? [{
          ...makeMemberEntry(
            aggregateMember,
            0,
            aggregateRenderRuns
          )
        }] : []
      ];
      if (!panelSeries.some((entry) => weightPanelSeriesHasPaintableCarrier(
        entry,
        parameters.showObservationMarkers === true
      ))) {
        return fail(
          "RENDER_NO_DATA",
          "render",
          "the selected display has no finite retained point, reconstructed vertex, event-held state, carry-in state, or painted initial state in the requested window.",
          "/data"
        );
      }
      const preparationBatch = tracePreparationBatchOperation(
        "weight",
        {
          skillId,
          dataContext: { ...data, series: null },
          parameters,
          analysisWindow: window,
          observation: dataObservation,
          targetValueUnit
        },
        members.map((member, sourceIndex) => {
          const fullMaterialization = fullEventMaterializations.get(member.prepared.id);
          const displayedMaterialization = paintedEventMaterializations.get(member.prepared.id);
          const stateWitnesses = mergeWeightStateWitnesses(
            paintedMemberIds.has(member.prepared.id) ? displayedMaterialization?.witnesses : void 0,
            derived?.stateWitnessesByMember.get(member.prepared.id)
          );
          const recordedInterval = rec(member.raw.recordedInterval) ?? {};
          return {
            sourceIndex,
            seriesIdentity: {
              kind: "weight_member",
              edgeId: String(member.raw.edgeId)
            },
            role: "source_series",
            inputPayload: member.raw,
            views: [
              {
                kind: "display",
                prepared: member.prepared,
                window: {
                  start: member.recordedStart,
                  stop: member.recordedStop,
                  unit: window.unit,
                  finalEdgeInclusive: member.recordedFinalEdgeInclusive
                },
                uncertaintyLower: member.uncertaintyLower,
                uncertaintyUpper: member.uncertaintyUpper
              },
              {
                kind: "state",
                prepared: member.statePrepared,
                window: {
                  start: member.stateStart,
                  stop: member.stateStop,
                  unit: window.unit,
                  finalEdgeInclusive: typeof recordedInterval.boundary === "string" && recordedInterval.boundary.endsWith("]")
                },
                uncertaintyLower: member.stateUncertaintyLower,
                uncertaintyUpper: member.stateUncertaintyUpper
              }
            ],
            transforms: weightPreparationBatchTransforms(
              member,
              parameters.showReferenceLines === true && paintedMemberIds.has(member.prepared.id)
            ),
            contextWitnesses: stateWitnesses,
            ...dataObservation.kind === "event_updated" && fullMaterialization ? {
              materialization: {
                full: fullMaterialization,
                displayed: displayedMaterialization ?? null
              }
            } : {},
            initialStatePainted: displayedMaterialization?.initialStatePainted === true,
            initialStateConsumedByDerivedAggregate: derived?.initialStateContributorIds.has(member.prepared.id) === true
          };
        })
      );
      operations.push(preparationBatch);
      if (derived) {
        operations.push(chainAggregateToPreparationBatch(
          derived.operation,
          preparationBatch
        ));
      }
      const displayedMembers = display === "individual" ? members : [
        ...display === "aggregate_derived_with_members" ? selectedMembers : [],
        ...aggregateMember ? [aggregateMember] : []
      ];
      const referenceMembers = display === "individual" ? members : display === "aggregate_derived_with_members" ? selectedMembers : [];
      const modelMembers = display === "individual" ? members : selectedMembers;
      const sourceMembers = sourceCarrierKind === "source_observation" ? members : [];
      const reconstructionMembers = sourceCarrierKind === "caller_reconstruction_point" ? members : [];
      const facts = weightDisclosureFacts(displayedMembers, panelSeries);
      const compileContext = traceCompilerContext(
        context(tableRows.length, facts),
        skillId,
        summaryFacts(
          display === "individual" ? members : selectedMembers,
          sourceMembers,
          reconstructionMembers,
          displayedMembers,
          modelMembers,
          aggregateMember,
          table.carrierCounts,
          table.sourceReadingCount,
          table.missingSourceReadingCount,
          referenceMembers
        )
      );
      const plan = compileTraceFigure(
        compileContext,
        [{
          id: "weights",
          label: display === "individual" ? "Identified synapses" : String(rec(data.membership)?.groupLabel ?? rec(data.membership)?.groupId ?? "Declared aggregate"),
          yLabel: `synaptic weight (${unitLabel(targetValueUnit)})`,
          series: panelSeries,
          referenceLines: referenceLines(referenceMembers, targetValueUnit)
        }],
        {
          xLabel: `time (${unitLabel(window.unit)})`,
          xDomain: [window.start, window.stop],
          showSamplePoints: parameters.showObservationMarkers === true,
          groupSeriesMarks: true,
          summaryStatements: [
            `Observation semantics: ${String(dataObservation.kind)}${typeof dataObservation.updateSemantics === "string" ? `, ${dataObservation.updateSemantics}` : ""}.`,
            ...derived ? [`Aggregate denominator is contributingCount at each evaluation time; zero contributors yield a missing value.`] : []
          ]
        },
        skillId
      );
      return done(
        withContractTable(plan, compileContext, skillId, tableRows),
        operations,
        facts
      );
    } catch (error) {
      return { errors: [tracePreparationError(error, skillId, "/data")] };
    }
  }
  if (skillId === "neuro.spike_raster") {
    let partition;
    try {
      partition = exactSpikeWindowPartition(data);
    } catch (error) {
      return fail(
        "SCIENCE_NUMERIC_RESOLUTION_UNREPRESENTABLE",
        "science",
        error instanceof Error ? error.message : "the event window cannot be represented on the spike-raster display axis.",
        "/data/window"
      );
    }
    const {
      times,
      timeUnit,
      windowUnit,
      nestOriginRelative,
      lowerTerms,
      upperTerms,
      boundary,
      displayStart,
      displayStop,
      inWindow: inWindow2,
      excludedBelow,
      excludedAbove,
      excludedCount,
      acceptedCount
    } = partition;
    const senders = strings4(data.eventSenderIds);
    const recorded = strings4(data.recordedSenderIds);
    const eventTrials = arr(data.eventTrialIds) === void 0 ? void 0 : strings4(data.eventTrialIds);
    const trialIds = strings4(data.trialIds);
    const suppliedEventIds = arr(data.eventIds) === void 0 ? void 0 : strings4(data.eventIds);
    const senderPopulationIds = strings4(data.senderPopulationIds);
    const outOfWindowPolicy = parameters.outOfWindowPolicy;
    if (excludedCount > 0 && outOfWindowPolicy !== "exclude_and_disclose") {
      return fail(
        "SCIENCE_EVENT_OUT_OF_WINDOW",
        "science",
        `${excludedCount} events are outside the declared ${boundary} window; a validated reject-policy request must not reach rendering.`,
        "/data/eventTimes/values"
      );
    }
    const populationBySender = /* @__PURE__ */ new Map();
    for (let index = 0; index < recorded.length; index++) {
      if (senderPopulationIds[index] !== void 0) {
        populationBySender.set(recorded[index], senderPopulationIds[index]);
      }
    }
    let orderedSenders = [...recorded];
    if (parameters.rowOrder === "canonical_sender_id") {
      orderedSenders.sort(compareUnicodeCodePoints3);
    } else if (parameters.rowOrder === "grouped_by_population" && senderPopulationIds.length > 0) {
      const sendersByPopulation = /* @__PURE__ */ new Map();
      for (const senderId of recorded) {
        const populationId = populationBySender.get(senderId);
        if (populationId === void 0) continue;
        const populationSenders = sendersByPopulation.get(populationId);
        if (populationSenders === void 0) {
          sendersByPopulation.set(populationId, [senderId]);
        } else {
          populationSenders.push(senderId);
        }
      }
      orderedSenders = [];
      for (const populationSenders of sendersByPopulation.values()) {
        for (const senderId of populationSenders) orderedSenders.push(senderId);
      }
    }
    const rowKey = (senderId, trialId) => JSON.stringify([senderId, trialId]);
    const rows = orderedSenders.flatMap((senderId) => {
      const trials = eventTrials ? trialIds : [null];
      return trials.map((trialId) => ({
        key: rowKey(senderId, trialId),
        label: trialId === null ? senderId : `${senderId} / ${trialId}`
      }));
    });
    const rowIndexByKey = new Map(rows.map((row, index) => [row.key, index]));
    const rowLabelByKey = new Map(rows.map((row) => [row.key, row.label]));
    const events = times.map((time, sourceOrdinal) => {
      const senderId = senders[sourceOrdinal];
      const trialId = eventTrials?.[sourceOrdinal] ?? null;
      const key = rowKey(senderId, trialId);
      const rowIndex = rowIndexByKey.get(key);
      const rowLabel = rowLabelByKey.get(key);
      if (rowIndex === void 0 || rowLabel === void 0) {
        throw new Error(
          `event ${sourceOrdinal} does not resolve to a row in the complete declared sender/trial universe`
        );
      }
      return {
        sourceOrdinal,
        eventId: suppliedEventIds?.[sourceOrdinal] ?? `source-ordinal-${sourceOrdinal}`,
        time,
        timeUnit,
        senderId,
        trialId,
        populationId: populationBySender.get(senderId) ?? null,
        rowKey: key,
        rowIndex,
        rowLabel,
        inWindow: inWindow2[sourceOrdinal]
      };
    });
    events.sort((left, right) => left.time < right.time ? -1 : left.time > right.time ? 1 : left.rowIndex !== right.rowIndex ? left.rowIndex - right.rowIndex : left.sourceOrdinal - right.sourceOrdinal);
    const conversion = windowUnit !== timeUnit ? conversionReceipt(windowUnit, timeUnit) : void 0;
    const derivedFacts = {
      ...excludedCount > 0 ? { excludedOutOfWindow: excludedCount } : {},
      ...conversion ? { unitConversions: [conversionDisclosureText("spike-raster window endpoints", conversion)] } : {}
    };
    const operation = {
      ...derivationOperation(
        "spike_raster.partition_and_rows",
        "cortexel.spike_raster.exact_window_partition",
        {
          boundary,
          lowerEndpointTerms: lowerTerms,
          upperEndpointTerms: upperTerms,
          displayUnit: timeUnit,
          rowOrder: parameters.rowOrder,
          markStyle: parameters.markStyle ?? "tick",
          outOfWindowPolicy,
          eventIdentityPolicy: suppliedEventIds ? "caller_declared_unique_event_ids" : "source_ordinal_v1"
        },
        data,
        { events, rows },
        {
          sourceEventCount: times.length,
          acceptedEventCount: acceptedCount,
          excludedOutOfWindow: excludedCount,
          excludedBelowOrAtOpenStart: excludedBelow,
          excludedAboveOrAtOpenStop: excludedAbove,
          displayStart,
          displayStop,
          displayUnit: timeUnit,
          rowCount: rows.length,
          drawnMarkCount: acceptedCount,
          stableSort: ["time", "rowIndex", "sourceOrdinal"],
          sourceClockMode: nestOriginRelative ? "nest_3_9_or_3_10_memory_native_binary64_ms" : "caller_declared_event_window",
          ...conversion ? { windowEndpointConversion: conversion } : {}
        }
      ),
      algorithmRevision: 2
    };
    const axisPrefix = data.timeBase === "trial_relative" ? `time relative to ${String(data.alignmentLabel)}` : "time";
    const activeSenders = new Set(
      events.filter((event) => event.inWindow).map((event) => event.senderId)
    );
    const rasterContext = distributionCompilerContext(
      context(events.length, derivedFacts),
      skillId,
      {
        eventCount: distributionCompilerNumber(events.length),
        excludedCount: distributionCompilerNumber(excludedCount),
        activeSenderCount: distributionCompilerNumber(activeSenders.size),
        recordedSenderCount: distributionCompilerNumber(recorded.length),
        trialCount: distributionCompilerNumber(eventTrials ? trialIds.length : 1),
        windowStart: distributionCompilerNumber(displayStart),
        windowStop: distributionCompilerNumber(displayStop),
        timeUnit,
        windowBoundary: boundary,
        timeBase: String(data.timeBase),
        rowCount: distributionCompilerNumber(rows.length),
        rowOrder: String(parameters.rowOrder),
        senderUniverseComplete: String(data.senderUniverseComplete),
        markCount: distributionCompilerNumber(acceptedCount)
      }
    );
    return done(
      compileRasterFigure(
        rasterContext,
        events,
        rows,
        displayStart,
        displayStop,
        `${axisPrefix} (${unitLabel(timeUnit)}); ${boundary}`,
        parameters.markStyle === "point" ? "point" : "tick",
        skillId
      ),
      [operation],
      derivedFacts
    );
  }
  if (skillId === "neuro.correlogram") {
    const statistic = String(parameters.statistic);
    const edgeCorrection = String(parameters.edgeCorrection);
    if (statistic !== "raw_pair_count" && statistic !== "target_rate_per_reference_event") {
      return fail(
        "RENDER_UNSUPPORTED_SKILL",
        "render",
        `correlogram statistic ${statistic} is outside the closed revision-2 product.`,
        "/parameters/statistic"
      );
    }
    if (statistic === "raw_pair_count" && edgeCorrection !== "none" || statistic === "target_rate_per_reference_event" && edgeCorrection !== "none" && edgeCorrection !== "eligible_reference_events") {
      return fail(
        "SCIENCE_CORRELATION_DENOMINATOR_INVALID",
        "science",
        `${statistic} cannot use edge correction ${edgeCorrection}.`,
        "/parameters/edgeCorrection"
      );
    }
    try {
      const mode = String(data.mode);
      const preBinned = mode === "prebinned_auto" || mode === "prebinned_cross";
      const rawInputs = eventCorrelogramInputs(data, parameters);
      if (!preBinned && !rawInputs) {
        return fail(
          "RENDER_UNSUPPORTED_SKILL",
          "render",
          `correlogram data mode ${mode} is outside the four closed revision-2 modes.`,
          "/data/mode"
        );
      }
      const axis = rawInputs ?? correlogramAxis2(parameters);
      const edges = [...axis.spec.edges];
      const centers = [...axis.centers];
      let pairCounts;
      let kind;
      let referenceEventCount;
      let targetEventCount;
      let referenceLabel;
      let targetLabel;
      let referenceRecordedSenderCount;
      let targetRecordedSenderCount;
      let window;
      let pairAccounting;
      let zeroLagRetainedDistinctPairs;
      let unitConversions = [...axis.unitConversions];
      if (rawInputs) {
        const result = computeCorrelogram(
          rawInputs.ref,
          rawInputs.tgt,
          rawInputs.spec,
          rawInputs.kind,
          pairwiseOperations
        );
        pairCounts = result.counts;
        kind = rawInputs.kind;
        referenceEventCount = rawInputs.ref.length;
        targetEventCount = kind === "auto" ? referenceEventCount : rawInputs.tgt.length;
        referenceLabel = rawInputs.referenceLabel;
        targetLabel = rawInputs.targetLabel;
        referenceRecordedSenderCount = rawInputs.referenceRecordedSenderCount;
        targetRecordedSenderCount = rawInputs.targetRecordedSenderCount;
        window = rawInputs.window;
        pairAccounting = result.receipt.pairAccounting;
        zeroLagRetainedDistinctPairs = result.zeroLagRetainedDistinctPairs;
        unitConversions = [...rawInputs.unitConversions];
      } else {
        kind = mode === "prebinned_auto" ? "auto" : "cross";
        pairCounts = numbers4(data.pairCounts);
        referenceEventCount = num(data.referenceEventCount);
        targetEventCount = kind === "auto" ? referenceEventCount : num(data.targetEventCount);
        const referenceTrain = rec(kind === "auto" ? data.train : data.referenceTrain) ?? {};
        const targetTrain = kind === "auto" ? referenceTrain : rec(data.targetTrain) ?? {};
        referenceLabel = String(referenceTrain.label);
        targetLabel = String(targetTrain.label);
        referenceRecordedSenderCount = strings4(referenceTrain.recordedSenderIds).length;
        targetRecordedSenderCount = strings4(targetTrain.recordedSenderIds).length;
        const windowRecord = rec(data.window) ?? {};
        window = {
          start: num(windowRecord.start),
          stop: num(windowRecord.stop),
          unit: String(windowRecord.unit),
          boundary: windowRecord.boundary
        };
        const countedPairCount = pairCounts.reduce((sum, count) => sum + count, 0);
        if (!Number.isSafeInteger(countedPairCount)) {
          throw new RangeError("pre-binned correlogram counted-pair sum exceeds the exact JSON integer domain");
        }
        pairAccounting = deriveCorrelogramPairAccounting(
          referenceEventCount,
          targetEventCount,
          kind,
          countedPairCount
        );
        const callerEdgeUnit = String(rec(data.binEdges)?.unit);
        if (callerEdgeUnit !== axis.lagUnit) {
          unitConversions.push(conversionDisclosureText(
            "pre-binned correlogram lag edges",
            conversionReceipt(callerEdgeUnit, axis.lagUnit)
          ));
        }
      }
      if (pairCounts.length !== centers.length) {
        throw new Error(
          `correlogram has ${pairCounts.length} pair counts for ${centers.length} materialized lag bins`
        );
      }
      let eligibleReferenceEvents;
      let denominators;
      let values;
      let valueStatuses;
      const valueUnit = statistic === "raw_pair_count" ? "1" : "Hz";
      if (statistic === "raw_pair_count") {
        eligibleReferenceEvents = pairCounts.map(() => null);
        denominators = pairCounts.map(() => null);
        values = [...pairCounts];
        valueStatuses = pairCounts.map(() => "defined");
      } else {
        const exactEligible = edgeCorrection === "none" ? pairCounts.map(() => referenceEventCount) : rawInputs ? deriveEligibleCorrelogramReferenceCounts(
          rawInputs.referenceTimesInSourceUnit,
          rawInputs.referenceTimeUnit,
          edges,
          axis.lagUnit,
          rawInputs.window
        ) : numbers4(data.eligibleReferenceEventCounts);
        const rateBins = deriveCorrelogramTargetRates(
          pairCounts,
          exactEligible,
          axis.binWidth
        );
        eligibleReferenceEvents = rateBins.map((bin) => bin.eligibleReferenceEvents);
        denominators = rateBins.map((bin) => bin.denominatorSeconds);
        values = rateBins.map((bin) => bin.value);
        valueStatuses = rateBins.map((bin) => bin.status);
      }
      const undefinedRateBinCount = valueStatuses.filter(
        (status) => status === "undefined_zero_eligible_reference_events"
      ).length;
      const observationDuration = convertDifference(
        window.start,
        window.stop,
        window.unit,
        window.unit
      );
      const denominatorStatement = statistic === "raw_pair_count" ? "none" : edgeCorrection === "none" ? "reference event count multiplied by the typed bin width in seconds" : "the per-bin eligible-reference count multiplied by the typed bin width in seconds; zero exposure yields null";
      const sourceAuthorityStatement = preBinned ? "Source event counts and exact pair numerators were declared by the pre-binned product; duration was derived from its window" : "Source event counts were derived from the explicit event arrays, and duration was derived from their shared window";
      const summaryStatements = [
        `Correlogram (${kind}): target ${targetLabel} relative to reference ${referenceLabel}. Positive lag means target follows reference. Declared senders, including silent: ${referenceRecordedSenderCount} reference and ${targetRecordedSenderCount} target.`,
        `${centers.length} centred left-closed/right-open bins of ${exactNumberText(axis.binWidthInLagUnit)} ${unitLabel(axis.lagUnit) || axis.lagUnit}, from centre ${exactNumberText(centers[0])} through ${exactNumberText(centers[centers.length - 1])}; the positive outer edge is excluded.`,
        `${statistic} (${valueUnit}); denominator: ${denominatorStatement}. ${undefinedRateBinCount} rate bins are null because their eligible-reference count is zero.`,
        `Events: ${referenceEventCount} reference and ${targetEventCount} target over ${exactNumberText(observationDuration)} ${unitLabel(window.unit) || window.unit}, boundary ${window.boundary}. ${sourceAuthorityStatement}`,
        `Exact pair accounting: ${pairAccounting.candidatePairCount} candidate = ${pairAccounting.countedPairCount} in-range + ${pairAccounting.outOfRangePairCount} out-of-range + ${pairAccounting.sameEventSelfPairCountExcluded} same-event self-pairs excluded.`,
        "Uncertainty kind none: no uncertainty interval is estimated or drawn.",
        ...pairAccounting.countedPairCount === 0 ? ["The all-zero pair statistic is reported without interpreting it as evidence of independence."] : []
      ];
      const tableRows = pairCounts.map((pairCount, index) => contractOrderedTableRow(skillId, {
        lagBinStart: edges[index],
        lagBinCenter: centers[index],
        lagBinEnd: edges[index + 1],
        pairCount,
        eligibleReferenceEvents: eligibleReferenceEvents[index],
        denominator: denominators[index],
        value: values[index],
        valueUnit,
        valueStatus: valueStatuses[index],
        uncertaintyLower: null,
        uncertaintyUpper: null
      }));
      const derivedFacts = {
        ...preBinned ? { preBinned: true } : {},
        ...unitConversions.length > 0 ? { unitConversions } : {},
        ...undefinedRateBinCount > 0 ? { missingValueCount: undefinedRateBinCount } : {}
      };
      const compileContext = distributionCompilerContext(
        context(tableRows.length, derivedFacts),
        skillId,
        {
          correlationKind: kind,
          targetLabel,
          referenceLabel,
          referenceRecordedSenderCount: distributionCompilerNumber(referenceRecordedSenderCount),
          targetRecordedSenderCount: distributionCompilerNumber(targetRecordedSenderCount),
          binCount: distributionCompilerNumber(pairCounts.length),
          binWidth: distributionCompilerNumber(axis.binWidthInLagUnit),
          lagUnit: axis.lagUnit,
          lagMin: distributionCompilerNumber(centers[0]),
          lagMax: distributionCompilerNumber(centers[centers.length - 1]),
          statistic,
          valueUnit,
          denominatorStatement,
          referenceEventCount: distributionCompilerNumber(referenceEventCount),
          targetEventCount: distributionCompilerNumber(targetEventCount),
          observationDuration: distributionCompilerNumber(observationDuration),
          timeUnit: window.unit,
          sourceAuthorityStatement,
          candidatePairCount: distributionCompilerNumber(pairAccounting.candidatePairCount),
          countedPairCount: distributionCompilerNumber(pairAccounting.countedPairCount),
          outOfRangePairCount: distributionCompilerNumber(pairAccounting.outOfRangePairCount),
          sameEventSelfPairCountExcluded: distributionCompilerNumber(
            pairAccounting.sameEventSelfPairCountExcluded
          ),
          undefinedRateBinCount: distributionCompilerNumber(undefinedRateBinCount),
          uncertaintyStatement: "No uncertainty interval is estimated or drawn."
        }
      );
      const geometry = compileNullableCorrelogramStemFigure(
        compileContext,
        edges,
        centers,
        values,
        `lag (${unitLabel(axis.lagUnit) || axis.lagUnit})`,
        statistic === "raw_pair_count" ? "pair count" : "target rate per reference event (Hz)",
        skillId
      );
      const summarizedGeometry = {
        ...geometry,
        accessibility: {
          ...geometry.accessibility,
          panelSummaries: summaryStatements
        }
      };
      const operation = derivationOperation(
        "correlogram.pair_count_and_rate",
        "cortexel.correlogram.exact_centered_pair_ladder",
        {
          mode,
          kind,
          statistic,
          edgeCorrection,
          lagConvention: "target_time_minus_reference_time",
          binBoundary: "left_closed_right_open",
          positiveOuterEdge: "excluded",
          binWidth: axis.binWidth,
          lagUnit: axis.lagUnit
        },
        data,
        {
          edges,
          centers,
          pairCounts,
          eligibleReferenceEvents,
          denominatorsSeconds: denominators,
          values,
          valueUnit,
          valueStatuses
        },
        {
          sourceAuthority: preBinned ? "prebinned_exact_pair_counts_and_declared_role_event_counts" : "explicit_raw_event_arrays",
          referenceEventCount,
          targetEventCount,
          referenceRecordedSenderCount,
          targetRecordedSenderCount,
          observationDuration,
          observationDurationUnit: window.unit,
          observationWindowBoundary: window.boundary,
          undefinedRateBinCount,
          ...pairAccounting,
          ...zeroLagRetainedDistinctPairs === void 0 ? {} : { zeroLagRetainedDistinctPairs }
        }
      );
      return done(
        withContractTable(summarizedGeometry, compileContext, skillId, tableRows),
        [operation],
        derivedFacts
      );
    } catch (error) {
      if (error instanceof PairwiseBudgetExceededError) {
        return fail(
          "RESOURCE_PAIRWISE_EXCEEDED",
          "budget",
          `correlogram pair formation exceeded the active limit of ${error.limit}.`,
          "/data"
        );
      }
      return fail(
        "SCIENCE_NUMERIC_RESOLUTION_UNREPRESENTABLE",
        "science",
        error instanceof Error ? error.message : "correlogram derivation failed",
        "/data"
      );
    }
  }
  if (rendererId === "figure.distribution") {
    if (skillId === "neuro.isi_distribution") {
      const resolvedBins = declaredBins(rec(parameters.bins));
      if (!resolvedBins) return { pending: rendererId };
      try {
        const window = rec(data.window) ?? {};
        const windowInput = {
          start: num(window.start),
          stop: num(window.stop),
          unit: String(window.unit),
          boundary: "[start,stop)"
        };
        const bins = {
          edges: resolvedBins.spec.edges,
          unit: resolvedBins.unit,
          finalEdgeInclusive: resolvedBins.spec.finalEdgeInclusive
        };
        const normalization = String(parameters.normalization ?? "count");
        const outOfRangePolicy = String(
          parameters.outOfRangeIntervals ?? "reject"
        );
        const zeroIntervalPolicy = parameters.zeroIntervalPolicy === "retain_as_zero" ? "retain_as_zero" : "reject";
        const recordedSenderIds = strings4(data.recordedSenderIds);
        const trialIds = arr(data.trialIds) === void 0 ? void 0 : strings4(data.trialIds);
        let sourceUnit;
        let excludedOutOfWindowCount;
        const result = data.mode === "intervals" ? (() => {
          const intervalRecord = rec(data.intervals) ?? {};
          sourceUnit = String(intervalRecord.unit ?? resolvedBins.unit);
          excludedOutOfWindowCount = null;
          return deriveIsiFromIntervals({
            intervals: numbers4(intervalRecord.values),
            intervalSenderIds: strings4(data.intervalSenderIds),
            ...arr(data.intervalTrialIds) === void 0 ? {} : { intervalTrialIds: strings4(data.intervalTrialIds) },
            trains: (arr(data.trains) ?? []).map((entry) => {
              const train = rec(entry) ?? {};
              return {
                senderId: String(train.senderId),
                ...typeof train.trialId === "string" ? { trialId: train.trialId } : {},
                spikeCount: num(train.spikeCount)
              };
            }),
            recordedSenderIds,
            ...trialIds ? { trialIds } : {},
            intervalUnit: sourceUnit,
            window: windowInput,
            bins,
            normalization,
            zeroIntervalPolicy,
            outOfRangePolicy
          });
        })() : (() => {
          const timeRecord = rec(data.eventTimes) ?? {};
          sourceUnit = String(timeRecord.unit ?? resolvedBins.unit);
          excludedOutOfWindowCount = 0;
          return deriveIsiFromEvents({
            eventTimes: numbers4(timeRecord.values),
            eventSenderIds: strings4(data.eventSenderIds),
            ...arr(data.eventTrialIds) === void 0 ? {} : { eventTrialIds: strings4(data.eventTrialIds) },
            recordedSenderIds,
            ...trialIds ? { trialIds } : {},
            intervalUnit: sourceUnit,
            window: windowInput,
            bins,
            normalization,
            zeroIntervalPolicy,
            outOfRangePolicy
          });
        })();
        const histogram = result.histogram.groups[0];
        if (parameters.xScale === "log" && (resolvedBins.spec.edges.some((edge) => !(edge > 0)) || result.zeroIntervalCount > 0)) {
          return fail(
            "RENDER_LOG_SCALE_NONPOSITIVE_DOMAIN",
            "render",
            "a logarithmic ISI axis requires strictly positive bin edges and intervals.",
            "/parameters/bins"
          );
        }
        const yLabel = histogramYAxisLabel(normalization, resolvedBins.unit);
        const intervalConversion = sourceUnit !== resolvedBins.unit ? conversionReceipt(sourceUnit, resolvedBins.unit) : void 0;
        const operation = derivationOperation(
          "isi.within_train_histogram",
          "cortexel.isi.within_train_histogram",
          {
            bins: resolvedBins,
            normalization,
            zeroIntervalPolicy: parameters.zeroIntervalPolicy,
            outOfRangeIntervals: parameters.outOfRangeIntervals
          },
          data,
          { counts: histogram.counts, values: histogram.values },
          {
            formedIntervalCount: result.intervalCount,
            binnedIntervalCount: histogram.binnedObservationCount,
            underRangeCount: histogram.underRangeCount,
            overRangeCount: histogram.overRangeCount,
            trainCount: result.trainCount,
            trainsWithoutIntervalCount: result.trainsWithoutIntervalCount,
            spikeCount: result.spikeCount,
            zeroIntervalCount: result.zeroIntervalCount,
            ...intervalConversion ? { intervalConversion } : {}
          }
        );
        const excludedIntervalCount = histogram.underRangeCount + histogram.overRangeCount;
        const rangeMetadata = excludedIntervalCount > 0 ? [
          { key: "formedIntervalCount", header: "Formed intervals", value: result.intervalCount },
          { key: "binnedIntervalCount", header: "Binned intervals", value: histogram.binnedObservationCount },
          { key: "underRangeCount", header: "Under range", value: histogram.underRangeCount },
          { key: "overRangeCount", header: "Over range", value: histogram.overRangeCount }
        ] : [];
        const rangeSummary = [
          `Exact conservation: ${result.intervalCount} formed intervals = ${histogram.binnedObservationCount} binned + ${histogram.underRangeCount} below range + ${histogram.overRangeCount} above range, across ${result.trainCount} declared trains and ${result.spikeCount} spikes.`
        ];
        const derivedFacts = intervalConversion ? { unitConversions: [conversionDisclosureText("inter-spike intervals", intervalConversion)] } : {};
        const densityUnit = reciprocalUnit(resolvedBins.unit);
        const tableRows = histogram.counts.map((count, index) => [
          resolvedBins.spec.edges[index],
          resolvedBins.spec.edges[index + 1],
          sameUnitDifference(
            resolvedBins.spec.edges[index],
            resolvedBins.spec.edges[index + 1],
            resolvedBins.unit
          ),
          resolvedBins.unit,
          count,
          normalization === "probability" ? histogram.values[index] : null,
          normalization === "density" ? histogram.values[index] : null,
          normalization === "density" ? densityUnit ?? null : null,
          histogram.binnedObservationCount,
          result.intervalCount,
          histogram.underRangeCount,
          histogram.overRangeCount,
          result.trainCount,
          result.spikeCount,
          excludedOutOfWindowCount
        ]);
        const isiWindowDuration = convertDifference(
          windowInput.start,
          windowInput.stop,
          windowInput.unit,
          windowInput.unit
        );
        const compileContext = distributionCompilerContext(
          context(tableRows.length, derivedFacts),
          skillId,
          {
            selectionLabel: String(parameters.selectionLabel ?? parameters.selectionId),
            intervalCount: distributionCompilerNumber(result.intervalCount),
            trainCount: distributionCompilerNumber(result.trainCount),
            senderCount: distributionCompilerNumber(recordedSenderIds.length),
            trialCount: distributionCompilerNumber(trialIds ? trialIds.length : 1),
            spikeCount: distributionCompilerNumber(result.spikeCount),
            windowStart: distributionCompilerNumber(windowInput.start),
            windowStop: distributionCompilerNumber(windowInput.stop),
            timeUnit: windowInput.unit,
            trainsWithoutIntervalCount: distributionCompilerNumber(
              result.trainsWithoutIntervalCount
            ),
            excludedOutOfWindowCount: excludedOutOfWindowCount === null ? "not retained by interval input" : distributionCompilerNumber(excludedOutOfWindowCount),
            binCount: distributionCompilerNumber(histogram.counts.length),
            binMin: distributionCompilerNumber(resolvedBins.spec.edges[0]),
            binMax: distributionCompilerNumber(
              resolvedBins.spec.edges[resolvedBins.spec.edges.length - 1]
            ),
            intervalUnit: resolvedBins.unit,
            xScale: String(parameters.xScale ?? "linear"),
            underRangeCount: distributionCompilerNumber(histogram.underRangeCount),
            overRangeCount: distributionCompilerNumber(histogram.overRangeCount),
            normalization,
            valueUnit: normalization === "density" ? String(densityUnit) : "1",
            windowDuration: distributionCompilerNumber(isiWindowDuration),
            zeroIntervalStatement: result.zeroIntervalCount > 0 ? `${result.zeroIntervalCount} exact zero intervals were retained by the declared policy.` : "No exact zero interval was observed."
          }
        );
        return done(
          withContractTable(compileBarFigure(
            compileContext,
            resolvedBins.spec.edges,
            histogram.values,
            `ISI (${unitLabel(resolvedBins.unit)})`,
            yLabel,
            skillId,
            parameters.xScale === "log" ? "log" : "linear",
            {
              tableMetadata: rangeMetadata,
              summaryStatements: rangeSummary,
              outputAuthority: { classId: "bins", groupId: "all" }
            }
          ), compileContext, skillId, tableRows),
          [operation],
          derivedFacts
        );
      } catch (error) {
        if (error instanceof DistributionDerivationError) return failDistribution(error);
        const message = error instanceof Error ? error.message : "ISI derivation failed";
        if (error instanceof EmptyHistogramNormalizationError) {
          return fail("RENDER_NO_DATA", "render", message, "/data");
        }
        const numericResolutionFailure = message.includes("overflows") || message.includes("unrepresentable") || message.includes("rounds across") || message.includes("rounded") || message.includes("safe-integer");
        return fail(
          numericResolutionFailure ? "SCIENCE_NUMERIC_RESOLUTION_UNREPRESENTABLE" : "INTERNAL_INVARIANT_VIOLATED",
          numericResolutionFailure ? "science" : "render",
          message,
          "/data"
        );
      }
    }
    if (skillId === "network.degree_distribution") {
      try {
        const ids = strings4(rec(data.nodeUniverse)?.ids);
        const conns = rec(data.connections);
        const nodeDegrees = rec(data.nodeDegrees);
        const direction = parameters.direction ?? "in";
        const countingPolicy = parameters.countingPolicy ?? "count_edges";
        const autapsePolicy = parameters.autapsePolicy === "exclude" ? "exclude" : "include";
        const normalization = parameters.normalization === "probability" ? "probability" : "count";
        const result = deriveDegreeDistribution({
          nodeIds: ids,
          ...data.mode === "node_degrees" ? {
            suppliedNodeIds: strings4(nodeDegrees?.nodeIds),
            suppliedDegrees: numbers4(nodeDegrees?.degrees),
            suppliedCountedConnectionCount: num(data.countedConnectionCount),
            suppliedCountedIncidenceCount: num(data.countedIncidenceCount),
            suppliedExcludedAutapseCount: num(data.excludedAutapseCount)
          } : {
            sourceIds: strings4(conns?.sourceIds),
            targetIds: strings4(conns?.targetIds)
          },
          direction,
          countingPolicy,
          autapsePolicy,
          binning: { mode: "per_integer_degree" },
          normalization
        });
        const collapsedMultapse = countingPolicy === "count_unique_neighbors" && result.countedIncidenceCount < result.countedConnectionCount;
        const derivedFacts = collapsedMultapse ? {
          multapseAggregated: true,
          multapseAggregation: "count_unique_neighbors"
        } : {};
        const edges = [
          result.degreeLow[0] - 0.5,
          ...result.degreeHigh.map((degree) => degree + 0.5)
        ];
        const tableRows = result.degreeLow.map((low, index) => [
          low,
          result.degreeHigh[index],
          result.nodeCounts[index],
          normalization === "probability" ? result.values[index] : null,
          result.nodeIds.length,
          result.countedConnectionCount,
          result.countedIncidenceCount,
          "1"
        ]);
        const operation = derivationOperation(
          "degree_distribution.exact_enumeration",
          "cortexel.degree_distribution.exact_enumeration",
          { direction, countingPolicy, autapsePolicy, normalization, binning: "per_integer_degree" },
          data,
          { degrees: result.degrees, nodeCounts: result.nodeCounts, values: result.values },
          {
            universeNodeCount: result.nodeIds.length,
            countedConnectionCount: result.countedConnectionCount,
            countedIncidenceCount: result.countedIncidenceCount,
            excludedAutapseCount: result.excludedAutapseCount
          }
        );
        const maximumDegree = result.degreeHigh[result.degreeHigh.length - 1];
        const compileContext = distributionCompilerContext(
          context(tableRows.length, derivedFacts),
          skillId,
          {
            direction,
            selectionLabel: String(parameters.selectionLabel ?? parameters.selectionId),
            countingPolicy,
            autapsePolicy,
            excludedAutapseStatement: autapsePolicy === "exclude" ? ` (${result.excludedAutapseCount} excluded)` : "",
            universeNodeCount: distributionCompilerNumber(result.nodeIds.length),
            scopeStatement: compilerNetworkScopeSummaryCell(data.scope),
            countedConnectionCount: distributionCompilerNumber(result.countedConnectionCount),
            countedIncidenceCount: distributionCompilerNumber(result.countedIncidenceCount),
            minDegree: "0",
            maxDegree: distributionCompilerNumber(maximumDegree),
            zeroDegreeNodeCount: distributionCompilerNumber(result.nodeCounts[0]),
            normalization
          }
        );
        return done(
          withContractTable(compileBarFigure(
            compileContext,
            edges,
            result.values,
            `${direction}-degree`,
            normalization === "probability" ? "probability" : "node count",
            skillId,
            "linear",
            {
              summaryStatements: [
                `Exact conservation: ${result.nodeIds.length} declared nodes are enumerated once; ${result.countedConnectionCount} retained connection rows produce ${result.countedIncidenceCount} counted incidences under ${countingPolicy}.`
              ],
              outputAuthority: { classId: "bins", groupId: "all" }
            }
          ), compileContext, skillId, tableRows),
          [operation],
          derivedFacts
        );
      } catch (error) {
        if (error instanceof DistributionDerivationError) return failDistribution(error);
        return fail(
          "INTERNAL_INVARIANT_VIOLATED",
          "render",
          error instanceof Error ? error.message : "degree-distribution derivation failed",
          "/data"
        );
      }
    }
    try {
      const isDelay = skillId === "network.delay_distribution";
      const normalization = String(parameters.normalization ?? "count");
      let resolvedBins;
      let histogramGroups = [];
      let measurementConversion;
      let renderAsGroups = false;
      if (data.mode === "prebinned") {
        const histogram = rec(data.histogram);
        const edgeRecord = rec(data.binEdges);
        resolvedBins = declaredBins(
          edgeRecord ? { mode: "edges", unit: edgeRecord.unit, edges: edgeRecord.edges, finalEdgeInclusive: true } : rec(parameters.bins)
        );
        if (!resolvedBins) return { pending: rendererId };
        let suppliedCounts = arr(data.counts) ? numbers4(data.counts) : void 0;
        const underRangeCount = num(data.underRangeCount) ?? num(data.excludedUnderRangeCount) ?? 0;
        const overRangeCount = num(data.overRangeCount) ?? num(data.excludedOverRangeCount) ?? 0;
        const observationCount = num(data.totalObservationCount) ?? 0;
        let values;
        let binnedObservationCount2;
        if (suppliedCounts) {
          values = histogramValues(
            suppliedCounts,
            resolvedBins.spec.edges,
            resolvedBins.unit,
            normalization
          );
          binnedObservationCount2 = suppliedCounts.reduce((sum, count) => sum + count, 0);
        } else {
          values = numbers4(histogram?.values);
          binnedObservationCount2 = observationCount - underRangeCount - overRangeCount;
          if (!Number.isSafeInteger(binnedObservationCount2) || binnedObservationCount2 < 0) {
            throw new Error("prebinned histogram conservation produced an invalid observation count");
          }
          if (normalization !== "count" && binnedObservationCount2 === 0) {
            throw new EmptyHistogramNormalizationError(
              "probability and density are undefined for an empty histogram"
            );
          }
          suppliedCounts = recoverHistogramCounts(
            values,
            resolvedBins.spec.edges,
            resolvedBins.unit,
            normalization,
            binnedObservationCount2
          );
        }
        const connectionCount = num(data.sourceConnectionCount) ?? num(data.consideredConnectionCount) ?? observationCount;
        const pairCount = isDelay ? num(data.consideredOrderedPairCount) : num(data.sourceOrderedPairCount);
        const pairAggregationApplies = isDelay ? parameters.countingPolicy === "per_ordered_pair" : parameters.observationUnit === "node_pair";
        histogramGroups = [{
          id: "all",
          label: String(parameters.selectionLabel ?? "All observations"),
          values,
          ...suppliedCounts ? { counts: suppliedCounts } : {},
          connectionCount,
          // Pre-binned rows no longer reveal pair multiplicity, so Cortexel cannot
          // assert that any multapse was actually collapsed.
          aggregatedMultapseCount: pairAggregationApplies && pairCount !== void 0 ? Math.max(0, connectionCount - pairCount) : 0,
          observationCount,
          binnedObservationCount: binnedObservationCount2,
          underRangeCount,
          overRangeCount,
          missingMeasurementCount: num(data.missingWeightCount) ?? 0,
          ...!isDelay ? { missingObservationCount: num(data.missingObservationCount) ?? 0 } : { missingObservationCount: 0 },
          zeroObservationCount: num(data.zeroWeightCount) ?? 0
        }];
      } else {
        const connections = rec(data.connections) ?? {};
        const measurement = rec(isDelay ? connections.delays : connections.weights) ?? {};
        const sourceUnit = measurement.unit ?? (isDelay ? "ms" : "nest:weight");
        resolvedBins = declaredBins(rec(parameters.bins), sourceUnit);
        if (!resolvedBins) return { pending: rendererId };
        const sourceIds = strings4(connections.sourceIds);
        const targetIds = strings4(connections.targetIds);
        const synapseModels = strings4(connections.synapseModels);
        const measured = (arr(measurement.values) ?? []).map(
          (value) => typeof value === "number" && Number.isFinite(value) ? value : null
        );
        const groupByModel = isDelay ? data.groupBy === "synapse_model" : parameters.grouping === "by_synapse_model";
        renderAsGroups = groupByModel;
        const bins = {
          edges: resolvedBins.spec.edges,
          unit: resolvedBins.unit,
          finalEdgeInclusive: resolvedBins.spec.finalEdgeInclusive,
          ...isDelay ? { edgeToleranceUlps: 8 } : {}
        };
        const result = isDelay ? deriveDelayDistribution({
          sourceIds,
          targetIds,
          delayValues: measured.map((value, ordinal) => {
            if (value === null) {
              throw new DistributionDerivationError(
                "SEMANTIC_LENGTH_MISMATCH",
                ["connections", "delays", "values", ordinal],
                "a delay connection row is missing its measurement."
              );
            }
            return value;
          }),
          delayUnit: sourceUnit,
          nodeUniverse: strings4(rec(data.nodeUniverse)?.ids),
          ...arr(connections.synapseModels) === void 0 ? {} : { synapseModels },
          groupBy: data.groupBy === "synapse_model" ? "synapse_model" : "none",
          countingPolicy: parameters.countingPolicy === "per_ordered_pair" ? "per_ordered_pair" : "per_connection",
          ...typeof parameters.multapseAggregation === "string" ? { aggregation: parameters.multapseAggregation } : {},
          bins,
          normalization,
          outOfRangePolicy: String(parameters.outOfRangeDelays)
        }) : deriveWeightDistribution({
          sourceIds,
          targetIds,
          weightValues: measured,
          weightUnit: sourceUnit,
          sourceUniverse: strings4(rec(data.sourceUniverse)?.ids),
          targetUniverse: strings4(rec(data.targetUniverse)?.ids),
          synapseModels,
          grouping: parameters.grouping === "by_synapse_model" ? "by_synapse_model" : "none",
          observationUnit: parameters.observationUnit === "node_pair" ? "node_pair" : "synapse",
          ...typeof parameters.aggregation === "string" ? { aggregation: parameters.aggregation } : {},
          signTreatment: parameters.signTreatment === "magnitude" ? "magnitude" : "preserve",
          bins,
          normalization,
          outOfRangePolicy: String(parameters.outOfRangeWeights)
        });
        if (parameters.xScale === "log" && result.minimumObservation !== null && !(result.minimumObservation > 0)) {
          return fail(
            "RENDER_LOG_SCALE_NONPOSITIVE_DOMAIN",
            "render",
            "logarithmic distribution observations must be strictly positive.",
            "/parameters/xScale"
          );
        }
        measurementConversion = sourceUnit === resolvedBins.unit ? void 0 : conversionReceipt(sourceUnit, resolvedBins.unit);
        histogramGroups = result.groups.map((group) => {
          const missingObservations = group.missingObservationCount;
          const aggregatedMultapseCount2 = isDelay && parameters.countingPolicy === "per_connection" ? 0 : Math.max(
            0,
            group.consideredConnectionCount - group.observationCount - missingObservations
          );
          return {
            id: group.groupId,
            label: group.groupId === "all" ? String(parameters.selectionLabel ?? "All observations") : group.groupId,
            values: group.values,
            counts: group.counts,
            connectionCount: group.consideredConnectionCount,
            aggregatedMultapseCount: aggregatedMultapseCount2,
            observationCount: group.observationCount,
            binnedObservationCount: group.binnedObservationCount,
            underRangeCount: group.underRangeCount,
            overRangeCount: group.overRangeCount,
            missingMeasurementCount: group.missingConnectionCount,
            missingObservationCount: missingObservations,
            zeroObservationCount: group.zeroObservationCount
          };
        });
      }
      const binCount = resolvedBins.spec.edges.length - 1;
      for (const group of histogramGroups) {
        if (group.values.length !== binCount) {
          return fail(
            "SEMANTIC_LENGTH_MISMATCH",
            "semantic",
            `group ${group.id} has ${group.values.length} histogram values for ${binCount} declared bins.`,
            data.mode === "prebinned" ? "/data" : "/parameters/bins"
          );
        }
        if (group.values.some((value) => !Number.isFinite(value) || value < 0)) {
          return fail(
            "SCIENCE_NORMALIZATION_UNVERIFIABLE",
            "science",
            `group ${group.id} contains a histogram value that is not finite and non-negative.`,
            data.mode === "prebinned" ? "/data" : "/parameters/normalization"
          );
        }
      }
      const underflow = histogramGroups.reduce((sum, group) => sum + group.underRangeCount, 0);
      const overflow = histogramGroups.reduce((sum, group) => sum + group.overRangeCount, 0);
      if ((isDelay ? parameters.outOfRangeDelays : parameters.outOfRangeWeights) === "reject" && (underflow > 0 || overflow > 0)) {
        return fail(
          "SCIENCE_BIN_EDGES_INVALID",
          "science",
          `${underflow} observations fall below and ${overflow} above the declared bin range under the reject policy.`,
          "/parameters/bins"
        );
      }
      if (parameters.xScale === "log" && resolvedBins.spec.edges.some((edge) => !(edge > 0))) {
        return fail(
          "RENDER_LOG_SCALE_NONPOSITIVE_DOMAIN",
          "render",
          "a logarithmic distribution axis requires strictly positive bin edges.",
          "/parameters/bins"
        );
      }
      const yLabel = histogramYAxisLabel(normalization, resolvedBins.unit);
      const sourceConnectionRowCount = histogramGroups.reduce(
        (sum, group) => sum + group.connectionCount,
        0
      );
      const formedObservationCount = histogramGroups.reduce(
        (sum, group) => sum + group.observationCount,
        0
      );
      const aggregatedMultapseCount = histogramGroups.reduce(
        (sum, group) => sum + group.aggregatedMultapseCount,
        0
      );
      const missingMeasurementCount = histogramGroups.reduce(
        (sum, group) => sum + group.missingMeasurementCount,
        0
      );
      const missingObservationCount = histogramGroups.reduce(
        (sum, group) => sum + (group.missingObservationCount ?? 0),
        0
      );
      const missingObservationCountKnown = histogramGroups.every(
        (group) => group.missingObservationCount !== void 0
      );
      const zeroObservationCount = histogramGroups.reduce(
        (sum, group) => sum + group.zeroObservationCount,
        0
      );
      const operation = derivationOperation(
        `${isDelay ? "delay" : "weight"}_distribution.histogram`,
        `cortexel.${isDelay ? "delay" : "weight"}_distribution.histogram`,
        {
          bins: resolvedBins,
          normalization,
          observationUnit: isDelay ? parameters.countingPolicy : parameters.observationUnit,
          ...(isDelay ? parameters.multapseAggregation : parameters.aggregation) !== void 0 ? { aggregation: isDelay ? parameters.multapseAggregation : parameters.aggregation } : {},
          ...(isDelay ? data.groupBy : parameters.grouping) !== void 0 ? { grouping: isDelay ? data.groupBy : parameters.grouping } : {},
          ...isDelay ? {} : { signTreatment: parameters.signTreatment }
        },
        data,
        {
          groups: histogramGroups.map((group) => ({
            id: group.id,
            ...group.counts ? { counts: group.counts } : {},
            values: group.values
          }))
        },
        {
          sourceConnectionRowCount,
          aggregatedMultapseCount,
          formedObservationCount,
          binnedObservationCount: histogramGroups.reduce(
            (sum, group) => sum + group.binnedObservationCount,
            0
          ),
          underRangeCount: underflow,
          overRangeCount: overflow,
          missingMeasurementCount,
          ...missingObservationCountKnown ? { missingObservationCount } : {},
          zeroObservationCount,
          prebinned: data.mode === "prebinned",
          ...measurementConversion ? { measurementConversion } : {},
          groups: histogramGroups.map((group) => ({
            id: group.id,
            connectionCount: group.connectionCount,
            aggregatedMultapseCount: group.aggregatedMultapseCount,
            observationCount: group.observationCount,
            binnedObservationCount: group.binnedObservationCount,
            underRangeCount: group.underRangeCount,
            overRangeCount: group.overRangeCount,
            missingMeasurementCount: group.missingMeasurementCount,
            ...group.missingObservationCount !== void 0 ? { missingObservationCount: group.missingObservationCount } : {},
            zeroObservationCount: group.zeroObservationCount
          }))
        }
      );
      const conversionFacts = measurementConversion ? [conversionDisclosureText(`${isDelay ? "delay" : "weight"} observations`, measurementConversion)] : [];
      const derivedFacts = {
        preBinned: data.mode === "prebinned",
        missingValueCount: missingMeasurementCount,
        ...conversionFacts.length > 0 ? { unitConversions: conversionFacts } : {},
        ...aggregatedMultapseCount > 0 ? {
          multapseAggregated: true,
          multapseAggregation: String(
            isDelay ? parameters.multapseAggregation : parameters.aggregation
          )
        } : {}
      };
      const rowsTotal = renderAsGroups ? histogramGroups.length * binCount : binCount;
      const xLabelBase = isDelay ? "delay" : parameters.signTreatment === "magnitude" ? "|weight|" : parameters.observationUnit === "node_pair" ? "per-pair weight" : "weight";
      const reportUngroupedAccounting = !renderAsGroups;
      const ungroupedAccountingMetadata = reportUngroupedAccounting ? [
        { key: "connectionCount", header: "Connection rows", value: sourceConnectionRowCount },
        { key: "observationCount", header: "Formed observations", value: formedObservationCount },
        {
          key: "binnedObservationCount",
          header: "Binned observations",
          value: histogramGroups.reduce(
            (sum, group) => sum + group.binnedObservationCount,
            0
          )
        },
        { key: "underRangeCount", header: "Under range", value: underflow },
        { key: "overRangeCount", header: "Over range", value: overflow },
        {
          key: "missingMeasurementCount",
          header: "Missing measurements",
          value: missingMeasurementCount
        },
        {
          key: "missingObservationCount",
          header: "Missing observations",
          value: missingObservationCountKnown ? missingObservationCount : null
        }
      ] : [];
      const ungroupedAccountingSummary = reportUngroupedAccounting ? [
        `Exact conservation: ${formedObservationCount} non-missing observations were formed from ${sourceConnectionRowCount} connection rows; ${histogramGroups.reduce((sum, group) => sum + group.binnedObservationCount, 0)} were binned + ${underflow} below range + ${overflow} above range. ${missingMeasurementCount} row measurements and ${missingObservationCount} observations were missing; ${zeroObservationCount} observations were measured zero.`
      ] : [];
      const groupedAccountingSummary = renderAsGroups ? histogramGroups.map(
        (group) => `Group ${group.id}: exact conservation gives ${group.observationCount} non-missing observations from ${group.connectionCount} connection rows; ${group.binnedObservationCount} were binned + ${group.underRangeCount} below range + ${group.overRangeCount} above range, with ${group.missingMeasurementCount} missing row measurements, ${group.missingObservationCount ?? 0} missing observations, and ${group.zeroObservationCount} measured zeros.`
      ) : [];
      const binnedObservationCount = histogramGroups.reduce(
        (sum, group) => sum + group.binnedObservationCount,
        0
      );
      const measurementRecord = rec(
        isDelay ? rec(data.connections)?.delays : rec(data.connections)?.weights
      ) ?? {};
      const measurementSourceUnit = data.mode === "prebinned" ? resolvedBins.unit : String(measurementRecord.unit);
      const valueUnit = normalization === "density" ? String(reciprocalUnit(resolvedBins.unit)) : "1";
      let compilerSummaryFacts;
      if (isDelay) {
        const rawDelays = data.mode === "prebinned" ? [] : numbers4(measurementRecord.values);
        const rawDelayExtent = distributionCompilerExtrema(rawDelays);
        const sourceResolution = rec(data.sourceResolution) ?? {};
        compilerSummaryFacts = {
          selectionLabel: String(parameters.selectionLabel ?? parameters.selectionId),
          consideredConnectionCount: distributionCompilerNumber(sourceConnectionRowCount),
          countingPolicy: String(parameters.countingPolicy),
          pairAggregationStatement: parameters.countingPolicy === "per_ordered_pair" ? ` with ${String(parameters.multapseAggregation)} aggregation` : "",
          observationCount: distributionCompilerNumber(formedObservationCount),
          observationKind: parameters.countingPolicy === "per_ordered_pair" ? "ordered_pair" : "connection",
          groupCount: distributionCompilerNumber(histogramGroups.length),
          groupByStatement: String(data.groupBy),
          scopeStatement: compilerNetworkScopeSummaryCell(data.scope),
          delayMin: rawDelayExtent === null ? "not retained by pre-binned input" : distributionCompilerNumber(rawDelayExtent.min),
          delayMax: rawDelayExtent === null ? "not retained by pre-binned input" : distributionCompilerNumber(rawDelayExtent.max),
          delayUnit: measurementSourceUnit,
          sourceResolution: `${distributionCompilerNumber(num(sourceResolution.value))} ${String(sourceResolution.unit)}`,
          binCount: distributionCompilerNumber(binCount),
          binMin: distributionCompilerNumber(resolvedBins.spec.edges[0]),
          binMax: distributionCompilerNumber(
            resolvedBins.spec.edges[resolvedBins.spec.edges.length - 1]
          ),
          binUnit: resolvedBins.unit,
          xScale: String(parameters.xScale ?? "linear"),
          underRangeCount: distributionCompilerNumber(underflow),
          overRangeCount: distributionCompilerNumber(overflow),
          normalization,
          valueUnit
        };
      } else {
        const connections = rec(data.connections) ?? {};
        const declaredSynapseModels = data.mode === "prebinned" ? strings4(data.contributingSynapseModels) : strings4(connections.synapseModels);
        compilerSummaryFacts = {
          selectionLabel: String(parameters.selectionLabel ?? parameters.selectionId),
          inRangeObservationCount: distributionCompilerNumber(binnedObservationCount),
          observationUnit: String(parameters.observationUnit),
          sourceConnectionCount: distributionCompilerNumber(sourceConnectionRowCount),
          sourceNodeCount: distributionCompilerNumber(
            strings4(rec(data.sourceUniverse)?.ids).length
          ),
          targetNodeCount: distributionCompilerNumber(
            strings4(rec(data.targetUniverse)?.ids).length
          ),
          scopeStatement: compilerNetworkScopeSummaryCell(data.scope),
          synapseModels: distributionCompilerSortedUnique(declaredSynapseModels),
          weightComparability: structuredCell5(parameters.weightComparability) ?? "unknown",
          signTreatment: String(parameters.signTreatment),
          weightUnit: measurementSourceUnit,
          missingWeightCount: distributionCompilerNumber(missingMeasurementCount),
          missingObservationCount: distributionCompilerNumber(missingObservationCount),
          zeroWeightCount: distributionCompilerNumber(zeroObservationCount),
          binCount: distributionCompilerNumber(binCount),
          binMin: distributionCompilerNumber(resolvedBins.spec.edges[0]),
          binMax: distributionCompilerNumber(
            resolvedBins.spec.edges[resolvedBins.spec.edges.length - 1]
          ),
          binUnit: resolvedBins.unit,
          xScale: String(parameters.xScale ?? "linear"),
          zeroEdgeStatement: resolvedBins.spec.edges.includes(0) ? "Zero is an explicit bin edge." : "Zero is not an explicit bin edge.",
          underRangeCount: distributionCompilerNumber(underflow),
          overRangeCount: distributionCompilerNumber(overflow),
          normalization,
          valueUnit
        };
      }
      const compileContext = distributionCompilerContext(
        context(rowsTotal, derivedFacts),
        skillId,
        compilerSummaryFacts
      );
      const plan = renderAsGroups ? compileGroupedBarFigure(
        compileContext,
        resolvedBins.spec.edges,
        histogramGroups,
        `${xLabelBase} (${unitLabel(resolvedBins.unit)})`,
        yLabel,
        skillId,
        parameters.xScale === "log" ? "log" : "linear",
        {
          summaryStatements: groupedAccountingSummary,
          outputAuthority: { classId: "bins" }
        }
      ) : compileBarFigure(
        compileContext,
        resolvedBins.spec.edges,
        histogramGroups[0]?.values ?? [],
        `${xLabelBase} (${unitLabel(resolvedBins.unit)})`,
        yLabel,
        skillId,
        parameters.xScale === "log" ? "log" : "linear",
        {
          tableMetadata: ungroupedAccountingMetadata,
          summaryStatements: ungroupedAccountingSummary,
          outputAuthority: { classId: "bins", groupId: "all" }
        }
      );
      const scopeSummary = compilerNetworkScopeSummaryCell(data.scope);
      const tableRows = [];
      for (const group of histogramGroups) {
        if (!group.counts) {
          throw new Error(`histogram group ${group.id} has no exact integer count channel`);
        }
        for (let index = 0; index < binCount; index++) {
          const lower = resolvedBins.spec.edges[index];
          const upper = resolvedBins.spec.edges[index + 1];
          const width = sameUnitDifference(lower, upper, resolvedBins.unit);
          const value = group.values[index];
          if (isDelay) {
            const observationKind = parameters.countingPolicy === "per_ordered_pair" ? "ordered_pair" : "connection";
            tableRows.push([
              group.id,
              lower,
              upper,
              width,
              resolvedBins.unit,
              group.counts[index],
              observationKind,
              normalization,
              scopeSummary,
              normalization === "probability" ? value : null,
              normalization === "density" ? value : null,
              normalization === "density" ? reciprocalUnit(resolvedBins.unit) ?? null : null,
              group.binnedObservationCount,
              group.connectionCount,
              group.underRangeCount,
              group.overRangeCount
            ]);
          } else {
            tableRows.push([
              group.id,
              lower,
              upper,
              width,
              upper <= 0 ? "negative" : "non_negative",
              group.counts[index],
              value,
              normalization === "density" ? reciprocalUnit(resolvedBins.unit) ?? null : "1",
              group.binnedObservationCount,
              group.missingObservationCount ?? 0,
              group.connectionCount
            ]);
          }
        }
      }
      return done(
        withContractTable(plan, compileContext, skillId, tableRows),
        [operation],
        derivedFacts
      );
    } catch (error) {
      const message = error instanceof Error ? error.message : "distribution derivation failed";
      if (error instanceof EmptyHistogramNormalizationError) {
        return fail("RENDER_NO_DATA", "render", message, "/data");
      }
      if (message.includes("no_aggregation")) {
        return fail("SCIENCE_AGGREGATION_REQUIRED", "science", message, "/parameters");
      }
      if (message.includes("logarithmic distribution observations")) {
        return fail(
          "RENDER_LOG_SCALE_NONPOSITIVE_DOMAIN",
          "render",
          message,
          "/parameters/xScale"
        );
      }
      return fail(
        message.includes("unit") || message.includes("reciprocal") ? "SCIENCE_UNIT_DIMENSION_MISMATCH" : message.includes("histogram") || message.includes("safe-integer") ? "SCIENCE_NORMALIZATION_UNVERIFIABLE" : "SCIENCE_NUMERIC_RESOLUTION_UNREPRESENTABLE",
        "science",
        message,
        "/data"
      );
    }
  }
  if (rendererId === "figure.matrix") {
    const matrixCompilerSummary = (facts) => {
      const template = SKILL_CATALOG[skillId].accessibility.summaryTemplate;
      const placeholderPattern = /\{([A-Za-z][A-Za-z0-9_]*)\}/g;
      const required = [];
      for (const match of template.matchAll(placeholderPattern)) required.push(match[1]);
      const requiredSet = new Set(required);
      const templateLiterals = template.replace(placeholderPattern, "");
      const supplied = Object.keys(facts);
      const missing = [...requiredSet].filter(
        (key) => !Object.prototype.hasOwnProperty.call(facts, key)
      );
      const extra = supplied.filter((key) => !requiredSet.has(key));
      if (required.length === 0 || templateLiterals.includes("{") || templateLiterals.includes("}") || missing.length > 0 || extra.length > 0) {
        throw new Error(
          `${skillId} matrix compiler summary facts do not exactly match its source template (missing ${missing.join(", ") || "none"}; extra ${extra.join(", ") || "none"})`
        );
      }
      for (const [key, value] of Object.entries(facts)) {
        if (!isOutputAuthoritySummaryFactSafeV1(value)) {
          throw new Error(`${skillId} matrix compiler summary fact ${key} is not safe display text`);
        }
      }
      const summary = template.replace(placeholderPattern, (_placeholder, key) => facts[key]);
      if (!isOutputAuthoritySummarySafeV1(summary)) {
        throw new Error(`${skillId} matrix compiler produced an unsafe summary`);
      }
      return summary;
    };
    const matrixCompilerNumber = (value) => value === 0 ? "0" : String(value);
    const matrixCompilerNoCompaction = () => "No compaction was applied; every accepted row is returned.";
    const matrixCompilerScopeStatement = (matrixScope) => {
      const kind = String(matrixScope.kind);
      if (kind === "single_process") return "single_process complete";
      if (kind === "global_merged") {
        return `global_merged across all ${String(matrixScope.worldSize)} declared ranks`;
      }
      if (kind === "mpi_target_rank_local") {
        return `mpi_target_rank_local rank ${String(matrixScope.rank)} in worldSize ${String(matrixScope.worldSize)}; localTargetUniverseComplete=${String(matrixScope.localTargetUniverseComplete)}`;
      }
      if (kind === "sampled") {
        return `sampled ${String(matrixScope.method)} from ${String(matrixScope.parentScope)}; retained ${String(matrixScope.retainedConnectionCount)} of ${String(matrixScope.sourceConnectionCount)} connection rows`;
      }
      throw new Error(`${skillId} matrix compiler encountered an unknown validated scope kind`);
    };
    const matrixCompilerScopeSummaryCell = (matrixScope) => {
      const kind = String(matrixScope.kind);
      if (kind === "single_process") {
        return structuredCell5({
          kind: "single_process",
          snapshotTime: matrixScope.snapshotTime
        });
      }
      if (kind === "global_merged") {
        return structuredCell5({
          kind: "global_merged",
          mergedRanksCoverage: "all_ranks_0_through_worldSize_minus_1",
          snapshotTime: matrixScope.snapshotTime,
          worldSize: matrixScope.worldSize
        });
      }
      if (kind === "mpi_target_rank_local") {
        return structuredCell5({
          kind: "mpi_target_rank_local",
          localTargetUniverseComplete: true,
          rank: matrixScope.rank,
          snapshotTime: matrixScope.snapshotTime,
          worldSize: matrixScope.worldSize
        });
      }
      if (kind === "sampled") {
        return structuredCell5({
          kind: "sampled",
          method: matrixScope.method,
          parentScope: matrixScope.parentScope,
          retainedConnectionCount: matrixScope.retainedConnectionCount,
          snapshotTime: matrixScope.snapshotTime,
          sourceConnectionCount: matrixScope.sourceConnectionCount
        });
      }
      throw new Error(`${skillId} matrix compiler encountered an unknown validated scope kind`);
    };
    const matrixCompilerAdjacencyMultapseStatement = (cells, cellMode, method) => {
      let count = 0;
      for (const cell of cells) if (cell.retainedConnectionRows > 1) count++;
      if (count === 0) return "No present cell contains multiple retained connection rows.";
      if (cellMode === "binary_presence") {
        return `${count} present ${count === 1 ? "cell contains" : "cells contain"} multiple retained connection rows; each maps to one binary-presence mark, while retained-row and complete-cell counts remain in the table.`;
      }
      return `${count} present ${count === 1 ? "cell aggregates" : "cells aggregate"} multiple retained connection rows using ${method}.`;
    };
    const matrixCompilerWeightMultapseStatement = (cells, method) => {
      let aggregated = 0;
      let unavailable = 0;
      for (const cell of cells) {
        if (cell.retainedConnectionRows <= 1) continue;
        if (cell.state === "valued") aggregated++;
        else unavailable++;
      }
      if (aggregated === 0 && unavailable === 0) {
        return "No present cell contains multiple retained connection rows.";
      }
      const statements = [];
      if (aggregated > 0) {
        statements.push(
          `${aggregated} valued ${aggregated === 1 ? "cell aggregates" : "cells aggregate"} multiple retained connection rows using ${method}.`
        );
      }
      if (unavailable > 0) {
        statements.push(
          `${unavailable} present ${unavailable === 1 ? "cell contains" : "cells contain"} multiple retained connection rows, but no complete aggregate is reported because at least one contributing weight is missing.`
        );
      }
      return statements.join(" ");
    };
    const matrixCellAuthority = (cell, state) => ({
      tag: "data_carrier",
      classId: "cells",
      provenance: {
        rowIndex: cell.rowIndex,
        targetId: cell.targetId,
        columnIndex: cell.columnIndex,
        sourceId: cell.sourceId,
        cellState: state
      }
    });
    const ids = strings4(rec(data.nodeUniverse)?.ids);
    const conns = rec(data.connections) ?? {};
    const sources = strings4(conns?.sourceIds);
    const targets = strings4(conns?.targetIds);
    const scope = rec(data.scope) ?? {};
    const scopeKind = String(scope.kind);
    const scopeCell = matrixCompilerScopeSummaryCell(scope);
    const observedRaw = arr(data.observedTargetIds);
    const observedTargets = observedRaw ? strings4(observedRaw) : void 0;
    const edgeIdsRaw = arr(conns.edgeIds);
    const synapseModelsRaw = arr(conns.synapseModels);
    const edgeIds = edgeIdsRaw ? strings4(edgeIdsRaw) : void 0;
    const synapseModels = synapseModelsRaw ? strings4(synapseModelsRaw) : void 0;
    const weightRecord = rec(conns?.weights);
    const weightValuesRaw = arr(weightRecord?.values) ?? [];
    const weightValues = weightValuesRaw.map((value) => value === null ? null : Number(value));
    const delayRecord = rec(conns?.delays);
    const delayValuesRaw = arr(delayRecord?.values) ?? [];
    const delayValues = numbers4(delayRecord?.values);
    const aggregation = String(parameters.multapseAggregation);
    const tableEnumeration = parameters.tableCellEnumeration === "dense" ? "dense" : "present_cells_only";
    const preliminaryContext = context(0);
    const topologyInput = {
      nodeIds: ids,
      sourceIds: sources,
      targetIds: targets,
      ...edgeIds ? { edgeIds } : {},
      ...synapseModels ? { synapseModels } : {},
      scope,
      ...observedTargets ? { observedTargetIds: observedTargets } : {},
      enumeration: tableEnumeration,
      maximumMaterializedCells: preliminaryContext.returnedTableRows
    };
    const observedSet = new Set(observedTargets ?? []);
    const fullyObserved = scopeKind === "single_process" || scopeKind === "global_merged";
    const observedRows = ids.map((id) => fullyObserved || scopeKind === "mpi_target_rank_local" && observedSet.has(id));
    const carriedAttributes = (ordinals) => {
      const carried = ordinals.flatMap((index) => {
        const entry = {};
        if (weightRecord) {
          entry.weight = weightValuesRaw[index] ?? null;
          entry.weightUnit = weightRecord.unit;
        }
        if (delayRecord) {
          entry.delay = (arr(delayRecord.values) ?? [])[index] ?? null;
          entry.delayUnit = delayRecord.unit;
        }
        if (Object.keys(entry).length === 0) return [];
        if (edgeIds !== void 0) entry.edgeId = edgeIds[index];
        if (synapseModels !== void 0) entry.synapseModel = synapseModels[index];
        return [entry];
      });
      return carried.length > 0 ? structuredCell5(carried) : null;
    };
    const tableModels = (models, includeComparabilityGroup) => models === null ? null : includeComparabilityGroup ? structuredCell5({
      models,
      ...typeof parameters.synapseModelGroup === "string" ? { comparabilityGroup: parameters.synapseModelGroup } : {}
    }) : structuredCell5(models);
    const stageForMatrixError = (error) => error.code.startsWith("SCOPE_") ? "scope" : error.code.startsWith("RESOURCE_") ? "budget" : error.code.startsWith("SCIENCE_") ? "science" : "semantic";
    try {
      if (skillId === "network.adjacency_matrix") {
        const cellMode = parameters.cellMode === "binary_presence" ? "binary_presence" : "multiplicity";
        const adjacencyAggregation = aggregation === "no_aggregation" ? "no_aggregation" : "sum";
        const matrix2 = deriveAdjacencyMatrix(topologyInput, cellMode, adjacencyAggregation);
        const tableRows2 = matrix2.tableCells.map((cell) => contractOrderedTableRow(skillId, {
          rowIndex: cell.rowIndex,
          targetId: cell.targetId,
          columnIndex: cell.columnIndex,
          sourceId: cell.sourceId,
          cellStatus: cell.state,
          cellValue: cell.cellValue,
          multiplicity: cell.multiplicity,
          retainedConnectionRows: cell.retainedConnectionRows,
          connectionSetComplete: booleanCell3(cell.connectionSetComplete),
          isAutapse: booleanCell3(cell.isAutapse),
          edgeIds: cell.contributingEdgeIds === null ? null : structuredCell5(cell.contributingEdgeIds),
          synapseModels: tableModels(cell.synapseModels, false),
          carriedAttributes: carriedAttributes(cell.contributingOrdinals),
          scopeSummary: scopeCell
        }));
        const multapseCells2 = matrix2.presentCells.filter(
          (cell) => cell.retainedConnectionRows > 1
        );
        const autapseCellCount = matrix2.presentCells.filter((cell) => cell.isAutapse).length;
        const snapshotTime2 = rec(scope.snapshotTime) ?? {};
        const summary2 = matrixCompilerSummary({
          selectionLabel: String(parameters.selectionLabel ?? parameters.selectionId),
          nodeCount: String(ids.length),
          cellMode,
          presentCellCount: String(matrix2.presentCellCount),
          connectionCount: String(matrix2.connectionCount),
          observedRowCount: String(matrix2.observedRowCount),
          absentCellCount: String(matrix2.absentCellCount),
          notObservedCellCount: String(matrix2.notObservedCellCount),
          autapseCellCount: String(autapseCellCount),
          scopeStatement: matrixCompilerScopeStatement(scope),
          snapshotTime: matrixCompilerNumber(Number(snapshotTime2.value)),
          snapshotTimeUnit: String(snapshotTime2.unit),
          multapseStatement: matrixCompilerAdjacencyMultapseStatement(
            matrix2.presentCells,
            cellMode,
            adjacencyAggregation
          ),
          compactionStatement: matrixCompilerNoCompaction()
        });
        let missingValueCount = 0;
        for (const value of weightValuesRaw) if (value === null) missingValueCount++;
        for (const value of delayValuesRaw) if (value === null) missingValueCount++;
        const derivedFacts2 = {
          ...cellMode === "multiplicity" && multapseCells2.length > 0 ? { multapseAggregated: true, multapseAggregation: adjacencyAggregation } : {},
          ...missingValueCount > 0 ? { missingValueCount } : {}
        };
        const compileContext2 = context(tableRows2.length, derivedFacts2);
        const outputEvidence2 = {
          axisOrder: matrix2.axisOrder,
          connectionCount: matrix2.connectionCount,
          presentCellCount: matrix2.presentCellCount,
          absentCellCount: matrix2.absentCellCount,
          notObservedCellCount: matrix2.notObservedCellCount,
          observedRowCount: matrix2.observedRowCount,
          cells: matrix2.presentCells.map((cell) => ({
            rowIndex: cell.rowIndex,
            columnIndex: cell.columnIndex,
            state: cell.state,
            cellValue: cell.cellValue,
            multiplicity: cell.multiplicity,
            retainedConnectionRows: cell.retainedConnectionRows,
            connectionSetComplete: cell.connectionSetComplete,
            contributingOrdinals: cell.contributingOrdinals
          }))
        };
        const operation2 = derivationOperation(
          "matrix.bind_and_aggregate",
          "cortexel.matrix.target_rows_source_columns",
          { axisOrder: MATRIX_AXIS_ORDER, cellMode, aggregation: adjacencyAggregation, tableEnumeration },
          {
            nodeIds: ids,
            sourceIds: sources,
            targetIds: targets,
            ...edgeIds ? { edgeIds } : {},
            ...synapseModels ? { synapseModels } : {},
            scope,
            observedTargetIds: observedTargets ?? null
          },
          outputEvidence2,
          {
            inputConnectionRows: sources.length,
            boundConnectionRows: matrix2.presentCells.reduce(
              (total, cell) => total + cell.retainedConnectionRows,
              0
            ),
            connectionConservationPassed: true,
            observedRowCount: matrix2.observedRowCount,
            tableEnumeration
          }
        );
        return done(withContractTable(compileMatrixFigure(compileContext2, {
          rowIds: ids,
          columnIds: ids,
          cells: matrix2.presentCells.map((cell) => ({
            rowIndex: cell.rowIndex,
            columnIndex: cell.columnIndex,
            value: cell.cellValue,
            state: "present",
            authority: matrixCellAuthority(cell, "present")
          })),
          observedRows,
          valueSemantics: cellMode,
          ...cellMode === "multiplicity" ? { numericScale: parameters.multiplicityScale === "log" ? "log" : "linear" } : {},
          valueLabel: cellMode === "binary_presence" ? "Foreground cell means at least one retained connection row" : "Foreground colour encodes exact complete-cell connection multiplicity",
          summary: summary2
        }, skillId), compileContext2, skillId, tableRows2), [operation2], derivedFacts2);
      }
      if (skillId === "network.weight_matrix") {
        const matrix2 = deriveWeightMatrix(topologyInput, weightValues, aggregation);
        const weightUnit = String(weightRecord?.unit);
        const tableRows2 = matrix2.tableCells.map((cell) => contractOrderedTableRow(skillId, {
          rowIndex: cell.rowIndex,
          targetId: cell.targetId,
          columnIndex: cell.columnIndex,
          sourceId: cell.sourceId,
          cellState: cell.state,
          aggregate: cell.aggregate,
          aggregation,
          weightUnit,
          contributingConnectionCount: cell.contributingConnectionCount,
          contributingWeightCount: cell.contributingWeightCount,
          missingWeightCount: cell.missingWeightCount,
          weightMin: cell.weightMin,
          weightMax: cell.weightMax,
          synapseModels: tableModels(cell.synapseModels, true),
          contributingEdgeIds: cell.contributingEdgeIds === null ? null : structuredCell5(cell.contributingEdgeIds),
          scopeSummary: scopeCell
        }));
        const aggregateValues = matrix2.presentCells.flatMap((cell) => cell.aggregate === null ? [] : [cell.aggregate]);
        const aggregateExtent = finiteExtent(aggregateValues);
        const missingValueCount = weightValues.filter((value) => value === null).length;
        const aggregatedMultapseCells = matrix2.presentCells.filter(
          (cell) => cell.state === "valued" && cell.retainedConnectionRows > 1
        );
        const distinctModels = synapseModels === void 0 ? void 0 : [...new Set(synapseModels)].sort(compareUnicodeCodePoints3);
        const modelStatement = distinctModels === void 0 ? "no synapse-model channel was declared" : distinctModels.length === 0 ? "the declared synapse-model channel is empty" : distinctModels.length === 1 ? `one declared model ${distinctModels[0]}; no cross-model pooling` : `caller-declared comparability group ${String(parameters.synapseModelGroup)} across ${String(distinctModels.length)} distinct declared models`;
        const colorScale = rec(parameters.colorScale) ?? {};
        const snapshotTime2 = rec(scope.snapshotTime) ?? {};
        const summary2 = matrixCompilerSummary({
          nodeCount: String(ids.length),
          valuedCellCount: String(matrix2.valuedCellCount),
          presentWithMissingValueCellCount: String(matrix2.presentWithMissingValueCellCount),
          presentWithoutValueCellCount: String(matrix2.presentWithoutValueCellCount),
          absentCellCount: String(matrix2.absentCellCount),
          notObservedCellCount: String(matrix2.notObservedCellCount),
          aggregation,
          connectionCount: String(matrix2.connectionCount),
          weightUnit,
          aggregateMin: aggregateExtent === void 0 ? "not available" : matrixCompilerNumber(aggregateExtent.min),
          aggregateMax: aggregateExtent === void 0 ? "not available" : matrixCompilerNumber(aggregateExtent.max),
          synapseModelGroupStatement: modelStatement,
          colorScaleStatement: colorScale.class === "diverging" ? `diverging about declared center ${matrixCompilerNumber(Number(colorScale.center))}` : "sequential",
          scopeStatement: matrixCompilerScopeStatement(scope),
          snapshotTime: `${matrixCompilerNumber(Number(snapshotTime2.value))} ${String(snapshotTime2.unit)}`,
          multapseStatement: matrixCompilerWeightMultapseStatement(
            matrix2.presentCells,
            aggregation
          ),
          compactionStatement: matrixCompilerNoCompaction()
        });
        const derivedFacts2 = {
          ...aggregatedMultapseCells.length > 0 ? { multapseAggregated: true, multapseAggregation: aggregation } : {},
          ...missingValueCount > 0 ? { missingValueCount } : {}
        };
        const compileContext2 = context(tableRows2.length, derivedFacts2);
        const outputEvidence2 = {
          axisOrder: matrix2.axisOrder,
          connectionCount: matrix2.connectionCount,
          presentCellCount: matrix2.presentCellCount,
          absentCellCount: matrix2.absentCellCount,
          notObservedCellCount: matrix2.notObservedCellCount,
          valuedCellCount: matrix2.valuedCellCount,
          presentWithMissingValueCellCount: matrix2.presentWithMissingValueCellCount,
          presentWithoutValueCellCount: matrix2.presentWithoutValueCellCount,
          cells: matrix2.presentCells.map((cell) => ({
            rowIndex: cell.rowIndex,
            columnIndex: cell.columnIndex,
            state: cell.state,
            aggregate: cell.aggregate,
            contributingOrdinals: cell.contributingOrdinals,
            contributingWeightCount: cell.contributingWeightCount,
            missingWeightCount: cell.missingWeightCount
          }))
        };
        const operation2 = derivationOperation(
          "matrix.bind_and_aggregate",
          "cortexel.matrix.target_rows_source_columns",
          { axisOrder: MATRIX_AXIS_ORDER, aggregation, tableEnumeration, colorScale },
          {
            nodeIds: ids,
            sourceIds: sources,
            targetIds: targets,
            ...edgeIds ? { edgeIds } : {},
            ...synapseModels ? { synapseModels } : {},
            weights: weightRecord,
            scope,
            observedTargetIds: observedTargets ?? null
          },
          outputEvidence2,
          {
            inputConnectionRows: sources.length,
            boundConnectionRows: matrix2.presentCells.reduce(
              (total, cell) => total + cell.retainedConnectionRows,
              0
            ),
            connectionConservationPassed: true,
            missingWeightCount: missingValueCount,
            completeAggregateCount: matrix2.valuedCellCount,
            tableEnumeration
          }
        );
        return done(withContractTable(compileMatrixFigure(compileContext2, {
          rowIds: ids,
          columnIds: ids,
          cells: matrix2.presentCells.map((cell) => ({
            rowIndex: cell.rowIndex,
            columnIndex: cell.columnIndex,
            value: cell.aggregate,
            state: cell.state,
            authority: matrixCellAuthority(cell, cell.state)
          })),
          observedRows,
          valueSemantics: "weight",
          colorScale: colorScale.class === "diverging" ? { class: "diverging", center: Number(colorScale.center) } : { class: "sequential" },
          valueLabel: `Foreground colour encodes complete ${aggregation} weight aggregate (${unitLabel(weightUnit)})`,
          summary: summary2
        }, skillId), compileContext2, skillId, tableRows2), [operation2], derivedFacts2);
      }
      const sourceUnit = String(delayRecord?.unit);
      const displayUnit = String(rec(parameters.displayUnit)?.unit ?? sourceUnit);
      const delayAggregation = aggregation;
      const matrix = deriveDelayMatrix(
        topologyInput,
        delayValues,
        delayAggregation,
        sourceUnit,
        displayUnit
      );
      const tableRows = matrix.tableCells.map((cell) => contractOrderedTableRow(skillId, {
        rowIndex: cell.rowIndex,
        targetId: cell.targetId,
        columnIndex: cell.columnIndex,
        sourceId: cell.sourceId,
        cellStatus: cell.state,
        delayAggregate: cell.delayAggregate,
        displayUnit,
        multapseAggregation: delayAggregation,
        delaySemantics: String(parameters.delaySemantics),
        contributingConnectionCount: cell.contributingConnectionCount,
        delayMin: cell.delayMin,
        delayMax: cell.delayMax,
        contributingEdgeIds: cell.contributingEdgeIds === null ? null : structuredCell5(cell.contributingEdgeIds),
        synapseModels: tableModels(cell.synapseModels, false),
        isAutapse: booleanCell3(cell.isAutapse),
        scopeSummary: scopeCell
      }));
      const delayAggregates = matrix.presentCells.map((cell) => cell.delayAggregate);
      const delayExtent = finiteExtent(delayAggregates);
      const multapseCells = matrix.presentCells.filter(
        (cell) => cell.retainedConnectionRows > 1
      );
      const unitConversions = sourceUnit === displayUnit || matrix.presentCells.length === 0 ? [] : [`delay: ${sourceUnit} -> ${displayUnit} (factor ${conversionFactor(sourceUnit, displayUnit)})`];
      const derivedFacts = {
        ...multapseCells.length > 0 ? { multapseAggregated: true, multapseAggregation: delayAggregation } : {},
        ...unitConversions.length > 0 ? { unitConversions } : {}
      };
      const snapshotTime = rec(scope.snapshotTime) ?? {};
      const summary = matrixCompilerSummary({
        matrixLabel: String(parameters.matrixLabel ?? "declared node universe"),
        rowCount: String(ids.length),
        columnCount: String(ids.length),
        presentCellCount: String(matrix.presentCellCount),
        absentCellCount: String(matrix.absentCellCount),
        notObservedCellCount: String(matrix.notObservedCellCount),
        multapseAggregation: delayAggregation,
        delaySemantics: String(parameters.delaySemantics),
        connectionCount: String(matrix.connectionCount),
        displayUnit,
        delayMin: delayExtent === void 0 ? "not available" : matrixCompilerNumber(delayExtent.min),
        delayMax: delayExtent === void 0 ? "not available" : matrixCompilerNumber(delayExtent.max),
        scopeKind,
        snapshotTime: matrixCompilerNumber(Number(snapshotTime.value)),
        snapshotTimeUnit: String(snapshotTime.unit),
        compactionStatement: matrixCompilerNoCompaction()
      });
      const compileContext = context(tableRows.length, derivedFacts);
      const outputEvidence = {
        axisOrder: matrix.axisOrder,
        connectionCount: matrix.connectionCount,
        presentCellCount: matrix.presentCellCount,
        absentCellCount: matrix.absentCellCount,
        notObservedCellCount: matrix.notObservedCellCount,
        cells: matrix.presentCells.map((cell) => ({
          rowIndex: cell.rowIndex,
          columnIndex: cell.columnIndex,
          state: cell.state,
          delayAggregate: cell.delayAggregate,
          contributingOrdinals: cell.contributingOrdinals
        }))
      };
      const operation = derivationOperation(
        "matrix.bind_aggregate_and_convert",
        "cortexel.matrix.target_rows_source_columns",
        { axisOrder: MATRIX_AXIS_ORDER, aggregation: delayAggregation, tableEnumeration, sourceUnit, displayUnit },
        {
          nodeIds: ids,
          sourceIds: sources,
          targetIds: targets,
          ...edgeIds ? { edgeIds } : {},
          ...synapseModels ? { synapseModels } : {},
          delays: delayRecord,
          scope,
          observedTargetIds: observedTargets ?? null
        },
        outputEvidence,
        {
          inputConnectionRows: sources.length,
          boundConnectionRows: matrix.presentCells.reduce(
            (total, cell) => total + cell.retainedConnectionRows,
            0
          ),
          connectionConservationPassed: true,
          unitConversionCount: sourceUnit === displayUnit ? 0 : 1,
          tableEnumeration
        }
      );
      return done(withContractTable(compileMatrixFigure(compileContext, {
        rowIds: ids,
        columnIds: ids,
        cells: matrix.presentCells.map((cell) => ({
          rowIndex: cell.rowIndex,
          columnIndex: cell.columnIndex,
          value: cell.delayAggregate,
          state: "present",
          authority: matrixCellAuthority(cell, "present")
        })),
        observedRows,
        valueSemantics: "delay",
        numericScale: parameters.scale === "log" ? "log" : "linear",
        valueLabel: `Foreground colour encodes complete ${delayAggregation} delay aggregate (${unitLabel(displayUnit)})`,
        summary
      }, skillId), compileContext, skillId, tableRows), [operation], derivedFacts);
    } catch (error) {
      if (error instanceof MatrixDerivationError) {
        const instancePath = error.code === "SCIENCE_DELAY_NONPOSITIVE" ? "/data/connections/delays/values" : error.code === "SCIENCE_AGGREGATION_REQUIRED" ? "/parameters/multapseAggregation" : error.code === "RESOURCE_MATRIX_CELLS_EXCEEDED" ? "/parameters/tableCellEnumeration" : "/data/connections";
        return fail(error.code, stageForMatrixError(error), error.message, instancePath);
      }
      throw error;
    }
  }
  if (skillId === "network.spatial_map_2d") {
    const positions = rec(data.positions);
    const universeIds = strings4(rec(data.nodeUniverse)?.ids);
    const sourcePositionIds = strings4(positions?.nodeIds);
    const sourcePositionIndex = new Map(sourcePositionIds.map((id, index) => [id, index]));
    const ids = [...universeIds];
    const sourceXs = numbers4(rec(positions?.x)?.values);
    const sourceYs = numbers4(rec(positions?.y)?.values);
    const xPositionUnit = String(rec(positions?.x)?.unit);
    const yPositionUnit = String(rec(positions?.y)?.unit);
    const positionUnit = xPositionUnit === yPositionUnit || conversionFactor(yPositionUnit, xPositionUnit) >= 1 ? xPositionUnit : yPositionUnit;
    const convertedSourceXs = xPositionUnit === positionUnit ? sourceXs : sourceXs.map((value) => convert(value, xPositionUnit, positionUnit));
    const convertedSourceYs = yPositionUnit === positionUnit ? sourceYs : sourceYs.map((value) => convert(value, yPositionUnit, positionUnit));
    const xs = ids.map((id) => convertedSourceXs[sourcePositionIndex.get(id)]);
    const ys = ids.map((id) => convertedSourceYs[sourcePositionIndex.get(id)]);
    const xLabel = `${String(rec(positions?.frame)?.xAxisLabel ?? "x")} (${unitLabel(positionUnit)})`;
    const yLabel = `${String(rec(positions?.frame)?.yAxisLabel ?? "y")} (${unitLabel(positionUnit)})`;
    const coordinateConversions = [
      ...xPositionUnit !== positionUnit ? [`x: ${xPositionUnit} -> ${positionUnit} (factor ${conversionFactor(xPositionUnit, positionUnit)})`] : [],
      ...yPositionUnit !== positionUnit ? [`y: ${yPositionUnit} -> ${positionUnit} (factor ${conversionFactor(yPositionUnit, positionUnit)})`] : []
    ];
    const spatialFacts = coordinateConversions.length > 0 ? { unitConversions: coordinateConversions } : {};
    const spatialOperations = coordinateConversions.length > 0 ? [{
      id: "spatial.coordinates.canonicalize_axes",
      algorithm: "cortexel.spatial_map_2d.canonicalize_axes",
      algorithmRevision: 1,
      parameters: { targetUnit: positionUnit, selectionRule: "finest_supplied_compatible_unit" },
      inputDigest: canonicalDigest({
        x: rec(positions?.x),
        y: rec(positions?.y)
      }),
      outputDigest: canonicalDigest({ nodeIds: ids, x: xs, y: ys, unit: positionUnit }),
      receipt: {
        x: xPositionUnit === positionUnit ? null : conversionReceipt(xPositionUnit, positionUnit),
        y: yPositionUnit === positionUnit ? null : conversionReceipt(yPositionUnit, positionUnit),
        coordinateCount: sourcePositionIds.length
      }
    }] : [];
    const positionIndex = new Map(ids.map((id, index) => [id, index]));
    const groupByNode = /* @__PURE__ */ new Map();
    const groupIndexByNode = /* @__PURE__ */ new Map();
    const universeSet = new Set(universeIds);
    const groupIds = /* @__PURE__ */ new Set();
    const declaredGroups = arr(rec(data.nodeUniverse)?.groups) ?? [];
    for (const [groupIndex, candidate] of declaredGroups.entries()) {
      const group = rec(candidate) ?? {};
      const groupId = String(group.id);
      if (groupIds.has(groupId)) {
        return fail(
          "SEMANTIC_DUPLICATE_ID",
          "semantic",
          `node-universe group id ${JSON.stringify(groupId)} is declared more than once. Group order and legend identity require unique ids.`,
          `/data/nodeUniverse/groups/${groupIndex}/id`
        );
      }
      groupIds.add(groupId);
      for (const member of strings4(group.memberIds)) {
        if (!universeSet.has(member)) {
          return fail(
            "SEMANTIC_UNKNOWN_REFERENCE",
            "semantic",
            `group ${JSON.stringify(groupId)} contains node ${JSON.stringify(member)}, which is outside the declared node universe. A group is a partition of that universe, never an extension of it.`,
            `/data/nodeUniverse/groups/${groupIndex}/memberIds`
          );
        }
        const previousGroup = groupByNode.get(member);
        if (previousGroup !== void 0) {
          return fail(
            "SEMANTIC_DUPLICATE_ID",
            "semantic",
            `node ${JSON.stringify(member)} belongs to both group ${JSON.stringify(previousGroup)} and group ${JSON.stringify(groupId)}. Group colour and marker shape require disjoint membership.`,
            `/data/nodeUniverse/groups/${groupIndex}/memberIds`
          );
        }
        groupByNode.set(member, groupId);
        groupIndexByNode.set(member, groupIndex);
      }
    }
    const domain = rec(positions?.domain);
    const center = rec(domain?.center);
    const extent = rec(domain?.extent);
    const centerX = rec(center?.x);
    const centerY = rec(center?.y);
    const width = rec(extent?.width);
    const height = rec(extent?.height);
    let domainBounds = null;
    let domainAxisX = null;
    let domainAxisY = null;
    if (centerX && centerY && width && height) {
      try {
        const canonicalCenterX = convert(num(centerX.value), String(centerX.unit), positionUnit);
        const canonicalCenterY = convert(num(centerY.value), String(centerY.unit), positionUnit);
        const canonicalWidth = convert(num(width.value), String(width.unit), positionUnit);
        const canonicalHeight = convert(num(height.value), String(height.unit), positionUnit);
        const xAxis2 = spatialDomainAxis(canonicalCenterX, canonicalWidth);
        const yAxis2 = spatialDomainAxis(canonicalCenterY, canonicalHeight);
        domainAxisX = xAxis2;
        domainAxisY = yAxis2;
        const boundary2 = rec(domain?.boundary);
        domainBounds = {
          xMin: xAxis2.lower,
          xMax: xAxis2.upper,
          yMin: yAxis2.lower,
          yMax: yAxis2.upper,
          centerX: canonicalCenterX,
          centerY: canonicalCenterY,
          periodX: canonicalWidth,
          periodY: canonicalHeight,
          periodicX: boundary2?.kind === "periodic" && boundary2.x === true,
          periodicY: boundary2?.kind === "periodic" && boundary2.y === true,
          edgeChordRule: boundary2?.kind === "periodic" ? String(boundary2.edgeChordRule) : "open"
        };
      } catch (error) {
        return fail(
          "SCIENCE_NUMERIC_RESOLUTION_UNREPRESENTABLE",
          "science",
          error instanceof Error ? `the declared spatial domain is not representable under the registered extent-relative binary64 policy: ${error.message}` : "the declared spatial domain is not representable under the registered extent-relative binary64 policy.",
          "/data/positions/domain"
        );
      }
    }
    const valueRecord = rec(positions?.value);
    const sourceValueValues = arr(valueRecord?.values) ?? [];
    const valueValues = valueRecord ? ids.map((id) => sourceValueValues[sourcePositionIndex.get(id)]) : [];
    const coincident = /* @__PURE__ */ new Map();
    for (let index = 0; index < ids.length; index++) {
      const key = canonicalize([xs[index], ys[index]]);
      const existing = coincident.get(key);
      if (existing) existing.push(ids[index]);
      else coincident.set(key, [ids[index]]);
    }
    const scopeSummaryCell2 = compilerNetworkScopeSummaryCell(data.scope);
    const nodeRows = strings4(rec(data.nodeUniverse)?.ids).map((nodeId) => {
      const index = positionIndex.get(nodeId);
      const missing = index === void 0;
      const x = missing ? null : xs[index];
      const y = missing ? null : ys[index];
      const sharing = !missing ? (coincident.get(canonicalize([x, y])) ?? []).filter((id) => id !== nodeId) : [];
      return [
        "node",
        nodeId,
        groupByNode.get(nodeId) ?? "ungrouped",
        x,
        y,
        missing ? null : positionUnit,
        missing ? null : String(positions?.status),
        booleanCell3(missing),
        domainBounds === null || missing ? null : booleanCell3(
          spatialDomainAxisContains(domainAxisX, x) && spatialDomainAxisContains(domainAxisY, y)
        ),
        sharing.length > 0 ? structuredCell5(sharing) : null,
        missing || valueRecord === void 0 || valueValues[index] === null ? null : valueValues[index],
        valueRecord ? String(valueRecord.unit) : null,
        null,
        null,
        null,
        null,
        null,
        null,
        null,
        null,
        null,
        scopeSummaryCell2
      ];
    });
    const connections = rec(data.connections);
    const sources = strings4(connections?.sourceIds);
    const targets = strings4(connections?.targetIds);
    const callerEdgeIds = strings4(connections?.edgeIds);
    if (callerEdgeIds.length > 0 && callerEdgeIds.length !== sources.length) {
      return fail(
        "SEMANTIC_LENGTH_MISMATCH",
        "semantic",
        `connections.edgeIds has ${callerEdgeIds.length} entries for ${sources.length} connection rows.`,
        "/data/connections/edgeIds"
      );
    }
    const edgeIds = sources.map((_source, index) => callerEdgeIds[index] ?? `connection-row-${index}`);
    const weights = arr(rec(connections?.weights)?.values) ?? [];
    const delays = arr(rec(connections?.delays)?.values) ?? [];
    const models = strings4(connections?.synapseModels);
    const weightUnit = typeof rec(connections?.weights)?.unit === "string" ? String(rec(connections?.weights)?.unit) : null;
    const delayUnit = typeof rec(connections?.delays)?.unit === "string" ? String(rec(connections?.delays)?.unit) : null;
    const universeOrdinal = new Map(universeIds.map((id, index) => [id, index]));
    const canonicalPair = (source, target) => universeOrdinal.get(source) <= universeOrdinal.get(target) ? [source, target] : [target, source];
    const pairKey2 = (source, target) => canonicalize(canonicalPair(source, target));
    const pairTotals = /* @__PURE__ */ new Map();
    for (let index = 0; index < sources.length; index++) {
      const key = pairKey2(sources[index], targets[index]);
      pairTotals.set(key, (pairTotals.get(key) ?? 0) + 1);
    }
    const boundary = rec(domain?.boundary);
    const routesByPair = /* @__PURE__ */ new Map();
    for (let edgeIndex = 0; edgeIndex < sources.length; edgeIndex++) {
      const key = pairKey2(sources[edgeIndex], targets[edgeIndex]);
      if (routesByPair.has(key)) continue;
      const [physicalSourceId, physicalTargetId] = canonicalPair(
        sources[edgeIndex],
        targets[edgeIndex]
      );
      const physicalSourceIndex = positionIndex.get(physicalSourceId);
      const physicalTargetIndex = positionIndex.get(physicalTargetId);
      let route;
      try {
        route = classifySpatialChord(
          { x: xs[physicalSourceIndex], y: ys[physicalSourceIndex] },
          { x: xs[physicalTargetIndex], y: ys[physicalTargetIndex] },
          domainBounds ?? void 0,
          physicalSourceId === physicalTargetId
        );
      } catch (error) {
        return fail(
          "SCIENCE_NUMERIC_RESOLUTION_UNREPRESENTABLE",
          "science",
          error instanceof Error ? `connection ${edgeIndex} cannot be routed in finite binary64: ${error.message}` : `connection ${edgeIndex} cannot be routed in finite binary64.`,
          `/data/connections/sourceIds/${edgeIndex}`
        );
      }
      if (physicalSourceId !== physicalTargetId && route.dx === 0 && route.dy === 0) {
        return fail(
          "RENDER_DEGENERATE_DOMAIN",
          "render",
          `connection ${edgeIndex} joins distinct nodes ${physicalSourceId} and ${physicalTargetId} at the same measured place under path kind ${route.pathKind}. A zero-length chord has no target end for an arrowhead, and Cortexel refuses rather than invent a separation.`,
          `/data/connections/sourceIds/${edgeIndex}`
        );
      }
      routesByPair.set(key, { physicalSourceId, physicalTargetId, route });
    }
    let minimumImageTieChordCount = 0;
    let minimumImageTieAxisCount = 0;
    for (const { route } of routesByPair.values()) {
      const axisCount = Number(route.halfPeriodTieX) + Number(route.halfPeriodTieY);
      if (axisCount > 0) minimumImageTieChordCount++;
      minimumImageTieAxisCount += axisCount;
    }
    const edgeRows = sources.map((sourceId, index) => [
      "connection",
      edgeIds[index],
      "not_applicable",
      null,
      null,
      null,
      null,
      "not_applicable",
      null,
      null,
      null,
      null,
      sourceId,
      targets[index],
      routesByPair.get(pairKey2(sourceId, targets[index])).route.pathKind,
      pairTotals.get(pairKey2(sourceId, targets[index])),
      typeof weights[index] === "number" ? weights[index] : null,
      weightUnit,
      typeof delays[index] === "number" ? delays[index] : null,
      delayUnit,
      models[index] ?? null,
      scopeSummaryCell2
    ]);
    const tableRows = [...nodeRows, ...edgeRows];
    const expectedSpatialRows = strings4(rec(data.nodeUniverse)?.ids).length + sources.length;
    if (tableRows.length !== expectedSpatialRows) {
      return fail(
        "INTERNAL_INVARIANT_VIOLATED",
        "internal",
        `spatial table materialized ${tableRows.length} rows for ${expectedSpatialRows} declared node/connection carriers.`,
        "/data"
      );
    }
    const nodeEncoding = rec(parameters.nodeEncoding) ?? {};
    if (nodeEncoding.mode === "value" && !valueRecord) {
      return fail(
        "RENDER_NO_DATA",
        "render",
        "nodeEncoding.mode is value, but positions.value was not supplied. Cortexel refuses rather than draw a uniform map under a numeric legend.",
        "/parameters/nodeEncoding/mode"
      );
    }
    const nodeColorScale = rec(nodeEncoding.colorScale);
    if (nodeEncoding.mode === "value" && nodeColorScale?.transform === "symlog") {
      const threshold = num(nodeColorScale.linearThreshold);
      const origin = nodeColorScale.kind === "diverging" ? num(nodeColorScale.center) : 0;
      const uniqueValues = [...new Set(valueValues.filter(
        (value) => typeof value === "number"
      ))].sort((left, right) => left - right);
      let previousTransform;
      for (const value of uniqueValues) {
        try {
          const delta = exactBinary64Sum([value, -origin]);
          const transformed = symlogTransform(delta, threshold);
          if (!Number.isFinite(transformed)) {
            throw new Error("the transformed value is not finite");
          }
          if (previousTransform !== void 0 && !(transformed > previousTransform)) {
            throw new Error("two distinct ordered values collapse to one transformed value");
          }
          previousTransform = transformed;
        } catch (error) {
          return fail(
            "SCIENCE_NUMERIC_RESOLUTION_UNREPRESENTABLE",
            "science",
            error instanceof Error ? `the declared spatial node symlog scale cannot preserve finite strict value ordering: ${error.message}` : "the declared spatial node symlog scale cannot preserve finite strict value ordering.",
            "/parameters/nodeEncoding/colorScale"
          );
        }
      }
    }
    const connectionDisplay = rec(parameters.connectionDisplay);
    const encodedValues = connectionDisplay?.valueEncoding === "weight" ? weights : connectionDisplay?.valueEncoding === "delay" ? delays : [];
    if (connectionDisplay && connectionDisplay.valueEncoding !== "none") {
      if (encodedValues.length !== sources.length) {
        return fail(
          "RENDER_NO_DATA",
          "render",
          `${String(connectionDisplay.valueEncoding)} was selected for connection encoding, but that complete parallel value channel was not supplied.`,
          "/parameters/connectionDisplay/valueEncoding"
        );
      }
      const valuesByPair = /* @__PURE__ */ new Map();
      for (let index = 0; index < sources.length; index++) {
        const key = pairKey2(sources[index], targets[index]);
        const entries = valuesByPair.get(key) ?? [];
        entries.push(encodedValues[index]);
        valuesByPair.set(key, entries);
      }
      for (const entries of valuesByPair.values()) {
        if (entries.length > 1 && (typeof parameters.multapseAggregation !== "string" || parameters.multapseAggregation === "no_aggregation")) {
          return fail(
            "SCIENCE_AGGREGATION_REQUIRED",
            "science",
            "multiple connection rows share one measured endpoint chord, so its encoded value requires an explicit sum, mean, min, or max aggregation. Reciprocal directions remain separate arrow/count evidence, but cannot carry two incompatible stroke values on one physical chord.",
            "/parameters/multapseAggregation"
          );
        }
        const finite = entries.filter((value) => typeof value === "number");
        if (finite.length > 0 && finite.length !== entries.length) {
          return fail(
            "RENDER_NO_DATA",
            "render",
            "one shared spatial chord cannot simultaneously encode a finite value and a missing value from parallel connections. Cortexel refuses rather than let the finite row visually stand in for the missing row.",
            "/data/connections"
          );
        }
        try {
          aggregateTopologyScalar(
            finite,
            entries.length === 1 ? "no_aggregation" : parameters.multapseAggregation
          );
        } catch (error) {
          return fail(
            "SCIENCE_NUMERIC_RESOLUTION_UNREPRESENTABLE",
            "science",
            error instanceof Error ? `the declared spatial chord aggregate is not representable as finite binary64: ${error.message}` : "the declared spatial chord aggregate is not representable as finite binary64.",
            "/data/connections"
          );
        }
      }
    }
    let coincidentNodeCount = 0;
    for (const members of coincident.values()) {
      if (members.length > 1) coincidentNodeCount += members.length;
    }
    let outsideDomainCount = 0;
    if (domainBounds) {
      for (let index = 0; index < ids.length; index++) {
        if (!spatialDomainAxisContains(domainAxisX, xs[index]) || !spatialDomainAxisContains(domainAxisY, ys[index])) outsideDomainCount++;
      }
    }
    const explicitMissingValueCount = countExplicitNulls(valueValues) + countExplicitNulls(encodedValues);
    const hasAggregatedPair = encodedValues.length > 0 && pairTotals.size < sources.length;
    const spatialDerivedFacts = {
      ...spatialFacts,
      ...explicitMissingValueCount > 0 ? { missingValueCount: explicitMissingValueCount } : {},
      ...hasAggregatedPair && typeof parameters.multapseAggregation === "string" ? {
        multapseAggregated: true,
        multapseAggregation: parameters.multapseAggregation
      } : {}
    };
    const frameRecord = rec(positions?.frame) ?? {};
    const uncertaintyRecord = rec(parameters.uncertainty) ?? {};
    const spatialCompileContext = topologyDynamicsCompilerContext(
      context(tableRows.length, spatialDerivedFacts),
      skillId,
      {
        mapLabel: String(parameters.mapLabel ?? parameters.mapId),
        drawnNodeCount: exactNumberText(ids.length),
        declaredNodeCount: exactNumberText(strings4(rec(data.nodeUniverse)?.ids).length),
        positionStatus: String(positions?.status),
        frameId: String(frameRecord.id),
        missingPositionCount: exactNumberText(
          strings4(rec(data.nodeUniverse)?.ids).length - ids.length
        ),
        xAxisLabel: String(frameRecord.xAxisLabel ?? "x"),
        yAxisLabel: String(frameRecord.yAxisLabel ?? "y"),
        positionUnit,
        domainStatement: domainBounds ? `closed x [${exactNumberText(domainBounds.xMin)}, ${exactNumberText(domainBounds.xMax)}] and y [${exactNumberText(domainBounds.yMin)}, ${exactNumberText(domainBounds.yMax)}] ${positionUnit}` : "no domain declared",
        boundaryStatement: domainBounds ? boundary?.kind === "periodic" ? `periodic x=${String(boundary.x)} y=${String(boundary.y)} with ${String(boundary.edgeChordRule)} edge chords` : String(boundary?.kind) : "not declared",
        minimumImageTieChordCount: exactNumberText(minimumImageTieChordCount),
        minimumImageTieAxisCount: exactNumberText(minimumImageTieAxisCount),
        coincidentNodeCount: exactNumberText(coincidentNodeCount),
        outsideDomainCount: exactNumberText(outsideDomainCount),
        nodeEncodingStatement: structuredCell5(parameters.nodeEncoding),
        connectionStatement: sources.length === 0 ? "no connections were supplied" : `${sources.length} source connection rows form ${pairTotals.size} unordered measured endpoint chords; direction groups remain explicit`,
        nodeUniverseStatement: `${rec(data.nodeUniverse)?.complete === true ? "complete" : "incomplete"}; order ${String(rec(data.nodeUniverse)?.order ?? "as_declared")}`,
        scopeStatement: scopeSummaryCell2,
        uncertaintyStatement: uncertaintyRecord.kind === "none" ? `No uncertainty was supplied (${String(uncertaintyRecord.reason ?? "not_provided")}).` : `Uncertainty kind ${String(uncertaintyRecord.kind)} was declared.`,
        compactionStatement: "No declared node or connection row was compacted; nodes without positions are omitted and disclosed.",
        tableStatement: `The complete table contains ${strings4(rec(data.nodeUniverse)?.ids).length} node rows followed by ${sources.length} source-order connection rows.`
      }
    );
    return done(withContractTable(
      compileSpatialMapFigure(spatialCompileContext, {
        nodes: ids.map((id, index) => ({
          id,
          x: xs[index],
          y: ys[index],
          group: groupByNode.get(id) ?? "ungrouped",
          groupIndex: groupIndexByNode.get(id) ?? declaredGroups.length,
          ...valueRecord ? { value: typeof valueValues[index] === "number" ? valueValues[index] : null } : {}
        })),
        connections: sources.map((sourceId, index) => ({
          id: edgeIds[index],
          sourceId,
          targetId: targets[index],
          sourceOrdinal: index,
          ...connectionDisplay?.valueEncoding !== "none" ? { value: typeof encodedValues[index] === "number" ? encodedValues[index] : null } : {}
        })),
        xLabel,
        yLabel,
        markerRadius: num(parameters.markerRadiusPx) ?? 3,
        nodeEncoding: nodeEncoding.mode ?? "uniform",
        ...rec(nodeEncoding.colorScale) ? {
          nodeValueScale: {
            kind: String(rec(nodeEncoding.colorScale)?.kind),
            transform: String(rec(nodeEncoding.colorScale)?.transform),
            ...num(rec(nodeEncoding.colorScale)?.center) !== void 0 ? { center: num(rec(nodeEncoding.colorScale)?.center) } : {},
            ...num(rec(nodeEncoding.colorScale)?.linearThreshold) !== void 0 ? { linearThreshold: num(rec(nodeEncoding.colorScale)?.linearThreshold) } : {}
          }
        } : {},
        ...connectionDisplay && connectionDisplay.valueEncoding !== "none" ? {
          connectionEncoding: {
            channel: String(connectionDisplay.channel),
            colorKind: rec(connectionDisplay.colorScale)?.kind === "diverging" ? "diverging" : "sequential",
            ...num(rec(connectionDisplay.colorScale)?.center) !== void 0 ? { center: num(rec(connectionDisplay.colorScale)?.center) } : {}
          }
        } : {},
        ...typeof parameters.multapseAggregation === "string" ? {
          multapseAggregation: parameters.multapseAggregation
        } : {},
        ...domainBounds ? { domain: domainBounds } : {}
      }, skillId),
      spatialCompileContext,
      skillId,
      tableRows
    ), spatialOperations, spatialDerivedFacts);
  }
  if (skillId === "neuro.response_curve") {
    const conditions = rec(data.conditions);
    const inputRec = rec(conditions?.input);
    const axis = conditions?.axis;
    const conditionIds = arr(conditions?.ids) ?? [];
    const conditionLabels = arr(conditions?.labels);
    const inputValues = inputRec ? (arr(inputRec.values) ?? []).map((value) => typeof value === "number" ? value : Number.NaN) : void 0;
    const mode = data.mode;
    const observations = rec(data.observations);
    const aggregates = rec(data.aggregates);
    const aggregateTrimmedCounts = arr(aggregates?.trimmedCounts);
    const response = rec(
      mode === "repeats" ? observations?.response : aggregates?.response
    );
    const responseValues = (arr(response?.values) ?? []).map(
      (value) => value === null ? null : typeof value === "number" ? value : Number.NaN
    );
    const responseMethod = response?.method ?? parameters.responseMethod;
    const repeatDesign = parameters.repeatDesign;
    if (parameters.responseMethod !== response?.method) {
      return fail(
        "SCIENCE_RESPONSE_METHOD_MISMATCH",
        "science",
        `parameters.responseMethod is ${JSON.stringify(parameters.responseMethod)} but the response values are typed as ${JSON.stringify(response?.method)}. Cortexel will not relabel one scientific quantity as another.`,
        "/parameters/responseMethod"
      );
    }
    const rateResponse = responseMethod === "mean_firing_rate" || responseMethod === "peak_firing_rate";
    const eventScopeVerification = verifyResponseEventScope(data.eventScope);
    if (!eventScopeVerification.ok) {
      return fail(
        "SCIENCE_EVENT_SCOPE_UNVERIFIABLE",
        "science",
        eventScopeVerification.message,
        `/data${eventScopeVerification.path}`
      );
    }
    const eventScopeAuthority = eventScopeVerification.authority;
    let rateAuthority;
    if (rateResponse) {
      rateAuthority = verifyResponseRateAuthority(
        response?.rateNormalization,
        data.eventScope
      );
      if (!rateAuthority.ok) {
        return fail(
          rateAuthority.path.startsWith("/eventScope") ? "SCIENCE_EVENT_SCOPE_UNVERIFIABLE" : "SCIENCE_NORMALIZATION_UNVERIFIABLE",
          "science",
          rateAuthority.message,
          rateAuthority.path === "/rateNormalization" ? `/data/${mode === "aggregates" ? "aggregates" : "observations"}/response/rateNormalization` : `/data${rateAuthority.path}`
        );
      }
    }
    let peakBasisVerification;
    let binnedPeakValueLatticeVerification;
    if (responseMethod === "peak_firing_rate") {
      peakBasisVerification = verifyPeakBasisAgainstWindow(
        response?.basis,
        data.measurementWindow
      );
      if (!peakBasisVerification.ok) {
        return fail(
          "SCIENCE_NORMALIZATION_UNVERIFIABLE",
          "science",
          peakBasisVerification.message,
          peakBasisVerification.path === "/measurementWindow" ? "/data/measurementWindow" : `/data/${mode === "aggregates" ? "aggregates" : "observations"}/response${peakBasisVerification.path}`
        );
      }
      if (mode === "aggregates" && peakBasisVerification.kind === "binned_count" && rateAuthority?.ok === true) {
        binnedPeakValueLatticeVerification = verifyBinnedPeakValueLattice(
          responseValues,
          response?.basis,
          response?.unit,
          rateAuthority.integerDivisor,
          mode,
          parameters.estimator,
          mode === "aggregates" ? arr(aggregates?.sampleCounts) : void 0
        );
        if (!binnedPeakValueLatticeVerification.ok) {
          const responseBase = `/data/${mode === "aggregates" ? "aggregates" : "observations"}/response`;
          const instancePath = binnedPeakValueLatticeVerification.path.startsWith("/values/") ? `${responseBase}${binnedPeakValueLatticeVerification.path}` : binnedPeakValueLatticeVerification.path.startsWith("/sampleCounts") ? `/data/aggregates${binnedPeakValueLatticeVerification.path}` : binnedPeakValueLatticeVerification.path === "/estimator" ? "/parameters/estimator" : `${responseBase}${binnedPeakValueLatticeVerification.path}`;
          return fail(
            "SCIENCE_NORMALIZATION_UNVERIFIABLE",
            "science",
            binnedPeakValueLatticeVerification.message,
            instancePath
          );
        }
      }
    }
    const responseAudit = rec(response?.audit);
    const rawAuditedEventCounts = arr(responseAudit?.eventCounts);
    const auditedEventCounts = rawAuditedEventCounts ? rawAuditedEventCounts.map(
      (value) => value === null ? null : typeof value === "number" ? value : Number.NaN
    ) : void 0;
    const rawAuditedPeakBinCounts = arr(responseAudit?.peakBinCounts);
    const auditedPeakBinCounts = rawAuditedPeakBinCounts ? rawAuditedPeakBinCounts.map(
      (value) => value === null ? null : typeof value === "number" ? value : Number.NaN
    ) : void 0;
    const definedPeakValueCount = responseMethod === "peak_firing_rate" ? responseValues.filter((value) => value !== null).length : 0;
    const undefinedPeakValueCount = responseMethod === "peak_firing_rate" ? responseValues.length - definedPeakValueCount : 0;
    const binnedPeakLatticeCheckedValueCount = binnedPeakValueLatticeVerification?.ok === true ? binnedPeakValueLatticeVerification.checkedValueCount : null;
    const hasCheckedBinnedPeakLatticeValue = binnedPeakLatticeCheckedValueCount !== null && binnedPeakLatticeCheckedValueCount > 0;
    const definedAuditedPeakBinCount = auditedPeakBinCounts ? auditedPeakBinCounts.filter((count) => count !== null).length : 0;
    const hasDefinedAuditedPeakBinCounts = definedAuditedPeakBinCount > 0;
    const definedAuditedEventCount = auditedEventCounts ? auditedEventCounts.filter((count) => count !== null).length : 0;
    const hasDefinedAuditedEventCounts = definedAuditedEventCount > 0;
    if (mode === "repeats" && peakBasisVerification?.ok === true && peakBasisVerification.kind === "binned_count") {
      if (!auditedPeakBinCounts) {
        return fail(
          "SCIENCE_NORMALIZATION_UNVERIFIABLE",
          "science",
          "raw binned-count peaks require exact parallel peakBinCounts so repeat rates and condition estimators can be re-derived without mode-dependent rounding",
          "/data/observations/response/audit/peakBinCounts"
        );
      }
      if (auditedPeakBinCounts.length !== responseValues.length) {
        return fail(
          "SEMANTIC_LENGTH_MISMATCH",
          "semantic",
          "response.audit.peakBinCounts must be parallel to raw binned-peak response values",
          "/data/observations/response/audit/peakBinCounts"
        );
      }
      const binnedBasis = rec(response?.basis);
      const binWidth = rec(binnedBasis?.binWidth);
      const binWidthValue = num(binWidth?.value);
      const binWidthUnit = binWidth?.unit;
      const rateUnit = response?.unit;
      for (let index = 0; index < auditedPeakBinCounts.length; index++) {
        const peakBinCount = auditedPeakBinCounts[index];
        const suppliedRate = responseValues[index];
        if (peakBinCount !== null && (!Number.isSafeInteger(peakBinCount) || peakBinCount < 0)) {
          return fail(
            "SCIENCE_COUNT_NOT_INTEGER",
            "science",
            "audited peak-bin counts must be exact non-negative safe integers or null",
            `/data/observations/response/audit/peakBinCounts/${index}`
          );
        }
        if (peakBinCount === null !== (suppliedRate === null)) {
          return fail(
            "SCIENCE_NORMALIZATION_UNVERIFIABLE",
            "science",
            "an audited peak-bin count must be null exactly where its response is null",
            `/data/observations/response/audit/peakBinCounts/${index}`
          );
        }
        if (peakBinCount !== null && suppliedRate !== null && rateAuthority?.ok === true && binWidthValue !== void 0 && binWidthUnit && rateUnit) {
          let expectedRate;
          try {
            expectedRate = deriveExactAggregateCountRateInUnit(
              BigInt(peakBinCount),
              rateAuthority.integerDivisor,
              1,
              binWidthValue,
              binWidthUnit,
              rateUnit
            );
          } catch (error) {
            return fail(
              "SCIENCE_NORMALIZATION_UNVERIFIABLE",
              "science",
              `the raw binned-peak rate could not be re-derived from its exact max-bin count (${error instanceof Error ? error.message : "numeric failure"})`,
              `/data/observations/response/values/${index}`
            );
          }
          if ((suppliedRate === 0 ? 0 : suppliedRate) !== expectedRate) {
            return fail(
              "SCIENCE_NORMALIZATION_UNVERIFIABLE",
              "science",
              `raw binned-peak rate ${suppliedRate} ${rateUnit} does not equal the one-round exact rate ${expectedRate} ${rateUnit} derived from peak-bin count ${peakBinCount}, divisor ${rateAuthority.integerDivisor}, and bin width ${binWidthValue} ${binWidthUnit}`,
              `/data/observations/response/values/${index}`
            );
          }
        }
      }
    } else if (auditedPeakBinCounts) {
      return fail(
        "SCIENCE_NORMALIZATION_UNVERIFIABLE",
        "science",
        "peakBinCounts is legal only for raw binned-count peak responses",
        `/data/${mode === "aggregates" ? "aggregates" : "observations"}/response/audit/peakBinCounts`
      );
    }
    if (auditedEventCounts) {
      if (auditedEventCounts.length !== responseValues.length) {
        return fail(
          "SEMANTIC_LENGTH_MISMATCH",
          "semantic",
          "response.audit.eventCounts must be parallel to response.values",
          "/data/observations/response/audit/eventCounts"
        );
      }
      const auditWindow = rec(data.measurementWindow);
      const auditWindowStart = num(auditWindow?.start);
      const auditWindowStop = num(auditWindow?.stop);
      const auditWindowUnit = auditWindow?.unit;
      const auditedRateUnit = response?.unit;
      for (let index = 0; index < auditedEventCounts.length; index++) {
        const eventCount = auditedEventCounts[index];
        if (eventCount !== null && (!Number.isSafeInteger(eventCount) || eventCount < 0)) {
          return fail(
            "SCIENCE_COUNT_NOT_INTEGER",
            "science",
            "audited event counts must be exact non-negative safe integers or null",
            `/data/observations/response/audit/eventCounts/${index}`
          );
        }
        if (eventCount === null !== (responseValues[index] === null)) {
          return fail(
            "SCIENCE_NORMALIZATION_UNVERIFIABLE",
            "science",
            "an audited event count must be null exactly where its response is null",
            `/data/observations/response/audit/eventCounts/${index}`
          );
        }
        const suppliedRate = responseValues[index];
        if (eventCount !== null && suppliedRate !== null && auditWindowStart !== void 0 && auditWindowStop !== void 0 && auditWindowUnit && auditedRateUnit && dimensionOf(auditWindowUnit) === "time" && dimensionOf(auditedRateUnit) === "frequency") {
          let expectedRate;
          try {
            expectedRate = deriveExactCountRateInUnit(
              eventCount,
              rateAuthority?.ok === true ? rateAuthority.integerDivisor : 1,
              auditWindowStart,
              auditWindowStop,
              auditWindowUnit,
              auditedRateUnit
            );
          } catch (error) {
            const detail = error instanceof Error ? error.message : "numeric conversion failed";
            return fail(
              "SCIENCE_NORMALIZATION_UNVERIFIABLE",
              "science",
              `the per-repeat mean-rate audit could not be re-derived (${detail})`,
              `/data/observations/response/values/${index}`
            );
          }
          if ((suppliedRate === 0 ? 0 : suppliedRate) !== expectedRate) {
            return fail(
              "SCIENCE_NORMALIZATION_UNVERIFIABLE",
              "science",
              `supplied mean rate ${suppliedRate} ${auditedRateUnit} does not equal the one-round exact ${rateAuthority?.ok === true ? rateAuthority.normalization : "declared normalization"} derived from audited count ${eventCount}, integer divisor ${rateAuthority?.ok === true ? rateAuthority.integerDivisor : 1}, and the exact typed measurement window; the derived value is ${expectedRate} ${auditedRateUnit}`,
              `/data/observations/response/values/${index}`
            );
          }
        }
      }
    }
    const uncertainty = rec(parameters.uncertainty);
    const uncertaintyKind = uncertainty?.kind ?? "none";
    if (uncertaintyKind !== "none") {
      return fail(
        "SCIENCE_UNCERTAINTY_UNSUPPORTED_FOR_SKILL",
        "science",
        `response-curve uncertainty variant ${uncertaintyKind} is not drawable in the current build. Cortexel refuses to drop or reinterpret it.`,
        "/parameters/uncertainty"
      );
    }
    if (responseMethod === "first_spike_latency" && response?.latencyReference !== "measurement_window_start") {
      return fail(
        "SCIENCE_NORMALIZATION_UNVERIFIABLE",
        "science",
        "revision 2 supports first-spike latency only from measurement_window_start; stimulus onset has no typed coordinate relative to the window",
        `/data/${mode === "aggregates" ? "aggregates" : "observations"}/response/latencyReference`
      );
    }
    if (responseMethod === "first_spike_latency" && response?.latencyReference === "measurement_window_start") {
      const window2 = rec(data.measurementWindow);
      const windowStart2 = num(window2?.start);
      const windowStop2 = num(window2?.stop);
      const windowUnit2 = window2?.unit;
      const responseUnit2 = response?.unit;
      if (windowStart2 !== void 0 && windowStop2 !== void 0 && windowUnit2 && responseUnit2 && dimensionOf(windowUnit2) === dimensionOf(responseUnit2)) {
        const closedStop = window2?.boundary === "[start,stop]";
        for (let index = 0; index < responseValues.length; index++) {
          const latency = responseValues[index];
          if (latency === null || latency < 0) continue;
          const comparison = compareExactUnitArraySumToDifference(
            [latency],
            responseUnit2,
            { value: windowStart2, unit: windowUnit2 },
            { value: windowStop2, unit: windowUnit2 }
          );
          if (comparison > 0 || comparison === 0 && !closedStop) {
            return fail(
              "SCIENCE_LATENCY_OUTSIDE_WINDOW",
              "science",
              `first-spike latency ${latency} ${responseUnit2} is referenced to the measurement-window start but lies outside the declared ${closedStop ? "closed" : "half-open"} window.`,
              `/data/${mode === "aggregates" ? "aggregates" : "observations"}/response/values/${index}`
            );
          }
        }
      }
    }
    const derived = deriveResponseCurve({
      conditions: {
        axis,
        ids: conditionIds,
        ...conditionLabels ? { labels: conditionLabels } : {},
        ...axis === "numeric" ? {
          inputValues,
          inputScale: inputRec?.scale ?? "linear"
        } : {}
      },
      estimator: parameters.estimator,
      responseMethod,
      repeatDesign,
      ...typeof parameters.trimFraction === "number" ? { trimFraction: parameters.trimFraction } : {},
      ...mode === "repeats" && peakBasisVerification?.ok === true && peakBasisVerification.kind === "binned_count" && auditedPeakBinCounts && rateAuthority?.ok === true ? {
        binnedPeakAudit: {
          peakBinCounts: auditedPeakBinCounts,
          integerDivisor: rateAuthority.integerDivisor,
          binWidthValue: num(rec(rec(response?.basis)?.binWidth)?.value),
          binWidthUnit: rec(rec(response?.basis)?.binWidth)?.unit,
          rateUnit: response?.unit
        }
      } : {},
      ...mode === "repeats" ? {
        repeats: {
          conditionIds: arr(observations?.conditionIds) ?? [],
          repeatIds: arr(observations?.repeatIds) ?? [],
          responses: responseValues,
          attemptedCounts: (arr(observations?.attemptedCounts) ?? []).map(
            (value) => typeof value === "number" ? value : Number.NaN
          )
        }
      } : {
        aggregates: {
          responses: responseValues,
          sampleCounts: (arr(aggregates?.sampleCounts) ?? []).map(
            (value) => value === null ? null : typeof value === "number" ? value : Number.NaN
          ),
          excludedCounts: (arr(aggregates?.excludedCounts) ?? []).map(
            (value) => typeof value === "number" ? value : Number.NaN
          ),
          ...aggregateTrimmedCounts ? {
            trimmedCounts: aggregateTrimmedCounts.map(
              (value) => typeof value === "number" ? value : Number.NaN
            )
          } : {}
        }
      }
    });
    if (!derived.ok) {
      const { issue: issue2 } = derived;
      if (issue2.code === "length_mismatch") {
        return fail("SEMANTIC_LENGTH_MISMATCH", "semantic", issue2.message, issue2.path);
      }
      if (issue2.code === "duplicate_condition" || issue2.code === "duplicate_repeat") {
        return fail("SEMANTIC_DUPLICATE_ID", "semantic", issue2.message, issue2.path);
      }
      if (issue2.code === "duplicate_input") {
        return fail("SCIENCE_RESPONSE_INPUT_DUPLICATE", "science", issue2.message, issue2.path);
      }
      if (issue2.code === "unknown_condition") {
        return fail("SEMANTIC_UNKNOWN_REFERENCE", "semantic", issue2.message, issue2.path);
      }
      if (issue2.code === "paired_repeats_incomplete") {
        return fail("SCIENCE_PAIRED_REPEATS_INCOMPLETE", "science", issue2.message, issue2.path);
      }
      if (issue2.code === "attempted_count_mismatch") {
        return fail("SCIENCE_NORMALIZATION_UNVERIFIABLE", "science", issue2.message, issue2.path);
      }
      if (issue2.code === "count_not_integer") {
        return fail("SCIENCE_COUNT_NOT_INTEGER", "science", issue2.message, issue2.path);
      }
      if (issue2.code === "count_estimator_incoherent") {
        return fail("SCIENCE_COUNT_ESTIMATOR_INCOHERENT", "science", issue2.message, issue2.path);
      }
      if (issue2.code === "rate_audit_incoherent") {
        return fail("SCIENCE_NORMALIZATION_UNVERIFIABLE", "science", issue2.message, issue2.path);
      }
      if (issue2.code === "trimmed_count_incoherent") {
        return fail("SCIENCE_NORMALIZATION_UNVERIFIABLE", "science", issue2.message, issue2.path);
      }
      if (issue2.code === "invalid_response_value") {
        return fail("SCIENCE_RESPONSE_VALUE_INVALID", "science", issue2.message, issue2.path);
      }
      if (issue2.code === "nonpositive_log_input") {
        return fail("RENDER_LOG_SCALE_NONPOSITIVE_DOMAIN", "render", issue2.message, issue2.path);
      }
      return fail(
        issue2.message.includes("binary64") || issue2.code === "invalid_numeric" ? "SCIENCE_NUMERIC_RESOLUTION_UNREPRESENTABLE" : "SCIENCE_NORMALIZATION_UNVERIFIABLE",
        "science",
        issue2.message,
        issue2.path
      );
    }
    const curve = derived.result;
    const responseUnit = response?.unit ?? "1";
    const inputUnit = axis === "numeric" ? inputRec?.unit ?? "1" : null;
    const inputLabel = axis === "numeric" ? inputRec?.label ?? "input" : conditions?.inputLabel ?? "condition";
    const inputUnitLabel = axis === "numeric" ? unitLabel(inputUnit) : "";
    const xLabel = axis === "numeric" ? inputUnitLabel ? `${inputLabel} (${inputUnitLabel})` : inputLabel : inputLabel;
    const responseLabel = responseMethod.replaceAll("_", " ");
    const responseUnitLabel = unitLabel(responseUnit);
    const verifiedRateAuthority = rateAuthority?.ok === true ? rateAuthority : void 0;
    const rateAuthorityText = verifiedRateAuthority ? responseRateAuthorityText(verifiedRateAuthority) : null;
    const rateAxisQualifier = verifiedRateAuthority ? responseRateAxisQualifier(verifiedRateAuthority) : null;
    const eventAxisQualifier = responseEventAxisQualifier(responseMethod, eventScopeAuthority);
    const qualifiedResponseLabel = `${responseLabel} [${eventAxisQualifier}${rateAxisQualifier ? `; ${rateAxisQualifier}` : ""}]`;
    const yLabel = responseUnitLabel ? `${qualifiedResponseLabel} (${responseUnitLabel})` : qualifiedResponseLabel;
    const curveLabel = parameters.curveLabel ?? parameters.curveId ?? responseLabel;
    const window = rec(data.measurementWindow) ?? {};
    const windowBoundary = window.boundary ?? "[start,stop)";
    const windowStart = num(window.start) ?? 0;
    const windowStop = num(window.stop) ?? 0;
    const windowUnit = window.unit ?? "ms";
    const windowBasis = `${windowBoundary} ${exactNumberText(windowStart)} to ${exactNumberText(windowStop)} ${windowUnit}`;
    const eventMembershipText = responseEventMembershipText(eventScopeAuthority);
    const eventScopeText = responseEventScopeText(eventScopeAuthority);
    const normalizedEventScope = eventScopeAuthority.normalizedScope;
    const eventScopeDigest = canonicalDigest(normalizedEventScope);
    let responseBasis = rateAuthorityText ? `caller-declared event scope ${eventScopeText}; rate normalization ${rateAuthorityText}; measurement window ${windowBasis}` : `caller-declared event scope ${eventScopeText}; measurement window ${windowBasis}`;
    if (responseMethod === "first_spike_latency") {
      responseBasis = `caller-declared event scope ${eventScopeText}; latency is the first selected spike (the minimum over the superposed union for a pooled scope) from ${response?.latencyReference ?? "unspecified reference"}; measurement window ${windowBasis}`;
    } else if (responseMethod === "peak_firing_rate") {
      responseBasis = auditedPeakBinCounts ? hasDefinedAuditedPeakBinCounts ? `caller-declared event scope ${eventScopeText}; caller-supplied exact max-bin counts after the declared sender pooling; defined repeat rates and defined ${String(parameters.estimator)} condition estimates re-derived at count level with one final rounding; ${rateAuthorityText}; ${peakEstimatorBasisText(response?.basis)}; measurement window ${windowBasis}` : `caller-declared event scope ${eventScopeText}; caller-supplied peak-bin count audit contained only nulls; null-mask alignment was verified, but no repeat rate or condition estimate existed to re-derive; ${rateAuthorityText}; ${peakEstimatorBasisText(response?.basis)}; measurement window ${windowBasis}` : definedPeakValueCount > 0 ? `caller-declared event scope ${eventScopeText}; caller-supplied peak value after the declared sender pooling; ${rateAuthorityText}; ${peakEstimatorBasisText(response?.basis)}; measurement window ${windowBasis}` : `caller-declared event scope ${eventScopeText}; no defined peak value was supplied; ${rateAuthorityText}; ${peakEstimatorBasisText(response?.basis)}; measurement window ${windowBasis}`;
    }
    const tableRows = [];
    for (const condition of curve.conditions) {
      if (mode === "repeats") {
        for (const repeat of condition.repeats) {
          tableRows.push([
            condition.conditionId,
            condition.conditionLabel,
            condition.input,
            inputUnit,
            repeat.repeatId,
            repeat.response,
            responseUnit,
            responseMethod,
            verifiedRateAuthority?.normalization ?? null,
            eventScopeAuthority.recordedSenderCount,
            repeat.response === null ? "true" : "false",
            null,
            null,
            null,
            responseBasis,
            null,
            null,
            null,
            null,
            null,
            repeat.estimatorRole,
            null,
            auditedPeakBinCounts?.[repeat.sourceOrdinal] ?? null,
            repeat.response !== null && hasDefinedAuditedPeakBinCounts ? "exact_peak_bin_count_to_condition_estimator_rate_one_round" : null,
            eventScopeAuthority.kind,
            eventScopeAuthority.selectionId,
            eventMembershipText,
            eventScopeAuthority.selectedEventTrainCount
          ]);
        }
      }
      tableRows.push([
        condition.conditionId,
        condition.conditionLabel,
        condition.input,
        inputUnit,
        null,
        condition.estimate,
        responseUnit,
        responseMethod,
        verifiedRateAuthority?.normalization ?? null,
        eventScopeAuthority.recordedSenderCount,
        condition.estimate === null ? "true" : "false",
        parameters.estimator,
        condition.sampleCount,
        condition.excludedCount,
        responseBasis,
        uncertaintyKind,
        null,
        null,
        null,
        null,
        "aggregate_estimate",
        condition.trimmedCount,
        null,
        condition.estimate !== null && hasDefinedAuditedPeakBinCounts ? "exact_peak_bin_count_to_condition_estimator_rate_one_round" : null,
        eventScopeAuthority.kind,
        eventScopeAuthority.selectionId,
        eventMembershipText,
        eventScopeAuthority.selectedEventTrainCount
      ]);
    }
    const aggregateSampleCounts = curve.conditions.map((condition) => condition.retainedCount).filter((count) => count > 0);
    const aggregateSampleCountExtent = finiteExtent(aggregateSampleCounts);
    const aggregateSampleCountBase = aggregateSampleCounts.length === 0 ? "0 retained in every condition" : aggregateSampleCountExtent.min === aggregateSampleCountExtent.max ? String(aggregateSampleCounts[0]) : `${aggregateSampleCountExtent.min}-${aggregateSampleCountExtent.max} by condition`;
    const aggregateSampleCount = curve.conditionsWithoutEstimate > 0 && aggregateSampleCounts.length > 0 ? `${aggregateSampleCountBase} where defined; ${curve.conditionsWithoutEstimate} condition${curve.conditionsWithoutEstimate === 1 ? "" : "s"} undefined` : aggregateSampleCountBase;
    const missingValueCount = curve.excludedCount;
    const derivedFacts = {
      ...missingValueCount > 0 ? { missingValueCount } : {},
      eventScopeMembershipCardinalityOnly: eventScopeAuthority.membershipKind === "cardinality_only",
      eventScopeExternalAuthorityDeclared: true,
      ...mode === "aggregates" ? {
        aggregateEstimator: parameters.estimator,
        aggregateSampleCount
      } : {}
    };
    const lineStatement = axis === "nominal" ? "No guide line is drawn because the condition axis is nominal." : "A straight guide joins only adjacent defined conditions; gaps break it, and it is neither a fit nor interpolation.";
    const responseExtent = finiteExtent(curve.conditions.map((condition) => condition.estimate).filter((value) => value !== null));
    const responseRangeStatement = responseExtent ? `Response ranges from ${exactNumberText(responseExtent.min)} to ${exactNumberText(responseExtent.max)} ${responseUnit}.` : "No declared condition has a usable response estimate.";
    const conditionCoverageStatement = curve.conditionsWithoutEstimate === 0 ? `${curve.conditions.length} ${axis} conditions; every declared condition has a usable estimate.` : curve.conditionsWithoutEstimate === 1 ? `${curve.conditions.length} ${axis} conditions; 1 condition has no usable estimate and retains an explicit gap.` : `${curve.conditions.length} ${axis} conditions; ${curve.conditionsWithoutEstimate} conditions have no usable estimate and retain explicit gaps.`;
    const summaryStatements = [
      `Response curve ${curveLabel}: ${responseMethod} in ${responseUnit}${rateAuthorityText ? ` with ${rateAuthorityText}` : ""} against ${inputLabel}${inputUnit ? ` in ${inputUnit}` : ""} on a ${curve.inputScale ?? axis} scale. Declared repeat design: ${String(parameters.repeatDesign)}.`,
      `Caller-declared event scope: ${eventScopeText}. Cortexel verified the internal structure and bindings, not the claim against an external recorder.`,
      conditionCoverageStatement,
      `Estimator ${String(parameters.estimator)} retained ${curve.retainedCount} of ${curve.attemptedCount} attempted repeats; ${curve.trimmedCount} defined responses were removed symmetrically by trimming and ${curve.excludedCount} undefined responses were excluded and counted.`,
      `Measurement window ${windowBasis}.`,
      `Response basis: ${responseBasis}.`,
      ...responseMethod === "peak_firing_rate" ? definedPeakValueCount > 0 ? ["Defined peak values were supplied by the caller; Cortexel verified basis consistency but did not rederive peaks from unavailable event trains."] : ["No defined peak value was supplied; Cortexel verified the declared estimator basis and retained the explicit gaps."] : [],
      ...auditedPeakBinCounts && hasDefinedAuditedPeakBinCounts ? ["For raw binned peaks, exact max-bin counts were supplied: Cortexel re-derived every defined repeat rate and formed each defined condition estimator at count level before one final rounding into the declared rate unit."] : auditedPeakBinCounts ? [`The raw binned-peak audit contained 0 defined max-bin counts and ${auditedPeakBinCounts.length} null entries; Cortexel verified null-mask alignment, but no repeat rate or condition estimator existed to re-derive.`] : [],
      responseRangeStatement,
      mode === "aggregates" ? "Only per-condition aggregates were supplied; individual repeats and pairing cannot be inspected." : "Raw attempted repeats are retained in deterministic condition and repeat order in the data table.",
      lineStatement
    ];
    const operationOutput = {
      axis,
      inputScale: curve.inputScale,
      inputUnit,
      responseMethod,
      responseUnit,
      normalizedDeclaredEventScope: normalizedEventScope,
      eventScopeDigest,
      rateNormalization: verifiedRateAuthority?.normalization ?? null,
      declaredRecordedSenderCount: eventScopeAuthority.recordedSenderCount,
      structurallyDerivedSelectedEventTrainCount: eventScopeAuthority.selectedEventTrainCount,
      rateIntegerDivisor: verifiedRateAuthority?.integerDivisor ?? null,
      responseBasis,
      peakBasis: responseMethod === "peak_firing_rate" ? response?.basis : null,
      peakBasisUniformExposureVerified: peakBasisVerification?.ok === true && peakBasisVerification.kind === "binned_count" ? peakBasisVerification.uniformExposureVerified : null,
      binnedPeakValueLatticeVerified: hasCheckedBinnedPeakLatticeValue ? true : null,
      binnedPeakValueLatticeCheckedValueCount: binnedPeakLatticeCheckedValueCount,
      binnedPeakValueLatticeEstimatorDenominatorMinimum: binnedPeakValueLatticeVerification?.ok === true ? binnedPeakValueLatticeVerification.estimatorDenominatorMinimum : null,
      binnedPeakValueLatticeEstimatorDenominatorMaximum: binnedPeakValueLatticeVerification?.ok === true ? binnedPeakValueLatticeVerification.estimatorDenominatorMaximum : null,
      estimator: parameters.estimator,
      conditions: curve.conditions.map((condition) => ({
        conditionId: condition.conditionId,
        conditionLabel: condition.conditionLabel,
        input: condition.input,
        estimate: condition.estimate,
        attemptedCount: condition.attemptedCount,
        sampleCount: condition.sampleCount,
        retainedCount: condition.retainedCount,
        trimmedCount: condition.trimmedCount,
        excludedCount: condition.excludedCount
      })),
      repeats: curve.sortedRepeats.map((repeat) => ({
        conditionId: repeat.conditionId,
        repeatId: repeat.repeatId,
        response: repeat.response,
        peakBinCount: auditedPeakBinCounts?.[repeat.sourceOrdinal] ?? null,
        estimatorRole: repeat.estimatorRole
      }))
    };
    const sourceOrdinals = curve.sortedRepeats.map((repeat) => repeat.sourceOrdinal);
    const derivationOperation2 = {
      id: "response_curve.estimate_by_condition",
      algorithm: "cortexel.response_curve.exact_condition_estimator",
      algorithmRevision: 1,
      parameters: {
        mode,
        axis,
        inputScale: curve.inputScale,
        estimator: parameters.estimator,
        responseMethod,
        normalizedDeclaredEventScope: normalizedEventScope,
        eventScopeDigest,
        rateNormalization: verifiedRateAuthority?.normalization ?? null,
        declaredRecordedSenderCount: eventScopeAuthority.recordedSenderCount,
        structurallyDerivedSelectedEventTrainCount: eventScopeAuthority.selectedEventTrainCount,
        rateIntegerDivisor: verifiedRateAuthority?.integerDivisor ?? null,
        peakBasis: responseMethod === "peak_firing_rate" ? response?.basis : null,
        peakBasisUniformExposurePolicy: peakBasisVerification?.ok === true && peakBasisVerification.kind === "binned_count" ? "exact_physical_endpoint_difference_equals_typed_bin_width_every_interval" : null,
        peakBinCountAudit: auditedPeakBinCounts ? "required_parallel_exact_max_bin_counts" : null,
        peakCountDerivationAlgorithm: hasDefinedAuditedPeakBinCounts ? "exact_peak_bin_count_to_condition_estimator_rate_one_round" : null,
        repeatDesign,
        ...typeof parameters.trimFraction === "number" ? { trimFraction: parameters.trimFraction } : {},
        missingPolicy: "exclude_null_and_count",
        numericConditionOrder: "unique_input_ascending",
        categoricalConditionOrder: "declared",
        repeatOrder: "condition_then_code_unit_repeat_id_then_source_ordinal",
        trimBoundaryTieOrder: mode !== "repeats" ? null : auditedPeakBinCounts ? hasDefinedAuditedPeakBinCounts ? "exact_peak_bin_count_then_code_unit_repeat_id_then_source_ordinal" : null : "numeric_response_then_code_unit_repeat_id_then_source_ordinal",
        nominalGuide: "none",
        orderedGuide: "adjacent_defined_conditions_only"
      },
      inputDigest: canonicalDigest({
        mode,
        conditions,
        measurementWindow: data.measurementWindow,
        normalizedEventScope,
        ...mode === "repeats" ? { observations } : { aggregates },
        ...verifiedRateAuthority ? {
          rateNormalization: verifiedRateAuthority.normalization,
          declaredRecordedSenderCount: verifiedRateAuthority.recordedSenderCount,
          rateIntegerDivisor: verifiedRateAuthority.integerDivisor
        } : {},
        estimator: parameters.estimator,
        responseMethod,
        repeatDesign,
        ...typeof parameters.trimFraction === "number" ? { trimFraction: parameters.trimFraction } : {}
      }),
      outputDigest: canonicalDigest(operationOutput),
      receipt: {
        rawRepeatsSupplied: curve.rawRepeatsSupplied,
        submittedRowsMatchDeclaredAttemptedCounts: mode === "repeats" ? true : null,
        declaredPairedRepeatIdSetsEqual: mode === "repeats" && repeatDesign === "paired" ? true : null,
        conditionOrder: curve.conditions.map((condition) => condition.conditionId),
        attemptedCount: curve.attemptedCount,
        retainedCount: curve.retainedCount,
        trimmedCount: curve.trimmedCount,
        excludedCount: curve.excludedCount,
        conditionsWithoutEstimate: curve.conditionsWithoutEstimate,
        eventScopeVariantRecognized: true,
        eventScopeSelectionIdStructurallyValid: true,
        eventKindAndCompletenessLiteralsValidated: true,
        eventPoolingLiteralMatchesVariant: true,
        rateNormalizationScopeCompatibilityVerified: verifiedRateAuthority ? true : null,
        declaredEventScopeKind: eventScopeAuthority.kind,
        declaredEventSelectionId: eventScopeAuthority.selectionId,
        declaredEventKind: eventScopeAuthority.eventKind,
        declaredEventCompleteness: eventScopeAuthority.eventCompleteness,
        declaredEventPoolingOperator: eventScopeAuthority.poolingOperator,
        declaredEventMembershipBinding: eventScopeAuthority.membershipKind,
        structurallyDerivedSelectedEventTrainCount: eventScopeAuthority.selectedEventTrainCount,
        declaredRecordedSenderCount: eventScopeAuthority.recordedSenderCount,
        explicitMemberIdentifiersUniqueVerified: eventScopeAuthority.membershipKind === "explicit_sender_ids" ? true : null,
        explicitMemberIdentifierCountMatchVerified: eventScopeAuthority.membershipKind === "explicit_sender_ids" ? true : null,
        explicitMemberIdentifierOrderNormalizedForSemanticDigest: eventScopeAuthority.membershipKind === "explicit_sender_ids" ? true : null,
        declaredMembershipCanonicalizationId: eventScopeAuthority.membershipKind === "canonical_sender_ids_digest" ? RESPONSE_EVENT_MEMBERSHIP_CANONICALIZATION_ID : eventScopeAuthority.membershipKind === "explicit_sender_ids" ? RESPONSE_EVENT_MEMBERSHIP_CANONICALIZATION_ID : null,
        derivedExplicitMembershipDigest: eventScopeAuthority.membershipKind === "explicit_sender_ids" ? responseEventMembershipDigest(
          arr(rec(eventScopeAuthority.normalizedScope.membershipBinding)?.senderIds) ?? []
        ) : null,
        canonicalMembershipDigestSyntaxAndCanonicalizationVerified: eventScopeAuthority.membershipKind === "canonical_sender_ids_digest" ? true : null,
        canonicalMembershipPreimageMatchVerification: eventScopeAuthority.membershipKind === "canonical_sender_ids_digest" ? "not_evaluable_preimage_unavailable" : null,
        canonicalMembershipPreimageCardinalityVerification: eventScopeAuthority.membershipKind === "canonical_sender_ids_digest" ? "not_evaluable_preimage_unavailable" : null,
        externalEventScopeClaimsVerification: "not_performed_source_unavailable",
        externalMemberIdentifierReferentsVerification: "not_performed_source_unavailable",
        eventScopeDigest,
        rateNormalization: verifiedRateAuthority?.normalization ?? null,
        rateIntegerDivisor: verifiedRateAuthority?.integerDivisor ?? null,
        peakBasisVerified: responseMethod === "peak_firing_rate" ? true : null,
        callerSuppliedPeakValue: responseMethod === "peak_firing_rate" && definedPeakValueCount > 0 ? true : null,
        peakValueRecomputed: responseMethod === "peak_firing_rate" && definedPeakValueCount > 0 ? false : null,
        definedPeakValueCount: responseMethod === "peak_firing_rate" ? definedPeakValueCount : null,
        undefinedPeakValueCount: responseMethod === "peak_firing_rate" ? undefinedPeakValueCount : null,
        peakBasisKind: peakBasisVerification?.ok === true ? peakBasisVerification.kind : null,
        peakBasisUniformExposureVerified: peakBasisVerification?.ok === true && peakBasisVerification.kind === "binned_count" ? peakBasisVerification.uniformExposureVerified : null,
        peakBasisMaterializedCount: peakBasisVerification?.ok === true && "materializedCount" in peakBasisVerification ? peakBasisVerification.materializedCount : null,
        peakBasisWidthOrStepInWindowUnit: peakBasisVerification?.ok === true && "widthInWindowUnit" in peakBasisVerification ? peakBasisVerification.widthInWindowUnit : peakBasisVerification?.ok === true && "stepInWindowUnit" in peakBasisVerification ? peakBasisVerification.stepInWindowUnit : null,
        peakBasisWindowUnit: responseMethod === "peak_firing_rate" ? windowUnit : null,
        binnedPeakValueLatticeVerified: hasCheckedBinnedPeakLatticeValue ? true : null,
        binnedPeakValueLatticeCheckedValueCount: binnedPeakLatticeCheckedValueCount,
        binnedPeakValueLatticeEstimatorDenominatorMinimum: binnedPeakValueLatticeVerification?.ok === true ? binnedPeakValueLatticeVerification.estimatorDenominatorMinimum : null,
        binnedPeakValueLatticeEstimatorDenominatorMaximum: binnedPeakValueLatticeVerification?.ok === true ? binnedPeakValueLatticeVerification.estimatorDenominatorMaximum : null,
        binnedPeakValueLatticeAlgorithm: hasCheckedBinnedPeakLatticeValue ? "exact_integer_peak_count_estimator_lattice_one_round_equality" : null,
        peakBinCountsSupplied: mode === "repeats" && peakBasisVerification?.ok === true && peakBasisVerification.kind === "binned_count" ? true : null,
        ...auditedPeakBinCounts ? {
          peakBinCountNullMaskVerified: true,
          rawBinnedPeakRatesRederived: hasDefinedAuditedPeakBinCounts ? true : null,
          conditionPeakEstimatorDerivedAtCountLevel: hasDefinedAuditedPeakBinCounts ? true : null,
          peakBinCountRateUnit: responseUnit,
          peakBinCountRateNormalization: verifiedRateAuthority?.normalization ?? null,
          peakBinCountRecordedSenderCount: verifiedRateAuthority?.recordedSenderCount ?? null,
          peakBinCountIntegerDivisor: verifiedRateAuthority?.integerDivisor ?? null,
          peakBinCountAlgorithm: hasDefinedAuditedPeakBinCounts ? "exact_peak_bin_count_to_condition_estimator_rate_one_round" : null,
          peakBinCountEstimatorOrder: hasDefinedAuditedPeakBinCounts ? "exact_peak_bin_count_then_code_unit_repeat_id_then_source_ordinal" : null,
          sumOfDefinedPeakBinCounts: auditedPeakBinCounts.reduce((total, count) => total + BigInt(count ?? 0), 0n).toString(),
          definedPeakBinCount: definedAuditedPeakBinCount,
          undefinedPeakBinCount: auditedPeakBinCounts.filter((count) => count === null).length
        } : {},
        auditEventCountsSupplied: mode === "repeats" && responseMethod === "mean_firing_rate" ? auditedEventCounts !== void 0 : null,
        ...auditedEventCounts ? {
          auditNullMaskVerified: true,
          auditRateNormalizationVerified: hasDefinedAuditedEventCounts ? true : null,
          auditRateUnit: responseUnit,
          auditRateNormalization: verifiedRateAuthority?.normalization ?? null,
          auditRecordedSenderCount: verifiedRateAuthority?.recordedSenderCount ?? null,
          auditIntegerDivisor: verifiedRateAuthority?.integerDivisor ?? null,
          auditNormalizationAlgorithm: hasDefinedAuditedEventCounts ? "exact_integer_count_to_declared_rate_unit_one_round_equality" : null,
          sumOfDefinedAuditedEventCounts: auditedEventCounts.reduce((total, count) => total + BigInt(count ?? 0), 0n).toString(),
          definedAuditedEventCount,
          undefinedAuditedEventCount: auditedEventCounts.filter((count) => count === null).length
        } : {},
        sourceOrdinals,
        sourceOrdinalDigest: canonicalDigest(sourceOrdinals),
        perCondition: curve.conditions.map((condition) => ({
          conditionId: condition.conditionId,
          attemptedCount: condition.attemptedCount,
          sampleCount: condition.sampleCount,
          retainedCount: condition.retainedCount,
          trimmedCount: condition.trimmedCount,
          excludedCount: condition.excludedCount
        }))
      }
    };
    const responseCompileContext = topologyDynamicsCompilerContext(
      context(tableRows.length, derivedFacts),
      skillId,
      {
        curveLabel,
        responseMethod,
        responseUnit,
        inputLabel,
        inputUnit: inputUnit ?? "not applicable",
        inputScale: curve.inputScale ?? axis,
        conditionCount: exactNumberText(curve.conditions.length),
        axisKind: axis,
        eventScopeKind: eventScopeAuthority.kind,
        eventSelectionId: eventScopeAuthority.selectionId,
        eventMembershipBinding: eventMembershipText,
        selectedEventTrainCount: exactNumberText(eventScopeAuthority.selectedEventTrainCount),
        recordedSenderCount: eventScopeAuthority.recordedSenderCount === null ? "not declared" : exactNumberText(eventScopeAuthority.recordedSenderCount),
        rateNormalization: verifiedRateAuthority?.normalization ?? "not applicable",
        estimator: String(parameters.estimator),
        retainedCount: exactNumberText(curve.retainedCount),
        attemptedCount: exactNumberText(curve.attemptedCount),
        trimmedCount: exactNumberText(curve.trimmedCount),
        excludedCount: exactNumberText(curve.excludedCount),
        repeatDesign: String(parameters.repeatDesign),
        responseBasis,
        windowStart: exactNumberText(windowStart),
        windowStop: exactNumberText(windowStop),
        timeUnit: windowUnit,
        responseMin: responseExtent ? exactNumberText(responseExtent.min) : "undefined",
        responseMax: responseExtent ? exactNumberText(responseExtent.max) : "undefined",
        uncertaintyStatement: uncertaintyKind === "none" ? `No uncertainty was supplied (${String(uncertainty?.reason ?? "not_provided")}).` : `Uncertainty kind ${uncertaintyKind} was declared.`,
        aggregationStatement: mode === "aggregates" ? "Only per-condition aggregates were supplied; raw repeats and pairing cannot be inspected." : "Every attempted raw repeat is retained in deterministic condition and repeat order in the complete table.",
        lineStatement
      }
    );
    return done(
      compileResponseCurveFigure(
        responseCompileContext,
        curve,
        { xLabel, yLabel, curveLabel, tableRows, summaryStatements },
        skillId
      ),
      [derivationOperation2],
      derivedFacts
    );
  }
  if (skillId === "neuro.psth") {
    const psthBinDeclaration = rec(parameters.bins);
    if (typeof psthBinDeclaration?.finalEdgeInclusive !== "boolean") {
      return fail(
        "SCIENCE_BIN_EDGES_INVALID",
        "science",
        "PSTH requires an explicit finalEdgeInclusive declaration; it does not inherit the common bin-schema display default.",
        "/parameters/bins/finalEdgeInclusive"
      );
    }
    const resolvedBins = declaredBins(psthBinDeclaration);
    const window = rec(data.relativeWindow);
    if (!resolvedBins || !window) return { pending: rendererId };
    const normalization = String(parameters.normalization);
    const denominatorPolicy = String(parameters.denominatorPolicy);
    const baselineRecord = rec(parameters.baseline);
    const baseline = baselineRecord ? {
      mode: "subtract_mean_rate",
      start: num(baselineRecord.start),
      stop: num(baselineRecord.stop)
    } : void 0;
    const common = {
      bins: {
        edges: resolvedBins.spec.edges,
        unit: resolvedBins.unit,
        boundary: String(psthBinDeclaration.boundary),
        finalEdgeInclusive: resolvedBins.spec.finalEdgeInclusive
      },
      relativeWindow: {
        start: num(window.start),
        stop: num(window.stop),
        unit: String(window.unit),
        boundary: String(window.boundary)
      },
      normalization,
      denominatorPolicy,
      ...parameters.senderExposurePolicy ? {
        senderExposurePolicy: String(parameters.senderExposurePolicy)
      } : {},
      ...baseline ? { baseline } : {}
    };
    const nullableNumbers4 = (value) => (arr(value) ?? []).map((entry) => entry === null ? null : Number(entry));
    let psth;
    try {
      if (data.mode === "prebinned") {
        const supplied = rec(data.rates);
        psth = derivePsth({
          ...common,
          mode: "prebinned",
          trialIds: strings4(data.trialIds),
          alignmentTimes: numbers4(data.alignmentTimes),
          alignmentUnit: String(data.alignmentUnit),
          counts: nullableNumbers4(data.counts),
          trialDenominators: nullableNumbers4(data.trialDenominators),
          recordedSenderCount: num(data.recordedSenderCount),
          includedTrialCount: num(data.includedTrialCount),
          excludedTrialCount: num(data.excludedTrialCount),
          ...supplied ? {
            suppliedValues: {
              kind: String(supplied.kind),
              unit: String(supplied.unit),
              values: nullableNumbers4(supplied.values)
            }
          } : {}
        });
      } else {
        const eventTimes = rec(data.eventTimes);
        psth = derivePsth({
          ...common,
          mode: "events",
          eventTimes: numbers4(eventTimes?.values),
          eventTimeUnit: String(eventTimes?.unit),
          eventSenderIds: strings4(data.eventSenderIds),
          eventTrialIds: strings4(data.eventTrialIds),
          recordedSenderIds: strings4(data.recordedSenderIds),
          trialIds: strings4(data.trialIds),
          alignmentTimes: numbers4(data.alignmentTimes),
          alignmentUnit: String(data.alignmentUnit)
        });
      }
    } catch (error) {
      if (error instanceof PsthDerivationError) {
        return fail(
          error.code,
          error.code.startsWith("RESOURCE_") ? "budget" : "science",
          error.message,
          error.instancePath
        );
      }
      throw error;
    }
    const operationOutput = {
      edges: psth.edges,
      binWidths: psth.binWidths,
      counts: psth.counts,
      trialDenominators: psth.trialDenominators,
      values: psth.values,
      valueUnit: psth.valueUnit,
      baselineCorrectedValues: psth.baselineCorrectedValues,
      baselineRate: psth.baselineRate
    };
    const operation = derivationOperation(
      "psth.derive",
      "cortexel.psth.exact_counts_normalization_and_baseline",
      {
        normalization,
        denominatorPolicy,
        senderExposurePolicy: parameters.senderExposurePolicy ?? null,
        bins: common.bins,
        relativeWindow: common.relativeWindow,
        baseline: baseline ?? null
      },
      { data, parameters },
      operationOutput,
      { ...psth.receipt }
    );
    const derivedFacts = {
      ...data.mode === "prebinned" ? { preBinned: true } : {},
      ...psth.missingBinCount > 0 ? { missingValueCount: psth.missingBinCount } : {},
      ...psth.excludedOutOfWindowCount !== null ? { excludedOutOfWindow: psth.excludedOutOfWindowCount } : {},
      ...psth.unitConversions.length > 0 ? { unitConversions: psth.unitConversions } : {},
      ...normalization === "mean_rate_per_selected_sender_per_trial" ? { rectangularSenderExposureAsserted: true } : {}
    };
    const psthFiniteValues = psth.values.filter(
      (value) => value !== null && Number.isFinite(value)
    );
    const psthValueExtent = distributionCompilerExtrema(psthFiniteValues);
    const zeroOnPsthAxis = psth.edges[0] <= 0 && 0 <= psth.edges[psth.edges.length - 1];
    const zeroIncludedByPsthMembership = zeroOnPsthAxis && (0 < psth.edges[psth.edges.length - 1] || psth.finalEdgeInclusive);
    const compileContext = distributionCompilerContext(
      context(psth.counts.length, derivedFacts),
      skillId,
      {
        seriesLabel: String(parameters.seriesLabel ?? parameters.seriesId),
        alignmentLabel: String(parameters.alignmentLabel ?? "alignment"),
        windowStart: distributionCompilerNumber(psth.relativeWindowStart),
        windowStop: distributionCompilerNumber(psth.relativeWindowStop),
        timeUnit: psth.relativeWindowUnit,
        windowBoundary: psth.relativeWindowBoundary,
        zeroVisibilityStatement: zeroIncludedByPsthMembership ? "Relative time zero is included and shown as a reference." : zeroOnPsthAxis ? "Relative time zero is an excluded stop-boundary coordinate shown only as a boundary reference." : "Relative time zero lies outside the displayed window and no zero reference is drawn.",
        binCount: distributionCompilerNumber(psth.counts.length),
        eventCount: psth.exactCountTotal,
        selectedSenderCount: distributionCompilerNumber(psth.selectedSenderCount),
        includedTrialCount: distributionCompilerNumber(psth.includedTrialCount),
        excludedTrialCount: distributionCompilerNumber(psth.excludedTrialCount),
        denominatorPolicy,
        normalization,
        valueMin: psthValueExtent === null ? "null" : distributionCompilerNumber(psthValueExtent.min),
        valueMax: psthValueExtent === null ? "null" : distributionCompilerNumber(psthValueExtent.max),
        valueUnit: psth.valueUnit,
        baselineStatement: psth.baselineRate === null ? "No baseline subtraction was requested." : `Baseline ${distributionCompilerNumber(psth.baselineRate)} ${psth.valueUnit} was subtracted from displayed values; the table retains both uncorrected and corrected values.`,
        missingBinStatement: psth.missingBinCount > 0 ? psth.missingBinCount === 1 ? "1 bin has no covering trial and remains missing, not zero." : `${psth.missingBinCount} bins have no covering trial and remain missing, not zero.` : "Every bin has positive declared trial exposure.",
        excludedEventStatement: psth.excludedOutOfWindowCount === null ? "The pre-binned input does not retain an out-of-window event count." : `${psth.excludedOutOfWindowCount} source events were excluded by the relative window.`,
        uncertaintyStatement: "No uncertainty interval was supplied or rendered."
      }
    );
    const projectedBins = linearScale(
      psth.edges[0],
      psth.edges[psth.edges.length - 1],
      64,
      compileContext.width - 32
    );
    for (let index = 0; index < psth.counts.length; index++) {
      const lower = projectedBins.map(psth.edges[index]);
      const upper = projectedBins.map(psth.edges[index + 1]);
      if (!Number.isFinite(lower) || !Number.isFinite(upper) || !(upper > lower) || formatCoordinate(lower) === formatCoordinate(upper) || formatCoordinate(upper - lower) === "0") {
        return fail(
          "RENDER_DEGENERATE_DOMAIN",
          "render",
          `authoritative PSTH bin ${index} has positive scientific width but collapses to zero width in the deterministic SVG coordinate grid at ${compileContext.width}px. Cortexel refuses rather than widen, overlap, or hide the interval; increase the output width or use scientifically justified wider bins.`,
          `/parameters/bins`
        );
      }
    }
    const psthPlan = compilePsthFigure(
      compileContext,
      psth,
      {
        seriesId: String(parameters.seriesId),
        alignmentLabel: String(parameters.alignmentLabel ?? "alignment"),
        seriesLabel: String(parameters.seriesLabel ?? parameters.seriesId),
        normalization,
        denominatorPolicy
      },
      skillId
    );
    const psthRects = psthPlan.panels[0]?.marks.find((mark) => mark.type === "rect");
    if (psthRects?.type === "rect") {
      let rectIndex = 0;
      for (let binIndex2 = 0; binIndex2 < psth.displayValues.length; binIndex2++) {
        const value = psth.displayValues[binIndex2];
        if (value === null) continue;
        const rect = psthRects.rects[rectIndex++];
        if (!rect || value !== 0 && formatCoordinate(rect.height) === "0") {
          return fail(
            "RENDER_DEGENERATE_DOMAIN",
            "render",
            `nonzero PSTH bin ${binIndex2} collapses to zero height in the deterministic SVG coordinate grid. Cortexel refuses rather than inflate a minimum-height mark or render an observed value indistinguishably from zero; change the scale by narrowing the displayed dynamic range or split the data into honest panels.`,
            `/parameters/bins`
          );
        }
      }
    }
    return done(
      psthPlan,
      [operation],
      derivedFacts
    );
  }
  if (skillId === "neuro.phase_plane") {
    const trajectories = rec(data.trajectories);
    const axes = rec(data.axes);
    const xAxisRecord = rec(axes?.x) ?? {};
    const yAxisRecord = rec(axes?.y) ?? {};
    const xAxisUnit = String(xAxisRecord.unit);
    const yAxisUnit = String(yAxisRecord.unit);
    const xLabel = `${String(xAxisRecord.label ?? "x")} (${unitLabel(xAxisUnit)})`;
    const yLabel = `${String(yAxisRecord.label ?? "y")} (${unitLabel(yAxisUnit)})`;
    const vectorField = rec(data.vectorField);
    const nullclines = rec(data.nullclines);
    const fixedPoints = rec(data.fixedPoints);
    const nullableConverted = (values, sourceUnit, targetUnit) => values.map((value) => value === null ? null : sourceUnit === targetUnit ? value : convert(value, sourceUnit, targetUnit));
    const fieldDomain = rec(vectorField?.domain);
    const fieldDxUnit = String(rec(vectorField?.dx)?.unit);
    const fieldDyUnit = String(rec(vectorField?.dy)?.unit);
    const directionMarkers = rec(parameters.directionMarkers);
    const arrowScaling = rec(parameters.arrowScaling);
    const convergenceFlags = phaseConvergenceFlags2(fixedPoints);
    const declaredConvergence = arr(fixedPoints?.converged) ?? [];
    const convergenceMismatch = convergenceFlags.findIndex(
      (converged, index) => declaredConvergence[index] !== converged
    );
    if (convergenceMismatch >= 0) {
      return fail(
        "SCIENCE_NORMALIZATION_UNVERIFIABLE",
        "science",
        `fixed point ${strings4(fixedPoints?.ids)[convergenceMismatch]} declares converged=${String(declaredConvergence[convergenceMismatch])}, but |residual| <= tolerance re-derives ${String(convergenceFlags[convergenceMismatch])} after unit conversion with the contract's relative 1e-9 comparison tolerance.`,
        `/data/fixedPoints/converged/${convergenceMismatch}`
      );
    }
    let tableRows;
    try {
      tableRows = phasePlaneTableRows(data, parameters, convergenceFlags);
    } catch (error) {
      const message = error instanceof Error ? error.message : "axis-normalized speed is unrepresentable";
      if (!message.includes("binary64")) throw error;
      return fail(
        "SCIENCE_NUMERIC_RESOLUTION_UNREPRESENTABLE",
        "science",
        message,
        "/data"
      );
    }
    const expectedPhaseRows = completenessRowPreflight(skillId, data);
    if (tableRows.length !== expectedPhaseRows) {
      return fail(
        "INTERNAL_INVARIANT_VIOLATED",
        "internal",
        `phase-plane table materialized ${tableRows.length} rows for ${expectedPhaseRows} declared carrier points.`,
        "/data"
      );
    }
    const convertedTrajectoryXs = trajectories ? nullableConverted(arr(rec(trajectories.x)?.values) ?? [], String(rec(trajectories.x)?.unit), xAxisUnit) : [];
    const convertedTrajectoryYs = trajectories ? nullableConverted(arr(rec(trajectories.y)?.values) ?? [], String(rec(trajectories.y)?.unit), yAxisUnit) : [];
    const convertedFieldXs = vectorField ? nullableConverted(arr(rec(vectorField.x)?.values) ?? [], String(rec(vectorField.x)?.unit), xAxisUnit) : [];
    const convertedFieldYs = vectorField ? nullableConverted(arr(rec(vectorField.y)?.values) ?? [], String(rec(vectorField.y)?.unit), yAxisUnit) : [];
    const convertedNullclineXs = nullclines ? nullableConverted(arr(rec(nullclines.x)?.values) ?? [], String(rec(nullclines.x)?.unit), xAxisUnit) : [];
    const convertedNullclineYs = nullclines ? nullableConverted(arr(rec(nullclines.y)?.values) ?? [], String(rec(nullclines.y)?.unit), yAxisUnit) : [];
    const convertedFixedXs = fixedPoints ? nullableConverted(arr(rec(fixedPoints.x)?.values) ?? [], String(rec(fixedPoints.x)?.unit), xAxisUnit) : [];
    const convertedFixedYs = fixedPoints ? nullableConverted(arr(rec(fixedPoints.y)?.values) ?? [], String(rec(fixedPoints.y)?.unit), yAxisUnit) : [];
    const convertedDomain = fieldDomain ? {
      xMin: convert(num(rec(fieldDomain.x)?.start), String(rec(fieldDomain.x)?.unit), xAxisUnit),
      xMax: convert(num(rec(fieldDomain.x)?.stop), String(rec(fieldDomain.x)?.unit), xAxisUnit),
      yMin: convert(num(rec(fieldDomain.y)?.start), String(rec(fieldDomain.y)?.unit), yAxisUnit),
      yMax: convert(num(rec(fieldDomain.y)?.stop), String(rec(fieldDomain.y)?.unit), yAxisUnit)
    } : void 0;
    const phaseXExtent = finiteExtent([
      ...convertedTrajectoryXs,
      ...convertedFieldXs,
      ...convertedNullclineXs,
      ...convertedFixedXs,
      ...convertedDomain ? [convertedDomain.xMin, convertedDomain.xMax] : []
    ].filter((value) => value !== null));
    const phaseYExtent = finiteExtent([
      ...convertedTrajectoryYs,
      ...convertedFieldYs,
      ...convertedNullclineYs,
      ...convertedFixedYs,
      ...convertedDomain ? [convertedDomain.yMin, convertedDomain.yMax] : []
    ].filter((value) => value !== null));
    const fieldDxs = numbers4(rec(vectorField?.dx)?.values);
    const fieldDys = numbers4(rec(vectorField?.dy)?.values);
    const magnitudeBasis = String(parameters.magnitudeBasis);
    if (magnitudeBasis === "physical" && dimensionOf(xAxisUnit) !== dimensionOf(yAxisUnit)) {
      return fail(
        "SCIENCE_UNIT_DIMENSION_MISMATCH",
        "science",
        `physical vector magnitude cannot combine ${xAxisUnit} and ${yAxisUnit}; use axis_normalized for incommensurable state axes.`,
        "/parameters/magnitudeBasis"
      );
    }
    const canonicalStateUnit = canonicalUnitFor(xAxisUnit) ?? xAxisUnit;
    const canonicalTimeUnit = canonicalUnitFor(fieldDxUnit) ?? fieldDxUnit;
    let fieldNormalizedComponents = [];
    try {
      fieldNormalizedComponents = vectorField ? fieldDxs.map((dx, index) => {
        const dy = fieldDys[index];
        if (!phaseXExtent || !phaseYExtent || !(phaseXExtent.max > phaseXExtent.min) || !(phaseYExtent.max > phaseYExtent.min)) return { x: 0, y: 0 };
        return {
          x: exactBinary64RatioToDifference(dx, phaseXExtent.min, phaseXExtent.max),
          y: exactBinary64RatioToDifference(
            convert(dy, fieldDyUnit, fieldDxUnit),
            phaseYExtent.min,
            phaseYExtent.max
          )
        };
      }) : [];
    } catch (error) {
      const message = error instanceof Error ? error.message : "axis-normalized vector is unrepresentable";
      if (!message.includes("binary64")) throw error;
      return fail(
        "SCIENCE_NUMERIC_RESOLUTION_UNREPRESENTABLE",
        "science",
        message,
        "/data/vectorField"
      );
    }
    const fieldMagnitudes = vectorField ? fieldDxs.map((dx, index) => {
      const dy = fieldDys[index];
      if (magnitudeBasis === "physical") {
        return Math.hypot(
          dx * conversionFactor(xAxisUnit, canonicalStateUnit) * conversionFactor(fieldDxUnit, canonicalTimeUnit),
          dy * conversionFactor(yAxisUnit, canonicalStateUnit) * conversionFactor(fieldDyUnit, canonicalTimeUnit)
        );
      }
      const normalized = fieldNormalizedComponents[index];
      return Math.hypot(normalized.x, normalized.y);
    }) : [];
    const phaseConversions = [];
    const noteConversion = (label, sourceUnit, targetUnit) => {
      if (sourceUnit !== targetUnit) {
        phaseConversions.push(`${label}: ${sourceUnit} -> ${targetUnit} (factor ${conversionFactor(sourceUnit, targetUnit)})`);
      }
    };
    if (trajectories) {
      noteConversion("trajectory x", String(rec(trajectories.x)?.unit), xAxisUnit);
      noteConversion("trajectory y", String(rec(trajectories.y)?.unit), yAxisUnit);
    }
    if (vectorField) {
      noteConversion("vector-field x", String(rec(vectorField.x)?.unit), xAxisUnit);
      noteConversion("vector-field y", String(rec(vectorField.y)?.unit), yAxisUnit);
    }
    if (nullclines) {
      noteConversion("nullcline x", String(rec(nullclines.x)?.unit), xAxisUnit);
      noteConversion("nullcline y", String(rec(nullclines.y)?.unit), yAxisUnit);
    }
    if (fixedPoints) {
      noteConversion("fixed-point x", String(rec(fixedPoints.x)?.unit), xAxisUnit);
      noteConversion("fixed-point y", String(rec(fixedPoints.y)?.unit), yAxisUnit);
    }
    const phaseFacts = phaseConversions.length > 0 ? { unitConversions: phaseConversions } : {};
    const phaseOperations = [{
      id: "phase_plane.carriers.canonicalize_and_verify",
      algorithm: "cortexel.phase_plane.canonicalize_carriers",
      algorithmRevision: 2,
      parameters: {
        xAxisUnit,
        yAxisUnit,
        magnitudeBasis,
        axisNormalizedBinary64Arithmetic: "exact_component_over_exact_extent_difference_then_one_final_round",
        convergenceRelativeTolerance: 1e-9
      },
      inputDigest: canonicalDigest({
        ...trajectories ? { trajectories } : {},
        ...vectorField ? { vectorField } : {},
        ...nullclines ? { nullclines } : {},
        ...fixedPoints ? { fixedPoints } : {}
      }),
      outputDigest: canonicalDigest({
        ...trajectories ? { convertedTrajectoryXs, convertedTrajectoryYs } : {},
        ...vectorField ? { convertedFieldXs, convertedFieldYs } : {},
        ...nullclines ? { convertedNullclineXs, convertedNullclineYs } : {},
        ...fixedPoints ? { convertedFixedXs, convertedFixedYs } : {},
        ...convertedDomain ? { convertedDomain } : {},
        fieldNormalizedComponents,
        fieldMagnitudes,
        convergenceFlags
      }),
      receipt: {
        conversions: phaseConversions,
        ...vectorField ? {
          fieldMagnitudeUnit: magnitudeBasis === "physical" ? `${canonicalStateUnit} ${canonicalTimeUnit}` : fieldDxUnit
        } : {},
        fixedPointCount: convergenceFlags.length,
        convergenceFlags
      }
    }];
    const trajectoryRawXs = arr(rec(trajectories?.x)?.values) ?? [];
    const trajectoryRawYs = arr(rec(trajectories?.y)?.values) ?? [];
    const nullclineRawXs = arr(rec(nullclines?.x)?.values) ?? [];
    const nullclineRawYs = arr(rec(nullclines?.y)?.values) ?? [];
    let missingPhasePointCount = 0;
    for (let index = 0; index < trajectoryRawXs.length; index++) {
      if (trajectoryRawXs[index] === null || trajectoryRawYs[index] === null) {
        missingPhasePointCount++;
      }
    }
    for (let index = 0; index < nullclineRawXs.length; index++) {
      if (nullclineRawXs[index] === null || nullclineRawYs[index] === null) {
        missingPhasePointCount++;
      }
    }
    const phaseDerivedFacts = {
      ...phaseFacts,
      ...missingPhasePointCount > 0 ? { missingValueCount: missingPhasePointCount } : {}
    };
    const trajectoryTimes = numbers4(rec(trajectories?.times)?.values);
    const uncertaintyRecord = rec(parameters.uncertainty) ?? {};
    const phaseCompileContext = topologyDynamicsCompilerContext(
      context(tableRows.length, phaseDerivedFacts),
      skillId,
      {
        yLabel: String(yAxisRecord.label ?? "y"),
        yUnit: yAxisUnit,
        xLabel: String(xAxisRecord.label ?? "x"),
        xUnit: xAxisUnit,
        trajectoryCount: exactNumberText(strings4(rec(trajectories?.universe)?.ids).length),
        trajectoryPointCount: exactNumberText(strings4(trajectories?.pointTrajectoryIds).length),
        timeStart: trajectoryTimes.length === 0 ? "not supplied" : exactNumberText(trajectoryTimes[0]),
        timeStop: trajectoryTimes.length === 0 ? "not supplied" : exactNumberText(trajectoryTimes[trajectoryTimes.length - 1]),
        timeUnit: trajectoryTimes.length === 0 ? "not supplied" : String(rec(trajectories?.times)?.unit),
        timeDirection: trajectories ? String(trajectories.timeDirection) : "not supplied",
        fieldSampleCount: exactNumberText(numbers4(rec(vectorField?.x)?.values).length),
        latticeKind: vectorField ? String(rec(vectorField.lattice)?.kind) : "not supplied",
        xDomainStart: convertedDomain ? exactNumberText(convertedDomain.xMin) : "not supplied",
        xDomainStop: convertedDomain ? exactNumberText(convertedDomain.xMax) : "not supplied",
        yDomainStart: convertedDomain ? exactNumberText(convertedDomain.yMin) : "not supplied",
        yDomainStop: convertedDomain ? exactNumberText(convertedDomain.yMax) : "not supplied",
        arrowScalingMode: vectorField ? String(arrowScaling?.mode) : "not supplied",
        magnitudeBasis: vectorField ? magnitudeBasis : "not supplied",
        nullclineCount: exactNumberText(strings4(nullclines?.curveIds).length),
        fixedPointCount: exactNumberText(strings4(fixedPoints?.ids).length),
        missingStatement: missingPhasePointCount === 0 ? "No supplied trajectory or nullcline point has a missing coordinate." : `${missingPhasePointCount} supplied path points have a missing coordinate and break their path; missing is never zero.`,
        uncertaintyStatement: uncertaintyRecord.kind === "none" ? `No uncertainty was supplied (${String(uncertaintyRecord.reason ?? "not_provided")}).` : `Uncertainty kind ${String(uncertaintyRecord.kind)} was declared.`
      }
    );
    return done(withContractTable(
      compilePhasePlaneFigure(phaseCompileContext, {
        xLabel,
        yLabel,
        ...trajectories ? {
          trajectories: {
            ids: strings4(rec(trajectories.universe)?.ids),
            labels: strings4(rec(trajectories.universe)?.labels),
            pointIds: strings4(trajectories.pointTrajectoryIds),
            xs: convertedTrajectoryXs,
            ys: convertedTrajectoryYs,
            timeDirection: String(trajectories.timeDirection),
            directionMode: String(directionMarkers?.mode),
            ...num(directionMarkers?.everyNPoints) !== void 0 ? { everyNPoints: num(directionMarkers?.everyNPoints) } : {}
          }
        } : {},
        ...vectorField ? {
          vectorField: {
            xs: convertedFieldXs,
            ys: convertedFieldYs,
            normalizedDxs: fieldNormalizedComponents.map((component) => component.x),
            normalizedDys: fieldNormalizedComponents.map((component) => component.y),
            magnitudes: fieldMagnitudes,
            magnitudeBasis,
            magnitudeUnit: magnitudeBasis === "physical" ? `${canonicalStateUnit} ${canonicalTimeUnit}` : fieldDxUnit,
            scaling: String(arrowScaling?.mode),
            maxArrowLengthFraction: num(arrowScaling?.maxArrowLengthFraction) ?? 0.05,
            ...convertedDomain ? { domain: convertedDomain } : {}
          }
        } : {},
        ...nullclines ? {
          nullclines: {
            ids: strings4(nullclines.curveIds),
            labels: strings4(nullclines.labels),
            pointIds: strings4(nullclines.pointCurveIds),
            xs: convertedNullclineXs,
            ys: convertedNullclineYs
          }
        } : {},
        ...fixedPoints ? {
          fixedPoints: {
            ids: strings4(fixedPoints.ids),
            labels: strings4(fixedPoints.labels),
            xs: convertedFixedXs,
            ys: convertedFixedYs,
            converged: convergenceFlags
          }
        } : {}
      }, skillId),
      phaseCompileContext,
      skillId,
      tableRows
    ), phaseOperations, phaseDerivedFacts);
  }
  if (skillId === "network.connection_graph") {
    const ids = strings4(rec(data.nodeUniverse)?.ids);
    const nodeIndexById = new Map(ids.map((id, index) => [id, index]));
    const conns = rec(data.connections);
    const sources = strings4(conns?.sourceIds);
    const targets = strings4(conns?.targetIds);
    if (ids.length) {
      const groups = arr(rec(data.nodeUniverse)?.groups) ?? [];
      const groupByNode = /* @__PURE__ */ new Map();
      const groupIndexByNode = /* @__PURE__ */ new Map();
      for (const [groupIndex, candidate] of groups.entries()) {
        const group = rec(candidate) ?? {};
        for (const memberId of strings4(group.memberIds)) {
          const previousGroup = groupByNode.get(memberId);
          if (previousGroup !== void 0) {
            return fail(
              "SEMANTIC_DUPLICATE_ID",
              "semantic",
              `node ${memberId} belongs to both group ${previousGroup} and group ${String(group.id)}; group colour, marker shape, and grouped layout require a partition, not overlapping memberships.`,
              `/data/nodeUniverse/groups/${groupIndex}/memberIds`
            );
          }
          groupByNode.set(memberId, String(group.id));
          groupIndexByNode.set(memberId, groupIndex);
        }
      }
      const degree = rec(parameters.degreeAnnotation);
      const scopeRecord = rec(data.scope) ?? {};
      if (degree && rec(data.nodeUniverse)?.complete !== true) {
        return fail(
          "SCOPE_NODE_UNIVERSE_REQUIRED",
          "scope",
          "a degree annotation requires a complete declared node universe; an omitted node can turn a visible isolate into an unobserved neighbour.",
          "/data/nodeUniverse/complete"
        );
      }
      if (degree && scopeRecord.kind === "sampled") {
        return fail(
          "SCOPE_NODE_UNIVERSE_REQUIRED",
          "scope",
          "degree over sampled edges is a property of the retained sample, not the declared network. Cortexel refuses the annotation rather than label it as network degree.",
          "/parameters/degreeAnnotation"
        );
      }
      if (degree && scopeRecord.kind === "mpi_target_rank_local" && degree.mode !== "in_degree") {
        return fail(
          "SCOPE_OUT_DEGREE_FROM_RANK_LOCAL",
          "scope",
          "a target-rank-local connection query cannot establish outgoing or total degree because remote-target connections live on other ranks.",
          "/parameters/degreeAnnotation/mode"
        );
      }
      if (parameters.nodeColorBy === "group" && groups.length > CATEGORICAL_SERIES_STYLES.length) {
        return fail(
          "RENDER_SERIES_LIMIT_EXCEEDED",
          "render",
          `${groups.length} node groups exceed the ${CATEGORICAL_SERIES_STYLES.length} registered distinguishable color/shape tuples.`,
          "/data/nodeUniverse/groups"
        );
      }
      const countingPolicy = degree?.countingPolicy === "per_unique_neighbour" ? "count_unique_neighbors" : "count_edges";
      const inDegree = degree ? computeDegrees(ids, sources, targets, "in", countingPolicy).degree : [];
      const outDegree = degree ? computeDegrees(ids, sources, targets, "out", countingPolicy).degree : [];
      const autapseCountByNode = /* @__PURE__ */ new Map();
      if (degree) {
        const autapseContribution = String(degree.autapseContribution);
        for (let edgeIndex = 0; edgeIndex < sources.length; edgeIndex++) {
          if (sources[edgeIndex] !== targets[edgeIndex]) continue;
          if (countingPolicy === "count_unique_neighbors" && autapseCountByNode.has(sources[edgeIndex])) continue;
          const nodeId = sources[edgeIndex];
          autapseCountByNode.set(
            nodeId,
            countingPolicy === "count_unique_neighbors" ? 1 : (autapseCountByNode.get(nodeId) ?? 0) + 1
          );
          const nodeIndex = nodeIndexById.get(nodeId);
          if (nodeIndex === void 0) continue;
          if (autapseContribution === "excluded") {
            inDegree[nodeIndex] = Math.max(0, inDegree[nodeIndex] - 1);
            outDegree[nodeIndex] = Math.max(0, outDegree[nodeIndex] - 1);
          }
        }
      }
      const positions = rec(data.positions);
      const positionedIds = strings4(positions?.nodeIds);
      const positionIndex = new Map(positionedIds.map((id, index) => [id, index]));
      const rawPositionXs = numbers4(rec(positions?.x)?.values);
      const rawPositionYs = numbers4(rec(positions?.y)?.values);
      const xPositionUnit = positions ? String(rec(positions.x)?.unit) : null;
      const yPositionUnit = positions ? String(rec(positions.y)?.unit) : null;
      const positionUnit = positions && xPositionUnit && yPositionUnit ? xPositionUnit === yPositionUnit || conversionFactor(yPositionUnit, xPositionUnit) >= 1 ? xPositionUnit : yPositionUnit : null;
      const positionXs = positions && positionUnit && xPositionUnit !== positionUnit ? rawPositionXs.map((value) => convert(value, xPositionUnit, positionUnit)) : rawPositionXs;
      const positionYs = positions && positionUnit && yPositionUnit !== positionUnit ? rawPositionYs.map((value) => convert(value, yPositionUnit, positionUnit)) : rawPositionYs;
      const measured = rec(parameters.layout)?.mode === "measured_positions";
      const graphConversions = measured && positionUnit ? [
        ...xPositionUnit !== positionUnit ? [`x: ${xPositionUnit} -> ${positionUnit} (factor ${conversionFactor(xPositionUnit, positionUnit)})`] : [],
        ...yPositionUnit !== positionUnit ? [`y: ${yPositionUnit} -> ${positionUnit} (factor ${conversionFactor(yPositionUnit, positionUnit)})`] : []
      ] : [];
      const graphFacts = graphConversions.length > 0 ? { unitConversions: graphConversions } : {};
      const graphOperations = graphConversions.length > 0 ? [{
        id: "connection_graph.coordinates.canonicalize_axes",
        algorithm: "cortexel.connection_graph.canonicalize_axes",
        algorithmRevision: 1,
        parameters: { targetUnit: positionUnit, selectionRule: "finest_supplied_compatible_unit" },
        inputDigest: canonicalDigest({ x: rec(positions?.x), y: rec(positions?.y) }),
        outputDigest: canonicalDigest({ x: positionXs, y: positionYs, unit: positionUnit }),
        receipt: {
          x: xPositionUnit === positionUnit ? null : conversionReceipt(xPositionUnit, positionUnit),
          y: yPositionUnit === positionUnit ? null : conversionReceipt(yPositionUnit, positionUnit),
          coordinateCount: positionedIds.length
        }
      }] : [];
      if (measured) {
        for (let edgeIndex = 0; edgeIndex < sources.length; edgeIndex++) {
          if (sources[edgeIndex] === targets[edgeIndex]) continue;
          const sourcePosition = positionIndex.get(sources[edgeIndex]);
          const targetPosition = positionIndex.get(targets[edgeIndex]);
          if (positionXs[sourcePosition] === positionXs[targetPosition] && positionYs[sourcePosition] === positionYs[targetPosition]) {
            return fail(
              "RENDER_DEGENERATE_DOMAIN",
              "render",
              `directed connection ${edgeIndex} joins distinct nodes ${sources[edgeIndex]} and ${targets[edgeIndex]} at the same measured coordinate. A zero-length chord has no target end for an arrowhead, and Cortexel refuses rather than invent a separation.`,
              `/data/connections/sourceIds/${edgeIndex}`
            );
          }
        }
      }
      const scopeSummaryCell2 = compilerNetworkScopeSummaryCell(data.scope);
      const scopeKind = String(rec(data.scope)?.kind);
      const nodeRows = ids.map((id, index) => {
        const position = positionIndex.get(id);
        const totalDegree = degree ? inDegree[index] + outDegree[index] - (degree.autapseContribution === "counts_once" ? autapseCountByNode.get(id) ?? 0 : 0) : null;
        return [
          "node",
          id,
          groupByNode.get(id) ?? null,
          null,
          null,
          null,
          null,
          null,
          null,
          null,
          null,
          null,
          null,
          degree && (degree.mode === "in_degree" || degree.mode === "total_degree") ? inDegree[index] : null,
          degree && (degree.mode === "out_degree" || degree.mode === "total_degree") && scopeKind !== "mpi_target_rank_local" ? outDegree[index] : null,
          degree ? structuredCell5({
            mode: degree.mode,
            countingPolicy: degree.countingPolicy,
            autapseContribution: degree.autapseContribution,
            ...degree.mode === "total_degree" ? { totalDegree } : {}
          }) : null,
          measured && position !== void 0 ? positionXs[position] : null,
          measured && position !== void 0 ? positionYs[position] : null,
          measured ? positionUnit : null,
          measured ? "measured" : "schematic (non-spatial)",
          scopeSummaryCell2
        ];
      });
      const edgeIds = strings4(conns?.edgeIds);
      const weights = arr(rec(conns?.weights)?.values) ?? [];
      const weightUnit = typeof rec(conns?.weights)?.unit === "string" ? String(rec(conns?.weights)?.unit) : null;
      const delays = arr(rec(conns?.delays)?.values) ?? [];
      const delayUnit = typeof rec(conns?.delays)?.unit === "string" ? String(rec(conns?.delays)?.unit) : null;
      const models = strings4(conns?.synapseModels);
      const pairKey2 = (source, target) => {
        const ordered = compareUnicodeCodePoints3(source, target) <= 0 ? [source, target] : [target, source];
        return canonicalize(ordered);
      };
      const pairTotals = /* @__PURE__ */ new Map();
      for (let index = 0; index < sources.length; index++) {
        const key = pairKey2(sources[index], targets[index]);
        pairTotals.set(key, (pairTotals.get(key) ?? 0) + 1);
      }
      const parallelEdges = rec(parameters.parallelEdges) ?? {};
      let maximumParallel = 0;
      for (const count of pairTotals.values()) {
        if (count > maximumParallel) maximumParallel = count;
      }
      if (parallelEdges.display === "separate_lanes" && maximumParallel > (num(parallelEdges.maxLanes) ?? 0)) {
        return fail(
          "RENDER_SERIES_LIMIT_EXCEEDED",
          "render",
          `one unordered endpoint pair carries ${maximumParallel} connections but maxLanes is ${String(parallelEdges.maxLanes)}. Revision 1 refuses instead of sharing a lane and hiding a connection.`,
          "/parameters/parallelEdges/maxLanes"
        );
      }
      const pairSeen = /* @__PURE__ */ new Map();
      const edgeRows = sources.map((sourceId, index) => {
        const targetId = targets[index];
        const key = pairKey2(sourceId, targetId);
        const parallelIndex = (pairSeen.get(key) ?? 0) + 1;
        pairSeen.set(key, parallelIndex);
        return [
          "connection",
          edgeIds[index] ?? null,
          null,
          sourceId,
          targetId,
          booleanCell3(sourceId === targetId),
          parallelIndex,
          pairTotals.get(key),
          typeof weights[index] === "number" ? weights[index] : null,
          weightUnit,
          typeof delays[index] === "number" ? delays[index] : null,
          delayUnit,
          models[index] ?? null,
          null,
          null,
          null,
          null,
          null,
          null,
          measured ? "measured" : "schematic (non-spatial)",
          scopeSummaryCell2
        ];
      });
      const tableRows = [...nodeRows, ...edgeRows];
      const expectedGraphRows = ids.length + sources.length;
      if (tableRows.length !== expectedGraphRows) {
        return fail(
          "INTERNAL_INVARIANT_VIOLATED",
          "internal",
          `connection-graph table materialized ${tableRows.length} rows for ${expectedGraphRows} declared node/edge carriers.`,
          "/data"
        );
      }
      const edgeEncoding = rec(parameters.edgeValueEncoding);
      const encodedValues = edgeEncoding?.mode === "weight" ? weights : edgeEncoding?.mode === "delay" ? delays : [];
      if (edgeEncoding && (edgeEncoding.channel === "color" || edgeEncoding.channel === "width_and_color") && edgeEncoding.colorScale !== "sequential" && edgeEncoding.colorScale !== "diverging") {
        return fail(
          "RENDER_NO_DATA",
          "render",
          "an edge color channel requires an explicit sequential or diverging colorScale; Cortexel refuses rather than infer a numeric color meaning.",
          "/parameters/edgeValueEncoding/colorScale"
        );
      }
      if (edgeEncoding && encodedValues.length !== sources.length) {
        return fail(
          "RENDER_NO_DATA",
          "render",
          `${String(edgeEncoding.mode)} was selected for edge encoding, but that complete parallel channel was not supplied.`,
          "/parameters/edgeValueEncoding/mode"
        );
      }
      if (edgeEncoding && parallelEdges.display === "bundled") {
        const valuesByDirection = /* @__PURE__ */ new Map();
        for (let index = 0; index < sources.length; index++) {
          const key = canonicalize([sources[index], targets[index]]);
          const entries = valuesByDirection.get(key) ?? [];
          entries.push(encodedValues[index]);
          valuesByDirection.set(key, entries);
        }
        for (const entries of valuesByDirection.values()) {
          const finite = entries.filter((value) => typeof value === "number");
          if (finite.length > 0 && finite.length !== entries.length) {
            return fail(
              "RENDER_NO_DATA",
              "render",
              "one bundled stroke cannot truthfully encode both finite and missing parallel-edge values. Cortexel refuses rather than let the finite rows visually stand in for missing rows.",
              "/data/connections"
            );
          }
        }
      }
      if (edgeEncoding) {
        const paintValueGroups = [];
        if (parallelEdges.display === "bundled") {
          const byDirection = /* @__PURE__ */ new Map();
          for (let index = 0; index < sources.length; index++) {
            const key = canonicalize([sources[index], targets[index]]);
            const entries = byDirection.get(key) ?? [];
            entries.push(encodedValues[index]);
            byDirection.set(key, entries);
          }
          paintValueGroups.push(...byDirection.values());
        } else {
          paintValueGroups.push(...encodedValues.map((value) => [value]));
        }
        const aggregateValues = [];
        try {
          for (const entries of paintValueGroups) {
            const finite = entries.filter((value) => typeof value === "number");
            const aggregate4 = aggregateTopologyScalar(
              finite,
              entries.length > 1 ? parameters.multapseAggregation : "no_aggregation"
            );
            if (aggregate4 !== null) aggregateValues.push(aggregate4);
          }
          const center = edgeEncoding.colorScale === "diverging" ? num(edgeEncoding.center) ?? 0 : 0;
          const uniqueValues = [...new Set(aggregateValues)].sort((left, right) => left - right);
          let previousTransform;
          for (const value of uniqueValues) {
            const centered = exactBinary64Sum([value, -center]);
            if (!Number.isFinite(centered)) {
              throw new Error("an aggregate minus its declared reference is outside finite binary64");
            }
            const transformed = edgeEncoding.scale === "symlog" ? symlogTransform(centered, 1) : value;
            if (!Number.isFinite(transformed)) {
              throw new Error("an encoded graph value transform is outside finite binary64");
            }
            if (previousTransform !== void 0 && !(transformed > previousTransform)) {
              throw new Error("two distinct ordered graph values collapse to one transformed value");
            }
            previousTransform = transformed;
          }
        } catch (error) {
          return fail(
            "SCIENCE_NUMERIC_RESOLUTION_UNREPRESENTABLE",
            "science",
            error instanceof Error ? `the declared graph edge aggregation or value scale cannot be represented as ordered finite binary64: ${error.message}` : "the declared graph edge aggregation or value scale cannot be represented as ordered finite binary64.",
            "/parameters/edgeValueEncoding"
          );
        }
      }
      const degreeForNode = (index) => {
        if (!degree) return void 0;
        if (degree.mode === "in_degree") return inDegree[index];
        if (degree.mode === "out_degree") return outDegree[index];
        const id = ids[index];
        const autapseCount2 = autapseCountByNode.get(id) ?? 0;
        return inDegree[index] + outDegree[index] - (degree.autapseContribution === "counts_once" ? autapseCount2 : 0);
      };
      const missingEncodedValueCount = countExplicitNulls(encodedValues);
      const directedGroups = /* @__PURE__ */ new Set();
      const connectedNodes = /* @__PURE__ */ new Set();
      let autapseCount = 0;
      for (let index = 0; index < sources.length; index++) {
        directedGroups.add(canonicalize([sources[index], targets[index]]));
        connectedNodes.add(sources[index]);
        connectedNodes.add(targets[index]);
        if (sources[index] === targets[index]) autapseCount++;
      }
      const bundledAggregation = parallelEdges.display === "bundled" && directedGroups.size < sources.length && typeof parameters.multapseAggregation === "string";
      const graphDerivedFacts = {
        ...graphFacts,
        ...missingEncodedValueCount > 0 ? { missingValueCount: missingEncodedValueCount } : {},
        ...bundledAggregation ? {
          multapseAggregated: true,
          multapseAggregation: parameters.multapseAggregation
        } : {}
      };
      const layoutMode = String(rec(parameters.layout)?.mode);
      const edgeValueStatement = edgeEncoding === void 0 ? "no edge value channel is encoded" : `${String(edgeEncoding.mode)} on ${String(edgeEncoding.channel)} with ${String(edgeEncoding.scale ?? "linear")} scale${typeof edgeEncoding.colorScale === "string" ? ` and ${edgeEncoding.colorScale} colour` : ""}${typeof parameters.multapseAggregation === "string" ? `; bundled multapses use ${parameters.multapseAggregation}` : ""}`;
      const degreeStatement = degree === void 0 ? "no degree annotation" : structuredCell5(degree);
      const graphCompileContext = topologyDynamicsCompilerContext(
        context(tableRows.length, graphDerivedFacts),
        skillId,
        {
          graphLabel: String(parameters.graphLabel ?? parameters.graphId),
          nodeCount: exactNumberText(ids.length),
          isolateCount: exactNumberText(ids.filter((id) => !connectedNodes.has(id)).length),
          edgeCount: exactNumberText(sources.length),
          multapseRowCount: exactNumberText(sources.length - pairTotals.size),
          autapseCount: exactNumberText(autapseCount),
          scopeStatement: scopeSummaryCell2,
          layoutMode,
          layoutSpatialStatement: measured ? `measured positions in ${String(positionUnit)}` : "schematic and non-spatial",
          nodeOrder: String(rec(data.nodeUniverse)?.order ?? "as_declared"),
          edgeValueStatement,
          degreeStatement,
          missingValueStatement: edgeEncoding === void 0 ? "No edge value channel is encoded." : missingEncodedValueCount === 0 ? "No encoded edge value is missing." : `${missingEncodedValueCount} encoded edge values are missing and retain the reserved missing-value encoding.`,
          compactionStatement: "No declared node or connection row was compacted.",
          tableStatement: `The complete table contains ${ids.length} node rows followed by ${sources.length} source-order connection rows.`
        }
      );
      return done(withContractTable(
        compileGraphFigure(graphCompileContext, {
          nodes: ids.map((id, index) => ({
            id,
            group: groupByNode.get(id) ?? "ungrouped",
            groupIndex: groupIndexByNode.get(id) ?? groups.length,
            ...degreeForNode(index) !== void 0 ? { degree: degreeForNode(index) } : {},
            ...measured ? {
              x: positionXs[positionIndex.get(id)],
              y: positionYs[positionIndex.get(id)]
            } : {}
          })),
          edges: sources.map((sourceId, index) => ({
            id: edgeIds[index] ?? `source-row-${index}`,
            sourceId,
            targetId: targets[index],
            sourceOrdinal: index,
            ...edgeEncoding ? { value: typeof encodedValues[index] === "number" ? encodedValues[index] : null } : {}
          })),
          layout: String(rec(parameters.layout)?.mode),
          parallelDisplay: String(parallelEdges.display),
          maxLanes: num(parallelEdges.maxLanes) ?? 1,
          ...typeof parameters.multapseAggregation === "string" ? {
            multapseAggregation: parameters.multapseAggregation
          } : {},
          ...edgeEncoding ? {
            edgeEncoding: {
              channel: String(edgeEncoding.channel),
              colorKind: edgeEncoding.colorScale === "diverging" ? "diverging" : "sequential",
              scale: edgeEncoding.scale === "symlog" ? "symlog" : "linear",
              ...num(edgeEncoding.center) !== void 0 ? { center: num(edgeEncoding.center) } : {}
            }
          } : {},
          nodeColorByGroup: parameters.nodeColorBy === "group",
          encodeDegreeAsArea: degree?.encodeAsNodeArea === true,
          ...degree ? { degreeLabel: String(degree.mode).replaceAll("_", "-") } : {},
          ...positionUnit ? { positionUnit } : {},
          ...measured ? { xLabel: `x (${unitLabel(positionUnit)})`, yLabel: `y (${unitLabel(positionUnit)})` } : {}
        }, skillId),
        graphCompileContext,
        skillId,
        tableRows
      ), graphOperations, graphDerivedFacts);
    }
    return { pending: rendererId };
  }
  return { pending: rendererId };
}
function assembleArtifact(validated, disclosures, plan, render, summary, derivationOperations) {
  const identity = getBuildIdentity();
  const catalog = SKILL_CATALOG[validated.skillId];
  const tableRowCount = plan.table.rowsTotal;
  const artifactWithoutDigest = {
    artifact: {
      name: ARTIFACT_CONTRACT_IDENTITY.name,
      version: ARTIFACT_CONTRACT_IDENTITY.version
    },
    buildIdentity: {
      packageVersion: identity.packageVersion,
      requestContract: identity.requestContract,
      artifactContract: identity.artifactContract,
      contractDigest: identity.contractDigest,
      catalogDigest: identity.catalogDigest,
      sourceRevision: identity.sourceRevision,
      release: identity.release
    },
    canonicalRequest: validated.canonicalRequest,
    inputAssurance: validated.inputAssurance,
    validation: {
      structural: { validatorId: "ajv-2020", validatorRevision: 1, result: "passed" },
      semantic: {
        validatorId: "cortexel-semantics",
        validatorRevision: 1,
        result: "passed",
        checkedRuleIds: validated.checkedValidatorIds
      },
      resource: { validatorId: "budget-preflight", validatorRevision: 1, result: "passed" },
      referenceComparison: { status: "not_run", oracle: null, oracleVersion: null },
      warnings: validated.warnings.map((w) => ({
        code: w.code,
        severity: w.severity,
        stage: w.stage,
        instancePath: w.instancePath,
        message: w.message
      }))
    },
    derivation: { operations: derivationOperations },
    budgetDecision: {
      outcome: "accepted_full"
    },
    provenance: {
      source: rec(validated.canonicalRequest.source) ?? { kind: "unknown" },
      requestDigest: validated.requestDigest,
      assurances: [
        { id: "structural", issuer: "cortexel", method: "json-schema-2020-12", result: "passed" },
        { id: "semantic", issuer: "cortexel", method: "named-validators", result: "passed" },
        { id: "reference", issuer: "cortexel", method: "independent-oracle", result: "not_run" }
      ],
      attestations: []
    },
    disclosures: disclosures.map((d) => ({ id: d.id, severity: d.severity, text: d.text })),
    render: {
      rendererId: catalog.renderer.id,
      rendererRevision: catalog.renderer.revision,
      width: render.width,
      height: render.height,
      themeId: plan.themeId,
      markCount: render.markCount,
      textCount: render.textCount
    },
    accessibility: {
      profileId: "cortexel-accessibility",
      profileVersion: "1.0",
      summary,
      tablePolicy: plan.table.policy,
      tableBinding: "shape_only",
      tableColumns: plan.table.columns.map((column) => column.key),
      tableRowCount
    },
    outputs: [
      {
        role: "figure_svg",
        mediaType: "image/svg+xml",
        sha256: render.digest,
        byteLength: utf8ByteLength(render.svg),
        normative: true
      }
    ]
  };
  const artifactDigest = canonicalDigestExcluding(artifactWithoutDigest, "artifactDigest");
  return deepFreeze({ ...artifactWithoutDigest, artifactDigest });
}
function countObservations(node) {
  if (node === null || typeof node !== "object") return 0;
  const pending = [node];
  let total = 0;
  while (pending.length > 0) {
    const current = pending.pop();
    if (Array.isArray(current)) {
      total = Math.min(Number.MAX_SAFE_INTEGER, total + current.length);
      for (const item of current) {
        if (item !== null && typeof item === "object") pending.push(item);
      }
    } else {
      for (const value of Object.values(current)) {
        if (value !== null && typeof value === "object") pending.push(value);
      }
    }
  }
  return total;
}
function maxLeafArrayLength(node) {
  if (node === null || typeof node !== "object") return 0;
  const pending = [node];
  let maximum = 0;
  while (pending.length > 0) {
    const current = pending.pop();
    if (Array.isArray(current)) {
      let hasNestedValue = false;
      for (const item of current) {
        if (item !== null && typeof item === "object") {
          hasNestedValue = true;
          pending.push(item);
        }
      }
      if (!hasNestedValue) maximum = Math.max(maximum, current.length);
    } else {
      for (const value of Object.values(current)) {
        if (value !== null && typeof value === "object") pending.push(value);
      }
    }
  }
  return maximum;
}
function binnedMarkPreflight(skillId, data, parameters) {
  const supported = /* @__PURE__ */ new Set([
    "neuro.isi_distribution",
    "neuro.psth",
    "network.delay_distribution",
    "network.weight_distribution"
  ]);
  if (!supported.has(skillId)) return 0;
  const binRecord = rec(data.binEdges) ?? rec(parameters.bins);
  let binCount = 0;
  const explicitEdges = arr(binRecord?.edges);
  if (explicitEdges) {
    binCount = Math.max(0, explicitEdges.length - 1);
  } else if (binRecord?.mode === "width") {
    const start = num(binRecord.start);
    const stop = num(binRecord.stop);
    const width = num(binRecord.width);
    if (start !== void 0 && stop !== void 0 && width !== void 0) {
      const materialized = tryEdgesFromWidth(start, stop, width);
      binCount = materialized ? materialized.length - 1 : 0;
    }
  }
  if (binCount === 0) return 0;
  if (skillId !== "network.delay_distribution" && skillId !== "network.weight_distribution") return binCount;
  const groupingRequested = skillId === "network.delay_distribution" ? data.groupBy === "synapse_model" : parameters.grouping === "by_synapse_model";
  if (!groupingRequested || data.mode === "prebinned") return binCount;
  const models = arr(rec(data.connections)?.synapseModels) ?? [];
  const groupIds = /* @__PURE__ */ new Set();
  for (const model of models) {
    if (typeof model === "string") groupIds.add(model);
  }
  const groupCount = Math.max(1, groupIds.size);
  return binCount * groupCount;
}
function degreeRowPreflight(data, parameters, ceiling) {
  if (data.mode === "node_degrees") {
    const degrees2 = numbers4(rec(data.nodeDegrees)?.degrees);
    let maximum2 = 0;
    for (const degree of degrees2) {
      if (degree >= ceiling) return ceiling + 1;
      if (degree > maximum2) maximum2 = degree;
    }
    return maximum2 + 1;
  }
  const nodeIds = strings4(rec(data.nodeUniverse)?.ids);
  const connections = rec(data.connections) ?? {};
  const sources = strings4(connections.sourceIds);
  const targets = strings4(connections.targetIds);
  const direction = parameters.direction === "out" ? "out" : "in";
  const excludeAutapses = parameters.autapsePolicy === "exclude";
  if (parameters.countingPolicy === "count_unique_neighbors") {
    const neighbours = new Map(nodeIds.map((id) => [id, /* @__PURE__ */ new Set()]));
    let maximum2 = 0;
    for (let ordinal = 0; ordinal < sources.length; ordinal++) {
      if (excludeAutapses && sources[ordinal] === targets[ordinal]) continue;
      const owner = direction === "in" ? targets[ordinal] : sources[ordinal];
      const counterpart = direction === "in" ? sources[ordinal] : targets[ordinal];
      const set = neighbours.get(owner);
      if (!set) continue;
      set.add(counterpart);
      if (set.size >= ceiling) return ceiling + 1;
      if (set.size > maximum2) maximum2 = set.size;
    }
    return maximum2 + 1;
  }
  const degrees = new Map(nodeIds.map((id) => [id, 0]));
  let maximum = 0;
  for (let ordinal = 0; ordinal < sources.length; ordinal++) {
    if (excludeAutapses && sources[ordinal] === targets[ordinal]) continue;
    const owner = direction === "in" ? targets[ordinal] : sources[ordinal];
    const degree = (degrees.get(owner) ?? 0) + 1;
    if (degree >= ceiling) return ceiling + 1;
    degrees.set(owner, degree);
    if (degree > maximum) maximum = degree;
  }
  return maximum + 1;
}
function completenessRowPreflight(skillId, data) {
  if (skillId === "network.spatial_map_2d" || skillId === "network.connection_graph") {
    return (arr(rec(data.nodeUniverse)?.ids)?.length ?? 0) + (arr(rec(data.connections)?.sourceIds)?.length ?? 0);
  }
  if (skillId === "neuro.phase_plane") {
    const trajectories = rec(data.trajectories);
    const pointIds = strings4(trajectories?.pointTrajectoryIds);
    const represented = new Set(pointIds);
    const declaredEmpty = strings4(rec(trajectories?.universe)?.ids).filter((id) => !represented.has(id)).length;
    return pointIds.length + declaredEmpty + (arr(rec(rec(data.vectorField)?.x)?.values)?.length ?? 0) + (arr(rec(data.nullclines)?.pointCurveIds)?.length ?? 0) + (arr(rec(data.fixedPoints)?.ids)?.length ?? 0);
  }
  if (skillId === "neuro.compartment_trace") {
    const declared = strings4(data.compartmentIds);
    const recorded = /* @__PURE__ */ new Set();
    let samples = 0;
    for (const candidate of arr(data.series) ?? []) {
      const series = rec(candidate) ?? {};
      recorded.add(String(series.compartmentId));
      samples += arr(rec(series.time)?.values)?.length ?? 0;
    }
    return samples + declared.filter((id) => !recorded.has(id)).length;
  }
  return 0;
}
function topologyMarkPreflight(skillId, data, parameters) {
  const connections = rec(data.connections) ?? {};
  const sources = strings4(connections.sourceIds);
  const targets = strings4(connections.targetIds);
  if (skillId === "network.connection_graph") {
    const nodeCount2 = strings4(rec(data.nodeUniverse)?.ids).length;
    const degreeLabels = rec(parameters.degreeAnnotation) ? nodeCount2 : 0;
    if (rec(parameters.parallelEdges)?.display !== "bundled") {
      return nodeCount2 + degreeLabels + 2 * sources.length;
    }
    const directed = /* @__PURE__ */ new Map();
    for (let index = 0; index < sources.length; index++) {
      const key = canonicalize([sources[index], targets[index]]);
      directed.set(key, (directed.get(key) ?? 0) + 1);
    }
    const bundleLabels = [...directed.values()].filter((count) => count > 1).length;
    return nodeCount2 + degreeLabels + 2 * directed.size + bundleLabels;
  }
  if (skillId !== "network.spatial_map_2d") return 0;
  const nodeCount = strings4(rec(data.positions)?.nodeIds).length;
  const domainMark = rec(rec(data.positions)?.domain) ? 1 : 0;
  const unordered = /* @__PURE__ */ new Map();
  for (let index = 0; index < sources.length; index++) {
    const ordered = compareUnicodeCodePoints3(sources[index], targets[index]) <= 0 ? [sources[index], targets[index]] : [targets[index], sources[index]];
    const key = canonicalize(ordered);
    const entry = unordered.get(key) ?? { sources: /* @__PURE__ */ new Map(), autapse: sources[index] === targets[index], count: 0 };
    const directionKey = canonicalize([sources[index], targets[index]]);
    entry.sources.set(directionKey, (entry.sources.get(directionKey) ?? 0) + 1);
    entry.count++;
    unordered.set(key, entry);
  }
  let edgeMarks = 0;
  for (const entry of unordered.values()) {
    edgeMarks += 1 + entry.sources.size;
    if (entry.autapse) {
      if (entry.count > 1) edgeMarks++;
    } else if (entry.count > 1 || entry.sources.size > 1) {
      edgeMarks += entry.sources.size;
    }
  }
  return nodeCount + domainMark + edgeMarks;
}
function buildFigureFromValidated(validated) {
  if (!isValidatedRequest2(validated)) {
    return {
      ok: false,
      errors: [
        makeError({
          code: "RENDER_UNVALIDATED_REQUEST",
          stage: "render",
          message: "buildFigureFromValidated requires the exact frozen token returned by a successful Cortexel validation. Plain objects, copies, proxies, and type casts have no validation authority."
        })
      ]
    };
  }
  const request = validated.canonicalRequest;
  const catalog = SKILL_CATALOG[validated.skillId];
  const presentation = rec(request.presentation) ?? {};
  const requestedProfile = presentation.budgetProfile ?? DEFAULT_PROFILE;
  const activeBudget = trySelectTighterBudgetProfile(
    validated.inputAssurance.budgetProfile,
    requestedProfile
  );
  if (!activeBudget) {
    return {
      ok: false,
      errors: [
        makeError({
          code: "INTERNAL_INVARIANT_VIOLATED",
          stage: "internal",
          message: "the host and request budget profiles are absent or incomparable in this build. Cortexel cannot combine resource authority without an explicit composition contract."
        })
      ]
    };
  }
  const { limits } = activeBudget;
  const markLimit = Math.min(catalog.budgets.maxVisibleMarks, limits.visibleMarks);
  const skillReturnedTableLimit = catalog.budgets.maxReturnedTableRows;
  const returnedTableLimit = Math.min(skillReturnedTableLimit, limits.returnedTableRows);
  const data = rec(request.data) ?? {};
  const universeSize = arr(rec(data.nodeUniverse)?.ids)?.length ?? 0;
  const connections = rec(data.connections);
  const parameters = rec(request.parameters) ?? {};
  const connectionCount = Math.max(
    arr(connections?.sourceIds)?.length ?? 0,
    arr(connections?.targetIds)?.length ?? 0
  );
  const completenessRows = completenessRowPreflight(validated.skillId, data);
  const completenessRowsAreExact = validated.skillId === "network.spatial_map_2d" || validated.skillId === "network.connection_graph" || validated.skillId === "neuro.phase_plane";
  if (completenessRowsAreExact && completenessRows > returnedTableLimit) {
    return {
      ok: false,
      errors: [
        makeError({
          code: "RESOURCE_COMPACTION_UNAVAILABLE",
          stage: "budget",
          instancePath: "/data",
          skillId: validated.skillId,
          message: `the request carriers require exactly ${completenessRows} complete audit rows, over the returned-table limit of ${returnedTableLimit}. Revision 2 has no excerpt or digest-bound complete sidecar, so Cortexel refuses before allocating render geometry.`,
          limit: {
            name: "returnedTableRows",
            limit: returnedTableLimit,
            observed: completenessRows
          }
        })
      ]
    };
  }
  if (validated.skillId === "network.degree_distribution") {
    const degreeRows = degreeRowPreflight(data, parameters, returnedTableLimit);
    if (degreeRows > returnedTableLimit) {
      return {
        ok: false,
        errors: [
          makeError({
            code: "RESOURCE_COMPACTION_UNAVAILABLE",
            stage: "budget",
            instancePath: data.mode === "node_degrees" ? "/data/nodeDegrees/degrees" : "/data/connections",
            skillId: validated.skillId,
            message: `the exact per-integer degree table requires more than ${returnedTableLimit} rows. Cortexel saturated the degree preflight at ${returnedTableLimit + 1} and refused before allocating a dense histogram or render geometry.`,
            limit: {
              name: "returnedTableRows",
              limit: returnedTableLimit,
              observed: returnedTableLimit + 1
            }
          })
        ]
      };
    }
  }
  if (validated.skillId === "neuro.response_curve") {
    const responseCurveRows = (arr(rec(data.conditions)?.ids)?.length ?? 0) + (data.mode === "repeats" ? arr(rec(rec(data.observations)?.response)?.values)?.length ?? 0 : 0);
    if (responseCurveRows > returnedTableLimit) {
      return {
        ok: false,
        errors: [
          makeError({
            code: "RESOURCE_COMPACTION_UNAVAILABLE",
            stage: "budget",
            instancePath: "/data",
            skillId: validated.skillId,
            message: `the complete response-curve table requires ${responseCurveRows} rows, over the returned-table limit of ${returnedTableLimit}. Artifact 1.0 has no excerpt or sidecar state, so Cortexel refuses before sorting, aggregating, or allocating render geometry.`,
            limit: {
              name: "returnedTableRows",
              limit: returnedTableLimit,
              observed: responseCurveRows
            }
          })
        ]
      };
    }
  }
  if (validated.skillId === "neuro.psth") {
    const psthRows = binnedMarkPreflight(validated.skillId, data, parameters);
    if (psthRows > returnedTableLimit) {
      return {
        ok: false,
        errors: [
          makeError({
            code: "RESOURCE_COMPACTION_UNAVAILABLE",
            stage: "budget",
            instancePath: "/parameters/bins",
            skillId: validated.skillId,
            message: `the complete PSTH audit table requires ${psthRows} rows, over the returned-table limit of ${returnedTableLimit}. Revision 2 has no bin compaction, excerpt, or digest-bound complete sidecar, so Cortexel refuses before aligning events, deriving exposures, or allocating geometry.`,
            limit: {
              name: "returnedTableRows",
              limit: returnedTableLimit,
              observed: psthRows
            }
          })
        ]
      };
    }
  }
  if (validated.skillId === "neuro.correlogram") {
    let correlogramRows;
    try {
      correlogramRows = correlogramAxis2(parameters).centers.length;
    } catch (error) {
      return {
        ok: false,
        errors: [makeError({
          code: "INTERNAL_INVARIANT_VIOLATED",
          stage: "internal",
          instancePath: "/parameters/lagRange",
          skillId: validated.skillId,
          message: error instanceof Error ? `the validated correlogram axis could not be materialized during table preflight: ${error.message}` : "the validated correlogram axis could not be materialized during table preflight."
        })]
      };
    }
    if (correlogramRows > returnedTableLimit) {
      return {
        ok: false,
        errors: [
          makeError({
            code: "RESOURCE_COMPACTION_UNAVAILABLE",
            stage: "budget",
            instancePath: "/parameters/lagRange",
            skillId: validated.skillId,
            message: `the complete correlogram audit table requires ${correlogramRows} centred lag-bin rows, over the returned-table limit of ${returnedTableLimit}. Revision 2 has no bin compaction, excerpt, or digest-bound complete sidecar, so Cortexel refuses before pair formation or render allocation.`,
            limit: {
              name: "returnedTableRows",
              limit: returnedTableLimit,
              observed: correlogramRows
            }
          })
        ]
      };
    }
  }
  let spikeAcceptedMarks = 0;
  if (validated.skillId === "neuro.spike_raster") {
    const sourceEventCount = arr(rec(data.eventTimes)?.values)?.length ?? 0;
    const recordedSenderCount = arr(data.recordedSenderIds)?.length ?? 0;
    const trialCount = Math.max(1, arr(data.trialIds)?.length ?? 0);
    const rasterRows = recordedSenderCount === 0 ? 0 : recordedSenderCount > Math.floor(limits.graphNodes / trialCount) ? limits.graphNodes + 1 : recordedSenderCount * trialCount;
    if (rasterRows > limits.graphNodes) {
      return {
        ok: false,
        errors: [
          makeError({
            code: "RESOURCE_BUDGET_EXCEEDED",
            stage: "budget",
            instancePath: "/data/recordedSenderIds",
            skillId: validated.skillId,
            message: `the declared sender-by-trial raster universe requires at least ${rasterRows} rows, over the active limit of ${limits.graphNodes}. Cortexel saturated the product preflight and refused before allocating rows or lookup maps.`,
            limit: {
              name: "rasterRows",
              limit: limits.graphNodes,
              observed: rasterRows
            }
          })
        ]
      };
    }
    if (sourceEventCount > returnedTableLimit) {
      return {
        ok: false,
        errors: [
          makeError({
            code: "RESOURCE_COMPACTION_UNAVAILABLE",
            stage: "budget",
            instancePath: "/data/eventTimes/values",
            skillId: validated.skillId,
            message: `the complete spike-event audit table requires ${sourceEventCount} rows, over the returned-table limit of ${returnedTableLimit}. Revision 2 has no excerpt or digest-bound complete sidecar, so Cortexel refuses before sorting events or allocating render geometry.`,
            limit: {
              name: "returnedTableRows",
              limit: returnedTableLimit,
              observed: sourceEventCount
            }
          })
        ]
      };
    }
    try {
      spikeAcceptedMarks = exactSpikeWindowPartition(data).acceptedCount;
    } catch (error) {
      return {
        ok: false,
        errors: [
          makeError({
            code: "SCIENCE_NUMERIC_RESOLUTION_UNREPRESENTABLE",
            stage: "science",
            instancePath: "/data/window",
            skillId: validated.skillId,
            message: error instanceof Error ? error.message : "the spike window is not representable during resource preflight."
          })
        ]
      };
    }
  }
  const binnedMarks = binnedMarkPreflight(validated.skillId, data, parameters);
  const topologyMarks = topologyMarkPreflight(validated.skillId, data, parameters);
  const preflightMarks = validated.skillId === "neuro.spike_raster" ? spikeAcceptedMarks : topologyMarks > 0 ? topologyMarks : binnedMarks;
  if (preflightMarks > markLimit) {
    return {
      ok: false,
      errors: [
        makeError({
          code: "RESOURCE_MARKS_EXCEEDED",
          stage: "budget",
          instancePath: "/data",
          skillId: validated.skillId,
          message: `the request requires ${preflightMarks} visible data marks, over the active limit of ${markLimit}. Cortexel refused it before allocating render geometry.`,
          limit: { name: "visibleMarks", limit: markLimit, observed: preflightMarks }
        })
      ]
    };
  }
  if (universeSize > limits.graphNodes) {
    return {
      ok: false,
      errors: [
        makeError({
          code: "RESOURCE_BUDGET_EXCEEDED",
          stage: "budget",
          instancePath: "/data/nodeUniverse/ids",
          skillId: validated.skillId,
          message: `the declared node universe contains ${universeSize} nodes, over the active limit of ${limits.graphNodes}.`,
          limit: { name: "graphNodes", limit: limits.graphNodes, observed: universeSize }
        })
      ]
    };
  }
  if (connectionCount > limits.graphEdges) {
    return {
      ok: false,
      errors: [
        makeError({
          code: "RESOURCE_BUDGET_EXCEEDED",
          stage: "budget",
          instancePath: "/data/connections",
          skillId: validated.skillId,
          message: `the connection snapshot contains ${connectionCount} rows, over the active limit of ${limits.graphEdges}.`,
          limit: { name: "graphEdges", limit: limits.graphEdges, observed: connectionCount }
        })
      ]
    };
  }
  if (catalog.renderer.id === "figure.matrix") {
    const matrixCells = universeSize * universeSize;
    if (matrixCells > limits.matrixCells) {
      return {
        ok: false,
        errors: [
          makeError({
            code: "RESOURCE_MATRIX_CELLS_EXCEEDED",
            stage: "budget",
            instancePath: "/data/nodeUniverse/ids",
            skillId: validated.skillId,
            message: `the ${universeSize} by ${universeSize} declared matrix exceeds the active limit of ${limits.matrixCells} logical cells. Cortexel preflights the universe product without materializing a dense matrix.`,
            limit: { name: "matrixCells", limit: limits.matrixCells, observed: matrixCells }
          })
        ]
      };
    }
  }
  const observations = countObservations(request.data);
  const longestSeries = maxLeafArrayLength(request.data);
  if (longestSeries > limits.observationsPerSeries) {
    return {
      ok: false,
      errors: [
        makeError({
          code: "RESOURCE_OBSERVATIONS_EXCEEDED",
          stage: "budget",
          instancePath: "/data",
          skillId: validated.skillId,
          message: `a single observation array carries ${longestSeries} entries, over the active per-series limit of ${limits.observationsPerSeries}.`,
          limit: {
            name: "observationsPerSeries",
            limit: limits.observationsPerSeries,
            observed: longestSeries
          }
        })
      ]
    };
  }
  const observationLimit = Math.min(
    catalog.budgets.maxObservations,
    limits.observationsPerRequest
  );
  if (observations > observationLimit) {
    return {
      ok: false,
      errors: [
        {
          code: "RESOURCE_OBSERVATIONS_EXCEEDED",
          severity: "error",
          stage: "budget",
          instancePath: "/data",
          message: `this request carries about ${observations} observations, over the ${observationLimit} active request budget for ${validated.skillId}. Reduce the data or reference it with a DataRef. Cortexel never silently truncates.`,
          limit: { name: "observationsPerRequest", limit: observationLimit, observed: observations }
        }
      ]
    };
  }
  if (validated.skillId === "neuro.correlogram") {
    try {
      const inputs = eventCorrelogramInputs(
        rec(request.data) ?? {},
        rec(request.parameters) ?? {}
      );
      if (inputs) {
        const pairs = countEligibleCorrelogramPairs(
          inputs.ref,
          inputs.tgt,
          inputs.spec,
          inputs.kind,
          limits.pairwiseOperations
        );
        if (pairs > limits.pairwiseOperations) {
          const pairCarrierPath = inputs.kind === "auto" ? "/data/train/eventTimes/values" : "/data";
          return {
            ok: false,
            errors: [
              makeError({
                code: "RESOURCE_PAIRWISE_EXCEEDED",
                stage: "budget",
                instancePath: pairCarrierPath,
                skillId: validated.skillId,
                message: `the eligible correlogram pair count is at least ${limits.pairwiseOperations + 1}, over the active limit of ${limits.pairwiseOperations}. The bounded sorted-window preflight formed no pairs and Cortexel refuses before derivation or geometry allocation.`,
                limit: {
                  name: "pairwiseOperations",
                  limit: limits.pairwiseOperations,
                  observed: pairs
                }
              })
            ]
          };
        }
      }
    } catch (error) {
      return {
        ok: false,
        errors: [makeError({
          code: "INTERNAL_INVARIANT_VIOLATED",
          stage: "internal",
          instancePath: "/data",
          skillId: validated.skillId,
          message: error instanceof Error ? `the validated correlogram could not be closed during pairwise preflight: ${error.message}` : "the validated correlogram could not be closed during pairwise preflight."
        })]
      };
    }
  }
  const forced = forcedDisclosures(validated.skillId, request);
  const makeContext = (_rowsTotal, extraFacts = {}) => {
    const facts = disclosureFacts(request, {
      ...extraFacts,
      budgetProfileId: activeBudget.profile
    });
    const disclosures = deriveDisclosures(facts, catalog.disclosures, forced);
    return {
      sourceRequestDigest: validated.requestDigest,
      width: num(presentation.width) ?? 720,
      height: num(presentation.height) ?? 440,
      themeId: presentation.themeId ?? "light",
      title: presentation.title ?? catalog.title,
      disclosures,
      summary: catalog.accessibility.summaryTemplate.replace(/\{[^}]+\}/g, "\u2026"),
      returnedTableRows: returnedTableLimit
    };
  };
  let compiled;
  try {
    compiled = compile(
      validated,
      makeContext,
      limits.pairwiseOperations,
      returnedTableLimit
    );
  } catch (error) {
    return {
      ok: false,
      errors: [
        makeError({
          code: "INTERNAL_INVARIANT_VIOLATED",
          stage: "internal",
          skillId: validated.skillId,
          message: "a request passed validation but its render-plan compiler could not process it. Cortexel refused to emit a partial or potentially misleading artifact."
        })
      ]
    };
  }
  if ("errors" in compiled) return { ok: false, errors: compiled.errors };
  if ("pending" in compiled) {
    return {
      ok: false,
      errors: [
        makeError({
          code: "RENDER_UNSUPPORTED_SKILL",
          stage: "render",
          skillId: validated.skillId,
          message: `stable skill ${validated.skillId} selected renderer ${compiled.pending}, but its compiler produced no render plan. Cortexel refuses to emit a schema-invalid partial artifact.`
        })
      ]
    };
  }
  if (!derivationOperationIdsAreUnique(
    compiled.derivationOperations.map((operation) => operation.id)
  )) {
    return {
      ok: false,
      errors: [makeError({
        code: "INTERNAL_INVARIANT_VIOLATED",
        stage: "internal",
        skillId: validated.skillId,
        message: "the compiler emitted more than 64 derivation operations, a blank operation id, or a duplicate operation id. Operation identity and order must be unambiguous before an artifact is assembled."
      })]
    };
  }
  if (compiled.plan.panels.some(
    (panel) => !Number.isFinite(panel.x) || !Number.isFinite(panel.y) || !Number.isFinite(panel.width) || !Number.isFinite(panel.height) || panel.width <= 0 || panel.height <= 0 || panel.axes.length > 0 && panel.height < MIN_PLOT_PANEL_HEIGHT
  )) {
    return {
      ok: false,
      errors: [
        makeError({
          code: "RENDER_LAYOUT_UNAVAILABLE",
          stage: "render",
          instancePath: "/presentation",
          skillId: validated.skillId,
          message: `the requested dimensions leave no finite plotting region of at least ${MIN_PLOT_PANEL_HEIGHT} CSS pixels per data panel after axes, legends, and mandatory disclosures are reserved. Increase the height or reduce the panel count.`
        })
      ]
    };
  }
  const context = makeContext(
    compiled.plan.table.rowsTotal,
    compiled.derivedDisclosureFacts
  );
  const disclosureBlocks = context.disclosures.map((disclosure) => ({
    id: disclosure.id,
    severity: disclosure.severity,
    text: disclosure.text
  }));
  const disclosureBoundPlan = {
    ...compiled.plan,
    disclosures: disclosureBlocks,
    table: {
      ...compiled.plan.table,
      metadata: {
        disclosures: disclosureBlocks.map((disclosure) => ({ ...disclosure }))
      }
    }
  };
  const summary = appendDisclosureSummarySuffix(
    disclosureBoundPlan.accessibility.summary,
    disclosureBlocks
  );
  const closure = closePlainRenderPlanForAuthorityV1({
    ...disclosureBoundPlan,
    accessibility: { ...disclosureBoundPlan.accessibility, summary }
  });
  if (closure.tag !== "closed") {
    return {
      ok: false,
      errors: [makeError({
        code: "INTERNAL_INVARIANT_VIOLATED",
        stage: "internal",
        skillId: validated.skillId,
        message: `the compiler did not produce a finite detached plain RenderPlan tree: ${closure.problems.map((problem2) => `${problem2.path || "/"} ${problem2.message}`).join("; ")}`
      })]
    };
  }
  const plan = closure.plan;
  let resourceCounts;
  try {
    resourceCounts = countPlanResources(plan);
  } catch (error) {
    return {
      ok: false,
      errors: [makeError({
        code: "INTERNAL_INVARIANT_VIOLATED",
        stage: "internal",
        skillId: validated.skillId,
        message: error instanceof RenderPlanGeometryError ? `the closed render plan contains invalid direction-bearing geometry in panel ${error.panelId} at ${error.markPath}: ${error.message}. Cortexel refused it rather than silently dropping the mark.` : "the closed render plan could not be validated for exact resource accounting. Cortexel refused it rather than certify an incomplete figure."
      })]
    };
  }
  if (topologyMarks > 0 && resourceCounts.markCount !== topologyMarks) {
    return {
      ok: false,
      errors: [
        makeError({
          code: "INTERNAL_INVARIANT_VIOLATED",
          stage: "internal",
          instancePath: "/data",
          skillId: validated.skillId,
          message: `the topology preflight proved ${topologyMarks} visible data marks, but the closed render plan contains ${resourceCounts.markCount}. Cortexel refused the inconsistent artifact rather than certify incomplete geometry.`
        })
      ]
    };
  }
  if (resourceCounts.markCount > markLimit) {
    return {
      ok: false,
      errors: [
        makeError({
          code: "RESOURCE_MARKS_EXCEEDED",
          stage: "budget",
          instancePath: "/data",
          skillId: validated.skillId,
          message: `the compiled figure requires ${resourceCounts.markCount} visible data marks, over the active limit of ${markLimit}. The requested representation was not silently truncated.`,
          limit: { name: "visibleMarks", limit: markLimit, observed: resourceCounts.markCount }
        })
      ]
    };
  }
  if (resourceCounts.textCount > limits.svgTextNodes) {
    return {
      ok: false,
      errors: [
        makeError({
          code: "RESOURCE_MARKS_EXCEEDED",
          stage: "budget",
          skillId: validated.skillId,
          message: `the compiled figure requires ${resourceCounts.textCount} SVG text nodes, over the active limit of ${limits.svgTextNodes}.`,
          limit: {
            name: "svgTextNodes",
            limit: limits.svgTextNodes,
            observed: resourceCounts.textCount
          }
        })
      ]
    };
  }
  if (plan.table.rowsTotal > returnedTableLimit) {
    return {
      ok: false,
      errors: [
        makeError({
          code: "RESOURCE_COMPACTION_UNAVAILABLE",
          stage: "budget",
          skillId: validated.skillId,
          message: `the complete returned table requires ${plan.table.rowsTotal} rows, over the active limit of ${returnedTableLimit}. Cortexel V1 has no table excerpt, sidecar, or reference mode, so it refuses instead of returning an incomplete table.`,
          limit: {
            name: "returnedTableRows",
            limit: returnedTableLimit,
            observed: plan.table.rowsTotal
          }
        })
      ]
    };
  }
  const contractSchema = catalog.accessibility.tableColumns;
  const contractColumns = contractSchema.map(({ key, header }) => ({ key, header }));
  const columnKeys = plan.table.columns.map((column) => column.key);
  const columnsMatch = plan.table.columns.length === contractColumns.length && plan.table.columns.every((column, index) => column.key === contractColumns[index].key && column.header === contractColumns[index].header);
  const completeReturnedTable = plan.table.policy === "complete_returned" && plan.table.rows.length === plan.table.rowsInline && plan.table.rowsInline === plan.table.rowsTotal;
  const keyIndices = contractSchema.map((column, index) => column.keyPart ? index : -1).filter((index) => index >= 0);
  const rowsWellFormed = plan.table.rows.every((row) => row.length === plan.table.columns.length && row.every((cell, index) => {
    const column = contractSchema[index];
    if (cell === null) return column.nullable;
    if (typeof cell === "number") {
      return Number.isFinite(cell) && (column.cellType === "finite_number" || column.cellType === "finite_number_or_string");
    }
    return typeof cell === "string" && (column.cellType === "string" || column.cellType === "finite_number_or_string");
  }));
  const compositeKeys = plan.table.rows.map((row) => canonicalize(keyIndices.map((index) => row[index])));
  if (!completeReturnedTable || !columnsMatch || new Set(columnKeys).size !== columnKeys.length || keyIndices.length === 0 || !rowsWellFormed || new Set(compositeKeys).size !== compositeKeys.length) {
    return {
      ok: false,
      errors: [
        makeError({
          code: "INTERNAL_INVARIANT_VIOLATED",
          stage: "internal",
          skillId: validated.skillId,
          message: "the compiler violated the closed complete-returned accessibility-table contract: exact ordered unique columns, complete row accounting, source-declared scalar/null domains, row widths, and unique composite row keys are mandatory. Cortexel refused the artifact."
        })
      ]
    };
  }
  const authorityGate = checkOutputAuthorityEmissionV1(validated, plan);
  if (authorityGate.tag !== "passed") {
    return {
      ok: false,
      errors: [makeError({
        code: "INTERNAL_INVARIANT_VIOLATED",
        stage: "internal",
        skillId: validated.skillId,
        message: `the final frozen RenderPlan failed request-bound OutputAuthority translation validation: ${authorityGate.messages.join("; ")}`
      })]
    };
  }
  let report;
  try {
    report = renderSvg(plan, sha256Digest);
  } catch (error) {
    return {
      ok: false,
      errors: [makeError({
        code: "INTERNAL_INVARIANT_VIOLATED",
        stage: "internal",
        skillId: validated.skillId,
        message: error instanceof RenderPlanGeometryError ? `the normative SVG serializer refused invalid direction-bearing geometry in panel ${error.panelId} at ${error.markPath}: ${error.message}. No partial SVG was emitted.` : "the normative SVG serializer could not emit the validated closed plan. No partial SVG was emitted."
      })]
    };
  }
  const outputBytes = utf8ByteLength(report.svg);
  if (outputBytes > limits.svgBytes) {
    return {
      ok: false,
      errors: [
        makeError({
          code: "RESOURCE_OUTPUT_BYTES_EXCEEDED",
          stage: "serialize",
          skillId: validated.skillId,
          message: `the normative SVG is ${outputBytes} UTF-8 bytes, over the active limit of ${limits.svgBytes}. Cortexel refuses the output rather than emitting an unbounded artifact.`,
          limit: { name: "svgBytes", limit: limits.svgBytes, observed: outputBytes }
        })
      ]
    };
  }
  const artifact = assembleArtifact(
    validated,
    context.disclosures,
    plan,
    report,
    summary,
    compiled.derivationOperations
  );
  const artifactStructure = validateArtifactStructure(artifact);
  if (!artifactStructure.ok) return { ok: false, errors: artifactStructure.errors };
  return { ok: true, artifact, svg: report.svg, plan, table: plan.table, disclosures: context.disclosures };
}
function appendDisclosureSummarySuffix(compilerSummaryBody, disclosures) {
  if (disclosures.length === 0) return compilerSummaryBody;
  const count = disclosures.length;
  return `${compilerSummaryBody} ${count} ${count === 1 ? "disclosure applies" : "disclosures apply"}: ${disclosures.map((disclosure) => disclosure.text).join(" ")}`;
}
function buildFigureFromJson(text, options = {}) {
  const outcome = parseAndValidateRequest(text, options);
  return dispatch(outcome);
}
function buildFigure(value, options = {}) {
  const outcome = validateRequestValue(value, options);
  return dispatch(outcome);
}
function dispatch(outcome) {
  if (!outcome.ok) return { ok: false, errors: outcome.errors };
  return buildFigureFromValidated(outcome.request);
}

export {
  buildFigureFromValidated,
  buildFigureFromJson,
  buildFigure
};
//# sourceMappingURL=chunk-PFDVYVHI.js.map